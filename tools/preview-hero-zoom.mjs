/* 一次性腳本：從 index.html 產生 preview/hero-zoom/index.html
   ---------------------------------------------------------------------------
   提案內容：HERO 照片要不要「緩慢地微微放大／縮小」（Ken Burns）。
   定案之後這支腳本連同提案頁一起刪掉（CLAUDE.md 第八節）。

   ⚠ 四件一定要做的事（CLAUDE.md 第八節第 2 點，前人踩過）：
     1. 相對路徑往上兩層 —— 含**多行的 srcset**（上一個 commit 就是漏這個破圖的）
     2. 拿掉 counter.js 與 data-views-self，窄帶數字改用內建的 data-count 示範值
     3. 切換條插在**最後一個** </body> 前面（用 lastIndexOf，註解裡也有那幾個字）
     4. preview/ 進得了 _site/（dist.mjs 的 OPTIONAL 已列，不必再改）

   跑法：node tools/preview-hero-zoom.mjs                                     */

import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..');
const OUT_DIR = join(ROOT, 'preview', 'hero-zoom');

let html = readFileSync(join(ROOT, 'index.html'), 'utf8');

/* ---- 1. 相對路徑 ------------------------------------------------------- */
/* 「assets/」前面不是斜線的才換 —— JSON-LD 裡的 https://fangren.net/assets/…
   與 <link href="/assets/icon-192.png"> 都是絕對路徑，不能動。
   這一條同時吃掉 srcset 那三行（它們各自獨立一行，逐一命中）。 */
html = html.replace(/(^|[^/.])assets\//g, '$1../../assets/');
html = html.replace(/(href=")(posts\/)/g, '$1../../$2');
html = html.replace(/(href=")(site\.webmanifest)/g, '$1../../$2');
html = html.replace(/(href=")\.\/(")/g, '$1../../$2');

/* ---- 2. 計數器 --------------------------------------------------------- */
html = html.replace('<script src="../../assets/counter.js" defer></script>', '');
html = html.replace(
  '<p class="band-views" data-views-self="home">',
  '<p class="band-views">'
);
/* 內建的示範值掛勾（index.html 第 5196 行的註解就是在講這個）。
   ⚠ 這個屬性**絕對不要跟著版型搬回正式站**。 */
html = html.replace(
  '<span class="views-n" aria-hidden="true">0</span>',
  '<span class="views-n" aria-hidden="true" data-count="1024">0</span>'
);

/* ---- 3. 夾住溢出的那一層 ----------------------------------------------- */
/* 2026-08-15 電腦版上線時，index.html 自己已經有 .hero-shot 了
   （absolute inset:0 + overflow:hidden，只包 <img>），提案頁直接沿用。
   在這裡擋一下，免得哪天正式站把它拿掉、提案頁卻悄悄變成溢出。 */
if (!/class="hero-shot"/.test(html)) throw new Error('index.html 少了 .hero-shot');

/* ---- 4. noindex ＋ 推導註解 -------------------------------------------- */
/* ⚠ 要**換掉** SEO 區塊裡那一行，不是另外補一行 —— 兩行同時存在時
   document.querySelector('meta[name=robots]') 讀到的是前面那個 index,follow。
   （build.mjs 不會處理 preview/，所以改在這裡是安全的。） */
{
  const re = /<meta name="robots" content="index[^"]*">/;
  if (!re.test(html)) throw new Error('找不到 SEO 區塊的 robots');
  html = html.replace(re, '<meta name="robots" content="noindex, nofollow, noarchive">');
}

const HEAD = `

<!-- ==========================================================================
     提案：HERO 照片要不要緩慢地微微縮放（2026-08-15）
     ==========================================================================

     起因（使用者）：三宅一生的官網點進去之後，主視覺「微微的放大、緩慢在動」，
     感覺像有一個視覺集中在暗示你往中間的圖片看過去，但力道不強。
     問題是：我們的 HERO 要做**逐漸放大**還是**逐漸縮小**。

     ---- 先講一件事實：isseymiyake.com 現在的第一屏其實沒有這個效果 ----------

     2026-08-15 實際打開量過（1280×720，Chromium）：
       ・#section0 的 .section__background > picture > img
         computed transform 是 none、animation 是 none。
       ・整份樣式表裡 32 組 @keyframes，含 scale 的只有 topscroll／bar1
         兩個（那是頁首的橫線，scaleY 0↔1），**沒有任何一條指到主視覺**。
       ・把 #main 底下 160 個元素的 transform／object-position／width／clip-path
         連續取樣 3 秒，**0 個有變化**。
       ・整頁唯一自己在動的東西是 **#section4** 裡的一支 Vimeo 背景影片
         （player.vimeo.com/video/1215400391，background=1&loop=1，位置 y≈2940）。

     所以那個「微微放大」多半是那支影片自己的鏡頭推進（或是站方換過版）。
     **這不影響提案** —— 想要的知覺效果很清楚，而且用 CSS 一行就能做，
     只是不要以為對面有一組現成的數字可以抄。下面的數字都是自己訂的。

     ---- 放大與縮小的差別，不是方向而是「靜止點」與「第一幀」 ---------------

     一個緩慢的縮放，眼睛讀到的是**運動向量的匯聚點**：
       ・放大（push-in）：畫面裡每一樣東西都往外飄，唯一不動的是原點 ——
         眼睛被留在原點上。這正是使用者說的「不自覺往中間看過去」。
         語意是**靠近、注視、進去**。
       ・縮小（pull-back）：東西往內收、邊緣露出更多，語意是**退開、看全貌**。
         它是「交代環境」的鏡頭，不是「請你看這裡」的鏡頭。

     這一站的 HERO 是**巷口的那棟建築**，收尾句是「到巷口的芳仁　一起想辦法」。
     兩者同向的是**放大**。

     ---- 但這一站有兩個純技術的考量，方向剛好相反 ---------------------------

     ① 解析度。2026-08-15 才修好 srcset 的 sizes（CLAUDE.md 第九節第 15 條）。
        **放大版的結束狀態會再多放大一個幅度**，**縮小版的第一幀才是最糊的那一幀**
        （而那正是 LCP 那一幀）。
     ② 靜止狀態。這一頁 99% 的時間是停著的。縮小版停在 scale 1.0
        ＝ 和現在**逐像素相同**，所有量過的數字（接縫、詩的對比、招牌 79.9%）
        在靜止時原封不動；放大版停在 1.06，那些數字要重量一次。

     取捨因此是：**放大 ＝ 第一眼是對的、久了偏 6%；縮小 ＝ 第一眼偏 6%、久了是對的。**
     使用者選了放大。

     ==========================================================================
     ★ 現況（2026-08-15）：**電腦版與手機版都定案上線了，這一輪在比 iPad**
     ==========================================================================

       電腦　放大 6%　12 秒　(min-width:721px) and (min-aspect-ratio:9/10)
       手機　放大 10%　6 秒　(max-width:720px)
       共用 cubic-bezier(.22,.61,.36,1)、原點 50% 100%、一次就停。

     **iPad 直放（9/10~2/3）與直立螢幕（≤2/3）還沒開。**
     這一頁把三道 @media 全拆掉，讓每一段都吃得到；**切換條的預設值跟著裝置走** ——
     已經上線的兩段直接放定案值（打開就是線上的樣子），iPad 兩段放
     電腦與手機的中間值 **8%／8 秒** 當起點。面板最後一格會告訴你這台落在哪一段。

     ---- iPad 這一輪要看的，其實是「第一屏會有兩件事在動」------------------

     iPad 兩段本來就有一個會動的東西：**「往下滑」的指標會慢速浮動**
     （heroCueBob，translateY 4px／2.4 秒，2026-08-15 定案，
     刻意連 prefers-reduced-motion 都不看，因為「會動」正是它存在的理由）。
     照片再加一個緩慢推進，第一屏就是兩個動作。所以切換條多了一排
     **指標：浮動（現況）／先停住**，把它關掉比一比就知道會不會互相搶。

     ⚠ 手機沒有這個問題（.hero-cue 在 ≤720 是 display:none）；
       電腦版有指標但它不浮動，也沒有這個問題。**只有 iPad 兩段要判斷。**

     ---- 已經量到的（詩的對比是保守值，見下面「量法」）--------------------

     手機（已定 10%）：

         視窗        不動    4%            6%            10%
         320×568     3.52    3.36 (-4.5%)  3.24 (-8.0%)  —
         375×812     3.85    3.54 (-8.1%)  3.49 (-9.4%)  —
         390×844     3.85    3.52 (-8.6%)  3.49 (-9.4%)  3.41 (-11.4%)

     iPad 兩段的**底子比手機好很多** —— 照片相對更高，詩底下多半還是天空：

         視窗                    不動    8%            10%
         834×1194（A 段）        4.93    4.61 (-6.5%)  4.53 (-8.1%)
         744×1133（B 段・mini）  4.95    4.62 (-6.7%)  4.56 (-7.9%)

     ⚠ 這些是保守值（整個文字框都算）。要對照 AA 4.5 得換算回
       2026-08-15 那一輪「只量字身」的數字，方法見下面。

     接縫：原點放下緣時 iPad 兩段是 **+0.12 / +0.13**（手機是 0.00~+0.51）。
     那點差來自**橫向**取樣範圍縮小，不是那一列換了。
     對照組：原點放正中時 6% 就是 +6.42。接縫那一輪在乎的量級是 2.7，
     所以下緣這一側完全安全。

     解析度：全部挑 2000w。iPad 834 DPR2 是 1.62 → 1.75 倍（8%），
     手機 DPR3 1.63 → 1.79 倍（10%）。**不是新問題，是本來就在放大。**

     ---- 原點一定要放在照片下緣，不要放正中 --------------------------------

     transform-origin 決定「哪一列不動」。照片的 object-position 是 50% 100%
     （下緣貼齊），而**照片與窄帶的接縫是四輪定案的成果**（窄帶從 #181614 起跑，
     那個值是照片最後一列的實測中位數，CLAUDE.md 定案表「照片與窄帶的接縫」）。
     把 transform-origin 也放在 50% 100%，那一列就**完全不動**，接縫零風險，
     縮放全部發生在往上的方向（那裡有 46% 的暗漸層蓋著，電腦版本來就看不到細節）。
     面板底下的「接縫」那一格就是在量這件事。

     ⚠ **正式站把 transform-origin 寫在電腦版那道 @media 裡面**，手機吃不到、
       會退回預設的 50% 50%。這一頁自己補了一條無條件的（切換條那段 CSS）。
       **手機定案上線時，那一行要搬出 @media 或再寫一份。**

     ---- 量法（面板底下那四格） --------------------------------------------

     ・全部**現場從 2000w 原檔取樣**算，cover 的裁切、transform、電腦版那層
       46% 的暗漸層都算進去。**兩個數字都跟「不動的時候」對照** ——
       這一頁要回答的是「縮放把數字往哪邊推」，不是絕對值。
     ・⚠ 取樣的來源**不能拿 <img> 自己去畫 canvas**：srcset 用 w 描述子時
       naturalWidth 回的是「除以密度之後」的 CSS 尺寸（實測 1088×719，
       不是 1600×1058），畫出來的解析度被壓掉一半。另開一個 Image 指 2000w。
     ・詩的對比取框裡**第 99 百分位**的亮點對白字 65%。這是**保守值**：
       整個文字框都算，含行距與行尾的空隙，所以會低於那一輪「只量字身」的
       5.0~8.7。看的是它往哪邊動、掉多少百分比。
     ・接縫取照片最後一列的**中位數**不是平均 —— 那一列有路燈與車頭燈，
       幾顆亮點就能把平均拉高 4 個 L*（CLAUDE.md 第九節第 11 條）。
     ・**效能**。一個滿版的圖層連續動十幾秒，正上方就是帶 backdrop-filter 的
       頁首，等於逼它每一幀重採樣。CLAUDE.md 第九節第 13 條已經記過
       「多一個合成層 ＝ 多一個會掉的東西」。面板的 fps 是真的在量，
       **請在手機上開著看最差幀**（電腦上量到 111 fps／最差 21ms）。

     ・**保守值怎麼換算回 AA。** 拿掉幅去乘那一輪「只量字身」的最低值：
       手機那一輪最低 5.00（在 320×568），10% 掉 11.4% → 約 **4.43**，
       **低於 AA 4.5**。使用者在兩個裝置上看過畫面之後仍然選 10%，
       所以這是**知情的取捨**，不是漏掉的。
       ⚠ 但這是換算不是實測 —— 值得回頭照那一輪的方法逐字重量一次。
         真的不夠的話，最省事的解不是把幅度收回來，而是把
         .hero-poem 的 text-shadow 加一層（現在是 0 1px 2px/.5 ＋ 0 0 4px/.28），
         那是局部的、不會動到構圖，2026-08-10「巷口」那一次就是這樣解的。

     ---- 這一頁的預設值：**跟著裝置走** ------------------------------------

       手機（≤720）　　　　　放大 10%／6 秒　　← 已上線，打開就是線上的樣子
       電腦（≥721 且 ≥9/10）放大 6%／12 秒　　← 已上線，同上
       iPad 兩段　　　　　　 放大 8%／8 秒　　　← 提案中，取中間值當起點

     曲線先快後慢、原點下緣、一次就停三段都一樣。
     iPad 可以先比 8% 對 6%／10%，再比 8 秒對 12 秒（攤得更慢 ＝ 更像暗示），
     然後把「指標」那一排關掉一次，看看少了浮動是不是比較不吵。

     ⚠ 這一頁**不看**系統的「減少動態效果」，不然開著那個設定的人（iOS 很多）
       會什麼都看不到。**上線版會看** —— 一張滿版照片的縮放正是那個設定針對的
       東西（位移／視差／縮放），和窄帶那個「原地長大的數字」、
       以及指標的浮動都不同類（那兩件都是刻意不看的）。

     網址參數（正規式一律 [a-z0-9]+）：
       ?z=in|out|off  &amt=2|4|6|8|10  &dur=6|8|12|20|40
       &ease=linear|out|inout  &org=bottom|center  &loop=once|breathe
       &cue=on|off
     ========================================================================== -->
`;
html = html.replace('</head>', HEAD + '</head>');

/* ---- 5. 切換條（一定要插在最後一個 </body> 前面）----------------------- */
const BAR = readFileSync(join(dirname(fileURLToPath(import.meta.url)), 'preview-hero-zoom.part.html'), 'utf8');
const i = html.lastIndexOf('</body>');
if (i < 0) throw new Error('找不到 </body>');
html = html.slice(0, i) + BAR + html.slice(i);

mkdirSync(OUT_DIR, { recursive: true });
writeFileSync(join(OUT_DIR, 'index.html'), html);
console.log('寫入 preview/hero-zoom/index.html（' + (html.length / 1024).toFixed(1) + ' KB）');
