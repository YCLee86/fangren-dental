# SEO／GEO 體檢報告　2026-09-20

評分依據：[RUBRIC.md](RUBRIC.md)　逐頁分數：[`score.mjs`](score.mjs) → [`score.json`](score.json)
量測對象：**`_site/`（上線版，已去註解）** 共 23 頁 —— 首頁 1、科別著陸頁 7、文章 15。
量測時的版本：`8568415b`。

> ## ✅ 2026-09-20 稍晚：**P1-1 與 P1-2 已經做完**（①②）
>
> 使用者看完這份報告：「①②③都做，先開提案頁給我看」，
> 看過提案頁之後定案 **標題層級 Ⓑ、科別那格 Ⓑ**。兩項都已經做進產生器，
> 推導與量測在 [`/history/seo-fix.html`](../../history/seo-fix.html)。
> **畫面一個像素都沒動**（390 與 1280 兩個寬度逐節點比對過）。
>
> **總分 76 → 79.5**，逐頁平均 **73 → 78**：首頁 89（不變）、
> 文章 **78 → 81**、著陸頁 **59 → 68**。
>
> **2026-09-20 再更新：E 組的兩分也拿到了。** 使用者把 **Google 商家檔案**與
> **LINE「門診時間」**改成和網站同一組（網站一個數字都沒動），並確認
> **週六早上沒有診**。E3 2 → 3、E1 2 → 2.5，**總分 79.5 → 81**。
> 還沒收的只剩 LINE「營業時間」與門口的磁鐵板（後者機器讀不到，不影響 SEO）。
>
> **2026-09-20 第三次更新：AI 爬蟲政策與 LCP 做完了。**
> 使用者定案 **乙案（擋訓練、放檢索），但不擋 `Google-Extended`**。
> A5 1 → 3、F1 1 → 3，**總分 81 → 85**。
> ⚠ 這一輪也修掉一個評分器的錯：F1 原本把「`fetchpriority` **或** `preload`」
> 當成兩項各給一分，和 RUBRIC.md 第六節寫的不一樣 —— **量錯的是評分器不是網站**。
>
> **2026-09-20 第四次更新：③ 做完了，D2 也被另一台補掉了 —— 總分 85 → 88。**
> 使用者自己查好六組來源給我（而且**帶了四件事實訂正**，不只是連結），
> 六篇補完，16 篇裡 8 篇有權威引用。**B4 0.5 → 3**
> （沒給滿是因為另外 8 篇仍然零引用）。
> 同一天另一台把**文章 `about` 的 Wikidata `sameAs`** 做完了
> （56 個實體填了 50），**D2 3.5 → 4**。
> 四件訂正與四個「日後會再踩」的坑寫進 DECISIONS.md，最要緊的一條是
> **特管辦法管的是美容醫學手術、不是牙科**。
>
> ~~③（外部權威引用）**版位定了但網址還沒補**~~ —— 這個容器連不到那些網站，
> 使用者會自己給連結。第三節 P2-1 那張表就是要給他的清單。
> 下面第一、二、三節的數字**保留體檢當下的原值**，不要改掉 ——
> 那是「改之前長什麼樣」的紀錄。

> ⚠ 量的是 repo 產出的上線版，**不是 fangren.net 上正在跑的那一版**。
> 容器連不出去，線上版本要自己開 https://fangren.net/version.txt 對 commit 編號
> （CLAUDE.md 第二節）。

---

## 一、總分

# **76 / 100**

| 組別 | 得分 | 滿分 | 一句話 |
| --- | --- | --- | --- |
| 組別 | 體檢當下 | ①②做完後 | 滿分 | 一句話 |
| --- | --- | --- | --- | --- |
| A　技術基礎與可檢索性 | 18 | **20** | 20 | ✅ AI 爬蟲政策定案，滿分 |
| B　內容品質與 E-E-A-T | 15 | **18.5** | 22 | 六篇補上權威引用（16 篇裡 8 篇有）；✅ B5 滿分（文章頁印出最後更新）。剩 B3／B6 |
| C　頁面結構與 AI 可擷取性 | 14.5 | **17** | 20 | ✅ C3 斷句、C5 補表格、C4 三科補回應句（三項都滿分）。**只剩 C2** |
| D　結構化資料與實體 | 15.5 | **17.5** | 18 | ① 麵包屑 ＋ 另一台補的 Wikidata sameAs |
| E　在地 SEO | 9 | **10.5** | 12 | ✅ 站外的看診時間收斂了（E1 2→2.5、E3 2→3）|
| F　體驗與效能 | 4 | **6** | 8 | ✅ LCP 提示做完。剩下的是那 175KB 內嵌 CSS/JS |
| **總計** | **76** | **89.5** | **100** | |

> ⚠ **訂正（2026-09-20）**：這張表先前有兩處加總寫錯 —— E 組寫成 11.5（實際 10.5）、
> C 組在 C3／C5 做完之前寫成 16.5（實際 15.5）。兩處相抵，**總分一直都是對的**，
> 但分組數字現在才是真的。逐項：
> A 4+3+3+4+3+3＝20／B 5+3+2.5+3+3+2＝18.5／C 3+2+3+3+3+3＝17（C4 那一輪之後）／
> D 5+4+3+2.5+3＝17.5／E 2.5+2+3+3＝10.5／F 3+2+1＝6。
>
> ⚠ **2026-09-20 再更新**：B5（新鮮度）2 → **3 滿分**，B 組 17.5 → 18.5，
> 總分 88 → **89**。做法是十六篇的 HERO 圖下面各印一行「最後更新：<日期>」，
> 和 JSON-LD 的 `dateModified` 同一個來源、逐篇比對過。
> **首頁卡與延伸閱讀卡沒有動**（使用者指定「首頁圖卡不用」）。
> 推導與四個落選的位置在 `/history/b5-updated.html`。
>
> ⚠ **2026-09-20 第三次更新：C4（Q&A／重點整理）2.5 → 3 滿分**，C 組 16.5 → **17**，
> 總分 89 → **89.5**。做法是替一般牙科、顯微根管、植牙假牙三科各補一句回應句，
> 那一塊因此從 `<ul>` 變成問答的 `<dl>`（三個 `<dt>` 共用一個 `<dd>`）。
> 著陸頁 general 71 → **75**、endo 69 → **73**、prosth 69 → **73**，著陸頁平均 72 → **74**。
> ⚠⚠ 這一項的代價不在分數在版面：兩行的回應讓介紹整塊 **+60px**，
> 390×700 上超出一屏 19~23px（一行的只 +32px，收得進去）——
> **使用者看過數字之後三科都挑了兩行版**，同兒牙那一次「不要管版面的限制」。
> ⚠⚠ 它同時**推翻了一條已定案的文案規則**（2026-08-19「lead 與 stance 一科只挑一種」），
> 那條規則現在怎麼寫、以及三句的全文在 `/history/topic-stance.html`
> 與 COPY.md 第九之二十一節。

**逐頁平均 73 分**：首頁 89、文章 78（73–82）、**著陸頁 59（55–60）**。

> 落差全部集中在同一個地方：**七個科別著陸頁**。它們是 2026-08-21 上線的
> SEO 專用頁，但目前是三組裡分數最低的。詳見第三節。

### 逐頁分數

| 頁面 | 分數 | 主要扣分 |
| --- | --- | --- |
| `/` | **89** | 頁面重量 261KB |
| `/topics/general/`、`/kids/` | **60** | 標題順序、無 Q&A、首段不成段、重量、LCP |
| `/topics/endo/`、`/ortho/`、`/perio/`、`/prosth/`、`/surg/` | **55–58** | 同上，另加內容深度 |
| `/posts/aligner-simulation/`、`/kids-first-visit/` | **82** | 麵包屑、LCP |
| `/posts/bass-brushing/`、`/crown-materials/`、`/orthodontics/`、`/wisdom-tooth/` | **80** | 無外部引用、麵包屑、LCP |
| `/posts/gum-bleeding/`、`/perio-laser/`、`/regular-checkup/` | **78** | 同上 |
| `/posts/bioceramic/`、`/kids-sedation/`、`/missing-tooth/` | **76** | 同上 |
| `/posts/kids-crown/` | **75** | 同上 ＋ 無表格 |
| `/posts/kids-arch-expansion/`、`/three-month-recall/` | **73** | 同上 ＋ 內容偏薄／無表格 |

---

## 二、先講**已經做對、不要動**的事

這些在同業裡是少見的，審這一輪的時候差點被我當成「還可以再加強」而動到手：

1. **結構化資料是全站最強的一塊（D 組 15.5/18）。** `@graph` 串 `@id`、
   看診時間與醫師名冊從畫面讀回來不重抄、`hasCredential` 只收部定專科、
   `og:image:width/height` 從 JPEG 檔頭算而不是寫死 —— 這些都做對了。
2. **⚠ 沒有加 `FAQPage`／`HowTo`／自評 `Review`，這是對的，不要因為「GEO 要結構化」而加回去。**
   Google 2026-05-07 起停止顯示 FAQ 複合式結果、8 月移除 API 資料；HowTo 2023 就下架；
   自評星等是明文禁止。這一站已經在 CLAUDE.md 第十節寫明，**這份報告完全同意**。
3. **文章的 answer-first 結構本來就很好。** 全站 95 個 h2 的第一段，
   **中位數 69 字、78% 落在 40–120 字的可擷取區間、超過 200 字的只有 1 段**。
   這正是 2026 被引用率最高的寫法，而且是自然寫出來的不是套出來的。
4. **15 篇文章篇篇有 `<dl class="keypoints">` 的問答式重點整理。** 這是 GEO 的理想形狀
   （問句 → 直答，段落自足），而且**不需要**配 `FAQPage` 標記也照樣被抓。
5. **圖片零缺失**：23 頁共 100+ 張圖，`alt` 覆蓋 100%、`width`/`height` 覆蓋 100%、
   WebP `<picture>` 就位、`alt` 是真的在描述畫面而不是塞關鍵字。
6. **在地實證內容（E4 滿分）**：停車格現地清查、門口地圖、周邊導航 ——
   這是 AI 回答在地問題時最會抓的那一種內容，而且抄不走。
7. **靜態 HTML、一頁一個網址、關掉 JS 內容還在**（CLAUDE.md 第一節第 1、2 條）。
   2026 年 AI 爬蟲多半**不執行 JavaScript**，這個決定現在的價值比當初訂的時候更高。

---

## 三、要修的（照影響排序）

### ✅ ~~🔴 P1-1~~　七個科別著陸頁與 15 篇文章之間，**連結是單向的** —— 2026-09-20 已修

**現況**（`links.mjs` 量測）：

- 著陸頁 → 文章：有（每頁 1–4 篇）
- **文章 → 著陸頁：15 篇全部是零。** 文章的內部連結只指向 `/`、`/#topics`、
  `/#doctors`、`/#clinic` 與另外 5 篇文章。
- 文章的 `BreadcrumbList` 第 2 層指向 **`https://fangren.net/#topics`（首頁錨點）**，
  不是 `/topics/perio/`。
- 畫面上那一行麵包屑裡，科別是 `<span class="post-tag" data-spec="perio">`，
  **是 span 不是連結**。

**為什麼要緊**：`data-spec="perio"` 已經在那個 span 上了，也就是**對應關係早就知道**，
只是沒接起來。結果是七個專為 SEO 做的著陸頁，各只拿得到 8 個站內入口
（首頁 1 ＋ 七頁互連 7），15 篇文章一個都沒有回指。對爬蟲與 AI 來說，
這等於在說「這一篇的上一層是首頁」，七個著陸頁的 hub 地位建不起來。

**怎麼修**（兩處，都在產生器裡）：

1. `tools/build.mjs`：`BreadcrumbList` 第 2 層的 `item` 從 `https://fangren.net/#topics`
   改成 `https://fangren.net/topics/${SPEC[p.tag]}/`。`SPEC` 那張表就在同一支的第 513 行。
2. 文章模板的 `<p class="crumbs">`：把 `<span class="post-tag" data-spec="…">` 換成
   `<a class="post-tag" href="/topics/<spec>/" data-spec="…">`。
   ⚠⚠ **那一段確認在 `<main>` 裡面**（量過：`<main>` 在第 10870 字元、`crumbs` 在 10961），
   所以 **15 篇的內容雜湊會一起變**，`updated` 會全部跳成當天。
   照 CLAUDE.md 第五節的判準，這一種是「對讀者不算內容變了」（只是把科別接成連結），
   → **要照那個程序把 `build-manifest.json` 裡那 15 筆的日期改回去，再跑一次 build。**

**預估**：D3 從 1.4 → 3.0（15 篇各 +2 分），著陸頁的站內入口從 8 → 10～13。

---

### ✅ ~~🔴 P1-2~~　七個著陸頁：**h1 排在 h2 後面，而且唯一的獨有內容沒有任何標題** —— 2026-09-20 已修

**現況**：著陸頁是 `index.html` 的快照，所以文件順序是

```
h2 主題與科別      ← 首頁那一節的標題，先出現
  h1 牙周治療      ← 這一頁真正的標題，在它後面
  h3 …文章卡…
h2 醫師介紹
h2 診所資訊
```

而那一頁**唯一不與首頁重複的內容**（`.tp-intro`：三個病人的問句、一句回應、
五步流程、一句收尾）**整塊沒有任何 h2／h3**，全部是 `<p class="tp-case">`、
`<p class="tp-step"><b>檢查</b>…`。

**量出來的結果**：

| | 與首頁的文字重疊率 | 獨有的四字詞 |
| --- | --- | --- |
| `/topics/general/` | 77.0% | 44 |
| `/topics/kids/` | 76.8% | 42 |
| `/topics/prosth/` | 75.4% | 35 |
| `/topics/ortho/` | 75.2% | 35 |
| `/topics/surg/` | 71.6% | 31 |
| `/topics/perio/` | 69.7% | 43 |
| `/topics/endo/` | 68.1% | 38 |

七頁各有 **68–77% 的文字和首頁一樣**，獨有的部分只有 200–350 個中文字，
而且沒有標題可以讓檢索系統把它切成一個段落。

**為什麼要緊**：段落級檢索靠標題定界。沒有標題的 `<p>` 群，被切成 chunk 之後
夾在「主題與科別」和「醫師介紹」中間，很難被判定成「這一段在回答牙周治療是什麼」。
這七頁最有價值的東西（**病人真的會問的三個問題** ＋ **一條從檢查到維護的流程**）
正好是 AI 最愛引用的兩種形狀，現在卻是最抓不到的那一塊。

**怎麼修**（全部在 `tools/topics.mjs` 的 `introBlock()`，改完跑 `node tools/topics.mjs`）：

1. **讓 h1 排到最前面**，或把首頁那個「主題與科別」的 h2 在著陸頁降成
   `<p class="sec-label">`／`<h2 class="sr-only">`（它在著陸頁是導覽列不是章節）。
2. **三句 `.tp-case` 改成 `<dl>`**：`<dt>` 是病人的問句、`<dd>` 是那句回應。
   現在的「三個問句 ＋ 一句總回應」拆成三組問答，是這七頁最划算的一改。
3. **五個 `.tp-step` 改成 `<ol>`**，`<b>檢查</b>` 改成 `<li><strong>檢查</strong>…`。
   有順序的東西用有順序的標記。
4. `.tp-first-h`（「從檢查到維護，大概會這樣走」）**改成真的 `<h2>`**，
   它本來就是一個標題，只是現在是 `<p>`。
5. 順手補 `twitter:card`（七頁都缺，`seoBlock()` 裡加一行）。

⚠ 文字一個字都不用改 —— **這一項改的是標記不是文案**，所以不必回 COPY.md 重審。
⚠ 但第 1 項會動到版型，照 CLAUDE.md 第八節的規矩，這一項該先開一頁提案給使用者看。

**預估**：著陸頁 59 → 約 75。

---

### 🟠 P2-1（進行中）　**15 篇文章裡，14 篇沒有引用任何外部權威來源**

**現況**：全站外部連結掃過，只有 `/posts/aligner-simulation/` 引了
`pubmed.ncbi.nlm.nih.gov`、`www.ncbi.nlm.nih.gov`、`www.ajodo.org`。
其餘 14 篇的外部連結只有 Google 地圖。B4 平均 **0.18 / 4**，是全表最低的一項。

**為什麼要緊**：這是 YMYL 的信任證據，也是 2026 年 AI 引用率差距最大的單一變因之一。
而且這一站有一個**別人沒有的優勢**：它的醫師名冊有六個部定專科，
文章引到學會指引或健保給付規定時，**機構身分是接得上的**。

**怎麼修**：不是每篇都塞五個連結。**一篇挑 1–3 個真的支撐關鍵主張的來源**，
優先順序：健保署／衛福部的給付與法規 → 台灣各專科學會的指引 → PubMed／Cochrane。
最該補的六篇（主張最需要證據的）：

| 文章 | 該引的那一類 |
| --- | --- |
| `regular-checkup` | 健保署的洗牙給付規定（半年一次那條） |
| `three-month-recall` | 健保署的三個月一次適用對象 |
| `kids-first-visit` | 兒童牙科學會的首次就診年齡、氟化物建議 |
| `kids-sedation` | 麻醉醫學會／兒牙學會的鎮靜分級與監測標準 |
| `gum-bleeding`、`perio-laser` | 牙周病學會的分期與治療流程 |
| `wisdom-tooth` | 保險給付與術後照護的官方說法 |

⚠ `aligner-simulation` 已經有現成的寫法可以照抄（`8568415b` 那一輪剛做過）：
**行內連結、描述性的錨點文字**，不是文末列一排參考文獻。

> **⚠⚠ 2026-09-20：網址要使用者自己給。**
> 這個容器連不到 `nhi.gov.tw`、`cda.org.tw`、`taop.org.tw` 與 `pubmed`
> （`curl` 回 000、WebFetch 回 `EGRESS_BLOCKED`），**沒查證過的網址不可以寫進
> 醫療網站** —— 猜一個等於把讀者送到一個不知道是什麼的地方。
> 處理方式同第十節那個 Wikidata `sameAs`。上面那張表就是要給他的清單。

⚠ **不要引到 Wikipedia 或內容農場**，那會反效果。

**預估**：B4 從 0.5 → 3，總分 +2.5。

---

### ✅ ~~🟠 P2-2~~　robots.txt 對 AI 爬蟲**沒有任何立場** —— 2026-09-20 已定案

使用者選 **乙（擋訓練、放檢索），但不擋 `Google-Extended`**。產生器在
`tools/build.mjs`（robots.txt 每次 build 整個重寫，手改會被蓋掉）。

| 誰 | 結果 |
| --- | --- |
| `Googlebot`（＝ AI Overviews／AI Mode 吃的索引） | 不變，照舊 |
| `OAI-SearchBot`／`ChatGPT-User`／`Claude-SearchBot`／`Claude-User`／`PerplexityBot` | **放行**（和舊版逐格相同，`/preview/`、`/history/` 一樣擋著） |
| `GPTBot`、`ClaudeBot` | **全站擋掉** |
| `Google-Extended` | **刻意不擋** —— 擋了 Gemini 的引用（grounding）會一起沒有 |

⚠⚠ **寫法上有一個會出事的地方：robots.txt 是「最明確的那一組全拿」。**
一旦替某支機器人開了自己的群組，它就**完全不看 `*` 那一組**了 ——
所以檢索型那一組必須把 `Disallow: /history/`、`/preview/`、`/version.txt`
**再寫一次**，否則那兩個目錄對它們就變成開放的。
驗收方式：照 RFC 9309 的「最長匹配勝出」自己解析一次，新舊版逐格比對
（不要用 Python 的 `urllib.robotparser` —— 它是「第一條命中就算」，
會把 `Allow: /` 判成贏過 `Disallow: /history/`，新舊版都會誤報成開放）。

⚠ 這擋不住不守規矩的爬蟲，原理上也做不到；而且**這個 repo 是 public 的**，
同一份 HTML 在 GitHub 上誰都讀得到。**不要再往這個方向加碼**
（同 `assets/img-guard.js` 檔頭那一條）。

### 🟠 P2-3　首頁與七個著陸頁，**每一頁都夾帶 175KB 的內嵌 CSS 與 JS**

**現況**（上線版，已去註解）：

| | HTML | 其中內嵌 CSS | 其中內嵌 JS |
| --- | --- | --- | --- |
| `/` | 261KB | 73.5KB | 116.3KB |
| `/topics/<spec>/` ×7 | 209–217KB | 73.8KB | 101KB |
| `/posts/<slug>/` ×15 | 23–41KB | 0 | 5–7KB |

文章頁很漂亮（23–41KB）。問題在首頁與七個著陸頁：**同一份 73.8KB 的 CSS 與
101KB 的 JS 被複製了八次**，而且因為是內嵌的，**跨頁完全不能快取** ——
使用者從首頁點進 `/topics/perio/`，那 175KB 要再下載一次。

**為什麼要緊**：直接打在 LCP 上（主執行緒要先解析完才畫得出東西），
也吃掉爬蟲的抓取預算。這是 F 組只拿 4/8 的主因。

**怎麼修**（由小到大，建議只做第 1 項）：

1. **把 101KB 的內嵌 JS 抽成 `assets/app.js`**（`defer`）。八頁共用一個檔，
   第二頁起是快取命中。⚠ 那裡面有一段是**開頁就要跑**的（著陸頁捲到 `#topics`
   的那段、夜間模式的 `NIGHT` 區塊），**那兩段要留在內嵌**，不能一起搬。
2. CSS 同理，但風險高很多 —— 這一站的樣式表內嵌是刻意的（避免第二個往返），
   要動之前先量「抽出去之後 LCP 是變好還是變壞」。**不確定就不要動。**
3. 不要為了減重去刪內容。

**預估**：F3 從 1 → 2，總分 +1。

---

### ✅ ~~🟡 P3-1~~　全站只有首頁的首屏圖有 `fetchpriority="high"` —— 2026-09-20 已修

⚠⚠ **量過之後才發現兩種頁面要治的不是同一件事**（原本這一節寫的是「22 頁都加
`fetchpriority` 就好」，那是錯的）。Chromium 390×844、DPR 2、模擬 4G 量 LCP：

| | LCP 元素 | 改之前 | 改之後 |
| --- | --- | --- | --- |
| 首頁 | `<span>` HERO 詩的一行（**不是圖**） | 2232ms | 2192ms（沒動） |
| `/posts/gum-bleeding/` | HERO `<img>` | 1144ms | 1124ms（**沒動**） |
| `/posts/kids-sedation/` | HERO `<img>` | 1412ms | 1416ms（**沒動**） |
| `/topics/perio/` | **`<div class="tp-intro">`** | 3264ms | **2440ms** |
| `/topics/kids/` | **`<div class="tp-intro">`** | 3800ms | **2542ms** |

**著陸頁的 LCP 根本不是圖片元素**，是 `.tp-intro` 那一塊 —— 而它慢是被
`.tp-intro::before` 那張線稿的**背景圖**拖住的（`lineart-perio.webp`
**3223ms** 才載完）。背景圖要等 74KB 的內嵌樣式表解析完 ＋ 版面算出來才被發現，
而這一頁的 HTML 有 213KB。**`fetchpriority` 對它完全沒有用，那是給 `<img>` 的。**

做法：
- **15 篇文章** → HERO `<img>` 加 `fetchpriority="high"`。
  ⚠ **實測 LCP 一點都沒動**（那張圖 858ms 就載完了，瓶頸在算版面不在下載）。
  留著是因為它語意上是對的、成本是零，在真實的慢速網路與冷快取下才會發揮。
  **不要因為這次沒量到差別就以為它有效，也不要據此去別的地方亂加。**
- **7 個著陸頁** → 在 `<head>` 最前面 `preload` 那張線稿，
  **而且跟著主題挑對那一張**（夜間是 `-night`）。線稿 3223ms → **749ms**。
  ⚠ 寫死白天那張的話，開夜間模式的人會白下載 42~63KB，真正要用的那張照樣晚到。
  接在 `NIGHT` 那支腳本後面做，那時 `dataset.theme` 已經定了。
  ⚠ 帶 `type="image/webp"`，不支援的瀏覽器會跳過這個 preload（CSS 那邊是
  `image-set`，會自己退回 PNG）。實測白天與夜間**各只下載一張**，沒有重複下載。

**⚠ 著陸頁仍在 2.4~2.5 秒**，剛好壓在門檻上。再往下要動的是 P2-3 那 175KB
內嵌 CSS/JS，那一項的風險仍然不成比例。

⚠ HERO 在 `<main>` 裡，15 篇的雜湊又動了一次 —— 照第五節還原日期（`fetchpriority`
是效能提示，對讀者不算內容變了）。

### 🟡 P3-2　兩個 meta description 太長、一個太短

Google 在中文是按像素寬截斷，全形字約佔兩倍，手機上大約 50–60 個中文字就到底。

| 頁面 | 現況 | |
| --- | --- | --- |
| `/posts/bioceramic/` | **104 字** | 後面約四成看不到 |
| `/posts/aligner-simulation/` | **103 字** | 同上 |
| `/topics/kids/` | **39 字** | 七頁裡最短，其餘 59–76，浪費了版位 |

⚠ 前兩篇的 description 是從 `post-meta` 來的，**改它會動到內容雜湊**
（CLAUDE.md 第五節）；`/topics/kids/` 的改 `tools/topic-copy.mjs`。

### 🟡 P3-3　三篇文章沒有表格

`kids-arch-expansion`、`kids-crown`、`three-month-recall`。
前兩篇都在比較「什麼時候做／哪一種」，本來就適合表格；
`three-month-recall` 在講「哪些人適用」，也是。
有表格的頁被 AI 引用約 **4.2 倍**，這三篇是現成的。
`kids-arch-expansion` 另外是全站最薄的一篇（839 字，其餘中位數約 1700）。

### 🟡 P3-4　幾個標題抓出來看不懂在講什麼

C2 平均 2.3/4。這一項要小心 —— **這一站的標題風格是刻意的，COPY.md 管著**，
不能為了 SEO 把它改成名錄式。但有幾個是**純指涉、抓出來完全沒有資訊**的：

| 文章 | 現在 | 抓出來的問題 |
| --- | --- | --- |
| `gum-bleeding` | 「那條關鍵的分界線」 | 什麼的分界線？ |
| `kids-sedation` | 「兩條不一樣的路」、「那一題」 | 哪兩條？哪一題？ |
| `bioceramic` | 「它多做了什麼」 | 「它」是誰？ |

**建議只改這幾個，而且是加字不是換句**：
「那條關鍵的分界線」→「牙齦炎與牙周炎：那條關鍵的分界線」。
語氣留著，實體補上去。⚠ **動到內文就會動內容雜湊**，而且這是文案，
照 TEAM.md 要先過作家與 COPY.md，不是工程面自己決定的。

### ✅ ~~🟡 P3-5~~　營業時間站內外三套 —— 2026-09-20 已收斂

**原本**是三組數字（網站／門口磁鐵板＝LINE 門診時間／LINE 營業時間），
Google 拿商家檔案、網站 JSON-LD 與 LINE 交叉比對時是一個不一致訊號。

**現在**：使用者把 **Google 商家檔案**與 **LINE「門診時間」**改成和網站同一組
（08:45–11:30／13:45–16:30／17:45–20:00），**網站一個數字都沒有動**。
同時確認**週六早上沒有診**（這件從 2026-08-13 掛著）。

剩下兩個都不影響搜尋：

| | 還是舊的 | 要緊嗎 |
| --- | --- | --- |
| LINE「營業時間」 | 08:35–12:05 | LINE 資料頁上的另一個欄位，爬得到，有空再收 |
| 門口的磁鐵板 | 08:45–12:00／13:45–16:40／17:45–20:30 | **實體牌子，機器讀不到，完全不影響 SEO** |

**E1 2 → 2.5、E3 2 → 3。**
⚠ E1 沒有給滿是因為 LINE「營業時間」那一欄還在，那一欄是爬得到的。

---

## 四、**不要做**的事（這一輪特地查過）

1. **不要做 llms.txt。** 理由見 P2-2。
2. **不要加 `FAQPage`。** 就算現在每篇都有現成的問答區塊，也不要加 —— 2026-05-07 已停用。
3. **不要把作者署名或「醫療審閱」加回頁面上。** 2026-08-02 定案移除，
   而且 `MedicalWebPage` 的 `lastReviewed` 宣告一個沒有人做的審閱等於造假
   （CLAUDE.md 第十節第 2 條）。**要補 E-E-A-T 請走 P2-1 的外部引用**，那是真的。
4. **不要為了 GEO 去改 HERO 那首詩或品牌文字。** Google 2026-05 那份指南講得最清楚：
   沒有額外要求、不需要特別的優化。這一站的聲音是資產不是障礙。
5. **不要動科別著陸頁的文案。** P1-2 要改的全部是標記。
6. **不要為了減重刪內容或刪註解機制。** 上線版已經去掉 37% 的註解了。

---

## 五、要使用者決定或提供的

| | 事 | 誰能做 |
| --- | --- | --- |
| 1 | ~~**AI 爬蟲政策選甲還是乙**~~ ✅ 2026-09-20 定案：乙，不含 `Google-Extended` | 只有使用者 |
| 2 | **P1-2 要不要先開提案頁**（會動到著陸頁版型） | 使用者 |
| 3 | **Google 商家檔案上的營業時間是哪一組**、要不要收斂 LINE 那兩組（P3-5） | 只有使用者（後台） |
| 4 | ~~文章 `about` 的 **Wikidata `sameAs`**~~ ✅ 2026-09-20 另一台做完（43 個不重複實體，38 個找到、5 個查不到就留白） | — |
| 5 | 要不要做**一位醫師一頁**的醫師介紹頁（B3 的天花板在那裡，目前 2.5/4） | 使用者 |
| 6 | 首頁 h1 寫「在地四十年」，1983 → 2026 已經是 43 年。`<title>` 當初就是為了這個把它拿掉的，h1 沒跟著改 | 使用者（文案） |

---

## 六、如果只做三件（①② 已完成，2026-09-20）

1. **P1-1 麵包屑與文章回指著陸頁** —— 兩個產生器各改幾行，15 篇一次到位，
   七個著陸頁才真的變成 hub。
2. **P1-2 著陸頁的標記結構**（h1 順序、問答改 `<dl>`、流程改 `<ol>`）——
   文字一個字不用改，七頁從 59 拉到約 75。
3. **P2-1 六篇補外部權威來源** —— 全表最低的一項，而且是 YMYL 唯一真正缺的證據。

①② 做完實測 **76 → 79.5**（逐頁平均 73 → 78）。③ 補完預估再 **+2.5**。
⚠ 原本估「三件做完約 85」偏樂觀 —— ② 把著陸頁從 59 拉到 68 而不是 75，
因為 C3（answer-first 段長）與 B6（內容深度）不是標記能解的，要真的多寫字。

---

## 七、重跑這份報告

```bash
node tools/dist.mjs
node drafts/seo-geo-audit/score.mjs
```

⚠ 一定要先跑 `dist.mjs` —— 直接量 repo 根目錄的 HTML 會把註解算進字數與重量，
這一站的註解寫得很長，量出來會差 37%。
