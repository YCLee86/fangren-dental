# 門口的停車告示（2026-08-23）

診所門口那一段被劃設紅線，以前多為柔性勸導、近來常有直接開單。
這是要貼在門口的 **A4 直式**告示：四點提醒 → 簡易地圖（現在位置 ＋ 三個停車場）
→ 三顆 QR（一場一顆，掃了直接開那一場的 Google 地圖）。

> ⚠⚠ **這一份刻意不放在診所網站上**（使用者 2026-08-23：「這個專案內容不需要
> 放在診所網站上，都刪除，但 Claude 以後要做還找得到就好」）。
> 所以它住在 `drafts/`：`tools/dist.mjs` 的 `ALWAYS`／`OPTIONAL` 都沒有 drafts，
> `tools/build.mjs` 也掃不到，**進不了 `_site/`、fangren.net 上找不到**。
> 原本的提案頁 `preview/clinic-map-door/` 已刪除。

## 怎麼重做

```bash
node drafts/door-notice/gen.mjs      # → drafts/door-notice/preview.html
node drafts/door-notice/pdf2.cjs     # → 兩份 A4 PDF（原色底／白底）
```

產出的 `preview.html` 與 PDF **沒有進版控**（可以重跑，不必留）。
`preview.html` 直接用瀏覽器打開就好，底下有切換條：
`?orient=w|e|n|s`（朝上的方向，w ＝ 正對診所）`?size=p|l`（A4 直／橫）
`?lot=blue|grey` `?you=on|off` `?ns=a|b|c|d`（指北針大小）
`?nw=a|b|c|d`（指北針胖瘦）`?qr=on|off`

## 檔案

| | |
| --- | --- |
| `gen.mjs` | 產生器。從 `index.html` 抽出地圖的 markup／CSS／JS，包一層旋轉，再接上底下三份 |
| `body.html` | 告示自己的 markup（四點提醒、三張停車場卡）＋ 切換條 |
| `style.css` | 告示自己的樣式（A4 直／橫兩套） |
| `script.js` | 旋轉、街名重排、路線圓點、指北針、QR 開關 |
| `pdf.cjs` / `pdf2.cjs` | 輸出 PDF（`pdf2` 一次出原色底與白底兩份） |

QR 的碼由 **`tools/qr.mjs`**（零依賴、留在 tools/）產生，向量、印多大都不糊。

## 定案的值（使用者一項一項選過的）

- 方向 **正對診所（西在上）**、紙張 **A4 直**
- 指北針：大小 36、胖瘦 1:1.5、「北」字級 ＝ 針長 × 0.52、位置在地圖上方帶子的右側
- 三顆 QR：**28.5mm、中心相距 65.2mm** —— 「相機會不會對錯」的治法是**拉開距離**，
  不是把碼做小
- 停車場順序 **P1 190m／P2 140m／P3 50m**，**刻意不是照距離排**：
  最近的 P3（斗六永樂站）很不好進出，列出來是為了完整、不是推薦

## 幾件不要再走回去的

- **抬頭與診所名稱都拿掉了**：紙貼在自家門口，看的人就站在門口。
- **「車主在拖吊車來之前出現就不會被拖吊」不寫** —— 那等於教人賭一把。
- 「這裡平常車不多，**看起來停一下沒關係**」被退回 ——
  那是替讀者把僥倖的念頭講出來，等於幫他找了個理由。**只擺事實，不要配旁白。**
- 第二句要寫成**現在的常態**（「近來常有直接開單情形」），
  不要寫成單一事件（「前幾天已經有人被開單」），嚇阻力差很多。

## 踩過、下次還會踩的

- **輸出 PDF 前要把視窗調成 A4 在 96dpi 的像素（794×1123）再切 print 媒體**，
  並發一次 `resize` —— 版面高度是 JS 現算的（`fit()`），只切媒體的話它用的還是
  螢幕那個視窗的盒子，地圖會比紙高、下緣被 `overflow: hidden` 切掉。
- **量測腳本要先把切換條 `display:none`** —— 它是 fixed 的，
  Playwright 的元素截圖照樣拍得到（直式的紙比較高，QR 帶正好落在它底下）。
- **驗收 QR 不能用眼睛**：把 PDF 轉成 300dpi 點陣，用解碼器真的掃一次。
  驗證用的兩個套件不是專案依賴：`pip install pymupdf opencv-python-headless`

## 周邊路邊停車格的現地清查（2026-09-15 起）

資料在 **[`parking-survey.json`](parking-survey.json)**，那是**唯一的出處** ——
門口告示、LINE／FB 貼文圖要用到路邊停車格，一律讀那一份，不要各自再記一份。

做法：使用者沿路實拍 → 照片裡的店招與 **2024/07 Google 街景**逐張比對定出站位與朝向
（誤差公尺級）→ 放大讀格內白漆的三位數編號。

**永樂街（文化路 → 中華路）已清查完畢：全線只有 5 格。**
（下表是給人看的摘要，**數值以 `parking-survey.json` 為準**）

| 格號 | 側 | 位置 |
| --- | --- | --- |
| 001 | 東 | 耀武羊威（99號）門口，**幾乎正對診所**；北邊接機車格、南邊接紅線 |
| 002 | 東 | 初次燒肉・四五六棋牌社前（001 再往北） |
| 003 | 西 | **大同路口紅線之後的第一格** |
| 004 | 西 | 老磚牆・舊瓦厝那一段 |
| 005 | 西 | 深藍灰色四層樓南邊，**西側最靠近診所的一格** |

- 西側「診所 → 文化路」與東側「診所 → 大同路」**都沒有汽車格**。
- 格子是「一格、紅線、一格」間隔的，不是連成一長排。
- ⚠⚠ **這些全是 2024/07 之後才畫的** —— 同位置的街景一格都沒有，路面也重鋪過。
  所以**街景上查不到路邊停車格是正常的，不代表沒有**；要查只能實拍。
**文化路北側（永樂街路口 → 西，第 ⑨ 段）：2 格 039、040**，就在元大銀行／元大證券
對面那面紅磚牆前，**收費標誌立在旁邊**。
⚠ 牌子上寫「地址：停車格037旁」，現地讀到的卻是 039、040 —— 037、038 可能再往西
一點，或牌子的登記地址只是概略。

**永安路（⑪）：格子最多的一條。**
・**東側**（文化路 → 大同路 那一段）：一整排連著的 **002～010**（004、009 還沒讀到號碼）。
　005・006・007 在全聯 斗六永安店門口。
・**西側**：郵局門口的裝卸貨車專用區；另有 **013**（悟理核心運動工作室前）與
　**012**（福道田餐廳前，大同路以南）。
### 圖：`parking-survey-map.png`

```bash
node drafts/door-notice/parking-map.mjs
```

拿**站上那張簡易地圖本人**（`index.html` 的 `.map-svg`）當底，把停車格畫在路旁。

- ⚠⚠ 街廓、路名、三塊停車場、診所圖釘**一個座標都不在那一支裡** —— 它是開那一頁、
  把格子注進去再截圖，所以**站上的地圖改了這張圖會跟著改**。
- ⚠⚠ **格號的唯一出處是 `parking-survey.json`**，那一支只有「畫在哪裡」；兩邊對不上就
  throw（負向測過）。不擋的話，清查資料改了圖還是舊的，**而畫面完全正常**。
- ⚠ 顏色一個都沒新增：停車格 `--map-park` `#365685`、卸貨區 `--ink-soft` `#5c5f57`。
- ⚠ 畫圖時把站上那三條**路線虛線與三塊牌子拿掉**，並把**永樂街、永安路兩個街名往南挪**
  （原本的位置正好是停車格那幾段）。那只是這張圖的排版，`index.html` 一個字都沒動。
- ⚠ 永安路東側那九格**由北往南 010→002 是推定的**，先後順序還沒現地確認。
- ⚠ 2026-09-15 站上那張地圖**補了一條沒畫到的小巷**（文化路上，平和街口與元大銀行之間，
  只往北通；同一輪平和街改成不穿過文化路、並補上 33 巷的街名）。這張圖已經跟著換了 ——
  **底圖是開 `index.html` 本人截的，所以站上改地圖這裡自動跟上**。
  ⚠⚠ **039／040 在那條小巷與平和街口之間（＝小巷以西）** —— 使用者 2026-09-15 現地指正。
  在那之前我照 040 的地標「靠元大銀行方向」把兩格畫在小巷**東邊**，**那是錯的**，
  日後看到舊圖不要拿它回頭訂正。兩格自己的東西順序沒有變（040 在 039 東邊）。
- ⚠⚠ **圖上有一排沒有號碼的格子**（文化路・平和街口以西，北側一排、南側一格）——
  那是使用者口述的、**照片沒拍到、號碼還沒清查**，畫的數量是示意（他指定
  「在簡易地圖的範圍內呈現足夠畫進去的數量就好」）。守門**只比對有號碼的格子**，
  所以那一排兩邊都跳過；日後走一趟把格號讀回來補進 JSON，圖會跟著對。

・⚠⚠ **網站地圖（診所簡易地圖，文化路～中華路 × 平和街～永安路）範圍內的永安路
　有 002～010（東側）與 013、012（西側）** —— **只有 011 在範圍外**。
　⚠ 2026-09-15 先前一度記成「012／013 也在範圍外、不要畫」，**那是錯的、已經更正**，
　現在那張圖上兩格都有。西側由北往南的順序是：**郵局裝卸貨區 → 013 →（過大同路）→ 012**。
・⚠ 還沒讀到 **004、009**。⚠ 東側那一排哪一端靠文化路還沒確認。
⚠ **郵局門口那一格是「裝卸貨車專用區」不是一般停車格**，見下面那一段。

⚠⚠ **永安路上那幾面「P 免費」招牌都是店家自己的、有消費條件**，不是公有免費停車場：
全聯斗六永安店的**特約停車場**（消費滿額折抵 1 小時）、頂好KTV包廂（24H）、
釣蝦場（**非本店消費者要收費**）。門口告示要用的話得把條件寫清楚，不能只寫「免費」。

**大同路（⑤⑥⑦）：只有 1 格 `001`**，在北側（永樂街路口往西），
利源中藥房・草藥行／張晟明・張清宜土地代書事務所 前。
南側（⑥）與往東到永安路那一段（⑦，兩側）都沒有。

**沒有汽車停車格的路段**（都是使用者現地確認）：平和街33巷整條（⑬）、
文化路南側（⑧，**指平和街口以東那一段**）、文化路兩側往東到永安路（⑩）、
平和街兩側（⑫）、大同路 ⑥⑦。

**⚠ 文化路・平和街口以西（還沒清查）**：使用者 2026-09-15 口述 ——
**北側有一整排連續的停車格、南側 1 格**，都在網站地圖的範圍內，**但照片沒拍到**，
格數與號碼都不知道。⚠ 它和上面那個「文化路南側 0 格」不衝突：那一條講的是
**平和街口以東**（永樂街路口往西約 100 m）那一段。

### ✅ ①～⑬ 段全部走完了（2026-09-15）

診所走得到的範圍內，路邊汽車停車格**只有這些**：

| 路 | 格數 | 備註 |
| --- | --- | --- |
| **永安路** | 一整排 | 東側 002～010（缺 004／009 的號碼）＋ 西側 013、012。⚠ 只有 011 在網站地圖範圍外 |
| **永樂街** | **5** | 001～005，⚠ 收不收費還沒有答案 |
| **文化路　北側** | **2** | 039、040，**有收費標誌** |
| **大同路　北側** | **1** | 001 |
| **文化路　平和街口以西** | ⚠ 未知 | 北側一整排＋南側 1 格，**使用者口述、照片沒拍到、號碼還沒清查** |
| 文化路南側（平和街口以東）・文化路往東・平和街・平和街33巷・大同路南側與往東 | 0 | — |

⚠⚠ **結論：診所周邊的路邊停車位少得可以一眼數完，而且大半集中在永安路。**
門口告示把「周邊三處停車場」擺在前面是對的。

### 永安路．郵局門口那一格是裝卸貨車專用區

牌子：**裝卸貨車專用區　單次限停 30 分鐘、超時依公告費率收費；開放小型車停放**，
週一至週日 **08–18 貨車 20 元／時**、**18–20 小客車 20 元／時**、
**20–08 小客車（⚠ 表上那一列沒有標費率，很可能免費，但牌子沒寫，不要當定論）**，
違者拖吊 110。地上漆著「郵局」「裝貨」。

### 收費（2026-09-15 拍到標誌）

**週一至週日 08:00~20:00，每小時 20 元。** 委託機關斗六市公所、廠商俥亭停車事業，
服務專線 (05)536-2425、免付費 0800-021111、查詢 <https://parking.yunlin.gov.tw/>。
身心障礙專用車牌或識別證全市每日四小時免費，超過依公告費率。

⚠⚠ **那面牌子寫的路段是「文化路（編號 西 01）」，不是永樂街。**
所以**永樂街那 5 格收不收費還沒有答案** —— 不能拿文化路的牌子去套。
旁證：斗六市 2025-07-01 起恢復路邊停車收費（全市 24 路段、約 1497 格、同一組時段與費率），
公告路段裡看得到文化路、大同路，**沒有看到永樂街**；加上永樂街那 5 格是 2024/07 之後
才畫的，它有可能是最新增設、甚至還沒開始收費。**寫門口告示之前一定要先確認這一件。**

**平和街33巷（整條，實拍清單第 ⑬ 段）已清查完畢：沒有汽車停車格。**

- ⚠ `body.html` 第三點寫著「周邊路段有畫設之路邊停車格」——
  現在有實證了（5 格），但**那句話還沒有依這份清查改寫過**，
  要不要點出「只有 5 格、最近的在對面」是使用者的決定。

### 還沒有答案的

- ⚠ **永樂街那 5 格收不收費**（見上面那一段）。這一題決定門口告示怎麼寫。
- **永安路東側的 004、009**，以及那一排哪一端靠文化路。
- 全聯特約停車場的消費門檻是多少（招牌上那一行還沒讀清）。
- 文化路北側的 **037、038 在哪**。
- **門口那段紅線的起點與終點**（街景上完全沒有，只有現地拍得到）。
- 其餘路段（大同路 ⑤⑥⑦、文化路往東 ⑩）的停車格，使用者之後補。

## 站上那張地圖多一顆「鄰近停車格」的標籤（2026-09-15）

使用者：「目前網站上的呈現只有付費停車場　保留原來網站的樣子　我另外加一個標籤是
鄰近停車格　按下去再顯示這些位置」。提案頁 `/preview/map-bays/`，
產生器 `node drafts/door-notice/map-bays-preview.mjs`。

⚠⚠ **格子的座標從 `parking-map.mjs` import 進來** —— 清查那張 PNG 與這一頁
只有一個出處，號碼的唯一出處仍然是 `parking-survey.json`（那一支有守門在對）。
所以 `parking-map.mjs` 的截圖那一段關在「直接跑這一支」底下，import 它不會順手重畫。

### 使用者挑定的（切換條上已經收成寫死的值，網址參數仍然吃得到）

| | 定案 | 參數 |
| --- | --- | --- |
| 標籤放哪裡 | **「位置與周邊停車」旁邊**（同一列，兩顆藥丸一樣高） | `?p=top\|bot` |
| 格子 | **空心・路牌藍的實線框**（2026-09-15 使用者：「藍實心好像怪怪的，做成空心給我看看」） | `?c=fill`、`?d=` |
| 號碼 | **不寫** | `?num=on` |
| 按下去時 | **三塊停車場熄滅，街名也一起淡下去** | `?lots=keep` |
| 點停車場時 | **格子收起來**（兩種模式互斥，沒有尺） | — |
| 路名 | 格子出現時 **文化路往東 62、永樂街往南 154、永安路往南 140**（讓開格子） | — |
| 永樂街 | 壹車房亮著時**往上 116 讓開那條點狀路線**，不再整條不見 | — |
| 未清查那一排 | **畫出來** | `?w=off` |

⚠⚠⚠ **永樂街那一項會動到站上現在的行為**（站上是 `.lbl.hide` 整條不見，
2026-08-14 使用者自己定的；2026-09-15 他改主意：「應該把永樂街的路名移位就好」）。
**還沒併進 `index.html`** —— 這一頁量到的差異就只有那一條路名：
177 個元素逐項比對（位置、大小、fill、stroke、opacity、visibility、線寬），
**只有那個 `<text>` 與它三個 `<tspan>` 有差**（opacity 0 → .28、往上 116）。

還開著的兩條尺：**虛線多疏多粗**（`?d=a|b|c|d`，預設 `d` 疏）、
**虛線的顏色**（`?k=night|ink|soft|card`，預設 `night`）。

⚠⚠ 虛線的顏色 2026-09-15 從卡色（白）換掉了 —— 使用者：「白天模式停車格白虛線
感覺怪怪的，改成和夜間一樣的虛線顏色看看」。**`#282b31` 不是新顏色**，它就是
夜間模式的卡色（`index.html` 第 5810 行），現在白天夜間釘死成同一支。
另外三格也全部是站上既有的（墨／柔墨／卡色）。

### 幾件不要再走回去的

- ⚠⚠ **標籤沒按下去的時候，地圖要和站上逐位元組相同** —— 八個寬度
  （430／393／390／375／360／320／834／1440，DPR 3）驗過。`#pv-bays` 是
  `display:none`，不是把它畫淡。改任何東西都要重驗這一條。
- ⚠⚠ **標籤的幾何整組照抄它旁邊那顆 `.card-map h3`**（字級 .88rem、上下內距
  `calc((34px × --type-scale − 1.6em − 2px) / 2)`、圓角 12px）。兩顆藥丸並排，
  高度不可以各算各的 —— 站上那顆在 ≥721px 乘了 `--type-scale`，這裡也要乘，
  不然電腦版一顆 38.25、一顆 32.2。**上下內距不要換算成 px。**
- ⚠⚠ **街名要淡下去不能只掛一次** —— 站上那條是 `.map-svg.dim`，而 `dim` 只有
  「有停車場亮著」時才會掛；這裡是**沒有**停車場亮著的狀態，所以自己補掛，
  而站上那支 `paintLots()` 每次跑都會把它拿掉。用 `MutationObserver` 補回來。
- ⚠⚠ **「點停車場就把格子收起來」一定要掛在捕獲階段**（`addEventListener(…, true)`）——
  站上那三塊自己的 handler 有 `stopPropagation()`，掛在冒泡階段的**永遠收不到、
  而且不報錯**。捕獲比目標早跑，所以先把格子收掉，站上那支 `paintLots()` 再照
  它自己的邏輯把停車場點亮。⚠ 桌機的 hover 不要跟著收 —— 那只是預覽，滑開就回去。
- ⚠ 驗「兩種模式互斥」要驗四條路：按標籤／點停車場／鍵盤 Enter 選停車場／
  標籤開著點地圖空白處。**最後那一條要等一拍再量** —— 街名淡下去是
  `MutationObserver` 補的，同步讀會讀到還沒補回來的那一格（量測的假警報，不是 bug）。
- ⚠⚠ **「圖說上面」那個位置在 `<figure>` 裡面**，標籤的 click 一定要
  `stopPropagation()` —— 不擋的話按標籤會順手觸發站上那條「點地圖其他地方＝全部熄滅」。
- ⚠ **裝卸貨車專用區（郵局門口）刻意沒有畫**：那一格停不得，畫在一張叫
  「鄰近停車格」的圖上是陷阱。要加回去先問使用者。
- ⚠⚠ **路名讓開要用 CSS 的 `transform`，不要去改 `x`／`y`** —— 標籤要能原封不動
  退回去，而「標籤沒按下去時地圖與站上逐位元組相同」是這一頁的硬條件。
  位移量與清查那張圖用的是同一組（`parking-map.mjs` 的 `move()`）。
  ⚠ `transition` 要把 `opacity` 一起寫回來 —— 站上那條 `.lbl` 本來就有，
  只寫 `transform` 會把它蓋掉，街名淡下去那一下就變成硬切。
- ⚠⚠ **「路名被格子蓋到幾條」要等過場跑完再量** —— 讓開是 .18s 的 transition，
  按下去的當下量到的還是舊位置，會誤報兩條（踩過）。`paint()` 因此排了一次
  260ms 之後的重量。**這是量測的假警報，不是版面的 bug。**
- ⚠⚠⚠ **永樂街那一段自己會動到它的 class，而它又被 MutationObserver 盯著** ——
  `classList.remove()` 就算那個 class 本來就不在，**也會寫一次 class 屬性、
  再觸發一次觀察者**。第一版就是這樣讓整頁卡死（開頁永遠 load 不完，
  **而且主控台一個錯都沒有**）。①只在真的有那個 class 時才拿掉 ②整段用旗標關起來。
- ⚠⚠⚠ **量標籤的原位要用 `getBBox()` 不要用 `getBoundingClientRect()`** ——
  那個標籤身上掛著 .18s 的 transform transition，用螢幕座標會量到「動到一半」的位置，
  於是下一次判斷又踩在錯的基準上。`getBBox()` 不吃元素自己的 transform，永遠是原位。
- ⚠⚠⚠ **用 `String.replace` 往產生器裡插一段程式，錨點要連縮排一起寫死** ——
  2026-09-15 踩過：錨點寫 `  chips.forEach(function (b) {`（兩個空白），
  結果先命中了**縮排四格、在 `paint()` 裡面的那一個**，整塊就靜靜地被塞進 `paint()`。
  後果是 `measure()` 讀不到那些變數、而且**每按一次標籤就多掛一組 MutationObserver**。
  ⚠ 所以產生器現在有一道守門：那支 inline script 要 `new Function()` 編得過，
  而且 `paint`／`measure`／`placeYongle`／`syncDim`／`boxOf`／`offOnLot`
  **只准在最外層各宣告一次**（負向測過）。
- ⚠ 顏色一個都沒新增，而且寫成 `var()`（`--map-park`／`--ink-soft`／`--card`），
  夜間模式才跟得上。

### 量出來、還沒有解的兩件

- ⚠⚠ **文化路・平和街口以西那一排（5 格），手機上只看得到 1 格** ——
  站上的地圖在手機是 `preserveAspectRatio="slice"`，左右各裁掉 65 個單位，
  而那一排落在 x 4~84。電腦版與 iPad 全部看得到。**格子的位置是量出來的，
  不要為了讓它出現在手機上把它往右挪。**
- ⚠ **號碼打開的話，永安路東側那一排有 9 個數字被同一條裁切線切掉**（面板會報），
  這是「不寫號碼」的量測依據，不是憑感覺。
