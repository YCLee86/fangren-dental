# 〈智齒要不要拔〉—— 草稿（2026-09-21 開）

來源是使用者貼進來的一份整理稿（六節：萌發年齡、長期追蹤、族群差異、四種命運、
無症狀的牙周狀況、四個建議）。這一份把它改寫成**站上的衛教文章**：
`/posts/wisdom-eruption/`（口腔外科 `surg`，站上第十八篇）。

預覽頁：`/preview/wisdom-eruption/`（noindex，定案上線後刪掉）

| 檔案 | 是什麼 |
| --- | --- |
| `preview-gen.mjs` | 產生預覽頁。骨架抓 `posts/wisdom-tooth/index.html`（同一科）。**改內文改這一支再重跑**，不要手改 `preview/` 底下那一頁 |
| `SOURCES.md` | 文中每一個數字的出處，以及**刻意沒有寫進文章的那些**與理由。⚠ 改任何數字前先看這一份 |

```bash
node drafts/wisdom-eruption/preview-gen.mjs
```

## 這一篇和〈拔智齒之後〉是兩件事

站上已經有 `posts/wisdom-tooth/`（〈拔智齒之後〉，2026-08-19 上線），講的是
**已經決定要拔之後**的事：傷口怎麼癒合、要不要填補材料、術後照顧、保險怎麼問。

這一篇講的是**在那之前的那一段**：它會不會長出來、現在那個「阻生」的判斷會不會變、
該留還是該拔。兩篇互相連著（這一篇連過去兩次，定案時 build 的「延伸閱讀」還會再牽一次）。

⚠ 〈拔智齒之後〉開頭那一節「先確認一件事：不是每顆智齒都要拔」和這一篇主題重疊，
但**那是四行的摘要，這一篇是整篇的論證**。兩邊的立場一致（沒有症狀、刷得到、
沒有影響前一顆牙就追蹤），沒有互相矛盾。**定案時不必去動那一篇。**

## 這一版做了什麼

- **骨架換成這件事本身的名詞**（時間／定局／比例／狀態／清潔／決定），
  不用空間比喻當骨架（COPY.md 第十之四節）。守門跑出來：內文「路 0、走 0、方向 0」。
- **改寫成站上的文體**（COPY.md 第十節）：第二人稱、具體場景、有立場的短句；
  去掉市井口吻。講「為什麼要多做這一步」時受益者寫出來（第十之三節）——
  例如拍全景片那一條寫的是「後面每一次判斷都是拿這張片子當基準」。
- **原稿的數字逐條查證**，查不到的一律拿掉，理由在 `SOURCES.md`。
  ⚠ 最要緊的兩件：① 原稿「推估台灣約六成」是**推的**，改成明講台灣還沒有人測過；
  ② 原稿「拖到三、四十歲風險上升」和它自己引用的台灣資料（**年齡不是風險因子**）
  互相矛盾，改成把「手術難度」與「併發症機率」分開講。
- 補「重點整理」（五問）與免責段落。
- 站內連結三處：〈拔智齒之後〉、〈八成人有牙周病〉（4 mm 那個數字）、〈半年一次的洗牙〉。

## 還沒做的

| | |
| --- | --- |
| **HERO 插畫** | 還沒畫。預覽頁上是一塊虛線佔位。畫之前先讀 ILLUSTRATION.md；出圖後跑 `node tools/hero-resize.mjs <原檔> wisdom-eruption-photo` 再跑 `node tools/webp.mjs` |
| **上線日期** | `post-meta` 的 `published` 是佔位符，定案那天填 |
| **`about` 的 Wikidata** | 阻生智齒 Q1968827、智齒拔除 Q849893、牙周病 Q520127 沿用站上已經查證過的三筆；**智齒冠周炎留白**（容器連不到 wikidata.org，不要猜 Q 編號） |
| **十四個外部連結** | 容器連不出去（`curl` 與 `WebFetch` 都被 egress proxy 擋，只有 `WebSearch` 能用），**沒有真的點開過**。請在手機上各點一次 |
| **要不要問診所** | 三件寫在 `SOURCES.md` 最後一張表：拔智齒的流程與次數、全景 X 光的費用與給付、智齒的追蹤間隔。**文章刻意都沒有寫**，只借站上已經定案的「半年一次的例行檢查」 |

## 驗過的

- 四個視窗（375／390／834／1440）：**都沒有水平捲動**（`scrollWidth === clientWidth`），
  三張表在 `.table-scroll` 裡也沒有溢出。
- 內文 3,712 字、最長一段 170 字 —— 兩項都在站上十七篇的範圍內
  （最長段的十七篇區間是 93～226）。

## 定案那天要做的事

1. `git mv` 預覽頁的內容搬成 `posts/wisdom-eruption/index.html`
   （照 CLAUDE.md 第五節：改 `<head>`、`post-meta`、`data-views-self="wisdom-eruption"`，
   把 noindex 與 `.pv-*` 那些東西拿掉，補回 HERO 與計數器那一塊）
2. `node tools/build.mjs`（首頁卡、sitemap、allowed-slugs、延伸閱讀、JSON-LD 會自己跟上）
3. 刪掉 `preview/wisdom-eruption/` 與 `preview-gen.mjs`，推導文字搬進 `history/`
