# 已定案的事與踩過的坑 —— 從 CLAUDE.md 搬出來的第九節

2026-09-18 從 [CLAUDE.md](CLAUDE.md) 搬過來的。**內容一個字都沒有改**，
節號與條號也原封不動（仍然叫「第九節」「第 15 條」「第九之〇節」），
所以 PALETTE.md／COPY.md／ILLUSTRATION.md／TEAM.md 裡那些
「CLAUDE.md 第九節第 N 條」的指路**內容仍然對得上**，只是檔名換成這一份。

> **為什麼搬出來**：CLAUDE.md 原本 42.9 萬字元、約 26 萬 token，
> 而它每一輪對話都整份載入 —— 連「改一行 CSS」都要先讀完 26 萬 token。
> 這一節（12.2 萬字元）與 LINE 那一節（26 萬字元）合計佔 89%。

## 什麼時候要讀這一份

| 你要做的事 | 讀哪一段 |
| --- | --- |
| **動任何已經定案的東西**（顏色、字級、版型、間距、地圖、夜間模式、分享圖、線稿…） | **「已經定案的（不要再問、不要重做）」那張表** —— 動手前先找那一列，裡面有當初為什麼這樣定、落選了什麼、哪些數字是取捨不是 bug |
| 寫任何產生器、提案頁、量測腳本 | **「這一輪學到、很容易再踩的坑」28 條** |
| 新開提案頁、或把提案頁搬進正式站 | 「把提案頁搬進正式站時，要一起帶的東西」＋「提案頁的切換條」 |
| 七科著陸頁、SEO／GEO | 九之〇 |

⚠⚠ **這一份不是歷史，是規則。** 裡面每一條都是「已經踩過一次」或
「使用者已經決定過」的事 —— 不讀就會再踩一次、或把他定案的東西改回去。

---

## 九、接續中的工作 —— 換電腦或手機時先讀這一節（2026-08-07）

### 九之〇、SEO／GEO 這一輪（2026-08-17～21）——✅ 全部上線了

起點是使用者收到 Search Console 的兩封信。三件技術面的成品 2026-08-18 上線，
**七科的著陸頁 2026-08-21 上線**。這一輪沒有待辦了，下面留著是為了記住怎麼做的。

#### 現在東西在哪

| | |
| --- | --- |
| 三件成品 | **已於 2026-08-18 併進 `main` 並上線**（分支 `claude/google-search-visibility-ok34xt` 功成身退） |
| 七科著陸頁 | **已於 2026-08-21 上線**：`https://fangren.net/topics/<spec>/`（general／perio／kids／endo／prosth／surg／ortho） |
| 產生器 | `tools/topics.mjs`（文案 `tools/topic-copy.mjs`）。**改完 `index.html` 要自己重跑**，build.mjs 不會呼叫它 |
| 推導存檔 | `/history/topic-pages.html` |

#### 已經上線的三件（2026-08-18）

1. **七篇文章文末各加一塊「重點整理」**（四到五組問答，`.keypoints`）。
   內文一個字沒改，答案全部從那一篇自己說過的話濃縮。刻意**沒有**加 `FAQPage`
   結構化資料（Google 2026-05 起不顯示）。顏色沒新增，問句用 `--accent-deep`。
2. **`tools/dist.mjs` 在組 `_site/` 時剝掉 HTML/CSS 註解**（新增 `tools/strip-comments.mjs`）。
   原始檔一個字不動。上線當天重量一次：首頁 raw 477.8 → 189.2KB、
   brotli **133.1 → 44.7KB（−66%）**；樣式表 brotli 17.8 → 3.5KB（−80%）。
   首頁首屏要下載的 HTML＋CSS 合計 **160.3 → 55.3KB**。
   驗收：首頁／兩篇文章／404 × 四個視窗（1440／1112／390／375）**逐像素相同、
   每個元素的 computed style 0 項有差、body 文字逐字相同**；八頁的 JSON-LD
   （首頁 @graph 12 節點、每篇 5 節點）逐字相同，canonical／robots／og 十項數量不變。
   **JS 註解刻意不碰。**

3. **卡片改顯示上架日期、首頁依 published 排序**（使用者：「文章卡片上的日期
   應該是上架時間，而不是最後更新時間。」）。

#### Search Console 那一側（使用者自己做的，已完成）

- **sitemap 已提交**（2026-08-17）。在那之前從來沒提交過，Google 是靠垃圾外連
  （`bye.fyi`、`m6xt.grosbeakk.lol`）繞過來發現首頁的。
- 「頁面會重新導向」那五筆**是正常行為不是問題** —— 全部是 `http://` 或 `www.`
  的舊網址，被 301 轉到正式網址。**不要再按「要求重新驗證」，永遠不會過。**
- 「已檢索－目前尚未建立索引」已解決（受影響 0 頁）。

#### 站況體檢（2026-08-17 實際量的，不是印象）

技術 SEO 9.5／結構化資料 9／語意與圖片 9／內容品質 8／效能 6.5／GEO 6／
E-E-A-T 6／內容廣度 5／關鍵字與著陸頁 4／**在地 SEO 3.5**／外部訊號 3。

> **瓶頸不在技術。** 投報率最高的兩件**只有使用者能做**，都還沒做：
> ① **Google 商家檔案**——看診時間三組不一致（見第十節），商家上是哪一組沒確認過。
> ② **真實的在地外部連結**（地方社團、公會名錄、學會頁面）。

#### 科別著陸頁 —— 進行中，做法已經定了、文案要重寫

**問題**：首頁「主題與科別」那七顆標記是 `.is-filtered-out` 純 CSS 隱藏，
**網址從頭到尾不變**。實測：那一節只有 121 個字，整個首頁 2362 字要代表七科，
「口腔外科」全首頁只出現 2 次。所以站上沒有任何一頁接得到「斗六 牙周病治療」。

**使用者四輪修正之後定下的做法**（`tools/topic-preview.mjs` 已經照它產生兩頁）：

- chips 從 `<button>` 換成 **`<a>`** —— 按鈕沒有網址，爬蟲點不下去、AI 引用不了。
- 那一排變成**全站的科別導覽列**，每一頁都帶著，目前那一科 `aria-current="page"`
  填滿套色。順帶長出八頁互連的內部連結網（錨點文字正好是科別名）。
- 著陸頁是 **`index.html` 的快照**（使用者：「現在首頁怎麼做的著陸頁就怎麼做，
  只是增加篩選下的科別說明或介紹而已」），**搜尋框留著**。
- ⚠ **該科以外的文章與醫師是在產生時真的刪掉，不是靠 JS 藏起來** ——
  靠 JS 篩的話 Google 抓到的仍是七科全部，繞回原點（同第一節第 1 條的精神）。
- ⚠ **HERO 拿掉**：照片與那首詩是全站門面，七頁各放一次會稀釋掉它。
  （使用者還沒對這一項表態。）

#### ✅ 版面 2026-08-18 定案（使用者：「中 ok，先這樣把版面確定了」）

切換條**已經全部拆掉**，下面每一項都是走過七輪修正之後定下來的：

| | 定案 |
| --- | --- |
| 開場的順序 | h1 → `2 位醫師・2 篇文章` → **三種處境**（一段一行）→ **一句同時回答三個人** → **流程**（標題 `flowTitle`，牙周是「從檢查到維護，大概會這樣走」；檢查／說明／治療／進階／之後，一步一行）→ **一句收尾**（決定權留給他） |
| 份量 | 整塊**收在手機的一屏之內，而且下一區要露出一角**（2026-08-18 晚間第三輪：430×769 上介紹下緣 667.1、文章卡從 689.5 開始，375×660 也收得進去）。⚠ 這一輪是**加內容又減高度**：多了「為什麼是這裡」與治療流程，高度反而從 661 收到 490。做法與量測基準見 COPY.md 第九之十一節；**文字要用病人的話講見第九之十二節**（不要用「量」、不要寫「囊袋深度／清創」、不要給療程時間、收尾要說出診所的目標） |
| 搜尋框 | 搬到「主題與科別」那一列**靠右**，寬度收到 10rem（390 上 240 → 160px）。框與底色沒動 |
| 頁首下的空隙 | `#topics` 上內距 2.4rem → **1rem**（390 上 47 → 25px） |
| 頁首 | **實心 `#393736`**（照文章頁做）—— 沒有 HERO 的話它會是透明的，白字對紙只有 1.13 |
| HERO | **不放** |
| 免責聲明 | 頁面**最後**（診所資訊之前），不是開場裡 |

**已經否決、不要再放回去的三件**（理由都在 COPY.md 第九節）：
・**症狀清單**（「這些狀況該來看一下」）—— 健檢問卷的聲音，不是陪伴的聲音。
・**三題問答**（會不會痛／要跑幾趟／健保有給付嗎）—— 問題本身就是三個障礙，
  使用者：「這個卡片資訊給出來的感覺對病患是推力而非拉力。」
・**學術式的介紹**（機轉、定義、術式分類）—— 第一版整個被退回。

**還沒決定的**：**每一頁要不要配一張圖**（見下面第 19 項）。
（「先做兩科還是七科全做」2026-08-19 有答案了：**七科齊了才切換**，見上面那一節。）

**兒童牙科 2026-08-19 定案，同樣先暫存**（`preview/topic-kids/`，九輪逐句改）。
⚠⚠ **這一科刻意不照牙周的骨架** —— 第一版照抄被使用者退回
（「看起來幾乎是複製貼上」）。七頁排在同一排導覽列上，**節奏要各自不同**：
牙周是「三個處境 → 一句回應 → 流程」，兒牙是「**兩組現場、各接一句回應**
（沒喊痛但怪怪的／已經在痛又不肯配合）→ 多做的幾件（標籤是承諾不是階段）」。
推導、四個痛點與九輪的落選寫法都在 COPY.md 第九之十三節，
**要寫其他科之前先讀那一節**（它記著三條會反覆踩到的：回應句不要用「我們」、
不要把兩種人並列成「都有」、不要承諾診所做不到的節奏）。

同一輪順手修掉的兩件（其他科會共用）：
・**橫捲的標記列開頁時看不到目前這一科**（第五顆以後整顆在畫面外），
  產生器現在會把那一列捲到目前這一科。
・**介紹區的行距 1.85 → 1.72**（`.tp-intro { --tp-lh }`，只有這一塊）。
  ⚠ 那是這一塊最後一格可以擠的空間，已經用掉了。

⚠ **兒牙這一頁刻意超出一屏**：使用者最後指定把［換牙　早期矯正是一個選項］
加回來並說「不要管版面的限制」——430 仍收得進一屏（介紹下緣 705.3、
文章卡 727.7），390 超出 27px、375 超出 65px。**不要為了版面再把它拿掉。**

⚠ 兒牙還有兩件要問診所：**舒眠實際怎麼做（誰執行、怎麼監測、什麼情況才建議）**、
**「接受轉診很多」要不要寫進頁面**（寫法不能變成同業比較）。

**一般牙科・定期檢查 2026-08-19 定案，同樣先暫存**（`preview/topic-general/`，五輪逐句改）。
⚠ 第三種節奏：開場一句 → 三個現場 → **「這裡現在是什麼樣子」**（標籤是名詞）→ 收尾。
中段變成「這一間長什麼樣」是因為**定期檢查的人沒有症狀，他要選的是一間可以固定
回來的診所**，所以這一頁回答的是「為什麼是這一間」。
⚠⚠ 這一頁**同時對兩種讀者說話**（七科裡唯一）：在地的長輩，以及**人在外地、
替爸媽找診所的子女** —— 使用者：「爸媽說『我不知道要去哪裡，你幫我找』。」
推導與材料對照表在 COPY.md 第九之十四節。
⚠ 那一節記著四條**其他科也會踩到**的：**「回來」是診所視角的字，全站著陸頁不要用**；
**不要用「不是 A，是 B」貶低一個沒出場的對手**；**不要替臨床判斷開空白支票**
（「能留就留」被否決的理由）；**兩件事接不順時，把橋搬到下一行的句首**。

⚠ 這一科還沒問到的：**搬遷年份**、**消毒與感控的具體做法**、
**高風險族群的密集回診實際怎麼排**、**有沒有看過同一家三代人**（有的話
［在地］那一行可以更硬）。

**顯微根管、植牙・假牙重建、口腔外科 2026-08-19 一次寫完，矯正整頁重寫**
（`preview/topic-{endo,prosth,surg,ortho}/`）——**七科到齊了**，都還在 `preview/`。
每一科的骨架與判斷在 COPY.md 第九之十五節，那裡有一張七科的節奏對照表。
⚠ 三件不可以踩的：**顯微根管那頁不能寫「專科醫師」**（陳芷鈴醫師是專科**訓練**
不是部定專科）、**植牙那頁不要替病人選**（三條路的優缺點都來自〈缺牙之後〉那篇）、
**矯正的療程長度與回診間隔還沒問到，不要給數字**。

**牙周那一頁的文案 2026-08-19 定案**（七輪逐句改，推導在 COPY.md 第九之十一、
十二節）。⚠ **矯正那一頁的文字還是舊版用字**，沒有經過「用病人的話講」那一輪，
不要拿它當範本 —— 要寫其他科就照牙周那一份的寫法。

**硬條件**：七頁要**全部寫完才切換** —— 標記連到空頁比現在還糟。

#### ⚠ 2026-08-19：牙周文案定案，但**先不上線**（使用者決定）

使用者問「這頁先上線、其他科還沒做，對瀏覽者會不會有影響」，看過三件影響之後
決定：**先暫存，等其他科別做好再一起放上來。**

三件影響（判斷的依據，日後有人想「先上一頁試水溫」就是這三條）：

1. **著陸頁那排導覽會有五顆死鏈** —— 沒做的科目前產生成 `href="#"`＋`data-todo`。
   那一排在著陸頁上的身分是全站的科別導覽，一半不能點 ＝ 網站壞了。
2. **首頁 chips 的行為會分裂** —— 有頁的那顆跳頁、其他六顆原地篩選，
   同一排控制項兩種行為，比七顆一致地篩選還難懂；而且同一科出現兩個入口
   （搜尋進來看到介紹頁、首頁點下去看到篩選結果）。
3. **紅利拿不到、重複內容反而集中** —— 這個做法最大的價值是八頁互連的內部
   連結網，只上一頁是孤島；而著陸頁是首頁的快照，只有一頁時它和首頁的重複度
   沒有其他科的內容可以分散。

**現在的狀態（不必再動它）**：兩頁都在 `preview/topic-{perio,ortho}/`，
三道 noindex 都在（頁面 meta、Worker 的 `X-Robots-Tag`、`robots.txt`），
站上沒有任何連結指過去，`index.html`／`assets/style.css`／`posts/` 一個字都沒動。

**真的要上線時要做的六件**（產生器抬頭也寫著）：
① 網址 `/preview/topic-<spec>/` → `/topics/<spec>/`
② 拿掉 `noindex`、補 canonical
③ 每頁自己的 `<title>` 與 description
④ JSON-LD
⑤ `tp-*` 那段樣式整段搬進 `index.html`（選擇器都已限定 `[data-topic]`）
⑥ 首頁 chips 從 `<button>` 換成 `<a>`，導覽列同時打開 —— **七科齊了才切**

> ✅ **2026-08-21：六件全部做完並上線。** 踩到的四件寫在
> `/history/topic-pages.html` 與下面那一節，其中兩件是**這一站特有、下次還會再踩的**：
> ・**⑤ 搬樣式的時候，那份檔案裡還有一段 `<script>`。**
>   `tools/topic-preview-style.html` 名字叫 style，裡面除了 `tp-*` 的規則還有
>   一支量頁首高度寫進 `--topic-pad` 的腳本（以及把橫捲那排捲到目前這一科）。
>   只搬樣式的話七頁會長高約 17px、而且變成不隨視窗調整。
>   搬進 `index.html` 之後要用 `if (document.body.dataset.topic)` 關起來。
> ・**⑤ 有兩條規則會漏到首頁上。** `#topics .sec-head`（著陸頁把它改成 flex）
>   與 `#topics { padding-bottom }` —— 搬進 `index.html` 就會作用在首頁。
>   兩條都加 `[data-topic]` 前綴關起來。**搬之前先把每一條選擇器唸過一次，
>   問「首頁有沒有這個東西」。**
> ・**⑥ 換成連結之後，產生器裡「把 `<button>` 換成 `<a>`」那一步變成空操作**，
>   `aria-current="page"` 就一直留在「全部」那顆上。改成**移動** `aria-current`：
>   先整排剝掉、再加到目前這一科；順便加一條「chips 還是 `<button>` 就 throw」。
> ・**驗證用的正規式也會踩 `<title>` 那個註解陷阱**（第九節第 8 條的近親）。
>   寫 `/<title>/` 而不寫結束標籤，會先命中註解裡那幾個字。
>   ⚠ 地圖那張 SVG 裡有三個 `<title>`（無障礙用），**不要順手一起換掉**。
>
> **上線之後使用者馬上回報兩件，當天修掉**（都是「提案頁的規矩帶到正式站」）：
> ・⚠⚠ **`counter.js` 不能拿掉。** 產生器第 9 步原本會把它剝掉 —— 那是**提案頁**
>   的規矩（第八節：不拿掉的話每開一次提案頁，首頁的計數就多一次）。可是這七頁
>   現在是正式站的頁，剝掉的結果是文章卡的瀏覽次數全部印一條「—」。
>   ⚠ 留著是安全的，理由是 **HERO 拿掉時 `data-views-self` 跟著消失了**：
>   counter.js 只有看到 `data-views-self` 才會 POST +1，這七頁只剩唯讀的
>   `data-views`。產生器現在有兩條 assert 守著（出現 `data-views-self` 就 throw、
>   少了 counter.js 也 throw）。
>   **通則：定案上線時要逐項回頭問「這一條是提案頁的規矩，還是正式站也要的」。**
> ・**「全部」那顆要指 `/#topics` 不是 `/`。** 從著陸頁按回來會落在首頁最上面的
>   HERO，還要自己再往下滑一屏才看得到文章 —— 按了等於沒按。
>   頁首那顆「全部文章」本來就指 `#topics`，這一顆跟它一致
>   （`html { scroll-padding-top }` 已經處理好頁首的高度，實測落在頁首下方 15px）。
>
> **⚠⚠ 再一輪（同一天）：使用者的螢幕錄影 ——「點標籤的時候會有頁面切換那種閃跳的
> 感覺，跟之前單純在首頁裡篩選的時候很順的切換很不一樣。」逐幀量出來是三件事，
> 三件各自要治**（同第九節第 13 條：「閃」從來不是一件事）：
>
> 1. **`/#topics` 那一跳是「滑的」而且是 load 之後才開始。**
>    Playwright 逐幀量：`scrollY` 到 DOMContentLoaded 與 load **都還是 0**，
>    再過約 320ms 才開始動，然後 74 → 296 → 571 → 740 → 768 滑了約 400ms ——
>    成因是 `html { scroll-behavior: smooth }`，**瀏覽器連「開頁跳到錨點」
>    這一次也照著滑**。使用者看到的就是「先出現整屏 HERO（照片還沒解好時
>    是一片深色），停一下才滑下來」。
>    → 改成在**解析到標記那一排的當下**就自己捲好，第一幀就在定位（實測
>      67ms 起 `scrollY` 一路都是 768）。
>    ⚠⚠ **那一刻文件的高度還不夠捲** —— 只解析到 HERO ＋ 這一節的開頭，
>      `scrollIntoView` 會被夾在半路。做法是先把 `html` 的 `min-height`
>      撐到 `250svh`，`load` 之後再拿掉（那時真正的高度約 11000px，遠大於它，
>      拿掉不會動到位置；仍然補一次 `scrollTo(0, y)` 當保險）。
>    ⚠ 捲之前要把 `scroll-behavior` 設成 `auto`，捲完還回去 —— 不然這一次
>      也會變成滑的，等於沒治（同第九節第 14 條那個「自己捲之前要關掉」）。
>    ⚠ **只在 `navigation.type === 'navigate'` 時才自己捲**：重新整理與
>      上一頁／下一頁要交給瀏覽器自己的捲動位置還原，那時使用者可能已經捲到
>      醫師介紹了，硬拉回主題與科別是另一種「跳」。
> 2. **著陸頁那排標記先畫在「還沒捲」的位置，下一幀才跳過去。**
>    那支腳本原本在頁尾，iOS 會先畫一張再跑它。
>    → 搬到那一排的**正下方**（`#topics` 裡面）。`--topic-pad` 那支同理，
>      搬到 `</header>` 底下 —— 它決定的是整個 `#main` 從哪裡開始。
>    ⚠⚠ **通則：凡是「決定第一幀長什麼樣」的腳本，就不能放在頁尾那一堆。**
>      放在它要影響的那個元素的正下方，解析到就跑，第一幀直接是對的。
> 3. **剩下的那一刀是「換文件」本身。** 標記從 `<button>` 變成 `<a>` 之後，
>    那一下就從「同一頁換內容」變成「換一份文件」，中間必然有一刀。
>    → 加 `@view-transition { navigation: auto; }`（.18 秒交叉淡入）。
>    ⚠ **這是全站的**，點文章卡、頁尾連結也會淡。不要就把樣式表最前面那一段
>      刪掉，其他東西一個都不受影響（瀏覽器不支援時它等於不存在）。
>    ⚠ `@view-transition` **只能寫在樣式表最外層，不能包在 `@media` 裡** ——
>      「減少動態效果」是靠 `::view-transition-old/new(root) { animation: none }`
>      關掉。這一項跟著那個設定走是刻意的：它是不折不扣的轉場效果，
>      和窄帶那個往上數的數字（刻意不看）不是同一類。
>    ⚠ **加了淡入不代表閃跳好了** —— 上面那兩件是位置晚一步才對，
>      淡入只是把硬切變軟。兩件事要分開看。
>
> 驗收：首頁四個視窗（390／375／834／1440）× 23 個選擇器 × 15 個 computed
> 屬性 ＋ bbox **0 項有差**；七頁的一屏量測和上線版逐項相同。

#### ✅ 2026-08-21：七科全部定案並上線


| 科別 | 狀態 | 還沒問到的（問到就補進頁面） |
| --- | --- | --- |
| 牙周治療 | ✅ 七輪定案 | 深層刮除分幾次、回診間隔、健保與自費要不要寫 |
| 兒童牙科 | ✅ 九輪定案 | 舒眠誰執行／怎麼監測／什麼情況建議、轉診很多要不要明講 |
| 一般牙科・定期檢查 | ✅ 五輪定案 | 搬遷年份、感控具體做法、高風險回診怎麼排、有沒有看過三代人 |
| 顯微根管 | ✅✅ **定案**（2026-08-19，四輪） | 活髓治療的判準、自費怎麼說明、要跑幾次、根管後的牙套是不是同一間做 |
| 植牙・假牙重建 | ✅✅ **定案**（2026-08-21，九輪） | 療程分幾階段各要等多久、三種方式的自費結構、植體廠牌要不要談 |
| 口腔外科 | ✅✅ **定案**（2026-08-21，重寫＋三輪） | 口外醫師的差異要不要補一句、術後須知要不要另做一份、多生牙什麼年紀建議拔 |
| 齒顎矯正 | ✅✅ **定案**（2026-08-21，重寫＋兩輪） | 療程多久、回診間隔、實際提供哪些方式、自費結構、要不要寫診所在雲林的矯正定位 |

**要改文案時**：只改 `tools/topic-copy.mjs`，改完跑 `node tools/topics.mjs`
（它會把每一科的 `ask` 印出來提醒）。**七科的節奏對照表在 COPY.md 第九之十五節、
十一條的檢查表在第九之十七節** —— 那兩張是防「七頁長得像同一個模子」與
「寫成診所視角」的工具，改任何一科之前先跑一次。

⚠ **`ask` 那一欄還一件都沒問到。** 問到了就補進頁面 —— 特別是矯正那一科，
現在整頁**刻意一個數字都沒有**（療程長度、回診間隔都還沒問），
不要因為「頁面看起來很空」自己填。

> **2026-08-19 稍晚：後四科（顯微根管／植牙／口外／矯正）逐句改完了。**
> 那一輪把牙周到一般牙科累積的規則整套套上去，推導在 **COPY.md 第九之十六節**。
> 已定案的三頁（一般／牙周／兒牙）**逐位元組沒有動到**，版面與產生器也一個字沒改。
> 四件值得記住的：
> ・**開場形狀要沿著導覽列的順序交錯**（lead／stance／lead／groups／stance／lead／stance）
>   —— 使用者說的「一頁一頁點過去長得一模一樣」，判準是**相鄰的頁**。
> ・**初稿三頁的 close 都是「空白支票」**（留得住就留／能不拔就先追蹤／要不要做你決定）。
>   ⚠ 「能不拔就不拔」和被否決的「能留就留」是同一個毛病，保守不代表不是支票。
>   定案一律改成**先立一個標準**（標準不是承諾）再把決定權交回去。
> ・**寫一科之前先把那一科醫師的專長欄逐項看過** —— 顯微根管初稿漏掉了
>   **活髓治療**與**根尖手術**，而那兩項正好是那一頁最有價值的兩句。
> ・**「回來」的禁令只針對「回診／回來看」**：「補回來／修回來」不在內
>   （已定案的兒牙那頁自己就寫著「處理完還要修回來」）。grep 掃紅線時會誤報。
>
> **⚠ 2026-08-19 再晚一點：顯微根管定稿了**（使用者：「顯微根管定稿」），
> 四輪。**已定案來到四科**（一般／牙周／兒牙／顯微根管），還沒定案的剩
> **植牙・假牙重建／口腔外科／齒顎矯正**三科（已逐句改過，使用者還沒逐頁看）。
> 定稿全文與推導在 COPY.md 第九之十六節。那一輪最值得記住的兩條：
> ・**拿一個自己並不佔優的維度去比，比不寫還糟** —— 初稿寫「拔掉之後還要補回來，
>   那條路更長」暗示做根管比較快，可是顯微根管本來就不見得快，讀者拿自己的
>   經驗一對照，整頁的信用就沒了。**這一科的賣點不在時間。**
> ・**一頁只寫給「已經失敗過」的人，會漏掉還沒開始的那一群，而後者通常才是大宗。**
>   前兩版的中段全寫給「做過又復發」的人，可是第一次要做根管的才是多數。
>   寫完一科先問一次：**第一次遇到這件事的人，這一頁有沒有他的位置。**
> ・⚠ 標籤用術語、內容用白話（活髓／牙髓／再治療都是使用者指定的），
>   **不要因為「要用白話」把它們改回去** —— 每一個下一句就被解釋掉了。
>
> **⚠⚠⚠ 2026-08-21：植牙・假牙重建也定稿了**（九輪），**已定案來到五科**
> （一般／牙周／兒牙／顯微根管／植牙），只剩 **口腔外科、齒顎矯正** 兩科。
> 那一頁整頁換過框架：從「三條路怎麼選」變成「**一口牙該從哪裡開始整理**」，
> 方案的比較交給〈缺牙之後〉那篇（**著陸頁給順序，文章給比較**）。
>
> ⚠⚠ **使用者當天指定：這一輪學到的方法要留給後面兩科用。** 原話是
> 「要記得這幾個著陸頁的概念，還有**看的人心裡在想什麼**、還有
> **有沒有符合診所給人的印象**這件事，記下來，剩下兩個著陸頁也要運用這個方式。」
> —— 整理成 **COPY.md 第九之十七節「著陸頁的檢查表」**，十一條，
> **寫口外與矯正之前逐條跑一次**。最要緊的三條：
> ・**診所視角有四種變形**，最隱蔽的是「主詞已經是你、問的卻是他沒有的概念」
>   （「你想恢復到什麼程度」他答不出來）。判準：**把那句話唸出來，他答得出來嗎。**
> ・**一句話「寫了不對、不寫也不對」時，要換框不是換字** ——
>   它多半被框在一個負面前提裡（「不必做假牙」「卡住就換做法」都是）。
> ・**頭銜要寫成「醫師在做的那件事」**，不要寫「這一科有部定專科」——
>   很多科都有，講「有」讀者一想就破功。
>   ⚠⚠ ~~口外那一頁的 close 正是這個寫法~~ —— **2026-08-21 已經改掉**
>   （見下面那一段）。
>
> **⚠⚠ 2026-08-21 稍晚：口腔外科整頁重寫**（使用者：「**目前看起來都只有講
> 拔智齒部分而已**」）。錯的不是句子，是**範圍** —— 他補了四件站上查不到的事實：
> 殘根、小朋友的多生牙、**有過不好拔牙經驗的人**、齒槽骨保存。
> 推導在 COPY.md 第九之十八節。三件值得記住的：
> ・**stance 從「不是每顆智齒都要拔」換掉** —— 那句話本身很好（是〈拔智齒之後〉
>   的小標），但它**把整頁又鎖回智齒**。意思改由 close 的「該拔的才拔」承接。
> ・⚠⚠ **提到「鐵鎚敲」就等於把畫面塞進他腦子裡** —— 使用者講了那個詞，
>   但頁面上寫「**不必硬來**」就好（第九之十七節 B：否定句會讓 A 更醒目）。
> ・**「上次拔牙的感覺，到現在還記得」不寫成「會怕」** —— 記得比怕更具體，
>   而且沒有替他下判斷。

⚠⚠ **改任何一科之前先讀 COPY.md 第九之十一 ~ 十五節**，尤其第九之十五節那張
**七科節奏對照表** —— 那是防「七頁長得像同一個模子」的檢查表
（牙周＝階段、兒牙＝承諾、一般牙科＝診所的事、根管＝要找的東西、
植牙＝方案名、口外＝時間、矯正＝他的猶豫）。

#### 檔案分工

```
tools/topic-copy.mjs   ← **要改文字只改這一份**。每一科：
                          lead 或 stance 或 groups（開場，三種形狀交錯用）
                          cases / flowTitle / flow（一步一行）/ close
                          另有 ask：那一科還要先問過診所才寫得下去的事
tools/topics.mjs       ← 產生器。改版型才動它。⚠ **改完 index.html 也要跑一次**
index.html             ← 著陸頁的樣式與那兩支 JS 就在裡面（2026-08-21 從
                          tools/topic-preview-style.html 搬進來，該檔已刪除）。
                          ⚠ 只作用在著陸頁的規則都用 `[data-topic]` 關起來
topics/<spec>/         ← 產出。**不要手改**，重跑就沒了
```

`node tools/topics.mjs` 會把每一科的 `ask` 印出來提醒。

⚠ **撰寫前必讀 [COPY.md](COPY.md) 第九節**（九之一 ~ 九之九）。最要緊的一句：
會來到這一頁的人**不是想認識這個科別，是帶著一個具體的擔心來的**，
這一頁的工作不是把事情講清楚，是讓他覺得被看見。

**產生器踩過並修掉的坑**（都寫在 `tools/topics.mjs` 的檔頭與各步驟旁）：
`srcset` 的路徑、`.chips button` 選擇器整組掉光、連結上不能掛 `aria-pressed`、
**門診表那排標記被誤改成連結**、**`<body>` 也有 CLAUDE.md 第八節那個坑**
（註解裡就寫著 `<body>` 這幾個字，`String.replace` 會換到註解裡那一個）、
**`aria-current` 要用移動的不是加上去的**（首頁 chips 換成連結之後，
原本那條把 `<button>` 換掉的規則變成空操作，`aria-current` 會一直留在「全部」）。

**醫師卡與文章卡的「亮起來」是在產生時寫死的**（2026-08-21 使用者指出漏掉）：
首頁那一套 `.tag-on`／`.tag-off` 本來是點 chip 之後由 JS 加的，著陸頁沒有那個
點擊動作，所以產生器直接把 class 寫進 HTML（步驟 7.5）——
**該科醫師的專長標記套色、其他醫師的專科藥丸退成白底、文章卡的主題標籤套色填滿**。
⚠ 這和「文章與醫師真的刪掉」是同一個道理：**靠 JS 加的東西爬蟲看不到**。


### ~~東西不在 `main` 上~~ —— **2026-08-21 起全部在 `main` 上了**

七科的著陸頁、產生器、文案、樣式與首頁 chips 的連結化都已經併進 `main` 並上線，
`preview/topic-*` 已刪除。新開的 session 直接在 `main` 就看得到。

### 已經定案的（不要再問、不要重做）

| 項目 | 定案 | 依據寫在哪 |
| --- | --- | --- |
| **著陸頁的「那一句回應」（`stance`）** | **一般牙科、顯微根管、植牙假牙三科 2026-09-20 起 `lead` 與 `stance` 兩個都有**（使用者：「都選 C　定案上線」）。⚠⚠ **這推翻了 2026-08-19 那條「lead 與 stance 是兩種節奏，一科只挑一種，不要兩個都給」** —— 成因不是文案是標記：七科最上面那塊「三個處境」，有回應句的四科是 `<dl>`（三個 `<dt>` 共用一個 `<dd>` ＝「問句 → 直答」），**沒有回應句就寫不出合法的 `<dl>`**（只有 `dt` 沒有 `dd`），那三科因此只能出 `<ul>`、被檢索系統讀成一串處境而不是問答（SEO／GEO 的 C4 卡在 2.5/3）。⚠⚠ **舊規則的理由沒有失效，只是降級成寫作要求**：並存的時候 **`stance` 不可以重講 `lead` 已經講完的事**，要接三個現場裡 `lead` 沒接住的那一個（顯微根管接「怕白做」、植牙接「說法都不一樣」），做不到就不要硬寫。這句話寫在**三個地方，改一處要三處一起看**：`tools/topic-copy.mjs` 檔頭 `stance` 那一格、`tools/topics.mjs` 那段會被印進七頁的註解、`index.html` 的 `.tp-lead` 樣式註解 —— 這是下面那張坑表第 28 條②「一輪定案之後，那一輪寫下的理由可能被下一輪弄成假的」的實例。定案三句：一般「這三種都很常見。**不痛不代表沒事** —— 自己來、幫家人問，先看過再決定。」（停在「現在就可以看」這一層，**不把中段那塊的話講掉**，`flowTitle` 就是「在芳仁，我們是這樣想的」）／顯微根管「第一次做、做過又發作，**怕的都是白做一次** —— 原因找得到，才談得上留。」（⚠ **刻意不再講一次「先看清楚」**，`lead` 已經講了；「才談得上留」是**標準不是承諾**，不可以改寫成「留得住」）／植牙「**問題不只一顆，答案也不只一種** —— 整口看過，才分得出哪些要動。」（⚠ **不回答「從哪裡開始」**，那是 `lead` 的問句、中段的 `flowTitle` 才是它的答案；踩過檢查表 **D** 不給壓力 —— 所以不是被否決過的「才知道要弄多大」、**J** 不重列「補過的、缺的、鬆的」）。⚠⚠⚠ **代價是一屏，而且是他看過數字之後選的，不要「修」它**：兩行的回應整塊 **+60px**、一行的 +32px（一行的折行線是 390 寬 **22 字**）；390×700 下緣 663／660／663 → 一行版 695／692／695 ✅、**兩行版 723／719／723 ❌ 超出 23／19／23**（430×769 三頁全收得進去：729／726／729）。**分數上一行與兩行完全一樣**（評分器只看有沒有 `dl` ＋ 回應），那 20px 買的純粹是「三個處境都被接住」—— 同兒牙 2026-08-19 那次（COPY.md 第九之十三節第 6 條「不要管版面的限制」）。⚠ 落選的一行版留在 `/history/topic-stance.html`，**不要日後拿它們「補救一屏」**。⚠ **兒牙沒有動**（走 `groups`、兩組各有回應、本來就滿分）；它註解裡「不要兩個都寫」那半句是引用被取消的通則，已改成「理由是它走 groups」。⚠ 另外四科這一輪**只改到註解**，而上線版是剝過註解的，所以那四頁逐位元組相同 | [/history/topic-stance.html](https://fangren.net/history/topic-stance.html)、COPY.md 第九之二十一節、`tools/topic-copy.mjs` 檔頭 |
| **文章 `about` 的 Wikidata** | **十六篇 56 個實體、43 個不重複的詞，2026-09-20 補上 `sameAs` 並上線**（38 個找到對應項目、5 個查不到就留白）。**2026-09-22 再補四個詞**（〈植牙能用多久〉〈根管鈣化〉〈智齒要不要拔〉三篇是那一輪之後才上線的）：植體周圍炎 Q601168、磨牙症 Q994942、牙齒外傷 Q2346266、**牙髓鈣化 Q7259585**（`根管鈣化` 與 `髓石` 兩個實體共用它，見下面第⑤⑥條）、智齒冠周炎 Q946882。每一筆都是去 API 查候選、再比對英文標籤／中文標籤／MeSH 編號／維基百科條目才填的，**沒有一個是推的**。對照：乾性齒槽炎 Q448753、兒童齲齒 Q2005391、口乾症 Q1162276、咬合不正 Q1320428、深咬 Q21286530、牙周病 Q520127、牙科恐懼症 Q143472、牙結石 Q144037、牙齦炎 Q673083、缺牙 Q3579096、阻生智齒 Q1968827、齒列擁擠 Q19607237、齲齒 Q133772、人工植牙 Q68892328、兒童早期矯正 Q73464882、全身麻醉 Q166543、固定牙橋 Q996044、局部塗氟 Q69523775、智齒拔除 Q849893、根管封填 Q73465827、根管治療 Q905815、氟化物防齲 Q5462842、洗牙 Q15139949、活動假牙 Q112612330、活髓治療 Q17146150、牙冠 Q143720、牙周再生手術 Q72284864、牙弓擴大 Q16635365、牙科鎮靜 Q7445147、牙結石清除 Q15139949（和洗牙同一個）、牙線潔牙 Q143978、空間維持器 Q73490675、窩溝封填 Q2262083、貝氏刷牙法 Q122537937、隱形矯正 Q593903、齒槽骨保存術 Q7553019、齒顎矯正 Q118301、牙根整平術 Q24894161。⚠⚠ **六個不是完全等號、使用者看過才上線的**：① **牙周病取 Q520127**（它的正式標籤是「牙周炎」，但**中文維基的「牙周病」就是轉到它**、英文維基的 Periodontal disease 也掛在它身上；字面相同的 **Q1059765 不要拿** —— 那是機器灌的 MeSH 條目，沒有任何維基百科連結）；② 智齒拔除→Q849893（拔牙，上位概念）；③ 阻生智齒→Q1968827（阻生牙，上位概念）；④ 活髓治療→Q17146150（蓋髓術，那一篇講的是生物陶瓷當活髓治療的修復材料）；⑤ **`根管鈣化` 與 `髓石` 都取 Q7259585**（`dental pulp calcification`／中文標籤「牙髓钙化」，MeSH D003784、ICD-10-CM K04.2、DOID:5608、MONDO_0003542、上位概念 dental pulp disease Q3368715）——**`髓石` 不是近似對應，它是那個項目登錄在案的中文別名**（`aliases.zh` 就寫著「牙髓石」「髓石」，英文維基的 Pulp stone 條目也掛在它身上，做法和 MeSH 把 Pulp Stone／Denticle 收進 D003784 一致）；`根管鈣化` 則是**取上位概念**（項目本身是「牙髓鈣化」）—— `pulp canal obliteration`／`calcific metamorphosis` 查過沒有獨立的概念項目，撈到的全是論文。⚠ 代價是**同一篇的兩個 `about` 指向同一個 Q**，那是知情的取捨，不是重複填錯；⑥ 智齒冠周炎→Q946882（`pericoronitis`／中文標籤「冠周炎」，MeSH D010497、DOID:3671、18 個維基條目，上位概念同 ②③）。⚠⚠ **五個刻意留白，不要「補齊」**：`牙根穿孔`、`乳牙牙冠`、`全鋯冠`、`金屬燒瓷牙冠`、`牙周治療` —— Wikidata 上只有論文、沒有概念項目。**2026-09-22 五個又只讀重查了一次（root perforation／stainless steel crown／zirconia crown／porcelain-fused-to-metal crown／periodontal therapy），回來的仍然全是 `scientific article` 與 `clinical trial`，結論不變。不要再查第三次。**`schema.mjs` 本來就寫著「沒填就整個略過」，硬綁一個近似的等於把文章指到別的東西上。⚠ 查的時候會撈到一堆同名的**論文與臨床試驗**條目（`is:scholarly article`），那些不是概念項目，守門要看 `P31`／`P279` 和有沒有 MeSH 編號、維基百科連結，不能只看標籤對不對。⚠ 改 `post-meta` 會動到內容雜湊 —— 那一輪十六篇的 `updated` 全跳成當天，照第五節那個程序還原了（純補後設資料，內文一個字沒改） | 各篇 `posts/<slug>/index.html` 的 `post-meta`、`tools/schema.mjs` 第 439 行那段註解、CLAUDE.md 第十節第 3 條 |
| **圖片不給人順手存走** | **CSS ＋ 一支 `assets/img-guard.js`，只擋 `<img>`**（2026-09-20 上線）。CSS（`index.html` 內嵌那份與 `assets/style.css` 各一份）：`-webkit-touch-callout: none`（iOS 長按不跳「儲存影像」）、`-webkit-user-drag: none`（不能拖到桌面）、`user-select: none`；JS 用**事件代理**擋 `contextmenu` 與 `dragstart`，只在 `e.target` 是 `IMG` 時 preventDefault。⚠⚠⚠ **這擋不住有心的人，而且原理上做不到** —— 瀏覽器要把圖畫出來就得先把檔案下載到使用者電腦，開發者工具的 Network、關掉 JavaScript、直接截圖，每一條都繞得過。**不要再花力氣往這個方向加碼**（右鍵警告視窗、把圖切片、canvas 重畫、擋 F12 …），那些一樣擋不住，只會弄壞正常訪客的體驗。⚠⚠ **只擋 `<img>`，不要擴到整頁或 `a`** —— 整頁封右鍵會連「在新分頁開啟連結」「複製這段字」一起擋掉，是在懲罰正常訪客。⚠ 真正有效的是**站上不放原檔**：`drafts/` 進不了 `_site`，外面拿得到的上限是縮過的那幾張（最大是文章 HERO 的 2000px ≈ 440KB，醫師頭像只有 400px ≈ 26KB）。**這一條不要破壞。**⚠ 線稿底圖是 CSS 的 `background-image`、地圖是 inline SVG，**本來就沒有「另存圖片」**，不必處理。⚠ `-webkit-touch-callout` 是 **iOS 專屬**：桌面 Chromium 解析時就丟掉它，`getComputedStyle` 與 CSSOM **都查不到** —— 驗收只能確認那條宣告有送出去，長按到底管不管用只有 iPhone 看得到（第一版拿 computed style 當判準，三頁全被誤判失敗）。⚠ 十五篇文章的 `<script>` 加在 `</main>` **外面**，所以內容雜湊沒動、日期一篇都沒跳。**還沒做、要先問使用者的兩件**：① 別站直接嵌我們的圖（Hotlink Protection，只有 Cloudflare 後台能開）；② 站上的圖**現在有送進 Google 圖片搜尋**（`sitemap.xml` 的圖片擴充，第十節第 6 條，那是刻意的 SEO 決定）——要不要退出是個取捨，不要自己動 | `assets/img-guard.js` 的檔頭 |
| **醫師卡上的臉書粉專** | **卡片底一行「標誌 ＋ 臉書粉絲專頁」＋ 科別色 `--accent-deep` ＋ 圓角方塊 18%**（2026-09-25 三輪定案上線，目前只有廖立揚醫師）。⚠ **標誌是圓角方塊不是臉書的圓形官方標誌** —— 使用者：「除了頭像 網站上的圖案都是方形帶圓角」。落選：名字旁／右上角、臉書官方藍、27%／38%。⚠ 網址是使用者給的**分享短網址**（去掉了 `?mibextid=`），會轉址，拿到完整網址再換。⚠ 點擊紀錄代碼 `fb:<醫師代號>`，**再加一位要改 `src/click.js` 的 `DOC_FB`**。推導在 `/history/doctor-fb.html` | — |
| **醫師卡的形象照** | **圓頭像 76px ＋ 資料全寬 ＋ 留白 8px**（2026-09-19 三輪定案上線，成品在首頁 `#doctors` 與七科著陸頁；目前九位裡**八位有照片**（李柄輝、李侑津、王俊偉、謝耀慶、陳芷鈴、楊小瑩、林晏妤、許馨文 —— 林晏妤與許馨文 2026-09-24 一起上線，許馨文 74%（框 797）、林晏妤 77%（框 1051 ＝ 整張寬，她戴手術帽、上緣改用帽頂留白 4% 定；同日第二輪換了照片，背景改冷灰診間，仍是整張寬 1051、頭高 78%）；謝耀慶 2026-09-23 上線，框 720；陳芷鈴同日上線，框 615；楊小瑩同日上線，框 845，同日第二輪換了照片（第一張髮型太圓、和圓框疊成兩圈），新照片框 795；見 `/history/doctor-photo.html` 第四到第九位那幾節）。⚠⚠ **廖立揚 2026-09-23 上線，當天由使用者要求移除**（「廖立揚醫師照片移除」，沒有說原因，**不要自己推測，也不要自己放回去**）—— 他那張卡回到純文字，成品檔 `assets/doctor-liao-liyang-400.jpg`／`.webp` 已刪，但 `tools/doctor-photo-crop.mjs` 的 FACES 裡那一段**註解掉留著**（三輪量出來的四個點與定案的框 `217,51,668,668`），四張原檔也還在 `drafts/doctor-photo/src/`；要放回來的五個步驟寫在那一段註解裡。推導仍在 `/history/doctor-photo.html` 第七位那一節，其餘維持原本的文字卡）。⚠⚠⚠ **`.doc[data-face]` 一定要寫 `align-content: start`**（2026-09-19 當天稍晚補的，**上線時就錯了、隔一輪才抓到**）—— 那張卡是 grid、兩列都是 `auto`，而外層 `.docs` 會把同一列的卡**拉到最高那張的高度**；多出來的空間 grid 預設（`align-content: stretch`）會**平均分給那兩個 auto 列**，頭像那一列跟著長高，名字被推低、專長被推遠。實測 1440 三欄：最高的李柄輝那張不受影響（8／−0.39），被拉伸的李侑津 **21.45／+6.78**、王俊偉 **34.14／+13.13**。⚠⚠ **驗收只量「第一張卡」或「只量手機」都驗不出來** —— 手機一欄每張卡自己一列沒有多餘空間，而第一張正是內容最長、永遠不會被拉伸的那張。**要掃過每一張有照片的卡 × 每一個斷點**（320／375／744／1112／1440 × 首頁與各科著陸頁）。⚠⚠ **留白是 8 不是 12** —— 12 最接近「沒有頭像時名字到專長的 11.2px」、提案頁的面板也這樣寫，但使用者在自己手機上挑了更緊的 8。**不要看到「12 比較接近現況」就改回去。**⚠⚠ **標記是 `data-face` 屬性，不是 class `has-face`** —— `tools/schema.mjs` 比對的是 `<article class="doc"` **緊接一個引號**，寫成 `class="doc has-face"` 會讓那位醫師**從 JSON-LD 的 @graph 裡整個消失**，畫面完全正常、不報錯。⚠ 距離全部交給 `row-gap`，**不可以靠 `.doc h3` 的 `margin-bottom`** ——那一列的高度由 76px 的頭像決定，h3 連同 `.7rem` 的下邊距只有 41px，整個被吃掉（症狀：「專長」貼著頭像下緣 0px），同一個下邊距還會讓名字的墨比頭像中心**高 5.6px**（第九節第 5 條：`align-self` 對的是盒子、眼睛讀的是墨）。⚠ **76px 在手機／iPad／電腦是同一個值**，字級有逐段放大、頭像刻意沒跟。⚠ 檔案 `assets/doctor-<slug>-400.jpg` ＋ `.webp`（400 ＝ 76 × iPhone 的 DPR 3 ＝ 228 取餘裕，一張 26~28KB，**刻意不做 srcset**）。JSON-LD 的 `image` **讀卡上 `<img>` 的 src**（取 JPEG 不取 `<source>` 的 WebP，爬蟲吃 JPEG）；**沒有照片的那幾位整個略過該欄位**，不放標誌或佔位圖代打。落選：Ⓒ 卡片頂圖（1440 上旁邊兩張字底下會空 **285px**，Ⓐ 現況是 53）、Ⓓ 創辦醫師橫幅（⚠ 它的理由「只有一張圖時創辦醫師本來就不同級」**第二張圖進來就不成立了**）、排法 Ⓑ 跟著縮排（手機資料欄 244→154、最長那欄 4 行→**7 行**）、Ⓒ 上下疊（每張卡 **+85px**）、字首圓章（沒有圖的那幾張維持空著）。⚠ 已知並經使用者指定上線的兩件：兩張都是 **AI 生成的形象圖不是實拍**；兩張的**背景色溫差很多**（冷灰診間 vs 暖木走廊），九張到齊時要整組再看一次。⚠ 裁切框逐張手量、**不能各裁各的**：量髮頂／眼睛／下巴／臉中線，換算成第一張的比例（眼睛在框高 44.2%、臉中線在框寬 49.3%），**框的大小照「頭寬」算不要照「頭高」算**（頭寬高比因人而異）。⚠⚠⚠ **給使用者挑的尺，單位要用「頭高佔框高的比例」，不可以用框的 px**（2026-09-23 廖立揚那一輪，四張一次全部偏掉才發現）—— 框的 px **跨不了醫師**，每個人的原檔尺寸與取景都不一樣，王俊偉的 660 和廖立揚的 698 根本不在同一把尺上；拿 px 當尺會整組偏小而且**在單張上完全看不出來**（那一輪面板上甚至寫著「四張的頭圍都和站上那三顆並排比過」，比的其實是我自己算出來的數字不是墨）。把新的候選和站上已上線的那幾顆**一起套格線量**，讀出來的是：李柄輝 81%、李侑津 81%、謝耀慶 ~79%、王俊偉 77%、廖立揚 74%、陳芷鈴 71%、楊小瑩 67%；新的一位就給四格、並把 77 與 81 標成「和某某一樣」。⚠ 量 marks 的做法也一起換掉：**先隨便裁一張 400px 出來，在成品上套格線讀、再換算回原檔座標**，不要直接目測原檔（廖立揚第一輪的臉中線偏了 10px、臉歪 2~3%，單張看不出來，並排才看得到）。⚠ 五位醫師五次，**兩次往「滿」走（王俊偉 660、謝耀慶 720）、三次往「鬆」走（陳芷鈴 615、楊小瑩 845、廖立揚 668）** —— 這一項沒有可以套的預設值，每一位都要給一把兩側都有格子的尺。⚠ 楊小瑩那一張同時說明了**為什麼不能只看框的 px**：她鮑伯頭、髮量厚，眼睛到髮頂 317（陳芷鈴 231），所以同樣的頭頂留白她的框要 845、頭高比例卻只有 67% —— 不是算錯，並排看是同一組。⚠⚠ **算出來的是起點，並排看才是判準**（王俊偉那張補的）—— 他髮量高又蓬，目測頭寬會低估，算出 627、頭頂只剩 3.5% 留白且頭明顯比另外兩張滿；627／660／690／724／760 五個圓排在一起 —— 我收成 724（頭頂 9.0%），**使用者改成 660**（頭頂 5.6%，和李侑津的 5.9% 幾乎一樣）。⚠ 第 28 條那條再一次：**定案定的是他螢幕上那一格，不是我算出來的那一格。**⚠⚠ **名字和藥丸放不下要斷行，而且藥丸要和名字切齊左緣**（2026-09-19 使用者指定）—— `.doc-role` 的 `margin-left: .5rem` **換行之後還在**，所以折到第二行的藥丸會比名字右 8px。純 CSS 沒辦法「只在行首取消邊界」，所以 `.doc[data-face] > h3` 改成 `display: flex; flex-wrap: wrap; column-gap: .5rem`（gap 不會出現在行首）。⚠⚠ **flex 會讓 `vertical-align` 整個失效**，而藥丸的抬升量是使用者 2026-08-11 五輪定的（見本表「專長標記的貼合」那一列），所以用 `position: relative; bottom` 把同一個量還回去，而且那個值收成 **`--role-rise`**、和 `vertical-align` 共用一份宣告（三個斷點各一處：`.158em`／iPad `.2136em`／手機 `.21em`）—— **要改抬升量只改那三處，不要在 flex 那段另外寫數字。**實測：抬升量逐項與改前相同（−0.87／−2.45／−3.48，375／744／1440，有圖無圖都一樣），折行時藥丸左緣對名字左緣 8 → **0**。⚠ 規則只掛在 `[data-face]` 上：沒有照片的那幾張目前在 320 都不會折（最長的「贋復假牙專科醫師」也放得下），而 flex 會讓 h3 在 ≥1041 矮 0.48px；九張都有照片之後自然涵蓋全部。⚠⚠⚠ **折行的那一行要「靠上＋補邊距」，不可以用 `align-self: center`**（2026-09-19 使用者回報「藥丸標籤的版型跑掉了」）—— 藥丸折到第二行之後整塊 h3 變兩行，**置中會把第一行往上推**：1042 寬實測，那張卡的名字比同一列另外兩張**高 13.6px**（+10.6 對 +24.2），而名字是一整列裡最強的視覺錨點，一張歪掉整列就散了。寫法是 `align-self: start; margin-top: calc((var(--face) - 1.5em) / 2)`，`1.5em` 就是 `.doc h3` 的行高，所以**單行時和置中完全等價**（實測 375／744／1042／1440 的名字上緣與「頭像下緣到專長」逐項相同）。已知取捨：折行那張的藥丸比頭像低 8.5px，所以它的「專長」比同列低 6px —— **名字齊平比那 6px 重要**，兩者不可能同時成立（藥丸要有地方掛）。⚠⚠ **藥丸放不下躲不掉，不要試著縮字級硬塞**：1042（三欄的下界）是全站最窄的卡（316px），名字那一行只有 175px；楊小瑩「贋復假牙專科醫師」與陳芷鈴「顯微根管專科醫師」都是八個字，放照片之後一定會折 —— 算過要塞得下得把藥丸從 14.4px 縮到 10.9px。另外 320 寬六個字就會折。⚠⚠⚠ **藥丸的抬升一定要寫 `top: calc(-1 * var(--role-rise))`，不可以寫 `bottom: var(--role-rise)`**（2026-09-19 踩過，使用者回報「藥丸形狀和文字的位置很明顯是不一樣的」）。成因：`skFit()` 會把一個 `class="doc-role"` 的探針插進**第一張** `.docs .doc h3` 裡現量藥丸的上下內距；那支探針是 `position:absolute` ＋ **行內** `top:0`，再吃到 `bottom` 就變成 **top／bottom／height 三者同時指定**，瀏覽器改用「容器高 − top − bottom」解高度 —— `--role-padB` 被算成 **0**（正確值 2.590／3.914），**藥丸矮了 2.6~3.9px、字貼著塊底**。⚠⚠ 而且 `--role-padT/padB` 寫在 `:root` 上，**九張卡全部中招**，不是只有有照片的那三張，而且畫面不會有任何錯誤訊息。寫成 `top` 就沒事（探針的行內 `top:0` 權重比較高，`bottom` 維持 auto）。⚠ 同一輪在 `skFit()` 的寫入那一段加了一道守門：**算出 ≤0 就整個 job 不寫**，退回樣式表裡的後備值（`--role-padT: .425em; --role-padB: .21em`）——`want` 是字級的 0.28~0.405 倍、行高又是 1，正常量出來一定是正數，掉到 0 只可能是探針量錯。⚠ **驗收要比「有照片的卡」與「沒照片的卡」的藥丸塊形**（塊高／字面離塊頂／字面離塊底），兩者必須逐項相同 —— 只看有照片那幾張是看不出來的，它們一起壞。⚠ **頭像那一行往左移 `--face-pull: 6px`**（2026-09-19 使用者指定「照片可以往左移一點，連帶名字和藥丸也可以跟著左移」）。⚠⚠ **那是視覺補償不是對齊錯誤** —— 量出來頭像的左緣和「專長」那顆標籤**本來就切齊**（差 0.00），但圓形的最左點只在它的垂直中央、上下都往內收，所以和方形切齊時看起來偏右。**不要把它「訂正」回 0。**做法：`.doc[data-face]` 的 `padding-left` 減掉那個量、`> dl` 用同樣的量補回來 —— **九張卡的「專長／資歷／學歷」左緣因此仍然完全對齊**（實測每個斷點都是單一值），沒有照片的那幾張一個值都沒動。落選 4／8px（並排比出來的，8 已經明顯左於標籤）。順帶：名字那一行因此多 6px 可用寬，折行的門檻往後推了一點。⚠⚠ **照片四周的三個間距鎖成同一個值**（2026-09-19 使用者：「名字和照片右緣的間距，要和照片和圖卡左緣的間距一樣」＋「再往上移一點」——那是同一件事）：**卡片左緣→照片 ＝ 照片→名字 ＝ 卡片上緣→照片**。改之前量出來左 14.19／到名字 14.39（本來就幾乎一樣）／**上 18.59**，差的只有上面那個。做法：`--face-gap: calc(1.2rem - var(--face-pull))` 同時餵 `padding-left`、`padding-top`，`column-gap` 再 **＋1px**（左緣與上緣是從**看得到的卡片邊**算的，中間隔著 1px 框線；到名字那一段沒有框線）。⚠ **不必逐斷點寫** —— `1.2rem` 會跟著主字級縮放（手機 rem 16／iPad 19／電腦 18），算出來的 13.2／16.8／15.6 和原本的 `padding-left` 逐項相同；實測三個斷點的三個間距都是單一值（14.19／17.8／16.59）。⚠ 頭像直徑收成 `--face`（欄寬、圖的寬高、名字那一行的上邊距三處共用）。⚠ **這件事只在使用者的機器上看得到**：他的字型（PingFang TC）比容器裡的寬，名字＋藥丸要 196px、可用 187.6px；容器裡量是 176.2px 剛好放得下。**驗收要用 `letter-spacing` 把字撐寬去逼出折行**，不能只信本機的字寬。加一位醫師的五個步驟寫在 `/history/doctor-photo.html` | [/history/doctor-photo.html](https://fangren.net/history/doctor-photo.html)、`tools/doctor-photo-crop.mjs` 的檔頭 |
| **「強制自動重排」只治了一處** | **只把 `skFit()` 改成「兩個 job 一起插探針、一起量」，以及 `cardClamp()` 改成「十三張卡量完再一起寫」**（2026-09-18，使用者貼的 PageSpeed 第二條）。全站強制重排 **80 次 → 60 次（−25%）、442 → 361 毫秒**，而**省下的時間幾乎全是 skFit 那一處**（82.4 → 13.3 毫秒）。⚠⚠ **cardClamp 那一改次數掉了 12 次、時間一毫秒都沒省** —— trace 攤開才看得到：貴的是**每一次呼叫的第一次整份樣式重算**（兩次共 68 毫秒），跟在後面那十二次一張 0.36 毫秒。留著的理由是「文章愈多它愈貴」與「PageSpeed 那張表會少 12 列」，**不是它省了時間**。⚠⚠ **剩下的三大筆刻意不動**：`paint()` 開頭那個 `scrollY`（1 次 143 毫秒）、`cardClamp` 的兩次重算（72 毫秒）、`pick()` 的三次（38 毫秒）——它們是**整份文件的樣式與版面計算，本來就非做不可**，腳本只是比瀏覽器自己排的時機早了一點。要真的省掉只有「等瀏覽器排完再量」一條路，而那會讓文章卡的摘要晚一幀才收成兩行（＝版面位移）、頁首晚一幀才換過去 ——**後者使用者 2026-08-15 已經明確要求過「頁首不要慢一幀」**。⚠⚠ **一度做了、又拆掉的**：`--glass`／`--head-fade`／`--lamp-*` 四處「值沒變就不要寫」的守門。推理很漂亮（少寫一次就少弄髒一次版面），**但量出來捲動那一段的樣式重算是 258 次 27 毫秒 vs 266 次 30 毫秒 —— 在雜訊裡**（窄帶那一段那些值本來就每一幀都在變）。**量不出來就不要把複雜度留在全站最細緻的那段捲動程式裡。**⚠ 驗收：那幾支現量腳本寫出來的 **756 個值（2 頁 × 3 寬度 × 7 個捲動位置 × 18 項）逐字相同、雜訊 0** —— 這一輪改的是時機不是算式。⚠ PageSpeed 另一條「網路依附元件樹狀結構」（關鍵路徑 782 毫秒）**不是我們的東西**：整條鏈是 `static.cloudflareinsights.com/beacon.min.js` → `/cdn-cgi/rum`，Cloudflare 的 Web Analytics 自己注入的，repo 裡一個字都沒有。要拿掉只能去 Cloudflare 後台關掉 Web Analytics（只有使用者打得開）。⚠ **不要為它加 preconnect** —— 那支是 async 的、不擋算繪，PageSpeed 自己也寫「沒有其他適合的預先連結候選來源」 | `index.html` 的 `skFit()` 與 `cardClamp()` 檔頭 |
| **站上的照片同時出一份 WebP** | **`<picture>` ＋ `<source type="image/webp">`，JPEG 留在 `<img>` 那一行當退路**（2026-09-18 上線）。起因是使用者貼的 Google PageSpeed Insights 診斷：首頁一次載 1327 KiB 的圖，它算得出可省 465 KiB。做法是**不重畫、不換內容**，把已經上線的 44 張 JPEG 用 `tools/webp.mjs` 轉存成 WebP（Chromium canvas、品質 **0.82** —— 和 `tools/hero-resize.mjs` 同一個數字，刻意的：一個站上只該有一個有損編碼的品質基準）。實測 **首頁 1327 → 855 KiB（−36%）**、HERO 那張夜景 367.8 → 129.1 KiB（−65%）、文章頁 −27%、著陸頁 −18%。⚠⚠ **分享圖那一路一律留 JPEG** —— `webp.mjs` 只掃 `src`／`srcset`，掃不到 og:image 的 `content=` 與 JSON-LD／sitemap 的 `"url"`，**那是刻意的**（第十節第 4 條那條「爬蟲不吃 SVG」同一族的顧慮：各家爬蟲對 WebP 的支援沒有保證）。⚠⚠ **AVIF 沒有做**：Chromium 的 canvas 產不出 AVIF，而且**不會報錯**（見下面那張坑表第 29 條）。⚠ 三個寬度**缺一張就整組退回只有 JPEG**（`webpSrcset()` 回空字串），忘了跑產生器只會少省流量，不會變破圖。⚠ 版面靠 `picture { display: contents }` 保持不變 —— **首頁內嵌樣式與 `assets/style.css` 各一份，兩邊要一模一樣**；三頁 × 三個寬度的元素幾何逐項比對過（唯一會動的 `.hero-shot img` 拿「同一版兩次」當雜訊基準證明是推進動畫），逐像素差 0.05~0.37% 全部落在照片自己那一塊。⚠ PSI 另一條「原始尺寸 796×447 大於顯示尺寸 673×376」**刻意沒有處理**：那是 800w 這一格對 Lighthouse 手機（412×823・DPR 1.75 ＝ 673 實體像素）高估 16%，換成 WebP 之後那 16% 只值 10 KiB，不值得為它多養一組檔案 ⚠⚠ **七科著陸頁的線稿走另一條路**（同日稍晚）：它是 CSS 的 `background`，包不了 `<picture>`，改用 **`background-image: image-set(url(…webp) type("image/webp"), url(…png) type("image/png"))`**，**前面那一行只有 PNG 的宣告一定要留著** —— 不支援 `image-set` 的瀏覽器（Safari 16 以下）會把整行丟掉，留著它才有退路，而且只覆寫 `background-image`、位置與大小仍由簡寫決定。⚠⚠ **線稿是無損比有損小**（白天那七張：無損 35.8~63.0 KiB、有損 0.82 是 40.8~62.1 KiB）——平塗的線畫色塊少，有損還得花位元去編碼它自己的振鈴。所以 `webp.mjs` 對 PNG **兩種都試、取檔案小的那一個**（白天七張全部走無損，夜間七張走 0.82）。實測白天 −65~70%、夜間 −20~30%，**科別著陸頁因此從 387 → 248 KiB（−36%，原本只有 −18%）**。⚠ 夜間那七條在 NIGHT 區塊裡、由 `tools/night-mode.mjs` 產生 —— **改那七行要改產生器不是改 index.html**；換過線稿的順序是 `night-mode.mjs` → `webp.mjs` → `topics.mjs` → `build.mjs`。⚠ 七科 × 白天／夜間十四種都驗過：只抓 WebP（沒有連 PNG 一起抓）、`::before` 的大小／位置／濃度逐項相同 | `tools/webp.mjs` 的檔頭 |
| **頁首選單的「目前在哪一節」** | **Ⓓ2 玻璃**（2026-08-10 上線）。三項**常駐一圈玻璃框**（.75px／紙色 .26），捲到哪一節那一項就多一塊底（紙色 .24）＋ 框變濃變粗（.55／1px），字轉純白。**HERO 那一屏三項都只有框**。原本的 hover 底線整條移除 —— 它只有 `:hover`，手機會黏住，三項點過就三條線一起留著 | 本檔第八節、`/history/nav-scrollspy.html` |
| **那三個玻璃框的高與圓角** | **框高 `--frame-em: 2.65`（＝字級的倍數，對 `a` 垂直置中）＋ 圓角 `--frame-r: 8px`**（2026-08-11 三輪上線）。**手機／iPad／電腦版同一組值**，所以那一段是基底規則、不在任何 @media 裡。落選：高 2.36／2.15、圓角 10／6px。⚠ 頁首因此離開全站的 12px 圓角族（使用者指定，不是漏改）；8px 落在既有的小標籤那一族（`.doc dt` 6／`.card-tag` 8.5／`.info-card h3` 9）。⚠ **不要改 `a` 的 `padding-block`** —— 會連動頁首高度與 HERO 詩的 8.0px 空隙 | 本檔第八節、`/history/nav-frame-height.html` |
| **頁首玻璃「招牌之前不要那麼糊」** | **平段：橫的螢幕（比例 ≥ 1:1）5px、直的螢幕 4px；轉場：招牌之前 **110px** 開始升，招牌（照片高度的 **79.9%**）之後維持 6px**（2026-08-11 三輪上線）。⚠ 成因是 `object-fit: cover` 的裁切，不是哪個裝置的模糊比較重（本來全都是 6px）——iPad 直放只留中間 46%（純立面與窗戶）、電腦看得到整張、手機的條子厚達照片高的 21% 會把天空一起平均進來。⚠ **轉場一定要用捲動距離表示，不是照片比例**：用比例寫，同一條規則在不同視窗會差四倍。⚠ **平段的分界用畫面比例不是 min-width** —— iPad 直放 834 寬會和電腦分在同一邊，而 iPad 橫放看得到 95% 的寬度、情況其實同電腦版。⚠ 「條子底下是照片的哪一列」要把 cover 一起算，`object-position` 讀 computed style 所以三種裁法都自動算對。⚠⚠ **iPad 那 4px 是在舊版型上挑的** —— 決定時 iPad 直放還吃電腦版的 100svh、只留 46% 寬（幾乎全是立面與窗戶），同一天稍晚 70svh 的 iPad 版型補上線（commit 8888946）之後放寬到 66%、天空回來了。**規則本身不受影響**（每一幀從實際盒子算，十二個視窗重驗過），但 iPad 現在比當初更不容易看出糊，4px 是偏保守的那一側 | 本檔第八節、`/history/head-blur-ipad.html|mobile|desktop/` |
| **點到的科別才亮起來** | **主題與科別按下去 → 帶出有關聯的醫師（專科**或**專長命中）與文章；命中的專長**淡色填滿**（該科的色 12% 混進卡色、字用深階）、沒命中的維持純文字；專科藥丸 ≠ 那一科就退成**白底字套色**；被篩到的文章卡主題標籤**套色填滿**。沒點任何一科時和改動前完全相同（四個寬度逐一元素量過）。⚠ 這一版正式放掉了「顏色 ＝ 篩選」那個不變量 | [PALETTE.md](PALETTE.md) 第六之二節、`/history/doc-spec-tags.html` |
| **專長標記的貼合** | **塊要包住的是字面框、不是行框**（2026-08-11 上線）。`line-height: 1`，塊高由上下兩個內距各自負責，JS 現量成「字面上下各留一樣多」＝ **0.28 個標記字級**；側邊 **`.4546em`**（留白比 0.551，≈ `.doc dt` 的 0.553）。上下不對稱 −2.18 → −0.01px。落選 Ⓥ .385／.32、Ⓧ .5／.42rem（那一輪的 Ⓧ3 是 `.34rem`，字級定案後改寫成同比例的 em）。**`.doc-role`（專科藥丸）第五輪一起擺正**：上下各留 **0.405**（＝維持它原本的塊高 20.86），`vertical-align` 與側邊 `.6rem` 都沒動。⚠ **其餘有底色的小標籤（`.doc dt`／`.card-tag`／`.chips button`／`.info-card h3`）一顆都沒動** —— 它們單獨站著，看不出來，不要為了一致順手改 | 本檔第八節、`/history/spec-tag-fit.html` |
| **套色之後那幾個字多大** | **`.83rem` ＝ 和沒套色的內文一模一樣，完全不縮**（2026-08-11 上線）。原本 `.748rem` 比內文小 1.31px／10%，標記亮起來卻讓字縮一階，方向是反的。落選 `.748`（原本）、`.79rem`。塊高 21.13px、抬升量因此正好是 **0**。⚠ **`.83rem` 更早被否決過**（會撐開行框），那個理由在塊高改成從字面框長之後不成立了。⚠ **再往上加字級的天花板不是塊高，是塊上緣離基線多遠**（現在 15.13px vs `.doc dd` strut ascent 14.97，那一列已經長高 0.13px） | 本檔第八節、`/history/spec-tag-size.html` |
| **加到主畫面的圖示** | **純白底 `#ffffff` ＋ 一般牙科的套色 `#3f654a`**（底色 2026-08-12 第五輪由使用者指定改純白：Google Maps 的白在螢幕上是 255、我們的紙色是 227，**兩者都原封不動通過玻璃效果**，所以並排就是實差 27 階。⚠ 這是**圖示專用**，站上底色仍是 `--paper`）（2026-08-12 兩輪上線，對比 5.23:1）。第一版用 favicon 的診療椅綠 `#4f6361`，使用者指定改成「一般牙科／定期檢查的綠」（〈定期檢查〉的 `data-spec` 就是 `general`）；份量幾乎沒變（5.04→5.23），換的是色相。⚠ **取的是「套色」那一階不是深階 `#2c5238`** —— 深階給白底上的**字**，套色給**填實的塊**（首頁 chip 按下去就是這樣站在紙色上）。⚠ **主畫面與分頁列現在不同色是刻意的**，兩者的底不一樣，不要順手改成一致。兩顆都是站上已經在用的值。幾何直接從診所的 AI 原始檔取出 —— 順帶量到**站上頁首那條路徑本來就是原廠向量**（等比例 0.6454888747，25 個節點最大殘差 0.005%）。⚠ **和 `assets/favicon.svg` 是兩個檔，不要合併**：主畫面這顆**底一定要不透明**（iOS 會把帶 alpha 的 apple-touch-icon 壓在純黑上），牙洞維持**原始比例**（favicon 放大 1.6 倍是為了 16px，這裡最小 180px）。⚠ `site.webmanifest` 的 `display` 是 `"browser"` 不是 `"standalone"`，也**沒有加** `apple-mobile-web-app-capable` —— 這站會連出去（地圖、LINE、電話），standalone 等於把人關在裡面。⚠⚠ **`apple-touch-icon` 只宣告一張 `icon-192.png`（`sizes="192x192"` ＋ `?v=`），全站不再有第二個 any 版尺寸** —— 2026-08-12 第六輪照 blog.ichentsai.tw 的成功案例重做。192 只比 iPhone 要的 180 大 6%，**只縮不放、而且幾乎不用縮**；放大會糊、大幅縮小也會糊。前五輪給過 180（1:1 仍糊，因為 iOS 去拿了 manifest 裡更大的那張）與 1024（÷5.7，更糊），且同時掛著根目錄 180×2 ＋ manifest 192/512/1024/maskable 共五個候選 —— **候選一多 iOS 就挑錯那張再縮放**，這才是一直糊的成因。⚠ **不要補回任何 any 版尺寸，也不要再放根目錄的 `apple-touch-icon.png`**。⚠ **換圖之後主畫面那顆要刪掉重加**，iOS 是在加入的當下把圖存進捷徑的。⚠⚠ **PNG 裡實際寫的綠是 `#205533` 不是 `#3f654a`** —— iOS 26 的玻璃效果只抬暗色（實測 +26/+14/+20，紙色紋風不動），解出通道映射後反推的補償值，疊加後正好還原成 `#3f654a`（ΔE 9.2→0.0）。**`icon.svg` 仍然放品牌真值，補償只在 `tools/app-icons.mjs` 裡**（`COMPENSATE = false` 可關）。`#205533` **不是新品牌色，別處不要用**。⚠ **模糊無解**：邊緣過渡 1px→5px 是 iOS 產生捷徑圖示時自己重新取樣的，站方無介面可影響（十一輪、三個實機對照頁測完，見 `/history/icon-homescreen-blur.html`）。⚠ **牙洞放大 ×1.18 ＋ 上移 12**（四案選 Ⓐ）—— 這推翻了原本「主畫面夠大、牙洞不必放大」那句：iOS 的柔化約 4~5px，原始比例的洞在手機上只有 11.1px，兩側各被吃掉就糊成斑點。⚠ **和 favicon 放大 1.6 倍的理由不同**（那是對抗 16px 的抗鋸齒），倍率不必一致。⚠ **整片套色版落選的理由是補償空間，不是銳利度**：iOS 會把背景色以 16% 均勻混進標誌，白標誌只剩 rgb(220,227,222)，而白已是 255 無法補；白底那版的扭曲則可反推補回（ΔE 9.2→0.0）。⚠ 落選 Ⓑ 米色＋苔綠、Ⓒ 深綠松＋米色（方向接近 2026-08-02 被換掉的「深綠方塊＋白牙」）、Ⓓ 白＋焦糖褐。**要再換只改 `assets/icon.svg` 那兩個色值再跑 `node tools/app-icons.mjs`，並把各頁 `icon-180.png?v=` 換成當天**（Safari 的圖示快取按網址存） | [PALETTE.md](PALETTE.md) 第六之二十節 |
| **看診時間卡＝門診表** | **日子當欄、早／午／晚當列、一格一顆點；按科別標記就套色**（2026-08-13 上線，四種版型、Ⓓ 三輪修正）。資料來自門口那面磁鐵板的照片。⚠ **表格是靜態 HTML，JS 只做套色** —— 理由同本檔第一條，看診時間是全站最關鍵的事實，爬蟲與無 JS 都要看得到。⚠ **這張卡是 `tools/schema.mjs` 讀營業時間的唯一來源**：開診日讀 `thead` 的 `<th scope="col">`、時段讀 `<th scope="row">` 裡的時間、休診日讀 `.info-note`。**不要把那個 div 的 class 從 `info-card` 改掉**（正規式比對整串），也不要把日子或時間搬進註解。⚠ 讀法假設**列出來的每一天都三節全開**，日後若有「某天只有早診」要改成逐格判斷。⚠ `.info` 因此從三欄改成「上面兩張、下面一整條」（表格＋八顆標記塞不進 347px，grid 會把同列的卡一起拉高）。⚠ 第一顆標記叫**開診時段**不是「全部」，**首頁「主題與科別」那排仍然是「全部」，兩者刻意不同**。⚠ 顏色一顆都沒新增：格線與淡掉的點 `--rule`、字與有診的點 `--ink-soft`、套色吃該科 `--accent` | `/history/clinic-schedule.html` |
| **手機的可讀性與橫捲標記** | **十二項一起上線**（2026-08-13，`preview/cta-mobile/` 十九項逐項對照後定案）：字級抬一階／窄帶三行間距 **+4**（gap 與上下內距一起加，只加 gap 會弄壞 08-10 那一輪的 11.7／11.8 平衡）／觸控目標 44px 兩批（窄帶地址電話・頁首選單・頁尾導覽用 `::after` 撐，版面不動；頁尾地址電話與搜尋框鄰居太近只能長高）／**兩排科別標記塊高一律 34px**（門診表那排原本 28.8 一起拉齊）／標記字級 **.90rem**（門診表 .88）／**主題與科別＋門診表兩排收成一排橫捲**／橫捲提示 **右緣淡出 16px ＋ 左右各一顆角形**（往版心外 10px、`--ink-soft` × .7）／頁尾更新日期交給 `build.mjs`。⚠ **那個「16px、三態各一張線性遮罩」2026-08-17 整組換掉了，見下一列**；角形那半邊一個值都沒動。⚠⚠ **標記內距是 `calc((34px − 1.6em − 2px) / 2)`，不要寫死 px** —— 字級一改塊高就會被頂開；高與字是一組的，字級上限 (塊高−2)/1.6。⚠⚠ **`.info-card` 那行 `min-width: 0` 不能刪** —— grid 項目預設 `min-width: auto`，一排七顆不換行撐開的是整張卡不是捲軸（375 上溢出 296px）。⚠ 角形掛在外層 `.swrap` 上（`display: contents`）：`mask-image` 會作用在元素**自己的偽元素**上，掛在捲動列身上會被自己的遮罩淡掉。⚠ `.swrap` 一定要 `<span>` 不能 `<i>` —— `display: contents` 不擋繼承，整排字會變斜。⚠ 角形用 **CSS 邊框**畫不是背景圖：只有 viewBox 沒有 width/height 的 SVG，Safari 當背景圖時有時直接不畫。⚠ 角形垂直對的是**字面中線**、由 JS 現量寫進 `--ar-y`（低 2.31px＝捲動列下內距 1.20 ＋ 中文字面中線高於行框中線 1.11）。⚠ 透明度跟著**各自那一側的餘量**連續變化（斜坡 48px），**不看手有沒有在動** —— 往右滑到底時左邊那顆是滿的。⚠ **沒選上的七項不要順手加回來**：手機行動條、頁首那顆 LINE、地圖入口做成按鈕、頁尾補上 LINE、第一屏的「下面還有」、**320px 頁首修正**（那一項是破圖不是美感問題，實測重疊 −1.2px，使用者看過數字仍未選）、文章卡標題固定兩行 | `/history/cta-mobile.html` |
| **橫捲那排標記的邊緣淡出** | **長度跟著捲動連續長出來 ＋ smoothstep ＋ 上限 10px**（2026-08-17 當天開提案頁、當天定案上線，只動 `index.html`）。**三條尺使用者全部選在提案的預設那一格**，等於把「對照現況」那一組（16 ＋ 直線 ＋ 立刻整段）整組否決掉。起因是使用者放大截圖：「標籤的模糊不自然，看起來不是連續的漸層，從『主』這個字的右緣就開始模糊了，覺得拖太長。」逐像素量過，他講的三件事全部成立，而且是**三個不同的成因**：① 改動前用 `data-sx`（start／mid／end／none）換四張固定 16px 的線性遮罩，`scrollLeft > 1` 那一刻左緣的淡出**整段 16px 瞬間跳出來** —— 他那張截圖正是 `scrollLeft ≈ 2`，左邊什麼都還沒捲掉、「全部」整顆看得見，卻已經被洗掉 16px；② **線性漸層的兩端各是一條可見的線**（值連續、一階導數跳階 ＝ 馬赫帶，同第九節第 10 條）；③ 16px 會**吃進第一個字**（「全部」只有 56.4px 寬、左右內距 12.8，斜坡終點 30px 離「主」的右緣 32.27px 只差 2.3px，**他量的位置逐字正確**），而填色標記上的**白字褪得比色塊快**，所以看起來不是「切口收軟」是「這幾個字糊掉了」。改法：**`--fade-l`／`--fade-r` ＝ 那一側還剩多少可以滑**（上限 `--fade-len`，捲 2px 就只淡 2px），和兩顆角形的 `--ar-fl`／`--ar-fr` 同一個邏輯，`data-sx` 整個拿掉；遮罩換成八個色停逼近的 **smoothstep（3t²−2t³）**，端點斜率 1.00 → 0.22；長度 16 → **10px**（小於左內距 12.8，斜坡停在字面之前）。⚠⚠ **`--fade-len` 是這條尺唯一的出處，JS 用 `getPropertyValue` 讀它** —— 要改長度只改樣式表那一行，不要在腳本裡再寫一個數字。⚠ **顏色一個都沒動**，改的是 alpha 的形狀與長度。⚠ 沒有 JS 時停在 `0px／10px` ＝ 改動前的起點那一態，不會壞。⚠ 驗收：`scrollLeft` 0→453 逐格量 `--fade-l/r` 連續、無跳階；1112×834（八顆排得下、沒有溢出那一格）對改動前**逐像素 0 個有差**，393／834 只有右緣最後 48 裝置 px（＝舊的 16px 斜坡）有差；七個視窗版面 0 項有差、無水平捲動 | 本檔第九節第 17 條、`/history/tag-fade.html` |
| **點標籤換頁，標籤不要跳** | **接回按下去那一刻那一排捲到哪裡**（2026-08-31 當天開提案頁、當天定案上線，只動 `index.html` 那一排底下的腳本，七科著陸頁跑 `node tools/topics.mjs` 跟上；**CSS 一行都沒動、顏色一個都沒加**）。起因是使用者的螢幕錄影：「點選標籤的時候因為我們現在進到著陸頁……但標籤就不一定是在點進去之前的位置，因為是不同的網頁。」⚠ **成因不是標籤自己會跳，是 2026-08-21 那一輪的 `bring()`**：它一律把「目前這一科」擺到那一排最左邊（`pad = min(48, 那一排寬 × .12)`，390 上 43.4px），而使用者是按著**畫面中間**那一顆進來的。逐幀量到牙周治療從 x≈155 跳到 x≈63；提案頁面板量到現況 **−37px**（首頁→牙周）、**−283px**（牙周→兒牙）。做法：按下去的當下把「這一顆的左緣離那一排可視範圍左緣多遠」記進 `sessionStorage`（`fangren:chips-x`，值 `{path, spec, t, off}`），新的一頁在**那一排的正下方**就算回去 —— 第一幀就定位（同第九節那三件「第一幀就要對」的事）。⚠⚠ **記的是那一顆自己的偏移量，不是 `scrollLeft`** —— 兩頁的那一排不見得一樣寬（著陸頁的搜尋框和標題同一列），以那一顆為錨才對得準，哪天標籤的寬度或字級改了也仍然對。⚠⚠ **用一次就丟**（讀出來立刻 `removeItem`、只認 60 秒內的）：重新整理、上一頁／下一頁、直接開網址、搜尋進來一律**退回原本的 `bring()`** —— 不這樣做的話「重新整理也會接回原位」，那是假的，真正的行為只在「按了標籤換頁」時才該發生。⚠ **`load` 要再算一次（字型到齊寬度會變）但不能搶**：記下自己寫進去的值，相差超過 1px 就當成是使用者自己滑的，整段放手（`bring()` 也一起收進同一個 `place()`）。⚠ **安全網**：按到的那一顆若原本只露出半截（它在淡出的斜坡底下也按得到），換頁之後讓它整顆看得到 —— 位移就是被切掉的那一截（實測 20~35px），比原本那一跳小一個數量級；整顆看得到時這一條不會生效。⚠ **沒有動 `bring()` 本身**（直接開網址／搜尋進來的人仍然一眼看到自己在哪一科），**沒有碰上一頁／下一頁**（那是瀏覽器的捲動位置還原），**沒有加任何動畫**（位置在第一幀就是對的）。⚠ 沒有 JS 或 `sessionStorage` 被擋掉（無痕）時整段不作用 ＝ 改動前。⚠ 驗收：四個視窗（390／375／744／1440）整顆看得到的標籤逐格 **差 0.00px**；淡出遮罩與兩顆角形的變數跟著對；1440（八顆排得下）完全不作用；水平溢出 0。⚠ **改完 `index.html` 要跑 `node tools/topics.mjs`**，七科的著陸頁才會跟上 | `/history/chip-carry.html` |
| **夜間模式** | **2026-09-15 十二輪定案上線**（起點是使用者給的 snowmed-taiwan.com）。配色 **Ⓑ 窄帶藍灰**（底 `#17191d`、卡 `#282b31`），七科字階與色票裡被當成字用的幾支**同色相只提亮度到 4.5**；沒選到的藥丸 ＝ 套色 20% 淡色塊；地圖 Ⓜ1 路比街廓亮（**停車場那三塊 2026-09-15 稍晚整組重算，見下一列**）；著陸頁浮水印換成**實心**（線圍起來的地方填 `#e2e5e6`、線挖空）濃度 **.09**；開關是 **iOS 滑桿、無字，太陽月亮畫在圓鈕上**，軌道白天琥珀 `#c28229`、夜間路牌藍 `#365685`（**夜間那顆圓鈕 2026-09-15 稍晚反過來了，見下面那一列**），放在**首頁與七科「主題與科別」那一行最右邊**（七科接在搜尋框右邊）、**文章頁頂端那一行最右邊**；**預設跟著系統 prefers-color-scheme（電腦也一樣）**，按過記在 **localStorage `fangren:theme`**。⚠⚠ **產生器 `node tools/night-mode.mjs`**：寫 `index.html`／`assets/style.css`／`posts/*`（三處都用 `NIGHT:START`～`NIGHT:END` 包起來，**不要手改**）、`assets/theme.js`、`assets/lineart-<spec>-night.png`；`--check` 只比對。**跑完一定接著跑 `node tools/topics.mjs` 與 `node tools/build.mjs`**。⚠⚠ **日後動到任何白天的科別色或換線稿，都要重跑它**（夜間那一階是現算的）。⚠⚠ **白天一個值都沒動** —— 夜間規則全部掛在 `html[data-theme="dark"]` 底下。⚠ 頁首放不下開關（390 寬時診所名和選單疊 8px），**不要搬回頁首**。⚠ 著陸頁的 `data-spec` 掛在 `<body>` 自己身上，夜間提亮的選擇器不能只寫後代（踩過：七科的套色字停在白天的深階）。⚠ 圓鈕位置看 `html[data-theme]` 不看 `aria-checked`（theme.js 是 defer，看 aria 會先閃一下白天的位置）。⚠ 文章頁的開關在 `<main>` 裡，第一次寫入動到內容雜湊 —— 十三篇的日期照第五節還原過。⚠⚠ **開關掛上去之後那一行排不下了，而且不是只有 320** —— 一行的最小螢幕寬是 416px，所以 430 以外每一支手機都會掉第二列；**2026-09-15 稍晚已經治掉，見下面「文章頁頂端那一行」那一列**。⚠ 404 與 `history/` 沒有接夜間模式（沒有那段 `<head>` 腳本，一律白天） | `/history/night-mode.html`、PALETTE.md 第六之二十三節 |
| **夜間地圖的停車場** | **2026-09-15 稍晚兩輪定案上線**（夜間模式定案當天）。起因是使用者的手機截圖：「停車場的藍好像在夜間模式變得不太清楚，雖然停車場因為圖形不小還算可以辨認，**但入口的指標就幾乎看不到了**」，第二輪「沒點到的停車場感覺可以暗一點」。⚠⚠⚠ **成因是同一個，而且是第九節第 28 條 ② 那一種**：夜間那一輪把白天那兩支（路牌藍 `#365685`、停車場灰 `#7a7d77`）**原封不動釘回來**，可是它們周圍的東西全部換過了 —— **白天的路面比街廓暗（96.2／83.3）、夜間比街廓亮 16 L\*（17.5／33.5）**。同一支顏色因此換了個意思，量出來是三件：**① 入口指標壓在路面上 4.79 → 1.11**（1.00 ＝ 完全看不見）／**② 點到的方塊對街廓 6.76 → 1.91**，而且從「比沒點到暗 3.6 L\*」變成「暗 15.8」＝ **深底上暗就是退後，點下去那一塊反而沉下去、語意翻面**／**③ 沒點到的那三塊對街廓 3.40 ＝ 整張圖上最亮的一群大塊，比診所自己那塊綠（2.14）還跳** —— 一張「診所在哪」的地圖份量排錯了。定案（三格都是他在提案頁上挑的、三個值都**算出來**不手填）：**入口指標 `#819dd1`**（對路面 3.00 ＝ 裝飾性圖形的門檻）／**點到的塊 `#607daf`**（釘在**白天那支灰的亮度 L\* 52.0** 上、只換色相）／**沒點到的塊 `#5a5d58`**（對街廓 2.12 ＝ 診所那塊綠的份量，順帶把白 P 從 3.80 修到 **6.08**）。⚠⚠⚠ **三個值不能共用一支，那是幾何不是偏好**：方塊的底是街廓、箭頭的底是路面，夜間那兩塊差 16 L\*；要箭頭對路面過 3:1 得走到 L\* 64.5，而**白色的 P 壓在那個亮度上只剩 2.49** —— **白 P 就是方塊那一把尺的天花板**，這件事要在送尺出去的時候先講（第 28 條 ③）。⚠⚠ **入口指標沒有第二條路**：要比路面（L\* 33.5）暗到 3:1 需要負的亮度，**只能提亮**。⚠⚠ **路面附近是死區**：沒點到那一支走到 L\* 33.5 附近時「貼著路的那一邊」就沒有邊界了（落選的 `#4e504b` 對路面 1.01），三塊都有一角是貼著路的。⚠⚠ **錨點只有三個而且都有出處**（白天那支灰／診所那塊綠／路面），改任何一個那三支會自己跟著動；⚠ 點到那一支釘的是**白天那一支**不是夜間的沒點到 —— 不然沒點到一暗它會跟著掉下去。⚠ **已知取捨**：白 P 壓在點到那一塊上 3.78，和沒點到第一版的 3.80 同一級（他挑過），**不要拿對比度來訂正**。⚠ 夜間的 `--map-park`（`#7a96ca`）**只剩路線的點與牌子在用**，不再是「點到的那一塊」。⚠ **改的是 `tools/night-mode.mjs`，跑完接著跑 `node tools/topics.mjs` 與 `node tools/build.mjs`**；文章頁沒有地圖，`assets/style.css` 與 `posts/*` 一個字都沒動。⚠ 驗收：白天那張卡 390×844 DPR3 **逐位元組相同**、夜間首頁與七科量到的三個 fill 逐字 ＝ 定案值 | `/history/night-map-park.html`、PALETTE.md 第六之二十三節 |
| **夜間開關的圓鈕** | **2026-09-15 稍晚兩輪定案上線**（夜間模式定案當天，第三件）。起因是使用者的兩張手機截圖：「白天的按鈕滑桿沒什麼問題　但夜間的滑桿　**月亮那塊圓的地方很亮**，只有月亮是夜間模式的深藍色　**如果反過來　圓形是深藍色　月亮　淡藍色**效果怎麼樣？」定案 **Ⓖ2：圓鈕 `#012c53`、月亮 `#9cb7ed` 而且填實**（不是線稿）；**軌道 `#365685` 沒動、白天那一態一個值都沒動**（150×105 的開關逐像素 0 個有差）。⚠ 兩支都是**路牌藍 `#365685` 的同色相同彩度、只動亮度**，沒有新增顏色。⚠⚠⚠ **這一輪唯一真正的取捨要先講（第 28 條 ③）**：現況那顆白鈕**對紙底 17.60，是整顆開關在夜間頁面上最顯眼的東西**，反過來之後最亮的一塊掉到 **2.37**（軌道），開關改由月亮被看見。⚠⚠ **圓鈕能有多深是幾何決定的不是偏好** —— 軌道 L\* 36.2、往下的地板是 0，所以「圓鈕比軌道暗」**最多只到 1.9:1**，再暗就比紙底（L\* 8.7）還暗、對比也不再增加；**沒有任何一格到得了 3:1**，圓的邊界一定比白鈕那一版弱，**不要拿對比度來訂正它**（同兒牙填色白字 3.22、分享卡帶子 4.40）。⚠⚠ **份量由實心月亮接手**：實心的墨佔圓鈕 **15.6%**、線稿只有 **10.5%**（Path2D ＋ 離屏 canvas 現場數像素，不是估的）—— 也因此**同一支藍在實心版上讀起來比線稿亮一階**，第二輪才要重新給一次「月亮多亮」的尺（落選 `#8aa6da` 5.75、`#b7ceff` 8.94）。⚠ **落選的 Ⓔ（圓鈕加一圈淡藍細框 `#8aa6da` 1px inset）是唯一把邊界補回 3:1 以上的做法**，日後有人回報「看不出那是一顆鈕」那條路還在。⚠⚠⚠ **填實那一條一定要寫成 `.tt-knob svg.tt-moon`** —— 只寫 `.tt-moon` 的話權重輸給 `.tt-knob svg { fill: none }`，月亮**靜靜地退回線稿**：背景色、字色、位移每一個數字都還是對的、畫面完全正常，**只有量 computed 的 `fill` 才看得出來**（通則：**驗一個「換了畫法」的改動，要量那個畫法本身**，不要只量顏色）。⚠ 圓鈕的 `transition` 要補 `background-color`，不然翻面那一下底色是硬切的。⚠ **改的是 `tools/night-mode.mjs`，跑完接著跑 `node tools/topics.mjs` 與 `node tools/build.mjs`**；這一輪只動 `index.html` 與 `assets/style.css` 的 NIGHT 區塊（CSS），**`posts/*` 的標記一個字都沒動，所以十三篇的日期沒有跳**。⚠ 提案頁是**獨立寫的、不是 `index.html` 的複本**（同 clinic-schedule），開關的幾何與 SVG 路徑逐字照抄產生器、只換狀態選擇器，所以六格每一顆都是**真的開關**（按一下翻到白天，當場看得到白天沒有被動到）。⚠ 踩過兩個不報錯的：**CSS 註解裡寫了註解的結束記號**（整段提早關掉，軌道量到 0×0、多 26px 水平捲動）、**模板字串的 CSS 註解裡不能有反引號**（這一站第六次）| `/history/night-toggle.html`、PALETTE.md 第六之二十三節 |
| **文章頁頂端那一行** | **2026-09-15 稍晚一輪定案上線**（夜間模式定案當天，第四件）。起因是使用者附文章頁的手機截圖：「首頁那一行還有空間可以壓縮給 白天/夜間模式切換滑桿嗎」。⚠⚠⚠ **量出來的第一件事是紀錄寫錯了** —— 上面那一列原本寫著「320 寬才會掉」，實際上 390 可用 **362px**、排一列要 **387px**、**差 25px**，**一行的最小螢幕寬 416px，430 以外每一支手機都會掉**（各段：首頁 26.3｜› 5.3｜科別藥丸 70.6｜**日期 110.6**｜瀏覽數 68.3｜開關 50.0｜五道溝 56.0）。⚠⚠ **而且它會再變差**：日期是全列最長的一段，現在最長只到「8 月 27 日」，**10 月起兩位數的月份進來要 412px、連 430 都會掉**。定案 **Ⓒ ＝ 兩件**：**十三篇 crumbs 那個 `<time>` 的字換成 `2026/09/07`**（`datetime` 屬性一個字都沒動）＋ **`.crumbs` 的橫向溝 `.7rem` → `.5rem`**（縱向那個 `.45rem` 是折行後的列距，**刻意沒有跟著收**），合起來省 **51px**，最小螢幕寬 416 → **364**（四位數瀏覽數 373）。⚠⚠ **那個日期寫法不是新發明的** —— 首頁卡片與延伸閱讀早就在印它（`tools/build.mjs` 的 `slashDate()`），所以這一輪要挑的不是格式是「要不要跟站上一致」；**通則：提案之前先去站上找有沒有現成的寫法，找得到就不要自己發明一個。** 它順手把「10 月會變差」整件事解掉了，因為**這一種是固定寬的**，12/11 和 9/7 一樣寬。⚠ **已知取捨：360 與 320 仍然是兩列**，使用者看過數字之後選的 —— 要進 360 得走 Ⓓ（「次瀏覽」收成眼睛圖示，再省 25px），而那是**把站上刻意關掉的 `.views svg` 重新打開**、又和首頁卡片的「數字 · 次瀏覽」不一致；**320 連 Ⓓ 都進不來**（要收到 292px）。⚠⚠ **落選的 Ⓔ 不是被否決，是另一個方向**：不壓縮任何東西、只把開關釘在右上角讓文字自己折下去，**320 到 430 每一個寬度開關都在第一列，而且純 CSS、一個字都不必改** —— 日後要再往那一行掛東西、或要照顧 320，那條路還在（`.crumbs` 給 `position: relative` ＋ 右內距 61px、開關絕對定位，再用一根零寬 30px 高的 `::before` 把第一列撐到和開關一樣高，負的右外距抵消它多佔的那道溝）。⚠⚠ **日期那一行在 `<main>` 裡，十三篇的內容雜湊都動了** —— 照第五節把 `tools/build-manifest.json` 的 `updated` 還原（**雜湊留新的**）再跑一次 build，十三篇的最後更新日一個都沒有跳（判準：換一種日期寫法是版型連動，不算內容變了）。⚠ **這一輪沒有動 `tools/night-mode.mjs`**：`.crumbs` 的溝在基底樣式裡、不在 NIGHT 區塊（`--check` 仍然全綠），`index.html` 與 `topics/` 一個字都沒動。⚠ 驗收：四篇文章 × 七個寬度 × 三位數與四位數瀏覽數 —— **430／414／393／390／375 一律一列、開關在第一列**，溝量到 8px、日期寫法逐篇正確、水平溢出 0、JS 錯誤 0 | `/history/crumbs-line.html` |
| **點選單那一下不要閃跳** | **六項一起上線**（2026-08-15，`preview/head-flash/` 九個候選、六版收斂）：**薄色改畫在頁首自己的 `background`**（漸層自己做出下緣淡出）／**薄色收滿的地方把濾鏡與遮罩整組拆掉**（`html.band-solid`，它涵蓋 `past-band`）／**窄帶壓上來就把薄色收到不透明**（同一條 smoothstep 一起收模糊與淡出）／**點選單先用目的地的捲動位置算一次頁首**（`paint(vy)`）／**那三個標籤瞬間跳**（只改這一次的 `scroll-behavior`）／**點下去就先亮**。⚠ 那個「閃」量出來是**三件事**：頁首漏畫 1~2 幀、頁首深淺慢一幀、燈回跳一幀。⚠ `::before` 與 `::after` 是負 z-index 的同一組，WebKit **整組一起丟**，顏色搬到隔壁沒有用，一定要畫在元素自己的 background 上。⚠ **不要加 `will-change`／`translateZ(0)`**，實測更糟（品牌標誌會整幀不見）。⚠ **只拆濾鏡不夠**，窄帶底下的字會透出來。⚠ **HERO 那一屏的玻璃一個值都沒動** | 本檔第九節第 13 條、`/history/head-flash.html` |
| **文章卡的「繼續讀」** | **日期那一列右邊一顆淡色塊的提示，配一個角形**（2026-08-27 當天開、當天五輪定案上線，提案頁 `preview/card-more-button/`）。起因是使用者在一般對話裡做了一版卡片版面調整（藥丸狀色塊 ＋ 箭頭 hover 位移）貼過來要團隊確認。定案九格全部是他在提案頁上挑的：**淡色塊**（9% 套色混進卡色）／**圓角 8.5px**／**字級 .78rem**／**胖瘦 1.85**／**角形 1.4px**／**放不下就掉下一行**／**日期與瀏覽之間用半形的點**／**角形箭頭**／**文字「繼續讀」**／**手機也放**。⚠⚠ **這顆不是連結，是 `<span aria-hidden>`** —— 文章卡本身就是 `<a class="card">`，裡面再放一個 `<a>` 是巢狀連結，瀏覽器會在解析時把它拆到外面；而且它指的是同一個網址，等於一張卡兩個連結、錨點文字還從標題退成「繼續讀」。做成純視覺提示之後，可按的一直是整張卡，**44px 觸控下限與螢幕閱讀器兩件事都不必另外處理**。⚠⚠ **那三個字用 CSS 的 `content` 產生，不要寫成文字節點** —— 首頁那支篩選讀的是整張卡的 `textContent`，寫成真文字的話搜「繼續」或「讀」會命中每一張卡（上線後實測三個查詢都是 0 張）。⚠⚠ **內距一律 em，不可以寫死 px**：原案是 `padding: 8px 17px`，字級一收內距不跟著收，**字愈小它愈胖**（.95rem 上塊高÷字級 2.65 → .78rem 上 2.88，而站上那一族是 1.80~2.41）。行高 1.6em，所以塊高÷字級 ＝ 1.6 + 2 × 上下內距；定案 1.85 ＝ `.125em`，左右 `.63em` ＝ `.card-tag` 那一族的比值。⚠⚠ **SVG 的 `stroke-width` 是 viewBox 的使用者單位，會跟著圖一起縮** —— 站上「往下滑」與「回到最上面」那兩顆角形是固定 18×9、寫 1.4 就是 1.4px；這一顆高度 `.58em` 跟著字級走，縮放比只有 0.402，照抄 1.4 畫出來**只剩 0.56px**。要 1.4px 就得寫 3.5。⚠ 沒有用 `vector-effect: non-scaling-stroke`，那會讓粗細不跟著字級走。⚠⚠ **那顆分隔點從全形「・」換成半形「·」**（`tools/build.mjs` 的卡片模板）—— 起因是使用者在 iPad 上「日期跟瀏覽次數中間間隔還大的，這邊空空看起來怪怪的」。全形字元自己就佔 16.2px（744 上），那一段合計 27.6px，**比當時缺的 18px 還多**。**通則：一列擠不下的時候，先把整列每一段都量一次再決定調哪一段，不要只盯著新加的那一個**（前一輪一路在調按鈕本身，方向不對）。⚠ 兩欄那一段（721~1040）是最緊的：**744（iPad mini）** 差 24.8px、**721** 差 36.3px，而字級 −.78 只買 6.8px、無箭頭 12.8、換「繼續讀」16.2、換「只有字」20.4 —— **沒有任何一條尺單獨救得回來**。定案這一組在 744 以上全部一行收得下，**721 仍有 3 張會掉到下一行靠右，那是選的行為不是破圖**。⚠ **已知取捨**：淡色塊配**兒童牙科**是 **4.14**，七科裡唯一低於 4.5 的（贋復 10.86／根管 7.36／矯正 8.26／口外 5.33／牙周 4.91／一般 7.12），使用者看過數字之後選的，同兒牙填色白字 3.22、線稿 4.24 —— **不要拿對比度來訂正它**。⚠ **圓角 8.5px 是定值，刻意沒進本檔最後那個乘 `--type-scale` 的圓角族**（提案頁上他點頭的就是 8.5），代價是電腦版與 iPad 上比旁邊的 `.card-tag` 小 1.06／1.59px。⚠ **延伸閱讀那三張卡沒有這一顆**，也沒有那顆分隔點 —— 它們是另一套 `rel-*` class，只有日期、沒有瀏覽次數，不是漏做。⚠ 顏色一個都沒新增。⚠ **改完 `index.html` 要跑 `node tools/topics.mjs`**，七科的著陸頁才會跟上。⚠ **本機驗收要先把瀏覽數補成真實值再量** —— 本機沒有 Worker，`.views` 被 `data-state="error"` 藏起來，那一列比正式站短；而且拿它當折行判斷的基準會全部誤判（藏起來的元素 rect 是 0,0,0,0） | `/history/card-more-button.html` |
| **頁首的搜尋入口（放大鏡）** | **三個選單右邊一顆只有圖示的放大鏡，點下去整條頁首變成搜尋列**（2026-08-26 當天開、當天兩輪定案上線，提案頁 `preview/head-search/`）。起因：「搜尋我還要拉回去主題科別標籤下面有點麻煩……點下去可以變成搜尋頁面。」六格全部是使用者在提案頁上挑的：**整條展開**（品牌也讓開）／**只有圖示、沒有玻璃框**／**打了字立刻捲到主題與科別**／**診所資訊一起搜**／**搜不到給三個出口**／**結果那一句改成可以點的三顆**。⚠⚠ **放大鏡是 `<a href="#topics">` 不是 `<button>`** —— 沒有 JS 就退回「跳到主題與科別」（＝使用者原本手動在做的事），有 JS 才 preventDefault。因此**頁首那支 scrollspy 的選擇器要排除它**（`a:not(.nav-q)`），不排除的話它會被當成第四個小節、燈會滑到放大鏡上。⚠⚠ **樣式要放在 `<head>`，不能塞在頁尾** —— 那個 `<form>` 在樣式表之前就被解析出來，關著的搜尋列會在開頁那 180ms 整條閃出來。⚠ **展開時要淡掉 `.site-nav` 整條，不是三個 `a`** —— `.nav-lamp` 是 nav 的直接子元素，只淡 a 的話它會單獨留在半透明的輸入框底下。⚠ **輸入框字級 16px 是硬條件**（iOS 對 <16px 的輸入框一聚焦就放大整頁），同 `.topic-search`。⚠ **站上那支篩選的 IIFE 一行都沒有改**：`assets/head-search.js` 把字寫進 `#q` 再送一個 input 事件，接在它後面（後掛的後跑）改寫那一句話。`.hs-n` 要排到下一個任務再寫，不然會被 `push()` 蓋掉。⚠⚠ **刻意沒有做同義詞對照表**（地址→永樂街、電話→05）—— 那會長出一份和畫面對不起來的第二真相，違反「篩得到什麼 ＝ 畫面上寫什麼」。「地址」「電話」在這一站是**圖示不是文字**、門診表寫的是「一二三四五」不是「週一」，這一類**擴大索引也搜不到**，只能靠出口。⚠ **索引診所資訊時要剔掉 `.hours-filter`**（那排科別標記是控制項不是內容，不剔的話「矯正」每次都會多命中一次看診時間）。⚠⚠ **文章頁一定要有這顆**：2026-08-15 定案「首頁切到文章頁，頁首不要變」，少一顆圖示比差 0.14px 嚴重。文章頁與著陸頁按下 Enter 是**送回首頁** `/?q=…#topics`（那些頁面上沒有全站的清單可以篩），只有首頁就地篩。⚠ 著陸頁自己那個 `.topic-search` 沒有動，它做的是「在這一科裡找」。⚠ **上線時踩到一件**：`assets/style.css` 的 `.brand-text` 帶著 `min-width: 0`（`index.html` 那一份沒有），多一顆放大鏡之後 320 寬上「芳仁牙醫診所」被裁掉（scrollWidth 79 > clientWidth 66），拿掉才對得起來 —— **兩份樣式表的頁首要逐項相同，差一個 `min-width` 平常看不出來，多一個元素就爆出來**。⚠ 顏色一個都沒新增：搜尋列吃頁首那三個玻璃框那一組，結果標記與出口吃 chip 那一組。⚠ **改完 `index.html` 要跑 `node tools/topics.mjs`**，七科的著陸頁才會跟上 | `/history/head-search.html`、`assets/head-search.js` |
| **首頁切到文章頁，頁首不要變** | **文章頁的頁首改成和首頁「捲動後」逐項相同**（2026-08-15 上線，只動 `assets/style.css`）。起因是使用者的螢幕錄影：「首頁切到文章頁的時候 banner 有變化」。逐幀比對 ＋ 九個寬度逐項量出來，**那三個玻璃框是對的（框高／圓角／線兩邊本來就相同），壞的是它周圍的每一項** —— 這一份抄到的是首頁**還沒捲動**那一組尺寸，可是使用者是從捲到下面的首頁點進來的。375 上的差：頁首高 59.17→62、標誌 27.73→30、診所名 12.86→14.25、標誌左緣 20→16、選單右緣 20→16；1440 上標誌左緣 180→208（跳 28px），而且**首頁是一行（診所名｜地名）、這裡是兩行**。改法：`--wide` 68rem→**1160px**、`--pad`→**`clamp(1.25rem, 3vw, 2.5rem)`**（＝首頁的 `--shell`／`--pad`），品牌與選單的每一個值從 `html.scrolled` 與首頁手機那一段**逐字抄**，`a` 補回 `padding-block: .35rem`，電腦版品牌收成一行（使用者指定）。⚠ **版心是整頁一起換的**（使用者選的）：只換頁首的話，同一頁的頁尾又對不上頁首（1440 上差 28px）。代價是內文欄窄一點（375 上 343→335、1440 上 640→624），`.post-hero` 吃同一個 `--pad`，「圖寬 ＝ 內文欄寬」仍然成立。⚠ **副標要抄 2026-08-13 抬過一階的那一組** `clamp(10px, 2.66vw, 11.7px)`，不是原本的 `clamp(9px, 2.35368vw, 11.25px)`。⚠ **兩行的 justify 兩行都要寫**：副標抬階之後比值 0.776/0.794 已經 > 0.75，比較寬的是副標那一行。⚠ 剩一格 **0.14px**（選單總寬）不追：`letter-spacing: .01em` 在 body 16px 與 17px 上算出 0.16／0.17px，**不要硬編 `.16px`**。⚠ 首頁那顆**亮起來的**選單項這裡仍然沒有，那是跨頁連結、沒有一節可以偵測，不是漏做 | 本檔第九節第 14 條 |
| **手機版的字級與地圖** | **七輪定案上線**（2026-08-15，`preview/card-type-mobile/`，五條尺一次選完）。**只動 ≤720px，電腦版與 iPad 逐像素 0 差異**。文章卡 **22.4／17.6**（行高 1.5／1.8，Ⓑ；落選 Ⓐ 19.8/16、Ⓒ 25/19 ＝ 參考站原尺寸）＋日期 **13.12**＋~~縮圖蓋過框線（border 改 inset box-shadow，照片 333→335）~~ ⚠ **這一項 2026-08-16 退回去了**（使用者放大截圖：「圖片超出卡片範圍了」）—— 那個做法讓框線只有卡身那半截有、照片那半截沒有，照片的左右緣正好停在框線**外面一圈**；1× 看是「照片滿到邊」，放大之後就是亮色照片從灰框裡凸出來的一條階。手機已吃回基底的 `1px solid var(--rule)`，照片重新內縮 1px（375 上 335／333）。**兩件事不可能同時成立：照片碰得到卡的外緣，就一定在框線外面。要走回去見 `git show 64a408d`。**醫師介紹／看診時段／位置與周邊停車三塊的**內文一律 16**（第一版做 17.6，使用者說太大），連動：醫師名 19.84、`--sk-fs` 16、標籤 15.62、`.doc-role` 抬升 .158→**.21em**。門診表：早午晚 `display:none`、左欄 `col.lab` **6rem**、點 **.75rem**、說明 16。地圖：裁掉左右到**可見 430 單位**（左右各餘 18.5）、牌子的字 **×1.4**、診所名牌尾巴 8.5→13、壹車房整塊往上 18（⚠⚠ 那個 −18 是 `--map-dy`，意思是**連尖端一起**往上；2026-09-18 起它旁邊多一個**只搬盒子**的 `--map-drop: 16`，見「位置與周邊停車」那一列第五件 —— **兩個不要合成一個**）。⚠ 參考站的字級是**從截圖反推**的（923px ÷ 393 CSS px ＝ 2.348 倍，再用整行幾個全形字回推），不是目測。⚠⚠ **牌子的幾何有一半寫死在絕對單位上**（底線間隙 2.2、兩行距 23、盒子到尖端），字一放大就全部不成比例 —— 三處都改成跟著字級走，其中「盒子貼著尖端」是由 JS 沿尾巴方向平移 `.rl-body`（`data-tail` ＝ 各自原本量到的 15.33／15.33／17.8／8.5，所以電腦版 dx=dy=0）。⚠⚠ **`preserveAspectRatio="slice"` 是 markup 屬性，電腦版也吃得到**（盒子高 531.43 是小數，浮點誤差會多縮放千分之一，整張圖 0.35% 的邊緣像素有差）—— 裁切改用外層 `.map-win` 當視窗（電腦版 `display: contents`）。⚠ `translate(0 0)` 也會重新光柵化，位移 0 就不要寫 transform。⚠ 診所名牌的字是 15px、停車場是 16px，所以底線間隙的比例是 **.14667 vs .1375**，不一樣。⚠ 門診表左欄的字是 `nowrap` ＋ 表格 `table-layout: fixed`，左欄太窄不會有水平捲動、會**橫著蓋到第一顆點**。⚠ 「間隔再縮短」報酬遞減：間隔 ＝（左欄 − 字寬）＋（日子欄 − 點）÷2，左欄每收 8px 只少 7px | `/history/card-type-mobile.html` |
| **手機版的卡片寬度** | **左右內距 20 → 14px，關在 `@media (max-width: 720px)`（2026-08-17 上線）**。使用者拿同一支手機的兩張截圖（fangren.net 與 blog.ichentsai.tw）：「它的卡片幅度比較大，調整成一樣的。」＝ 前一天電腦版那一輪的手機版，同一個參考站。⚠⚠ **手機只有一條尺可以動**：`--shell`（1160／1280）在手機上從來沒有生效過，版心一直是視窗自己，所以卡片寬 ＝ **視窗寬 − 內距×2** ——寫死 `.card` 的寬一樣會讓卡片脫離版心（同電腦版那一輪的理由）。四格 20（現況）／16／14／12，使用者在自己手機上和參考站並排比過選 **Ⓒ 14**（393 上卡片 353 → **365**、文字欄 314.2 → **326.2**、縮圖 351 → 363、標誌左緣 20 → 14、選單右緣 373 → 379）。⚠⚠ **12px 是這條尺的地板**：再收到 8px，393 寬上頁首（品牌 140.6 ＋ 選單 177.8）會把版面撐成 397px、整頁多 4px 水平捲動（393／375／320 都一樣，最小那台不是最先出事的）。定案的 14 離牆還有 2px。⚠⚠ **參考站這一側量不到** —— 雲端 session 的網路政策擋掉 `blog.ichentsai.tw`（CONNECT 回 403），所以這一輪**沒有**像電腦版那樣的兩站逐項比對表，是靠提案頁讓使用者自己並排比。日後要重驗，量的是參考站手機版的左右留白。⚠⚠ **`assets/style.css` 要逐字照抄同一個斷點**，否則 2026-08-15 那條「首頁切到文章頁，頁首不要變」立刻失效（標誌左緣會差 6px）；驗收五個手機寬度逐項比對，兩頁頁首完全相同，只剩那 0.14px 的字距差（早就決定不追）。⚠ **文章頁的內文欄與 HERO 圖跟著寬 12px**（375 上 335 → 347），「圖寬 ＝ 內文欄寬」仍然成立。⚠ **順手修掉 `sizes` 裡寫死的舊內距**：`calc(100vw - 2 * clamp(1.25rem, 3vw, 2.5rem))` → **`calc(100vw - 28px)`**（文章 HERO 與延伸閱讀兩處）。這不是 3.4% 的無關痛癢 —— 430 寬 DPR2 上舊字串算 780 會挑 800w、實際要 804，正好跨過候選界線。⚠ 文章 HERO 那一行在 `<main>` 裡，七篇的日期照第五節的程序還原過。⚠ 電腦版與 iPad（1440／1280／1112／1041／834／744）逐選擇器比對 **0 項有差**（唯一浮動的 `.hero-photo img` ±0.3px 是那條 10% 推近動畫的取樣雜訊） | `/history/card-width-mobile.html` |
| **電腦版的版心與卡片寬度** | **版心 1160 → 1280px、左右內距 → 24px，關在 `@media (min-width: 1041px)`（2026-08-16 上線）**。使用者拿同一個視窗的兩張截圖（fangren.net 與 blog.ichentsai.tw）：「卡片版面明顯比蔡依橙 2.0 小，比對調整成一樣的。」⚠ **先量再做** —— 兩站逐項比對之後差的**只有一件事**：字級、圓角（13.5 vs 14）、框線、縮圖比例在 2026-08-15「+12%」那一輪就已經對齊了，剩下沒對上的是卡片寬（1440 上 341.08 vs 394.66，小 15.7%）。⚠⚠ **卡片寬不是可以直接寫的值**：＝（版心 − 內距×2 − 欄距×2）÷ 3 —— 直接寫死 `.card` 的寬會讓卡片脫離版心，和上面的「主題與科別」、下面的醫師卡與診所資訊對不齊，所以改的是**版心與內距**。定案 1280／24px，實測 1280 上 388.88 vs 389.66、1440 上 393.88 vs 394.66（差 −0.78 ＝ 欄距 25.2 vs 24，**刻意不追**：1.4rem 是全站的間距族，為了 0.8px 開一個沒有人會記得的例外）。⚠ **版心是整站骨架**，這一改同時動到頁首、頁尾、HERO 窄帶、主題與科別、醫師介紹與診所資訊 —— 刻意的，只放寬卡片會讓它比同頁其他區塊寬出一截。⚠⚠ **`assets/style.css` 的 `--wide`／`--pad` 要逐字照抄同一個斷點**，否則 2026-08-15 那條「首頁切到文章頁，頁首不要變」立刻失效（1440 上標誌左緣差 60px）；驗收 1280 上兩頁標誌左緣都是 24、選單右緣都是 1241。⚠ **內文欄跟著寬 32px**（624 → 656）：`--content` 44rem 沒動，但它的左右內距吃同一個 `--pad`。⚠ **三個 `sizes` 一起重量**、斷點從 1160 換成 **1041**（＝三欄的下界）：首頁卡 373 → **393**、延伸閱讀 350 → **396**、文章 HERO 624 → **656**（原本那個 373 在 +12% 那一輪就已經對不上了，實測 341）；三處在 DPR 1／2／3 都不改變挑哪一張檔，是把量測值寫對、不是修 bug。⚠ 文章 HERO 那一行在 `<main>` 裡，改它會動到內容雜湊 —— 七篇的日期照第五節的程序還原過。⚠ **iPad 橫放（1112）吃得到這一段**（內距 33.36 → 24，卡片 +1.9%），手機與 iPad 直放整段吃不到 | `/history/card-width-desktop.html` |
| **電腦版的字級** | **整體放大 12%（根字級 16 → 18px，2026-08-15 上線）**。使用者拿 blog.ichentsai.tw 當參考站（那一站 1425 上：卡片標題 19／摘要 15／日期 14／chips 16、塊高 44.4／選單 15／頁尾 14／文章內文 17），五段（現況／+6／+12／+19／+25%）比過之後選 **+12%**：卡片標題 16.32→18.36、摘要 13.60→15.30、標記 14.40→16.20、選單 13.76→15.48。⚠ 全站幾乎都是 rem，所以主尺**只改一個 `html { font-size }`**，行距、內距、卡片留白會等比例跟上 —— 不是只有字變大；`body` 原本寫死 16px 要一起改成 `1rem`。寫死 px 的幾何另外接上倍率：頁首高 76／60、窄帶高 84、兩排藥丸塊高 34、圓角族 6~12px、頁首標誌 38／32、小節標題的枝條寬 3px。⚠ **覆寫 `:root` 的變數不能寫 `html`** —— `:root` 是虛擬類別（0,1,0），權重比 `html`（0,0,1）高，排在後面也贏不了（第一版就是這樣，頁首高原封不動停在 76）。⚠ 藥丸內距維持 `calc((34px × 倍率 − 1.6em − 2px) / 2)` 那條回推式，**不要換算成 px**。⚠ `.card-map h3` 的圓角要排在 `.info-card h3` **後面**（那張卡是 `info-card card-map`，兩條都命中）。⚠⚠ **這一段整組關在電腦版那道 @media 裡，而且放在整份樣式表的最後 —— 靠順序決勝，往前搬就失效。**⚠ **唯一真的會破圖的是窄帶**：地址電話是絕對定位、和三格同一列，兩邊的**字**相距現況 81.4px → +12% **18.9**（已經比群內那 1.9rem＝30.4 還窄，分組讀不出來了）→ +19% **−12.3 疊到字**。同一輪因此把它改成**置中一列**夾在瀏覽數與三格之間（照手機／iPad 已經在用的排法），窄帶 84 → 105.9px；721~1159 那一段要把兩行版順手設的溝（`.ico` 固定寬 ＋ 連結 `gap: 0`）還原。⚠ **地圖 SVG 裡的字**（路名、停車場牌子、P）與**搜尋框的 `min-height: 44px`** 刻意不跟著放大：前者是 viewBox 內的使用者單位（那張圖 42 輪才排好），後者是觸控目標下限不是造型。⚠⚠ **文章頁（`assets/style.css`）還沒做**（iPad 見下一列、手機由另一台同日逐項定案）—— 所以 2026-08-15 上午那條「首頁切到文章頁，頁首不要變」**目前不成立**（標誌 36 vs 32、品牌名 17.28 vs 15.36、選單 15.48 vs 13.76、捲動後頁首高 67.5 vs 60）。要補就是把同一段搬進 `assets/style.css` 的電腦版 | `/history/type-scale.html` |
| **六篇文章的 HERO** | **手刻的幾何 SVG 全部換成點陣插畫**（2026-08-16，兩天七頁提案、六篇湊齊一次上線）。`assets/hero-*-photo-{800,1600,2000}.jpg`，推導在 `/history/hero-photos.html`。⚠ **六張的明暗沒有統一是使用者選的**（刷牙＝明亮浴室、定期檢查＝白天、牙齦＝偏暖、擴弓＝混合、換牙＝夜晚吊燈、缺牙＝夜市與暖燈）—— 要統一是回頭重生成、不是後製調色。⚠⚠ **`sizes` 要量過再寫**：文章欄寬上限是 `--content` 44rem ＝ 704px，721~1159 那一段圖**不會跟著視窗長**（900 上實測 650，寫 `calc(100vw - 2*--pad)` 會算成 846）；定案三段式 `(min-width:1160px) 624px, (min-width:721px) 660px, calc(…)`。首頁卡 `46vw`／`92vw`、延伸閱讀 `350px`／`30vw` 也都是量出來的。⚠⚠ **改 `post-meta` 的 `hero` 會動到內容雜湊，六篇的「最後更新」一起跳成當天** —— 照第五節的陷阱程序還原（這一輪做了兩次）。⚠ `og:image` 改指 `-1600.jpg`，寬高由 `build.mjs` 新增的 `jpegSize()` 讀檔頭算（1600×**893**，不是 900）。⚠ **舊的六張 `hero-*.svg`、六張 `-1600.png` 與 `tools/og-images.mjs` 都還留著沒刪**，要不要清掉還沒問過使用者 | ILLUSTRATION.md 第七節、`/history/hero-photos.html` |
| **文章 HERO 的比例** | **不再只有 16:9 —— 2026-09-05 起多一種 4:3**（〈三個月一次的洗牙與塗氟〉那張，`assets/hero-recall-photo-{2000,1600,800}.jpg`，2000×1493）。使用者：「我們不需要這麼拘泥於每次都用橫幅的，用一些比較符合新的我們概念上的垂直比例拉高點的尺寸來畫畫看。」⚠⚠ **決定比例的是電腦版不是手機**：1440 上內文欄 656px，六種比例實測「圖佔一屏多少」——16:9 是 41%、4:3 是 **55%**、1:1 就跳到 73%、4:5 是 **91%**（點進文章第一眼只有一張圖）。手機那一欄從頭到尾都很寬鬆（4:5 也才 54%），只看手機會挑錯。⚠⚠ **首頁卡的縮圖不能跟著變** —— `.card-thumb` 是 `aspect-ratio: 16/9` ＋ `object-fit: cover`，十二張並排，改一張就要重裁十一張。所以拉高的圖在卡片上只露出中間一條：**4:3 露 75.3%（上下各裁 12.4%）**、5:4 露 70%、1:1 只剩 56% —— **臉與關鍵物件不可以放在上下那兩條裡**，提示詞要寫死。⚠⚠⚠ **而「畫面正中央」在兩個尺寸底下不是同一件事**：文章頁看得到完整的 4:3，卡片上置中裁切之後，站在正中央那一格會變成**唯一完整、又最大**的東西 ＝ 卡片的主角。這一輪就是為了這件事把醫事人員從中間搬到右下（第六版）。**畫之前先問：這張圖被裁成卡片之後，站在正中央的是誰。**⚠ 兩支工具已經接上：`tools/hero-resize.mjs` 的比例守門從寫死 2000×1116 改成 `RATIOS` 允許清單（對不上仍然 throw，非 16:9 時多印一行提醒）；`tools/build.mjs` 的卡片與延伸閱讀從寫死 `width="2000" height="1116"` 改成 `heroDim()` 現場讀 JPEG 檔頭。⚠ **文章頁那個 `<img>` 的 width/height 是手寫的**，要寫真值（這一張是 2000×**1493** 不是 1500 —— 寫錯不會變形，但載入前會留錯高度）。⚠ **`sizes` 沒有動**（三段式那一條是按欄寬寫的，和比例無關） | ILLUSTRATION.md 第十六節、`/history/three-month-recall.html` |
| **iPad 的文章卡、醫師卡、診所資訊** | **主尺不動，逐區塊再抬一階**（2026-08-16 一輪定案上線）。使用者拿三張 iPad 直放的截圖：「你幫我做像手機那樣放大字級的幾個版本。」**先量再做**：上一列那個 +19% 其實已經把醫師卡（15.77 vs 手機 16）與診所資訊拉到手機的水準了，**剩下沒跟上的只有文章卡**（19.38 vs 22.4，小 13%）。定案：**文章卡 Ⓒ 標題 23.94／摘要 18.62／日期 15.96、醫師卡 Ⓑ 名 22.40／內文與標記 17.59／灰標籤 17.10、診所資訊 Ⓒ 門診表與說明 18.62／點 13.30**（落選：每條尺的 Ⓐ 與「Ⓑ 手機同尺」，文章卡的 Ⓑ 22.4/17.6 也落選）。⚠⚠ **主尺（頁首、選單、兩排科別標記、卡片留白）一個值都沒動，是使用者看過 +25% 之後選的** —— 主尺一動就踩到兩件：744 寬那台 iPad mini 的「品牌 → 選單」94.9 → **60.9px**（+31% 只剩 26.9），以及**首頁與文章頁的頁首落差**從 2.88 拉到 3.84px（診所名）—— 2026-08-15 才定案「首頁切到文章頁，頁首不要變」，但那之後的放大（電腦版 +12%、iPad +19%）**只做了首頁**，`assets/style.css` 一個值都沒接。⚠⚠ **專科藥丸的抬升量 `.2136em` 是「保留站上現在的偏差」，不是「算出正確值」** —— 式子是 `.158em ＋ 0.345 × 1.05rem ÷ .8rem × (1.179/1.05 − 1)`；實測九張醫師卡，現況塊比字面偏高 +1.49px、這一版 +1.48px（原封不動），而**公式的 .158em 反而會把它對齊到 −0.17px**。取 .2136em 是因為提案頁上使用者點頭的就是那一個。**不要看到 −0.17 就去改**，要重新對齊全站那幾顆藥丸是另一件事。⚠⚠ **`col.lab` 一定要跟著字級長**（6.3 × .98÷.8 ＝ **7.72rem**）：表格是 `table-layout: fixed` ＋ `tbody th` 是 `nowrap`，左欄太窄**不會**有水平捲動，時間會**橫著蓋到第一顆點**。實測「時間 → 第一顆點」45 → 42px。⚠ 「早／午／晚」按原比值 .82÷.8 ＝ 1.025 跟上（`1.0045rem`），那三個字只有手機收起來。⚠ `.card-date` 與 `.info-note` 站上自己帶 `!important`，覆寫也要帶。⚠ `.info-card p` 沒有列進去是刻意的：這一頁上它只命中 `.info-note` 那一句，寫了不會生效。⚠ **這一段排在 iPad 那三段後面、手機那段（≤720px）前面** —— 它覆寫的 `.doc dt`／`.hours-grid … th` 是 2026-08-13 那批同權重的全域規則、靠順序決勝；手機那段是 `max-width: 720px`、和它互斥，所以「手機那段留在最後」那條規則沒有被破壞。⚠ **地圖 SVG 裡的字沒有跟著長**（路名 18.9／停車場名 13.7／距離 12.9／診所名牌 12.9，現況與改動後完全一樣）——那是 viewBox 內的使用者單位、跟版面寬度走。旁邊的說明長到 18.62 之後它相對變小了；手機那一輪的解法是「裁掉左右 ＝ 放大 30%」，iPad **還沒做，是已知落差不是漏做**。⚠ 驗收：32 個選擇器 × 五個視窗（1440／1160／390／375／**iPad 橫放 1112×834**）逐項比對舊新版 **0 項有差**，iPad 直放三個視窗只有該動的 23 項動了；上線的值對提案頁只差 0.00045px。整頁長 744 上 6921 → 7468px（最長的文章卡標題從兩行變三行）。⚠ **文章卡標題 23.94 現在比醫師名 22.40 大**（改動前是反過來的），那是使用者選 Ⓒ 配 Ⓑ 的結果、不是漏改 | `/history/type-scale-ipad-2.html` |
| **iPad／直式平板的字級** | **整體放大 19%（根字級 16 → 19px，2026-08-15 上線）**。使用者：「iPad 版的字級也偏小。」同一把五段尺比過之後選 **+19%** —— **比電腦版的 +12% 再大一階，是使用者挑的**（卡片標題 16.32→19.38 正好落在參考站的 19、標記 14.40→17.10、選單 13.76→16.34、詩 16.5→19.4）。做法與電腦版那一列完全相同（主尺一個 `html { font-size }`，寫死 px 的幾何另外接上倍率），差別在三件 iPad 才有的事：⚠ **這一段涵蓋兩段直式平板**（合成一道 `max-aspect-ratio: 9/10`）：A 段 iPad 直放、B 段直立螢幕與 **iPad mini 直放 744×1133 ＝ 0.657**；**iPad 橫放不吃這一段**（比例 > 1，它落在電腦版那一段 ＝ +12%）。比例正好 0.9 的視窗兩段都命中，iPad 這一段排在後面所以贏 —— 和站上原本的分段行為一致。⚠ **兩段各有一個寫死的 126px 要跟著倍率長**：A 段的 `--ph: calc(100svh - 126px)`（＝照片高度，詩的字級與位置按它算，不乘就會算錯比例）、B 段的 `--band-h: 126px`（收尾句讓開的量讀它，不乘會被窄帶蓋住）。實測窄帶 126.6 → 150.4px。⚠ **「往下滑」那顆指標的大小（44×39.5）與兩條角形不放大**（觸控下限與圖形，不是字），但它的垂直位置「距窄帶下緣 34.9px」＝ 對齊「5個／部定專科」那一列的中線，窄帶裡全是 rem，所以那個 34.9 要跟著長（四個尺寸實測偏差 ±0.3px）。⚠ 窄帶長高會吃掉照片（A 段照片是 flex 吃剩下的），834×1112 上 985.4 → 961.6px，「照片＋窄帶正好一屏」維持 ±0.0px —— 那是正常行為不是破圖。四個尺寸（834×1112／744×1133／820×1180／1024×1366）× 五段量過：水平捲動 0、和現況比沒有新增任何元素溢出；詩壓在照片上的對比 5.34 → 5.31、收尾句 6.79 → 6.92；詩÷照片 1.70% → 2.02%（電腦版是 1.88%）。**最緊的一格是最小那台 iPad 的「品牌 → 選單」192.6 → 94.6px**（+25% 會掉到 60.4，再往上加就是這一格先出事）。⚠⚠ **量測面板把版面暫時切到「現況」再切回來的時候，一定要先關掉 transition** —— 頁首高與標誌的 flex-basis 掛著 .2s 過場，切過去那一瞬間讀到的還是舊值，「現況」會量成「放大後」（踩過：「頁首→詩」量成 8.08%，正確是 10.00%）。同一件事也會騙過「調完視窗直接量」，**跨斷點驗證要重新載入再量** | `/history/type-scale-ipad.html` |
| **iPad 的「往下滑」指標** | **窄帶右下角、兩條一模一樣的角形 ＋ 套色的底 ＋ 慢速浮動**（2026-08-15 五輪定案上線）。間距 **1.5px**、白 **.36**、底 **`rgba(226,229,230,.10)`**、圓角 `--frame-r`、**沒有框線**、觸控目標 44×39.5、浮動 translateY 4px／2.4 秒（看 prefers-reduced-motion）。⚠ **水平是 `right: calc(var(--pad) + 28px)`**（2026-08-15 第八輪，使用者：「角形的位置太右下，往左一點」），離螢幕右緣 **49.6~58.7px** ≈ 它自己的一個身寬；原本貼齊版心右緣只有 21.6~30.7px，比它自己還窄，讀起來就是被擠到角落。⚠⚠ **讓開的量是定值，不要改成對齊第三格** —— 窄帶三格置中、版心滿版，「5個／部定專科」字尾到右緣從 iPad mini 的 104px 到 12.9 吋的 236px 差一倍以上，用它當錨點兩台 iPad 會差很多；使用者感覺到的「太右下」是離**螢幕角落**多遠。落選 16／44／64（64 在最窄的 721 上離字尾只剩 29px，那是這條路的盡頭）。⚠ 那一輪**垂直位置一個字都沒動**。⚠⚠ **記號不要放在照片上**：量過指標正下方那一塊 44×59，背景在**同一塊裡**從 L\* 0（門口那根深色柱子）跳到 L\* 52（亮著的門）—— 不是「不夠亮」是**變異太大**，白線與暗暈都會有一半的位置不見。窄帶是一整片乾淨的深色，搬上去就沒有這個問題。⚠ 垂直對齊「5 個／部定專科」那一列的中線 ＝ **距窄帶下緣 34.9px**（四個尺寸都一樣，窄帶的內容不會重排）；水平 `right: var(--pad)` 貼版心右緣。⚠ **兩條角形要用同一張 SVG 畫**，不要一條 SVG 一條 CSS 邊框 —— 跨距 18 vs 18.4px、視覺重量也不同，使用者一眼看出「長度不一樣」。data URI 的 SVG **一定要寫 width/height**（Safari）。⚠ 白 .36 配 .10 的底是 **3.2:1**（裝飾性圖形門檻 3:1）。**底若換成 .16，白就不能低於 .45**（.36 只剩 2.6:1）—— 底愈濃、白反而愈不明顯。⚠ keyframes 只寫 `translateY`，**不要照抄電腦版**那一組（電腦版帶 `translateX(-50%)`，照抄會往左跳半個身）。⚠⚠ **浮動那一條刻意不看 prefers-reduced-motion**（2026-08-15，使用者回報「在 iPad 上沒有慢速浮動」）——判準同窄帶那個往上數的數字：那個設定針對的是位移／視差／縮放，**原地上下 4px、2.4 秒一次**不屬於那一類，而「會動」正是這顆存在的理由。**但點下去的捲動仍然看它**（平滑捲動正是它要關掉的東西）。⚠ **點到會亮**：`:active` ＋ 捲動期間掛 `.is-lit`（底 .10 → **.24**、角形 .36 → **.85**，＝電腦版 hover 那一階），捲完才拿掉，所以整段 620ms 都亮著。**亮起來要寫在減少動態那條 return 之前** —— 第一版寫在後面，開了減少動態的人點下去完全沒有反應。⚠⚠ **點下去的捲動改成自己用 rAF 做**（2026-08-15 第六輪）：使用者拿 iPad 的螢幕錄影回報「點下去看起來不是平滑捲動，比較像切換」。`html { scroll-behavior: smooth }` 在 Chromium 上確實會動（逐幀 0→32→132→…→1118，約 500ms），但 **iOS Safari 對「點錨點連結」這一種捲動不吃那條 CSS**。現在 preventDefault ＋ rAF，620ms、ease-in-out cubic。⚠⚠ **history.pushState 要在動畫「之前」推**（踩過）：放在動畫結束才推，瀏覽器記在「上一步」那一格的捲動位置已經被更新成目的地，按上一頁只會清掉 hash、畫面停在原地。先推就正確（實測上一頁回 y=0、下一頁回 1118）。⚠ 自己捲之前要把 `scroll-behavior` 設成 `auto`，不然瀏覽器會對每一幀再平滑一次。⚠ 開了「減少動態效果」就整段不接手，交還給原生的瞬間跳轉。⚠ 落選：圓鈕（使用者：「圓圈跟網站主題不搭，主題是方框導圓」）、字＋角、窄帶把手、方框有框線、放在照片上的線＋角＋小點 | `/history/ipad-cue.html` |
| **「回到最上面」的按鈕** | **紙色玻璃的方框 ＋ 首頁那兩條角形翻過來**（2026-08-15 三輪定案上線）。底 `rgba(244,244,245,.8)`（＝`--card` 的 .80）＋ `blur(6px) saturate(1.15)`、**沒有框線**、角形 `rgba(92,95,87,.82)`（＝`--ink-soft` 的 .82）、方框 **44×44**、圓角 `--frame-r`、離螢幕右下角 **20px ＋ safe-area**、捲過**半個螢幕**才淡入（同時上浮 6px）、點下去底收成不透明並**亮到捲完為止**。⚠ 起因是使用者拿 blog.ichentsai.tw 的螢幕錄影，但**影片那一顆的底不能照抄** —— 那一站的頁面底色是白的，淡白底才看得見；這一站的紙色是 `--paper` #e2e5e6，同一層淡白在紙上等於沒有。第一輪三案 Ⓐ 紙上的卡（不透明）／Ⓑ 墨玻璃（.72）／Ⓒ 深綠松（實心）**全部落選**：「底色是實的我不喜歡，還是要有點透；但墨玻璃又太重了，比較好的效果應該是比較淡的淡灰色玻璃感。」⚠ **記號也沒照抄影片**（那是全箭頭）—— 使用者指定用首頁那顆「往下滑」的**兩條角形**翻過來，每條 18×9、path `M1 1l8 7 8-7`、筆畫 1.4、間距 1.5px，整組 18×19.5，**尺寸是定值、不跟著方框縮放**。兩條要用**同一張 SVG 裡的兩條 path**。⚠ **角形是柔墨不是墨**：第二輪用 `--ink` #2a2c27，使用者「看起來是黑色，要改成灰色才不突兀」。柔墨正是站上「紙上的小角形」既有的那一支（首頁那兩顆橫捲提示就是`--ink-soft` ＋ `.7`）。⚠ **圓角吃 `--frame-r` 變數**（電腦版 ×1.125 → 9px，自動跟著頁首那三個玻璃框），**但 44px 寫死不放大** —— 那是觸控下限不是造型。⚠ 捲動**自己用 rAF 做**（620ms、ease-in-out cubic），iOS Safari 不吃 `scroll-behavior`。用 `<button>` 不是 `<a href="#top">`，不在歷史紀錄裡塞空的 hash。⚠⚠ **「亮起來」不要抄 `.hero-cue` 那個 300ms 的 timeout** —— 它在第 300ms 就燒掉了，而捲動要跑 620ms，實際只亮不到一半。要捲的時候根本不掛 timeout，由捲動結束那一刻拿掉。**站上的 `.hero-cue` 目前仍是舊寫法，那是另一件事。**⚠ 落選：圓形（2026-08-15 iPad 那顆時使用者已說過「圓圈跟網站主題不搭，主題是方框導圓」，所以圓角三案全部落在 8~12px）、全箭頭、單角形、模糊 12／20px、框線細墨線／細白線、底 .45／.62、方框 48／52、圓角 10／12、出現「一屏」／「過 HERO」、離角 12／16、點下去「中」／「明顯」，以及三個開關（進場不浮上來、電腦版不放、捲到頁尾就收起來）**都沒有選** | `/history/back-to-top.html` |
| **iPad／直立螢幕的 HERO** | **兩段都改成「照片＋窄帶正好一屏」**（2026-08-15 兩輪上線）。使用者：「iPad 版也有類似的問題。」平板有兩段 @media，一台 iPad 只落在其中一段：**A 段**（比例 2/3~9/10）70svh → `.hero` 直向 flex ＋ `100svh`、照片 `flex:1`；**B 段**（≤2/3）62svh → **`100svh`**（窄帶本來就壓在照片上，這樣就已經一屏）。改動前第一屏底下露出 18~21%（B 段 38%）。⚠⚠ **iPad mini 直放 744×1133 ＝ 0.657 落在 B 段**，不是 A —— 最小的那台 iPad 原本照片最小。⚠ 詩改成跟著 `--ph` 等比例（字級 `.0170×--ph`、位置 `--head-h + .100×--ph`）：改動前字級撞 15px 下限、`padding-top` 撞 5.4rem 上限，照片一拉高比例就掉了。定案把電腦版的兩個比值追回來（字÷照片 1.70% vs 1.67%、空隙÷照片 11.0% vs 10.0%）。⚠ **順便修掉 B 段一個改動前就有的破圖**：地址電話絕對定位和三格同一列，744×1133 水平重疊 **92.9px**（900×1600 是 19.5px），「部定專科」被電話蓋住。修法抄 A 段（地址電話一行置中），並把 **`--band-h` 改成 126px** 讓收尾句自己讓開（不改會被蓋 9px）。⚠ 選擇器要寫 `body .contact-pair`（0,1,1）。⚠ **「往下滑」的指標兩段都放**：細線（1px×30、上端淡出）＋角形＋**暗暈**＋一顆沿線往下的 2px 小點，離窄帶上緣 **110px**。使用者：「單一個角型只是勾尾，看不出在幹嘛，再多一點點暗示性、但不要太強。」⚠⚠ **不要只拿掉底塊** —— 它落在亮著燈的門面上，背景最亮處 L\* 46~64（白字對比 2.7~5.3），淺色底塊在亮處會不見，所以改用 drop-shadow 的暗暈。⚠ 位置不能照抄電腦版的 `bottom: calc(100% + 4px)`，那會落進照片最後 84px 的接縫裡。⚠ 接縫**不必動**（A 段的柏油層收到 alpha 1，六組實測都是 #161413）。⚠ 手機／電腦／iPad 橫放逐項量過 0 差異 | `/history/hero-ipad-fullbleed.html` |
| **手機版 HERO 的動態** | **維持 10%／6 秒／先快後慢，改成不看「減少動態效果」＋ 等照片畫出來才開跑**（2026-08-16 一輪定案上線）。起因是使用者：「目前手機版本打開的時候 HERO 圖片都不會動，好像也是可以稍微動一下，但因為手機的畫面比較小，所以動得太誇張或是動的幅度太大好像也不是很好。」⚠⚠ **先查「為什麼不會動」，不要直接再加一個動畫** —— 站上前一天（08-15 commit `fe1baab`）就已經有 `hero-push-10`。Playwright 390×844 逐 100ms 取樣量到**兩個成因**：① 開了「減少動態效果」時 scale **從頭到尾 1.0000**（旁證：08-15 他回報「iPad 上沒有慢速浮動」是同一個設定）；② 4x CPU ＋ 4Mbps 節流下照片 **1756ms** 才解好，那一刻 10% 的行程**已經走掉 37%**，而 `cubic-bezier(.22,.61,.36,1)` 是先快後慢、被吃掉的正是最快那一段。⚠ **落選的是三個新案**（Ⓐ 4%／10 秒等速、Ⓑ 7%／12 秒等速、Ⓒ 跟著捲動 8%）—— 使用者選「現況」，**幅度、時間、曲線一個數字都沒改**，上線的是兩件修復。⚠⚠ 修復一：拿掉 `@media (prefers-reduced-motion: reduce)` 那一條（**三個斷點一起解除**）。**這只解除照片這一條** —— `index.html` 其餘每一處 `prefers-reduced-motion`（`scroll-behavior`、各種 transition、往下滑那顆指標的捲動）**一個都沒動**，`assets/style.css` 也沒動。⚠⚠ 修復二：`html.hero-hold .hero-photo img { animation-play-state: paused }`，由**緊接在 `<img>` 後面**的 inline 腳本掛上、圖一好就拿掉、3 秒保險絲。**方向是「JS 只會讓它變好」** —— 沒有 JS ＝ class 永遠不會掛上 ＝ 動畫照舊立刻開跑；**絕對不要反過來寫成「預設 paused、等 JS 來解」**，那會讓沒有 JS 的人永遠停在 `scale(1)`。腳本也**不能搬到頁尾那一堆**，搬過去中間那幾百毫秒已經先跑掉了。⚠ `transform-origin: 50% 100%` 五個候選都沒動（原點放下緣，照片最後一列不動，那一列的 `#181614` 是接縫四輪定案的依據；改成正中接縫直接毀掉，ΔL\* +6.42）。⚠ 沒有做「來回呼吸」，那是 08-15 電腦版那一輪就否決過的。⚠ 驗收：五個情境逐 100ms 取樣（減少動態效果開著的手機／電腦／iPad 三個都從「不動」變成正常跑完；節流下起跑點從「已走 37%」變成 0%；**關掉 JavaScript 的行為和改動前完全相同**），版面 390×844／375×812／834×1112／1440×900 四個視窗 23 個選擇器**0 項有差** | `/history/hero-motion-mobile.html` |
| **手機版 HERO 的照片要多大** | **抵下緣 ＋ 水平置中 ＋ 詩按照片高度等比例**（2026-08-15 兩輪定案上線）。起因是使用者：「電腦版滿版效果很好，手機上照片就縮小了，感覺沒有電腦版那麼好。」照片從 `74.72vw`（390 上 291px ＝ **一屏的 34%**）改成**吃掉「一屏扣掉窄帶」的高度**（`.hero` 直向 flex ＋ `height:100svh`，`.hero-photo` `flex:1`），390×844 上 719px ＝ **一屏的 85%**，照片＋窄帶正好一屏 —— 和電腦版是同一個做法。⚠⚠ **cover 在直式框上垂直永遠是整張照片**（倍率由高度那一項決定），拉高只往左右裁，天空與街面的上下關係一列都沒動 —— 所以手機那條 `object-position: left center` 要**整條刪掉**，基底的水平置中就是定案要的「街景多」。想改成偏右的裁法就**不能寫死百分比**（看得到的原圖寬會隨螢幕高度變）。⚠ **不要改成整屏 `100svh`**：窄帶（地址、電話、1983／9／5）會被整條推出第一屏，評分表上它就是輸在這一項（找資訊的效率 2.0／5）。⚠ 詩：位置 `.200×--ph`、字級 `clamp(min(12px,3.8vw), .0215×--ph, 15.5px)`、字距 `.32em`、行高 `2.45`、全形空格不縮、首行後 `.9em` —— **後四項和電腦版逐字相同**。`--ph: calc(100svh - 125px)`（窄帶實測 123~125px）。⚠⚠ **位置與字級一律按 `--ph` 等比例，不能只看 vw**（踩過兩次：寫 vw 時 390×844 好好的，320×568 三行全壓到房子；改吃 svh 則只有「抵下緣」那一格準）。⚠⚠ **這一輪推翻了 Ⓢ1「詩不准碰到房子」那條約束** —— 它真正的理由是對比度，而它是在照片只有 291px、牆又小又花時訂的。夜景的清水模牆自己就夠暗，詩壓在牆上白字對比 **5.0~8.7**（六視窗最低 5.00，AA 是 4.5）。**`/history/hero-mobile-skyline.html` 那張天空楔形表只適用於舊版型，不要再拿它解字距。**⚠ 窄帶接縫的起點色跟著裁切重新量：`#151311` → **`#181614`**（L\* 5.30→7.28，每列取中位數），曲線 S(t^1.6) 一個字沒改。⚠ 上緣壓深（`.hero-photo::after`）**維持關著**，三案裡使用者選「不放」；「往下看」的角形也**仍然不放**（2026-08-13 落選過，這一輪再問一次，答案一樣）。⚠ 電腦版／iPad 直放 70svh／旋轉 62svh **一個值都沒動**（逐項量過 0 差異）。⚠ **同一天補上 2000×1323 的原檔與 `sizes` 修正**（使用者提供原圖）：照片一拉高，`sizes="100vw"` 會讓 DPR2 的手機去挑 800w、放大 2.72 倍，見本節第 15 條 | `/history/hero-mobile-fullbleed.html` |
| **首頁的分享圖** | **裁成 1.91:1 ＋ 左上的標示 ＋ 下緣三格**（2026-08-22 當天開、當天定案上線，成品 `assets/og-home.jpg`，產圖 `node tools/og-home.mjs`）。起因是使用者拿 LINE 的截圖回報「分享首頁時的圖片……顯示的文字內容也不是現在上面的文字」。⚠ **那張卡不是舊的也不是快取** —— 逐字就是 `index.html` 當時宣告的 og:*。⚠⚠ **LINE 不裁圖**（從截圖量：635×426 ＝ 1.491:1，和原檔 1.512:1 一樣，照原比例顯示），所以原本那張 1.51:1 會長成一張**又高又暗**的卡：平均 L* 21.3、幾乎全黑 48.0%、邊緣密度 **16.9%** —— 比 ILLUSTRATION.md 第十一節那張「像鬼屋欸」的失敗版（19.3%）還低。⚠ 卡片實際只有 **212 CSS px** 寬（不是一般說的 250）。⚠⚠ **三格不能壓在照片上** —— 亮著的騎樓在原檔 y 860~1220、幾乎貼著底邊（1323），底下只剩 103px 路面，怎麼裁都會蓋到門面；而且**拉寬反而更糟**（框下緣固定時，騎樓下緣到框下緣在原檔是定值，框愈寬比例尺愈小、那段距離在畫面上就愈短）。正解是使用者點出來的、站上早就有的做法：**帶子脫離照片**（同第六之十八節 iPad／手機版）。⚠ 但**不要把照片縮短**（比例 1.91 → 2.4，屋頂會被切，使用者退過一次）——**照片維持 1.91 的裁法、帶子疊在下緣蓋掉路面**。⚠ **帶子上緣要淡進來**（前 42% smoothstep 把 alpha 從 0 長到 1）：站上那條窄帶不必淡入，是因為它上面本來就是暗的路面；這張圖的帶子正上方是**亮著的騎樓**，硬邊會切出一條線。接縫色是現場量照片最後一列的**中位數**，之後用 S(t^1.6) 走到 `--band-bot`（同站上手機版）。⚠ 三格的字、字重、字距、內距、分隔線**全部在 1200 寬的視窗上打開 `index.html` 量回來**（1× 產出 603px vs 站上 603.375px）；**數字是襯線、單位是黑體**（2026-08-08 那條）。⚠ `og:description` 同時換成 **「到巷口的芳仁　一起想辦法」**（詩的第四句）——原本的「雲林斗六・永樂街｜巷口牙醫｜1983年創立」不是錯的，但「巷口牙醫」「創立」**站上一個字都沒有**，而地名與 1983 現在印在圖上了，寫兩次是浪費氣泡那兩行。⚠ `tools/topics.mjs` 的 `OG_FALLBACK` 一起換成 `og-home.jpg`（還沒畫圖的六科退到它） | ILLUSTRATION.md 第十一節、`/history/og-home.html` |
| **科別著陸頁的分享圖** | **壓在上緣的玻璃帶：左邊科別名、右邊標誌＋芳仁牙醫診所＋雲林斗六・永樂街**（2026-08-22 當天開、當天定案上線，成品 `assets/og-topic-general.jpg`，產圖 `node tools/og-resize.mjs drafts/og-topic-<spec>-src.jpg <spec>` → `node tools/og-plate.mjs <spec> --tint 0.70 --ink 0.18 --loc full --locpos stack`）。⚠⚠ **實心色牌被退回，不要走回去** —— 使用者：「忽略了一個很重要的要素 —— **不搶戲、和諧**，這也是診所品牌的核心概念之一。」⚠⚠ **玻璃要用「深階」不是「套色」，底下還要墊一層墨**，這是量出來的：紙色字壓在套色 .34 的玻璃上最亮處只有 **1.74**（幾乎看不見）；定案的深階 .70 ＋墨 .18 是中位 5.90・最亮處 4.40。⚠ 「最亮處」＝ 天空從玻璃後面透上來那一段，**要單獨量**，而且只能取兩段文字**中間**那塊沒有字的玻璃，否則會把紙色的字算進背景裡。⚠⚠ **最亮處 4.40 過不了站上的 4.5，那是使用者看過三格（淡 3.32／中 4.40／濃 5.73，照片還剩 24%／16%／10%）之後的取捨，不是漏看** —— 濃那一格過得了，但照片只剩 10%、已經接近他退回的那塊實心板。**不要拿對比度來「訂正」它。**⚠ 地名的排法是**手機版首頁那一套**（兩行、沒有細豎線、兩行都 `text-align-last: justify`、字級比值 0.774），**它只有帶街名才成立**：地名 8 個字 × 0.774 剛好等於診所名 6 個字、兩行天然等長，只寫「雲林斗六」四個字要撐 1.366em ＝ 首頁的十八倍，會變成四個孤字（`og-plate.mjs` 有一道量測，超過 0.15em 就改成靠右切齊）。⚠ **訊息卡的標題不帶地名、`<title>` 與 JSON-LD 的 name 帶著地名**，兩個刻意不一樣（使用者：「訊息標題有（雲林斗六），這個應該是在網站裡的」）。⚠ **2026-08-24 起牙周與顯微根管有自己的圖了**（`og-topic-perio.jpg`／`og-topic-endo.jpg`），**兒童牙科那張已經產好、在工作分支上等上線**（`og-topic-kids.jpg`），**矯正 2026-08-24、植牙 2026-08-25 也上線了**，**口腔外科 2026-08-25 上線 —— 七科到齊，`OG_FALLBACK` 從此沒有人會用到**（留著當退路）。⚠ 口外那一張走了**十六輪**（推導在 `drafts/og-topic-surg-prompt.md`），帶子定案是**深階 `#784e84`**、補償色 `#a66eb4`，**紙色字 5.15，是七科裡第一張真的過 4.5 的** —— 成因是那張圖頂端是一片平整的隧道襯砌，**「最亮處」和「中位」幾乎一樣**（5.12 vs 5.15），不像前幾張有天空從玻璃後面透上來那一段。**通則：帶子後面那一塊愈均勻，可用的濃度範圍愈寬。**見 `/history/surg-band.html`。⚠⚠ **兒牙那一張是第一次「紙色字撐不住」** —— 紙色壓在兒牙套色 `#c28229` 上只有 **2.54**（牙周 3.83、顯微根管 4.12 都在已被接受的那一級），所以 `og-plate.mjs` 新增了 **`--fg`／`--locop`**（預設值沒動，另外三張重跑照舊是紙色）。**判準：暖色底（琥珀、芥末、褐）用純白字，冷色與深色維持紙色。**⚠⚠ 帶子要落在**套色**還是**深階**，2026-08-24 是把四個候選做成 LINE 訊息的模擬（`node drafts/line-mock.mjs <spec>`）讓使用者挑的，他選了**套色 `#c28229` ＋ 純白字**（主名 3.23／地名 2.93，**低於站上的 4.5 是他知情的取捨，不要訂正**），理由是帶子的顏色要等於站上那顆標籤。七科的值與做法在 ILLUSTRATION.md 第十一節。⚠⚠ 但**參數不要照抄**：2026-08-23 起帶子改用**相乘上色**（`--blend multiply --tintcolor <補償色> --blur 6`），而補償色是「該科的套色 ÷ 帶子後面那面牆」算出來的，每一科每一張圖都不一樣 —— 顯微根管那張是白牆配 `#cb5755`，做法與實測見 ILLUSTRATION.md 第十一節。⚠ **頁面上不放圖**（手機版已經很擠，會壓到文章卡）—— `og:image` 是 `<head>` 裡的 meta，本來就不會顯示在頁面上，兩件事不必取捨。⚠⚠⚠ **2026-08-23：分享卡會被訊息 app 左右裁掉，帶子上的字因此要收進中央 73%。** 使用者拿手機截圖回報「帶子上的字都被裁到了」——**一般牙科那張已經上線兩天才被抓到**。從截圖實測（iPhone 1125×2436，量帶高÷卡片寬再和原圖的 104/1200 比）：**LINE 只看得到原圖寬度的 89.7%**（左右各裁 5.2%）、**iMessage 只有 78.7%**（左右各裁 **10.7% ≈ 128px**），兩者都是對稱裁。⚠⚠ **2026-08-24 更正：這兩個 app 的名字一度被我標反。** 分辨方法：**LINE 的泡泡是綠的（rgb 109,230,123）、卡片底下沒有網址；iMessage 是灰泡泡、底下有一行 `fangren.net`** —— 不要照截圖檔名的順序猜。修法是 `og-plate.mjs` 新增 `BAND_PAD_X = 160`（＝1200 的 13.3%，比 LINE 實測多 2.6% 當餘裕；144 試過，最長的「一般牙科・定期檢查」離裁切線只剩 10px 太緊），**帶子的底色仍然滿版，只有內容退進來**；同時加了一道守門，字超出安全區或兩組字之間空隙 <24px 就 throw。⚠ **驗收要模擬那兩個裁法**（iMessage 中央 78.7% → 581 device px、LINE 中央 89.7% → 683 device px），不要只看 1200 寬的原圖。 | ILLUSTRATION.md 第十一節、`/history/og-topic-card.html` |
| **著陸頁的線稿底圖** | **介紹區右下角的線稿人物，濃度分兩段：手機 .10、≥834 .48**（2026-08-22 開、08-23 定案上線，成品 `assets/lineart-general.png` 832×788，產圖 `node tools/topic-lineart.mjs general --art <線稿檔> --crop 97,123,832,788`）。起因是使用者：「那個圖片放進來**會壓縮到版面，我不要那樣**，我想到把圖片的某個部分做成頁面的底，但只要保留線條。」⚠⚠ **不動任何 markup —— 用 absolute 的偽元素**，它不佔高度也不影響換行；驗收九個寬度（1440~320）介紹區高度與加圖之前**逐格相同**、無水平捲動。⚠⚠ **走錯的第一條路是「從既有插畫抽線稿」，兩版都被退回，而且成因是方法本身不是參數** ——從有陰影、有材質的插畫上撿邊，筆畫粗細會跟著原圖明暗走、材質與皺褶也會變成線，後製拿不掉。**要一個「畫出來的」風格就得畫。**⚠⚠ **改用生成之後第一版風格全過、內容四件全錯，而其中三件是提示詞自己寫出來的**（「彼此互看」← 我叫她轉向他；「很像選舉看板」←「relaxed」是形容詞，擋不住正面對稱的平掌，要寫**幾何**；髮型只講了畫法沒講是低馬尾）—— 第二版**加附姿勢參考圖**一次就中，**七張參考圖分三組、說明分開寫**（畫法／姿勢視線髮型表情／長相服裝年齡層）。⚠⚠ **這一輪最要緊的兩句：大小不影響對比度、位置才影響**（最壞底色 ＝ 紙色與套色按濃度混合，和圖多大無關；圖變大只是讓更多字落在上面）；**卡住的是柔墨的次要文字不是主文**（`--ink` 紙上 11.15、.58 才掉到 4.5；`--ink-soft` 紙上只剩 5.14、**濃度 .101 就掉到 4.5**，而 `.tp-case`／`.tp-flow` 用的正是柔墨）。⚠⚠ **而「有沒有字壓到」完全是寬度決定的**：那幾行文字都比介紹區窄（iPad 上一行約 420px、區塊寬 784px），所以 **≥834 實測 0%**、只有手機會壓（390 上 8／20／33／46%）——**所以濃度分兩段，不要收成一個值**。⚠ **`background-image` 吃不到 `opacity`**（第一版濃度那條尺完全沒作用，徵兆是「改變的面積 0.0%」），要用 `::before` ＋ `opacity`。⚠ `right` 不能給負值（390 上 `-1rem` 直接讓整頁可以左右拉）。⚠⚠ **CSS 裡的 `url()` 在做著陸頁快照時也要改路徑** —— `tools/topics.mjs` 原本只換 `href`／`src`／`srcset`，而樣式表整段是從 `index.html` 照抄的，`url("assets/…")` 在 `/topics/<spec>/` 底下會 404 **而且不報錯**（已補一條 replace）。⚠ **不要改用根目錄絕對路徑 `/assets/…`** —— 舊站 `yclee86.github.io` 還活著，那邊會壞。⚠⚠ **一般牙科與牙周各一組值，不要互抄**（見下一列）；其餘五科畫好之後把 `[data-topic="<spec>"]` 加進那三條選擇器的清單，**不要改成 `[data-topic]` 一網打盡**（沒有圖的科目會畫出一個空的偽元素） | ILLUSTRATION.md 第十二節、`/history/topic-lineart.html` |
| **牙周的線稿底圖** | **分段 721px：手機 `min(81.25%, 390px)`／aspect 832:755／`.115`，≥721 `min(100%, 480px)`／aspect 1024:755／`.15`**（2026-08-24 當天開、當天定案上線，成品 `assets/lineart-perio.png` 1024×755，產圖 `node tools/topic-lineart.mjs perio --art drafts/lineart-perio-v1.jpg --crop 0,180,1024,755 --flip`）。使用者指定只畫「醫師噴水、細菌被嚇跑噴走」那一段；姿勢參考從前一天定案的分享圖原檔裁（**分享圖在前、線稿在後**），提示詞一次就過（線佔 6.34%、粗細一致 1.60、實心填色 0 塊）。⚠⚠ **四個值和一般牙科全部不一樣，那不是漏改成一致** —— 是使用者在自己的手機與 iPad 上逐格挑的。⚠⚠ **分段是 721 不是 834**：使用者是在 **iPad mini 直放 744** 上定的「iPad 版」，用 834 那台會被歸到手機那一段，他看到的和定案的就不是同一件事。⚠ **`--flip` 是這一輪新增的**（圖在介紹區右下角，人物面向右等於背對整頁文字、朝版心外面噴）；實作是**先裁再翻**，所以 `--crop` 的座標一律在原圖上量。⚠⚠ **大小是免費的、濃度不是**：最壞底色只看濃度（390 上大小四格壓到線的行數 7→8→12→19，**對比一格都沒動**），所以「想更明顯」先走大小那一條。⚠⚠ **圖檔的右緣不是人物的邊** —— 醫師背後還有白袍下襬與水管尾巴，靠右對齊會對齊到那一截空白，人就縮在中間（使用者：「線稿一直往中間移」）。手機那一段因此**從右緣裁掉 192**，裁法是「框窄一截 ＋ 圖照高度撐滿靠左」（`background: left center / auto 100%`），**比例尺不變、只是往右移 65px**。⚠⚠ **iPad 不能照抄手機那一組**：iPad 的字級 +19%、流程那五行往右伸得更遠，同一組值在 834 上會壓到**柔墨**，對比只剩 2.86。⚠⚠ **已知取捨：≥721 的 `.15` 是使用者看過數字之後選的** —— 744 上 11 行壓到線、其中柔墨 4.32 低於 4.5（1440 上 7 行、一樣 4.32）。**不要拿對比度來「訂正」它**（同兒牙填色白字 3.22、分享卡帶子 4.40）。手機那一段選的是**算出來的 AA 上限 .115**（4.50 ✓，牙周的臨界濃度 .116 比一般牙科的 .101 寬鬆，因為 `#317d78` 比 `#3f654a` 淺）。⚠ 驗收十二個寬度（含斷點兩側 721／720）介紹區高度與「把偽元素關掉」**逐格相同**、無水平捲動 | ILLUSTRATION.md 第十二節、`/history/topic-lineart-perio.html` |
| **顯微根管的線稿底圖** | **分段 721px：手機 `min(76%, 360px)`／`.107`，≥721 `min(70%, 330px)`／`.30`；aspect 887:949 兩段共用**（2026-08-24 當天開、當天定案上線，成品 `assets/lineart-endo.png` 887×949，產圖 `node tools/topic-lineart.mjs endo --art drafts/lineart-endo-v1.jpg --crop 0,53,887,949`）。使用者指定「**用分享圖裡醫師和顯微鏡的部分**」，所以整張只有**坐在醫師椅上的醫師 ＋ 吊臂式顯微鏡 ＋ 兩條光線**；提示詞一次就過（線佔 6.4%、筆畫 4.9‰、粗細一致 1.40、實心填色 0 塊）。⚠⚠ **這一科的值又和前兩科都不一樣，一樣不是漏改成一致** —— 三科三組，每一輪都讓使用者在自己的機器上逐格挑。⚠⚠ **近正方的圖，卡住的是高度不是對比度**：887×949 在 480px 寬時算出 **514px 高**，比介紹區（1440 上 480.7）還高，而偽元素是 `bottom: 0`，會往上凸到 h1 那一區（744 凸 13、834 凸 7、1041 以上凸 33px）。提案頁的面板因此比前兩科多一行「圖有沒有比介紹區高」的判斷；定案的 330px（→353 高）九個寬度都收得住。⚠ **不必 `--flip`**（分享圖裡的醫師本來就面向左＝朝版心裡面，牙周那張朝右才要翻）、**也不必裁右**（墨的最右端第 886 欄就是椅背本身，沒有空白可裁）—— **朝向與裁不裁每一科各自判斷**。⚠⚠ **第一版 iPad 那一段沿用前兩科從 360／.48 起跳，使用者：「好像太大了　而且太濃」** —— 所以 ≥721 那一段**整把尺往下移**（大小 250／300／**330**／360，濃度 .15／.22／**.30**／.48），按鈕改印**實際值**（`330px`、`.30`），不再寫「大／更大」：兩段的尺不一樣時相對詞會騙人。⚠ **手機 `.107` 是算出來的 AA 上限**（柔墨 `#5c5f57` 壓在 `#ae4f4d` 上剛好 4.50）；三支套色**愈深愈緊**：一般牙科 .101／牙周 .116／這一支 **.107**，**每一科現算一次不要抄**。⚠⚠ **≥721 的 `.30` 純粹是美感，不是取捨** —— 那一段只壓到深墨（`.tp-reply`／`.tp-close`，1~3 行），**濃度上限 .665**，實測 7.59。**「iPad 會壓到柔墨」不是通則**（牙周那張是橫的、橫向伸進流程那五行才會，這張是直的完全沒壓到）。⚠ 驗收十二個寬度（含斷點兩側 721／720）介紹區高度與加圖之前**逐格完全相同**、無水平捲動、圖每一格都收在介紹區裡 | ILLUSTRATION.md 第十二節、`/history/topic-lineart-endo.html` |
| **兒童牙科的線稿底圖** | **兩段的裁量不一樣：手機 裁 144 欄／`aspect 854:857`／`min(85.6%, 419px)`／`right: -14px`／`.152`，≥721 裁 **192** 欄／`aspect 806:857`／`min(77.5%, 420px)`／`right: -20px`／`.22`**（2026-08-24 當天開、當天定案上線，成品 `assets/lineart-kids.png` 998×857，產圖 `node tools/topic-lineart.mjs kids --art drafts/lineart-kids-v1.jpg --crop 26,128,998,857 --flip`）。使用者指定「**線稿圖就選醫師和治療椅的小孩，治療椅不需要整個畫進來**」，姿勢參考從當天上線的分享圖裁（`x395 y175 470×440 ×3`），提示詞一次就過（筆畫 5.9‰、粗細一致 1.33、實心填色 0 塊；線佔 8.53% 高於指引，成因是衣服上那些塗鴉圖案，是刻意留的品牌細節）。⚠⚠ **這一輪最值得記住的是「再往右」有三段不同的機制**：① **框貼齊螢幕邊**（`right` 給負值）—— 上限手機是**頁面內距 14px**，≥721 是**最窄的那台**（721 只有 23.8px、iPad mini 744 只有 22.5px），**不是版心留白**（1041 以上有 189px 以上），給 32px 在 744 上直接多出 10px 水平捲動；② **出血**（框不動、背景往右推，超出的被框裁掉，沒有水平捲動）—— 但推到 +160 醫師整個出畫面、只剩小孩，**那是內容問題不是版面問題**；③ **定案用牙周那一套「框窄一截 ＋ 圖照高度撐滿靠左」**，比例尺不變。使用者選的「出血 +50」換算成圖檔就是**裁掉右邊 144 欄**（50 × 998 ÷ 347），框寬跟著乘 0.8557，**逐像素等於出血版**。⚠⚠ **翻轉這一輪是烘進圖檔的**（`--flip`），不要用 CSS 的 `transform: scaleX(-1)`：翻轉之後「畫面上的往右」在未翻轉的座標裡是**往左**，背景位置與量測都要跟著鏡射（提案頁那一版踩過，按下去圖往反方向跑）。⚠⚠ **連「裁多少」都是兩段各一個值**（前三科兩段相同），所以 `aspect-ratio` 也要分兩段寫。⚠ **手機 `.152` 是算出來的 AA 上限**（柔墨 `#5c5f57` 壓在 `#c28229` 上剛好 4.50）；四支套色**愈淺愈寬鬆**：一般牙科 .101／牙周 .116／顯微根管 .107／**兒牙 .152**，**每一科現算一次不要抄**。⚠ **≥721 的 `.22` 實測 4.24、低於站上的 4.5**，那是使用者看過四格（.15 是唯一過關的 4.51）之後選的取捨，同牙周那張的 4.32 —— **不要拿對比度來訂正它**。⚠ 提案頁本身學到兩件：**切換條在手機上量到 26.6% 太高**（收緊到 24% ＋ 一顆「收起」），以及**產生器是模板字串、註解裡不能出現反引號**（第九節第 8 條的近親）。⚠ 驗收十二個寬度（含斷點兩側 721／720）介紹區高度與「把圖關掉」**逐格相同**、無水平捲動 | ILLUSTRATION.md 第十二節、`/history/topic-lineart-kids.html` |
| **齒顎矯正的分享圖與線稿** | **分享圖：科幻語彙（浮空的發光螢幕群）＋ 站上的手繪畫風**（2026-08-24，十三版，`assets/og-topic-ortho.jpg`；帶子 `--tintcolor #5095ed`，實測 rgb(68,120,180) 對站上套色 `#4478b5` 只差 1 階，紙色字 3.61 —— 使用者看過白字 4.57 後選紙色）。**線稿手機段定案 68%／right −10px／出血 +16px／濃度 .121**（`assets/lineart-ortho.png`，`--flip`），**≥721 仍暫定**。⚠⚠ 三條會再用到的：**①「亂」只有交給畫面裡最大的東西才畫得出來**（前四版交給片子／模型上的小牙列，212px 上只剩 9×5 與 39×10px，四版全退）；**② 邊緣密度是「邊」給的不是「深」給的**（大面板讓密度從 17.7 掉到 14.2%，一大片平坦色塊不管亮暗都拉低密度）；**③ 上緣的白框要「填色」不是「外插」**（`og-topclean` 逐欄外插把螢幕的深藍拉到頂端，安靜區從 0.32% 惡化到 38%，改用新寫的 `drafts/og-topfill.mjs`）。⚠ 「再往右」有兩段機制：框推出版心（地板＝頁面內距 14px）→ 出血（框不動、背景推、超出被裁），定案是兩段疊用。⚠ 提案頁產生器 **`tools/lineart-preview.mjs` 留在版控裡**，四把尺**依斷點換單位**（手機調 %、≥721 調 px；濃度也分兩組）。⚠ 臨界濃度五科各不同：.101／.116／.107／.152／**矯正 .121** | ILLUSTRATION.md 第十一、十二節、`drafts/og-topic-ortho-prompt.md` |
| **植牙・假牙重建的線稿底圖** | **兩段都定案**（2026-08-25 當天開、當天定案上線）：**手機 `min(92%, 360px)`／`right: 0`／出血 `+48px`／濃度 `.121`**，**≥721 `min(70%, 400px)`／`right: -8px`／出血 `0`／濃度 `.22`**（`assets/lineart-prosth.png` 972×896，產圖 `node tools/topic-lineart.mjs prosth --art drafts/lineart-prosth-v2.jpg --crop 36,83,972,896`，**沒有 `--flip`**）。⚠⚠ **這一輪真正的教訓不在圖，在工具：提案頁的「出血」那把尺對植牙／矯正從頭到尾沒有接上。** 站上這條規則有兩種寫法（`background: url(…) center / contain no-repeat;` 與 `background: url(…) no-repeat; background-size: contain;` ＋自己一行 `background-position`），`tools/lineart-preview.mjs` 只接了前者 —— 後者那兩科按了**完全沒反應、不報錯、畫面正常**，圖一直停在 CSS 寫死的 `+48px`，換算回圖檔是 140 個原始像素、右邊留白只有 32，所以醫師指路那隻手被切掉約 108 像素。使用者連著兩輪說「醫師的手被裁掉了」「還是沒回來欸」，我卻連著兩輪去改**裁切**。修法：加 `reBgLong`、出血預設值改成從**該科自己的規則**讀（原本不限定選擇器，會抓到別科的），並新增守門「四把尺的變數沒出現在該科規則裡就 throw」（牙周／兒牙走 `auto 100%`，那兩科跳過出血這一項），六科各跑一次確認過。⚠ **同一個症狀 2026-08-24 出現過一次**（站上寫 `right: 0;` 沒有 px，正規式要求 px 就命中不到）——**「按了沒反應」不報錯，只有拿數字量才看得出來**，改完這支產生器要量一次 computed style。⚠⚠ **≥721 那一段一定要自己寫一行 `background-position`**，不寫會繼承基底的 `+48`。⚠⚠ **兩段的出血差很多（+48 vs 0）不是漏改成一致** —— 手機的框只有 333px 寬，+48 等於把右邊 14% 推出框外，那是他要的出血效果；iPad 的框 400px、壓到的字也少，整張看得到反而好。⚠⚠ **不翻轉，七科第一個** —— 這一張有階梯，不翻的時候「往右上走」和閱讀方向一致，翻過來反而是走回去；提案頁那一輪特地加了「翻／不翻」的切換讓他在手機上比。同一輪也把提案頁的兩把位置尺改成**雙向**（`←48 … 0 … →20`）。⚠ **裁切改了兩次，兩次本身都是對的**（右邊真的貼邊 → 留白 32；下面真的少了 88 列 ＝ 最底下那幾階與扶手的腳），定案 `36,83,972,896`、四邊留白 11／32／0／7，**都沒切到墨**。⚠ **「線稿一定會多畫一條地面線」的通則這一張不適用**：y=910~914 那條橫貫 x 58~724 的密線是**最底下那一階的階緣**，裁掉就少一階 —— **裁之前先看一眼那條線是什麼**。⚠⚠ **已知取捨**：≥721 的 `.22` 實測柔墨 **4.02**（744／721 各 1 行，834 以上 0 行），`.15` 是唯一過關的那一格（4.51）；手機的 `.121` 也在這一科的 AA 上限 **`.097`** 之上 ——那是六科裡最緊的一支（`#335b8b` 是六支套色裡最深的）。兩個都是使用者看過數字之後選的，同牙周 4.32、兒牙 4.24，**不要拿對比度來訂正**。⚠ 六科的臨界濃度：.101／.116／.107／.152／.120／**植牙 .097**，**每一科現算一次不要抄**。⚠ 驗收十二個寬度（含斷點兩側 720／721）介紹區高度與「把圖關掉」逐格相同、無水平捲動、每一格都收在介紹區裡 | ILLUSTRATION.md 第十二節、`/history/topic-lineart-prosth.html` |
| **口腔外科的線稿底圖** | **手機 `min(52%, 188px)`／`right: 0`／出血 `+32px`／濃度 `.18`**（2026-08-25 定案上線）；**≥721 `min(70%, 220px)`／`right: 48px`／出血 0／`.30`**（2026-08-26 另開一次提案頁定案）。⚠⚠ **`right` 給正值（框往左推）是七科第一次** —— 前六科不是 0 就是負值（往右推出版心）；這一張是直的，貼齊右緣時人物離文字太遠、像被推到牆角，往左 48px 之後圖的右緣離螢幕 72px（744 上）。⚠ **≥721 那把「大小」的尺是為這一張重開的**（140~220px，不是前幾科的 200~400）：介紹區最矮的一格 443.1px，換算回寬度只有 224.6px，預設那把尺有五格根本不能選（產生器因此加了 `--sizes`／`--wide-sizes`）。⚠⚠ 同一輪修掉提案頁**一直都在的量測錯誤**：面板把偽元素的框寫死成「貼齊介紹區右緣」，沒把它自己的 `right`／`bottom` 算進去 —— 只要那把尺不是 0，「幾行字壓在圖上」與「圖的右緣離螢幕多少」就一起偏掉（這一輪偏 48px，兒牙 −14、矯正 −10 也各偏過）。成品 `assets/lineart-surg.png` 478×943，產圖 `node tools/topic-lineart.mjs surg --art drafts/lineart-surg-v2.jpg --crop 274,42,478,943`，**沒有 `--flip`**（她在圖裡本來就朝畫面左邊）。⚠⚠ **這一輪畫了五版，使用者最後選的是第一、二版** —— 中間三版全部在治「潛盾像電風扇」，而那三版一路把圖治壞了。v3 的診斷是對的（刀盤畫成全圖最大的正圓、細輻條從中央往外放射、刀盤正面朝我們），但 v4 照著改之後五項量測全過、使用者卻說「整個風格歪掉」：機器橫跨畫面 **53%**、筒身 **3.9 個頭高**（提示詞寫的是「＝她的前臂」約 1.2 個頭）、握把變成伸到邊緣的細砲管、她還轉向了畫面右邊。⚠⚠⚠ **通則：提示詞裡哪一段字最多，模型就把哪一個當主角** —— v4 的 `THE MACHINE` 那一段 240 字比 `WHO` 還長，等於一邊要它畫人、一邊說這張圖是在畫這台機器。**要治造型靠把形狀寫死＋列 AVOID，不是靠字數。**⚠ 逐條重看提示詞另外抓到三個**字面完全合乎、意思完全相反**的地方：`COMPOSITION` 寫「人與機器**合佔** 85%」（機器一大人就被擠小）、扁橢圓的「寬」拿它自己的高當尺、`narrow`／`slim`／`tail` 三個字加起來就是一根細管。**每一版被退回之後要把提示詞從頭唸一次，不要只改被點名的那一句。**⚠⚠ **這一張是七科裡最「直」的**（比例 0.51，前六科 0.87~1.07），所以卡住的完全是**高度**（同顯微根管那條，但更極端）：390 上照預設 76% 會比介紹區高 133px。⚠⚠ **px 上限是量出來的**：手機那一段吃到 720px 寬，而**介紹區愈寬愈矮**（460~520 只有 381.9px，全段最矮）→ 圖高 ≤ 381.9 → 寬 ≤ 193.6 → 取 **188px**（390 上仍由 52% 決定 ＝ 他挑的那一格逐字不變，394 以上才由上限接手）。**不要調大**，430 上光用 52% 就高出 3px。⚠ 提案頁多了一把通用的「**版本**」尺（`--alt`／`--main-label`／`--alt-label`）：**切版本要連 `aspect-ratio` 一起換**（只換圖檔的話直的那一張會被壓扁），長寬比從 PNG 的 IHDR 讀。⚠ **已知取捨**：`.18` 在這一科的 AA 上限 **.118** 之上，實測柔墨 **4.19**（390 上 8 行壓到），是使用者看過數字之後選的，同牙周 4.32、兒牙 4.24、植牙 4.02 —— **不要拿對比度來訂正**。七科的臨界濃度：.101／.116／.107／.152／.120／.097／**口外 .118**，**每一科現算一次不要抄**。⚠ 驗收十四個寬度（含斷點兩側 720／721、兩段都量）介紹區高度與「把圖關掉」逐格相同、無水平捲動、每一格都收在介紹區裡（手機最緊的 500 還餘 11px、≥721 最緊的 834 以上還餘 9px） | ILLUSTRATION.md 第十二節、`/history/topic-lineart-surg.html` |
| **診所在地圖上的標記** | **綠塊直接寫「芳仁牙醫」＋ 一顆站在上面的圖釘，標誌搬進圖釘頭上的白圈**（2026-08-21 當天開、十輪定案上線）。起因是使用者拿 cizoo.com 的截圖：「我喜歡這個圖釘釘在地圖上的樣子……圖釘上圓圈就放診所的 logo，那個綠色的區塊就直接寫芳仁牙醫。」原本那塊**綠底白字的對話框（`.clab`）整組刪掉**。定案的一組：綠釘白圈／頭的半徑 24.48／腰身 30 度（頭與釘子的交接處 ＝ 頭的一半，看得出分界）／釘子長 1.55 個半徑／尖端圓角 .06／尖端落在 y 245（綠塊上緣 236）／字級 20 ＋ 底線／影子「中」、太陽高 65 度、往右上 30 度倒。⚠⚠ **影子不可以用 CSS 的 `filter: drop-shadow()`** —— 這張 SVG 整個在 `clip-path` 底下，**iOS Safari 會把那層濾鏡整個丟掉**（螢幕錄影逐幀量：切在「重」的那一幀對「無」只差 3.3 階、2 個像素，同一組值在 Chromium 上是 90 階、2000 CSS px²）。改成**畫出來的**：同一條釘身路徑疊十六層、每層很淡。⚠⚠ **影子是投影不是位移** —— 使用者：「你把整張地圖當成貼在牆壁上、光源照下去，但我想要的是**地圖平貼在桌上**、光源照下去的感覺。」這張圖是俯視圖、圖釘站在桌面上：尖端（接觸點）不動，愈高的部分被推得愈遠，寫成 `matrix(1 0 −L·cosφ −L·sinφ 0 0)`、`L = cot(太陽高度)`。圓形的頭因此變成**斜的橢圓**，那是對的。⚠ 疊出來的模糊，總濃度是 **1−(1−α)^層數**，不是 α×層數 —— 層數一改 α 要跟著換算。⚠ 釘子兩側是對「尖端那顆小圓」拉切線，**要取右邊那個切點**（取錯邊輪廓會從尖端折回去 ＝ 使用者說的「末端有奇怪的分岔」）。⚠ 顏色一顆都沒新增（綠 `--map-mark`／字與圈 `--card`／影子 `--ink`），地圖的 viewBox 與街廓、路名、停車場座標一個單位都沒動。⚠ 一起清掉的：`sizeLabels()` 裡只有那塊名牌在用的 `data-pin-k`／`data-ink-gap` 與 `inkLeft()`（停車場那三塊逐值相同）。⚠⚠ **上線當天追加：整張圖的名字從 `<title>` 改成 `aria-label`** —— SVG 的 `<title>` 同時是無障礙名稱**和桌機的原生提示框**，滑鼠停在圖上任何沒有自己 `<title>` 的地方（含三塊牌子）都會跳出「芳仁牙醫診所簡易地圖」那個框（使用者：「為什麼會出現這個附註框」）。改成 `aria-label` ＋ `aria-describedby` 之後機器照樣讀得到、畫面不再跳框；**診所的圖釘與停車場那三塊牌子則各自補上一個 `<title>`**，滑到哪一個就說「○○・在 Google 地圖上開啟」（和它們的 `aria-label` 逐字一致）。⚠ 三塊停車場**色塊**自己的 `<title>`（名字＋步行時間）**沒有動** —— 那三塊不是連結，點下去是帶出路線，寫成「在 Google 地圖上開啟」會是錯的 | `/history/clinic-map-pin.html` |
| **鄰近停車格** | **「位置與周邊停車」旁邊多一顆標籤，按下去才畫出 24 格路邊停車格**（2026-09-15 開、09-16 定案上線）。起因：「目前網站上的呈現只有付費停車場　**保留原來網站的樣子**　我另外加一個標籤是鄰近停車格　按下去再顯示這些位置」。⚠⚠ **格子的標記由 `node drafts/door-notice/map-bays.mjs` 寫進 `#map-clip` 的 `BAYS:START`～`BAYS:END`，不要手改** —— 座標的唯一出處是 `drafts/door-notice/parking-map.mjs`（清查那張 PNG 和站上這一份同一份座標），號碼的唯一出處是 `parking-survey.json`（那一支有守門在對）。**清查改了要重跑它，接著跑 `node tools/topics.mjs` 與 `node tools/build.mjs`。**⚠⚠ **標籤的幾何整組照抄它左邊那顆 `.card-map h3`**（`.88rem`、上下內距 `calc((34px × --type-scale − 1.6em − 2px)/2)`、圓角 12px）—— 兩顆藥丸並排，高度不可以各算各的；站上那顆在 ≥721px 乘了 `--type-scale`，這裡也要乘（不然電腦版一顆 38.25、一顆 32.2）。⚠ **上下內距不要換算成 px。**⚠ 格子＝**實心路牌藍 ＋ 最細密的虛線框**（`dasharray 2 1.6`、粗 1.2），虛線的顏色是 `#282b31` ＝ **夜間模式的卡色**，白天夜間釘死成同一支（使用者：「白天模式停車格白虛線感覺怪怪的，改成和夜間一樣的虛線顏色看看」）。⚠⚠ **透感白天 `.55`、夜間 `.72`，那不是漏改成一致**：白天的路面是 `--rule` 淺灰、路牌藍壓上去本來就重，要透多一點才退得下去；夜間的路面是 `#4c4f55`、格子是提亮過的 `#7a96ca`，透太多就沉進路面裡。⚠ 用 `fill-opacity` 不是 `opacity`（後者會連框線一起淡掉）。⚠⚠ **兩種模式互斥**：按標籤 ＝ 三塊停車場熄滅、**街名也一起淡下去**（`dim` 不能只看有沒有停車場亮著）；點停車場 ＝ 格子收起來。桌機的 hover 不跟著收（那只是預覽）。⚠⚠ **標籤關掉時要回到開頁那一態**（壹車房亮著 ＋ 它那條點狀路線，2026-09-16 使用者：「鄰近停車格標籤不亮的時候，要讓壹車房的停車場和路線亮」）—— 關掉之後停在「全部熄滅」是空的。**已知取捨：先點過合廷、再開關一次格子，回來的是壹車房不是合廷**（「關掉 ＝ 回到開頁的樣子」，沒有記住上一顆）。⚠⚠ **點地圖空白處：格子亮著的時候也回到開頁那一態**（2026-09-16 使用者：「鄰近停車格的標籤亮的情況下，點地圖任一個位置，也是回到壹車房停車場亮的模式」）—— 格子亮著時整張圖沒有任何停車場亮著，再收成「全部熄滅」等於從一個空的狀態走到另一個空的狀態；那一下**逐字等於把標籤關掉**（走同一條路 `setBays(false)` ＋ 回到 `DEFAULT_LOT`，驗收時兩條路的狀態逐格相同）。⚠ **「點地圖空白處 ＝ 全部熄滅」那一態還在，只是往後挪了一步** —— 格子沒亮時照舊一下就全部熄滅（2026-08-14 第四十二輪使用者退回過拿掉它的做法：要看清楚路名就得有一個什麼都沒選的狀態），格子亮著時是「第一下回壹車房、第二下才全部熄滅」。⚠ 標籤那顆 `<button>` 在 `<figure>` **外面**（`.bay-chips` 是 `.card-map` 的子元素、figure 的兄弟），所以按標籤不會冒泡到那個 handler、不會按一下被算成兩下。⚠⚠ **夜間沒點亮的標籤 ＝ 套色 20% 的淡色塊 ＋ `#90abe1` 的字**，照站上 NIGHT 區塊那條（底與框 `color-mix(--accent 20%, --card)`、字 `--pill-ink`）；路牌藍沒有現成的 `--pill-ink`，照「同色相只提亮度到 4.5」現算（淡色塊 `#384050`、原色壓上去只有 3.47、提到 L\* 69.9 ＝ `#90abe1`），實測 4.51，和站上其他藥丸的 4.54 同一級。⚠⚠ **那三條夜間規則刻意寫在 `.card-map h3` 後面那一段、不在 `NIGHT:START` 區裡** —— 那一區是 `tools/night-mode.mjs` 產的，手改會被下一次重跑蓋掉（`--check` 仍然全綠）。⚠ **已知取捨**：夜間**點亮**那一態白字對 `#7a96ca` 只有 **2.98**，站上夜間的填色藥丸本來就是這一級（`--accent` 在夜間沒有被覆寫，兒牙 2.54）—— **不要拿對比度來訂正**。⚠⚠ **不寫號碼是量出來的**：手機上永安路東側那一排有 **9 個數字**會被地圖的 slice 裁掉，而且一格只有 8.2×15.0px。⚠⚠ **文化路・平和街口以西那 5 格（示意、號碼還沒清查）手機上只看得到 1 格** —— 那一排落在 x 4~84，而手機左右各裁 65 個單位。**格子的位置是量出來的，不要為了讓它出現在手機上把它往右挪。**⚠ **裝卸貨車專用區（郵局門口）刻意沒有畫**：那一格停不得，畫在一張叫「鄰近停車格」的圖上是陷阱。⚠ 格子出現時三條路名讓開（文化路往東 62、永樂街往南 154、永安路往南 140），**用 CSS 的 `transform` 不要去改 `x`／`y`**（標籤要能原封不動退回去），`transition` 要把 `opacity` 一起寫回來（不然街名淡下去那一下變硬切）。⚠ 驗收：標籤沒按下去時地圖與改動前**逐位元組相同**（八個寬度 × DPR 3 × 白天夜間），唯一的差是永樂街那一條（見下一列）；上線之後整頁 69 項版面量測 **0 項有差**（六個寬度）；水平溢出 0、JS 錯 0 | `/history/map-bays.html` |
| **永樂街被壓到時移位** | **不再整條不見，改成往上 191 讓開**（2026-09-16 上線，和上一列同一輪）。⚠⚠ **這推翻了 2026-08-14 使用者自己定的那條**（「被路線或牌子壓到的街名直接不見」）—— 他 2026-09-15 改主意：「永樂街在壹車房亮的時候被我拿掉了，**現在看應該把永樂街的路名移位就好**」。做法：`hideCovered()` 把永樂街排除，另一支 `placeYongle()` 走三段階梯 **往上 191 → 往下 154 → 真的沒地方才交還給「藏起來」**。⚠⚠ **往上要讓開的不只那條點狀路線** —— 診所圖釘那片影子 `.cm-sh` 量到 x266~351、y228~245，**正好伸進永樂街那一欄（x324~354）**，所以 116 不夠（字底停在 y250 剛好落在影子上），191 才夠（字底 y175、`.cm-pin` 從 y183 才開始）。障礙物的清單是 `.dots.on`／`.rlab.on`／`.cm-pin`／`.cm-plot`。⚠⚠⚠ **`placeYongle()` 要排在 `paintLots()` 的最後** —— 它量的是 `.dots.on`／`.rlab.on` 的**實際外框**，排在那兩行 toggle 之前的話量到的是上一次的狀態，**開頁那一次就會停在原位**（踩過）。⚠⚠⚠ **量標籤的原位要用 `getBBox()` 不是 `getBoundingClientRect()`** —— 那個標籤身上掛著 .18s 的 transform transition，用螢幕座標會量到「動到一半」的位置，下一次判斷就踩在錯的基準上；`getBBox()` 不吃元素自己的 transform。⚠ 只有這一條路名改了行為，其餘（永安路14巷…）照舊 | `/history/map-bays.html` |
| **位置與周邊停車** | **首頁「診所資訊」的第一張卡**（2026-08-14 四十二輪定案上線）。線的畫法、圓角方外框；診所 ＝ 一般牙科綠 `#3f654a` 的實心塊＋白色標誌＋**綠底白字名牌**（⚠ **標誌與名牌 2026-08-21 換成「綠塊寫字 ＋ 站在上面的圖釘」**，見上一列；那一塊綠色的位置與形狀沒有動）；停車場平常是柔墨混 80%（`#7a7d77`），**點到才變路牌藍 `#365685`** 並帶出點狀步行路線、距離與時間、該場的入口箭頭。牌子是淺底藍字＋空心三角，上面有圖釘與停車場名字，連到**使用者本人分享的 Google 地圖短網址**。開頁先亮壹車房；點停車場以外的地方 ＝ 全部熄滅、街名回到滿的（那一態是為了看路名，**不要拿掉**）。⚠ **同一輪把地址與電話兩張卡拿掉了**，不是漏掉 —— JSON-LD 讀的是 `clinic.json`，而 HERO 窄帶與頁尾都各有一組，那是第三份。⚠ 藍是「點到」的意思不是「停車場」的意思，三塊都上藍就只剩深淺兩階可用（實測是死路）。⚠ 停車場平常那一階不能再更淡，白 P 只剩 4.18:1，下限 3。⚠ P 自己指定 Arial/Helvetica＋700，加粗靠 `paint-order: stroke` 的同色描邊 —— 中文字型堆疊在不同系統會合成假粗體。⚠ 三個連結的對應只靠使用者貼上來的順序，動它之前先在手機上點過。⚠⚠⚠ **2026-09-15 街網改過三件**（使用者拿 Google 地圖對照）：**① 文化路北邊多一條沒有名字的小巷**（中心 x 212、寬 12，只往北通到文化路、不往南）——位置是量出來的不是抓的：那張截圖（1125 寬）上平和街中心 169、永樂街中心 890、小巷中心 491 ＝ 兩條街之間的 **44.7%**，換算到 viewBox（109／339）＝ 211.7；寬度一把四格的尺（12／17／21）他選 **12 ＝ 站上那兩條沒有名字的巷子**。**② 平和街到文化路就到底、沒有穿過去** —— 所以文化路北邊那一排的西塊 `width 114 → 226`（橫跨平和街到小巷左緣 206）、中塊 `x 124 width 200 → x 218 width 106`；**其餘四排仍然留著平和街的缺口**（它從文化路往南是真的存在）。⚠⚠ 那一件的資料本來就在第一輪自己量的那張表裡（x=174 北邊一個像素都沒有），量到了卻沒有去讀 ——**通則：為了 A 去量出來的東西往往同時回答了 B，量完要把整張表唸過一次。****③ 平和街33巷補上街名**（使用者：「規格按照平和街19巷」）：同一個 `.lbl-xs`、**同一個 x=176**（和 19 巷上下對齊）、`y 331.5` ＝ 把字面框對到 33 巷那一條（321~333）的中線，和 19 巷對它那一條（219~236）的關係**逐字相同**（都低 0.75）。⚠ 那個 331.5 是算出來的不要手填 —— 兩條巷子一條 17 寬一條 12 寬，照抄 19 巷的 y 會掉出巷子。⚠ **永安路北邊那一截沒有動**：它在那張截圖的右邊界外（換算約 x 1304，圖只有 1125 寬），沒有證據就不改；永樂街那一截是對的（x 845~925 在文化路以北量得到）。⚠ 三件都只動 `index.html` 的街廓與街名，**顏色一個都沒加、其餘座標一個單位都沒動**；改完要跑 `node tools/topics.mjs` 與 `node tools/build.mjs`，`night-mode --check` 仍然全綠。⚠⚠⚠ **2026-09-18 第四件：33 巷過了永樂街那一條更窄的巷子**（使用者拿 Google 地圖的空照截圖：「仔細看平和街33巷過了永樂街，有一條更窄的既有道路連通到永安路」）—— 永樂街～永安路那一欄的街廓 `x354 y197 h206` 拆成 **`y197 h135` ＋ `y338 h65`**，巷子 **寬 6、中心 y335**，西接永樂街 x354、東到永安路 x455，**不標街名**（它沒有名字，同文化路北邊那條與 19 巷北邊那條平行巷）。⚠⚠ **兩個值都是使用者在提案頁上逐格挑的，而且兩格都不是我的預設**（我送的是「寬 8 ＋ 齊 33 巷」）——**「往南 8」的意思是它不是 33 巷的延伸、是另一條巷子**，那正是「位置」要做成一把尺而不是直直畫過去的理由。⚠⚠ **它是「地上有、圖資沒有」的那一種**：Google 自己畫的 33 巷路帶在那張截圖的 x≈1330（永樂街東邊一點）就收成一個圓頭，所以**不要拿 Google 的路名回頭訂正它**。旁證躺在自己家的檔案裡 ——`drafts/door-notice/parking-survey.json` 那一輪現地走過「33 巷整條約 230 m」，而同一份清查裡永樂街→平和街 100 m ＋ 永樂街→永安路 85 m 才 185 m。**通則：使用者從外面的畫面指出一件事時，先翻一次自己家的檔案有沒有對得上的紀錄。**⚠⚠ **街廓要拆成兩塊 rect，不可以在街廓上蓋一條路面色的長條** —— 站上每一塊街廓都有 rx=4 的圓角，蓋長條的話巷子兩側是直角，那是一張看起來很像的假圖。⚠ 寬度與偏移**在那張截圖上量不到**（1 個 viewBox 單位只有 5.5 個像素，又是斜照的 3D 影像），所以做成尺。⚠⚠ **這一件跟著要重跑的比前三件多兩支**：`drafts/channels/post-map-door.mjs` 與 `post-fb.mjs` ——商家貼文與臉書那幾張「位置與周邊停車」的圖是**讀 index.html 畫的**，⚠ 那是已經匯出、可能已經貼出去的成品，**要不要換掉貼出去的那幾張是使用者的決定**；`drafts/door-notice/parking-survey-map.png` 也是拿站上那張圖當底，但它的產生器在暫存區、沒進版控，沒有跟著重畫。⚠ 順手訂正一件：抬頭那段註解寫著「14 巷切永樂街以東兩欄、33 巷切西邊三欄」，和底下實際畫出來的 rect 對不起來。⚠ 還沒問到的兩件：那條巷子要不要標名字；空照圖上另外看得到一條**南北向**的窄巷（換算約 viewBox x≈388、寬約 7，從這一條往北通）要不要一起畫。⚠⚠⚠ **2026-09-18 第五件：壹車房那塊牌子在手機上往下 16，尖端留在原地**（使用者拿手機截圖：「壹車房的對話框可以往下移一點　讓大同路正常顯示　**對話框指向的位置不要變**　讓整個對話框呈現往左下拉出來的感覺」）。⚠⚠⚠ **成因是兩件事疊起來、而且只在手機發生**：手機的牌子比電腦版高 16（字級 16 → 22.4），整塊又 `--map-dy: -18` 往上（2026-08-15 使用者挑的），所以上下的餘裕從電腦版的 **28／29.5** 變成 **4.3／37** —— 大同路壞在上面那 4.3。⚠⚠ **而我這台量到的 4.3 是「還沒壞」，他手機上是 0** ——`hideCovered()` 的餘裕是 **2 個螢幕像素**，而 iOS 的字面框比 Chromium 這邊高一階（PingFang 的 ascent+descent ≈ 1.40em、Noto Sans TC ≈ 1.16em），算下來那一格正好貼到 ＝ 整條街名被藏起來。**通則：凡是靠 `getBoundingClientRect()` 比對外框的判斷，我這台量到的餘裕要當成「他那邊會少 4~5 個單位」，剩個位數就等於壞了。**⚠⚠ **16 不是挑的是算的**：`(37 − 4.3) ÷ 2 ≈ 16` ＝ **把兩邊拉回電腦版那種對稱**（390 上量到 20.3／21.4，七個手機寬度 19.7~22.9）。上限是幾何不是美感 —— 再往下 20 以上牌子的下緣會壓到中華路，**換它被藏起來**。⚠⚠⚠ **`--map-dy` 和新的 `--map-drop` 是兩件事，不要合成一個**：前者連尖端一起搬（他 2026-08-15 挑的那 −18，尖端因此落在 y476 而不是電腦版的 y492），後者**只搬盒子**、尖端留在 y476。⚠⚠⚠ **底邊要往上挪到「剛好把尖端含進來」為止**（`bc = 盒子中線 − max(0, DROP − bw)`）—— 尖端跑到底邊外面的話，那一側的輪廓會從盒子邊往回折出一個**缺口**，畫出來是一支帶鉤子的細旗子；**2026-08-15 使用者就為了同一種形狀退過一次**（「壹車房的對話標籤形狀很怪」，那次是盒子的右緣越過尖端）。那條式子在 DROP 為 0 時回 0 ＝ 和改動前逐字相同，所以**電腦版與 iPad 的那條路徑一個字都沒有變**（721/744/834/1112/1440 逐字相同）。⚠ 要讓尾巴**本身**更斜就加寬底邊（`data-bw`，預設 7），不要去動那個 clamp —— 缺口就是它在擋的東西。⚠ 只有 `dir === "right"` 吃 `--map-drop`（朝下那兩塊要斜得另外給一個「底邊中心」，同診所那塊當年的 `data-bx`／`data-tx` —— 現在沒有人需要，所以沒有做）。⚠ 驗收：13 個寬度 × 白天夜間 —— 尖端 y 逐格和改動前相同、大同路與中華路都沒有被藏起來、水平溢出 0、JS 錯 0；`night-mode --check` 全綠。⚠ 這一件**只動 `index.html` 的樣式與那支 `sizeLabels()`**（街廓、街名、座標、顏色一個都沒動），跟著跑 `node tools/topics.mjs` 與 `node tools/build.mjs`；**商家貼文與臉書那幾張不必重跑** —— 它們畫的是電腦版（`post-map.mjs` 開 1440）或門口那張告示自己的地圖 | [PALETTE.md](PALETTE.md) 第六之二十一節、`/history/clinic-simple-map.html`、`/history/clinic-map-alley.html`、`/history/map-alley-east.html` |
| **文章頁印「最後更新」，卡片不印** | 2026-09-20 定案 **Ⓔ：貼著 HERO 圖的下緣**（`<p class="post-updated-line">`，十六篇一起上線）。起因是體檢的 B5 —— Google 對文章日期要的是**畫面與結構化資料兩邊都有、而且一致**。規格跟站上原本就有的 `.post-hero figcaption` 借（`.55rem` 上距／`.78rem`／`--ink-soft`／同一組 `--content`＋`--pad`），所以字的左緣和圖、和導言是同一條；**但沒有真的做成 figcaption，日期不是圖說**。⚠⚠ **首頁卡與延伸閱讀卡不印**，使用者指定「首頁圖卡不用」，和第一節第 3 條一致 —— **不要順手補上去**。⚠ 那一行的字由 `build.mjs` 寫（靠 `time.post-updated` 找到），格式是 `2026/09/20` 不是 `zhDate()`；它**不進內容雜湊**（`normalize()` 已有佔位符規則），所以不會變成日期迴圈。落選的四個位置與量到的數字：Ⓐ 頂端那一行（**連 430px 都塞不下**，中間只空 54.5px、最短寫法要 76.4px，而且 +31px 加在大標題正上方）／Ⓑ 併進文末免責 +25px／Ⓒ 免責上面 +42px／Ⓓ 延伸閱讀上面 +50px；Ⓔ 是 +32px。⚠⚠ 量頂端那一行時**兩件事都量錯過**：折行偵測不可以用「子元素 top 不一樣就算一列」（那是 flex 的垂直對齊，要照左緣有沒有往回跳數），以及**本機沒有 Worker 時 `.views` 整個 `display:none`**，不補成真值會多量出 68~80px。⚠ 量那一行的左緣**不可以量盒子**，它有 `padding-inline`，要用 `Range` 量畫出來的字。⚠⚠ 這一輪之後**第五節那個陷阱變重了**：一次改到全部文章的 `<main>` 而不還原 `updated` 的話，**十六篇會一起印出當天的日期**，那是讀者直接看得到的假話（這一輪誤傷七篇，已照程序還原）。成績 B5 0→1、總分 88→89 | 第一節第 3 條、第五節、`/history/b5-updated.html` |
| **文章的外部權威引用** | 2026-09-20 使用者查好六組來源，補進六篇（＋另一台的〈八成人有牙周病〉自己就有，16 篇裡 8 篇有）。**行內連結、描述性錨點文字**，不是文末列參考文獻（照 `aligner-simulation` 那一篇的寫法）。⚠⚠ 同一輪**訂正了四件事實**，不是只補連結：①〈三個月一次〉的「部分身障人士」→「身心障礙者」（法規不是「只有部分適用」，是依是否屬特殊醫療服務計畫走 91104C 或 91090C，**都是三個月一次**；寫「部分」會讓人白跑一趟）②〈孩子第一次看牙〉的塗氟與窩溝封填**不是健保給付、是政府補助**（經費來自公務預算與菸捐，只是透過健保卡與健保署作業申報）—— 內文、h2、重點整理、meta description、`post-meta` 摘要、免責句**六處都要一起改**③〈牙周病治療〉補上健保正式名稱**齒齦下刮除術（91006C～91008C）**，「牙根整平」只出現在 91015C／91018C 的註記裡，讀者拿收據對帳時找的是前者 ④〈兒童舒眠〉的法規句整句重寫，見下一列。⚠⚠⚠ **不要連全聯會那張「您可以 3 個月洗牙一次喔」單張**（111/08/31，ntid=24）—— 它列的是中風／帕金森、**完全沒有 65 歲以上**，連了會和站上自己的名單打架。⚠ 那是**年度公告計畫**，115 年度版上線前要再確認一次。⚠ 窩溝封填的年齡規定改過很多次（113/6/14 從「6～9 歲」放寬到「6～未滿 12 歲」、113/11/27、115/5/14 公告 115/9/1 生效），所以**要連口腔健康司的法規頁面**（會指向最新版），**不要連固定的舊版 PDF**。⚠ 臺灣牙周病醫學會官網**沒有公開的民眾衛教**（民眾專區是空的、文宣要會員登入），牙齦炎／牙周炎的分界改掛 2017 World Workshop 的兩篇共識（doi 10.1111/jcpe.12940 與 .12946）。⚠ 這一輪的改動**是真的內容變了**，所以六篇的日期**讓它跳、沒有還原**（第五節的判準：補證據＋訂正事實對讀者算內容變了） | 第五節、`drafts/seo-geo-audit/REPORT.md` P2-1 |
| **⚠⚠ 特管辦法管的不是牙科** | 〈兒童舒眠〉原本寫「受規範的特定手術應由他全程在場、親自執行」。字面上沒說錯，但**《特定醫療技術檢查檢驗醫療儀器施行或使用管理辦法》的適用範圍是特定美容醫學手術**（削骨、拉皮、鼻整形、義乳、抽脂那八類），**不是牙科處置，更不是兒童牙科鎮靜** —— 放在一篇談兒童鎮靜的文章裡，讀者百分之百會理解成「兒童牙科鎮靜受這條管」。2026-09-20 整句重寫成**講明適用範圍、再說明「可以拿來當判斷的尺」**。另外訂正兩個用詞：法條寫的是**「重度鎮靜」不是「深度鎮靜」**；而且「屬中度、輕度鎮靜者，才得由**手術醫師以外**、受過麻醉相關訓練的醫師執行」—— **「手術醫師以外的第二名醫師」這個條件原本的句子沒帶到**。⚠ 牙科鎮靜**目前沒有專屬法規**，日後看到有人說「牙科鎮靜依特管辦法」，那是誤用 | `posts/kids-sedation/`、第五節 |
| **看診時間本身** | **維持 08:45–11:30／13:45–16:30／17:45–20:00**（使用者 2026-08-13 指定）。⚠⚠ 換版型的過程中曾一度改成板子上的 12:00／16:40／20:30，**使用者當天就指定改回來** —— 所以**不要再依板子或 LINE「訂正」這三個數字**，那是明確的決定，不是漏改。⚠⚠ **2026-09-20 更新：三邊不一致已經收斂了，而且是站外改過來的** ——
使用者把 Google 商家檔案與 LINE「門診時間」改成網站這一組，
並再次確認**週六早上沒有診**（那件從 2026-08-13 掛到那天）。
**網站一個數字都沒有動。** 還沒收的只剩 LINE「營業時間」（08:35–12:05，有空再收）
與門口的磁鐵板（實體牌子，機器讀不到、不影響 SEO）。細節見第十節。休診日沒變（週六、週日），但**「週六、週日休診。」那句話 2026-08-13 由使用者拿掉了** —— 表格只列一到五就看得出來，`tools/schema.mjs` 因此改成**從表格反推休診日**（沒有列出來的那幾天），不再讀那句話。底下只剩**「國定假日與各科特別門診請來電確認。」**（使用者定的字，逐字，不要潤飾），而且**不是紅字** —— 使用者指定用 `--ink-soft`，`--brick` 因此在首頁上一次都沒用到了—— 兒牙、口外這類特別門診的醫師不是每週都到，但**不要在表上標哪幾顆是隔週、不要列日期或當月排班** | 第十節、`/history/clinic-schedule.html` |
| 首頁區塊改名 | 「治療項目」→ **主題與科別** | [COPY.md](COPY.md) 第六節（含六輪否決清單） |
| 「最新文章」小節標題 | **拿掉**，只留 `aria-label` | 同上 |
| 窄帶瀏覽數 | 加在 1983／9／6 **上面**，單位「次瀏覽」，捲進畫面才往上數 1.4 秒 | 本檔第八節 |
| 那個動畫看不看 `prefers-reduced-motion` | **不看**（2026-08-08）。iOS 的「減少動態效果」很多人開著，看它就等於數字永遠不動；那個設定針對的是位移／視差／縮放，原地長大的數字不屬於那類。頁面其他三處 CSS 仍然照看 | — |
| 瀏覽數的字 | **黑體**（和「次瀏覽」同字同級同色），襯線版落選 | `/history/home-mobile-n3-views-serif.html` |
| 文章卡計數器 | 「日期 ・ 數字 次瀏覽」，**不做動畫**。眼睛圖示與日期前的「更新」都已拿掉 | — |
| **HERO 詩的第三行** | **`裡裡外外都在換　講究　顧牢牢`**（2026-08-16 六輪定案上線）。原本是「有跟著換的　也有一直顧的」，使用者：「還是比較模糊」——**它沒有指名道姓**，換的是什麼、顧的是什麼全靠讀者自己填。要講的是四十年間設備、空間、環境一直在換，但講究沒變、品質一直有人守著。⚠ **「顧」是使用者指定要留的**（「台語會用到這個字，蠻貼合在地的感覺」），而台語裡「顧」的受詞是**人或看得見的東西**（顧囝仔、顧店），所以「顧講究」念起來卡、`品質一直顧` 會斷在半空（他自己聽出來的），要有尾巴／受詞／人才收得住。⚠⚠ **「顧招牌」被否決得很準**：「招牌可以顧著沒錯，可是顧著不見得跟人有連結，人家會覺得說你顧招牌、做你想做，不見得是病患的需求」—— 顧招牌是**顧自己的名聲**，方向朝內。⚠ 「顧的還是人」同樣落選（「那不然我們顧的是鬼嗎」）。⚠ 「品質」被換成「講究」是因為**它是從做的人那一側講出來的** —— 品質是驗收的人給的評語，講究是自己給自己的要求。落選詞：機器（太窄）、硬體（不夠白話）、招牌、良心（醫療廣告用爛了）、老規矩（顯得不進步）、手路（要想一下）。⚠⚠ **第二個斷點是次級的**（`.g2`，`letter-spacing: -.57em` ＝ 四分之一形的空格，寬 .43em ＝ 第一個斷點的 36.5%）：「講究」與「顧牢牢」是並列的兩件事，但合起來才是這一句的下半，**三個等寬的斷點會被讀成三件並列的事**（使用者：「人家就說怎麼還有第三段」）。字元本身仍是全形空格 U+3000，複製與朗讀完全一樣。⚠ 那個 −.57em **跟著字距走**（＝ 字距 − .75em），字距一改就要重算 | COPY.md 第一之二節、`/history/hero-line3.html` |
| **HERO 四句的字距** | **統一 `.18em`**（2026-08-16 上線，和上一列同一輪）。改動前是**兩條不同的規則**：詩 `.32em`、收尾句 `.12em`，**差 2.67 倍**（390 上 4.95 vs 1.86px），使用者一眼看出來：「前三句的間隔都很大，比第四句還大，為什麼？」⚠ 他先要了兩個極端（三句收成 .12／收尾句放成 .32），**兩個都否決**：「四句的文字間隔要維持一致，收窄跟放寬都不太對，做一個中間的」；五案（.12／.18／.22／.26／.32）比過選 **.18**（不是正中間的 .22）。⚠⚠ **這一改一半推翻了 2026-08-09 那一輪**（提案「Ⓒ 鬆」把字距從 .2 放到 .32，起因是使用者的朋友說「logo 的字跟 cover image 的字大小差不多，看起來 noisy」）—— **行高 2.45、首行後 .9em、max-width 40em 一個值都沒動**，只有橫向收緊，而且 .18 比那一輪之前的 .2em 還窄一階。當時只比了詩自己，沒有把收尾句放在一起看。⚠⚠ **詩的 letter-spacing 在 `index.html` 裡有兩處**（基底規則 ＋ 手機的 @media，刻意各寫一份），**改要一起改**；收尾句只有一處，它的 `::before` 暗斑是 em 的 inset，會自己跟上。⚠ 五個視窗（320／375／577／834／1440）實測不折行、無水平捲動，對比度手機三個寬度一格都沒變 | 同上 |
| **收尾句的字級** | **和詩一模一樣大（Ⓕ2，2026-08-12 上線）**。兩條 clamp 併成一個 `--hero-fs`，`.hero-poem` 與 `.hero-sign` 都讀它，四段尺寸各只改這一個變數，**四種版型（手機／電腦／iPad 直放／直立）全部推平**。⚠ **不要再給 `.hero-sign` 寫第二條 font-size** —— 改動前它自己有一條 clamp，三個端點和詩的都不一樣，「小多少」因此隨螢幕寬度漂（375 上 .837、430 上 .957）。⚠ 落選 Ⓕ1 ×.92、Ⓕ3 ×1.08 | 本檔第八節、`/history/hero-sign-size.html` |
| **照片與窄帶的接縫** | **接縫在窄帶那一側解決，照片不准動**（2026-08-12 四輪定案上線）。手機的窄帶改成從 `#151311`（照片最後一列的實測值）起跑、**整條 smoothstep 攤到 100%**（smoothstep 起點斜率是 0，接縫這一側自動是平的）—— 比原本深，而且漸層長度 53px → **107px**、最陡的一列少 40%，這是使用者第二輪說「從黑邊到深藍有點短、很突兀，可以再拉長一點」改的；第三輪再說「黑漸層可以再往下一點」，曲線因此改成 **S(t^1.6)**（黑撐到窄帶的 51%，原本 36%），頭尾斜率仍然是 0。`--band-bg` 因此不再是色停（變數還在，後備色與分隔線在用）。兩層的 alpha 一律改成 smoothstep（**顏色與範圍一個都沒動**）。iPad 的柏油層維持 84px，只把最後 24% 從 `.88` 收到 1。三種版型三個成因：手機**顏色與色溫都對不上**（ΔL\* +2.69~+3.65、暖轉冷）、iPad 是**殘影從 12% 突然斷成 0**、電腦／旋轉是**線性漸層起點的一階導數不連續**（馬赫帶）。二十二個視窗量過，手機那 291 列**逐像素 0 個有差**。⚠⚠ **不要在照片下緣疊柏油漸層** —— 第一版那樣做，被使用者否決（「照片都被弄掉了」）。⚠ 這**不是**六之三節否決的「深一階」：那一案不透明的位置在窄帶**下緣**（螢幕最底），這裡在**照片與窄帶的交界** | [PALETTE.md](PALETTE.md) 第六之十九節 |
| 電腦版 HERO 裁切 | **整屏 `100svh` ＋ `object-position: 50% 100%`**（下緣貼齊、要切切天空） | [PALETTE.md](PALETTE.md) 第六之三節 |
| 電腦版窄帶位置 | **壓在整屏下緣**，不是接在照片下面 | 同上 |
| **iPad 直放** | **照片 `70svh` ＋ 窄帶脫離照片接在下面**，地址電話一行置中夾在瀏覽數與三格之間。接縫保留原本的模糊＋柏油兩層（原地搬到照片最後 84px，數值一個沒改），窄帶從實測合成色 `#161413` 接下去。關在 `@media (min-width:721px) and (min-aspect-ratio:2/3) and (max-aspect-ratio:9/10)`，四組尺寸逐項比對過電腦版與手機版沒被動到 | [PALETTE.md](PALETTE.md) 第六之十八節 |
| 直立（旋轉）螢幕 | **照片不整屏 `62svh`**，字級改讀 `svh`。關在 `@media (min-width: 721px) and (max-aspect-ratio: 2/3)` 裡，**電腦版與手機版不受影響**（iPad 直放 0.75 這一段吃不到，**改由上面那一列的 `(2/3, 9/10]` 接手，不是還在看電腦版**）。直排落選 | [PALETTE.md](PALETTE.md) 第六之十三節 |
| 窄帶三格間距 | **拉開**（左右各 4rem） | 同上 |
| 兒牙的科別色 | **1a `#c28229`**（十一輪；字與框用 `#9e6301`）。兩個提案頁都已套上、切換條已刪 | [PALETTE.md](PALETTE.md) 第六之二節 |
| chip 的兩態 | 未選＝白底＋深階字與框，選取＝套色填滿。**兒牙不例外** | 同上 |
| 窄帶底色 | **毛玻璃輕 ＋ 柏油漸層「標準」**（「深」落選：底端收成實色，等於把毛玻璃還回去） | [PALETTE.md](PALETTE.md) 第六之三節 |
| 窄帶第一格文案 | 1983年 **中華路開業**（原本是「開業至今」） | — |
| **窄帶的地址與電話（電腦版）** | **一行、中間 1.9rem；貼版心外緣、離照片下緣 15.18px；線條空心圖示（Lucide）；底線常駐；靜止 `.65`／hover `.95` —— 這條帶子上只准兩個白** | [PALETTE.md](PALETTE.md) 第六之十七節、`/history/hero-contact.html` |
| **同上（手機版）** | **一行置中，夾在瀏覽數與 1983／9／6 之間**；字 `.7rem`、圖示 `.762`／`.710rem`（照電腦版的比例換算，不是照搬）；≤360 間距收到 1.1rem 防折行。**標記因此必須待在 `.shell` 裡**，電腦版靠絕對定位脫離版面 | [PALETTE.md](PALETTE.md) 第六之十七節第八小節、`/history/hero-contact-mobile.html` |
| 手機窄帶的 1983／9／6 | **往下坐 2px**（`.stats` margin-top 2 ＋ shell 下內距 −2）。原本上面 9.7／下面 13.8，現在 11.67／11.78，**窄帶高度沒變** | 同上 |
| 那組圖示的大小 | 釘子 `.85rem`（畫出 12.68px）、話筒 `.7918rem`（11.33px）＋下移 .8px。**要拿「字面」比不是「字級」** —— 數字沒有下伸部，字面只占字級 0.88，用字級算話筒會矮一截、看起來在飄 | 同上（第六之十七節第四小節） |
| 文章卡陰影 | 靜止就有，hover 加重；**位移／模糊／alpha 三個量一起放大**，縮圖同時 `scale(1.05)` | [PALETTE.md](PALETTE.md) 第六之四節 |
| 手機版的卡片 | **只有靜止陰影，沒有 hover**（沒有游標，`:hover` 會黏住） | 同上 |
| 兩頁誰是準 | **電腦版是準**。手機版先做、推廣成電腦版，電腦版定案後再回推手機版 | — |
| 主選單 | **兩頁都三項**（全部文章／醫師介紹／診所資訊）。「主題與科別」不放進去 | [PALETTE.md](PALETTE.md) 第六之五節 |
| 主題與科別 | 標記最前面一顆「全部」，後面接搜尋框（同時篩文章與醫師） | — |
| 「全部」的灰 | **線框 `#4c4948`／填色 `#5f5d5c`**，兩階都錨在頁首玻璃的 `#393736` 往上提亮度 | [PALETTE.md](PALETTE.md) 第六之五節 |
| 頁首「全部文章」 | 跳到 **`#topics`**（主題與科別），不是 `#articles` —— 篩選工具在那裡 | — |
| 文章頁的重點色 | **跟著該篇的科別走**，寫在 `<body data-spec="…">`。字階要用對紙的那一階，不能從首頁搬 | [PALETTE.md](PALETTE.md) 第六之六節 |
| **植牙・假牙重建**的藍 | **填色 `#465885`、白底的字 `#2a4677`**（填色 Ⓒ、字階 Ⓓ3，2026-08-31 同一天先後兩輪）。⚠⚠ **字階那一輪的起因**：使用者看到剛上線的成品說「內文的字可以也調看看？現在的看起來很深色很黑」——量出來 **全站的墨 `--ink #2a2c27` 是 L\* 17.6，而當時的字階是 L\* 17.7，一樣深**，所以那顆彩色的重點字讀起來就是黑的。它也是全站唯一的離群值（比第二深的根管 30.7 還深 13.1 階，兩階落差 20.1 是全站最大）。⚠⚠ **它被壓那麼深是 2026-08-09 為了讓牙周（後來整組給矯正）的深階活下來，而矯正的深階同一天早上已經搬走（ΔE 25.4）** —— **通則：「為了讓別人活下來而委屈的值」，等別人搬走了要記得回來鬆綁**（這一顆委屈了三週沒有人回來看過）。新值 L\* 29.9 和根管 30.7・全部 31.3・一般 31.5 同一群，兩階落差 **7.9**（家族 5.9~15.1）。⚠ 那一輪**卡不到 AA**（四案的字對紙 7.40~11.14，門檻 4.5），判準是「讀起來像不像一個顏色」。推導在 `/history/spec-prosth-deep.html`。（Ⓒ，2026-08-31 換掉 2026-08-09 那組 `#335b8b`／`#182f4b`）。⚠⚠ **來源是使用者拍的富山路面電車 5・8 號線路線圖**：「看起來跟植牙假牙贗復的標籤主題色很像。」量法是那條線八段逐段取中位數、每段各自用旁邊的看板底色做白平衡 —— **不是目測配色**。⚠ 定案的 Ⓒ **只取實測的色相與彩度（h 282.6・C\* 28.0），亮度留在家族帶**（L\* 37.8，和舊值 37.9 幾乎一樣），走的是兒牙那一輪定下的做法：**顏色的出處是色相，不是那個 HEX**。落選 Ⓐ `#3f527e`（實測原值）、Ⓑ `#3c4c71`（L\* 32.5，掉出家族帶 36~48）。⚠⚠ **換來的：全站最近的一對填色從 13.3（矯正×植牙）拉開到 16.0** —— 加上同一天稍早換掉的矯正深階，**第六之十四節那句「矯正 × 植牙是全站最近的一對（13.3／10.5）」整條都不成立了**（深階彼此現在 25.4，全站最近的深階對是一般×牙周 18.6）。⚠ 色相從 273 走到 282，**往第六之十三節被使用者退回的「藍紫」方向回去 9 度**（當年是從 287 拉到 273）——他在提案頁上看過真實版面才選的，不是漏看。⚠⚠ **這一顆的顏色烘在兩個產出檔裡，換色一定要一起重跑**：`assets/lineart-prosth.png`（吃**套色**）與 `assets/og-topic-prosth.jpg` 的帶子（吃**深階**，補償色 `#3971bd` → `#143162` → 字階再提亮後 **`#2e55a2`**）。⚠ 順帶查出一件**本來就存在的落差**：那張分享圖的文件寫的是「定案用套色版」，但線上跑的那張帶子量出來是**深階**。這一輪照線上的實況（深階）重做，沒有改變它的長相。⚠ 兩階仍是同色相（Δh 1.4°）。八個位置 0 個不過（白字 7.01、字對紙 11.14）。彩度要跟著亮度按色域比例走，固定住會變灰、和矯正撞在一起；**字階刻意探到 L\* 18.9，是為了讓牙周的深階活下來**（那條約束因為矯正深階已換而解除，字階這一輪停在 17.7）。彩度要跟著亮度按色域比例走，固定住會變灰、和矯正撞在一起；**字階刻意探到 L\* 18.9，是為了讓牙周的深階活下來** | [PALETTE.md](PALETTE.md) 第六之十、十三、十四節 |
| **齒顎矯正**的藍 | **套色 `#4478b5`、白底的字 `#31637f`**（套色 2026-08-10 從牙周整組接手，**白底的字 2026-08-31 從 `#244369` 換掉**）。⚠⚠ 換字階的起因是使用者在別的提案頁上看出來的：「矯正的深階字套色顏色看起來很深，反而很像假牙重建的顏色。」他是對的，而且指出一個**這份文件從來沒算過的距離** —— 標記那一排上**被選到那顆用填色、其餘六顆用深階**，所以真正並排的是「**A 科的深階 × B 科的填色**」，而 PALETTE.md 全篇比的是「填色對填色、深階對深階」，那兩種組合在那一排上根本不會同時出現。補算：矯正的深階對植牙填色只有 **11.3**，其他六科是 27~87，色相更只差 0.7°。⚠⚠ **通則：日後動任何一顆科別色，四種距離都要算** —— 填色×填色、深階×深階、自己的深階×別人的填色、自己的填色×別人的深階。⚠ **只提亮是死路，不要走回去**：同色相往上提會直接撞進植牙的填色（L\* 37.9 那一格算出來是 `#335b8c`，ΔE **0.6**）——卡住的是「矯正和植牙同色相」不是亮度。⚠ 定案的 h 249.3 **正是矯正原本那一支藍**（`#3C596B` 是 h 248.1，2026-08-10 換掉之前用的就是它）。⚠⚠ **代價：矯正是全站唯一「兩階不同色相」的科**（Δh 23.5°，其餘六科 0.2~4.1°）——使用者說「不同色相是什麼意思我看不懂」，所以另做了一頁把**同一顆標記的兩態並排**（左＝被選到填滿、右＝沒被選到）＋ 口腔外科當對照，他看過才定的。⚠ 換來的：兩階落差 21.6 → **9.7**（回到家族刻度），**全站最近的深階對從 10.5（矯正×植牙）變成 18.6（一般×牙周）**；八個位置 0 個不過（字對卡 5.93、對紙 5.15，和口外完全同一級）。⚠ **套色 `#4478b5` 一個字都沒動**，白字 4.57 仍是全站最緊的一格。⚠ **矯正的線稿與分享圖都不必重產** —— 那兩張吃的是填色那一階。⚠ `tools/og-plate.mjs` 的 `DEEP` 表已經跟著改（那支的帶子可以吃深階）。從使用者給的 `#3496F2` 收到「白底黑底等效」那條線上。從使用者給的 `#3496F2` 收到「白底黑底等效」那條線上。**白字只有 4.57:1，是全站最緊的一格，不能再亮**。它同時解掉「矯正不夠亮」：L\* 36.3 → 49.5，**提亮那一輪（第六之十二節）整組作廢** | [PALETTE.md](PALETTE.md) 第六之十四、十六節 |
| **牙周治療**的青綠 | **套色 `#317d78`、白底的字 `#2a6d69`**（Ⓣ1，2026-08-10）。色相來自診療椅／深綠松那一支（h 186~190），**牙周等於回到診療椅**。填色頂在白字剛好過 AA 的高度。⚠ 字階對全站連結色深綠松只有 ΔE 13.6，是全站第二緊的一格 | [PALETTE.md](PALETTE.md) 第六之十六節 |
| 色票藍灰 `#3C596B` | **釋出**（矯正原本用它）。和 `--moss`／`--taupe`／`--tile` 一樣留在變數表裡，不再有科別指向它 | 同上 |
| 顯微根管的紅 | **套色 `#ae4f4d`、白底的字 `#89202d`**（⑪·⑥，2026-08-09）。深階是開幕紅花籃的實測原值。**苔綠＝Logo 色被釋出，科別色裡不再有 Logo 色 —— 這是已知的取捨，不要提議改回去** | [PALETTE.md](PALETTE.md) 第六之十一節 |
| 一般牙科・創辦醫師的綠 | **套色 `#3f654a`、字 `#2c5238`**（⑪·Ⓑ，2026-08-09）。色相＝花藝實測「枝葉亮處」的色相；兩階只差亮度 7.9。`--chair` 釋出但留著。**牙周已於同日拆出去自己一色**，不要再把它併回這一條 | [PALETTE.md](PALETTE.md) 第六之十一節第七小節 |
| 兒牙填色上的白字 | **維持白字**（3.22:1，低於 AA）。2026-08-08 把備案做成實際樣子看過才決定的，**不要再提** | [PALETTE.md](PALETTE.md) 第六之二節 |
| **搜尋紀錄**（2026-09-20） | **記，但只記統計不記人**。存的只有四欄：查詢字串、來源（`home` 或 `topic:<spec>`）、當下搜到幾筆、ISO 時間戳。⚠⚠ **沒有 IP、沒有 User-Agent、沒有 cookie、沒有任何能串起同一個人的識別碼 —— 不要順手加**：這是牙醫診所的站，訪客打進去的字本身就可能涉及健康狀況（「牙齦流血會不會是癌」），一旦能回溯到個人就從統計變成健康相關的個資。⚠ **不是每按一鍵記一筆**：站上那支篩選器是逐鍵即時篩的，照著記的話「植」「植牙」「植牙費」會變成三筆，而排名要的是他**最後**找的那個字，所以只在「這次搜尋結束」時送（Enter／失焦／離開頁面／停手 8 秒），同一次瀏覽同一個字只送一次。⚠ **「搜到幾筆」要把診所資訊那兩張卡算進去**（算法照抄 `head-search.js` 的 `infoHits`）——漏算的話「停車」會被記成搜不到，報告就會叫人去寫一篇根本不缺的文章。⚠ **同一個字可以同時出現在「搜得到」與「搜不到」兩張表**（站上後來多了那篇文章，它就從搜不到變成搜得到）——硬歸成一類會讓其中一張說謊。⚠ 報告頁 `/admin/search/` **本身沒有任何資料**，只有一個密碼框；密碼在 Cloudflare 的加密變數 `REPORT_KEY`（repo 是 public 的，寫進來等於沒鎖），沒設就一律拒絕（fail closed，不會變成「沒設＝不用密碼」）。⚠ **刻意不寫進 `robots.txt`**：那份是公開檔案，Disallow 等於公告這條路徑存在，而且它擋的是檢索不是收錄；真正有效的是 Worker 的 `X-Robots-Tag` ＋ 頁面自己的 meta。⚠ 資料表由 `src/search.js` 自己建（`CREATE TABLE IF NOT EXISTS`），**不必手動跑 wrangler** | `src/search.js` 檔頭、`assets/search-log.js` 檔頭、`d1-schema.sql`、守門 `tools/search-test.mjs` |
| **點擊紀錄**（2026-09-20） | **全部都記**（使用者：「我覺得都要記」）。存的只有三欄：**按了哪一顆**（白名單代碼）、**從哪一頁按的**（`home`／`topic:<spec>`／`post:<slug>`）、ISO 時間戳。⚠⚠ **和搜尋紀錄同一條紅線：沒有 IP、沒有 User-Agent、沒有 cookie，連「這次瀏覽的流水號」都沒有** —— 有流水號就能把動作串成軌跡（看了植牙那篇 → 打了電話），那和一張次數表是兩種東西，不要順手加。⚠⚠ **站上的標記一個字都沒有改**：代碼是從既有的 class 與 href 推出來的（`assets/click-log.js` 的 `RULES`）。理由是十六篇文章頁的連結在 `<main>` 裡，加 `data-click` 會動到內容雜湊、十六篇的「最後更新」一起跳成當天，而那一行 2026-09-20 起讀者看得到（CLAUDE.md 第五節那個陷阱）。代價是**版面改了 class 那一顆就靜靜地不再被記** —— 守門 `tools/click-test.mjs` 會真的在瀏覽器裡把每一顆按一次。⚠⚠ **事件代理掛在捕獲階段不是冒泡**：地圖上那三種（診所、停車場的名字、停車場那一塊）的 handler 自己呼叫了 `stopPropagation`（不擋住的話點名字會先把牌子收起來），冒泡到不了 `document`，寫成冒泡的第一版**那三顆一筆都沒記到而站上完全正常**。代價是讀到的狀態是「按之前」，所以夜間開關那一條要自己算翻面之後的值。⚠ **報告上先分「離站／站內」再排名次**：次數天生就多的站內元件（回到最上面、科別標記）混在一張表上會把真正重要的那幾顆擠掉；離站 ＝ 打電話、開導航、加 LINE。⚠ **同一顆在不同頁面上分開存、報告上再合起來算名次**（首頁的電話與文章頁的電話是兩件事，但散成三列會讓每一列都看起來很少）。⚠ 每日上限 20000 筆（一筆是兩次寫入，仍在 D1 免費額度內）；報告在 `/admin/search/` 第二個分頁，**和搜尋紀錄共用同一把 `REPORT_KEY`**。⚠ 站上目前**沒有任何看得到的 LINE 連結**（只在 JSON-LD 的 `sameAs` 裡），`line` 這個代碼先留著，哪天頁尾放一顆就會自己開始記 | `src/click.js` 檔頭、`assets/click-log.js` 檔頭、`d1-schema.sql`、守門 `tools/click-test.mjs` |

### 還沒決定的

0. ~~頁首選單的「目前在哪一節」要用哪一案~~ —— **2026-08-10 定案 Ⓓ2 玻璃並上線**，
   見上面定案表與本檔第八節的 `/history/nav-scrollspy.html`。
   > ~~還沒做的那一半：文章頁的頁首沒有那圈框。~~ —— **2026-08-12 由另一台
   > 補上線**（commit `77b1d7a`，只搬「靜止的框」那一態，`--lamp-pad` 改用
   > CSS 的 `min()` 算），**2026-08-15 再把框以外的每一項也對齊**
   > （見上面定案表「首頁切到文章頁，頁首不要變」）。
   > 文章頁仍然**沒有「亮起來」那一態**：那三項是跨頁連結，這一頁沒有任何
   > 一節可以偵測，硬要選一個亮是說謊 —— 這不是漏做。
0.5 ~~那三個玻璃框的高與圓角~~ —— **2026-08-11 三輪全部定案並上線**，見上面定案表
   與第八節的 `/history/nav-frame-height.html`。框高 `--frame-em: 2.65`、圓角 `--frame-r: 8px`，
   **手機／iPad／電腦版共用同一組值**（使用者第三輪指定推到電腦版與 iPad）。
   > ~~留下來的一件：文章頁（`posts/`）的選單仍然沒有框。~~ —— 框 2026-08-12
   > 補上線，其餘每一項 2026-08-15 對齊，兩邊現在吃同一組 `--frame-em`／
   > `--frame-r`／字級／間距。見上面第 0 項與定案表。
1. **要不要把定案套進 `/history/home-desktop.html`**，並刪掉中間過程的幾頁
   （`home-desktop-hero/` `-align/` `-banner/` `-fullscreen/`）。
   `home-desktop-band/` 自己的切換條已經全部刪掉了。
2. COPY.md 第六節列的兩件：chips 按下的狀態要不要降一階、故事類內容之後怎麼進「主題與科別」。
3. HERO 收尾詩的三案 A／B／C（[COPY.md](COPY.md) 第四節），從 2026-08-05 就擱著。
4. ~~齒顎矯正的藍要不要提亮~~ —— **2026-08-10 結案上線，見上面定案表。**
   走的不是提亮那條路：使用者從 `/history/spec-ortho-palette.html` 的八案挑中青綠、**改給牙周**，
   **牙周原本的藍整組給矯正**，矯正因此從 L\* 36.3 跳到 49.5（比提亮三案最激進的 Ⓒ 還亮）。
   > **`/history/spec-ortho-bright.html` 的三案與 [PALETTE.md](PALETTE.md) 第六之十二節整組作廢**，
   > 留著看推論就好，**不要再拿去實作**。第六之十五節（米色／稻草灰那八案）同理。
5. **[PALETTE.md](PALETTE.md) 第六之八節（科別的意象）要重寫**（2026-08-09 起，08-10 更嚴重）。
   顯微根管離開苔綠之後綠色段只剩一般牙科 151；**2026-08-10 牙周落在 h 190（青綠）、
   矯正搬到 h 273**，所以冷色端是牙周 190／矯正 273／贋復 273／外科 320。
   > **兩條意象已經不成立**：矯正原本綁「候診沙發」（那塊布是 `#4e718b`），
   > 牙周原本綁的是拆出來的那顆藍。反過來**牙周現在回到診療椅那一支（h 190）**，
   > 這一段其實可以寫得比原來更順。整節還沒動。
6. **手機版 HERO 的詩要怎麼排**（2026-08-09 起）。起因是使用者的朋友說
   「左上角 logo 的字跟 cover image 的字大小差不多，看起來會比較 noisy，可以增加 spacing」。
   實測成立，**而且手機比電腦嚴重**：375×812 上詩的上緣比頁首那一塊的下緣還高 2.9px，
   兩塊字是疊著的；1440×900 上也只有 63px。
   > **電腦版已結案並上線**（2026-08-09，commit `09b6f9d`）：提案 Ⓒ 鬆 ——
   > 距離 4vh→9vh（空隙 63→108px）、字距 .2em→.32em、行高 1.95→2.45、
   > 首行後間隔 .5em→.9em、max-width 34em→40em。三案的比較留在
   > `/history/head-hero-space.html`（`?sp=a|b|c|c2`）。**字級一個都沒動。**
   >
   > **手機版也結案並上線了**（2026-08-10）：提案 **Ⓢ1** ——
   > `padding-top 15vw → 17.9vw`、字距 `.2em → .13em`、全形空格 `1em → .65em`
   > （375 上空隙 −2.9 → 8.0px）。行高 1.75 與首行間隔 .55em **維持原樣**。
   > 一起進去的還有兩個非動不可的等比例修正（左緣 5.13vw、字級下限 9.5px，見下面第 3 點）
   > 與收尾句的 Ⓔ2 陰影（第 10 項）。全部寫在 `index.html` 手機 @media 的長註解裡。
   >
   > 以下是當時的過程，留著避免有人重走：使用者說 Ⓐ／Ⓑ／Ⓒ 三案在手機上「都不好，壓到房子了」，
   > 並給了四點：①第一行再往下降 ②降了會壓到房子，所以收兩段文字之間的間距、
   > 甚至字距 ③第二行比照 ④第三行拆成兩行。
   > 新提案頁 **`/history/hero-mobile-skyline.html`**（`?m=m1|m2|m3`，`&sky=1` 疊出天空邊界），
   > 完整推導在那一頁 `<head>` 最後的註解。要接手就從那一頁看，不要重新目測。
   >
   > **那一頁量出來的三件事，接手前先知道：**
   > 1. 照片的可用天空是一個**楔形**（逐列掃描 JPG 得到的實測表就寫在那一頁）：
   >    390 寬時右界從 y52px 的 291px 一路收到 y140px 的 182px，y216px 以下只剩 64px。
   >    詩是「左上角固定、往右往下長」，方向正好相反 —— 這才是一往下推就撞房子的原因。
   > 2. **使用者的第 4 點瞄錯行了**：拆第三行**一點高度都換不到**（拆或不拆，
   >    能降到的最低點完全一樣）。卡住整首詩的是**第一行**（16 格，最長，
   >    而屋簷正好從它右上方壓下來）。拆第一行才換得到。
   > 3. 手機的幾何原本**不是等比例的**：`--pad` 撞在 1.25rem 下限（各寬度都是 20px）、
   >    字級 clamp 的下限 10px 在 320 寬會贏過 3.06vw。兩者都會讓同一組 vw 參數
   >    「390 上剛好閃過、375／320 上就壓到」。那一頁把左緣改成 5.13vw、
   >    字級下限降到 9.5px，整組才等比例。**最壞情況是 375（不是 320，也不是 390）**：
   >    376 以上字級撞到 11.5px 上限、相對變小反而安全。
   >
   > **⚠⚠ 2026-08-15 更新：這一整條（含上面那張楔形表與 Ⓢ1 的每一個數字）
   > 只適用於 `74.72vw` 那個版型，已經不是現況。** 照片改成「抵下緣」之後，
   > 楔形整個放大，而且量出來**詩壓在牆上白字對比 5.0~8.7 一樣過 AA** ——
   > 「詩不准碰到房子」這條約束的真正理由是對比度，不是構圖。
   > 現在的值見上面定案表「手機版 HERO 的照片要多大」與
   > `/history/hero-mobile-fullbleed.html`。**不要再照這張表去解字距。**
   >
   > 更大的位移只能靠拆行（Ⓜ1～Ⓜ3，最多能到 39px），使用者看過之後說
   > 「我要的感覺應該在現況和 Ⓜ1 之間」，所以才有 Ⓢ1／Ⓢ2 這兩階，最後選了 Ⓢ1。
   > **提案頁 `/history/hero-mobile-skyline.html` 留著**，那六案與整條「位置↔字距」的
   > 階梯表都在裡面，日後要再調位置直接查表，不要重新目測。
7. **⚠ 全站最近的一對是 ΔE 13.3（填色）／10.5（深階）**，都低於原本的最小值 16.1；
   兩顆同色相 h 273，只差亮度。**不是待決事項，是要記住的約束** ——
   日後動到其中任何一顆，都要先把這一對重算過。見 [PALETTE.md](PALETTE.md) 第六之十四節。
   > **原本是「牙周 × 贋復」。牙周換青綠之後那組藍整組給了矯正，所以現在是
   > 「矯正 × 贋復」** —— 數字一模一樣，只是主角換人（第六之十六節）。
8. ~~牙周的青綠要幾階~~ —— **2026-08-10 定案 Ⓣ1 並上線**（兩階 `#317d78`／`#2a6d69`）。
   > **留下來的約束**：牙周的字階對全站連結色深綠松 `#214D48` 只有 ΔE 13.6，
   > 是全站第二緊的一格（最緊的是矯正×贋復 13.3／10.5）。**要動深綠松、牙周或矯正，
   > 得把這幾顆一起算。** 三案的完整數字在 `/history/spec-perio-teal.html` 與第六之十六節。
9. ~~三格間距的 4rem「拉開」沒有生效~~ —— **2026-08-10 已修好並上線。**
   `.stats li { padding-inline: 4rem; }`（2026-08-07 定案）之後還有一條同權重、
   排在後面的 `.stats li { position: relative; padding-inline: 1.9rem; … }`，後者贏，
   所以**定案後整整三天線上跑的都是當初被淘汰的 1.9rem**。
   修法是把後面那一份的 `padding-inline` 刪掉、讓原本那條接手（沒有用 !important）。
   > **留下來的規則**：`padding-inline` 在這份樣式表裡**只准出現在那一行**。
   > 要調三格的間距就改那個 4rem，不要在下面的 `.stats li` 裡再寫一次。
   > 對照頁 `/history/band-stats-gap.html`，數據見 [PALETTE.md](PALETTE.md) 第六之十七節第七小節。
10. **這個站的綠現在有三支**（2026-08-10 起）：一般牙科 h 151、牙周青綠 h 190、
   深綠松（連結色與診所資訊）h 186。後兩支同色相，只靠亮度分開。
   **診所資訊那顆要不要跟著重看，是還沒問過的一題。**
11. ~~收尾句「巷口」壓在車頭燈上~~ —— **2026-08-10 定案 Ⓔ2 並上線**（只在手機）。
    使用者選完 Ⓢ1 後回報「巷口」看不清楚。逐字量過照片底下的亮度（375 寬）：
    「巷」最亮 219、「口」最亮 236，白字 65% 疊上去的對比度只有 1.24／1.12
    （1.0 ＝ 完全看不見），其他十個字都在 3～7 —— **是局部一個亮點，不是整句都暗**。
    ⚠ **2026-08-15 更新：那盞燈已經被新裁法裁掉了。** 收尾句底下最亮的一列
      L*97 → L*40，白字對比度 1.09 → 6.50（六個視窗最低 3.25，在 320×568）。
      **Ⓔ2 那塊暗斑留著**（不礙事，還多一層保險），但它已經不是非有不可 ——
      日後若有人要簡化手機版的 HERO，這一塊是可以拿掉的候選。
    定案的 Ⓔ2 只動 `.hero-sign::before`：`rgba(25,22,20,.72) → .9`、
    範圍 `-1.1em/-1.4em → -1.5em/-2em`，**字的 text-shadow 一個值都沒改**。
    > **兩條不要違反的規則：**
    > ・**blur 維持 18px。** 一收就會看得出那是一個框，而「看不出是框、
    >   只是路面自己暗下去」正是這塊東西的設計重點。
    > ・**只在手機。** 電腦版的裁切是下緣貼齊，收尾句落在純暗的路面上，
    >   十二個字最亮處都 ≤58、對比度 ≥5.9 —— 不需要，也不該加。
    >
    > 落選的 Ⓔ1（加厚字自己的 text-shadow，最內層 `0 0 2px /.9`）留在
    > `/history/hero-mobile-skyline.html?e=e1`。它對點光源更有效，
    > 但字會變重，離收尾句那種很輕的落款感遠一些。
12. ~~醫師卡與文章卡：點到的科別才亮起來~~ —— **2026-08-11 五輪定案並上線。**
    見上面的定案表與第八節的 `/history/doc-spec-tags.html`。
    > **留下來的一件**：文章頁（`posts/<slug>/`，走 `assets/style.css`）**沒有這一套**
    > —— 那邊沒有篩選工具，所以沒有「亮起來」這件事。這不是漏做，是那一頁沒有這個
    > 需求。但**日後若有人改動 `.doc-role` 或 `.card-tag`，要記得那兩個選擇器現在
    > 多了 `.tag-off` / `.tag-on` 兩態**。
13. ~~套色之後專長那幾個字要多大~~ —— **2026-08-11 定案 `.83rem` 並上線**
    （＝和沒套色的內文一模一樣，完全不縮）。見上面的定案表與第八節的
    `/history/spec-tag-size.html`。
    > **留下來的約束**：再往上加字級的天花板**不是塊高，是塊的上緣離基線多遠** ——
    > 現在 15.13px，而 `.doc dd` 那條 strut 的 ascent 只有 14.97px，
    > 那一列已經因此長高 0.13px（看不出來）。再加就會開始把行距推開。
14. ~~頁首玻璃「招牌之前」要多輕~~ —— **2026-08-11 三輪全部定案並上線。**
    見上面的定案表與第八節的 `/history/head-blur-ipad.html|mobile|desktop/` 三頁。
    > **留下來的一件**：文章頁（`posts/<slug>/`，走 `assets/style.css`）**沒有這一套**
    > —— 那邊沒有 HERO 照片，頁首底下從第一屏開始就是紙色，沒有「照片的哪一列」
    > 可以算。這不是漏做。
15. ~~收尾句「到巷口的芳仁　一起想辦法」要多大~~ —— **2026-08-12 定案 Ⓕ2 並上線**
    （＝和詩一模一樣大，四種版型全部推平）。見上面的定案表與第八節的
    `/history/hero-sign-size.html`。
    > **留下來的規則只有一條**：詩與收尾句共用 `--hero-fs`，
    > **不要再給 `.hero-sign` 寫第二條 font-size** —— 那正是這一輪查出來的成因。
16. ~~照片與窄帶之間看得到一條分界線~~ —— **2026-08-12 四輪定案並上線**。
    提案頁已刪除，推導在 `/history/hero-band-seam.html`。見上面的定案表與
    [PALETTE.md](PALETTE.md) 第六之十九節。
    > **⚠ 第一版被否決了，方向整個掉頭 —— 不要再走回去。**
    > 第一版是在**照片下緣**疊柏油漸層（手機 22vw、收到不透明），
    > 使用者：「你把照片的下半部做成漸層黑然後接上窄帶這個做法錯的，應該是要把
    > 窄帶做成比較深的漸層接在照片下半部，因為這樣照片下半部整個都黑掉的很奇怪，
    > 照片都被弄掉了。」手機的照片只有 291px 高，22vw 是它的 29%。
    > **原則：接縫在窄帶那一側解決，照片不准動。**
    > 日後再遇到「兩塊東西接不起來」，先問「能不能只改我自己這一塊」。
    >
    > **這一版留下來的三條規則：**
    > ・**兩層的 alpha 一律是 smoothstep，不要改回線性。** 線性漸層的起點就是一條線
    >   （一階導數跳階），這是電腦／旋轉螢幕那條線的成因，和顏色無關。
    >   ⚠ smoothstep 只改形狀，**顏色與範圍一個都沒動** —— 而且前段比線性更透，
    >   照片反而露得比現在多。
    > ・**手機的窄帶從 `#151311` 起跑，整條 smoothstep 攤到 100%。** 顏色要接上，
    >   **斜率也要接上**（照片往下變暗、窄帶往下變亮，在同一列折返會長出馬赫帶）；
    >   smoothstep 的起點斜率是 0，所以不必另外寫平段。
    >   ⚠ **漸層要用滿整條窄帶**，不要只走到 62% 就到底 —— 使用者第二輪就是嫌
    >   「從黑邊到深藍有點短、很突兀」。攤開之後 53px → 107px。
    >   ⚠ **曲線是 S(t^1.6) 不是對稱的 smoothstep** —— 對稱的走到一半就已經是
    >   中間色，使用者第三輪嫌「黑漸層可以再往下一點」。取 1.6 次方之後
    >   「還算黑」的範圍從窄帶的 36% 變成 51%，而頭尾斜率仍然是 0。
    >   拉長不會有色帶：Chromium 自己抖動，實測 107px 裡有 65 個色階。
    > ・**iPad 的柏油層維持 84px**，只把最後 24% 從 .88 收到 1 —— 那 20px 的照片
    >   本來就已經被壓掉 88%，收掉剩下的 12% 看不出來，但殘影變成漸漸消失的。
17. ~~「回到最上面」的按鈕要長什麼樣~~ —— **2026-08-15 三輪定案並上線。**
    見上面的定案表與 `/history/back-to-top.html`。
    > **留下來的兩件：**
    > ・**站上的 `.hero-cue`（往下滑那顆指標）「點下去會亮」實際只亮 300ms。**
    >   它掛了一個 300ms 的 timeout 拿掉 `.is-lit`，而捲動要跑 620ms ——
    >   那個 timeout 先燒掉，`clearTimeout` 來不及。這一輪的按鈕已經改成
    >   「要捲的時候不掛 timeout」，`.hero-cue` **還沒改**。要修就是同一個改法。
    > ・文章頁（`posts/<slug>/`，走 `assets/style.css`）**沒有這一顆**。
    >   那是還沒問過使用者的事，不是漏做。
18. ~~六篇文章的 HERO 要換成點陣插畫，等圖湊齊才一次上線~~ —— **2026-08-16 六篇
    一起定案上線**，見上面的定案表與 `/history/hero-photos.html`。
    > **留下來的三件：**
    > ・**六張的明暗沒有統一，那是使用者選的**（他看過提案頁之後說「就這樣　一起上線」）。
    >   日後要統一是**回頭重生成，不是後製調色**。
    > ・**舊的六張 `assets/hero-*.svg`、六張 `hero-*-1600.png` 與 `tools/og-images.mjs`
    >   都還留著沒刪**（退回去的路）。要不要清掉還沒問過使用者，**不要自己刪**。
    >   ⚠ 2026-08-16 稍晚 `preview/post-cta/` 也收掉之後，**那六張 SVG、六張
    >   `hero-*-1600.png` 與 `tools/og-images.mjs` 已經完全沒有人引用了** ——
    >   全站 grep 過，只剩 `history/` 的文字存檔提到它們。
    > ・ILLUSTRATION.md 第六節第 9 項「縮圖點開是什麼互動」仍然沒問過 ——
    >   站上沒有燈箱，卡片縮圖點下去是連到文章。

19. ~~科別著陸頁的文案要重寫~~ **七科的文案 2026-08-19~21 全部定案並上線**
    （`/topics/<spec>/`）。⚠ **那七頁現在是範本，不是反例** —— 早期的版本才是。
    **每一頁要不要配一張圖仍然沒決定，這一項留著就是為了這一題。**

    > **圖片這一題我的評估是「要配，而且要新畫」，但還沒問過使用者：**
    >
    > ⚠⚠ **2026-08-22 更正：「七頁都沒有 `og:image`」是錯的。** 那七頁是
    >   `index.html` 的快照，`<head>` 裡首頁那組手寫的 og 一起帶過來了，
    >   所以**一直都有分享圖** —— 診所夜景 `assets/hero-clinic-night.jpg`
    >   （1600×1058），分享到 LINE／FB 不是空白卡。`seoBlock` 確實不產
    >   `og:image`，但它不是唯一的來源。**下判斷之前先打開產出的檔案看一眼。**
    >
    > ⚠⚠ **同一天查出並修掉一件真的壞掉的事**：那組手寫的 og 位置在
    >   `SEO:START` 的**前面**，而 FB／LINE 的爬蟲遇到重複的 og 屬性一律
    >   取**第一個** —— 所以 `og:title`／`og:description`／`og:url` 顯示的
    >   全是**首頁**那一組，七頁分享出去長得一模一樣，`og:url` 還寫著
    >   `https://fangren.net/`，和同一頁正確的 `canonical` 互相矛盾。
    >   修法：`tools/topics.mjs` 步驟 10 把手寫那四個屬性刪掉
    >   （`og:site_name` 與 `og:image*` 留著），讓 `seoBlock` 產的那組生效。
    >   **通則：`index.html` 的 `<head>` 裡凡是 `SEO:START` 以外的手寫 meta，
    >   做快照時都要逐條問一次「這一條對著陸頁還成立嗎」。**
    >
    > ・**分享圖現在七頁共用一張診所夜景。** 它至少「一看就是芳仁」，
    >   但七頁分不出彼此；要各科一張是下面這一題。
    >   ⚠⚠ **2026-08-22 使用者的判斷：頁面上不要放圖。** 他的理由是手機版
    >   已經很擠，圖放進去會壓到下面的文章卡 —— 量過成立（那七頁有一條硬條件
    >   是「介紹收在手機一屏之內、下一區露出一角」，430 上只差 22px，
    >   六頁會破，兒牙那頁本來就超出一屏）。
    >   **所以這一題已經收斂成「要不要各科一張專屬的分享圖」，
    >   不再包含「頁面上要不要顯示」** —— `og:image` 是 `<head>` 裡的 meta，
    >   從來就不會在頁面上顯示，兩件事不必取捨。
    > ・**沒有圖會讓它看起來就是「篩選結果」** —— 那正是使用者一開始的質疑
    >   （「這個跟我首頁已經有的篩選很像欸」）。全站每一篇文章都有 HERO，
    >   唯獨這七頁沒有，讀起來像半成品。
    > ・~~**矯正那一頁最需要**：它零篇文章，所以整頁**連一張圖都沒有**。~~
    >   ⚠ **2026-08-19 起矯正不再是零篇** —— 〈牙齒矯正：年齡、矯正器與第一次諮詢〉
    >   已上線（`/posts/orthodontics/`，站上第九篇），那一科的著陸頁底下現在有一張卡。
    >   ⚠ **2026-08-20 起顯微根管也不再是零篇**（〈根管治療的生物陶瓷〉）。
    >   **上線當天七科的文章數是**：一般 2、牙周 2、兒牙 2、顯微根管 1、
    >   植牙 1、口外 1、矯正 1 —— **沒有任何一科是零篇了**，但有四科只有一張卡
    >   （顯微根管、植牙、口外、矯正），那四頁底下最空，也最需要自己的圖。
    > ・⚠ **不能用「拿該科文章的 HERO 來頂」省事** —— 會缺圖的正好是文章最少的
    >   那幾科，等於在最需要的地方失效。
    > ・⚠⚠ **要畫的是「病患的處境」，不是牙齒解剖圖或器械示意圖。**
    >   這一條和文案是同一件事：ILLUSTRATION.md 的既有語彙本來就是人物與生活場景
    >   （〈牙齦流血〉那張就是「搧扇子、買冰飲、冰敷臉頰、照鏡子看牙齦」四格），
    >   正好對得上 COPY.md 第九節那個「從病患角度出發」的方向。
    >   **診療室裡的器械特寫是反方向的。**
    > ・**所以圖的內容要等文案定了再發包** —— 圖是文案開場那一幕的畫面，
    >   兩件事是一組的，不要分開決定。
    > ・規格照文章 HERO 那一套：三個寬度（800／1600／2000）、`sizes` 要現場量過
    >   再寫（第九節第 15 條）、`og:image` 指 `-1600.jpg`、寬高由 `jpegSize()` 讀。
    > ・位置建議放在 `<h1>` 底下、開場那段之上（＝文章頁 HERO 的位置）。
    >
    > **⚠⚠ 2026-08-22 使用者定案了方向**：「**要做就做到好**，我們可以重新生成
    >   科別圖片，目標是要在 250px 下給人清楚辨識……重點是訊息 app 中要讓人清楚
    >   看到整個品牌風格和價值，像文章圖片那樣。」
    >   → **七科各畫一張專屬的分享圖，用站上的插畫語彙，但構圖為 250px 重新設計。**
    >   規格（含十張現有插畫在 250px 下的實測、硬條件九條、色牌與管線）寫在
    >   **[ILLUSTRATION.md](ILLUSTRATION.md) 第十一節**，通則寫在
    >   **[TEAM.md](TEAM.md) 第六節「曝光原則」**。
    >   ⚠ 實測結論：**十張文章插畫縮到 250px 只有兩張還看得懂** ——
    >   多格分割與人多沒有視覺中心的場面全部糊掉。**文章 HERO 那一套結構
    >   不能直接搬到分享卡上。**
    >   ⚠ 「頁面上要不要顯示」是另一題，使用者說**之後再討論**（見上一段）。
    >
    > **✅ 2026-08-23：「頁面上要不要放圖」一般牙科有答案了 —— 放，但放成「底」。**
    >   使用者：「那個圖片放進來會壓縮到版面，我不要那樣。我想到把圖片的某個部分
    >   做成頁面的底，但只要保留線條。」成品是 `assets/lineart-general.png`，
    >   墊在介紹區右下角、absolute 的偽元素、**版面一個像素都沒動**。
    >   規格見上面定案表那一列與 ILLUSTRATION.md 第十二節，推導在
    >   `/history/topic-lineart.html`。
    >   **✅ 2026-08-24：牙周與顯微根管的線稿底圖也上線了**
    >   （`assets/lineart-perio.png`、`assets/lineart-endo.png`，推導在
    >   `/history/topic-lineart-perio.html` 與 `/history/topic-lineart-endo.html`）——
    >   ⚠⚠ **三科三組值，分段、大小、濃度、要不要翻轉、要不要裁右全部各自判斷，
    >   不要互抄。** 顯微根管那一輪多學到一條：**近正方或直的線稿，卡住的是高度**
    >   （480px 寬會算出 514px 高，比介紹區還高、會凸到 h1 那一區）。
    >   ⚠ **這和分享圖是兩件不同的圖**
    >   （分享圖是彩色插畫、只出現在訊息卡上；底圖是單色線稿、只出現在頁面上），
    >   **✅ 2026-08-24 稍晚：兒童牙科兩張都做好了**（分享圖 ＋ 線稿底圖）。
    >   **✅ 2026-08-24 更晚：齒顎矯正兩張也做好了** —— 分享圖十三版
    >   （`assets/og-topic-ortho.jpg`），線稿手機段定案、**≥721 那一段還是暫定**
    >   （`assets/lineart-ortho.png`，提案頁 `/preview/topic-lineart-ortho/` 還開著）。
    >   **✅ 2026-08-25：植牙・假牙重建兩張也做好了**（分享圖 `assets/og-topic-prosth.jpg`、
    >   線稿 `assets/lineart-prosth.png`，兩段都定案）。
    >   **✅ 2026-08-25：口腔外科的分享圖也定案上線了**（`assets/og-topic-surg.jpg`，十六輪，帶子用深階 `#784e84`）—— **七科的分享圖到齊**。**✅ 2026-08-25 稍晚：口外的線稿底圖也定案上線了**
    >   （`assets/lineart-surg.png`，五版 —— **使用者選的是第一、二版**，中間三版都在治
    >   「潛盾像電風扇」而把圖治壞了）。**七科兩種圖全部到齊。**
    >   ⚠ **≥721 那一段 2026-08-26 也定案了**（220px／`right: 48px`／0／.30）——
    >   七科兩種圖、兩個斷點全部定案上線，這一項到此結案。
    >
    > **✅ 2026-08-25：七科的分享圖與線稿底圖全部畫完上線了。**
    >   下面這一段留著是為了下一次還會用到（換圖、補畫、或哪一科要重畫）：
    >   **`drafts/topic-lineart-prompt.md`** —— 那一份 2026-08-23 已經改寫成
    >   **可以重複使用的模板**（風格規格、要附哪七張圖、提示詞、門檻、管線、驗收），
    >   不再是一般牙科那一輪的紀錄。五張風格參考圖也已經裁乾淨進版控
    >   （`drafts/lineart-ref-1-walking.png` ~ `-5-bubbles.png`）。
    >   ⚠⚠ **順序是「分享圖在前、線稿在後」** —— 線稿要附一張**姿勢參考圖**才畫得準
    >   （一般牙科那次是從已經畫好的分享圖裡裁一段），沒有它就得用文字描述動作，
    >   而**文字描述動作一定會漂**（第一版四件全錯，三件是提示詞自己寫出來的）。
     >   ⚠⚠ **大小與濃度不要憑感覺挑，但也不要互抄** —— 七科七組值
    >   （一般牙科 360／.10／.48 分段 834、牙周 390→480／.115／.15 分段 721、
    >   顯微根管 360→330／.107／.30 分段 721、兒牙、矯正、
    >   植牙 92%→400px／.121／.22 分段 721、**口外 52%→220px／.18／.30 分段 721**），
    >   每一組都是使用者逐格挑的。
    >   臨界濃度跟著該科的套色走（愈深愈緊：.101／.116／.107／.152／.120／.097／**.118**），
    >   **現算一次**。
    >   ⚠⚠ **圖很「直」的時候，px 上限要量「介紹區最矮的那一格」**（口外 2026-08-25）——
    >   手機那一段吃到 720px 寬，而介紹區愈寬愈矮，460~520 那一段最矮。
    >   只在使用者的手機寬度上驗會漏掉（430 上就已經凸出去了）。
    >
    >
    > **✅ 2026-08-22 當天：一般牙科那一張畫完、帶子定案、整組上線。**
    >   成品 `assets/og-topic-general.jpg`，帶子的規格見上面定案表那一列與
    >   ILLUSTRATION.md 第十一節，推導在 `/history/og-topic-card.html`。
    >   **這一項的分享圖已經七科到齊**（口外 2026-08-25 定案上線）—— 牙周／顯微根管／兒童牙科／
    >   齒顎矯正 2026-08-24、植牙・假牙重建 2026-08-25 都已上線
    >   （兒童牙科四輪：梗 → 太空曠 → 護理師的衣服 → 定案，推導與定稿提示詞在
    >   `drafts/og-topic-kids-prompt.md`；植牙十三版，在 `drafts/og-topic-prosth-prompt.md`）。
    >   口外那一科的 `og:image` 仍然退回診所夜景。圖畫好之後跑兩行指令就好，
    >   `og-plate.mjs` 會自己抓那一科的深階色，**不必再問帶子怎麼做**。
    >   ⚠ 構圖的安靜區**從「左下角 46%×40%」改成「上緣整條 17%」**（帶子換位置了），
    >   ILLUSTRATION.md 第十一節第 8 條已經改掉，舊的那個角落條件作廢。
    >
    > **落選的替代方案**：每個小節各配一張小圖 —— 七頁 × 四五張等於三十幾張，
    > 而且會把頁面拉得很長，不划算。

（兒牙白字那一件已經結案了，見上面的定案表。口腔外科的紫已定案 `#8e6299`／`#784e84`，
見 PALETTE.md 第六之九節與 `/history/spec-surg-violet.html`；贋復假牙的藍已定案
`#335b8b`／`#182f4b`，見第六之十、十三、十四節與 `/history/spec-perio-split.html`；
顯微根管的紅已定案 `#ae4f4d`／`#89202d`，見第六之十一節與 `/history/spec-endo-red-all.html`；
**`#4478b5`／`#244369` 原本是牙周的，2026-08-10 起改屬齒顎矯正，牙周換成青綠
`#317d78`／`#2a6d69`**，見第六之十四、十六節與 `/history/spec-perio-split.html`、
`/history/spec-perio-teal.html`。）

### 把提案頁搬進正式站時，要一起帶的東西（2026-08-08 補，踩過）

提案頁是**自給自足的靜態快照**，正式站需要、但快照裡刻意沒有的東西，搬版型的時候
會整個掉在原地。2026-08-07 用電腦版提案頁換掉 `index.html`（commit `2c0f7aa`）時就漏了兩樣：

1. **`<script src="assets/counter.js" defer>` 整支沒帶過去。**
   結果首頁六張卡的瀏覽數永遠是「—」，窄帶那個數字則停在提案頁的**示範值 8642**
   （真值當時是 190）。文章頁一直都有掛，所以只有首頁壞。
2. **`data-views-self="home"` 被寫成 `data-views="home"`。**
   `-self` 才會 POST +1，沒有它首頁的計數就永遠不會長。

所以：**窄帶的 `data-count` 是提案頁專用的示範值，正式站絕對不要留著**，
真值只能來自 D1。`index.html` 的 `.band-views` 現在改成「真實數字回來 ∧ 捲進畫面」
兩個條件都到齊才跑動畫，並靠 `.is-on` 決定現身，在那之前是 `visibility: hidden` ——
不會先閃一個 0。API 掛掉或沒有 JS 就一直隱形，文章卡那邊則連前面的「・」一起收掉。

### 這一輪學到、很容易再踩的坑

1. **`backdrop-filter` 只作用在元素自己的框裡**，框的邊緣本身就是一條可見的線，
   跟顏色無關，調 alpha 消不掉。要用 `mask-image` 讓**模糊本身**淡入／淡出。
2. **CSS 自訂屬性的預設值那一行要排在覆寫它的規則前面。**
   `[data-spec]` 和 `[data-spec="kids"]` 權重相同，
   `[data-spec] { --accent-deep: var(--accent) }` 若排在後面，
   會把兒牙特別指定的深階字蓋回去 —— 顏色看起來「沒吃到」就是這個原因。
3. **在暗底上疊一個同樣暗的顏色，看不出任何變化。** 柏油漸層第一版整條用 `#221e1c`
   （L\* 12）疊在 L\* 6~15 的路面上，使用者說「看不出漸層」就是這個原因。
   要有變化就得換到更深的實測值（照片最底那一列 `#161413`，L\* 6.6）。
4. **iOS 沒有內建任何中文襯線字，`serif` 會整包退回黑體 —— 連數字一起。**
   這一頁是 `<html lang="zh-Hant-TW">`，WebKit 解 generic `serif` 是**照語言**去找的，
   找不到中文襯線就退回系統中文字（PingFang），而 PingFang 有拉丁字，
   所以 1983／9／6 也跟著變黑體。電腦上看不出來（有思源宋體／Times）。
   解法是把一個**確定存在於 iOS 的拉丁襯線字**排在 generic 前面：
   `"Noto Serif TC", "Source Han Serif TC", "Times New Roman", Times, serif`。
   字體比對是逐字做的 —— 數字命中 Times，中文在 Times 裡找不到就繼續往後掉到
   `serif`，跟以前一模一樣，**所以中文一個字都不會變**。
5. **rAF 與 IntersectionObserver 在 `document.hidden` 的分頁裡完全不會被呼叫**，
   連 IO 的第一次回呼都不送。窄帶數字原本「IO 說看得到才開跑」，
   在那種情境下會永遠停在 0。現在改成自己 `getBoundingClientRect()` 判斷，
   並在動畫外面加一條 `setTimeout` 保險絲，時間到了直接寫最終值。
6. **iOS Safari 的 `window.innerHeight` 不是眼睛看得到的高度** —— 它是工具列收起後的
   大視窗高度，比當下可見範圍高出整條網址列（100vh 在 iOS 爆框是同一個原因）。
   拿它判斷「捲進畫面了沒」會**提早**成立，動畫在使用者還沒捲到之前就跑完，
   等他捲到時數字早就停在最終值 —— 症狀是「iPhone／iPad 上完全不會動」。
   要用 `visualViewport.height`（iOS 9.3 以後都有），退回 `innerHeight`。
7. **不要在頁面還在載入的那一刻開跑動畫。** 手機上那一刻主執行緒在解 HERO 那張大圖，
   rAF 拿不到幾個影格，1.4 秒的動畫被壓成一兩下跳動，等於沒有動畫。
   窄帶數字現在多等一個條件：`load` 之後再 300ms（上限 3 秒，圖再慢也要讓數字出現）。
8. **CSS 註解裡不能出現 `</style>` 那個字串**（2026-08-11，`/history/doc-cross-spec.html` 踩過）。
   HTML 解析器找結束標籤是**純字串比對**，不管它在不在 CSS 註解裡 —— 寫下去樣式表就在
   那一行被切斷，後面整段全部失效。**症狀最惡劣的地方是它不報錯**：畫面照常、
   舊樣式都在，只有新加的那一段「沒吃到」，很容易往選擇器權重或快取的方向找一小時。
   `<script>` 裡的 `</script>` 同一條。這一站的註解寫得很長，特別容易踩。
   要在註解裡指這件事，就用文字描述（「這份樣式表結尾」），不要貼標籤本身。
   ⚠ **同一條的近親：CSS 註解裡不能出現註解的結束記號（星號＋斜線）。**
   2026-08-12 寫「0.194 L 星號 斜線 px」當單位，那兩個字元連在一起就把整段註解
   提早關掉，底下 `.band` 的整條規則跟著失效 —— `background-image` 變成 `none`、
   接縫又跳回 +2.7，**而且完全不報錯**。這一站的註解常常要寫 `L*`（CIE 明度），
   後面接斜線的機率不低：單位改寫成「每像素」，或把星號和斜線隔開。
9. **中文小字和大字並排，對基線就是低的**（2026-08-11，`/history/doc-spec-tags.html` 量出來）。
   瀏覽器對的是**基線**，眼睛讀的是**字面中線**；中文字面中線在基線上方 0.345em
   （Noto Sans TC，400px 下實測 `李柄輝`／`創辦醫師`／`口腔檢查`／`一般牙科治療`
   四組 ink 中線 0.336~0.359em）。所以小字每比大字小 1px，看起來就低 0.345px。

       要往上抬的量 ＝ 0.345 × (父層字級 − 自己的字級) ÷ 自己的字級　（單位 em）

   ⚠ **`em` 指的是元素自己的字級**，所以同樣是 `.72rem` 的兩顆標記，父層不同值就不同：
   醫師卡的專科藥丸（父層 h3 16.8px）是 `.158em`，專長標記（父層 dd 13.28px）是 `.053em`。
   ⚠ **`.doc-role` 正式站本來就沒寫 vertical-align**，所以線上那顆一直低 1.82px。
   ⚠ 抬升會不會撐高行框要自己算：抬完之後元素離基線的高度只要還在 strut 的
   ascent 以內就不會（那一輪是 16.26 < 18.42、15.04 < 16.22，十一個視窗實測沒變）。
   ⚠ **符號正負很容易寫反**：`vertical-align` 正值是**往上**。第一版寫成 `-.12em`
   反而把字往下推了 1.38px，加上原本就低的 0.61 共 1.99px，使用者一眼就看出來。
   ⚠⚠ **有底色的標記要對「塊」，不是對「字」**（第二輪才發現，同一天）。
   把字對齊之後使用者仍然說「看起來對到下緣」—— 因為**填滿的標記，眼睛讀的是那個
   色塊**，而**塊裡的字本來就不在塊的正中央**：塊高 ＝ line-height ＋ 內距 ＋ 框，
   那條線對的是字型的 ascent／descent，中文字面中線比它高 **0.66px**（.72rem 的標記）。
   所以「字對齊了」＝「塊往下掉 0.66px」。要對塊就在字對字的量上再加那 0.66px：
   `.doc-role` .158 → .218em、專長標記 .053 → .111em。
   **判斷方法**：量「塊上緣比旁邊字的 ink 上緣高多少」與「塊下緣比 ink 下緣低多少」，
   兩個數字要一樣。修完是 3.85 / 3.87（原本 3.20 / 4.53）。
   ⚠ **同樣的偏差，字愈小愈明顯**：藥丸旁邊是 16.8px 的名字，1.38px 的不對稱看不太
   出來；標記旁邊只有 13.28px，4.53px 的下墜佔了三分之一行高，一眼就看到。
   ⚠⚠⚠ **抬升量算得出來，但不能寫死** —— Blink 把 inline-block 的**塊內基線位置
   進位到整數 px**，所以「塊中線離自己基線多少」不是字級的線性函數（實測五個字級
   的「基線離塊頂」是 13.797／13.797／14.797／15.797／15.797，跳階），
   **而且同一個字級在不同 DPR 上會落在不同的整數**（同一顆標記 DPR1 要抬 0.274px、
   DPR3 要抬 1.274px）。寫死的 em 會在某些字級／某些裝置上差到 1px。
   解法是**開頁時放一顆零寬探針量一次**，把差額寫進 CSS 變數（`nav-scrollspy` 的
   `--lamp-pad` 是同一個做法）。
   ⚠ **量「那一行的基線」時，探針要插在標記的後面，不是前面。** 三種寫法踩過兩種：
   ①挑一顆純文字當參照 —— 那些詞自己會折行、rect 跨兩行，差幾十 px；
   ②插在前面 —— 標記被擠到下一行時探針留在上一行（375／320 上量到 −19.44 就是這個）；
   ③**插在後面** —— 零寬的東西不會自己折行，一定留在標記那一行。
   ⚠⚠⚠ **第三輪（同一天）：上面兩輪對的都是「塊相對於旁邊那行字」，但塊心本來就
   不在自己那幾個字的中線上，所以對完看起來還是低的。** 有底色的標記，塊的大小
   一直是從「行框 ＋ 內距」長出來的，而**行框對的是字型的 ascent／descent** ——
   那兩個值涵蓋拉丁字母的上伸與下伸部，中文方塊字用不到那麼多，
   **而且用不到的部分上下不一樣多**（`line-height: 1.5` ＋ 上下各 .05rem 的結果是
   上面 3.51px、下面 5.69px，**下面多 62%**）。
   解法是把行框從尺寸裡拿掉：**`line-height: 1`，塊高改由上下兩個內距各自負責**，
   由 JS 現量成「字面上下各留一樣多」。做法與落選案見 `/history/spec-tag-fit.html`。
   ⚠ 這個偏差**這一站每一顆有底色的小標籤都有**（`.doc dt` 0.389／0.542em、
     `.doc-role` 0.354／0.519、`.card-tag` 0.439／0.546、`.chips button` 0.604／0.821）。
     只有專長標記被看出來，是因為它夾在一句話裡、左右都有字可以對。
     **其餘那幾顆這一輪一個值都沒動**，不要為了「一致」順手改。
   ⚠ 量字面框用 `canvas` 的 `actualBoundingBoxAscent/Descent`，但**要在 400px 上量
     再等比例縮** —— Blink 回來的值顆粒很粗（實測 400px 上以 1/64 em 為階）。
10. **一條線性漸層在它的起點是一條線**（2026-08-12，照片與窄帶的接縫量出來）。
   `linear-gradient` 從 alpha 0 起跑，值是連續的，但**一階導數在那一列跳階** ——
   人眼對這個很敏感（馬赫帶），結果就是「顏色明明接得上，還是看得到一條邊」。
   解法是把 alpha 換成 **smoothstep（3t²−2t³）**：頭尾兩端的斜率都是 0，
   起點與終點都找不到。**顏色一個都不用動，改的是 alpha 的形狀。**
   ⚠ 同一個道理反過來：兩塊東西接在一起時，**只有顏色對上還不夠，斜率也要對** ——
     照片往下變暗、窄帶往下變亮，在同一列折返就會長出一條馬赫帶，
     所以兩側都要收平（窄帶的漸層因此在開頭補了四個色停）。
   ⚠ 「窄帶要拿一個和照片下緣一樣的顏色去接」這個做法本身有極限：
     **殘影也要對。** iPad 那邊接縫顏色只差 0.13 L\*，但柏油層收在 alpha .88、
     照片還剩 12% 活著，過了接縫變成 0 —— 眼睛看到的不是亮度跳階，是**殘影斷掉**。
11. **量一列的顏色要取中位數，不要取平均**（同一輪）。
   HERO 那張照片的下半部有路燈、車頭燈、門口的燈，**幾顆亮點就能把整列的平均
   拉高 4 個 L\***（實測 iPad 接縫上方：中位數 6.45、平均 10.4）。
   拿平均去比對接縫會得到完全錯的結論。
   ⚠ 這一輪的量法：Playwright 逐視窗擷圖 → 用 `zlib` 自己解 PNG（8-bit RGB／RGBA、
     非交錯，Chromium 的擷圖就是這種）→ 每列取中位數 L\* → 看相鄰兩列的差。
     容器裡**沒有 PIL、沒有 ImageMagick**，但 playwright 裝在
     `/opt/node22/lib/node_modules/playwright`（CommonJS，ESM 要 `import pkg from`
     再解構，具名匯入會失敗），Chromium 在 `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`。
12. **兩塊東西接不起來的時候，先問「能不能只改我自己這一塊」**（2026-08-12，
   照片與窄帶的接縫被使用者退回一次才學到）。當時的解法是**去改另一塊**——
   在照片下緣疊柏油漸層，把它染成窄帶的顏色。接縫確實不見了，
   但**那一塊是照片，它有內容**：手機的 HERO 只有 291px 高，那一層 22vw
   就吃掉 29%，路面與亮著燈的門面整片變黑。使用者一眼就看出來
   「照片都被弄掉了」。改成讓窄帶自己變深去接照片，照片一個像素都沒動，
   接縫一樣不見。
   ⚠ 判斷的方法很簡單：**兩塊東西裡，哪一塊是純裝飾？改那一塊。**
     窄帶是一條顏色，改深改淺沒有損失；照片是內容，蓋掉就是少掉。
   ⚠ 一起學到的是**要拿數字證明「沒有動到」**：逐像素比對照片那 291 列，
     0 個像素有差。這種話用看的不算，量了才算。
13. **「頁首閃跳」量出來是三件不同的事，不要當成一件治**（2026-08-14~15，
   使用者回報「右上三個標籤切換的時候會有畫面閃跳的感覺」，六版定案，
   推導與九個候選在 `/history/head-flash.html`）。四支螢幕錄影拆成 30 fps
   逐幀比對，量到的是：
   ・**① 整條頁首漏畫 1~2 幀**（33~67ms）。白字落在紙色上對比度剩 1.1。
     頁首那一條的平均亮度從 63.1 跳到 232.3。
   ・**② 頁首的深淺慢一幀。** 內容已經到目的地，頁首還套著出發點那一段的
     玻璃狀態，套在紙色上就是一條上深下淺的板。頁首的深淺是 JS 在 scroll 的
     rAF 裡寫的，主執行緒本來就比合成器晚一幀。
   ・**③ 選單的燈回跳一幀**（62 幀醫師介紹、63 幀診所資訊、64 幀才穩）。
     iOS 的捲動由 UI 行程驅動，某一幀讀到的 rect 還是上一個捲動位置。
   ⚠ **先確認漏畫的是哪一層再動手。** 閃的那幾幀放大看，三個玻璃框、
     `.nav-lamp`、品牌與所有白字**全都還在**，燈也已經移到剛點的那一項；
     再把三顆標籤橫著並排比前後五幀，**垂直位置一模一樣** —— 所以不是 JS 慢、
     不是 `html.scrolled` 掉了、也不是版面問題。不見的只有整個頁首裡唯一帶
     `backdrop-filter` 的那一層。沒有這一步會往 scrollspy 的節流或
     `scroll-behavior` 的方向找很久。
   ⚠⚠ **`::before` 和 `::after` 是負 z-index 的同一組，WebKit 整組一起丟。**
     把顏色從一層搬到隔壁那層**沒有用**（錄影第 198 幀兩層一起不見）。
     顏色要畫在**元素自己的 background** 上 —— 那一層在負 z-index 那組之前
     就畫完了。這是整件事的關鍵。
   ⚠⚠ **不要用 `will-change` / `translateZ(0)` 想把那一層釘住 —— 實測更糟。**
     加了之後連品牌標誌與「芳仁牙醫診所」那幾個字都會整幀不見。
     **多一個合成層 ＝ 多一個會掉的東西。**
   ⚠ **有一種閃不是漏畫。** 捲動太快，中間剛好只有一幀落在「窄帶還在頁首
     底下」那一段，那一幀就**合法地**長成玻璃的樣子。畫得完全正確，
     眼睛讀起來一樣是閃。這一種只能靠「沒有中間那幾幀」——那三個標籤瞬間跳。
   ⚠ **只拆濾鏡不夠。** 使用者實測「加了 h 就不閃了」證明濾鏡是元凶，但同一輪
     回報「捲動到窄帶的時候底下的字突然變得比較清楚」—— 窄帶底下有字
     （地址電話、1983／9／5），模糊拿掉就會透上來（390 上最大差 65 階）。
     **拆濾鏡要和「把薄色收到不透明」綁在同一條 smoothstep 上一起做。**
   ⚠ **薄色不要同時畫在兩個地方。** 中間版本 `background-color` 與
     `background-image` 兩條都在頁首身上，平常 alpha 是 1 看不出來，
     跳一格那一幀 class 與變數短暫不同步就會**暗一階**。
   ⚠ **門檻要用幾何、不要用進度值。** `band-solid` 第一版寫成 `t >= 1`，
     手機與 iPad 過關但**電腦版 1440 沒到** —— `#topics` 那個落點的 t
     只有 0.88~0.92（三個視窗都是），而那正是唯一會出事的落點。
     改成跟著頁首高走（`(頁首高 − 窄帶.top) / 頁首高`）九個落點才全部過。
   ⚠ **點選單改瞬間跳，不要 `preventDefault` 自己 `scrollTo`** —— 那會把
     那一步歷史紀錄弄丟，上一頁就回不到上一節。只把這一次的
     `root.style.scrollBehavior` 設成 `auto`，捲完再交還給 CSS。
     而且**只給頁首那三個標籤**：頁尾導覽、往下看的指標都沒有這個問題。
   ⚠ 順帶治好一件本來就不太對的事：改動前點下去要等約 **380ms（23 幀）**
     燈才移過去（它在等捲動跨過判斷線），現在是點的當下就到。
   ⚠ 這類改動要**逐像素證明**：新版對改動前的線上版，三個視窗 × 八個位置，
     HERO 頂 0 個像素有差、照片與窄帶交界最大 3~4 階、紙色區 92934 個像素
     差 1~2 階（只有 3 個超過 8）。驗「保險有沒有用」的方法是**用
     `display: none` 把那幾層藏起來**模擬漏畫的那一幀：改動前條子整條消失、
     重現了錄影裡那張白底鬼影，改動後條子仍然是深的。
   ⚠ 這一整段只有首頁有。文章頁走 `assets/style.css`，那邊的頁首本來就是
     一塊實心 `var(--head)`（沒有照片可以糊），碰不到這件事。
14. **兩份樣式表要「長得一樣」的時候，要問的是「和哪一態一樣」**（2026-08-15，
   文章頁的頁首整組對齊首頁時查出來的）。`assets/style.css` 的註解從
   2026-08-07 就寫著「品牌、選單、字級、間距全部和首頁相同」，**它抄的是
   首頁一開始那一組尺寸** —— 可是首頁捲動之後會套 `html.scrolled` 整組縮一階，
   而**使用者一定是從捲到下面的首頁點進文章的**，比較的對象從來不是那一組。
   文章頁自己「永遠停在捲動後那一階」，尺寸卻停在捲動前，兩件事對不起來。
   ⚠ **判斷方法**：這一頁永遠是哪一態，就去抄那一態，不要抄「基底那一條」。
   ⚠ **只對頁首不夠，版心要一起看。** 頁首的左右緣是 `--pad` 與版心寬度決定的
     （首頁 1160／20–40px、文章頁原本 1088／16–32px），只改頁首那一條的話
     1440 上頁首會比同一頁的頁尾往內縮 28px。版心是整站的骨架，要換就整頁換。
   ⚠ **抄之前先確認那個值後來有沒有被別的一輪改過。** 副標的字級在
     `html.scrolled` 那一段是 `clamp(9px, 2.35368vw, 11.25px)`，但
     2026-08-13「手機的可讀性」十二項又在**另一個 @media 裡**把它抬成
     `clamp(10px, 2.66vw, 11.7px)`。照第一個抄，375 上就會小 1px。
     **grep 那個選擇器的每一次出現，不要只看第一次。**
   ⚠ **驗收要用「兩頁互換看不看得出來」，不是用眼睛掃。** 做法：九個寬度
     各截兩張頁首（DPR 3），逐像素相減。定案時除了首頁那顆**亮起來的**選單項
     （這一頁沒有，刻意的）之外，幾何位移量到 0.0 裝置像素、bbox 完全相同，
     剩下的只有字緣的次像素抗鋸齒（0.1~1.2% 的像素差 8 階以上）。
   ⚠ **有一格是不該追的**：選單總寬差 0.14px，成因是 `letter-spacing: .01em`
     在兩頁 body 上算出來的絕對值不同（首頁 16px→0.16、文章頁 17px→0.17，
     字距是以**計算值**繼承的）。硬編 `.16px` 等於把首頁的 body 字級寫死在
     另一份檔案裡，日後沒有人會記得回來改。**0.06% 的寬度差，留著。**

15. **`srcset` 的 `sizes` 只看版面寬度，但 cover 的倍率是高度決定的**（2026-08-15，
   手機版 HERO 改成滿版之後量出來）。`.hero-photo img` 是 `object-fit: cover`，
   框比照片瘦的時候縮放倍率 s ＝ 照片高 ÷ 照片列數 —— 可是瀏覽器挑 srcset 是拿
   `sizes` 去比，寫 `100vw` 就等於告訴它「只要 390px 寬」，於是**嚴重低估**：

       裝置                  挑到的檔        實際需要   放大
       iPhone 14 Pro DPR3   1600w（1058列） 2157 列    2.04 倍
       DPR2 的手機           800w（529列）   1438 列    2.72 倍  ← 最慘
       iPad Pro 11 DPR2     1600w           1672 列    1.58 倍

   ⚠ **症狀是「版面沒問題但照片糊」，而且只有高 DPR 的實機看得出來** ——
     擷圖如果用 deviceScaleFactor 1 就完全重現不出來，一定要用 2 或 3 去量。
   ⚠ 正確的請求寬度 ＝ 照片**整張**在畫面上多寬 ＝ 長寬比 × 照片高度。
     現在寫 `sizes="calc(1.512 * 100vh)"`（1.512 ＝ 2000/1323）：
     各版型照片高是 62svh~100svh，用 100vh 統一估最多高估 18%，
     換來一條式子走遍全站，而電腦版 1440 DPR1 算出 1361 仍然挑 1600w、沒有被迫多載。
   ⚠ **不要用 `img.naturalWidth/naturalHeight` 去驗證放大倍率** —— srcset 用 w
     描述子時，瀏覽器回的是「除以密度」之後的 CSS 尺寸，算出來會剛好都是 1.00 倍。
     要自己按檔名對照真實列數（800→529、1600→1058、2000→1323）。
   ⚠ 這件事**只在照片被拉高之後才變嚴重**：改動前手機的照片只有 291px，
     800w 綽綽有餘。**日後只要動到 HERO 的高度，就要回來重驗一次 srcset 挑哪一張。**

16. **手機上「點下去閃一塊黑影」多半不是自己畫的，是 iOS 的點擊高亮**（2026-08-17，
   使用者回報「停車場切換的時候會很快速短暫的出現黑影」）。
   `-webkit-tap-highlight-color` 預設是半透明的黑，任何**可點的東西**都會有
   —— 這一站只有「回到最上面」那顆 `.btt` 關掉過，地圖那三顆停車場沒有。
   ⚠ **在 SVG 裡它蓋的是那個群組的外框，所以會比看得到的圖形大一圈。**
     停車場的 `<g class="lot">` 裡還有一顆入口箭頭 `.ent`（`visibility: hidden`
     不是 `display: none`，故意的，見那一段的註解），外框因此把箭頭一起算進去，
     黑影往旁邊凸出一塊 —— 畫面上讀起來是「方塊旁邊多一塊黑影」，
     很容易誤判成陰影、濾鏡或哪一層漏畫。
   ⚠ **關掉它要寫在容器上**（這次是 `.map-fig`）：這個屬性**會繼承**，
     三顆停車場、牌子上那三個連結、以及「點空白處＝收回」的那一層一次涵蓋。
     寫在 `.lot` 上只治得到被點的那一顆。
   ⚠ **診斷方法**：向使用者要螢幕錄影，用 ffmpeg 逐幀拆開（這一次是 60fps、
     1126×2436），量「哪一塊變暗、暗幾階、持續幾幀」。這一次是 **5 幀（83ms）**
     然後淡出，色塊旁邊那一條從 200.6 掉到 142.0 —— 淡出、蓋住整個外框、
     只有點擊那一刻出現，三件事合起來就只可能是系統的點擊高亮。
     ⚠ 容器裡**沒有 ffmpeg**，`pip install imageio-ffmpeg` 會帶一支靜態的進來。
   ⚠ 這種改動要拿數字證明沒有動到別的：`.card-map` 在 390／834／1440 三個視窗
     擷圖逐子像素比對，**0 個有差**（它不影響版面，桌機也根本不會畫）。
17. **「淡出」這種東西有三個獨立的量，使用者一句話會同時講到三個**（2026-08-17，
   橫捲那排標記的邊緣淡出。使用者：「標籤的模糊不自然，看起來不是連續的漸層，
   從『主』這個字的右緣就開始模糊了，覺得拖太長。」）。拆開來是：
   ・**長度怎麼出現**（跳出來 vs 跟著捲動長出來）
   ・**曲線的形狀**（線性 vs smoothstep）
   ・**長度本身**（16 vs 10px）
   三個各自有一句抱怨，**修一個不會治好另外兩個**，一起改才對得起那一句話。
   ⚠ **「不連續」這個字要當成兩件事去找**：一是曲線自己的一階導數跳階
     （＝第 10 條那條馬赫帶），二是**狀態切換造成的跳階** —— 這一輪真正最刺眼
     的是後者：`data-sx` 在 `scrollLeft > 1` 那一刻把左緣的遮罩從「不淡」
     換成「淡滿 16px」，畫面上沒有任何東西被捲掉，字卻先糊了。
     **凡是「跟著捲動」的效果，長度／濃度都要是捲動量的連續函數**，
     不要用門檻換整張。這一站的兩顆角形（`--ar-fl`／`--ar-fr`）本來就是這樣寫的，
     遮罩沒跟上而已。
   ⚠⚠ **遮罩壓在有底色的標記上時，字會比色塊先不見。** 填色標記上的字是白的，
     白（255）褪到紙色（226）幾乎沒有距離，而色塊（95）還有 131 階可以走 ——
     所以同一條 alpha 斜坡，眼睛先看到「字糊了」再看到「塊淡了」。
     **判準因此不是「斜坡多長好看」，是「斜坡會不會碰到字面」**：
     這一排左內距 12.8px，10px 的斜坡剛好停在第一個字之前。
   ⚠ 使用者報位置的時候會拿**旁邊那一行字**當尺（他用的是標題「主」的右緣）。
     那種座標**量得出來也對得上**：斜坡終點 30px、「主」的右緣 32.27px，差 2.3px。
     **先照他給的錨點量一次，不要當成模糊的形容詞。**
   ⚠ 用 CSS 變數控制長度的漸層，**每一個色停都要寫成 `calc(var(--len) * k)`**；
     長度收到 0 時所有色停疊在同一點，後面那個贏，等於「這一側不淡」——
     不必再為「不淡」寫一張 `mask-image: none`（實測 1112 那一格對改動前
     逐像素 0 個有差）。

18. **用 Chromium 產圖，一定要挑 `headless_shell`，不能挑完整版 `chrome`**（2026-08-19，
   做 `assets/logo.png` 時踩到）。這個容器裡兩支都在：

       /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell   ✅
       /opt/pw-browsers/chromium-1194/chrome-linux/chrome                          ❌

   完整版**實際畫出來的高度比 `--window-size` 少 87px**（視窗外框），四種尺寸實測
   都是固定 87：600→513、800→713、512→425。
   ⚠⚠ **而 PNG 仍然輸出完整尺寸**，底部那一截靜靜地變成空白，
     **Chromium 不報錯、Node 不報錯、檔案大小也很正常**。症狀是「形狀下緣被平切」，
     而且只有把圖放大看才看得出來 —— 這一次是使用者看圖發現的，我的三道檢查
     （透明比例、顏色、牙洞有沒有挖穿）全部通過。
   ⚠ `tools/app-icons.mjs` 與 `tools/favicon-ico.mjs` **沒有這個問題**：它們的
     `chromeCandidates()` 本來就把 `headless_shell` 排在整份清單最前面。
     新寫產生器時直接照抄那份清單，不要自己寫死路徑。
   ⚠⚠ **檢查要檢查「形狀對不對」，不是只檢查「顏色和透明度對不對」。**
     `tools/logo-png.mjs` 因此加了兩道：① 標誌的長寬比要等於 2.02918（±0.02）
     ② 最下面四列的寬度要收窄（末列 < 首列的一半）。被平切時最後一列會突然很寬，
     第 ② 道抓得到第 ① 道漏掉的邊界情況。實測故意用完整版 chrome 跑，
     會停在「標誌長寬比 2.1739 對不上 2.02918」並拒絕寫檔。

19. ~~**`assets/icon.svg` 的註解說「牙洞放大 1.18 倍」，實測方向相反**~~ ——
   **✅ 2026-09-03 查清楚了：註解是對的，基準是 AI 原始檔。**
   使用者當天把標誌的 Illustrator 原件上傳進 `brand/`，量出來原件的
   **牙洞寬 ÷ 外框寬 ＝ 0.078894**（外框長寬比 2.02918，和 `icon.svg` 逐位對得上），
   所以 `icon.svg` 的 0.093080 ＝ 原件的 **×1.1799**（＝註解寫的 ×1.18），
   頁首那顆的 0.102514 ＝ 原件的 **×1.2995**。
   **兩個檔各自都是對的，沒有東西要改**，下面那段是當時的紀錄：

   （2026-08-19 量到）用真貝茲極值量三條路徑：

       index.html 頁首   外框長寬比 2.02926   牙洞寬 ÷ 外框寬 0.102514
       assets/icon.svg   外框長寬比 2.02918   牙洞寬 ÷ 外框寬 0.093080  ← 小 9.2%

   外框形狀兩者完全相同（小數五位都一樣），差的只有牙洞：`icon.svg` 那顆是頁首的
   **×0.908**，不是註解寫的 ×1.18。
   ⚠ 當時寫「可能的解釋是『×1.18 是相對於 AI 原始檔，而頁首那顆放得更大』，
     但沒有 AI 檔可以驗證」—— **2026-09-03 拿到 AI 檔，就是這一個解釋**（見上）。
     ⚠⚠ **通則：這一站有好幾條「還沒查清楚」是卡在拿不到原始素材**，
     不是卡在推理。素材進來的那一天要回頭把它們結掉。
   ⚠ `assets/logo.png` 取的是**頁首那一條**，理由見第十節第 2 項。

20. **⚠⚠ 這張地圖裡不能用 CSS 的 `filter`**（2026-08-21，診所的圖釘那一輪）。
   地圖的每一樣東西都在 `<g id="map-clip">` 那個 `clip-path` 底下，**WebKit 對
   「被裁切的群組裡再套 CSS filter」會整個丟掉** —— 影子第二版用 `drop-shadow()`，
   Chromium 上四階都畫得好好的，**iOS Safari 上一階都沒有畫**。
   使用者的螢幕錄影（1126×2436、60fps）逐幀量：切在「重」的那一幀對切在「無」
   的那一幀，最深只差 **3.3 階、有感的像素只有 2 個**（同一組值在 Chromium 上是
   90 階、2000 CSS px²）。
   ⚠ **症狀是「使用者說看不出來、你在電腦上看得一清二楚」** —— 先別忙著加強，
     先問「這個效果在他那一側到底有沒有被畫出來」，逐幀量一次就知道。
   ⚠ 解法是**畫出來**：同一條路徑疊十六層、每層很淡、逐層放大。純圖形，
     Safari 與 Chromium 都一定畫得出來。
   ⚠ **疊出來的模糊，總濃度是 `1−(1−α)^層數`，不是 α×層數。** 層數一改 α 就要
     跟著換算（`α_new = 1−(1−α_old)^(舊層數/新層數)`），十層改十六層時忘了換算，
     四階整組黑掉一倍。

21. **俯視圖上「站著的東西」的影子是投影，不是把剪影往旁邊挪**（2026-08-21，
   同一輪。使用者一句話點破前七輪：「你把整張地圖當成貼在牆壁上、光源照下去，
   但我想要的是**地圖平貼在桌上**、光源照下去的感覺。」）。
   ・貼在牆上的東西（和牆平行）→ 影子 ＝ 剪影整個平移。
   ・站在桌上的東西（俯視）→ **接觸點不動**，愈高的部分被推得愈遠：

         x' = x − L·cos(方位)·y
         y' =   − L·sin(方位)·y        （y 往上為負，所以取負號）
         L  = cot(太陽高度)            （太陽愈高 L 愈小、影子愈短）

     寫成一個 `matrix(1 0 −L·cosφ −L·sinφ 0 0)` 就結束了。
   ⚠ 圓形的東西投影之後會變成**斜的橢圓**，那是對的、不是畫錯。
   ⚠ 接觸點永遠銳利、愈遠愈糊 —— 模糊的那幾層要**以接觸點為原點放大**。
   ⚠ 判斷「影子讀不讀得出方向」的量：**位移要壓過擴散**。擴散比位移大的時候，
     影子往四面八方長，看起來就是「在背後暈開」。

22. **兩張擷圖逐像素比對之前，先確認它們的取樣是不是對齊的**（2026-08-21）。
   各自重新載入之間有 ±1 個裝置像素的差，落在硬邊上會量到 155 階 —— 那全是位移
   不是要量的東西。三種做法：① 同一個 page 只按按鈕切；② 只取「底色本來就均勻」
   的像素（3×3 鄰域最大最小差 < 3）；③ 兩邊都捲到同一個整數位置。
   ⚠ **提案頁的切換條是 fixed 的，Playwright 的元素截圖照樣拍得到它** ——
     不先 `display:none` 藏起來，會量到 22% 的像素有差，那全是切換條。

23. **⚠⚠⚠ 「線上看不到」先想快取，而且 404 也會被快取**（2026-08-25，追了七八輪，
   使用者按了八個網址）。症狀是植牙的線稿「手機、iPad 上都沒有」「剛才有看到、
   重新整理又沒了」，而 repo、`origin/main`、`npm run build` 的產物、
   `_site` 那一份逐視窗的實際渲染、六科逐科比對 —— **每一關都是對的**。

   成因全部在 Cloudflare 的邊緣快取：
   ・**這一站的 HTML 沒有內容雜湊在檔名上**（網址就是 `/topics/prosth/`），
     舊的 HTML 被存住，改版就會「有時候看得到、有時候沒有」。
   ・⚠⚠ **404 也會被快取，而這一站的網址是「會長出來的」** —— 新文章、新的
     著陸頁、剛加的 `/version.txt`。**檔案還沒上線之前有人先打開過那個網址，
     那個 404 就被存住**，之後檔案明明上線了、拿到的還是 404。
     這一輪就是我把 `/version.txt` 的網址在建置完成之前給了使用者。
   ・⚠ **無痕視窗擋不掉這件事** —— 無痕跳過的是使用者自己手機的快取，
     跳不過邊緣的。查快取問題**唯一可靠的手段是換一個沒人開過的網址**
     （後面接 `?t=<隨便一串>`）。

   **已經治掉的**（`src/worker.js`，2026-08-25）：HTML 加 `Cache-Control: no-cache`
   （可以存，但每次回來驗，沒變回 304）、**404 與 `/version.txt` 一律 `no-store`**。
   圖片、樣式表、JS 沒有動。

   ⚠⚠ **「網站好了嗎」現在有標準答案：開 <https://fangren.net/version.txt>**
   （`tools/dist.mjs` 每次建置寫的，內容是 commit 編號＋時間，不進版控，
   所以永遠是那一次建置的真值）。**推完之後不要立刻把網址給使用者**，
   先等建置跑完 —— 這一輪那個 404 就是這樣被存進去的。

   ⚠⚠⚠ **2026-09-03：第一次遇到「三層都對，但線上就是舊的」——是 Cloudflare 沒有部署。**
   使用者回報 `/preview/line-bind-done/` 開起來「沒有」（其實是開得起來、但停在上一版：
   那一版預設顯示的是純文字案、而且還沒有頭圖）。`version.txt` 讀到 **`b297b51`（15:12）**，
   而 `main` 的頭是 **`e75ccec`（15:35）**，**中間隔了七小時四十分鐘**。
   查過的四關全部是對的：
   ・**GitHub 上的 main** ＝ `e75ccec`（不是看本機的 `origin/main` ref —— 是
     **從 GitHub 重新 clone 一份**下來確認的，兩個檔都在）。
   ・**乾淨檢出跑 CI 那條指令**：`git clone` 到暫存區、`git checkout e75ccec`、
     `npm run build` → **exit 0**、259 個檔案、`_site/version.txt` 寫的正是 `e75ccec`。
   ・**上線那一份真的跑得動**：把 `_site` 當根目錄開伺服器實際載入那一頁，
     HTTP 200、卡片 268px、頭圖載到、主控台 0 個錯誤。
   ・**`version.txt` 不會說謊**：Worker 對它設 `no-store`（就在本節下面那一段），
     所以使用者讀到的 `b297b51` 就是線上的真值。
   **結論：那一次 push 根本沒有被建置或部署。** 剩下的四個可能只有使用者打得開後台：
   ① 建置失敗（看 Deployments 最上面那筆有沒有紅字）
   ~~② 建置額度用完~~ —— **2026-09-04 使用者拍後台排除了：`35 / 3,000` 分鐘**
      （Workers & Pages → Usage → Workers build minutes this month）。九月只用了 1.2%。
   ③ **Build watch paths** 把那次改動的路徑排除掉了（Settings → Build）
   ④ 有一個舊版本被固定在 100%（Version History，同 08-25 那一輪看的那一頁）

   ⚠ 順帶從那兩張截圖確認的兩件事：**Workers Builds 自己的 Deploy command 就是
   `npx wrangler deploy`，那是正常的**（建置頁面上會標成 `Manually deployed`）——
   **不要把它和第二節那條紅線搞混**，紅線禁的是「在自己電腦上跑」，不是 CI 跑。
   另外，**建置是會失敗的**：他打開的那一筆 `#fabf43b6`（32 天前）是紅色的 ❌、
   17 秒、**停在 `Deploying` 那一段**。所以 ① 仍然是最可能的一個。
   ⚠ **通則：`version.txt` 落後超過十分鐘就不要再說「等一下就好」** ——
   那是「沒有部署」，不是「還在部署」。**要去查的是後台，不是 repo。**
   ⚠ 順手記一件：repo 的 `.git` 已經 **250 MiB**（歷年的 JPEG／PNG），
   Cloudflare 每次建置都要 clone 一次。目前還沒出事，但它只會愈來愈大。

   ⚠ 順手記一件**還沒查清楚**的：`version.txt` 只寫 ASCII，中文的 commit 訊息
   寫進 `.txt` 在 Safari 上是一整排亂碼（`.txt` 沒有地方宣告編碼）。
   Worker 現在對它補了 `charset=utf-8`，但檔案本身仍然只寫 ASCII，不必冒險。

24. **產生器是模板字串的時候，字串裡的 `\n` 會在「產生的當下」就變成真的換行**
   （2026-08-31，`tools/spec-color-preview.mjs` 踩到）。切換條那一整段是 JS 的
   模板字串，裡面寫 `'…'+'\n'+'…'` 的話，產出的 HTML 就變成一個**從中間斷開的
   字串字面值**，整支腳本語法錯誤。要寫 `'\\n'`。
   ⚠ **症狀是「切換條按了完全沒反應」，而且畫面一切正常、主控台要打開才看得到** ——
     和第 8 條那個「註解裡不能出現結束標籤」是同一種病：不報錯、只是那一段沒作用。
   ⚠ 治法是**在產生器裡加一道守門**：把產出的 `<script>` 抓出來，
     逐行數單引號，奇數就 throw（那一支現在就是這樣擋的）。
     ⚠ 這一條和第 8 條合起來的通則：**產生器寫出來的東西要自己驗一次，
     不要只驗產生器本身跑不跑得動。**

25. **提案頁的覆寫規則，目標科別要寫死，不要用「當頁的那一科」**（2026-08-31 踩到）。
   同一支產生器做了兩頁（植牙一頁、矯正一頁），覆寫寫成
   `html [data-spec="' + 當頁的 spec + '"] { --accent: 植牙候選的填色 }` ——
   在矯正那一頁，當頁的 spec 是 `ortho`，於是**植牙候選的顏色被套到矯正頭上**，
   示範裡「被選到」那顆矯正變成 `#335b8b`。
   ⚠ **症狀很像「顏色沒吃到」，其實是吃到了別人的**。驗收要拿
     `getComputedStyle` 讀出實際的 rgb 去比對預期的那一顆，
     不能只看「有沒有變」（它確實變了，只是變錯）。

26. **⚠⚠⚠ 使用者貼的圖沒有落進 `uploads/` 時，去對話的逐字稿裡撈**（2026-09-05，
   〈三個月一次的洗牙與塗氟〉那張 HERO 連著三輪都沒落地）。症狀是**畫面讀得到、
   檔案拿不到** —— `/root/.claude/uploads/<session-id>/` 裡最新的還是幾輪之前那張，
   全機 `find` 也掃不到，於是 `tools/hero-resize.mjs` 沒有東西可以吃。

   **圖其實一直都在**：每一則訊息裡的圖片是 base64 存在逐字稿裡的 ——

   ```
   ~/.claude/projects/<專案>/<session-id>.jsonl
   → "media_type":"image/jpeg","data":"<base64>"
   ```

   逐行掃出來解碼就是**原尺寸的檔案**（這一次是 2000×1493，和使用者電腦上那個
   逐位元組相同 —— **不是**聊天室縮過的 1200 寬那一份）。
   ⚠ 那個 jsonl 這一次是 **33MB**，要用 `readline` 逐行讀，不要整份 `JSON.parse`。
   ⚠ **同一張圖會出現很多次**（每一輪都重送一次上下文），用 md5 去重、取最後一個。
   ⚠ 也要順手比對尺寸：**同一張圖可能同時有原檔與縮圖兩種**，挑大的那個。

   ⚠⚠⚠ **2026-09-07 補：鍵的順序不是固定的。** 同一份 jsonl 裡有兩種寫法 ——
   `"source":{"type":"base64","media_type":"…","data":"…"}` 與
   **`"source":{"data":"…","media_type":"…"}`（data 在前）**。
   要求「media_type 在前、data 在後」的正規式會**靜靜地漏掉後面那一種**
   （`grep -c media_type` 數得出來、撈出來卻少一半）。
   做法：**只抓 `"data":"<base64>"`，副檔名從頭兩個位元組認**（`89 50`＝PNG、`FF D8`＝JPEG）。
   ⚠ 撈出來沒有檔名，**哪張是哪張要自己認**：先把新舊兩張都縮成 48×72 的灰階
   算相關係數（對得上的 0.75~1.00），篩不掉的再裁出標題那一條疊起來看一眼 ——
   **先用數字篩，不要一開始就一張一張看**。

   ⚠⚠ **走不通的兩條，不要再試**：① 請使用者貼本機路徑（`C:\Users\…`）——
   雲端 session 跑在容器裡，看不到他的硬碟，貼過來只是一段文字；
   ② 叫他「再傳一次」—— 這一輪傳了三次，三次都沒落地。
   **第一時間就去逐字稿撈，不要先來回請他重傳。**

27. **`git mv drafts/<slug> posts/<slug>` 會把整個資料夾搬過去**（2026-09-05 踩到）。
   草稿資料夾裡通常還躺著幾份 `ARTICLE-v*.md`、`FACTCHECK.md` 與一次性的產生器，
   而 `tools/dist.mjs` 是**遞迴複製 `posts/`** 的 —— 那些檔會安靜地跟著上線。
   **只搬 `index.html`**，其餘留在 `drafts/<slug>/`。

28. **⚠⚠⚠ 版面的事不要一次只給一個值 —— 給一把尺；而且改完要回頭掃一次**
   （2026-09-07，貼文的看診時間圖走了二十輪之後，使用者：「這次應該有很多基本的
   美感和概念都是我慢慢修出來的，之前你們沒有這麼糟糕，怎麼回事呢。」他是對的，
   而且逐輪回看，成因不是「美感」而是**四個可以寫成規則的做法**）。

   ⚠⚠ **他點名的十幾件裡，有一半以上是我自己上一個改動造成的第二層效應**：
   ・圖案改成「等重」（每一顆寬度不同）→ **同一欄不同列對不齊**（他先看到）
   ・整體放大 → **一天和一天的圖案擠成一片**（他先看到）
   ・列距拉開 → **「早午晚收成一行」那個決定已經過期了**（他先看到並提出斷行）
   ・標籤改成 block → **對齊方式跟著從置中變成置左**（他先看到）
   每一件都是「我改的東西讓自己先前的某一句話變成假的」，而我沒有回頭掃。

   **四條要照做的：**

   ① **尺是預設，不是加分項。** 凡是大小／間距／比例／濃度這一類「他的眼睛才是
     裁判」的事，**一次給三到四格**（這一站第八節的切換條就是這件事），不要送一個
     我估的值等他退回。這一輪我送單值送了十幾次，最後是**他開口要**「做幾個版本」
     我才給 —— **那是流程退步，不是他難伺候**。
     ⚠ 產生器裡多出一張圖只是多一次 screenshot，成本幾乎是零。

   ② **改完要問「這一改讓哪一句話變成假的」。** 每一個定案的值背後都掛著一個前提
     （「一行放得下」前提是列距很緊、「間距 11 剛好」前提是欄有多寬）。
     動到任何一個共用尺寸（倍率、內距、欄寬、列距）之後，**把相關的決定逐條唸一次**，
     不要等他發現。
     ⚠ 做法：把那些相依關係寫進產生器的註解與那一輪的 README，
     並且**在每次出圖時把一整組「讀起來對不對」的數字印出來**
     （群內÷群間、標籤有沒有折行、置中偏多少、留白比例、對比度），
     這樣回歸會表現成「某個數字動了」，而不是等他看圖。

   ③ **自己推導出來的限制正在壓縮成品時，要當場講。**
     這一輪最貴的一件是「主頁大格只看得到中間 537 列」——那是我自己推出來的限制，
     我為了守住它，把版面縮在正中央一小塊、每一輪都在削 2~4px 的內距去擠進去，
     **卻從來沒有跟他說「這條限制正在讓字變小」**。他看的是成品，一句
     「周圍還有很多空間」就把六輪的前提推翻了。
     ⚠ 判準很簡單：**當我開始為了守一條限制而削別的東西時，那就是要講的時候**
     （餘裕剩個位數 px、或連續兩輪都在挪空間），把取捨攤開讓他選。

   ④ **每一輪都要用「成品會被看到的尺寸」看一次，並且先說出哪裡不對。**
     這一輪我每一輪都打開 PNG 看，但看的是「有沒有壞」，不是「讀起來好不好」。
     守門也全是壞掉檢查（溢出、折行、對比、超出安全帶），
     **沒有一條在量「這一塊讀不讀得出是一群」「留白比例對不對」**——
     那幾條都是他先講、我才補上去的。
     ⚠ 通則：**壞掉檢查擋不住醜**。每加一種新的版面關係，就要同時想好
     「它壞掉的時候會是哪一個數字不對」，當場把那個數字加進面板。

   ⑤ **⚠⚠⚠ 一份資料搬到新的媒介，要逐項問「原本沒有意義的性質，在這裡有沒有
     變成看得見的東西」。**（2026-09-08 補，使用者：「不同的 logo 一開始非常凌亂，
     後來被我指定位置才舒服很多，這個你們一開始也應該要注意到的。」）
     那八格的圖案順序是從 `index.html` 的 `data-in` 讀回來的，而**站上那張門診表
     一格只畫一顆點**（有診／沒診）—— 誰寫在前面完全看不出來，所以那串字的順序
     從來沒有人決定過，它是任意的。貼文圖一格要畫二到四顆圖案，
     **同一份資料換了媒介，順序突然變成資訊**，而我原封不動照搬，
     等於讓一個沒有人決定過的東西決定版面。
     ⚠ 通則：順序、間距、大小寫、檔名、寫的先後，都可能在新媒介裡突然開始說話。
     **那就是一個還沒有人決定過的設計項** —— 要當設計項處理（給一把尺、問使用者），
     不可以沿用資料裡碰巧的那一份。
     ⚠⚠ 它和第 ② 條是一對：② 是「我的改動讓自己先前的一句話變成假的」，
     ⑤ 是「**別人的資料在新場合多長出一個意思**」。兩件都要回頭掃，方向不同。
     ⚠ 已經做成守門（`post-hours.mjs` 的 `checkOrder()`，兩道）：
     **同一行出現過的兩科左右順序要一致**、**斷成兩行時第一行要由天天有診的那一科帶頭**。
     ⚠⚠ **不可以寫成全域的拓樸排序**（會誤報）——「四早」是 general・perio 同一行，
     而「五午／五晚」刻意讓 perio 自己站第一行、general 在第二行，跨行比對就翻臉了。
     **守門要擋的是「讀起來會亂」，不是「和某個表不一樣」。**

   ⚠ 誠實的另一半：這一輪有一部分本來就該由他決定（配色、形狀、浮水印、文案口氣、
   最後選 1.30 還是 1.352），那些不是失誤。**要檢討的是上面那五條**，
   不是「他改得多」。

29. **⚠⚠ Chromium 的 `canvas.toDataURL()` 遇到不支援的格式會靜靜回一張 PNG**
    （2026-09-18，WebP 那一輪）。`toDataURL("image/avif", 0.7)` 不會 throw、
    不會回 `null`，回來的是 `data:image/png;…` —— 同一張圖量到 **2959 KiB，
    比原本的 JPEG 大 8 倍**。唯一看得出來的線索是那個離譜的數字，
    而如果腳本只印「省了幾 %」不看正負號，這個錯會一路寫進 `.avif` 的檔名裡。
    ⚠ 守門要**檢查回來那個字串的開頭是不是你要的格式**，不是檢查它跑不跑得完。
    ⚠ 同一族的近親（本節第 5 條「量畫出來的東西」）：比對兩種編碼差多少，
    要把產出**再解回來逐像素比**，不可以只比檔案大小 —— 檔案小是目的，
    不是「沒有變壞」的證據。

30. **⚠⚠ 包一層 `<picture>` 會把 flex／grid 的那個 item 換掉。**
    這一站有三處 `<img>` 是 flex item（首頁文章卡、延伸閱讀卡 `.rel-card > a`、
    HERO 的 `.hero-shot`），包上去之後變成那個殼才是 item、`<img>` 退成孫子，
    寬高與對齊會整組跑掉。解法是 `picture { display: contents; }` ——
    讓殼整個不生盒子，版面和沒包之前逐像素相同。
    ⚠ **不要改成 `display: block`／`inline`**，那是「換一個盒子」不是「沒有盒子」。
    ⚠ `<source>` **要自己的 `sizes`**，它不會去抄 `<img>` 那一份；
    漏了的話 `srcset` 的 `w` 描述子會退回 100vw 去挑檔，挑到的往往比需要的大。

31. **⚠⚠ 量「強制自動重排」要用 CDP 的 trace，而且捲動一定要用合成手勢。**
    （2026-09-18）第一版在頁面裡跑 `scrollTo` 的迴圈去捲，量出 165 毫秒 ——
    **那全是量測腳本自己的**（`scrollTo` 與 `scrollHeight` 都會強制重排），
    比整站真正的還多。要用 `Input.synthesizeScrollGesture`（走合成器，
    完全不經過頁面的 JS）。⚠ trace 要開 `disabled-by-default-devtools.timeline.stack`
    才拿得到堆疊，沒有堆疊的 `Layout` 是瀏覽器自己排的、不算強制。
    ⚠ **開頁那一段也要錄進去**（先 `Tracing.start` 再 `goto`）——
    Lighthouse 量的主要就是那一段，`cardClamp`／`skFit` 都在那時跑。
    ⚠⚠ **要看「幾次」不要只看毫秒**：同一份程式碼跑六次，總毫秒在 278~442
    之間跳（±30%），**次數 80 卻每一次都一樣**。拿毫秒當前後對照會得到
    任何你想要的結論。
    ⚠⚠ **「一量一寫」不一定是主因。** `cardClamp` 十三張卡拆成兩趟之後
    次數 16 → 4、**時間一毫秒都沒省** —— 貴的是每一次呼叫的**第一次整份樣式
    重算**（31 毫秒），跟在後面那十二次一張只要 0.36 毫秒。
    **先看 trace 給的「幾次、各幾毫秒」再決定值不值得拆**，不要照著
    「read/write 分離」這條通則一路改下去。

32. **⚠⚠ 同時開著的分頁，`requestAnimationFrame` 會被節流 —— 「新舊並排比對」
    的快照一定要一次開一個。**（2026-09-18）比對舊版與新版寫出來的 CSS 變數時，
    第一版同時開三個 context（舊、舊、新）再各自 `scrollTo` ＋ 等兩幀，
    量出四個「差異」。**其實是背景那兩個的 rAF 根本沒跑**，捲動之後
    `paint()` 一次都沒有被呼叫，讀到的是上一個捲動位置留下來的值。
    ⚠ 症狀很像雜訊（每一次跑，不一樣的那幾格還會換位置），所以**很容易被
    「這是 HERO 動畫」打發掉** —— 判準是「舊對舊也不一樣」要能穩定重現，
    不穩定就先懷疑量測方法。改成「開一個 → 收完全部 → 關掉 → 再開下一個」
    之後 756 個值全部逐字相同、雜訊 0。

### 提案頁的切換條

`home-desktop-*` 幾頁都有切換條，網址可帶參數直接開到某一案
（`?band=`、`?gap=`、`?hero=`、`?align=`、`?fill=`）。
**參數的正規式要寫 `[a-z0-9]+`**，寫成 `[a-z]+` 會吃不到 `glass1` 這種帶數字的值，
比對失敗後悄悄退回預設，等於參數沒作用 —— 已經踩過一次。

切換條全部是提案用的，**定案後連同 `data-*` 屬性一起刪掉**，不要留到正式站。

---
