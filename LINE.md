# 診所的 LINE 官方帳號 —— 從 CLAUDE.md 搬出來的第十一節

2026-09-18 從 [CLAUDE.md](CLAUDE.md) 搬過來的。**內容一個字都沒有改**，
小節編號也原封不動（仍然叫「第十一之三節」「第十一之七節」），
所以 `drafts/channels/README.md` 裡那些「CLAUDE.md 第十一之 N 節」的指路
**內容仍然對得上**，只是檔名換成這一份。

> **為什麼搬出來**：CLAUDE.md 原本 42.9 萬字元、約 26 萬 token，
> 而它每一輪對話都整份載入。這一節自己就佔 26 萬字元（60.6%）——
> 而網站那一側的工作（改 CSS、發文章、調色）**完全用不到它**。

## ⚠⚠⚠ 要做 LINE 的任何一頁，先讀這三件

不知道就一定會做錯：

1. **Flex 的字級是固定 px 不是比例**（`xxs 11／xs 13／sm 14／md 16／lg 19／xl 22／xxl 27`）——
   規格頁一旦和真實算繪對不上，上面做的每一個判斷都是假的。
2. **`button` 不支援圖示**，要「圖示＋文字」得做成可點的 `box`（代價是失去按壓效果）。
3. **這個帳號沒有專人即時回覆** —— 所以訊息上**不可以寫「有問題隨時問」那一類的承諾**，
   那是說謊（第十一之三節那條紅線，守門在掃）。

完整的硬條件在第十一之二節，紅線在第十一之三節。
讀完這一份再讀 **[`drafts/channels/README.md`](drafts/channels/README.md)**（那條線的總檔案）。

---

## 十一、診所的 **LINE 官方帳號**（2026-09-02 起）—— 新的聊天要做 LINE 就先讀這一節

⚠⚠ **這是一條和網站平行的線。** 成品**不在 fangren.net 上**，是要貼進
**LINE 官方帳號後台**的訊息、圖與 JSON。網站這邊只放**規格頁**（`preview/line-*/`），
用途是讓使用者在手機上看，以及**之後整包拿給廠商看**。

### 十一之一、東西在哪

| | |
| --- | --- |
| **⭐⭐⭐ 給廠商的回覆（現在要給的是這一頁）** | ✅ **2026-09-21 做好了**，頁面 **`/preview/line-brief-0921/`**，文字 `drafts/channels/brief3.json`，產生器 `node drafts/channels/build-brief3.mjs`，守門 `node drafts/channels/check-brief3.mjs`（十六道，⑯ 與 ⑮ 負向測過）。使用者：「針對連結資料廠商給的回覆如照片／預約成功通知已完成呈現並上線／約診記錄查詢除了廠商回覆和最近一次設計好的版本有落差外／另外約診狀態 廠商這次回覆和之前不一樣　廠商之前說診所端取消患者在手機上查詢就不會跳出約診　但這次卻說　診所端取消　患者手機上還查得到且顯示已取消／整理好回覆頁面」。⚠⚠⚠ **編號一個都不換**（同 9/14 與 9/18）：這一輪接在 3-6 後面 ＝ **3-7 ~ 3-12**，「3」與「4」仍然是 9/14 那兩件。⚠⚠⚠ **2026-09-21 稍晚整頁換了形狀**（使用者：「PM 角色換成資深 PM　**文字語言要精簡賅要**　**問題、事件、解決方案要次序分明逐項列出**　重新整理目前頁面」→ TEAM.md 第一節升級成**資深 PM** ＋ 寫法四條）：**編號一個都沒動**，換的是每一節內部 —— **摘要表（項次／問題／狀態）放最前面**、每一件事拆成**問題／事件／解決方案三格**、逐項編號。守門第 ⑯ 道驗「摘要表在最前面 ＋ 六個項目各三格、一格都不空」，產生器那一側三格有一格是空的就 throw。⚠ 節號不再交給 CSS 的 counter 畫（那一版外層漏設 counter-reset，靜靜印成 1-7），全部是寫死的字，第 ⑧ 道連頁內每一個 `#錨點` 都驗。六節：**3-7** 9/21 的回覆（三句逐字 ＋ 截圖）／**3-8** 已對上的七項（表）／**3-9** 待解決四項（3-9-1 日期順序／3-9-2 四個值的用字／3-9-3 藥丸底色，甲 底統一白字・乙 沒有底套色字／3-9-4 藥丸位置與內距）／**3-10** 待釐清①　約診狀態兩次的回覆不一樣／**3-11** 診所這一側看到的（2026-09-21 使用者補的第一手事實：**診所在樂衍上「刪除」那一筆，病人的查詢就不會顯示**；9/21 的回覆講的是「取消」還看得到 —— ⚠⚠⚠ **那兩個詞不可以順手統一掉**，整節就是在問樂衍後台的「刪除」與「取消」是不是兩顆不同的按鈕，守門第 ⑮ 道在擋）／**3-12** 待翔評回覆（五題，每題標著屬於哪一節）。⚠⚠ **這一輪最值得記的是量法**：廠商送來的是一張截圖，比例尺**不可以用「假設卡片是 micro」倒推**（那正是要下的結論之一）—— 拿「約診狀態」那四個 `xs` 的字量，中日韓的字一個字的進位就等於字級，所以進位 ÷ 13 就是比例尺（量到截圖 1px ＝ 手機 0.506px），另外兩行（日期 `lg`、姓名 `md`）當交叉驗證。推回去：**卡片 162.1px ＝ `micro` 162px**（翔評真的換了）、帶子 13 顆一顆不少、墨 155.0 ＋ 左右留白 3.5／3.5 ＝ 卡片寬（＝ 真的滿版）。⚠⚠⚠ **統一套的那顆藍從截圖取樣是 `#4478b4`，等於定稿裡「已排定」那一色 `#4478b5`** —— 而定稿的紅字壓在它上面只有 **1.14:1**，所以「紅字藍底」那一種不能出。⚠ 頁面上每一個對比度都是**產生器現算**的，守門第 ⑩ 道把頁上每一個 `x.xx` 拿去和現算的對；第 ⑭ 道擋「替診所答應」（日期那一項收不收是使用者要決定的）。⚠ 上一版是 **`/preview/line-brief-0918/`**（9/18，`brief2.json` ＋ `build-brief2.mjs` ＋ `check-brief2.mjs`，十五道守門）—— **4 綁定成功的自動回覆**那一件還在那一頁，這一輪沒有碰。 ✅ **2026-09-22 加 3-13「預約醫師」**（使用者：「廠商的後控平台在約診可以呈現預約醫師　把這個欄位更新到之前已經定稿的約診完成通知與約診查詢頁面上」＋ 翔評後台「預約設定」的截圖 `ref-0922-doctor.jpg`）：兩則都在**日期底下、帶子上面**多一列「預約醫師　李柄輝」，排法照「約診狀態」那一列（標籤 `xs` `#5C5F57`、間距 9px、基線對齊），姓名 `sm` 粗體 `#2A2C27`、只填姓名；micro 可用 134px，這一列 103px。⚠⚠ 兩份 Flex 寫在 **`drafts/channels/single-card-doctor.json`**（build-brief3.mjs **產生的，不要手改**）—— 從 `single-card-band.json` 讀回來只插一列，守門第 ⑰ 道驗「拿掉那一列就和定稿逐字相同」。多一個值 `{{doctor}}`。⚠ **位置是 PM 挑的，使用者還沒挑過**；另一個候選是放帶子下面（輪播和約診狀態排成兩列）。⚠ 例子的醫師名從 index.html 的名冊驗過，不要編。 |
| **⭐⭐ 給廠商的待調整項目總整理（9/14 那一版）** | ✅ **2026-09-14 做好了**（使用者：「把這幾天的 line 頁面設計與定稿整理起來，並與廠商回傳的頁面對照，針對需調整的部分詳列清楚、規格、做法。最前面先把未定案的項目寫出來……先前的提案頁保留太多調整版本，這樣給廠商看太亂」）。頁面 **`/preview/line-brief/`**，產生器 `node drafts/channels/build-brief.mjs`，文字 **`drafts/channels/brief.json`**（抬頭「未定案」是使用者逐字寫的，不要潤飾），守門 `node drafts/channels/check-brief.mjs`（十二道；⚠ 這台 Windows 沒有 playwright，版面改用瀏覽器量過）。順序照他指定：**未定案四件 → 1 尚未綁定按約診查詢（現況／換成、規格、Flex JSON）→ 2 看診前 48 小時提醒（同檔名換圖、快取做法）→ 3 約診通知與紀錄查詢（3-1 浮水印對照 ＋ 位置規格／3-2 單張定稿・九顆原色與色碼表／3-3 輪播 micro 定稿・九顆淡墨表／3-4 約診狀態／3-5 日期／3-6 輪替規則與算好的例子）→ 4 綁定成功的自動回覆 → 5 已完成六則**。⚠⚠ **只放定稿與廠商現況，過程一律不印**（守門 ⑫）。⚠⚠ 卡片、浮水印幾何、兩則寬度全部 import 自 `build-vendor.mjs`（export 加了 `OFF／wmWidth／cardLines／MEGA／MICRO／BUB…`），按鈕從 `welcome-card.json` 照抄、標題從 line-bind-prompt 讀。⚠⚠⚠ **同一天定下「單張＝各自原色、輪播＝淡墨」**（使用者逐條寫的那一份就是這樣分）：`booked-card.json` 輪播那一則的網址改成 `wm-{{watermark}}-ink12.png`，`_浮水印` 每一顆多一欄 `url_carousel`，`publish-assets.mjs` 也認這一欄 → **九張 ink12 已放進 `assets/line/`**（30 個檔）。⚠ `/preview/line-vendor/` 與 `/preview/line-card-spec/` 的單張九顆仍畫成淡墨、沒有跟著改 —— 那兩頁現在是舊的整理，要給廠商的是這一頁。⚠ 3-2／3-3 那九張卡的日期是**照 (月＋日)%9 反算出來會落到那一顆的看診日**（只取週一到週五），不是塞進去的 |
| **⭐ 廠商對接的現況與待答** | ✅ **2026-09-11 做好了**（使用者：「增進新角色　把這些資料都整理好」）＝ **TEAM.md 第 12 號角色**（資深廠商與系統對接經理）的第一份交付物。頁面 **`/preview/line-vendor/`**，資料 **`drafts/channels/vendor-log.json`**（唯一出處），產生器 `node drafts/channels/build-vendor.mjs`，守門 `node drafts/channels/check-vendor.mjs`（負向測過）。⚠⚠⚠ **2026-09-13 起這一頁也是給廠商看的**（使用者：「現在頁面有太多說明　與內部說明的敘述　要整理成給廠商看的頁面　我們已經定案　了解的文字說明都不需要」）—— 所以原本那句「這一頁是給我們自己看的」**已經不成立**，但 **`/preview/line-spec/` 仍然沒有合併**，分工換了一刀：**line-spec ＝ 那七則的文字、圖檔網址與 Flex 規格**（做好的東西），**line-vendor ＝ 現況、我們請對方改什麼、還沒有答案的題目**（要談的事）。六節：① 在跑的十二則 ② 09-10 的改版 ③ 浮水印 ④ 約診狀態的定案 ⑤ 綁定完成 ⑥ 待答。⚠⚠⚠ **砍掉的六塊（往返、回覆逐句、別家帳號、綁定．三種現象、④ 走到這裡的過程、待答的後台／診所／素材三組）一個欄位都沒有從 JSON 裡刪掉** —— 砍的是排版，**守門第 ⑭ 道因此有兩半**：頁面上不可以出現那六塊（接回去不會讓任何一道版面守門翻臉），**而且 JSON 裡不可以少**（落選理由與推導不要跟著畫面一起消失）。三種負向測過。⚠⚠ **已經定案的那幾條只留「照這樣做」** —— 通則：**一條規格要不要附理由，判準是「不附的話對方會做錯嗎」**（會就留一句，例如藥丸要寫成 horizontal box ＋ `flex: 0` ＋ `filler`）。⚠⚠ **口氣要跟著讀者換**：「等我們回覆」「再回信」「那是使用者的決定」不是解說、是**內部的姿態**，掃字掃得到（`使用者`／`我們自己`／`回信`），掃完是 0。⚠⚠ **節號換成半形 1~6，圈號 ①~⑫ 留給那十二則**（那組編號是雙方共用的詞，一個都不換）——跟著把頁內互指掃一遍（第 50-22、51-2 節記過兩次）。量到 HTML 89.9 → **45.5KB**、430px 整頁高 26,179 → **14,979px**、頁上的字 12,393 → **5,622 字**。推導在 README 第五十二節。⚠⚠⚠ **守門第 ② 道是這一頁特有的、也是最有價值的一道：頁面上不可以出現任何一則訊息的文字** —— 那些字的出處是六份 Flex JSON ＋ `auto-reply.txt` 且各自有守門，抄進來就是第八個真相；那一道**從那些檔裡現抽長句**（≥10 個字）去掃，不是寫一份禁語表。⚠ 以下是 2026-09-11 那一版的七節（**節號與內容 09-13 已經換掉，見上一句**，留著是為了看得懂當初為什麼是這樣分）：**① 這個帳號會自動送出哪幾則**（12 則・誰送・是不是公版・改一個字找誰 —— ⚠ 這張表是我們從截圖拼出來的，那份清單要了四次沒拿到，**其中四則是從截圖裡冒出來的**）／**② 廠商 09-10 的改版**（六件對上、四件還沒對上，配我們送過去的規格圖 ＋ **廠商送來的三張畫面**，見第十一之七節第 13 項）／**③ 浮水印那九顆**／**④ 四封往返**／**⑤ 對方的回覆涵蓋到哪裡**（七句，⚠ 通則：**「做不到」一律先改寫成「誰做不到」**）／**⑥ 別家七個帳號**（最用得上的兩張寫完整 —— 品御與玉山，其餘五張各一行）／**⑦ 還沒有答案的 40 題，照「誰能答」分四組**（廠商 26／後台按一次 6／診所自己 5／要一張截圖 3，最先要 9 題）。⚠⚠ **分組是這一頁真正的貢獻**：以前廠商才知道的、後台按一次就知道的、**診所自己才知道的**混在同一張清單裡 —— 後果是問了也沒用的題目佔著位置，而診所五分鐘能答的一直沒人去問。⚠ 零 JS、noindex，八個寬度水平溢出 0。⚠ **要改內容改 JSON 再重跑**，守門第 ① 道會重跑逐位比對。⚠⚠⚠ **2026-09-11 整頁改寫**（使用者：「文字太多了　精簡一點　也要附圖對照文字看　**不要有太情緒或是指控誰說謊　只要基於事實　客觀的敘述就好**」），三件各自獨立：**① 配圖** —— 新增 `node drafts/channels/vendor-shots.mjs`，把十則各自規格頁**已經拍好**的產出檔縮成 210px 見方的 JPEG，那一節因此從一張橫捲的表變成「一則一格、左邊擺那一則自己的圖」（⚠ **只搬不畫**，同 `oa-shots.mjs`；⚠ 原檔十張 3.3MB 而 `/preview/*` 是 `no-store`，**出 JPEG 不是 PNG** —— 同樣 210px，PNG 489KB、JPEG 114KB）；**② 九顆浮水印內嵌成 SVG** —— 形狀讀 `brand/shapes/`、寬度與顏色讀 `wm-sizes.json`、科別名讀 `index.html`，三份都是既有的出處，不抄第二份；**③ 判語整組拿掉** —— 「真不真」與 `對／只有一半對／有反例` 那組色籤換成「**說明的是**什麼／**還沒回答**什麼」，「證據力／主力／補刀」換成「最接近我們的狀況／補充」，整頁的 ⚠ 與警示色一起拿掉。⚠⚠⚠ **科別名那一步踩到一個不報錯的**：`data-spec="([a-z]+)"[^>]*>` 會先命中**樣式表那一段的 CSS 註解**（兒童牙科的名字變成「裡的時間，和這顆」，而每一道守門都過）—— 要連 `href="/topics/…"` 一起比對並要求剛好七科。**通則：從 `index.html` 抓東西，選擇器要挑「只有那一排才有」的形狀。**⚠⚠ **精簡的做法是「收起來」不是「刪掉」**：★ 的 9 題攤開、其餘 31 題收進 `<details>`（**HTML 自己就有，不必 JS**）。量到 390px 上整頁 10,915 → **10,621px**，而且多了兩節與 12 張圖。⚠ 新增守門 ⑧（每張圖找得到、`width`／`height` 對得上實檔、沒有孤兒縮圖）與 ⑨（不准出現判語、`class="tag…"`、⚠ —— **加回去不會讓任何一道版面守門翻臉**），兩道都負向測過。⚠⚠⚠ **2026-09-11 再一件：九顆改成「原色 ＋ 它真正的濃度」上下兩張**（使用者：「這裡的 logo 顏色放了原色　之前有一版是浮水印的顏色　把浮水印那一版加回來　但原色也保留」——「之前那一版」＝ `/preview/line-booked/` 擺的那批真的 PNG）。⚠ **濃度不在這一頁寫死**：出處是 `booked-card.json` 引用的檔名（`wm-…-12.png`）抽出來的，換成 `-08` 會自己跟著淡、抽到不只一種就 throw。⚠⚠ **排下去會有一個看得見的問題**：九顆的相對高度 0.28~0.50（寬度是按墨的面積正規化的、**不是等高**），虛線因此落在不同高度 ＝ 第九節第 28 條 ① 那一種「同一欄不同列對不齊」，**而且是這一改自己造成的**；修法是每一格的框固定成**最高的那一顆**的長寬比（算出來 2.008:1，**是算的不是填的**）、形狀垂直置中。⚠ 分隔線只用 `margin-top` ＋ `border-top`，**不可以加 `padding-top`**（全站 border-box，內距會從 `aspect-ratio` 的框裡吃掉高度）。⚠⚠⚠ **用 CSS 畫浮水印是這條線明文避開的做法，所以要自己證明它沒有說謊**：把頁上九張 inline SVG 與 `wm-<形狀>-12.png` 各自疊在卡片色上、縮到同寬、逐像素相減 —— **平均階差 0.023~0.088**（最大 5~10 只在抗鋸齒的邊緣）。⚠ 那個比對要在 **HTTP** 上跑，`file://` 會讓 canvas 被判成跨來源、`getImageData` 直接丟例外。⚠⚠ **規則沒有被推翻**：line-booked 是交給廠商的那一頁、只准擺真的 PNG；line-vendor 是我們自己看的，內嵌 SVG 換到的是「形狀與寬度只有一個出處」。**哪一頁給誰看，決定它可以畫還是只能擺檔案。** ⚠⚠⚠ **2026-09-11 再一件：綁定完成收成「兩個處理方向」**（使用者指定，第 ⑥ 節，推導 README 第四十三節）—— 第四十一節與第 ⑦ 節都是**證據**，這一節才是**決定**。**方向一 從源頭不要送**（對方回不能拿掉，理由 a 通知病患 ＝ 我們自己那張卡就在做、玉山也是商家自己送且不含號碼，**a 撐不住**；理由 b **櫃檯用那個號碼查病患資料** ＝ 這一輪才知道的新資訊，**要先問診所自己**）／**方向二 留著但讓兩則不一樣**（對方那一句涵蓋的是**後台**的自動回應，而這一則不必由後台回）。⚠⚠ **問法要跟著換**：對方回過「能不能拿掉」，再問一次只會拿到同一句 —— 現在問的是條件句「若櫃檯用不到，關不關得掉」。⚠⚠⚠ **兩個方向卡住的是同一件事：那句綁定成功歸誰送**，改由廠商系統在綁定當下送我們那張卡，理由 a 與方向二同時成立。⚠⚠ **別家帳號重新分成三種**（原本按「哪一家」排）：① 之後沒有再跳訊息（品御）／② 兩則不一樣（潔客幫・中信・國泰世華・玉山）／③ 什麼都沒送（BREAD）——**第三種是這一次才分出來的，它說明的正好是方向一**。**通則：同一批材料換一個問題就要重新分組**，按「哪一家」排看不出哪一個方向手上是空的；每一種都要同時寫**說明的是**與**還沒說明的**。⚠ 附圖 `reply-0910.jpg`（750×1080）——**裁切下緣要量過再切**，第一版切在 y 2050 正好把時間戳切成一半，畫面上只是右下角多一塊糊掉的灰而**每一道守門都會過**。⚠ 它和「別家帳號的畫面不進 repo」不衝突：那條管的是**別人家的畫面**。⚠⚠ 守門多一道 **⑩ 頁面上不可以出現 `undefined`**（欄位名打錯時範本會把它原字印出來，每一道版面守門都會過）＋ 兩個方向與三種現象的欄位要齊全，兩種都負向測過。⚠⚠ **2026-09-12：十二則的狀態逐則更新**（①②⑨⑩⑫ 已完成／⑪ 診所自行製作／⑧ 定稿・等送／**⑤ 補上兩張圖**）—— ③④⑥⑦ 一個字都沒動，那四則正是還卡在廠商那一側的。⚠ **⑩ 改成「已完成」但「先不送」那一句留著**：**做完了和可以送了是兩個欄位**。⚠ `圖` 現在吃「字串或一組」，兩張**直向疊**（那一欄只有 64px，並排會把字擠到剩 80 幾 px）。⚠⚠ 那兩張由 `node drafts/channels/bind-prompt-png.mjs` 產（line-bind-prompt 第一次有自己的產出檔）——**第三十九節那條「只有 index.html」擋的是「複製別人的圖進來」，不是自己畫的**，守門改成一張寫死的允許清單、多一個檔照樣 throw。⚠⚠⚠ **2026-09-12 稍晚：⑧ 那個「圖片 bug」查清楚了 —— 頭圖裡被媽媽抱著的幼兒多出第三隻手，而且 `db1825f`（09-10，另一條對話）早就修好了**，所以狀態從「圖片待抽換」改成「定稿・等送」、**組別沒有動**（廠商還沒換上新版）。**通則：口述轉來的狀態要先當成「可能已經過期」查一次** ——只要出現「還沒／待／要抽換」這一類指向別人手上的進度的詞，先 `git log --all --grep` 一次。見第十一之七節第 12 項與 README 第四十六節。⚠⚠⚠ **2026-09-13 稍晚：抬頭那一塊從「現在等你們的四件」換成「未定案」四件**（使用者把四件連同子項寫出來，再補一句「上面寫的順序 1 包括 1-1 到 1-5 移到 3，我寫的 2 3 移到 1 2」）：**1 尚未綁定按約診查詢（抽換藍色圖卡）／2 看診前 48 小時提醒（原圖有 bug，抽換新圖）／3 約診通知與紀錄查詢版面（3-1 浮水印的大小與位置・3-2 約診狀態四個值・3-3 浮水印改淡墨素色・3-4 日期格式照廠商那一版・3-5 九顆怎麼輪的規則）／4 綁定成功的自動回覆設定（4-1~4-4）**。⚠⚠⚠ **這一輪真正的工作不在抬頭那一塊，在「他順手寫在括號裡的四句話同時把別處變成假的」** ——「這個只是設計資料，沒關係」「不需要特別問廠商怎麼安排」「拿掉，不需要再深入」各自收掉一件事，所以第 2 節「還沒對上的四件」變成**還開著兩件 ＋ 看過之後收掉兩件**（收掉的**不搬進「對上的六件」** —— 那六件是「照規格改了」，這兩件是「不必改」，混在一起日後看不出哪幾件真的動過），第 6 節待答的廠商那一組 29 → **25 題**（★9 → 8）。**通則：使用者在括號裡順手講的每一句都要回頭問「這一句讓哪一段變成假的」** —— 括號裡的話最容易被當成語氣而不是指令。⚠⚠ **收掉的四題連同理由收進 `待答.已決`，沒有刪**（守門 ⑮ 在盯）。⚠⚠⚠ **這一改暴露一個本來就在的缺口：待答那一節根本沒有號碼** —— 別處寫著「第 6 節的第 1 題」「第 6 節的第 11 題」，而那一組是 `<ul>`、每一題前面只有 ★ 或 ・，**那兩句話從一開始就指不到任何東西**，收掉四題之後還會整組位移。兩件一起治：那一組改成 `<ol>`、每一題自己帶 `value`（★ 那幾題被抽到前面，不帶會從 1 重數）；**號碼不要寫死** —— 資料裡寫 `{{題:關鍵字}}`，產生器載完 JSON 現算，找不到或找到不只一題就 throw。**通則：一個「第 N 個」的指路，要嘛不寫，要嘛讓產生器現算** ——寫死的那個數字不會壞、不報錯、畫面完全正常，它只是有一天開始指到別題去。⚠⚠ **第 3 節多一排「九顆各自畫在卡片上該有的樣子」**（使用者：「9 個 logo 的效果都要放上去，給廠商看到，目前只有針對廠商放的兩個做比對，另外七個也要先做好顯示上去」）——同一支 `plate()`、同一個框、同一個比例尺；`clipPath` 的 id 改成流水號（原本從「標的字數 ＋ size ＋ right」拼，兩張撞在一起時會去吃別人的 clipPath **而畫面正常**）。⚠⚠⚠ **浮水印的顏色定案成淡墨素色**（柔墨 `#5c5f57`、12%，形狀寬度位置一個數字沒動），所以九宮格**虛線底下那一排**與那九張卡都畫成淡墨 —— **但上面那一排仍然印原色，那不是漏改**：第 4 節定案的「色票」那一條寫著「＝第 3 節那九顆的同一份色」，拿掉原色那一句就沒有出處了。**通則：一個東西同時是兩件事的出處時，換掉其中一件不可以順手把它刪掉。**⚠ 順手修掉兩個不報錯的：`b()` 補上 `` ` `` → `<code>`（這條線每份資料都用反引號標欄位名與色碼，不轉的話交出去那一頁印的是反引號本身）、**資料裡不可以放 HTML**（`b()` 會逐字跳脫，畫面上出現一整串 `&lt;a href…`，改成多一個 `連` 欄位由範本組）。⚠⚠ **新加的 CSS 註解在樣板字串裡 ＝ 會印進頁面**，裡面寫了警示記號被守門 ⑨ 當場擋下，改寫那句說明時**又在說明裡寫了同一個記號**、第二次又被擋（這條線第六次「掃字撞到自己的說明」）。守門加第 ⑮ 道（未定案四件的順序與子項 5＋4／收掉那一件沒印回來且 `待答.已決` 還在／九顆上卡真的是九張／畫的是淡墨不是原色且色碼有印出來／題號還是記號、算出來指得到），**負向測過七種**。量到 HTML 45.5 → **64.1KB**（多的幾乎全是那九張卡的路徑資料）、430px 整頁高 14,979 → **18,086px**、頁上的字 5,622 → **6,555**。推導在 README 第五十三節。⚠⚠⚠ **2026-09-13 再一件：第 1 節「待調整」那一組照抬頭那四件重排**（使用者：「後面待調整的細部內容部分　依照剛剛的文字排序　重新整理」）——印出來從 ③④⑤⑥⑦⑧ 變成 **⑤⑧⑥⑦③④**（⑤→未定案 1、⑧→2、⑥⑦→3、③④→4），每一格的名字後面掛一顆淡框的小標「未定案 N」，讀的人才連得回抬頭那一塊。⚠⚠⚠ **①~⑫ 那組編號一個都不換**（同 09-12 分兩組那一輪：那是雙方共用的詞），**細部那幾節也一節都沒有搬** —— §2→§3→§4 本來就是未定案 3、§5 是未定案 4，未定案 1 與 2 沒有自己的一節。⚠⚠ **對照寫在資料裡不是產生器裡**：每一列的 `未定案` 存的是**那一件的標題**不是序號 —— 抬頭那四件改了名字會 throw，存序號的話插一件就整組位移**而畫面完全正常**；卡片上那個 N 是現算的。⚠⚠⚠ **順手抓到一個上一輪留下的假話**：⑥ 那張卡的註還寫著「六件對上、**四件還沒對上**」，而 09-13 早上已經收成「還開著 2 件 ＋ 收掉 2 件」——**兩個數字都對不上、每一道守門都會過、畫面完全正常**（第九節第 28 條 ② 又一次）。修法不是把數字改對，是資料裡寫 `{{改版:對上}}`／`{{改版:未對上}}`、產生器現算陣列長度（對不到一組陣列就 throw）＝ **第五十三節那條通則的第二種寫法：一個「第 N 個」或「共 N 件」的數字，要嘛不寫，要嘛讓產生器現算**。⚠ 守門新增 ⑯ 並讓 ⑮ 多守那兩個數字，**負向測過六種**（甲 排回圈號順序／乙 不印記號 ＝ ⑯ 擋；丙 漏宣告／丁 已完成也宣告／戊 宣告不存在的件 ＝ 產生器 throw、門 ① 重跑時擋；庚 把數字寫死 ＝ ⑮ 擋）——⚠ 丙丁戊 由產生器擋，⑯ 的資料那一半是第二層網。量到 HTML 64.1 → **64.5KB**、430px 整頁高 18,086 → **18,097px**、水平溢出 0。推導在 README 第五十四節。⚠⚠⚠ **2026-09-13 再一件：浮水印的位置定案「往右下溢出 18／10px」**（使用者看著那九顆問：「9 個 logo 這樣排　做出來會像我們的模擬圖嗎？因為我看你們模擬圖放了兩個版面　但我要的應該只是其中一個」）——**答案是不會**：那九顆當時畫的是 **JSON 那一版**（貼齊右下角、整顆收在卡內），而模擬圖那一版是**溢出 18／10px** 的。⚠⚠ **他問到的正是 42-7 那個「我們自己送過去的兩份互相矛盾」** —— 那一節早就記著「回信之前要先解決」，但一直沒有人做決定，是他從那張「同一張卡畫三次」的對照圖一眼看出來的（**前兩格都是我們自己的**）。定案走**溢出**：看得到 74.7~76.1%，⚠ **牙洞逐顆驗過**（那是這顆標誌唯一的識別特徵，第三十八節）——六顆量過洞位置的全部沒被切到，最緊的 r1c1 離下緣 10px；`r1c3`／`r2c3`／`r3c3` 的洞位置**從來沒有量過**（`HOLE` 表只有六筆）。⚠⚠⚠ **但 Flex 寫不出溢出**（`offsetEnd`／`offsetBottom` 不吃負值），所以 `booked-card.json` 那兩行**仍然是 `0px`、那不是規格是「寫不出來的那一半」** ——而**廠商 09-10 那一版已經做出溢出了**（8~10／6~7px），所以要求寫成「**照你們的做法，把溢出量統一成 18／10px**」＝ 問他們自己手上就有的做法（同 30-2 那條「對方說做不到就去他的成品上找反例」的正向版）。⚠ **沒有自己發明一種 Flex 寫法** —— 這條線的規矩是「那一份 JSON 等廠商答完再寫，現在寫等於寫兩份、其中一份會丟掉」。⚠⚠ 頁面三件：**對照從三格收成兩格**（「我們的 JSON」那一格整個拿掉，小標的「三次」改成**從資料現算**＝第五十三、五十四節那條通則第三次）、**九顆上卡的偏移量從對照表「我們要的」那一格讀回來**（不在產生器裡另寫一組 —— 兩處不一樣的話同一頁會同時出現兩種位置**而版面完全正常**）、第 3 節開頭多一塊「位置與大小・定案」（**決定在前、比對在後**）。⚠ **`preview/line-booked/` 一個像素都沒動**（那一頁的模擬圖本來就是溢出那一版，他挑的正是它）。⚠⚠ 守門 ⑮ 多四件並**負向測過四種**（九顆排回 0／0／「我們的 JSON」加回去／「我們要的」改回收在卡內 ＝ 產生器 throw／定案少印兩條）；⚠ **第一次跑被守門 ⑨ 擋下來** —— 定案那幾條我寫了警示記號，而這一頁 09-11 起是給廠商看的、整頁不准出現它（**口氣要跟著讀者換**，寫規格時很容易順手帶進內部語氣）。量到 HTML 64.5 → **62.2KB**。推導在 README 第五十五節。⚠⚠⚠ **2026-09-13 再一件：那兩處都改成「畫在真的卡片上」，日期照廠商那一版**（使用者：「把約診狀態和 9 個 logo 依照廠商的日期格式都做一次頁面放進來」）——改之前**第 3 節那九顆是一張空白的卡、第 4 節是一個小框裡四列藥丸**，而日期格式當天早上才定案（照廠商 09-10 那一版，收進 `待答.已決`），**這一頁上沒有任何一個地方畫得出那個寫法**。現在兩處都是真的那張卡：〔病人姓名〕／日期／（第 4 節多一列）`約診狀態` ＋ 藥丸／浮水印。⚠ 日期的唯一出處是 `vendor-log.json` 的 `日期`，產生器開頭就驗寫法，**裡面不可以再寫死一個日期**（守門第 ⑰ 道在掃 —— 寫死的話換一個例子只會換掉其中一半，**同一頁上會出現兩種日期而畫面完全正常**）；字級與間距也只有一份 `CARD`（照 Flex 的 paddingAll 14／md 16／lg 19（margin 6）／xs 13），兩處共用。⚠⚠⚠ **第一版用 `<text>` 自己畫、自己估字寬，那一道放行了但畫出來真的超出去** ——逐像素量到那一行實際 **250.1px、右緣 264.1**，而可用寬度只有 240；估出來卻是 234.7（**低估 6.6%**），成因是**這台容器裡沒有 Noto Sans TC**、退回的字型數字比較寬（＝第 50-19 節那條）。**通則：不要在產生器裡估字寬 —— 估出來的數字會跟著字型跑，而它報「放得下」的那一天，字會靜靜地畫到卡片外面、不報錯。**改成 **`<foreignObject>` 讓瀏覽器自己排**，估寬那個函式整個刪掉（**一個不準的估算比沒有估算還糟**），`.cb` 那組 CSS 兩處共用。⚠ 守門第 ⑮ 道抓「九顆上卡」的正規式要跟著換 —— `foreignObject` 裡有 `</div>`，原本那條會被切在第一張卡上（**通則：在一塊「用 `</div>` 找邊界」的區域裡塞進 HTML，那條邊界就壞了**）。⚠ 第 4 節**四張卡各掛一個值**（同 line-booked 第 ②之二 節：一張卡看不出四種的相對份量），卡寬 207、**浮水印的偏移量從第 3 節的對照表讀回來**（不另寫一組），**哪一顆是算出來的**（`(9+11)%9=2` → `r1c3`，＝請廠商照著填的那條規則，順手示範一次）。⚠⚠ **日期在 207 的卡上折成兩行，那不是破圖，正好是「那一格不要限制行數」的樣子** ——這一頁因此第一次把 `maxLines: 1` 會出什麼事畫出來了。量到卡片 207×142.3、浮水印 188×61・看得到 91%、八個寬度溢出 0（含 320）。⚠ 藥丸的行高與上下內距一個字都沒動（守門 ⑬ 仍在比對）。守門新增 ⑰ 並**負向測過五種**（換日期寫法／產生器寫死／不畫那兩行／改回 `<text>`／少畫一張卡）。量到 HTML 62.2 → **70.1KB**。推導在 README 第五十六節。⚠⚠⚠ **2026-09-13 再一件：那張卡底下一大塊空白，以及星期幾被折走**（使用者：「預約成功通知　對話框裡文字下有一個很大的空白　看起來會覺得是文字寫好，再另外塞 logo 浮水印，但空間不夠所以往下拉對話框……我們一開始是把 logo 當浮水印，這樣就不會有很大的空白」「約診狀態的頁面，星期幾不要和前面的日期斷開」）。**兩件各自獨立。**⚠⚠⚠ **① 空白不是卡片被撐開，是少畫了兩行** —— 那張卡 268×**149**，而 149 是照廠商成品量的，量的是**四行**（姓名／日期／兩行灰色小字）；上一輪只畫了姓名與日期，底下就留了 **67.8px** 的空，浮水印落在那塊空裡，讀起來正是他講的那句。**通則：一個框的尺寸是照「完整的內容」量來的時候，畫進去的內容就不可以是節錄** —— 少畫的每一行都會變成一塊沒有人解釋得了的空白，而讀的人一定會替它找一個解釋、多半怪到那塊空白裡的東西頭上。**那個數字是量誰量出來的，就把誰畫完。**修法是**四行從 `booked-card.json` 讀出來**（`cardLines()`：字級照 Flex 的固定階、顏色、粗細、`margin` 全部從那份 JSON 的 text 節點讀，寫在每一行自己的 style 上；**CSS 裡只留 padding 與行高**，寫進 CSS 就變成第二份規格），不是在這一頁打字。⚠ 第 4 節那四張吃的是**另一則**（`約診紀錄查詢` 的 micro bubble，兩行），同一支 `cardLines()` 讀，**不要共用同一組字**。量到內容高 **145.28／149**，十三張逐格相同。⚠⚠ **這是這一頁唯一一處會印出訊息文字的地方，所以第 ② 道要開一個例外**（掃之前把 `<foreignObject>…</foreignObject>` 剝掉）——**例外不可以變成洞**：剝掉的那幾塊要真的存在（一塊都沒有就 throw，不然整段拿掉等於把 ② 關掉），而且 ⑰ 逐行比對卡上的字和 JSON 一不一樣。**單一出處沒有被破壞，只是多一個地方把它畫出來。**⚠⚠⚠ **② 星期幾被折走 —— 把它移到日期後面是不夠的**：輪播那張卡 207px、可用 179，而日期是 lg 19 粗體、**本來就放不下一行**，一定會折。廠商那一版折成「2026/09/11 11:50 ／ **星期五**」；只把星期幾移到前面之後，實測折在**「星期 ／ 五」中間**，那三個字自己被拆開、比原本還糟。成因是中日韓文字**每一個字之間都是合法的折點**，所以「哪兩個字會被拆開」完全由那一行有多寬決定，而寬度跟著字型跑（這台容器沒有 Noto Sans TC，量到 183.3px，比 LINE 寬約一成）。**通則：要兩樣東西不被拆開，不可以靠「把它們排在一起」** —— 只要那一行會折，折點就是寬度與字型決定的，不是順序決定的；**要它一定不拆就自己斷行**。定案的要求是一句話兩件事：**星期幾移到日期後面、時間放到第二行**（那一格填成「2026/09/11 星期五」＋換行＋「11:50」）——四樣東西一個都沒換，換的只有排法與一個換行符，`wrap: true`／不要 `maxLines` 那一句仍然要。⚠ 頁面上那一行要 **`white-space: pre`**（交給瀏覽器再折一次就會折在「星期／五」中間）；量到 183.3 ／ 60.5px。⚠ 兩種寫法都留在資料裡（`日期.廠商` 是他們送來的、`日期.例` 是我們要的），只留一份的話「有沒有東西要調」就講不清楚了，守門會比對兩者不可以相同。⚠ 守門第 ⑰ 道整段重寫並**負向測過八種**（日期改回廠商那一版／斷行換成空格 ＝ 產生器 throw；拿掉 `white-space:pre`／第 3 節只畫兩行／第 4 節少畫一行／卡上的字自己打一份 ＝ ⑰；把訊息文字抄進 JSON／`foreignObject` 全部不見 ＝ ②）。⚠ 新寫的 CSS 註解又寫在**樣板字串裡面**、警示記號被印進 HTML，當場被第 ⑨ 道擋下來（第七次）。量到 HTML 70.1 → **74.9KB**。推導在 README 第五十七節。⚠⚠⚠ **2026-09-13 最後一輪：日期收成一行**（使用者：「約診完成通知　和約診狀態頁面　時間不要斷行」）——上一輪那一版把時間放到第二行，正是他現在要治的；**「時間不斷行」＋「星期幾跟著它的日期」合起來只剩一條路：整串要放得下一行**。⚠⚠ 輪播那張卡可用只有 **179px**、日期又是全卡最大的字（lg 19 粗體），所以這是量出來的不是排法問題（這台容器／他手機上量到的）：`2026/09/11 星期五 11:50` **250／229** 兩種卡都折・`2026/09/11 (五) 11:50` **230／210** 單張一行、輪播折・`9月11日 (五) 11:50` **188／172**（只剩 5px 餘裕）・**`09/11 (五) 11:50` 170／155 ＝ 兩種卡都一行，定案**。⚠⚠⚠ **中文月日落選的理由不是「餘裕小」，是它不是固定寬**：10月／11月／12月多一個數字（約 +10px），那三個月在輪播上就會折 ——**一年有四分之一的時間，時間會掉到第二行，而我們挑的例子是 9 月、看不出來**。**通則：一把「剛剛好放得下」的尺，要先問那個字串會不會變長 —— 日期、姓名、金額這類由資料填進來的欄位，永遠拿最長的那一種來量。**⚠⚠ 這一改推翻了 2026-09-05 的「月日改中文」，所以**三處一起換**（`vendor-log.json` 的「日期」／`booked-card.json` 的說明／`preview/line-booked/` 的 `DATE` 表多一格 `nu`、`dt` 預設改成它），**`shot-booked.png`／`shot-query.png` 那五張也要重跑**（那兩張被 line-vendor 與 line-spec 引用）；⚠ **提醒卡沒有動**（268 寬、22px，中文月日放得下，而且它不是輪播）。⚠⚠⚠ **跟著長出來的一件：卡片自己要矮 28px** —— 日期少一行之後那四行只剩 120.9px，而框還寫著 149（照廠商成品量的），底下又空一塊 ＝ 他早上第一句話講的那件事，所以 `浮水印.對照.卡.h` 改成 **121**。**通則（第五十七節那條的反面）：框的尺寸是照內容量來的，內容少一行，框就要跟著矮。**⚠⚠ **廠商自己那兩張截圖上那一行是一行，但那不是反證** —— 那是**診所端後台**的卡（348.7／269.3px），比病人手機上的 268／207 寬；而「病人手機上那張卡到底多寬」到今天仍然沒有人量過（26-6 從 20-10 開著到現在），所以這一輪取「最窄的卡 ＋ 最寬的字型也放得下」那一種，不賭那 5px。⚠ 守門第 ⑰ 道換三件加兩件、**五種負向測過**：日期改回長的那一種／例子裡放回換行（產生器 throw）、`white-space: pre` 被寫回去（畫不下也不折、會靜靜地溢出卡片）、**日期畫成兩行**（現場量墨、照 top 分組，17 張卡每一張都只准一列）、**卡高和那四行撐出來的高度對不上 ±1.5px**（＝底下又空一塊）——後兩道量的都是**畫出來的東西**不是屬性。⚠ 順手修掉一件本來就在的：這一頁引用 line-booked 那兩張規格圖時**尺寸是寫死的**，而那兩張圖這一輪自己矮了 —— 改成現場讀 PNG 檔頭（**守門 ⑧ 當場抓到，是它先講的**）。量到日期 169.9px／最窄的卡可用 177px、卡片 268×121、HTML 74.9 → **74.6KB**。推導在 README 第五十八節。⚠⚠⚠ **同一天稍晚整段被推翻了 —— 定案是 `2026/09/11 星期五 11:50`**（使用者：「日期格式錯了　是2026/09/11 星期五 11:50」）：上面那個縮寫（拿掉年份、半形括號）**已經作廢，不要拿它「訂正」現行的寫法**；那張量測表與「中文月日不是固定寬」那條通則仍然成立。⚠⚠⚠ **這個要求沒有讓字變長，換的只有順序** —— 廠商 09-10 送來的是 `2026/09/11 11:50 星期五`，定案是把星期幾移到日期後面、時間放最後，**同樣那些字元、同樣的寬度**，所以對他們是一個順序的要求、不是換格式，而且**折不折行相對於他們現在送的那一版一點都沒有變**（那一串現在就在跑）。**通則：要求對方改字之前，先算「這個改動讓那一串變長了嗎」** —— 沒變長的話，折行是既有的狀況、不是我們引進的，講成「改了會折」等於用一個不存在的代價去談一個免費的要求。⚠ 誠實的一半：**「時間不要斷行」在 268 的單張卡上成立（約 225px／可用 240），在 207 的輪播卡上做不到**（可用 177px）—— 那裡會折成兩行，而**折點落在時間前面、星期幾仍然跟著它的日期**，一個字都沒有消失；那一格因此要保持 `wrap: true`、**不要 `maxLines`**（`maxLines: 1` 正是現在真的被截成「…15:45 星...」的成因）。⚠⚠⚠ **這一輪最值得記的是產圖那一側：這台容器沒有 Noto Sans TC 也沒有 PingFang，拉丁數字會退到寬一成的字型**，於是日期會在「LINE 上放得下」的地方折，而且**折在「星期／五」中間** —— 出圖（`shot-*.png`）與量測會一起說一個不存在的問題。解法不是改版面，是**在 `.cb`（build-vendor）與 `preview/line-booked/` 的 body 多插一個拉丁字型（Arial）當校正**：手機與電腦上前面三個中文字型會先命中、它們自己就含拉丁字，**那一側一個像素都不會變**，中日韓的字也照原本的堆疊走（量到「2026/09/11 星期五」這台 183.2 → 校正後 156.4，他手機上 167.7）。**通則：模擬別人的介面時，只要「會不會折、折在哪裡」是要判斷的東西，就得先確認這台機器的字寬和那一側對不對得上** —— 字型不一樣時折點會落在不同的地方，而畫面看起來完全正常（＝「那一側寫死的尺寸我們也要寫死」那條的字寬版）。⚠ 守門第 ⑰ 道換三件加兩件、**六種負向測過**（改回廠商的順序／放回換行 ＝ 產生器 throw；**拿掉 `.cb` 的拉丁校正**／單張卡畫成兩列／**輪播折在星期幾中間**（列數一樣是 2，只有把每一列的字收回來才分得出來）／寫回 `white-space: pre`／卡高對不上 ±1.5px）；⚠ 順手修掉一件：⑰ 掃「產生器裡有沒有寫死一個日期」會**掃到註解裡舉的例子**，掃之前要先剝註解，不剝的話它會一直誤報、誤報久了就會有人把它拿掉。三處一起換（`vendor-log.json`／`booked-card.json` 的說明／`line-booked` 的 `DATE` 表多一格 `ymdw`），五張 `shot-*.png` 與 14 張縮圖重跑。量到 HTML **75.0KB**。推導在 README 第五十九節。⚠⚠⚠ **2026-09-14：line-booked 的規格整段合併過來**（使用者：「把 line-booked 提案頁裡的資料合併回 line-vendor　1. 9 個 logo ＋ 色碼播放規則　2. 約診成功通知版面　3. 約診查詢頁面」）——**三件裡只有兩件真的缺**：預約成功那兩節本來就有，缺的是**規格與色碼**（九宮格只有形狀與科別、規則只有一句）與**整個輪播那一則**。第 3 節因此重排成 ③之一 ~ ③之五（挑哪一顆／每一顆填什麼＋表／卡片本身／預約成功／約診紀錄查詢），**六節的編號一個都沒動**（頁內與這一份加起來幾十處「第 N 節」指過來，第 50-22、51-2 節已經記過兩次），第 4 節一個字都沒改。⚠⚠⚠ **那張表不是在這裡算的，是從 `booked-card.json` 的 `_浮水印` 讀回來的** ——**那一份才是要交出去的東西**，頁面上印一份自己算的話，交出去的 JSON 和廠商照著看的表有一天會分家、而兩邊各自都很正常（＝第 62、68-7 節那條的第三種：同一件事有兩份時，先問哪一份是要交出去的那一份）；讀進來不等於信它 —— 每一列的兩欄寬度對 `wmWidth()`、色碼與長寬比對 `wm-sizes.json`，對不上就 throw。⚠⚠⚠ **輪播那 11 張模擬圖畫的是廠商現在跑的 207px 不是規格的 162px**（這樣它和第 4 節那四張、和廠商送來的截圖才比得起來），**而且要把這件事寫在那一節開頭** —— 連同「換成 `micro` 之後日期那一行放不下、要在那一格的值裡帶一個換行」（第 61-1 節定案）。**沒有自己把這一頁改成 162**：09-14 那一列就記著那是他的決定，真的改過去日期會折在「星期／五」中間（他 09-13 退回過的樣子）。⚠⚠ **九宮格底下那個 px 變成第三個數字了**（原本寫 `r3c3・215px`，而新表上同一顆是 186／142）——改成「原寬 215px」並寫明那是基準不是要填的值。**通則：補一張新的表之前，先掃一次那一節原本有沒有印過同一個量的別的數字。**⚠ 規格裡每一個數字寫成 `{{值:…}}` 現算，**要排成第二道 walk**（頭上那一道跑在檔案開頭，`WM`／`BUB`／`OFF` 都還沒算出來）；`BUB`（兩則的卡片寬與 `paddingAll`）**照 line-booked 的算式再算一次、不抄 162**。⚠ 守門 ② 收窄一格：**整行就是一條網址**不算「訊息的文字」（`auto-reply.txt` 裡有一行就是 `https://fangren.net`，而這一節本來就要印圖檔的網址）——網址後面接著任何一個字都照掃，負向測過。⚠ 新增守門 **⑱**（規格 17 條都印得出來、那張表九列逐格 ＝`_浮水印`、產生器真的在讀那一份、算式與 `mega`／`micro`／`wrap` 那幾句都在），⑰ 加上那 11 張輪播卡（張數、兩行字逐字、浮水印是淡墨且往右下溢出、**寬度要照輪播那一欄**、卡上不可以有約診狀態那一列）；⚠ 那幾張用 `carcard` 多一個 class —— **守門是靠 class 分辨兩節的卡的**。⚠ ⑱ 比對頁面要在加粗、`code` 與 `{{值:…}}` 三種邊界上切開再逐段找（`b()` 會在字中間插標記，整條去 `includes` 會全部誤報）。**八種負向測過。**⚠ 順手改掉頁面上還對著廠商講「他們」「回信要寫成問句」那兩處（這一頁是給他們看的），內容一個字沒變；剩下的「他們」三處指的是新竹品御那個帳號，是對的。⚠⚠ **沒有加「掃內部語氣」的守門** —— `我們自己`／`他們` 在這一頁有合法用法，**一道會誤報的守門遲早會被拿掉**。推導在 README 第六十九節。 | TEAM.md 第 12、13 節、README 第 42-6、42-9、四十三、四十四、四十六、六十九節 |
| **⭐ 只留定案與現況對照的那一頁** | ✅ **2026-09-14 做好了**（使用者：「現在這個提案頁裡有很多我們內部反覆修改的版本。給廠商看的只要**最後定稿和現況對照**就好。把需要的內容抽出來　**另外做成新的提案頁**」）。頁面 **`/preview/line-card-spec/`**（約診卡　定案規格與現況對照），產生器 `node drafts/channels/build-card-spec.mjs`，守門 `node drafts/channels/check-card-spec.mjs`（八種負向測過）。四節：**1 現況對照**（我們送過去的規格圖／廠商 09-10 送來的三張畫面／對上的六件／還開著的兩件／看過收掉的兩件）**2 浮水印**（挑哪一顆・九列的表・位置與大小定案・九顆的形狀與色碼）**3 卡片**（`mega`／`micro`／內距／底色／哪一格不要限制行數・約診狀態那一列）**4 照這個規格畫出來的樣子**（兩則各「同一張卡畫兩次」＋「九顆各自畫在卡片上」）。⚠⚠ **刻意沒有的**（守門第 ⑪ 道在盯，接回去不會讓任何一道版面守門翻臉）：在跑的十二則、綁定完成那兩個方向、還沒有答案的 25 題、抬頭那塊未定案四件、以及**每一件怎麼走到定案的過程** —— 那些留在 `/preview/line-vendor/`。⚠ **兩頁都沒有互相連結**，各自只連回 `/preview/line-spec/` —— 給誰哪一個網址是人決定的。⚠⚠⚠ **這一頁不新寫任何東西**：卡片、浮水印、那張表、兩則的對照全部從 `build-vendor.mjs` **import** 過來，所以那一支的寫檔關在「直接執行」底下（不關的話，產這一頁會順手把 line-vendor 也重寫一次），末尾 `export` 放出十幾塊，`revised`／`settled` 從常數改成**函式**。⚠ 驗收：重跑 `build-vendor.mjs`，`preview/line-vendor/index.html` **逐位元組相同**。⚠⚠⚠ **「見第 N 節」那個號碼是這一輪最隱蔽的坑 —— 同一段字同時活在兩套編號裡**（第 50-22、51-2、54 節那條通則的第三次現場；前兩次是「插一節之後號碼位移」）。做法照 `{{題:…}}`：資料裡寫 **`{{節:關鍵字}}`**、`esc()`／`b()` 現算，`節表` 一頁一份、`設節表()` 切換，`vendor-log.json` 裡 20 處「第 N 節」全部換掉。⚠⚠ **光看數字抓不到**（新頁自己也有第 3 節、第 4 節，一個指向浮水印的「第 3 節」搬過來仍然是一個存在的節、只是講的是別的東西）—— **所以守門守的是資料那一側**：一個寫死的都不准有。⚠⚠⚠ **只有「呼叫 `設節表()` 之後才組出來的字串」會吃到新的一份**，在模組載入時就組好的區塊非改成函式不可 —— 改之前那兩塊會靜靜地帶著 line-vendor 的號碼印在新頁上，**而每一道守門都會過**。⚠ 關鍵字在某一頁沒有對應的節時**要 throw 不要退回空字串**：那正好是「那一段字提到這一頁沒有的東西」。這一輪撞到一次（約診狀態定案的 `_說明` 寫著「第一層要問的仍然是第 6 節的第 N 題」，而新頁沒有第 6 節）—— 改成**把那一題的內容直接寫出來**，兩頁都更清楚。⚠⚠ **圖不複製一份**：廠商那三張截圖住在 `preview/line-vendor/`，`revisedHtml()` 收一個前綴；守門第 ⑧ 道因此有兩半（每一張 `src` 都要以 `../` 開頭、**這個資料夾裡只准有 `index.html`**）。⚠ 第 ② 道那個例外（卡片上真的畫出來的那幾行）**這一頁有兩種容器**（`foreignObject` 與 `.cb`），剝掉的那兩種都要真的存在，不然這個例外就變成一個誰都能鑽的洞。⚠ 第 ⑬ 道數「對照有幾格」**不要數 `<figcaption><b>`**（九顆上卡那兩排也是這個形狀，第一版數出 24 格），改成拿資料裡那幾個「標」逐個去找。 | README 第七十節 |
| **⭐ 約診卡・單一版型（帶子壓在下緣）** | ⚠ **提案中**（2026-09-18 開，`/preview/line-single-card/`）。使用者：「我們之前和廠商提需求的**約診完成通知與約診查詢**頁面的版型，我想另外做一個**單一版型**。不要之前用的 logo 浮水印效果，**改用 line 立牌上的帶子 logo，壓在頁面的下緣**。」產生器 `node drafts/channels/single-card.mjs`，守門 `node drafts/channels/check-single-card.mjs`（七種負向測過），零 JS、noindex。推導在 `drafts/channels/README.md` **第七十五節**。⚠⚠⚠ **這一頁一個字、一條線都沒有自己畫**：卡上那幾行、字級、內距、兩則的卡片寬、現況那顆浮水印全部 import `build-vendor.mjs`；帶子的幾何 import `stand-card.mjs` 的 `帶()`／`bandSvg()`，而且**27 顆那一條每次跑都拿 `drafts/channels/band/fangren-band-27.svg` 逐位元組比對**（這一頁是「拿定案那一條來用」不是「再畫一條」）。⚠⚠ **換掉浮水印真正的重點在廠商那一側**：要填的值從「九顆各一組（兩欄寬度、長寬比、網址）」變成**一張圖 ＋ `size: full`，一個數字都不必填**；`position: absolute`（那一項到今天還沒有實機驗過）**不必用了**；圖檔 18 張 → **1 張**。⚠ **換到的代價只有一件**：顏色不再跟著那一筆約診換（浮水印是 `(月＋日)%9`），那正是要問他的一題。⚠⚠ Flex 的結構：body `paddingAll: 0` → 內容那一塊自己 `paddingAll: 14px` → 帶子 `image`／`size: full` 排在它後面；**帶子要 PNG**（Flex 的 image 不吃 SVG），所以頁面上擺的就是那五張 1024 寬的 PNG、不是 inline SVG（要判斷的正是「縮到 10px 高之後還讀不讀得出是標誌」，向量在螢幕上會比實際清楚 —— 同 og-topic-card 那一輪）。⚠⚠⚠ **踩到一個每一項數字都正常的**：出圖那一頁寫 `svg{width:1024px;height:40px}`，那一條會連**巢狀的**那 27 個 `<svg>` 一起吃到（Chromium 吃 SVG2 的 width/height 幾何屬性，**CSS 贏過 markup**）—— 每一顆被撐成整條那麼寬、內容置中之後跑到框外，**最右邊那一顆（兒牙的琥珀）直接被裁掉不見**，而尺寸、長寬比、檔案大小、墨量每一項都正常。選擇器一定要寫 **`svg.bnd`**；修好之後墨量 6.3% → **22.8%**。**通則：`svg{}`／`img{}` 這種元素選擇器在 SVG 裡面會吃到巢狀的同名元素 —— 頁面上有巢狀 SVG 時，尺寸一律綁 class。**⚠⚠ **抓到它的是顏色那一道守門，而那一道第一版也是假的**：「有幾種顏色」數不出問題（抗鋸齒量到 177 種），要數的是**那八支色各自出現了幾個像素**，而且門檻**要看 alpha > 8 不是 alpha > 250** —— PNG 存的是**非預乘**的 alpha，半透明的邊緣像素 RGB 仍然是原色；最小的那一顆（19.8px）**一個實心像素都沒有**。⚠ **卡片要 `box-sizing: content-box`**（`.stcard` 有 1px 外框，border-box 的話 268 會被吃掉 2px，帶子畫出來 10.4 而說明寫 10.5）——內容寬要真的是 **268／162**，守門量 rect 在盯。⚠⚠ **輪播那張卡（micro 162）的日期要自己斷行**：交給瀏覽器折，它折在「星期／五」中間（實測）＝ 2026-09-13 使用者退回過的樣子；`booked-card.json` 已經定案「那一格的值自己帶一個換行」，斷點是從那一行日期本人切出來的（切完要接得回原字串，產生器在驗）。⚠ **顆數 ＝ 帶子多高**（滿版）：268 的卡上 9 顆 30.8／18 顆 15.6／**27 顆 10.5px**，162 的卡上 18.6／9.4／6.3px；只給 9 的整數倍（那三條相鄰的限制在**接縫上**也要成立）。⚠⚠ **七顆套色的位置是挑過的，只有 27 顆那一份有**（9 與 18 顆一律淡墨，那不是漏上色）；另做了一格「每一顆自己的科別色」（九顆一循環、同色仍然不相鄰）。⚠ **還沒決定**：帶子多少顆、哪一種顏色、輪播要不要少一輪、以及**約診狀態那一列在單張上也會出現**（值是「已排定」）—— 那是「單一版型」帶來的一件新事，不是原本的規格。等他挑完才寫 Flex 的 JSON（同第十九節那條）。 | README 第七十五節 |
| **⭐ 整包給廠商的那一頁** | ✅ **2026-09-05 做好了**（使用者：「診所官方 line 需要的素材和頁面都處理完了　統整成給廠商的網頁給我看」）＝ 第十一之七節第 3 項那個「最後一步」。規格頁 **`/preview/line-spec/`**，守門 **`node drafts/channels/check-spec.mjs`**。⚠⚠ **它不重抄任何一則的文字** —— 七則的字各有出處（六份 Flex JSON ＋ `auto-reply.txt`）且各自有守門，再抄一份就是第八個真相。頁上只放三種東西：**事實**（什麼時候送／送給誰／**誰送**／型別／變數）、**成品**（那七頁自己拍的 13 張 PNG，不是用 CSS 再畫一次）、**出路**（連回各自的規格頁）。⚠ 「誰送」是全頁最重要的一欄：**招呼圖卡與自動回應診所後台自己就設得完**，其餘五則是廠商在送 —— 這件事以前散在各頁，攤在同一張表上才看得出「有兩則根本不必等人」。⚠⚠⚠ **這一頁真正的貢獻是把 24 題排成一個順序**（照「答不出來會擋住什麼」）：**Ａ 最急 3 題**（評價卡可能是 review gating／`maxLines:1` 把日期截掉 ＝ 唯一正在出錯的／「到底會自動送出哪幾則」＝ 盤點漏過兩次）、**Ｂ 送得出什麼 5 題**（會改變成品長相）、**Ｃ 版型與資料 9 題**、**Ｄ 會出事的 4 題**、**Ｅ 診所自己在後台按一次就有答案的 3 題**（⚠ 刻意分出來，混在廠商清單裡等於把自己按一下就知道的事外包出去）。⚠⚠ 同一輪補掉**兩個會讓整包不能用的缺口**：① `welcome-card.json` 從第一版就引用 `hero-welcome.jpg`，**而沒有任何腳本產它**，且產生器出的是 **1040×520、超過 LINE 的 1024 上限** → 新增 `node drafts/channels/welcome-hero-resize.mjs`（**不動 `line-hello/generate.mjs` 的 W／H** —— 那一支裡對話框、尾巴、驚嘆號的位置全是在 1040 座標裡量的定案值，這一支只做最後一次等比例縮小 98.4615%；⚠⚠⚠ **守門的地標不可以用亮度**，底下是實景照、暗處亮處鋪滿整張，外框會回 0~1 ＝ 永遠會通過的守門，改抓對話框那條深綠 `#2C5238`）；② **JSON 裡 21 個 `url` 全指向 `https://fangren.net/assets/line/`，而那個資料夾根本不存在** → 新增 `node drafts/channels/publish-assets.mjs`（**清單的唯一出處是那七份 JSON**，不在腳本裡再手寫一份；三道守門：引用的檔都找得到／都 ≤1024×1024／沒有孤兒檔）。實測 **21 個檔、0.78MB，已經放進 `assets/line/` 並會跟著上線**（`assets` 在 `dist.mjs` 的 ALWAYS 裡，**不必改 dist.mjs**）。⚠ 招呼卡的圖片檔同時從 `drafts/channels/welcome-card{,-chat}.png` 搬到 `preview/line-welcome/shot-welcome{,-chat}.png`（19-19 那句「日後要招呼卡也上網就把 OUT 改到 preview 底下」就是這一天）—— **`drafts/` 進不了 `_site`**，整合頁引用不到。⚠⚠ 守門踩到這條線的老坑：**紅線掃描被「兩條紅線」那一節自己引用的禁語擋下來**（「不可以出現『有問題隨時問』那一類的承諾」），修法是掃到 `id="red"` 為止 —— **通則：這條線每一份檔案都把說明寫在資料裡面，掃字一定會撞到自己的說明**（check-cancel 掃 emoji 撞到 `_說明` 的 ⚠、check-review 改壞測試改到 `_說明` 裡的引文，同一件事）。負向測過五種（改 width／改表上尺寸／改「共 N 個檔」那一句／加 `<button>`／刪一個 assets 檔）**都擋得下來**。⚠ **這一頁刻意零 JS**（守門連 `<script>` 都擋）——交給廠商的東西沒有理由需要跑腳本。量到八個寬度（430／393／390／375／360／320／834／1440）水平溢出 **0**、13 張圖都載得到且都寫了尺寸、死錨點 **0**；輪播那張 855px **自己一條 `overflow-x:auto`**，表格同理；**第一張以外一律 `loading="lazy"`**（全部約 2.4MB，而 Worker 對 `/preview/*` 設 `no-store`）。⚠ 舊的 `/preview/line-reply/` **內容已落後，整合頁刻意沒有連它**。推導在 `drafts/channels/README.md` 第二十六節。⚠⚠⚠ **2026-09-07 整頁改寫成「只留模擬圖」**（使用者：「廠商對接窗口也是**專業的 line 設計工程師**，所以**只要保留模擬圖的部分就好**，附帶標題文字或是哪一個介面的簡單說明，**目前大量的文字解說都不用**。另外之前做的**衛教懶人包圖卡、主題與科別介紹圖卡、診所資訊**都放進來。」）——拿掉的是總表、每一則的事實列、檔案清單、Flex 的硬條件、圖檔清單、24 題、兩條紅線（42.7KB → **15.6KB**）；⚠⚠ **一個字都沒有消失**，那些東西本來就各自住在那一則自己的規格頁裡，每一個標題底下都連著 ——這一頁從「整包規格」變成「**模擬圖的總覽 ＋ 入口**」。⚠⚠⚠ **只留下兩處一行的提醒是刻意的**：⑦ 評價邀約「先不要送」、⑤ 約診查詢「日期正在被截斷」——那兩件**現在就會出事**，不是解說。**通則：使用者說「不要解說」的時候，要分得出哪幾句不是解說是警報。**⚠⚠⚠ 補進來的三組卡**不在 `main` 上，而且它們的分支和 `main` 沒有共同祖先**（`git merge-base` 回空的）—— 住在 `origin/claude/line-merchant-message-input-p1w3r0` 的 **`drafts/line-oa/`**，所以是 `git checkout <分支> -- drafts/line-oa` **把檔案取過來，不能 merge**。⚠⚠ **`grep` 找不到使用者說的東西時，先 `git log --all --grep`** —— 這一輪 grep「懶人包」回空的，`git log --all --grep=懶人包` 一下就指到那條分支。⚠⚠ 取過來要先對三件：**定案的是案 D 不是案 E**（`preview-health-carousel-e.png` 上還印著 09-02 就改掉的七句，擺上去就是說謊）、**電話是作廢的 `(05)5339-369`**（08-27 已全站統一成 `05-5339369`，那張卡剛好卡在換法那一天 ——**通則：從別條線取回來的東西，要拿站上「現在」的定案逐項對一次**）、**收尾那格的篇數 11 → 13**（重跑產生器才會對）。⚠ 圖由新增的 `node drafts/channels/oa-shots.mjs` **只搬不畫**（`drafts/` 進不了 `_site`），等比例縮回它自己的 CSS px（carousel 是 DPR 2、診所資訊是 DPR 3），四張 4.75MB → **2.17MB**。⚠ `.pv-scroll img` 從寫死 855px 改成 `width:auto;max-width:none`（四張各有各的寬），**基底那條 `.pv-shot img{width:268px}` 會贏過屬性，非把 `width:auto` 寫回來不可**。⚠ 守門跟著縮：拿掉圖檔清單那一道（表不在頁上了，`publish-assets.mjs --check` 在做同一件事）、**倍率從常數 3 改成 `scaleOf(src)`**（`shot-oa-*.png` 是 1×，寫死 3 的話那四張全部誤報）、圖 13 → **18** 張。負向測過六種都擋得下來。⚠⚠⚠ **2026-09-07 稍晚：使用者「有幾則我用手機打開來看　顯示比例怪怪的　很巨大」——`.pv-scroll img{width:auto}` 把捲軸裡的圖畫成原始像素尺寸**（`shot-query` 855 → **2565**、`shot-cancel-chat` 402 → **1206**，那八則自己拍的是 3× 所以整整大三倍；**壞的不只新加的那幾張，前一天就在頁上的兩張也一起壞了**）。⚠⚠ **每一道既有的守門都過**：width/height 屬性寫得好好的、和實檔 ÷ 倍率也對得上、**水平溢出仍然是 0**（它在 `overflow-x:auto` 裡）、八個寬度全綠 ——**只有把「畫出來多寬」（`getBoundingClientRect().width`）印出來才看得到**。**通則：`<img>` 的 `width` 屬性只是預設值，任何一條 CSS 的 `width` 都贏過它 ——「屬性寫對」不等於「畫出來是那個大小」，量測要量 rect 不要量屬性。**修法：`.pv-scroll img` 只留 `max-width:none`，**捲軸裡每一張各自寫 inline `style="width:NNNpx"`**（和它的 width 屬性同一個數字）；守門新增兩道（沒寫或對不上就 throw、樣式表再出現 `width:auto` 就 throw），負向測過都擋得下來。⚠ 同一輪把那三張的**卡片從 300px 縮到 268px**（`oa-shots.mjs` 的 `CARD = 268/300`）——`flex-preview.mjs` 畫的是 mega bubble 300，而整合頁其餘十四張都是病人手機上量到的 268，不縮的話那三張大 12%、讀起來也是「特別大」。⚠⚠ **行政那兩張（預約協議／掛號與文件費用）使用者指定拿掉**，⑪ 只剩衛教那一條輪播；**`drafts/line-oa/admin-carousel-d.json` 與它的預覽都留著沒刪**（拿掉的是「這一頁要不要給廠商看」）。量到八個寬度水平溢出 0、**17 張圖全部畫在它自己宣告的尺寸上**、JS 錯 0。⚠ **同一天再一件：⑧ 颱風那一則的「純文字版」也使用者指定拿掉**（「颱風　臨時休診的現有文字版拿掉」），這一頁只留圖卡版、剩 **16 張圖**；⚠⚠ **`/preview/line-typhoon/` 那一頁的純文字版沒有動** ——那是「廠商送不出 Flex」時的退路，拿掉的是「這一頁要不要給廠商看」不是把那條路作廢（同行政那兩張）。⚠⚠⚠ **再一件：⑨ 診所資訊那三顆按鈕上畫的是「破圖圖示」**（使用者拿手機截圖回報「按鈕上的小圖案顯示怪怪的」）—— 圖釘、話筒、標誌三顆都畫成瀏覽器的破圖佔位圖示，而且是**烘進 `shot-oa-clinic.png` 裡的**。**成因是這一輪自己的指令順序**：`flex-preview.mjs` 把 JSON 裡「未來的網址」換算成本機路徑（`n.url.replace("https://fangren.net/", ROOT + "/")`），而我在修完電話之後就先出了圖，**那時 `assets/line-{pin,phone,logo}.png` 還沒從那條分支取過來**（下一步才取），Chromium 就靜靜地畫了三顆破圖。⚠⚠⚠ **每一道數字守門都通過**（長寬比、上限、非黑像素 ≥10%、尺寸對得上實檔、alt、inline 寬度、連結、錨點、八個寬度溢出 0）——**和上面那個 `width:auto` 同一類：只有把圖打開看才看得出來**。修法兩件：三個檔案已經在了所以重跑一次（`flex-preview.mjs clinic-info-flex.json` → `oa-shots.mjs`）；並在 **`flex-preview.mjs` 的 `image` 那一節加守門，找不到檔案就 throw**（原本只是退到 `handouts/` 再找一次，找不到就畫下去）。**通則：產生器引用不到的外部檔案一律 throw，不要畫下去** —— 畫下去會生出一張「尺寸全對、看起來很正常」的圖，它會通過每一道守門一路上線，最後由使用者在手機上發現。⚠ 版面一個像素都沒動（仍是 356×553）。推導在第二十八節。⚠⚠⚠ **2026-09-07 再一次：模擬圖總覽與交付包合併成這一頁**（使用者：「可以把模擬圖跟交付包結合成一份嗎」）。所以這一頁**現在整份是產生出來的**（`node drafts/channels/build-spec.mjs`），一則就是一段：**標題（時機 ＋ 完整規格連結 ＋ 廠商要的／廠商回覆）→ 說明 → 模擬圖 → 圖檔的線上網址 → 可以整段複製的文字**。⚠⚠ **留下來的網址是 `/preview/line-spec/`，不是 line-handover** —— 廠商兩封回信都照 ①~⑪ 這組編號在講，而那組編號是他在 line-spec 上看到的；`/preview/line-handover/` 改成一頁**指路**（那個網址當天給過，直接刪會 404 而且會被邊緣快取存住）。⚠ 錨點沿用 `#m1`~`#m11`（阿拉伯數字，不是圈號）。⚠⚠ **模擬圖的尺寸不寫死，現場讀 PNG 檔頭再除以它的倍率**（那八則自己拍的 3×、`shot-oa-*` 是 1×）——寫死的話哪一頁重跑過出圖腳本這裡就開始說謊，而症狀是「圖畫得比宣告的大／小」、版面不會破。⚠ 產生器與守門一起改名合併：`build-handover.mjs` → **`build-spec.mjs`**、`check-handover.mjs` **併進 `check-spec.mjs`**。⚠ 新增 ⑧ 颱風那一則（原本交付包沒有，因為廠商第一封信沒問到）。⚠⚠ 這一輪 `*/` 那個坑**連踩兩次**：註解裡寫 `**/preview/…` 與 `line-*/shot-*.png`，兩個都含著「星號＋斜線」，把區塊註解提早關掉、在十幾行外報 `ReferenceError`／`SyntaxError`（第九節第 8 條）。**路徑不要寫進區塊註解裡帶萬用字元的形式。**推導在 README 第三十二節。 |
| **⭐ 交付給廠商的那一包** | ✅ **2026-09-07 做好了**（廠商回信列了八項要的東西，使用者：「把需要的圖檔　文字　跟剛才的網頁一起彙整好」）。規格頁 **`/preview/line-handover/`**，產生器 **`node drafts/channels/build-handover.mjs`**，守門 **`node drafts/channels/check-handover.mjs`**。⚠⚠⚠ **一個字都不重打** —— 每一則的文字是從它自己那份 Flex JSON（或 `auto-reply.txt`）抽出來的，圖檔清單也是從同一份 JSON 的 `"url"` 收的；**要改文案就改 JSON 再重跑，不要改那一頁**。守門最強的一道就是「重跑一次、逐位比對」（不一樣＝有人手改了頁面，或改了 JSON 卻忘了重跑）。⚠ **和 `/preview/line-spec/` 是兩件事不要合併**：line-spec ＝ 模擬圖的總覽（使用者 09-07 指定「只留模擬圖」），line-handover ＝ 交付包（圖檔網址、可複製的文字、還沒有答案的問題）。兩頁互連。⚠⚠⚠ **廠商問「⑩ 只需要一般牙科、齒顎矯正兩項嗎」—— 他沒看錯，是那張圖騙了他**：`shot-oa-topics.png` 是**一列四格的格狀**，而整合頁把它放進 `overflow-x:auto`，**手機上看得到的只有最左邊那一欄** ＝ 第 1 格（一般牙科）與第 5 格（齒顎矯正）。caption 上那句「⚠ 這一條可以左右滑」沒有用 —— **一欄剛好填滿畫面，右邊沒有任何東西被切到**。改法：`flex-preview.mjs` 加 `--row`（一列排開 ＝ LINE 上真的橫著滑的樣子），右邊那一格一定會被切一半，**切口本身就是「還有更多」**；`oa-shots.mjs` 的寬度上限 1600 → 4000。**通則：「還有更多」要靠被切一半的東西講，不要靠一句提示。**⚠⚠⚠ 盤圖檔時查出**三件不報錯的**：① **衛教的十張頭圖全部超過 LINE 的 1024**（1124／1200／1060／1058／1056 寬）；② **三張「看大圖」的副檔名對不上**（JSON 寫 `.jpg`、實檔是 `.png`，按下去 404）；③ `handout-aligner.jpg` 是 **5538×8000、2.6MB**。→ 新增 `node drafts/channels/publish-handouts.mjs`（清單的唯一出處是 `health-carousel-d.json`；頭圖縮到 1024 寬並把**實際 W×H 寫回 JSON 的 `aspectRatio`**、三張 PNG 轉 JPEG、>2000 寬的縮到 1600），**20 個檔 6.31MB → 3.08MB，已放進 `assets/`**。⚠ 「看大圖」是 uri action、在瀏覽器裡開，**沒有 1024 的限制**，只有頭圖要縮。⚠⚠ **「按鈕中的診所 icon 原圖檔」＝ 要向量不是要那幾張 PNG** → `node drafts/channels/icon-svg.mjs` 出 `assets/line-src/{mark,shape-r2c3,pin,phone}.svg`（`currentColor`，幾何從 `brand/shapes/` 與 `index.html` 讀回來、不抄第二份）；⚠ **Flex 的 image 不吃 SVG**，送進 LINE 的仍然是 PNG，頁面上要寫清楚。⚠ 量長寬比**不可以用 `img.naturalWidth`**（瀏覽器對 SVG 進位到整數，mark 會回 2.000、真值 2.029），要從 viewBox 算。⚠ 這一輪還踩到三個老坑：**註解裡出現 `../line-` 加星號加斜線把區塊註解提早關掉**（第九節第 8 條，症狀是十行外某一行報 `Unexpected identifier`）、**模板字串的 CSS 註解裡不能有反引號**（第三次）、**`font: 15px/1.85 inherit` 是無效的簡寫**（`<pre>` 退回等寬字，**中文看不出來**，只有 LINE、日期、檔名變等寬）；版面兩件：`.pv-files .d` 不可以 `nowrap`（手機溢出 150~260px）、表格一定要給 `min-width`（不給的話外層的 `overflow-x` 永遠用不到）。量到兩頁 × 八個寬度水平溢出 0、圖全載得到、死錨 0、JS 錯 0；交付頁列 **45 個圖檔的線上網址**全部找得到。推導在第二十九節。⚠⚠⚠ **2026-09-07 稍晚它併進 `/preview/line-spec/` 了**（見上一列）——`/preview/line-handover/` 現在只剩一頁**指路**（手寫的靜態檔，不是產生的；刻意不用 `<meta http-equiv="refresh">` 自動跳，那樣使用者看不出發生什麼事、下次還是會按到舊網址）。**這一列留著是為了看得懂當初為什麼是兩頁。** |
| **總檔案** | **[`drafts/channels/README.md`](drafts/channels/README.md)** —— 後台的實際數字、管道地圖、「LINE 上該有哪幾則訊息」的全盤盤點與評分、五個外部參考（早安美芝城／天仁茗茶／星宇航空／HERMES／國泰世華・玉山）、提醒卡改版、滿意度調查卡的風險。**開工前先讀它的第十四、十六節** |
| **自動回應** | ✅ **2026-09-05 定稿**（使用者：「Ok 就這樣定稿 存下來喔」）：**純文字 ＋ 一則通用（後台的「一律回應」）＋ 改約那一段跟已定稿的「綁定完成」逐字一致**。⚠⚠ **成品是一段純文字、不是 Flex JSON**（四則裡唯一）：`drafts/channels/auto-reply.txt` 是**唯一的出處**，守門 `node drafts/channels/check-auto-reply.mjs` 拿它去對規格頁的**兩個地方**（「要貼進後台的字」那一塊、聊天室裡真的送出的那一則），**三邊逐字**才算數。規格頁 `/preview/line-auto-reply/`（**切換條已拿掉**，五把尺收成寫死的預設值，⚠ **網址參數仍然吃得到**：`?n=now` 現況／`?c=card` 圖卡／`?g=rand` 三張隨機），圖三張 `node drafts/channels/auto-reply-png.mjs`（加 `--chat`／`--old`）。⚠⚠ **定案的理由不在版面**：這一則留在後台，**字就留在診所自己手上**；交廠商才有 Flex 圖卡與三張隨機，但**改一個錯字都要找廠商**。⚠⚠⚠ **這一輪修掉一件「量測壞掉但畫面正常」**：泡泡的上限本來寫成 `min(268px,74%)`，393 上只給 **252**、360 上只有 **228**，而 **LINE 的泡泡不隨螢幕縮放** —— 那幾個寬度上量到的折行與孤字全部偏鬆（上一輪那個「孤字 1 處」的誤報就是這樣來的）。修法照 `line-remind` 那一頁：**窄螢幕讓手機框貼齊頁面兩邊**（`@media (max-width:429px)` 拿掉左右框線與圓角、`margin-inline:-14px`）、**聊天室內距 11→8**、上限寫死 **268**，面板現場算「這個螢幕給得起幾 px」、給不到就標紅並寫出偏鬆幾 px（430／393／390／375／360 **都給得到 268**，**320 只給 231、偏鬆 37px**）。**通則（第三次記）：模擬別人的介面時，凡是那一側寫死的尺寸，我們這邊也要寫死** —— 寫成百分比不報錯、畫面也正常，只有拿數字量才看得出來。⚠⚠ **和 09-02 那一版比，少了一句「各類事情請在開診時間打電話給診所」** —— 所以**電話現在只跟著「改約或取消」出現**。**要不要另外補一句涵蓋其他事情，是還沒問過使用者的一題**，規格頁上寫著，**沒有自己加回去**。⚠ **「兩則的改約寫法不一樣」跟著解掉了**（守門有一道在比對 `bind-done-card.json`，**改一邊會被擋下來**）；⚠ 但**招呼圖卡的「看診前2天」與提醒卡的「48 小時」仍是兩種講法**，那是另一件、還開著。⚠ 「一則底下能掛幾個對話框」（3 或 5 兩種說法）**定稿只用一個對話框，所以現在不影響我們**。量測 40 組（五個寬度 × 八種組合）：溢出 **0**・孤字 **0**・JS 錯 **0**、定稿那一則最寬的一行 **204px**、一屏 **34~40%**（430→360），關鍵字命中那一格 **17~20%** ＝ 仍然是一半。以下是走到定稿的過程 —— 提案頁 2026-09-04 開（四把尺：情境／做幾則／放什麼／改約那段），舊的多情境模擬頁 `/preview/line-reply/` 還在（**內容已經落後**：綁定完成與提醒卡都各自定稿過了，那一頁上的是舊版；要不要收掉還沒問使用者）。⚠⚠ **後台的能力這一輪查清楚了三件（第 25-1 節）**：① 一則自動回應底下可以掛**好幾個對話框**、每一個各自選類型，所以**文字和圖可以交替**，不是一則文字配一張圖（⚠ 上限查到 3 或 5 兩種說法，**要在後台按到不能再加為止**）；② 「做幾個版本」有三種，**關鍵字分流**與**時段分流**是準的，**好幾則都不設關鍵字的話 LINE 會隨機挑一則送**（❌ 不要做，同一件事會給兩種答案）；③ ⚠ 這裡的「圖卡」**只能是圖文訊息**（一張圖切格子、字烘在圖裡）—— ⚠⚠⚠ **2026-09-05 更正：我先前寫的「Flex 後台送不出去」是錯的講法**（第 25-11 節）。**LINE 平台完全送得出**（Flex 是 Messaging API 的型別，回覆類不計費），限制在**後台那個編輯介面**的型別下拉沒有 Flex；**這間診所的廠商顯然送得出**（綁定完成／提醒卡／預約成功／評價卡全都是 Flex）。**通則：「做不到」要講清楚是誰做不到** —— 寫成「平台不支援」是死路，寫成「後台那個表單沒有這個欄位」只是「換一個人送」，這一次差點把一條可行的路關掉。⚠⚠⚠ 查更正時挖到更要緊的一件：**後台的「自動回應訊息」開著時會把病人打的字攔下來自己回、不轉給廠商的 webhook**（postback 一律走 webhook，所以按鈕那條沒事、打字那條被攔）——**二手資料，廠商一句話能確認**。**所以真正的決策點是「這一則歸誰」不是「送不送得出」**：留後台＝只有圖文訊息但**診所自己改得了字**；交廠商＝有 Flex、隨機也容易，但**改一個錯字都要找廠商**。✅ **2026-09-05 使用者定案：走純文字**（「那用文字就好了」），理由就是最後那一件 —— **字要留在自己手上**；⚠ **三張隨機因此一併擱下、不是作廢**（純文字沒有圖可以換，哪天交給廠商會一起回來）。⚠ **要問廠商的那一題要換問法**：原本寫「關鍵字回應送不送得出 Flex」**答案一定是後台不行、等於白問**，改成「**打字那一則可以改由你們的系統回 Flex 嗎（後台自動回應要關掉）／能不能三張隨機／日後改字的流程是什麼**」。⚠⚠ **但做得到不等於該做**：這一則**每打一句就送一次**，390 上純文字佔一屏 38%、配圖 65%，而**他再傳一次**就是 75% vs **131%（整個畫面被洗掉）** —— **建議維持純文字**，要用圖就掛在關鍵字上。⚠ 「改約那一段」另開一把尺：09-02 的參和 **09-04 定稿的綁定完成**（使用者自己寫的「最晚…、於診所開診時段來電變更」）**講法不一樣**，要不要統一是他的決定，所以做成尺不直接改。⚠ 分時段那條路的隱藏成本：**時段只認星期＋時間、分不出國定假日**，所以開診那一則只能寫「這個時段診所有開診」，不能寫「現在有人在」。⚠⚠⚠ **2026-09-05 使用者定方向：要圖卡，而且要三張隨機**（原話：「這樣雖然是讓患者在 line 上得不到回應，但至少有有趣的圖片」）——**25-2 那個「建議純文字」他看過數字之後仍然選圖，那是決定，不要再拿洗版的數字回頭勸。**⚠⚠⚠ **同一輪要更正我自己上一輪的一句話**：25-1 把「好幾則不設關鍵字→隨機」列成 ❌ 不要做，理由是「同一件事會給兩種答案」——**那個理由在這個用法下不成立**（三則**文字一字不差、只有圖不同**），**更正：可以做，而且它正好是他要的機制**。**通則：一個做法被否決時，記下的要是「為什麼」不是「不要做」** —— 理由消失了結論就要跟著翻。四條路（第 25-9 節）：**Ⓐ 三則「一律回應」只換圖**（⚠ 要先實測 LINE 到底會不會隨機，兩個二手來源說法**相反**，**五分鐘的後台實測步驟寫在頁面上與 25-9**）／**Ⓑ 三則各設不同時段**（✅ 一定可行，但同一天連傳兩次是同一張）／**Ⓒ 每季換一張**（零風險）／**Ⓓ 請廠商用 API**。**建議先測 Ⓐ，不成立走 Ⓑ**。⚠⚠ Ⓐ 成立要跟著處理兩件：① **三則文字要一字不差、改文案要改三次**，改漏一則病人會隨機拿到舊版**而且不會有人發現**；② **綁定完成那一則會被波及**（廠商把「✅ 09xxx 綁定成功！」改成固定字串之前，綁定那一刻也會隨機吐一張圖）。三張圖的硬條件：**1040×1040 且三張比例一致**（不一致會忽高忽低）、**用 223px 判斷看不看得懂**、格線要對齊版型的格子、**三張要一眼分得出來**（`rich-4`／`rich-4b` 就是反例）、**每一張都要能獨立成立**（他可能只看過其中一張）。⚠ **2026-09-05 提案頁跟著收成純文字**：切換條**五排收成三排**（情境／做幾則／改約那段），**「放什麼」與「圖」兩把尺從切換條上拿掉、網址參數仍然吃得到**（`?c=ti`／`?c=card`／`?g=rand`）——同提醒卡那一輪：**已定案的尺不佔畫面，但要回頭看還開得出來**。量測 800 組：溢出 0・孤字 0・JS 錯 0・**切換條 23.4% → 14.7%**；純文字四格一屏 **38%（一則通用）／24%（分時段）／19%（關鍵字命中）／30%（現況）**——**關鍵字那一格只有一則通用的一半，那是「做幾則」這把尺唯一真正的理由**。以下是走到這裡的過程：提案頁加了第五把尺 `?g=` 與手機框底下的**「＋ 他再傳一次」**（⚠ **隨機一張靜態畫面看不出來**，要連著送才感覺得到；面板報輪數與抽到的順序，**真的隨機就是會連兩次同一張（1/3），沒有做不重複**）。⚠ 三張圖目前用 `rich-2`／`rich-4b`／`rich-4` 當**佔位符**，**不是定案的三張**。量測（五個寬度、**1280 組**）：孤字 0・水平溢出 0・泡泡 ≤268・JS 錯 0・切換條 **23.4%**（⚠ 五排時量到 24.4%、越過那條線，排距 6→**4px** 才收回來）；三張隨機連按四次「再傳一次」在 390 上是 **65% → 131% → 196% → 261%**，序列 `Ⓐ→Ⓑ→Ⓑ→Ⓐ`（**四輪就抽到一次連續重複，那不是 bug**）。⚠⚠⚠ **2026-09-05 稍晚：「圖卡」那一格補上了**（`?c=card`，第 25-10 節）—— 使用者：「我想看我說的圖卡會長什麼樣子，因為你們剛才說自動回覆沒有圖卡的選項」。**他點出我只講了一半**：說了「後台沒這個選項」，卻沒把「如果有會長什麼樣」畫給他看。⚠ 那一格**不是現在做得到的**，是**拿去問廠商的那張圖**（和取消確認、招呼卡同一題）。⚠⚠ **量出來一件會改變建議的事：圖卡比「文字＋圖」還省** —— 只有文字 38%／**圖卡 53%（一個對話框）**／文字＋圖 65%（兩個對話框），傳兩次是 75%／**106%**／131%。**「要圖」走 Flex 的成本反而最低**，因為它是一則不是兩則。廠商說不行才退回「文字＋圖」。⚠⚠⚠ **三張圖的比例取決於走哪一條路，畫之前一定要先定**：圖文訊息 **1040×1040**（方的、切格子、字烘在圖裡）vs Flex 圖卡 **1024×512（2:1）**（只當頭圖、字不烘進去）——**兩條路的圖不能共用**。⚠⚠ 這一輪同一個坑踩第二次：**`<img>` 寫了 height 屬性、CSS 沒補 `height:auto`**，`aspect-ratio` 蓋不過呈現屬性，卡片量成 **841px（一屏 101%）**；以及**卡片寬度寫成 `min(268px,74%)`**，393 上量到 252.3 —— **LINE 的卡片不隨螢幕變**（已定稿那兩頁都寫死 268），在 252 上做的折行判斷全是假的（那一輪誤報的「孤字 1 處」就是這樣來的）。**通則：模擬別人的介面時，凡是那一側寫死的尺寸，我們這邊也要寫死。**量測 800 組：溢出 0・孤字 0・JS 錯 0；圖卡 **268×433.3**、五個寬度逐格相同、字級 16/14/14/13/13px（＝ md/sm/sm/xs/xs）。 |
| **⭐ 商家貼文・看診時間圖**（第二條線的起點） | ⚠⚠⚠ **這不是訊息，是主頁上的一張圖** —— 貼在 LINE 官方帳號的**商業簡介 → 最新貼文**那個模組裡（**不是 LINE VOOM**，見下）。✅ **2026-09-07 定稿 Ⓖ**（1.30 倍 ＋ 時段標籤斷兩行），**09-08 匯出**成 `preview/line-post-hours/fangren-hours-1080.png`（1080×1080・PNG・約 100KB，**就是這一張要貼**；其餘候選都叫 `post-hours-…`）。規格頁 `/preview/line-post-hours/`，產生器 `node drafts/channels/post-hours.mjs`，守門 `node drafts/channels/check-post-hours.mjs`。**推導在 `drafts/channels/README.md` 第三十三節與第三十四節。**⚠⚠⚠ **資料只有一個出處**：科別名／五天／三個時段與時間／每一節有哪些科（`data-in`）／休診說明／電話**全部從 `index.html` 的 `.hours-grid` 與 `.hours-filter` 讀回來**，一個字都不重打 —— 所以「站上的表」和「這張圖」不可能不一致，改排班只要改 `index.html` 的 `data-in`（再跑 `tools/topics.mjs` 與 `tools/build.mjs`）。⚠⚠ **安全帶**：主頁那三格量出來是**大格 823×409（2.01:1）、小格 410×410**，一張 1080 見方的圖擺進大格照 cover **只看得到中間 537 列**，所以品牌與電話收在那條帶子裡；定稿這一張上下各被切 **120px**（切的是留白）。**⚠ 但「大格到底是 cover 裁還是留白」還沒驗過** —— 使用者在後台按一次「預覽」就知道，白守也不虧。⚠⚠⚠ **2026-09-08 更正一件我講錯的**：09-07 我寫「主頁那塊『最新貼文』抓的是 LINE VOOM 的貼文」，**錯的** —— 使用者兩張截圖同時成立：**VOOM 是 3221 好友・4 追蹤者・0 貼文**，而**商業簡介的最新貼文裡那張圖已經在了**。跟著確定五件：評分表整份仍然成立（它評的本來就是主頁那三格）／**「不要寫『有問題留言問我們』」那條紅線不適用**（模組沒有留言區）／**「會不會過期」那根扣分軸變輕**（模組項目沒有時間戳，「三格永久櫥窗」更站得住）／**打擾 0 確定**（不進聊天室、不通知好友）／**那個模組有 `編輯 ／ ↑ ／ ↓ ／ 🗑`**，可以放好幾則、順序自己排、隨時撤。⚠⚠ **VOOM 那條路不要做**：3221 個好友只有 **4 個追蹤者**，要有曝光等於從零養一個社群帳號，而這間診所「沒有人管內容」（管道表那一欄）。⚠⚠⚠ **通則（第三次現場）：先問「這件事現在是誰在做、介面長什麼樣」** —— 25-13 把「他每次現打的字」當成模板、22-1 把「診所端後台的截圖」當成病人手機的寬度，這一次是把「主頁的模組」當成「VOOM 的貼文」，**三次都是看著一個名字就認定它是什麼**。⚠⚠ **「詳情」那一欄要不要寫、寫什麼**：2026-09-08 給了三案（只有事實／多一句怎麼看這張圖（**建議**）／多一句對著人講），**使用者還沒挑**；產生器印的那一份是舊的第一版，**他挑完要回頭改 `detail`**。⚠ 三個要點：**第一行放時間不要放「這是一張門診表」**（說明只先露一兩行）、**圖裡的電話點不下去而說明裡的可以**（這是說明欄唯一做得到而圖做不到的事）、**不可以寫「有問題歡迎私訊」**（沒有專人）。⚠ **還開著三件**：直排（一節一欄）使用者沒表態（它撞到硬牆：晚那一節四科 ＝ 四倍高，26px 時超出安全帶 114 列）／話筒與號碼要不要改成純墨色／**站上要不要把「植牙・假牙重建」一起改名成「假牙重建」**（那張圖已經改了，站上五處一個字都沒動）。⚠⚠⚠ **2026-09-09：浮水印改成三張接起來的那一顆**（`wmFor("hours")`，形狀 `r3c1`、濃度 5% → **4%**、壓在**看得到那一段的下緣**）—— 圓心騎在三格的十字縫上，見下面第三格那一列與 README 第 36-17 節。⚠⚠ 這一改順手收掉 34-7：那張後台截圖證明**大格是 cover 裁切**，所以「壓在下方」是原圖 y≈812 不是 1080 的底邊。⚠ **要跟這張圖一起重跑另外兩張**（`node drafts/channels/post-triptych.mjs`），單獨跑一支會讓那顆圓接不起來 | 第三十三、三十四節 |
| **⭐ 商家貼文・位置與周邊停車** | ⚠⚠ **和看診時間那張是同一條線的第二張，但難題相反**：看診時間是給**大格**（823×409，1080 見方 cover 之後只看得到中間 537 列）→ 難題是**安全帶**；這一張是給**小格**（410×410，方進方 → 整張都看得到，但整張縮到 **0.380×**）→ 難題是**410px 上還讀得出什麼**（同 og-topic-card 那一輪：**在成品的尺寸上量，不要在素材的尺寸上量**）。**兩格要的是兩張不同的圖，不要互換** —— 這一張的內容佔 44~1036，放大格標題與電話會被切掉。⚠ **同一個字級在這兩格上差一倍**，所以這張的標題 46／頁尾 32 比看診時間那張（畫出來 40.3／29.9）大，**那不是不一致**；小格上量到標題 17.5px、地址與電話 12.1px，而**地圖上的字一律小於 10px**（路名 9.6／診所 8.7／停車場名 7.0／距離 6.5）—— **那是故意的**，小格上地圖是一張圖、字由標題與頁尾扛。產生器有一道**不是壞掉檢查**的守門在擋：標題或頁尾在小格上小於 10px 就 throw。⚠⚠⚠ **地圖不重畫也不重抄**：開的是 `index.html` **本人**，把站上那張 SVG 的算繪（牌子、點狀路線、被壓到的街名全部是站上那支 JS 現算的）**1:1 截下來**貼進畫布（deviceScaleFactor 1，沒有重新取樣）——座標、顏色、字級、圖釘的十六層影子**一個字都不在產生器裡**，站上改地圖這張圖跟著改。⚠ 地圖四角是圓的（`clip-path` rx=18），元素截圖會連框後面那一塊底色一起帶走 —— 截圖前把那張卡的底色設成畫布的底色，**出圖後再讀那張 PNG 的角落像素比對**，對不上就 throw（接縫那一圈只有放大才看得到）。⚠⚠ **三塊停車場全亮**（站上一次只亮一塊，那是**手機一格放不下三塊牌子**的解，不是資訊分層；貼文放得下就攤開，＝看診時間那張的同一個判斷）。代價只有一個而且站上不可能發現：**那三塊牌子從來沒有同時出現過**，永樂站與合廷**疊 28.3 個單位**。**往左挪 50 是解出來的不是挑的**：① 兩塊之間至少空出 **2×padX**（padX ＝ 牌子自己的左右內距 11，從 `sizeLabels()` 讀回來）② **尾巴要留在它自己那塊 P 上**（落在 220，那塊 P 是 206~264）—— 三塊同時出現時，尾巴指在那塊停車場本身上才分得出誰是誰。兩個條件都驗，掉出去就 throw。⚠⚠⚠ **左右各留 179px 是結構性的，沒有第三條路**（照第九節第 28 條 ③ 主動攤開）：地圖是**直的**（560×620 ＝ 0.903）、畫布是**方的**，所以是**高度**在卡；**把地圖四周的空白裁掉反而更瘦**（0.903 → 0.724，這張圖空的是左右不是上下），**要它滿版就得上下切掉 111 個單位以上**、永樂站與壹車房兩塊牌子都會不見。所以只能在「地圖大一點」和「字大一點」之間挑 ＝ 規格頁那把尺。⚠ **七格的尺**（第 28 條 ①）：Ⓐ 建議（三塊全亮・46／32・頁尾三行）／Ⓑ 頁尾不放停車那一句／Ⓒ 只亮壹車房／Ⓓ 字大一階 54／38／Ⓔ 字小一階 40／28／Ⓕ 標題換一句（兩案）／Ⓖ 沒有標題。⚠ **Ⓑ 那一句建議留著**：「周邊三處停車場・走路 1~3 分鐘」是整張圖上**唯一在 410px 讀得出「有停車位」**的地方，而且它是從三塊牌子的距離**現算**的（三處、1~3 分鐘），不是打上去的字。規格頁 `/preview/line-post-map/`，產生器 `node drafts/channels/post-map.mjs`，守門 `node drafts/channels/check-post-map.mjs`。**推導在 `drafts/channels/README.md` 第三十五節。**⚠ 站上地圖那一版**沒有被選上**（使用者改指名門口那張告示的版本，見下），檔名維持流水號 `post-map-…`。⚠⚠⚠ **2026-09-08 同日使用者指名換版本**：「之前做過一版印出來的停車建議（因為有警察開單）用那個版本製作　有 P1 P2 P3 和 QR code 的那個」＝ **`drafts/door-notice/`** 那張 A4 告示。產生器 **`node drafts/channels/post-map-door.mjs`**（→ `door-*.png`，和上面那一版同一頁、同一支守門），推導在 README **第 35-8 ~ 35-12 節**。⚠⚠⚠ **換媒介的三件**（第九節第 28 條 ⑤）：① **「現在位置」四個字**要換成「芳仁牙醫」——門口那張是給**站在門口的人**看的，看貼文的人不在那裡；做法不是去改門口那張的腳本，是把它**已經擺好、已經轉正**的那個 `<text class="you">` 的兩個 tspan 換掉（剛好也是 2+2 個字）。② **「鄰近停車場可參考如下圖」要求地圖在文字底下**，版面因此鎖住「四點在上、地圖在中」。③ **三顆 QR** 見下。⚠⚠ 反過來它有一件**在方畫布上比站上那張好**：門口那版把地圖**轉了 90 度**，所以地圖是**橫的（1.274）**而不是站上那張直的（0.903）——方畫布吃橫的圖吃得下，這就是它贏的地方（`door-north.png` 是代價的展示：北在上時只剩 249px 寬）。⚠⚠⚠ **QR 真的拿解碼器掃過**（門口那張的 README 就寫著「不能用眼睛驗收」），數字寫進 `door-qr.json`、守門會再掃一次去對：**200px 的碼在原圖 3/3、大格 2/3、小格 0/3**；**收小到 140px 連原圖都 0/3**（所以「把碼縮小換地圖大一點」走不通）。⚠⚠⚠ **而且還有一件更根本的：看貼文的人正拿著那支手機，掃不了自己螢幕上的碼** —— **通則：一個做法搬到新媒介之前，先問「它成立的那個前提還在不在」**（QR 成立的前提是「碼在別的東西上」，在螢幕上那個前提就沒了）。→ LINE 上接得住的是**「詳情」欄裡三條可以點的網址**（已產好，順序照使用者排的 P1／P2／P3，**刻意不是照距離**）；**建議 Ⓒ 不放 QR**（地圖大 50%），但**他指名的是有 QR 那一版所以沒有自己拿掉**，Ⓐ 的碼維持 200px。⚠⚠⚠ **踩到一個「每一個數字都對、只有把圖打開看才看得到」的坑**：直接去改門口那張 `.sh-map` 的寬高（A4 橫式那一組）之後，地圖畫出來**少一條街**（「永安路」整行不見），而元素尺寸、長寬比、viewBox、裁切框全部正確 —— 成因是那張紙的版面是它自己那支 `fit()` ＋ `cqw` 算的，從中間插手會踩到它沒預期的狀態。**修法：改那張紙有多寬、讓它自己算**（用 A4 **直**式那一組，順序也和方畫布一樣），並**新增一道守門：六條主街的字每一條都要畫得出來**（外框不可以是 0×0）。⚠ 近親：**元素截圖是「在那個元素的框裡切一刀」，壓在框裡的別的東西也會被拍進去** ——撐高之後告示自己的 QR 那一排疊進來，圖上多了兩顆小 QR 而每一道尺寸守門都會過；修法是先藏起來、**再驗一次框裡真的沒有別的東西**。⚠⚠⚠ **2026-09-08 定案 `door-c`：不放 QR ＋ 把「紅線」「開單」那兩句拿掉**（使用者：「選 C 但前兩行　紅線　開單的文句拿掉」）。⚠⚠⚠ **那是第四件換媒介的事，而且是他看出來的 —— 我送上去的每一格都還留著那兩句**：門口那張是給**車已經停好、站在門口的人**看的，那兩句擋的是「就停一下」；看貼文的人**還沒出門**，同一句話讀起來是「這間診所門口會被開單」＝ **警告不是幫忙**。⚠⚠ **通則：換媒介要逐項問的不只是「這個元件還成立嗎」（版面與功能），還有「這句話對新的讀者是什麼意思」（語氣）** —— 上面那三件全是前者，這一件是後者，所以整組漏掉。⚠⚠ **換到的東西比想像大**：那兩句 ＋ 三顆碼的高度全部給了地圖，**516 → 920px（大 78%）**，左右頂到版面內距（各 40px）；**小格 410px 上的街名 7.4 → 13.2px ＝ 第一次過 10px**，所以「地圖上的字一律小於 10px、由標題與頁尾扛」那條已知取捨**被這一改解掉了**。⚠⚠ **不可以照順序砍前兩條**：`dropPts()` 會驗「砍掉的那兩句真的含著紅線／開單、留下的真的沒有」，守門再驗一次外加**門口那張本人仍然是四句**（那張紙一個字都沒動）—— 不這樣做的話 `body.html` 哪天改字或換順序就會**靜靜地砍錯句子**。⚠ **「詳情」那一欄跟著拿掉同樣兩句**（圖寫兩句、詳情寫四句 ＝ 同一則貼文有兩個版本的事實），守門有一道在盯。⚠ **順手長出一題**：前兩句一走，整張圖第一眼是「周邊路段有畫設之路邊停車格」——**沒有人知道這是誰家的**；做了一格 `door-c-title`（標題「芳仁牙醫　周邊停車」，地圖 920 → 822）**擺出來讓他挑，沒有自己加上去**（不加也說得過去：主頁三格是一起看的，上面那格已經寫著芳仁牙醫的門診表）。⚠⚠ **這一輪的坑**：改頁面時「前後兩個錨點」之間含著自己剛加進去的同一段字，`index()` 命中的是新加的那一塊，**中間整塊被吃掉而且不報錯**（標題案、紀錄那幾格、整張 QR 表全沒了）。修法是**展示用的那一塊改成另一個 class**（`.pv-cut`）——**通則：一段字同時是「資料」又是「展示品」的時候，兩者一定要用不同的標記。**⚠ **小格右**那一格（主題與科別七科）還沒做 —— 七張分享圖現成，但都是 **1.91:1，要重裁成方的**。⚠⚠⚠ **2026-09-08 同日再一輪：下排那兩行的字級**（使用者：「下排停車場的名稱和距離　步行時間字級有點小」）—— **量出來他是對的**：那兩行在小格 410px 上只有 **9.9／8.4px**，而這一線的判準一直是「小格上小於 10px ＝ 等於沒寫」。⚠⚠ **這一排從頭到尾沒有人量過** ——「標題或頁尾小於 10px 就 throw」那道守門只盯標題與頁尾（＝第九節第 28 條 ④「壞掉檢查擋不住醜」又一次）；現在每次出圖都印。⚠ **給的是尺不是值**：字一大那一排就變高，**高度是從地圖借的** —— Ⓐ 現況 9.9／8.4（地圖 920）／Ⓑ ×1.10 一行 10.6／9.1（914）／**Ⓒ ×1.30 折行 12.9／11.0（758，建議 —— 兩行第一次都過 10px）**／Ⓓ ×1.45（680）／Ⓔ ×1.60（648）。⚠⚠ **中間有一道硬牆**：三個名字原本 `nowrap`，**×1.15 就開始互相碰到**（三格的自然寬加兩條溝 1020 > 1000），要再大只能**讓名字折行**。⚠⚠⚠ **折行的時候號碼牌要獨立成一列** —— 只開 `white-space:normal` 的話三格**各折在不同的地方**（一格牌子在字旁邊、一格在字上面），那正是第 28 條 ⑤ 那一種「沒有人決定過的東西被折行決定了」；`wrapn` 因此改成 `flex-direction:column`。⚠⚠⚠ **這一輪修掉三個「量測壞掉但畫面正常」**：① 那道「名字撐破格子」的守門**從來沒有生效過**（`nm.scrollWidth > lot.clientWidth`，而 `.nm` 是 flex 容器、寬度本來就等於 `.lot`，式子恆等於 0）；② 改成「把 `.nm` 複製出去量」**也是假的**（裡面的字是 flex 的匿名項目、仍然被壓縮，26px 和 28px 回同一個數字）——**通則：一個東西在 flex 或 grid 裡就量不到它「本來要多寬」，要量就把容器暫時切成 `max-content` 再量、量完還原**；③ **判準本身也錯**：一格 1fr 是 333、三個名字長短不一，**短的用不完、長的可以借旁邊那條溝**，所以要比的是「三格的自然寬加兩條溝 vs 整條」不是「有沒有超過自己那一格」。⚠ 順手修掉一個真的 bug：`repeat(3,1fr)` ＝ `minmax(auto,1fr)`、最小值是 min-content，**一格撐破會把整個 grid 推出畫布**（實測右緣到 1138）→ 改 `minmax(0,1fr)`（＝站上 `.info-card{min-width:0}` 那一條）。**代價：那幾張當紀錄用的圖的地圖尺寸整組變了**（Ⓐ 516→460／noqr 772→714／c-qr 666→610／北在上 246→218／標題那格 822→766），README 與規格頁的數字都已跟著換；**定案那張 `door-c` 的 920×723 沒有變**。⚠⚠⚠ **2026-09-08 同日再一件：折行的斷點要自己指定**（使用者：「Ⓒ ×1.30　壹車房　斷行　中華路停車場　斷行　190公尺　分成三行」）—— 交給瀏覽器折的話它折在「中華路停車**／**場」，**一個字被丟到下一行**；三張卡裡只有 P1 放不下（10 個字 × 34px ＝ 340 > 欄寬 315），而 `white-space:normal` 不知道那個名字是**兩截**（品牌－分店）接起來的。→ 名字裡本來就有一個**全形破折號 U+FF0D**，那才是它自己的斷點：折行那一種照它拆成兩個 `.ln`、各自一列，讀起來是 **壹車房／中華路停車場／190 公尺・步行 3 分鐘**。⚠⚠⚠ **破折號同日由使用者指定拿掉**（「壹車房後破折號拿掉」）——**折行那一種，斷點就是分行**，破折號的工作已經由換行做掉了。⚠ **只有畫在圖上的那一份拿掉**：`door-detail.txt`（貼進「詳情」欄的字）與門口那張紙**一個字都沒動**——那兩份是**一行**，破折號在那裡仍然是唯一的分隔。**同一個名字在兩種排法底下需要的標點不一樣，那不是不一致。**⚠ 守門加一道：畫在圖上的名字要等於「資料把破折號拿掉」（一行那一種要等於原文），擋的是「資料換了名字、拆法沒跟上」——症狀是行尾多一個破折號或整個名字擠成一行，**兩種都不報錯**。⚠⚠ **一行那一種也要包成 `.ln`**（畫出來一模一樣）—— 不然「名字畫成幾列」量不到：`.nm` 裡還有那顆號碼牌，**字級不同、rect 的 top 就不同**，直接量會被數成 2~3 列（第一版就是這樣報「3/3/3 列」）＝ **同一輪的第四個「量測壞掉」，成因也同一種：****量了一個裡面裝著不只一種東西的容器**。⚠ 守門加一道：自己斷行的每一段只准佔一列，被再折就 throw。⚠ 代價：兩列之間改吃 flex 的 `gap`（7px）比瀏覽器自己折的行距寬一點，那一排高 226 → **235**、地圖 764 → **754**（Ⓓ 676、Ⓔ 644）；**Ⓐ／Ⓑ 與所有紀錄用的圖一個像素都沒動**。⚠⚠⚠ **2026-09-08 同日再一件：指北針小一格**（使用者：「指北針的標示和北可以小一點」）——門口那張紙用的 `?ns=b`（針長 36）是**站在門口、離半公尺**挑的，這張圖要在**小格 410px** 上看，同一組就顯得吵；這裡帶 **`?ns=c`**，**門口那張一個字都沒動**。給了三格：Ⓝ1 `ns=b` 整組 31.8px・「北」9.2／**Ⓝ2 `ns=c` 27.6・8.0（現在這樣）**／Ⓝ3 `ns=d` 23.5・6.7（都是小格 410px 上的值）。⚠ **「北」三格都在 10px 以下，但這一格不套那條線** —— 那個字**不必讀、形狀認得出來就好**（旁邊還有箭頭）：**那條 10px 的線是給「要讀完的字」的，不是給記號的**。⚠⚠ **指北針畫在地圖那張 SVG 的 viewBox 裡面**，所以換大小**不會動到版面**（三張逐格相同），**但它一收整張圖就瘦一點**（長寬比 1.274 → **1.283**）→ 同樣的高度放得下更寬的圖，**所有地圖尺寸因此整批 +4~6px**（Ⓐ 920→**926**／c110 920／c130 **758**／c145 680／c160 648／c-title 828／c-qr 670／a・b 520／noqr 776／qr140 598／北在上 248）。⚠⚠⚠ **2026-09-08 再一件：地圖要多大**（使用者：「指北針變小　地圖的部分就有餘裕大一點　調整一下」）——**他的直覺對，但那個餘裕只有 4px**：指北針在地圖那張圖的 viewBox 裡面，收小只讓圖瘦一點。⚠⚠⚠ **真正卡住地圖的是高度，而且左右還空著 121px**：地圖的寬 ＝ `min(剩下的高度 × 長寬比, 版面寬)` ＝ `min(592×1.283, 1000)` ＝ 758 —— 所以「地圖大一點」只有一條路：**從這一頁自己的上下留白拿**（第九節第 28 條 ③ 把取捨攤開讓他選）。⚠ **指北針那條帶子不可以拿來抵**：門口那張的規則是「**指北針不可以壓在路上**」（使用者 2026-08-23 指定，寫在 `door-notice/script.js` 裡）——**沿用別人的元件之前要先讀它為什麼長這樣**。四格（上下留白／段距 → 地圖／小格 410px 上的留白）：Ⓜ1 現況 44／18 → 758／17.0px、Ⓜ2 34／14 → 804（+6%）／13.2、**Ⓜ3 建議 26／11 → 840（+11%）／10.3**、Ⓜ4 18／8 → 876（+16%）／7.1。⚠ 四格**只有留白不一樣**。⚠ **左右的內距沒有跟著收**（一直是 `PAD 40`）—— 收它一點都換不到地圖（高度在卡），只會讓上面那兩句變長，所以 Ⓜ3 之後「上下比左右緊」是刻意的。✅ **2026-09-08 使用者：「選 M3」—— 已經寫回 `MARGIN0 = 26`／`GAP0 = 11`**，所以**每一張 door-*.png 都跟著換了**（`c130-m1/m2/m4` 留著當尺上的對照，`c130` 自己就是 Ⓜ3）：Ⓐ 926→**998**（頂到左右內距 1000，Ⓐ 與 Ⓑ 因此一樣大）／c110 920→998／**c130 758→840**／c145 680→**762**／c160 648→**730**／c-title 828→**910**／c-qr 670→**752**／a・b 520→**602**／noqr 776→**858**／qr140 598→**680**／北在上 248→**286**。⚠⚠ **順帶一件量得出來的副作用：那三顆 QR 縮到大格 823 從 3/3 變成 0/3** —— 碼一樣大（每格 3.72px，本來就在解碼器的邊緣），是那一排往下挪了一點、縮圖的取樣跟著換了。結論沒有變（這張圖本來就是給小格的），但它是好例子：**版面只要挪動，QR 就要重掃一次，而畫面上完全看不出來**。⚠⚠⚠ **2026-09-08 同日再一件：浮水印**（使用者：「試試看這個壓像門診表的浮水印看看　可能要更淡一點」）—— 做法**逐字照看診時間那一張**：同一顆圓 logo（`brand/shapes/shape-r3c1.svg`，形狀從那裡讀、不抄第二份）、同樣 **660px**、同樣 `right:-60 / top:470`、同樣墨色 ＋ 很低的 opacity，**沒有新增顏色也沒有多一個檔案**。⚠ **大小照抄是刻意的**：兩張圖在主頁三格裡是**上下相鄰**的，份量不一樣就會讀成兩件事。⚠⚠⚠ **但位置不能照抄 —— 2026-09-08 使用者：「感覺可以壓左上　因為右下相對比較多要認真看的資訊」，他是對的**：門診表那一張的右下角只有「國定假日…」與電話兩行，**這一張的右下角是三張停車場卡**（名字＋號碼牌＋距離共九段字 ＝ 整張圖上唯一要逐字讀的東西）；左上那兩句提醒只有兩行、行長不一，右邊本來就空著。**通則：「照抄那一張」要逐項問「那一角在這一張上裝的是什麼」—— 位置抄得過去的前提是兩張圖那個角落的內容份量差不多。**⚠ 切出去的量因此改成**比例**（寬的 9.1%、高的 7.6% ＝ 門診表那一張 −60／−50 對 660 的比值），`WMBX`／`WMBY` 各一個常數，**換形狀時位置自動跟著換**；右下那一格留著當對照。⚠⚠⚠ **換哪一顆 logo 也是一把尺**（使用者：「圖案換　用幾個不同的 logo 試看看」）—— **不可以沿用同一個寬度**：九顆的長寬比 1.00~3.08、墨佔外框 59.6~83.2%，同寬的話細長那幾顆會輕很多（＝約診卡 22-13 同一件事）。**寬度的唯一出處是 `preview/line-booked/wm-sizes.json`**，這一支只乘一個倍率（`wmWidth()`）。五格：**Ⓛ1 r3c1 660×660（現在這顆）**／Ⓛ2 r1c1 679／Ⓛ3 r3c2 673／Ⓛ4 r2c2 762×572／**Ⓛ5 r1c2 ＝ 站上頁首那顆 952×469**；⚠ 長寬比 3.08 那三顆等重之後要 **1193~1364px、比畫布還寬**，沒有列進來。⚠ **五格在小格 410px 上幾乎分不出來**，形狀的差別只有 1080 原圖看得到 —— 那本身就是一個判斷。⚠⚠⚠ **但有一件照抄就會壞：這一張的地圖是一張不透明的 PNG** —— 門診表那張的浮水印壓在表的**後面**（表的底是透明的），這裡的地圖蓋掉畫布中央 840×655，浮水印擺在後面會被那個長方形**切掉一大塊**（圓形沿著地圖的邊突然消失）。所以這裡改成**壓在最上面**（`z-index:2`）、濃度更低；在這麼淡的一端兩種做法讀起來一樣。**通則：一個效果搬到別的版面之前，先問「那個版面的底是不是透明的」——「壓在後面」只有在它後面真的看得到東西時才成立。**⚠⚠⚠ **QR 上面不可以壓任何東西**：那三顆碼自己拉到浮水印前面（`.lot .qr` 給 `z-index:3`），因為壓上去**畫面上看不出來**、只有拿解碼器掃才知道（同門口那張 README「QR 不能用眼睛驗收」）。四格（墨／底色／壓在上面的柔墨）：Ⓦ1 ＝ 門診表那張 5.0%／`#eaeaeb`／5.66、**Ⓦ2 4.0%／`#ececed`／5.68**、Ⓦ3 3.0%／`#eeeeef`／5.76、Ⓦ4 2.0%／`#f0f0f1`／5.80。✅ **2026-09-08 使用者定了三格：「4% R1c2」＋「再往左移」** —— 濃度 **Ⓦ2 4%**、形狀換成 **`r1c2` ＝ 站上頁首那顆**（952×469）、左邊切出去 **180px**（＝寬的 18.9%，尺是 87／**180**／280／380）。⚠ **門診表那一張仍然是 r3c1・5%・壓右下，兩張不一樣是他挑的，不要順手改成一致。**⚠ 「再往左」的單位是**畫布 px** 不是百分比（他看的是離左邊多遠），常數 `WMX0`，**只作用在 tl 這一側**；**垂直一個字都沒動**。⚠⚠⚠ **這一輪踩到一個「每一道守門都過、只有把圖打開看才看得到」的**：等重換算的基準原本寫成「目前這一顆」，**把預設換成 r1c2 的那一刻基準跟著變成 r1c2，整組尺一起縮水**（r1c2 952→660、r3c1 660→458），他挑的那一顆畫出來**比他看到的小 31%**。→ 基準寫死成 `WMREF = "r3c1"`（`WMW0 = 660` 的意思是「**r3c1 畫 660 那麼重**」不是「不管哪一顆都畫 660」）。**通則（第 28 條 ② 的又一次）：一把相對的尺，換掉預設就等於換掉基準。**⚠ **對比度不是這一格的判準**（四格都遠高於 4.5），純粹是看起來舒不舒服；守門 `wmCheck()` 仍然在算，而且**前景與背景兩邊都要合成一次**（壓在最上面時字自己也會被染一層墨，擺在後面的話只有背景會）。⚠ **濃度一定要在會被看到的尺寸上比**：`door-slot-410-wm.png` 是四格縮到小格 410px 並排（浮水印在那裡只有 251px）。⚠⚠⚠ **2026-09-08 最後一輪：挑定的全部寫回預設，規格頁收成一件事**（使用者：「不要標題　Ⓧ4　Ⓒ×1.3　Ⓝ3　**其他都不要再顯示在提案頁上**　我覺得可以比 Ⓧ4 再左一點」）—— 寫回常數的六件：**下排字 ×1.30 ＋ 折行**（`LOTK0`／`WRAPN0`）、**指北針 Ⓝ3**（`NS0 = "d"`）、**留白 Ⓜ3**、**拿掉那兩句**（`DROP`）、**不放 QR ＋ 不加標題**（`build()` 的預設）、**浮水印 4%・`r1c2`・壓左上**，所以 `build("c")` 本身就是定案那一張。⚠ 指北針換到 `ns=d` 之後那張圖又瘦了一點（1.283 → **1.292**），**地圖 840 → 848px**（同一個高度放得下更寬的圖）；下排兩行在小格上仍是 12.9／11.0px。⚠⚠ **還沒挑定的只剩一件：浮水印再往左多少** —— 他挑的 Ⓧ4 是 380 又說「可以再左一點」，所以**整把尺往左移一格重開**：380（上一輪他挑的）／**440（現在的預設）**／500／560，看得到的寬度 572／512／452／392px（小格 410px 上 217／194／172／149）。⚠⚠ **這是一把有盡頭的尺**（那一顆畫出來只有 952 寬，切 560 就剩右邊 392px，形狀認不出來就變成一團灰）—— **尺給出去之前要先知道它的盡頭在哪**。⚠⚠⚠ **規格頁從 45 張圖收成 8 張**（實檔 7 個）：拿掉下排字級 Ⓐ~Ⓔ、指北針 Ⓝ、留白 Ⓜ、濃度 Ⓦ、換 logo Ⓛ、壓哪一角、標題那一格、**QR 那張表（連同 `door-qr.json` 與守門那道opencv 重掃）**、北在上，以及**「站上那張簡易地圖」的整個版本**（`post-map-*.png`／`profile-3up.png`／`slot-410.png`／`detail.txt`；`post-map.mjs` 沒有刪）。**每一格的推導都在 README 第三十五節與 git 裡**（`git show 4fe0021 -- preview/line-post-map/`）—— **要回頭比就從那裡取，不要重畫**。⚠⚠ **面板不可以跟著一起收**：格子從 `CASES` 上拿掉了，但「指北針多高／留白多少／地圖多大／浮水印多濃、對比剩多少」**每次出圖仍然要印** —— 不然哪天有人動到留白或形狀，這裡不會有任何一個數字變（第 28 條 ④）。⚠⚠⚠ **收頁面時守門真的擋了一次**：`pv-cut` 那兩句我憑印象打成「違規停放曾有警方前來開單，請多留意。」，而門口那張本人是「過往多為柔性勸導，近來常有直接開單情形。」——**通則（又一次）：凡是別處已經定案的字，一個字都不要重打，讓守門去對。**✅⭐ **2026-09-08 定稿 Ⓧ2 ＝ 440px 並匯出**（使用者：「Ⓧ2 定稿　匯出給我」）——**要貼的檔案是 `preview/line-post-map/fangren-map-1080.png`**（1080×1080・PNG，同看診時間那張的命名；其餘兩張是模擬圖 `door-profile-3up.png`／`door-slot-410.png`），「詳情」欄的字照 `door-detail.txt` 貼、**不要重打**。定稿的每一格全部寫回常數：不放 QR・拿掉那兩句・下排字 ×1.30 折行・指北針 Ⓝ3・留白 Ⓜ3・**浮水印 4%・`r1c2`・壓左上・左邊切掉 440px**・不加標題。量到地圖 848×656、下排兩行在小格 410px 上 12.9／11.0px、浮水印看得到 512px（小格 194）、壓在它上面柔墨 5.68。⚠⚠ **檔名改在最後一步**：產生器內部仍然叫 `door-c`，兩張模擬圖都讀它，排完之後才 `renameSync`；**清檔那一行要一起清 `FINAL`**，不然重跑會留下上一次那張。⚠ 規格頁最後收成 **3 張圖**，「再往左」那把尺整節拿掉（四格的數字留在 README 35-15）。⚠⚠⚠ **2026-09-09：浮水印改成三張接起來的那一顆**（`wmFor("map")`，形狀從 `r1c2` 換成 **`r3c1`**、壓**左上**、濃度 4%）—— **原本那兩把尺（`WMSH0` 換形狀、`WMX0` 往左切多少）留著但已經不生效**，見下面第三格那一列與 README 第 36-17 節。⚠ **要跟這張圖一起重跑另外兩張**（`node drafts/channels/post-triptych.mjs`）。⚠ **還開著兩件，都不是這張圖的事**：小格右那一格**已經做好了**（主題與科別・醫師，見下一列）、以及**小格到底是不是照 410×410 裁**（後台按一次「預覽」就知道）。 | 第三十五節 |
| **⭐ 商家貼文・主題與科別・醫師**（第三格） | ⚠⚠⚠ **使用者一句話就是規格**：「因為門診表已經有七個科別　**所以不要只是又重複只列七個科別**」—— 大格（門診表）已經把七個科別名連同圖案印過一次，所以這一格要接的是門診表**沒有**回答的兩件事：**誰**（九位醫師，門診表上一個人名都沒有）與**做得了什麼**（站上醫師卡的專長，二十三項）。⚠ **先更正一個數字：不是七位，是九位分屬七科**（矯正與兒牙各兩位）—— 這正好是答案的關鍵，**七行裝得下九位**，「一格放不下」其實不成立。⚠⚠⚠ **塞不塞得下不是問題，量出來是**：小格 410×410、0.380×，螢幕 10px ＝ 畫布 26.3px；九位的「名字＋科別」19.7px 很寬鬆、加上每位三項專長 11.0px 貼著地板、**站上醫師介紹整區 1045 字只剩 8.5px 過不了**、**七張現成分享圖拼成一格每張只剩 129px**（低於它自己設計的 250px）。**所以要決定的是那一格回答什麼** —— 34-1：主頁三格接的是還沒加好友的人，他問「我這個問題你們看不看」，而陌生人不認識李柄輝、卻看得懂「口腔外科」。⚠⚠ **「不要重複」有一個免費的做法：科別的名字整個不寫、只留圖案**（同一組 `SHAPE` 與 `BRAND.mix`，產生器有兩道守門逐格比對門診表那一張，兩張並排差一階就會被看出來）。⚠⚠⚠ **但那個賭注只成立一半，而且是量出來的**：把門診表那張讀進 canvas 找七科顏色的墨落在哪幾列 —— 大格看得到 y 271.5~808.5，而**齒顎矯正 516~809・假牙重建 362~814・口腔外科 492~809 三科的圖例被切掉**。⚠⚠ **這件事對門診表那張自己也成立**（`post-hours.mjs` 的註解就寫著「圖例是那張表讀不讀得懂的前提」），但它壓著 34-7 那個沒收掉的前提：**主頁那一格是裁切還是留白還沒驗過**（後台按一次「預覽」就知道）—— 留白的話這一整段不成立。**Ⓓ 存在的理由就是這個。**⚠ **四格的尺**（第 28 條 ①）：Ⓐ 九位醫師（不寫科別名，姓名 19.7px 最大，但看不出做得了什麼）／**Ⓑ 醫師 ＋ 他在做的事（建議 —— 同時回答誰與做什麼，而且一個科別名都沒寫；代價是專長那行 11.0px）**／Ⓒ 做得了什麼（二十三項專長，一個字都沒有另外寫，但沒有醫師）／Ⓓ 一科一行 ＋ 該科醫師（最好懂、也是唯一不靠圖例的，**但它就是他說不要的那一種**）。⚠ **Ⓒ 照專長自己的 `data-spec` 分群不是照那位醫師的科別**（有的醫師專長橫跨三科，站上那幾顆標記本來就各自上色），同一個詞只留一次。⚠ **Ⓑ 取前三項是版面決定的**（最多的那位有七項、一定折三行），**順序照站上、沒有重新挑**。⚠ **圖上放不下七個入口，「詳情」欄接得住**：七條 `/topics/<spec>/` ＋ 一條 `/#doctors`，而且點得下去（同小格左那張地圖的三條停車場網址）。⚠⚠⚠ **這一輪踩到兩個「畫面正常、數字不對」，而且第二個更值得記**：① 固定寬度的欄位被字撐破時**不會換行也不會溢出畫布，只會靜靜地壓在隔壁那一欄的字上面**（Ⓓ 的科別名欄第一版給 260px，最長的要 356px，「一般牙科・定期檢查」和「李柄輝」直接疊在一起）；② **補上去的第一版守門也是假的** —— 它用 `scrollWidth > clientWidth`，可是那一欄是 `overflow: visible`，**Chromium 對不能捲的元素回的 `scrollWidth` 就是 `clientWidth`**，字凸出去了它還是回「剛剛好」，那一道**放行了**。改用 `Range` 量墨才抓得到。**通則：要問「這段字本來要多寬」就得量墨，不要問盒子**（近親：第三十五節那三個「在 flex 裡量不到本來要多寬」）。規格頁 `/preview/line-post-docs/`，產生器 `node drafts/channels/post-docs.mjs`，守門 `node drafts/channels/check-post-docs.mjs`。**推導在 `drafts/channels/README.md` 第三十六節。**⚠⚠⚠ **2026-09-09：Ⓒ ＋ Ⓓ 合併**（使用者：「我喜歡 C+D　把資訊整合再調整看看」）—— **Ⓐ／Ⓑ 整組拿掉**（留在 git：`git show ae10fa2 -- drafts/channels/post-docs.mjs`）。骨架是 **Ⓓ 的一科一列**、內容補上 **Ⓒ 的專長**：`[圖案] 牙周治療 …… 李侑津` ／ 縮排那一行是那一科的專長。⚠⚠ **每一科的專長全部印，不要沿用 Ⓑ 那個「取前三項」** —— Ⓑ 是「一位醫師一行」、最多的那位七項會折三行；這裡是「一科一行」，最多的一科只有五項、一行放得下。**同一個 slice 換了骨架就不成立了。**⚠ 醫師照 `data-spec` 分到那一科、專長照**專長自己的** `data-spec` 分群，**兩邊分法不同是對的**。⚠ **四案只差排法**：**Ⓔ 兩行（建議，折行 0/7、內容 836px）**／Ⓕ 專長做成小塊（0/7、803）／Ⓖ 三欄一列（⚠ **4/7 折到三行**、756）／Ⓗ 醫師和專長同行（⚠ 1/7、849）。⚠⚠ **字級與標題四案完全相同**（科別 36／醫師 31／專長 29，標題一律「芳仁牙醫　科別與醫師」）—— 這一輪比的是排法，標題或字級跟著動，那把尺就同時動了兩件事。**兩個例外是那一案自己的形狀逼出來的**：Ⓕ 列距 12→7（描邊小塊自己高 12px，七列多 84px 就超出畫布）、Ⓗ 列距 12→10（第二行的行高由**醫師那 31px** 決定）。⚠⚠ **「折行」要用印的不是用 throw 擋** —— Ⓖ 折到三行是它的代價不是壞掉（第 28 條 ④）。⚠⚠⚠ **三個坑，成因都是「選擇器命中不到」**：① **對齊守門不可以一律量左緣**（Ⓔ／Ⓕ／Ⓗ 的醫師名是靠右的，第一版四案全部誤報）——**守門要跟著這一版的版面走，不要跨案照抄**；② **第二位醫師被畫成斜體、而且兩個名字黏在一起** —— 歸正與間距都寫在 `.mtop .who i` 上，而 Ⓗ 那一案的 `<i>` 包在粗體裡、**後代選擇器命中不到**，**每一道數字守門都過、只有把圖打開看才看得到**；③ **模板字串的 CSS 註解裡不能有反引號**（這條線第五次，先 `SyntaxError` 再 `ReferenceError`）。⚠⚠⚠ **2026-09-09 稍晚：「保留 E F　有些醫師專長是不同科的　更新一下」** —— Ⓖ／Ⓗ 拿掉（留在 git：`git show d01e961 -- drafts/channels/post-docs.mjs`），而他點出來的那件事是**一個內部矛盾不是漏寫**：同一列裡的兩樣東西用兩套規則算 —— **專長照「專長自己的 `data-spec`」分、醫師照「那張卡自己的 `data-spec`」分**，兩套一定岔開，而畫面上完全正常（牙周那一列印著**李柄輝**的「牙周照護」卻只掛李侑津；李侑津的植牙手術・固定假牙・活動假牙印在假牙重建那一列，那一列只掛楊小瑩）。⚠⚠⚠ **修法不要自己發明 —— 站上的篩選本來就有答案**：`index.html` 的 `specHit()` 寫著「專科 ＝ 那一科，**或**專長裡有一個詞屬於那一科」，所以按下站上牙周那顆標記本來就會亮出李侑津**和**李柄輝。照抄之後「這張圖印誰」＝「站上按那一科會亮誰」，**不可能對不起來**；補進來的跨科出現 **6 次**（一般牙科 ＋3、牙周 ＋1、假牙重建 ＋2，其餘四科沒有）。⚠⚠ **順序是一個新的設計項不是沿用**（第九節第 28 條 ⑤ 又一次）：站上一個人只出現在一張卡上，所以「誰排前面」從來不必回答，補進跨科的之後它才開始說話 —— 定案**本科的排前面**，兩群內部照站上卡序。⚠ **剩下的那把尺**：跨科的那幾位要不要用**柔墨**分深淺（柔墨那一格是照抄站上的 `tag-off`；另一格全部同一階、四位並排份量一致）＝ Ⓔ／Ⓕ × 兩格四張圖。⚠ 守門新增兩道並負向測過：**頁上那張「每一列印誰」逐格 ＝ 拿 `index.html` 現算的結果**、以及**產生器裡真的寫著那條規則**（只有前者的話，頁面對了、圖卻可能還是舊的）。⚠⚠⚠ **2026-09-09 再一件：「標題　芳仁牙醫 科別與醫師　9位醫師 6個專科　網址　拿掉」** —— 標題、上下兩條分隔線、窄帶那兩個數字與 fangren.net 全部不畫。⚠ **他是對的，而且三格擺在一起才看得出來**：上面那格門診表已經寫著芳仁牙醫，這一格再寫一次診所名 ＝ 同一個名字在同一個畫面上出現兩次。⚠⚠ **那三樣佔的高度不是零，所以立刻長出一個新的設計項：空出來的要不要用掉**（第 28 條 ②）—— **沒有自己放大**，多一把倍率的尺。⚠⚠⚠ **而卡住的是「寬度」不是「高度」**：拿掉標題空出來的是上下，可是每一列的圖案、科別名、醫師名擠在**同一行**、都不折行 —— 最寬的那一列（**一般牙科・定期檢查**，四位醫師）要 **863px**／可用 **1000px** → 天花板 **1.159 倍**。**而那一列會是最寬的，正是上一輪把三位跨科的補進來造成的** ＝ 第 28 條 ② 又一次。尺三格（**只掛在 Ⓔ 上，Ⓕ 的第一行逐字相同、天花板一模一樣**）：Ⓔ 現在這樣（小格 410px 上 科別 13.7／醫師 11.8／專長 **11.0**）／Ⓔ3 ×1.08（14.8／12.7／11.9）／**Ⓔ4 ×1.15 頂到上限**（15.7／13.5／**12.7**）——**專長那一行一直貼著 10px 的地板，放到 1.15 才第一次離開 12px**。⚠ 放大是**整組等比例**（字級、圖案、縮排、列距、小塊內距一起乘），不是只放大字；小塊的框線維持 1px。⚠⚠ 守門新增三道並負向測過：**產生器不准再出現 `class="hd"／"rule"／"ft"`**（畫面上多一行字不會讓任何一道尺寸守門翻臉）、**`detail` 以外不准出現 `fangren.net`**、**「還能放大多少」每次出圖都印**（第 28 條 ④）。⚠ **「詳情」第一行「9 位醫師駐診・6 個部定專科」刻意留著** —— 圖上不寫之後這張圖沒有一個地方講得出診所有多少人，而「詳情」正是接這種事的地方；**要不要一起拿掉是他的決定，沒有自己動**。⚠⚠⚠ **2026-09-09 定案：「選 F 但治療項目不要套色 維持淡色 框起來，醫師名字等級都一樣 不要分色，空出來的空間要好好運用」** —— 三件各自獨立，加上一件跟著長出來的。**① 排法定 Ⓕ**（Ⓔ 拿掉，留在 git：`git show c6b7a66 -- drafts/channels/post-docs.mjs`）。**② 專長小塊不吃科別色**（字柔墨、框 `--rule`）——⚠⚠ **代價與收穫在同一句話上：整張圖的彩色從此只剩最左邊那七顆圖案**，而那正好是好事：**那七顆是圖例、要對得上門診表那一張**，小塊也上同色時等於同一列有兩種東西在說同一件事、彩色的份量壓過資訊。⚠ **所以圖案不可以跟著淡化**（守門只擋小塊）。**③ 九位醫師同一階墨**（`tag-off` 那一套整組拿掉）——⚠ 誰是本科的仍然看得出來，**因為本科的排前面**：36-13 那個順序決定因此從「錦上添花」變成**唯一的線索**。**④ 空出來的高度做成一把尺**——⚠⚠⚠ **而它有兩個方向、卡在不同的東西上**：**字要多大卡在「寬度」**（最寬的一列 863px／可用 1000px ＝ 天花板 **1.159 倍**）、**列距卡在「高度」**（那才是空出來的那一塊）。**使用者說的「空出來的空間」是高度，可是直覺會想拿它放大字，而放大字一點都用不到那塊高度** —— 兩個方向要分開給，不然那把尺是髒的。四格（同一個排法，括號是**小格 410px 上**的字級，地板 10px）：Ⓕ1 現在這樣（13.7／11.8／**11.0**，內容 803）／Ⓕ2 只放大 1.15（15.7／13.5／12.7，917）／**Ⓕ3 放大 1.15 ＋ 列距拉開（建議）**（15.7／13.5／**12.7**，**994**）／Ⓕ4 只拉開列距（13.7／11.8／**11.0**，957）。⚠ 放大是**整組等比例**（字級、圖案、縮排、列距、小塊內距一起乘）。⚠⚠ 守門新增兩道並負向測過，擋的都是「加回去不會讓任何一道尺寸守門翻臉」：**小塊又套回 `TONE`**、**醫師又出現 `.xd`**。✅⭐ **2026-09-09 定稿 Ⓕ3 並匯出**（使用者：「選 F3　給我圖片」）——**要貼的檔案是 `preview/line-post-docs/fangren-docs-1080.png`**（1080×1080・PNG，同看診時間與地圖那兩張的命名；另外兩張是模擬圖 `slot-410.png`／`profile-3up.png`），「詳情」欄照 `detail.txt` 貼、**不要重打**。挑定的兩個值**寫回常數**（`K0 = 1.15`、`MPAD0 = 13.5`），守門多一道逐字比對那一行 —— 不然「這一頁畫的」和「他挑的」會分家。量到：科別 **15.7px**・醫師 **13.5**・治療項目 **12.7**（小格 410px 上，地板 10px）、內容高 **994／1000**、最寬的一列 **992／1000px**（還能放大 1.008 倍 ＝ 到頂了）、折行 0/7、七科的圖案顏色與形狀逐格 ＝ 門診表那一張。⚠ 規格頁從四格收成三張圖，**四格的數字留在 README 36-15 與 git（`git show e71fd3d`）**；⚠⚠ **面板不可以跟著一起收**（第 28 條 ④）。⚠ 順手修掉一個「案數一改就留白」的：`slot-410.png` 的畫布寬原本寫死兩欄，收成一格之後右半是一大塊底色，**而長寬比對得上實檔、守門抓不到**。⚠ **還開著的四件都不是這張圖的事**：「詳情」那兩個數字要不要拿掉、「假牙重建」的縮寫沿用門診表那張（站上一個字都沒動）。⚠⚠⚠ **2026-09-09 最後一輪：三張圖的浮水印接成同一顆圓**（使用者：「這是三張呈現的樣子　內容暫時不會調整　但 logo 浮水印要選一樣的　讓這三張呈現一個完整的 logo。第一張是門診表 logo 要壓在下方，左下是科別醫師 logo 壓在右上，右下是地圖停車 logo 壓在左上。logo 用門診表那張圓圓的。」）—— ＝ 一顆圓標誌**騎在三格中間那個十字縫上**（圓心在合成座標的 411.5, 411.5），大格看到上半、左下看到左下那一象限、右下看到右下那一象限。**幾何只有一個出處 `drafts/channels/wm-triptych.mjs`**，三支產生器一律 `wmFor("hours"|"docs"|"map")`，濃度統一 **4%**（門診表那張本來是 5%，接在一起會在縫上一深一淺）、形狀統一 **`r3c1`**（小格左那張原本挑的 `r1c2`／壓左上因此**不再生效**，那把尺留著只是紀錄）。⚠⚠⚠ **這一輪最貴的一課：同一個 px 寬度在三格上不一樣大** —— 大格 823÷1080 ＝ **0.7620**、小格 409÷1080 ＝ **0.3787**，差 **2.012 倍**，所以「三張都畫 660px 的圓」會變成一顆大的配兩顆小的、接不成一顆；**小格那兩張在原圖上就得畫成兩倍大**（直徑 500 時：門診表 656.1px、兩個小格各 1320.3px）。**通則：一個東西橫跨好幾個會被縮放的容器時，尺寸要寫在「合起來」那個座標系裡，再各自除以自己的比例尺 —— 不要在每一張圖上各寫一個 px。**⚠⚠ 三格的尺寸是**從使用者的後台截圖逐像素量的**（iPhone 1125×2436：大格 1023×508、小格 508×508、三道縫都是 6 → 換算回 823 那一組 ＝ 大格 823×409、小格 409×409、縫 5，而且 **823 ＝ 409＋5＋409** 剛好對得起來）。⚠⚠⚠ **那張截圖順手回答了 34-7 那個開著很久的問題：大格是 cover 裁切不是留白** —— 所以門診表那張的「壓在下方」是**看得到那一段的下緣**（原圖 y≈812）不是 1080 的底邊；那件事一直是我們自己推的、沒有人驗過，**現在收掉了**。✅ **直徑 2026-09-09 定案 500**（「選 500　匯出給我」）＝ 大格寬的 61%、門診表原圖畫 656.1px、兩個小格原圖各畫 1320.3px。尺是 `node drafts/channels/post-triptych.mjs`（380／500／620，**每個直徑四張：排在一起 ＋ 三張各自獨立** —— 使用者：「三個大小的版本都要作成獨立的和排在一起的給我看」）；⚠⚠⚠ **「獨立看」和「貼上去」只有門診表那一張會差**：大格是 cover 裁切，圓的下半含牙洞貼上去一律看不到，三個直徑都只露出上面 49~50%，而獨立看整張 1080 分別是 100%／91%／83%。⚠ 那 12 張是**尺**不是成品，**挑定之後已經清掉**（數字留在 README 36-17），守門有兩道在盯：`tri-*.png` 不准留在那個資料夾、`wm-triptych.mjs` 的 `DIA` 要真的是 500—— ⚠⚠ **它跨三支產生器**，所以直徑用環境變數 `WM_DIA` 傳、一次跑完三張，**跑完再用預設值跑一次**（不然 repo 裡留下的是尺的最後一格，而規格頁說的是定案那一格，兩邊分家且沒有人會發現）；`post-docs.mjs` 清檔時因此要跳過 `tri-*.png`。⚠ 模擬圖的版面也只有一份（`tripleHtml`）—— 三支各畫一份的話，哪一支的縫或順序寫錯就會靜靜地畫出一顆接不起來的圓。⚠⚠ **後台目前的排序不對**：使用者那張截圖裡站在大格的是科別醫師，要照他自己講的順序把**門診表排到第一則**（那個模組有 `↑ ／ ↓`），不然三張的浮水印會錯位。⚠ 守門 `checkOne()` 把三格的圓算回合成座標比一次（三格都回 411.5／411.5、直徑 500），另加兩道：**產生器一定要 import `wm-triptych.mjs`**、`svg.wm` 裡**不准出現 `right:`／`bottom:`**（自己寫一份的話每一道尺寸守門都會過，只有把三張拼起來看才看得出來）。⚠ **還開著一件**：34-7 的另一半（小格是不是也照 410×410 裁，後台按一次「預覽」就知道）。⚠⚠⚠ **2026-09-09 再兩件**（使用者：「科別醫師 一般牙科．定期檢查 只要一般牙科就好」、「科別醫師也給一點貼文的文字說明」）。**① 縮寫**：做法沿用 09-07 那條「植牙・假牙重建 → 假牙重建」——**只換貼文那幾張圖上的顯示名**，`index.html`／`topics/`／`tools/topic-copy.mjs` 一個字都沒動。⚠⚠⚠ **門診表那一張跟著改了，那是刻意的**：`DISP` 上面本來就寫著「同一組貼文所以沿用同一份對照表（不然兩張圖對同一科有兩個名字）」，而三格是擺在同一個畫面上一起看的 —— 照字面只改一張，主頁上就會同時出現「一般牙科」與「一般牙科・定期檢查」。**要退回去只要把 hours 那一行刪掉。**⚠⚠ 守門從「各寫一份」改成**讀產生器那一份**（原本 `check-post-docs.mjs` 自己抄了一份 `DISP`，改了縮寫那一道會對著舊名字驗而且沒有人會發現），並**逐字比對 `post-hours.mjs` 的那一份**，兩張不一樣就 throw；兩道都負向測過。⚠⚠⚠ **這一改讓「卡住的是寬度不是高度」翻過來了**（第九節第 28 條 ② 又一次）：「一般牙科・定期檢查」正是**最寬的那一列**（四位醫師），收成四個字之後 **777px／可用 1000px、寬度天花板從 1.008 跳到 1.287 倍**，可是**高度早就用滿**（內容 994／1000，只剩 1.006）——**放大這條路仍然走不動，只是換了一堵牆**；面板因此改成**兩邊都算、印出誰先撞牆**，不再寫死一句結論。⚠ 字級一個都沒動（15.7／13.5／12.7px）。**② 「詳情」補一段說明**：原本只有數字那一行加八條網址、讀起來是一張清單；**八條網址三案完全相同，差的只有開頭**（`LEADS`，`DOCS_LEAD=a|b|c` 切）——Ⓛa 只有事實／**Ⓛb 多一句怎麼看（現在放的）**／Ⓛc 對著人講。⚠ 兩條規矩沿用門診表那一張：**第一行不要寫「這是一張科別與醫師表」**（說明欄只先露一兩行）、**不可以寫「有問題歡迎私訊」**。⚠ **Ⓛc 刻意不寫「打電話問我們」** —— 這張圖上已經沒有電話，而號碼的唯一出處在 `index.html`，為了一句話寫死一組號碼不划算。⚠⚠ 規格頁那塊「三案並排」是**展示品不是資料**，class 是 `pv-txt pv-alt`（守門拿第一個 `class="pv-txt"` 去對 `detail.txt`，同名會被吃掉 —— 同第三十五節那個 `pv-cut`）。⚠ **還沒挑**：Ⓛa／Ⓛb／Ⓛc，以及「詳情」第一行那兩個數字要不要一起拿掉。 | 第三十六節 |
| **⭐ 同一組貼文圖搬到 Facebook** | ⚠⚠⚠ **這一格不是 LINE** —— 同一組 1080 見方的貼文圖也貼到診所的臉書粉專上（2026-09-09 使用者第一輪：「這三個圖片似乎也很適合放在臉書上　看一下臉書的圖片尺寸　你們討論要調整成什麼樣子　做好給我看」；第二輪：「**因為臉書沒有像 line 那樣的版面　我比較傾向用之前的版本　單一張看順眼就好**　但你們還是把這兩張和地圖弄成臉書適合的尺寸　**地圖也挑一個不一樣的浮水印**壓上去」）。規格頁 **`/preview/fb-post/`**，產生器 `node drafts/channels/post-fb.mjs`，守門 `node drafts/channels/check-post-fb.mjs`，**推導在 `drafts/channels/README.md` 第三十八節**。⚠⚠⚠ **結論是「尺寸、版面、文字都不用改，只有浮水印要改」** —— 三張圖的內容**一個像素都沒有另外畫**，用的是同樣那三支產生器、只把浮水印切成臉書版（`WM_MODE=fb`）；另做一套就會長出第二個真相（哪天改了排班，臉書那一份還停在舊的）。⚠⚠⚠ **第一版做錯了方向，這一條最值得記**：我把浮水印做成「每一張各自一顆**完整**的標誌、整顆進到畫布裡」，推理是「一塊往畫布外流出去的弧讀起來是一團淡灰不是標誌」—— 他的答覆是**用之前的版本就好**。「之前的版本」＝ 三格拼一顆**之前**那一組，每一張自己一顆、**而且本來就是切出去的**，是他 2026-09-08 在那兩頁上一格一格挑定的（門診表 `r3c1`・660×660・壓右下 right −60 / top 470・**5%**、壓在表的後面；科別醫師 `r1c2`・952×469・壓左上・左邊切 440・4%、壓在最上面）。**通則：一個看起來是「壞掉」的性質，可能正是使用者挑定的樣子** ——「切出去」在三格版裡確實是壞的（那一塊弧只有三格並排才拼得起來），在「一張一顆」的版本裡卻是刻意的裁法，兩件事長得很像、成因完全不同。**判斷方法：去 git 看那一張圖在三格版之前長什麼樣，不要從第一原理重新推一個。**⚠⚠ **地圖那一顆是唯一一個新決定**：它原本和科別醫師共用 `r1c2`，理由是「兩個小格並排，浮水印不一樣會讀成兩件事」——**那個理由在臉書上不成立**（兩則分開貼），所以他指定換一顆。做成一把尺（`WM_FB_MAP`，五格）：**`r2c2` 762×572・長寬比 1.33（建議）**／`r1c1` 679（⚠ 和門診表同比例）／`r3c2` 673（⚠ 同上）／`r2c1` 965×477（⚠ 和科別醫師同比例）／`r2c3` 1193×387。⚠ **寬度不可以沿用同一個數字**（九顆長寬比 1.00~3.08、墨佔外框 59.6~83.2%，同寬的話細長那幾顆會輕很多）——一律按墨的面積正規化，基準 `r3c1` 畫 660，唯一出處 `preview/line-booked/wm-sizes.json`；⚠ **切掉多少不是另一把尺**：一律切到**看得到 512px** 為止 ＝ 科別醫師那一張切 440 之後剩下的寬度，換形狀時份量才不會跟著跳。⚠ **尺寸不用改**：臉書動態的方形照片貼文原生就是 **1080×1080**，這三張正好是；⚠ **常看到的 1200×630 是連結預覽卡的規格不是照片貼文的**，不要拿來套；4:5（1080×1350）在手機上佔更多畫面，但這三張的內容是方的，拉成 4:5 只會在上下多兩條空白。檔案 100／189／123KB（上限 8MB）。⚠ 這幾個數字是**二手的**（Meta 自己的說明頁這個容器連不出去）。⚠⚠ **版面不用改而且反而變好**：拿他那張後台截圖換算「佔螢幕多寬」——LINE 大格 **90.9%**、小格 **45.2%**、臉書照片 **滿版 100%**，所以門診表**一樣大**、另外兩張**變成兩倍大**；而且門診表那張在 LINE 大格是 cover 裁切、只看得到中間 537 列（**標題「芳仁牙醫開診時段」與最下面那行電話在 LINE 上根本看不到**），臉書整張都看得到 —— **那兩樣是免費拿回來的**。⚠⚠⚠ **文字不用改，但紅線回來了**：三則的第一行本來就講完那件事（折疊線之上露出 3／4、3／11、3／13 行），而**臉書有留言區**（LINE 主頁那個模組沒有，第三十四節確認過）、這個帳號一樣沒有專人回覆 —— **第十一之三節那條在這裡回來了**，現在一句都沒踩到，守門擋的是「日後有人為了臉書多寫一句」。⚠⚠ 守門踩到這條線的老坑（第四次）：**掃字撞到自己的說明**（頁面上解釋「為什麼不可以寫」的那一句本身就含著禁語）——那一段包成 `<span data-red>` 掃之前剝掉，**而且要驗那個標記還在、只有一段**，不然亂加一通就等於把守門關掉。⚠⚠ **一件只有臉書才看得出來的**：地圖那則的**前兩句，圖上已經印著一模一樣的兩句** —— LINE 那邊「詳情」是收起來的所以看不出重複，臉書的文字**就貼在圖的正上方**。**沒有自己改掉**（那一則的文字和 LINE 那一份是同一個出處），做了兩張模擬並排讓他挑。⚠⚠⚠ **2026-09-09：改放相簿**（使用者：「但還是有相簿功能欸　我感覺要放在相簿裡　這樣要怎麼做呢　**我是覺得有沒有被裁剪好像還好**」）—— **他推翻的是我上一輪寫的「三張分開貼不要一次貼三張」，而他是對的**：那條規則只顧著「會不會被裁」，而**裁切只影響動態上的縮圖**，點進去看到的一直是完整原圖。⚠⚠ **相簿換到的正好是這三張圖存在的理由** —— 第三十四節那個判斷是「當成主頁上那三格永久櫥窗」，而**臉書的動態是流水**，粉專上唯一能長期站著、隨時翻得到的地方就是相簿。⚠⚠ 還有一件**非用相簿不可**、我寫那條禁令時根本沒想到的：**一次選三張貼成一則，整組只有一段共用的文字**，而這三張的說明各不相同 —— **相簿裡每一張各自有自己的說明欄**，那三段字才貼得上去。⚠⚠⚠ **通則：一條「不要做 A」的規則，要連它擋掉的東西一起寫下來** —— 只算 A 的代價、不算不做 A 會失去什麼，日後就沒有人有材料可以推翻它，而它有一天一定要被推翻。⚠ **怎麼建**（二手資料，Meta 的說明頁這個容器連不出去）：粉專 → 相片 → 相簿 → 建立相簿 → 命名（建議「診所資訊」，不要用月份年份）→ 三張一起上傳（門診表 → 科別與醫師 → 位置與周邊停車）→ 每一張各自貼它自己那段說明 → 發布；已經單獨貼出去的照片**事後也搬得進相簿**。⚠ **相簿之外還要做的一件：門診表那一張另外單獨再貼一則、釘選** —— 建相簿在動態上是一則**格狀的多圖貼文**，釘它等於釘一組縮圖。**留言區那條照舊。**⚠⚠ 順帶一件被這一改弄成半真半假的（第九節第 28 條 ②）：地圖那顆浮水印可以和科別醫師不一樣，理由是「兩則分開貼不會並排」——**相簿的格狀縮圖裡它們又並排了**；**沒有改**（那顆是他挑的，而且只有 4% 的墨、縮到相簿縮圖幾乎看不見）。⚠⚠⚠ **守門最重要的兩道是讀真的畫出來的像素**：`post-fb.mjs` 跑完會**用預設值再跑一次**把 LINE 那三張還原成三格版，少了那一步三張就接不成一顆圓**而且每一張看起來都很正常** —— 第 ⑦ 道讀原始碼，第 ⑨ 道真的去解那六張 PNG：**ⓐ 同一格的 LINE 版與臉書版不可以逐位元組相同**（相同 ＝ 還原沒有跑，**這一道最強、不需要容差**）、**ⓑ 那一層淡墨的重心要落在各自的幾何算出來的位置**（±100px；⚠ 不可以比對某一個色階的精確值 —— 4% 與 5% 合成出來只差一階，而 Chromium 的進位在不同版面上不一定一樣，要用一段區間）。**通則：一個「有沒有還原」的守門要量產出物不要量原始碼** ——原始碼對的時候，檔案仍然可能是上一輪跑剩的。⚠⚠⚠ **2026-09-09 再一輪：牙洞**（使用者：「**地圖那個 logo 選得不錯　不過 logo 原本的牙洞在左上　這裡看不到了　把牙洞移到右上**」「**科別醫師那個 logo 在左上　移到右下比較好**」）——**兩句話講的是同一件事**：牙洞是這顆標誌**唯一的識別特徵**（外框只是一團圓角的有機形狀，九顆彼此本來就只差比例），所以「切出去」可以、**切掉牙洞不可以**。⚠⚠ **而這件事不會讓任何一道守門翻臉**（長寬比、等重、重心、檔案大小、八個寬度全部照過）——**只有把圖打開看才看得出來**，所以是他先看到的。做法：把每一顆的洞**量出來當資料**（渲染成黑的、從邊界 flood fill，剩下沒被淹到的白色連通區就是洞），寫進 `wm-triptych.mjs` 的 `HOLE` 表：r1c1 29.4/81.5、r1c2 71.3/75.7、r2c1 50.0/20.3、**r2c2 17.5/23.9**、r3c1 56.7/81.9、r3c2 50.0/24.0（％）。**地圖**（r2c2）的洞在左上，而這一張切掉的正是左邊那 250px ——**左右鏡射**之後落在畫布 (378.6, 93.7) ＝ 右上。**科別醫師**（r1c2）從壓左上搬到右下，**而且非連著轉 180° 不可**：它的洞在形狀的右下角（71.3/75.7），原樣搬過去之後看得到的是形狀的左上那一塊、洞算出來在 x 1246.8（畫布只有 1080）；轉 180° 之後**看得到的逐像素就是原來那一塊、只是換到對角**，洞跟著回來（841.2, 760.8）。⚠⚠⚠ **同日稍晚使用者推翻了「一定要轉」那一半**：「**科別醫師那個 logo 不要轉　牙洞不要出來沒關係**」—— 他是對的，**一顆倒過來的標誌比一個看不到的洞更明顯**；洞是識別特徵沒錯，但**方向也是**，而我只把其中一件當成不可退讓的。**通則：一條「某某一定要如何」的規則，要先問它是為了什麼**（這裡是「讓人認得出那是芳仁的標誌」）——**滿足它的手段如果傷到同一個目的，那條規則就該退成偏好**。做法：`hole: "show" | "cut"` **由每一格自己宣告**、守門照宣告驗，不再是全域的硬條件；`FB.docs` 的翻轉回 `false`（壓哪一角、切多少、濃度、形狀一個數字都沒動），所以那一張看得到的是形狀的**左上那一塊**、洞落在畫布外的 x 1246.8。`WM_FB_DOCS=flip` 仍然產得出轉過的那一版，**頁面上沒有**。⚠ 面板與 `wmDesc()` 那一句跟著改口（`cut` 的印「牙洞在畫布外＝他選的不轉」，**不要印一個看起來像壞掉的座標**）。⚠ 守門兩道新的並負向測過：**docs 不准再被轉**（轉回去畫面照樣正常、每一道尺寸守門都會過）、**`FB.docs` 一定要宣告 `hole: "cut"`**。⚠⚠⚠ **翻轉一律做在整個 `<svg>` 元素上**（`transform: scale(±1, ±1)`），外框與洞一起翻 ——**絕對不要只翻 `<path d>`**：洞是 `fill-rule: evenodd` 挖的，只翻外框的話洞會落到形狀外面、**靜靜地消失而且不報錯**（`remind-marks` 那一輪已經踩過一次）。⚠ `post-map-door.mjs` 的位置字串是自己從 left/top 拼的（不是吃 `WM.css`），**翻轉要記得一起帶上**。⚠ 守門加三道並負向測過（拿掉 map 的鏡射／把 `HOLE` 填成形狀正中央／把 docs 轉回去）：① 宣告 `show` 的那幾格，洞心要落在畫布裡（離邊 ≥8px）② PNG 上洞心那一小塊**沒有**被淡墨染到（<20%）③ 從洞心往形狀中心走 13% 的地方**有**（>60%）—— ② 和 ③ 兩件都要，只驗②的話整顆沒畫出來也會通過。⚠ 面板那一句收成 **`wmDesc()` 一份**（三支產生器共用）：寫法一分家，同一顆浮水印會有三種說法，而且改欄位名時會留下「切掉 undefined」這種**不報錯的**殘留（這一輪就留了一個）。⚠ **還開著三件**：地圖那顆浮水印挑哪一個、地圖那則的前兩句要不要拿掉、以及兩張小的上面沒有寫診所名字（科別醫師那張的標題是他 09-09 指定拿掉的，理由「上面那格門診表已經寫著芳仁牙醫」在臉書上不成立 ——不過**臉書每一則貼文上面本來就有粉專的名字與頭像**，所以只有在圖被單獨存下來、轉傳出去時才看得出差別，**沒有自己加回去**）。另外門診表那張上下各留 152px 的空白是為了 LINE 大格的裁切留的，臉書整張都看得到，**要不要把內容放大用滿還沒問他**。⚠⚠⚠ **2026-09-09 再一件：關鍵字標籤**（使用者：「每張相片的文字說明是不是也能 tag 關鍵字？你們想一下」）—— **可以，但三件會改變結論**：① **hashtag 在臉書多半是「出口」不是「入口」**（點下去離開這個粉專、進到滿是別人貼文的標籤動態；臉書的觸及靠演算法與分享，標籤搜尋量很小）——**唯一不會把人送走的是只有我們在用的那一個**（`#芳仁牙醫診所`），**所以判準不是「加幾個」是「這一個把人送去哪裡」**；② **紅線在這裡最容易踩，而且是既有的那一條** —— `#推薦`／`#最好`／`#第一`／`#無痛`／`#免費` 這一類**評價性或招徠性**的字醫療機構不要碰，和第三十四節「⑫ 促銷優惠抽獎 ＝ 紅線」同一族（`TAG_RED` 在掃，兩邊各一道）；⚠ `#斗六牙醫` 是事實描述不是評價，所以 Ⓒ 案有它，**但要不要走是他的決定**；③ **臉書專屬，所以一律 append 不改原始檔** —— 三則的文字是從 LINE 那三份 `detail.txt` 讀回來的，標籤接在最後、前面空一行，**為了加標籤去改它們兩條線就分家**。⚠ 排版三條：接在最後（前三行是折疊線之上）、一則最多三個、標籤裡不能有空白或全形符號（會截斷；遇到空白就結束，「#芳仁 牙醫」只會剩「#芳仁」）。三案 `FB_TAGS=none|brand|topic`，**預設 `none`**（他問的是「想一下」不是「改」）：Ⓐ 不加／**Ⓑ 只加品牌（建議）**／Ⓒ 品牌＋主題。⚠⚠⚠ **順手查到的那一件比標籤本身值得做：門診表那一則的說明從頭到尾沒有出現「芳仁」「斗六」或「牙醫」** —— LINE 上不要緊（那段「詳情」收在診所自己的主頁裡），**臉書每一則貼文都是獨立的東西、會被分享被截圖，而那一則正是要釘選的那一則**。**用正文補比加標籤有用得多**（標籤在折疊線之下，正文第一行在上面），**但那三份是兩條線共用的、沒有自己動**，最小改法擺在頁面上讓他挑。⚠⚠ **通則：問「要不要加關鍵字」之前，先去看正文裡已經有哪些字** —— 這一輪真正的缺口不是少了標籤，是最重要的那一則沒有寫自己是誰。⚠ 守門第 ③ 道改成**先切掉最後那一行標籤再逐字比本文**（整行每一段都以 `#` 開頭才算標籤，不然本文最後一行有個 # 會被誤吃），標籤另驗 ≤3 個・第一個一定是 `#芳仁牙醫診所`・不准踩紅線。⚠ 這一輪 `*/` 那個坑又踩一次（第九節第 8 條，這條線第三次）。⚠⚠⚠ **2026-09-09 定案：臉書的相簿貼文也是三格，所以照 LINE 那樣拼一顆完整的標誌**（使用者貼了粉專上那一則的截圖：「結果貼文也有三格是欸　那還是像 line 那樣做一下讓同一個 logo 完整顯示好了　給我適合放在臉書貼文的圖片」）。⚠⚠⚠ **這推翻了上面那句「臉書沒有像 LINE 那樣的版面」，而那句話是我從第一原理推的、沒有量過** —— 臉書把「一次三張照片」排成的正是「上面一張寬的、下面兩張方的」。**通則：「那個平台沒有這種版面」是一句一張截圖就能推翻的話，在拿它當前提之前先貼三張上去看一眼**；更該記的是那一則早就貼出去了，我卻沒有請他回報就接著做下一件。⚠⚠ **量出來的那一組**（`TRI_FB`，從他截圖逐像素量 —— ⚠ **臉書深色模式，縫是暗的不是白的**，第一版照白色找回「一條都沒有」）：截圖 838×1063・大格 **829×413**（x 3—831・y 136—548）・縫 **3**・小格 **413×413**，而且 **829 ＝ 413＋3＋413** 對得起來；和 LINE 那一組（823／409／5）**差不到 1%**（圓畫出來差 0.7~1.0%），**但兩份各記各的** —— 兩邊都是量出來的事實，寫成同一份等於宣稱「它們一定一樣」。⚠ 大格一樣是 **cover 裁切**（標題與電話那行都不見了）。⚠⚠ **順序不用重排**（他那一則已經是大格門診表、左下科別醫師、右下地圖停車），**但三張要重新上傳**（貼上去的是「一張一顆」那一版，接不成一顆圓）。⚠ **模式現在三種**：預設 ＝ LINE 三格／`fb` ＝ 臉書三格（定案）／**`fb-solo` ＝ 一張一顆那一版**（09-08 挑定的值，**沒有刪、留著當退路**）——所以 `FB`／`HOLE`／`wmFbFor()` 全部留著，上面那兩輪的牙洞推導對 `fb-solo` 仍然成立、對現在這一版不適用。⚠ 跟著收掉：地圖浮水印那把五格的尺（三張共用同一顆圓，沒有「換哪一顆」了），規格頁 13 → **9 張圖**；新增 `fb-3up.png`（三張拼起來）。⚠ 守門負向測過兩種：⑦ 多一道「真的用 `WM_MODE=fb` 跑三張」、⑧ 整段改寫（`TRI_FB` 四個數字、`829 ＝ 413＋3＋413`、**`checkOne(DIA,"fb")` 三格算回合成座標要同心同大小** ＝ 最強的一道，外加三道守著 `fb-solo` 那份紀錄）、⑨ 的牙洞那一段拿掉。⚠⚠ **撈那張截圖多學到一件**（第九節第 26 條的補充）：**使用者貼的圖也可能是 WebP**（`RIFF…WEBP`，PNG／JPEG 那兩個魔術位元組都不是，撈出來變 `.bin`），而容器裡沒有 PIL，**解 WebP 要靠 Chromium**（畫進 canvas 再讀 `getImageData`）。 | 第三十八節 |
| **商家貼文・還要貼什麼**（盤點與評分） | ⚠⚠⚠ **這一份 09-07 只在對話裡給過，09-08 才補進檔案**（`drafts/channels/README.md` **第三十四節**）—— 使用者 09-08 說「前面的建議其他項目和評分再拉出來一次看看」，我第一次去拉，**拉出來的是第十四節那張「訊息」的表**（那是另一件事），因為檔案裡根本沒有這一份。**通則：一份盤點如果只在回覆裡出現過，下一次要用的時候一定找不到 ——「要不要寫進去」問完就要寫，不要等做完圖再說。**⚠⚠ **評分軸和訊息那邊不一樣**：貼文不進聊天室、不計則數、不花錢，所以「−打擾」近乎 0，**扣分軸換成「−會不會過期」**（那八則訊息全是事件觸發、寫好放著就永遠成立；貼文有時間戳，而「誰在管文字」是⚠ 沒有人）。十二項的分數：**① 看診時間 90（✅ 已定稿匯出）**／⑤ 國定假日休診公告 74（沒寫）／② 颱風臨時休診 72（訊息版定稿、**貼文的圖沒做**）／③ 位置與周邊停車 69（**2026-09-08 定案 `door-c`**，見上一列）／④ 七科介紹 63（七張分享圖現成，**1.91:1 要重裁成方的**）／⑥ 衛教懶人包十張 59（圖已上線）／⑦ 費用制度異動 56／⑧ 醫師介紹 52／⑨ 站上 13 篇文章 47／⑩ 環境設備 33／⑪ 節慶花絮 18／**⑫ 促銷優惠抽獎 ＝ 紅線**。⚠⚠⚠ **最重要的判斷：不要把貼文當成「開始做內容」** —— 那會撞上兩件已定案的事（診所不做定期推播、沒有人管內容）。做法要反過來：**當成主頁上那三格永久櫥窗**，貼三則「三個月不動也不會變錯」的東西然後不管它（大格＝看診時間 ✅／小格左＝位置與周邊停車／小格右＝主題與科別七科），事件來了臨時加一則、**事過就撤**。⚠ **不要一開始就排七則**。⚠ 紅線剩兩條（不促銷、不出現病人可辨識的內容）—— 留言那條 09-08 確認不適用 | 第三十四節 |
| **⭐ 圖文選單・綁定手機**（第三條線的起點） | ⚠⚠⚠ **這不是訊息也不是貼文，是聊天室最底下那條常駐的選單**（圖文選單／rich menu）。起點是 2026-09-09 使用者拿了**另一家診所**（同一個廠商）的截圖 ＋ 我們那張綁定插圖：「把他合在這個頁面的下方　取代這個　點此 sign in 圖片看看　或是　在 line 商家帳號裡，這個下方的圖片尺寸可以變動嗎？**我把我們做好的圖片合進去被裁剪成很奇怪**」。⚠⚠⚠ **「被裁得很奇怪」是量得出來的**：那條選單量到 **1125×378 ＝ 2.976:1** ＝ LINE 的**小型版型 2500×843（2.966:1）**，而插圖是 **1376×768 ＝ 1.792:1** —— 寬度對齊之後高度只放得下 768 → **464 列，四成被切掉**，切掉的正好是上面那排頭和下面的腳。**不是裁法沒調好，是比例差太多**，調裁切位置永遠治不好。⚠⚠⚠ **尺寸可不可以變要看是誰在設**（這一輪最重要的答案）：**後台**（LINE Official Account Manager）只有兩種版型、比例寫死 —— 大型 2500×1686（1.483）／小型 2500×843（2.966），各另有 1200 與 800 的等比版；**廠商用 Messaging API 送**則可以自訂，硬條件是 **寬 800~2500、高 ≥250、寬÷高 ≥1.45、≤1MB、JPEG 或 PNG**。**這張插圖 1.792 剛好過得了 1.45**，所以「整張圖一個像素都不裁」做得到、但**只有廠商那一側做得到**。⚠ 版型裡的**格子**（幾格、切在哪）自己切得了，**比例不行**。⚠ **通則（第三次記）：「做不到」要講清楚是誰做不到** —— 寫成「LINE 不支援」是死路，寫成「後台那個表單只有兩種版型」只是換一個人送。⚠⚠ **判準是「在手機上多大」不是「圖檔多大」**（同 og-topic-card 那一輪）：選單一律畫成螢幕寬，2500 的圖在 375 CSS px 上是 **×0.15** → 小型 **375×126**、大型 **375×253**。面板因此有兩欄，**兩欄各治一件事**：**看得到原圖多少**（Ⓧ 硬塞壞在這一格 ＝ **60%**）與**一個人頭在手機上多高**（Ⓐ 壞在這一格 ＝ **13.5px**，原圖那顆頭 86×82px）—— **只看其中一欄會挑錯**（Ⓧ 的人頭 22.3px 比 Ⓐ 大得多，但那是切掉四成換來的）。四格：**Ⓧ 現況**（小型硬塞・看得到 60%・**整條沒有一個字叫人綁定**）／**Ⓐ 圖 ＋ 字欄**（小型・94%・人頭 13.5px・主標 28.5px，**建議**，後台就設得了）／**Ⓑ 大型 ＋ 字帶**（97%・人頭 22.3px，圖最好看，**代價是吃掉 253px ＝ 手機畫面的三分之一，而且常駐**）／**Ⓒ 自訂 2500×1396**（＝插圖原比例・**100%**・**但整張都是圖、沒有地方放字**，要廠商送）。⚠ 中間走過一版「小型・拉近手勢」（放大 1.35 倍只留兩隻手指與那顆牙），**畫出來被否決**：患者變成一隻沒有身體的手臂、醫師的頭被上緣切掉 —— **那張圖的梗是「躺著的人」對「飛過來的一群人」，拉近就沒有了**，不要重走。⚠ **底色用插圖自己的奶油色 `#fcf4e4`**（掃出來占 21.7%），字欄與圖之間**接縫才會不見**；用站上的紙色會出現一條分界。**顏色一個都沒有新增。**⚠ 字帶那一行第一次就溢出 136px —— **算寬度要把 `letter-spacing` 算進去**（`.10em` 在 8.5 個全形字上就是多一個字的寬）。⚠⚠ **沒有把別家診所的畫面放上來**：只用了截圖最底下那條 LINE 自己的選單列（`drafts/line-richmenu-chatbar.png`，y 2196~2435），他們的圖文選單一個像素都沒有取（repo 是公開的）；選單上方那段聊天室底色掃出來是純的 `#1A1A1A`，填色就等於原圖。⚠ **官方文件那個網域這個容器連不出去**（egress 擋掉），上面那組硬條件是二手來源，**廠商一句話能確認、已經寫進那一頁的問題清單**。⚠⚠⚠ **2026-09-09 稍晚：模擬圖改成整支手機**（使用者：「提案頁也要放模擬在 line 裡的樣子」）——原本四格只畫選單上下那一小條，看不出它在整支手機裡佔多少；現在是 **375×812 CSS px**（出圖 1125×2436）：LINE 抬頭 → 今天 → **已定案的招呼圖卡**（真的產出檔，不是用 CSS 再畫一次）→ 選單 → 選單列。⚠⚠ **這一改讓 Ⓑ 大型的代價第一次看得見**：選單吃掉 253px 之後**招呼圖卡的頭圖被擠出畫面上緣**，所以聊天室寫成 `justify-content: flex-end`（貼著底部長）—— 那不是模擬做壞了。⚠⚠ **一個像素都沒有用他那張截圖**（聊天室、選單列、手機外框全部自己畫，只有 1125×2436 這個尺寸是量來的），`drafts/line-richmenu-chatbar.png` 已刪。⚠ 出 **JPEG** 不是 PNG（四張 PNG 合計 4MB，而 `/preview/*` 是 `no-store`）；`shot-*.png` 一起拿掉（整支手機那張已經把選單畫在一模一樣的 375px 上）。⚠⚠⚠ **守門差點被 `loading="lazy"` 掛住** —— 畫面外的 lazy 圖永遠不會開始載，「等 `complete`」的 Promise 因此永遠不 resolve、不報錯也不結束。**通則：任何「等圖載完」的量測，要先把 lazy 關掉。**⚠⚠⚠ **2026-09-09 稍晚：藥丸拿掉，Ⓐ 與 Ⓑ 各長成一把三格的尺**（使用者：「我喜歡 A 和 B　但 **B 的尺寸太大　讓歡迎頁的訊息被捲到上面去**　B 的文字提示字級和帶子要縮小／**A 很好　但不用點一下　這樣會讓人不知道點哪裡（雖然圖片也可以點）**　點一下拿掉後…字級感覺可以小一點　不然…**和圖片搶戲**」）。⚠⚠⚠ **他對 Ⓑ 的診斷是對的（招呼卡真的被擠出畫面），但「帶子縮小」治不了那個症狀** —— 大型版型是 **2500×1686 寫死的**，手機上永遠 **375×253**，帶子收多少都一樣；收小帶子換到的是**插圖變大**（97% → 99%）。**要讓選單變矮只有換版型 ＝ Ⓐ 的 375×126（一半）。**尺仍然照他說的做，但頁面開頭先把這件事寫清楚。**通則（第 28 條 ③ 的近親）：使用者指定一個做法來解某個症狀時，先算那個做法治不治得了那個症狀 —— 治不了就照做、同時把真正的解法擺在旁邊。**⚠⚠ **藥丸拿掉的理由**：它是整條選單上唯一「看起來像按鈕」的東西，而**整條本來就都可以按** —— 一顆假的按鈕反而把可按範圍講小了。⚠ **Ⓑ 帶子裡的「點一下」留著不是漏改成一致**（在那裡只是一行字的開頭，不會長出假的按鈕）。六格：**Ⓐ1 28.5/13.8・Ⓐ2 24.8/12.3・Ⓐ3 21.8/11.4（建議）**／**Ⓑ1 帶子 49.5・Ⓑ2 41.3（建議）・Ⓑ3 34.5**。⚠⚠⚠ **Ⓑ 那把尺有轉折點、不是愈小愈好**：帶子收到畫布上 **291px** 時圖欄正好 ＝ 插圖的 1.792，那是「看得到」的最高點（Ⓑ2 99%）；再收下去**裁法從上下切換成左右切**，Ⓑ3 掉回 **96%**。**通則：一把「愈小愈多」的尺要先算出轉折點，不然面板會出現一個看起來像壞掉的數字。**⚠ 副標的上外距跟著自己的字級走（`.24em`），字一小間距要跟著收，不然是「字變小、空隙沒變」＝ 只動了一半。⚠⚠ **圖片經濟**：七格都嵌 2500 那一份 ＝ 2.6MB 而 `/preview/*` 是 `no-store`，所以每格多一份 **`strip-<id>.jpg`（1125 寬 ＝ 375 的三倍）**給頁面擺，**整支手機那張只做三張**（現況 ＋ 兩族的建議）—— 那張回答的是「在整個畫面裡佔多少」＝ **版型**的事，字級那把尺在上面看不出差別。⚠ **守門：表的第一格要逐字對標籤，不可以只比圈號** —— 尺上有 Ⓐ1／Ⓐ2／Ⓐ3，只比 `Ⓐ` 的話三列會互相通過。⚠⚠⚠ **同一輪修掉一個「少一個圖檔就跑不完」**：等圖那一段寫成 `i.complete && i.naturalWidth ? null : new Promise(…)`，而**載不到的圖 `error` 燒完就是 `complete: true、naturalWidth: 0`** —— 於是替它掛了一個**永遠不會再有事件的 Promise**，守門停在那裡不報錯也不結束，**而那正是它最該說話的時候**（`bad` 裡早就記著「頁上引用的 X 不在」，但 `bad` 最後才印）。**通則：「等某件事發生」的條件只能問「它結束了沒」，不可以順便問「結果對不對」** —— 失敗也是一種結束，把「成功」寫進等待條件，失敗就等成了永遠；對不對是等完之後的事。⚠⚠⚠ **2026-09-09 再一輪：Ⓐ3 的圖欄那把尺**（使用者：「**Ⓐ3 的版本　文字和圖中間感覺還很有餘裕　圖是不是可以再往左一點或是放大一點**」）—— **他是對的，而且那塊空的是字級那把尺自己造成的**（第九節第 28 條 ② 又一次）：字欄 1080 是配 **Ⓐ1 的 28.5px** 定的，字收到 Ⓐ3 之後最寬那一行只剩約 705px，**字的右緣到圖空著 36.5px ＝ 整條選單的一成**；面板本來沒有這一欄，所以只有他看得到，**現在每次出圖都印「字到圖」**。⚠⚠⚠ **但他給的兩條路只有一條存在：「放大」不可能** —— 插圖**已經吃滿選單的高度**，cover 的倍率由高度那一項決定（`max(欄寬/1376, 843/768)`，後者一直比較大），**圖欄變寬不會讓圖變大、只會多露出左右兩邊**；要圖真的變大只有換版型（Ⓑ）。**通則：一張 cover 的圖「放大」和「露出更多」是兩件事，先算是哪一項在決定倍率。**⚠⚠⚠ **這把尺也有轉折點**（同 Ⓑ 的帶子）：圖欄 ÷ 843 ＝ 插圖的 1.7917 時是 **1510px（字欄 990）**，那裡整張一個像素都不裁；再往左圖欄比插圖還扁，**裁法從「切左右」翻成「切上下」**、切掉的正是後排的頭與椅腳。四格：Ⓐ3 現況（圖欄 213px・94%・字到圖 36.5）／Ⓐ3-2（220・97%・29.7）／**Ⓐ3-3 建議（227・100%・23.0）**／Ⓐ3-4（234・**96.8%**・上下切）。⚠ **字放不下不是這把尺的邊界**（990 扣掉內距還有 789px，最寬那一行 705px）——**卡住的是裁法翻面**。⚠ 整支手機那張換成 Ⓐ3-3；**Ⓐ1／Ⓐ2 的圖留著、字級那把尺還看得到**，沒有因為他挑了 Ⓐ3 就把前兩格刪掉。⚠⚠⚠ **同日再一件：「換 Ⓐ2　圖片往左放呢」—— 可以，而且推得到的位置和 Ⓐ3 完全一樣**。這件事一開始不明顯（Ⓐ2 的字比 Ⓐ3 寬，最寬那一行 760 vs 705px，直覺會以為推不了那麼左），**但擋住這把尺的不是文字放不放得下，是裁法翻面那個轉折點**：文字要放得下的下限是 `130＋760＋70 ＝ 960`、裁法不翻面的下限是 `2500 − 843×1.7917 ＝ 990` —— **後者先撞到**。所以兩種字級的下限都是 990，**Ⓐ2-3 與 Ⓐ3-3 的圖一模一樣**（圖欄 227px、100%、都切左右），**差的只有字級與剩下的空隙**（Ⓐ2-3 留 14.6px、Ⓐ3-3 留 23.0px）。⚠⚠ **通則：一把尺同時被兩個條件夾住時，要算出「哪一條先撞到」再回答** —— 只看「Ⓐ2 的字比較寬」會給出「推不了那麼左」這個錯的答案，而真正的天花板從頭到尾是幾何、和字級無關。⚠ 整支手機那張因此做了兩張（`MOCK` ＝ `x, a2c, a3c, b2`），擺在一起才看得出「要挑的只剩字多大」。⚠ 順手補一道 4 秒保險絲：任何等待都不可以沒有盡頭。✅⭐ **2026-09-09 定案 Ⓐ2-3 並改字**（使用者：「那就用 A2-3　不過綁定手機　改成　手機綁定　做好　給我預覽　**其他的不用再顯示出來**」）——**要上傳到 LINE 的是 `preview/line-richmenu/fangren-richmenu-2500.jpg`**（2500×843・252KB；另外兩張 `shot-richmenu.jpg`／`shot-richmenu-chat.jpg` 是模擬圖）。定案全部寫回常數：小型版型・字欄 990・主標 165／副標 82・主標「**手機綁定**」。量到手機上 375×126・圖欄 227×126・插圖 **100% 看得到**・主標 24.8px・副標 12.3px・字到圖 14.6px・一個人頭 13.5px。⚠ 「綁定手機」→「手機綁定」四個字沒有換寬度，版面一個數字都沒動。⚠⚠⚠ **順手長出一件要問廠商的**：選單列那一行是廠商設的「**手機註冊**」，而圖上寫「**手機綁定**」——**同一條選單上兩個詞**，而這條線其餘每一則一律用「綁定」；已寫進那一頁的問題清單。⚠⚠ **規格頁從 13 格收成 1 格**（圖 16 → 2 張），字級／圖欄／帶子／自訂／硬塞全部拿掉，**推導留在 README 37-4-3~37-4-7 與 git**（`git show 7420f27 -- drafts/channels/richmenu-bind.mjs`）——要回頭比從那裡取、不要重畫；`kind: "band"`／`"full"` 的程式碼留著（大型與自訂的退路），②「尺寸可不可以變」那一節留著（廠商要的事實）。⚠⚠ **面板不可以跟著收**（第九節第 28 條 ④）：只剩一格，但看得到多少／裁哪邊／一個人頭／字到圖每次出圖仍然要印。⚠ 守門三件跟著改：表沒有了 → **把那一格的每一個數字逐字在頁上找一次**；檔名**讀 numbers.json 記的那三個**；新增一道**主標一定要是「手機綁定」**（換回舊寫法畫面照樣正常、每一道尺寸守門都會過，只有讀字才看得出來）。⚠⚠⚠ **2026-09-09：使用者抓到一個 bug —— 六格被一格換掉了**（「現在這樣有個 bug　**本來是加入好友可以點選單**　要綁定才有提醒通知和約診查詢　**但現在變成馬上要綁定**　所以我認為應該要改成**綁定啟用完整功能**　就像森釉牙醫這樣　那我們的內文也要跟著調整」）。⚠⚠⚠ **他說的是文案，但根在結構，而且那是我做的**：這個帳號的選單本來是 **3×2 六格**（README 第 6-1 節就寫著：診所資訊／約診查詢／診療項目／醫師介紹／衛教資訊／基本設定，選單列「點我選服務」），**加好友就點得到**；而上面那一整輪我做的是**整條一格的綁定橫幅** ＝ 把六格全部換掉，**綁定之前一格都點不到**，其中三格的內容還是這條線自己剛做完的（診所資訊／主題與科別七科／衛教懶人包 12 張）。⚠⚠⚠ **我從頭到尾沒有查過那條選單現在長什麼樣，而檔案裡就寫著。通則（這條線第四次現場）：先問「這件事現在是誰在做、介面長什麼樣」** —— 25-13 把「他每次現打的字」當成模板、22-1 把「診所端後台的截圖」當成病人手機的寬度、34-2 把「主頁的模組」當成 VOOM 的貼文，**這一次是把「一條已經有六格的選單」當成空的**；⚠ 更該記的是**這四次的答案全都已經寫在自己的檔案裡**，不是查不到，是沒有去查。⚠⚠ **而那正好也是他那句話改不動的原因**：「啟用**完整**功能」的前提就是「**不完整的那一份現在就能用**」，整條被橫幅佔滿的話那個前提是假的 —— **文案和選單結構是同一件事，不是兩件**。**擺法三條**：① **建議 橫幅只給還沒綁定的人、綁定完成換回六格**（＝森釉現在的樣子）——⚠ **要廠商做分眾選單，那一題因此從「要不要做」變成「非做不可」**（不做的話綁好的人永遠回不到六格）／② 退路 **六格裡挪一格給綁定**（後台就設得了，最合理的是「基本設定」那一格，**但那一格底下是什麼還沒問到**，綁定有可能早就在裡面）／③ 橫幅與六格同一條選單（後台有沒有這種版型要按一次才知道）。⚠⚠⚠ **文案量出一個硬上限：主標只放得下 4 個全形字**（字欄 990、內距 130/70 ＝ 可用 790px；主標 165px 時 4 字 699.6 ✓、**6 字 1049.4 ✗**，連縮到 145px 都還超出 132px；副標上限 9 字）—— **所以「啟用完整功能」當不了主標**，那個意思只能放進副標，而字欄不能再放寬（990 是裁法翻面的地板）。**通則：文案的候選要先拿版型量過再送上去 —— 送一個放不下的字，等於送一個假選項。**四格（版型／字級／圖欄／裁法一個數字都沒動）：**Ⓐ 手機綁定／綁定後啟用完整功能／看診提醒與約診查詢（建議）**、Ⓑ 完整功能／綁定手機就啟用／…、Ⓒ 手機綁定／立即啟用完整服務／…、Ⓝ 現在這樣（**「綁定後都在這裡」就是他說的那個 bug**）。⚠⚠⚠ **「30 秒」四格一個都沒寫** —— 森釉那張寫「30秒 綁定手機」，但**我們沒有量過芳仁的綁定要多久**，那條流程是廠商的。**通則：別人卡片上的數字不可以照抄，那是他們量的、不是我們的事實**；守門有一道在掃（文案裡出現「N 秒」就 throw）。⚠ 守門另兩道新的：**卡片上真正畫的字要出現在頁面上**（圖是點陣的，頁面沒把字打出來，改了文案這裡一個字都不會變）、**六格的名字要留在頁上**（收乾淨就會再犯一次）。⚠⚠⚠ **同日他再補一句：「還有歡迎訊息　沒有改到　加入好友還不能看選單」—— 他是對的，而且這一句替那三條擺法做了決定。**已經定稿的招呼圖卡（`welcome-card.json`，**他自己逐字寫的**）第二塊是「**下面有一排選單／診所的資訊都在裡面**」，而那句話**是在六格還在的時候寫的**、送出的時機正好是**加好友那一刻**（那時還沒有人綁定）—— 整條換成橫幅之後，**那句話在它送出的當下就是假的**。⚠ **證據就在我們自己那張聊天室模擬上**（招呼卡寫著「下面有一排選單」、它下面那一條是綁定橫幅），**那張圖我看過好幾輪，一直沒有把上下兩塊放在一起讀**。→ **建議因此從「森釉那條」換成「六格挪一格」**（那一條「下面有一排選單」成立、不必動他定稿的字、不必等廠商）；而**綁定的曝光沒有真的少掉** —— 招呼卡上那顆滿版的「點這裡綁定」還在，出現時機正好是加好友那一刻，**綁定的主力本來就在那裡**。⚠ 真的要走森釉那條，招呼卡最小的改法兩個（**都動到他自己寫的字，沒有自己改**）：甲「綁定後下面會有一排選單…」、乙 那一塊整個拿掉。⚠⚠ **通則（這條線又一次）：一則訊息改了，要把「它旁邊那幾則」逐則唸一次** —— 這條線每一則都各自定稿、各自有守門，很容易只顧著改的那一則；而招呼卡與圖文選單是**同一個畫面上下相鄰**的兩塊，一塊講「下面有一排選單」、一塊把選單換掉，**兩塊各自都對，合起來才是錯的**（近親：第 28 條 ② 是「我的改動讓自己先前的一句話變成假的」，這一條是「**讓別人那一則的一句話變成假的**」）。⚠ 守門補一道：頁上招呼卡那兩行要**逐字等於 `welcome-card.json`**。⚠⚠⚠ **而那一道第一次寫錯了、負向測沒擋下來**：它掃的是**整頁**，而底下「甲」那個改法的句子裡也有同一串字 —— 改壞了照樣會過；改成只在 `id="wc-quote"` 那一塊裡找才擋得住。**通則（第三十五節那個 `pv-cut` 的近親）：一段字同時是「資料」又是「展示品」時，兩者一定要用不同的標記，守門也要指名那一塊 —— 掃整頁等於沒掃。**規格頁 `/preview/line-richmenu/`，產生器 `node drafts/channels/richmenu-bind.mjs`，守門 `node drafts/channels/check-richmenu.mjs`（負向測過十三種）。**推導在 `drafts/channels/README.md` 第三十七節。**⚠ **還開著四件**（都要使用者點頭）：**文案是我們擬的**（用詞跟著已定稿的綁定完成卡走，但這條線前面每一則的字最後都換成他的版本）／**選單列那行字**（現在是「手機註冊」）誰在設、改不改得動 —— Ⓒ 那一格全靠它講話／**點下去要去哪裡**（綁定網址還是佔位符，同第十一之七節第 1 項）／⚠⚠ **綁定完成之後這條選單要不要換掉** —— 已經綁好的人每次打開聊天室還看到「綁定手機」是說謊，LINE 分得了眾但那要廠商做 | 第三十七節 |
| **櫃檯的小立牌**（不是訊息，是印出來擺在櫃檯的那張卡） | ⚠ **文字提案中**（2026-09-16 開，`/preview/line-stand/`，**兩案**；**插圖與版面下一輪** —— 使用者：「風格和圖案預計換成診所網站和先前一致風格的人物。**先處理文字部分**」）。文字的唯一出處 `drafts/channels/stand-card.json`，產生器 `node drafts/channels/stand-card.mjs`，守門 `node drafts/channels/check-stand-card.mjs`（十一種負向測過）。推導在 `drafts/channels/README.md` **第七十一節**。⚠⚠⚠ **這是這條線第一次做印刷品，而印刷品和訊息差在「改不了」** —— 訊息寫錯下一則就蓋過去，這張卡會擺一年而且會被拍照。所以兩條比訊息更嚴：**不印任何會變的數字**（「看診前2天」與「48 小時」兩種講法還沒統一，四案一律只寫「看診前」，守門在擋）、**不印我們保證不了的事**。⚠⚠⚠ **現況那張踩到的最重的一句是「再也不怕忘記看診時間」** —— 使用者自己 2026-09-12 講過同一件事（第五十節 50-10：系統哪天沒送，病人說「你們沒提醒我」看似很合理，而「診所購置這個系統是友善提醒，不代表患者自己可以不用記得」）。另外「輕鬆接收診所新訊」**承諾了一個診所沒有在做的服務**（診所不做定期推播，而 2.2% 的封鎖率正是因為幾乎不發）。⚠⚠⚠ **開工第一件事是把那顆 QR 解碼**：解出來是 **`https://q.1talk.co/HQPRd` ＝ 廠商的短網址，不是 LINE 的加好友碼** —— 所以「掃完先加好友還是先驗證」不在我們手上，而**那一件決定卡片寫一步還是兩步**；⚠ 那個短網址轉去哪裡查不到（容器連不出去），但**診所拿一支沒加過的手機掃一次，三十秒就知道**。⚠ 解碼照這條線的規矩：兩張照片各自解過一次（opencv，臨時裝的）。⚠⚠ **四案用同一條規則算字級**（主文字高 ＝ min(卡寬 6%，版心 84% ÷ 最長那一行的字數)），所以比的是文字本身 —— **字少的自動變大，那正是「只有一句」那一案真正的賣點**；⚠ 版心那個 84% 是從現況量來的，所以現況那一列是**定義不是驗證**。⚠⚠⚠ **踩到一個畫面完全正常的**：卡片裡的長度**不可以用百分比** —— 百分比的參照是父層那一欄（手機上和卡片不一樣寬），同一張 300px 的卡內距在 430 上量到 24.1px、390 上 21.7px，430 因此裁掉三行字；一律寫 `calc(var(--cw) * k)`，守門逐寬度量內距。⚠⚠ **紅線掃描只掃卡片上的字**（這一頁本身就在解釋那幾個禁語，掃整頁一定撞到自己的說明 —— 第九次），而**現況那一張是正向對照、一定要掃得出東西**：只驗「新案是 0」的話，一張永遠匹配不到的掃描表也會過。⚠ 卡片量到 **98 mm 寬・比例 1.49**（≈ 4×6 吋），現況主文每字 3.8% 卡寬 ＝ 3.7 mm、QR 邊長 45% 卡寬 —— 全部從照片推的。⚠⚠ **2026-09-16 收掉了**：使用者拿尺貼著現況那張拍了一張照，量出來 **≈ 97 × 146 mm**，差 1%。⚠ 還沒有答案的七題在那一頁最底下。⚠⚠⚠ **2026-09-16 第二輪：抬頭定案「芳仁牙醫有 LINE 囉」（LINE 用綠泡泡框起來），「為您服務」升成紅線**（使用者：「不要說在 line 為您服務　這樣會覺得好像 line 上可以回覆留言　即時回答」）。⚠⚠⚠ **歸錯類的是我不是他**：我原本把那一句和五個驚嘆號寫成同一條（「廣告的聲音」＝ 口氣），而他的理由是**既有的紅線** —— 這個帳號沒有專人即時回覆（第十一之三節），所以那一句**寫了就是說謊**。掃描表補了 為您服務／為你服務／即時回／馬上回／線上客服／有問必答，現況因此從 5 個紅線詞變成 6 個。**通則：一句話被歸到「口氣」那一類之前，先問它是不是踩到某一條已經寫下來的紅線** —— 口氣是「我覺得可以更好」、紅線是「寫了就是說謊」，面板上要標不同的級別（同第 25-13 節）。⚠ **副標整個收掉**（原本「官方 LINE」）：抬頭已經講完了，副標就是同一件事講兩次。⚠⚠⚠ **抬頭因此要有自己的字級規則，而且要把那顆泡泡算進去**：`min(卡寬 8.5%, 版心 84% ÷ (全形當量 ＋ .04×字數 ＋ .52×泡泡數))` —— 字距與泡泡的左右內距會把那一行撐寬，不算進去的話抬頭會**靜靜地溢出卡片**（`overflow:hidden`，畫面上只是最後一個字不見，**而每一道尺寸守門都會過**）。算出來 8.23 mm、佔卡寬 84%（現況沒有泡泡，撞上限 8.33 mm ＝ 改動前的值）。⚠ 抬頭是 **`nowrap`**（放不下要被擋下來，不可以靜靜折成兩行）；⚠⚠ **字距與泡泡內距在 CSS 與算式裡只有一份出處**（`--hd-ls`／`--li-pad`），守門逐個比對。⚠⚠ **現況那一張的 LINE 刻意不套泡泡** —— 那一張是照片上逐字抄的對照，加工過就不是對照了。⚠⚠⚠ **那顆泡泡是用 CSS 畫的示意，不是 LINE 官方的標誌檔**（綠是品牌綠 `#06C755`，但那是別人的商標）—— 印之前要照 LINE 的標誌使用規範，已列成待答的一題。⚠⚠ **守門第 ② 道跟著改**：原本拿原字串去 `includes`，而抬頭裡的 LINE 現在被一個 `<span>` 包著、整條字串不會再原樣出現在 HTML 裡 —— 改成**先剝標籤、還原實體再比對**。**通則：一道「這一行有沒有印出來」的守門，只要那一行裡有任何一段被包了標籤就會失效。**⚠⚠⚠ **而剝完標籤它仍然是空的，成因是我自己寫的說明** —— 四案那一節我加了一句「抬頭四案相同：**芳仁牙醫有 LINE 囉**」，**那一行在頁面上本來就有一份**，拿整頁去 `includes` 一定找得到。兩件一起治：那一句改成從資料讀（`pill(S.案[0].抬頭)`）、第 ② 道改成**一張卡一張卡地看**（把每一塊 `<div class="card…">…</div>` 抓出來逐張比對，順帶數卡片張數）。**通則（又一次）：一段字同時是「資料」又是「展示品」時，兩者要用不同的來源，守門也要指名那一塊 ——掃整頁等於讓說明文字替卡片背書。**⚠⚠ **又踩到第六十二節那條「負向測試不要用 `git checkout --` 還原」**：那個檔這一輪還沒 commit，一行指令把整輪的守門改動全部擦掉、**不報錯**，接下來兩格都在對著上一次 commit 的舊守門跑。**還原一律用 `cp` 備份再蓋回來**（第二次踩到）。⚠ 十一種負向測過。⚠⚠⚠ **2026-09-16 第三輪三件各自獨立**（使用者：「這是官方授權的logo 放上去」＋他自己寫的三行＋「最下面那一排 Alleypin 用診所 9 個 logo 隨機排列橫幅取代」，推導在 README **第 71-9 節**）。**① 抬頭那顆綠泡泡換成官方授權的標誌檔**（`preview/line-stand/line-logo.png`，1001×1000、四角透明、綠量到 `#06C755`）——⚠⚠⚠ **那是別人的商標，一個像素都不要改**：CSS 只有等比例縮放，守門逐項掃 `filter`／`border`／`background`／`clip-path`／`mask`／`opacity`／`mix-blend` ＋ 要有 `width:auto`。⚠⚠ **抬頭的字級規則要重推**：泡泡佔 2.52 個全形字、標誌只佔 **1.10**（高 1.10 em × 長寬比 1.001），所以抬頭**變窄**、字級從「被版心夾住的 8.26」跳到**撞上限的 8.33 mm**、那一行從佔版心 100% 掉到 71.6%，標誌印出來高 **9.2 mm**。⚠⚠⚠ **長寬比從 PNG 的檔頭讀不手填** —— 手填的話換一個檔，抬頭會算出一個「放得下」的字級、實際畫出去溢出卡片（`overflow:hidden`，只少掉最後一個字，每一道尺寸守門都會過）。⚠⚠ **周圍的留白是他寫的字帶來的不是我們補的**：他指定的抬頭本來就在 LINE 前後各一個半形空白 ＝ 標誌左右各留 **49%** 個標誌高，遠過 LINE 規範的四分之一，所以 `留白em` 是 0。**通則：一個「要不要另外補間距」的問題，先看使用者給的那串字本身有沒有帶。**⚠⚠⚠ **踩到一個整頁多 600px 水平捲動的**：那條 CSS 原本寫 `.card .li`，而**頁面上的說明也擺了一顆標誌**、落在卡片外面吃不到，於是用**原始像素 1001px** 畫出來。**通則：一個元素同時是「卡片裡的東西」又是「說明裡的展示品」時，樣式的選擇器不可以綁在卡片上。**⚠ 守門的 `strip()` 要**先把 `<img>` 還原成它的 `alt`** 再剝標籤（順帶守住 alt）。**② 他自己寫的三行 ＝ 案 Ⓔ，照寫、不改、亮紅**（第一行就是 Ⓒ 的第一行）——⚠⚠⚠ 那三行踩到我們自己列的兩條：「再也不怕忘記看診時間」是拆解第 2 條（理由是**他自己 2026-09-12 講的**）、「輕鬆接收診所新訊」是第 3 條（理由是**診所自己講的**）；做法照這條線一貫的：**照寫、把證據攤開、給一條最小改法（Ⓕ），不要自己改也不要不講**。⚠⚠ **守門那一道要翻面但不可以拿掉**：原本「每一案紅線都要 0」→ **亮紅的案要在資料裡寫明 `例外.紅線`、那句話要印在頁上，而且至少要有一案是乾淨的**（不然會被一張「大家都有例外」的表靜靜地關掉，負向測過）；時間的數字（`例外.數字`）同一套。**通則：一道守門要為使用者的決定開例外時，例外要寫在資料裡、要印在頁上、而且要留一個「全部都有例外就失效」的下限。**⚠ 「約診前2天」留著，但它和招呼圖卡的「看診前2天」一致、和綁定完成的「48 小時」**不一致**，兩則要先統一才印得下去。Ⓕ 只動三處（約診前→**看**診前／自動提醒→**我們**會提醒你／第三行換成查得到自己下次約在哪一天），最長那一行 20 → 15 個字、字 4.12 → **5.49 mm**。**③ 底下那條帶子 ＝ 診所自己的九顆 logo**（形狀讀 `brand/shapes/`、寬度讀 `preview/line-booked/wm-sizes.json`，**一個路徑資料都不抄第二份**，守門擋「產生器裡出現寫死的 `d="M …"`」）——⚠⚠ **等墨不等高**：那九個寬度是按墨的面積正規化的，高度 61~107（差 **1.76 倍**），排成一條就是第九節第 28 條 ① 那一種「同一排高高低低」；做法照 line-vendor 那一輪：**框固定成最高的那一顆、形狀垂直置中**（守門逐顆驗）。⚠⚠⚠ **「隨機」印出來的東西不能真的隨機** —— 這張卡只印一次，所以「隨機」＝ **不照九宮格的順序**，而那個順序**要定下來寫進 JSON**（第 28 條 ⑤）：定案 `r1c3 r1c1 r2c3 r2c2 r3c1 r3c3 r1c2 r3c2 r2c1`，兩條限制寫成守門（**三顆最長的不可以相鄰**、**顏色相同的兩顆不可以相鄰** —— 後者只有排下去才看得到）。⚠ 排法用「**SVG 裡面再放 SVG**」不是 flex（每一顆各自的 viewBox 原封不動帶著走，沒有排版的捨入誤差）。⚠ 那一條上**一個字都沒有**（多一個出口會分掉這張卡唯一的工作）。⚠ **那三格顏色（ⓐ 各自的科別色／ⓑ 全部品牌綠／ⓒ 全部柔墨）不是被比下去的，是前提換掉了** —— 它們全部是「白紙上的一條標誌」，而定案給了這一條一塊底色，同一把尺套不上去；理由收在 `色案_落選`（守門要求那段話還在）。⚠ 資料夾那一道從「只准有 index.html」放寬成**一張寫死的清單**（同 check-bind-prompt 那一輪）。⚠ 抓卡片的正規式要寫死整個 class 的值 —— 只比對開頭的話 `<div class="cards">` 也會被算進來（這一輪多一節，當場多數出一張卡）。⚠ 待答第 5 題改口成「授權涵蓋印刷嗎」（**記錄來源，不是把問題刪掉**）、第 6 題改成「帶子要用什麼顏色」。**十種負向測過**。⚠⚠⚠ **2026-09-16 第四輪兩件**（使用者：「用 E 但字級照 F」、「約診前2天自動提醒！ 改成 看診前2天自動提醒！」，推導在 README **第 71-10 節**）。**① 改字照做**，而且它把上一輪記著的瑕疵收掉一半：這張卡和**招呼圖卡逐字一致**了，⚠ **和綁定完成那一則的「48 小時」仍然不一致**（待答第 4 題原封不動 ——**印出來的東西改不了，兩則要先統一才印得下去**）；Ⓕ 的註因此從三處差異收成兩處。**② ⚠⚠⚠ 「字級照 F」不是把規則改掉，是替那一案宣告一個例外** —— 主文的字級一直是一條規則算的（`min(卡寬 6%, 版心 84% ÷ 最長那一行的字數)`），**四案比的是文字本身、字少的自動變大，那正是那張表的用途**；規則一改那張表就不再比文字。做法：資料裡寫 `"字級": { "照": "f" }`，產生器**自然那一格照算**、面板同時印「**釘在 Ⓕ・規則會算 5.88**」，**看得出這一釘是往上還是往下**（這一次往下：Ⓔ 斷行後最長只剩 11 個字，規則本來會給到 6% 上限的 5.88 mm）。**通則：使用者指定「這一案照那一案」時，把它做成資料裡的一個例外，不要去動那條規則 —— 規則是那張比較表存在的理由。**⚠⚠⚠ **③ 釘上去之後他原本那第三行放不下，而症狀完全看不出來**：Ⓕ 的字級 **5.49 mm ＝ 一行 15.0 個全形字**，而那一行 20 個字 ＝ **佔 112% 卡寬**，卡片是 `overflow:hidden` ＋ 每行 `nowrap`，**畫面上只會少掉最後幾個字，而每一道尺寸守門都會過** —— 所以照它自己那個逗號斷成兩行（**11 ／ 9**，字一個都沒改），**產生器直接 throw** 不是印警告。⚠⚠⚠ **負向測過六種，而其中一種第一次沒擋下來**：「頁上有沒有印 `5.88`」那一道，**Ⓑ 與 Ⓒ 的字高剛好也是 5.88**，掃整頁一定命中 —— 改成找那一格印出來的**整句**（`釘在 Ⓕ…・規則會算 5.88`）。**通則（這條線第十次撞到「掃整頁等於沒掃」）：一道「某個數字有沒有印出來」的守門，要找那個數字所在的那一句，不要找那個數字 —— 一張表上同一個值往往不只出現一次。**⚠⚠⚠ **2026-09-16 第五輪：兩把間距的尺，兩件各自獨立**（使用者：「芳仁牙醫有 LINE 囉／掃碼加入，完成手機綁定　**這兩行中間有很大的間隔　縮小一點　要預留空間給下面畫診所人物插圖**」、「**line logo 和前後文字間距也有點大**」，推導在 README **第 71-11 節**）。**① 抬頭到主文（尺 Ⓗ）** —— ⚠⚠ **他看到的量得出來**：抬頭的字面框下緣到第一行主文是 **12.8 mm**，而主文行與行之間只有 **4.3 mm**，**差三倍**。⚠⚠⚠ **成因是那個間距有兩段各自獨立、而只有一段在 CSS 裡看得到**：`.card .bd` 自己的上外距（卡寬 8%）**加上**第一行段落的 `margin:.28em` ——`.bd` 是 flex 項目、**flex 項目是 BFC 根**，兩段**不會合併**，只收一段就只治一半。⚠⚠⚠ **省下來的高度真的會落到下面，那是這一改成立的理由**：卡片固定長寬比、帶子靠 `margin-top:auto` 釘在最下面，所以上面收多少、QR 底下到帶子之間就多多少。四格（抬頭到主文／÷行距／空出來）：Ⓗ1 現況 12.8／3.0／18.6 mm・Ⓗ2 10.4／2.4／21.0・**Ⓗ3 建議 8.4／2.0／23.0**・Ⓗ4 7.0／1.6／24.4。⚠ **地板不是 0**：收到比主文行距還小，抬頭就會被讀成主文的第一行（守門要求 ≥ 行距的 1.4 倍）。⚠ 這把尺**只動垂直的間距**，字級、字數、版心一個數字都沒碰。**② 標誌左右的留白（尺 Ⓘ）** —— ⚠⚠ 量出來 **0.388 em ＝ 標誌自己高度的 35.3%**，而抬頭裡兩個相鄰的中文字只差字距 0.04 em。⚠⚠⚠ **改法不是改他的字，是把那兩個半形空白換成標誌自己的左右留白**：半形空白實際多寬**跟著字型跑**（這台 0.348 em），而這張卡要交給美編照規格排 —— **規格不可以是一個會跟著字型跑的東西**。所以畫出來的抬頭不再含那兩個空白，改成「左右各留 N% 個標誌高」（＝ LINE 規範本身的單位，換檔換字級都不必重挑）；**JSON 裡他寫的那一行一個字都沒有動**（⑥之二 比 JSON、② 比畫出來的那一份）。⚠⚠ **切法只有一份出處 `切抬頭()`**：`mark()`（畫）與 `hdOf()`（算字級）吃同一個函式，分家的話抬頭會算出一個放得下、畫出去卻溢出卡片的字級。四格 35.3%（現況）／32%／**28%（建議）**／25%；⚠⚠ **地板是規範不是美感**（選了就要印得出去的才放進尺裡），⚠ 而那條「至少四分之一」是**二手的**，已併進待答那一題（連同要一份規範原文）。⚠ **抬頭的字級一個數字都沒動**（早就撞在 8.5% 上限），那一行從佔卡寬 69.0% 收到 67.6%。⚠⚠ **這一改讓兩句話變成假的**（第九節第 28 條 ②）：`標誌._說明` 那句「留白是他寫的空白給的，所以 `留白em` 是 0」、以及待答第 5 題那句「都很寬鬆」。⚠⚠⚠ **踩到三個**：**① `--bd-gap` 寫成純數字，`margin-top` 整條無效 ＝ 0，而畫面完全正常** —— 四張案卡的抬頭貼著主文，長寬比、溢出、內距每一道都過。**通則：一個靠自訂屬性傳進去的長度，要在它的宣告處就是一個長度**（`calc(var(--cw) * k)`），不要傳一個裸數字讓別人乘。**② 補上去的那一道守門第一次也是假的（這一站第十一次「掃整頁等於沒掃」）**：它掃整頁找那串字，而**尺上那八張卡的 inline style 裡也寫著同一串** —— 改成先把 `:root{…}` 抓出來再 test。**③ 尺上每一格都是覆寫，所以「預設值」從來不會被它們量到** —— **通則：量完尺還要量一次沒有被覆寫的那幾張**，不然預設值是這一頁上唯一沒有人驗過的東西。⚠ 量「QR 底下到帶子」時，最底下那一塊**不一定是最後一個 `.cue`**（有的案只有 QR 上面那一行），要取底邊最低的那一個。⚠ 尺上卡寬 **166px** 是算出來的（(760 − 28 − 3×22) ÷ 4）——**間距這種東西並排才比得出 1~2 mm**。**七種負向測過。**⚠⚠⚠ **2026-09-16 第六輪：Ⓗ4 定案；「留白看不出差別、Ⓘ4 都還不夠」**（推導在 README **第 71-12 節**）。**① 抬頭到主文定案 Ⓗ4 ＝ 卡寬 2%**（抬頭到主文 **7.0 mm**、主文行距的 **1.6 倍**、QR 底下到帶子空出 **24.4 mm**，改動前 18.6）；尺從四張卡**收成一張表**（同 50-22：尺可以收，它量出來的數字不可以跟著消失）。⚠ 順手修掉一個自己造成的假話（第 28 條 ②）：Ⓗ3 的標籤還寫著「建議・**現在這樣**」—— **一把尺收掉時每一格的標籤都要重新唸一次**，那些字是在「還沒挑」時寫的。**② 標誌留白：他兩句都對，而且是兩件事。**⚠⚠ 「看不出差別」是**我送的尺不對** ——四格只差 **0.94 mm**，而且分在四張卡上；改成「**只有抬頭那一行**」疊起來、左緣對齊、**一條虛線標著現況那顆標誌的左緣**，用 `cqw` 跟著容器放大（電腦約 2.4 倍卡片大小）。**通則：一把尺比不出來的時候，先問是「範圍太窄」還是「擺法沒有共同基準」——兩個都要治。**⚠⚠ 「Ⓘ4 都還不夠」＝ 他要的比我們手上記的規範還緊。**先查過一件會改變答案的：那個標誌檔本身沒有內建留白**（零依賴解 PNG 逐像素量過，綠色圓角方塊**貼滿整個 1001×1000 的框**，不透明佔 95.7%）——**通則：要判斷「留白夠不夠」之前，先量那個素材自己帶不帶留白。**做法照 `紅線` 那一輪：尺拉成五格 **35.3%（現況）／25%（規範下限・預設）／18%／12%／6%**，後三格在資料裡宣告 `例外`、頁面上逐格標紅。⚠⚠ 守門因此從「地板 ≥25%」**翻面成三件**：預設一定要 ≥25%（**那條線要由他跨，不是我們**）／跨線的每一格都要宣告例外**而且那句話要印在頁上**／**至少要有一格守得住規範**（不然一張「大家都有例外」的表會把這一道靜靜地關掉）。⚠ 那條「至少四分之一」仍然是**二手的**，而**那一題現在會決定版面**。⚠⚠⚠ **踩到兩個，症狀都是「多一條虛線、每一道尺寸守門都會過」**：① 線的位置算成「佔卡寬的百分之幾 ÷ 版心」，而那幾行的字級是 `8.5cqw` ＝ **容器寬的 8.5%** —— 多除了一次版心。**通則：一個換算出來的座標，要和「畫它的那個東西」吃同一個基準。**② 改對式子還差 12.6px：`left` 的百分比吃 **padding box**、`cqw` 吃 **content box**，差一個左右內距。**通則：同一條版面上混用 `%` 與 `cqw` 就是混用兩個基準** ——症狀永遠是「差一點點」，而在一條當基準線的虛線上，差一點點就是整件事失效。守門因此多一道**量出來**的：那條線要落在第一列的標誌左緣（±2px）。⚠ 另外「數帶子」那一道改成**數頁面上真的有幾張 `card sc`** —— 照尺的格數算的話，抬頭到主文那把收成表之後就會對著一個不存在的數字驗。**六種負向測過。**⚠⚠⚠ **2026-09-16 第七輪：Ⓘ4 定案（＝ 跨過規範那條線），四案收成兩案**（使用者：「Ⓘ4　左右各留 12.0% 個標誌高 ＝ 1.10 mm　⚠ 低於我們手上記的「至少四分之一個標誌高」　**選這個保留　提案頁其他可以先刪掉了**」，推導在 README **第 71-13 節**）。**① 留白定案 12%（卡片上 1.10 mm）** —— 71-12 那一輪的做法（不自己跨、把線畫在哪裡講清楚、三格標紅擺出去）走完了，這一輪只把他挑的那一格寫回預設。⚠⚠⚠ **守門因此翻面而不是放水**：預設**可以**低於規範，但只准在三件同時成立時 —— ① 那一格宣告了例外 ② 那句話印在頁上 ③ **頁面上仍然講得出那條線在哪**。⚠⚠ **第 ③ 件才是真正在守的東西**（尺一收，最容易一起消失的就是那個地板），做法是把它釘在**那張表的欄位抬頭**上（「對規範（25%）」）——⚠ **不可以寫成「頁面上有沒有出現 25%」**，別處順便提到也會命中（**這條線第十二次「掃整頁等於沒掃」**）。⚠ 面板也要在跨線時講一句（第 28 條 ④）。⚠ 尺收成一張表，五格的留白比與 mm 都留著；**「・定案」由產生器現算貼上去，標籤裡不要再寫一次**（第一版兩邊都寫，印出來是「12%（定案）・定案」）。⚠ 兩張表的灰底一起改成淡掉**「現況」那一列**，不是淡掉定案那一列。**② Ⓐ Ⓑ Ⓒ Ⓓ 從頁面上拿掉** —— ⚠⚠⚠ **Ⓕ 不可以跟著走，三個理由**（都已經是守門）：Ⓔ 的字級釘在它身上（產生器會 throw）／它是 Ⓔ 那兩條紅線唯一寫下來的最小改法／**至少要留一案是乾淨的**。⚠ 現況那一張也留著（紅線掃描的正向對照）。⚠⚠ **拿掉的是畫面不是理由**：`刪案` 記著走了哪幾案、什麼時候、`git show 904fff5:…`，三件都印在頁上。⚠⚠ **「四案」那個詞要現算**（`案數詞`）。⚠⚠⚠ **守門當場抓到一件：拿掉那四案，順手也拿掉了「不印數字」那條路** —— 「至少要有一案沒印」那個下限唯一的現場正是 Ⓐ~Ⓓ，拿掉之後 Ⓔ 與 Ⓕ 都印著「看診前2天」。**這不是守門在礙事，它擋下來的正是它存在的理由。**處理照 71-10 那一套翻面不放水：一案都不乾淨時，**頁面上非得講得出那條路不可**（建議不印 ＋ 兩種講法差在哪 ＋ 要走它只要把 Ⓔ 那一行的「2天」拿掉）；**沒有自己改 Ⓔ 的字，也沒有自己補一個第三案。**⚠⚠ 順著查出一句**已經過期兩輪**的話：待答第 4 題還寫著「四案都只寫『看診前』」——從 Ⓔ 進來那天起就是假的了。**通則（第 28 條 ② 又一次）：一句「全部都如何」的話，加一案、減一案都要回頭唸一次。**⚠ 順手修掉兩處死指標（Ⓔ 的註指著 Ⓒ、待答有一題叫「Ⓓ 那一句要不要寫」）與 `.li` 那段已經假掉的註。**九種負向測過**；⚠ 其中兩種由**產生器**擋（守門跑不到那一步），所以那兩個 throw 要**說人話**，不要丟 `TypeError`。 ⚠⚠⚠ **2026-09-16 稍晚：間距那一句是兩件事，所以是兩把尺**（使用者：「這段間距再調一下　**除了每段之間縮減一點**　**感覺離最上段還有不少空間**」）—— 主文段與段之間（新開的 **Ⓙ**）、以及抬頭到主文那一段（**Ⓗ**，上午才定案 Ⓗ4、同一天稍晚**重新打開**，多三格）。⚠⚠⚠ **兩把不是獨立的，而且收 Ⓙ 會讓 Ⓗ 讀起來更遠** ——抬頭到第一行主文那一段由三截疊起來（`.bd` 的上外距 ＋ **第一行段落自己的上外距 ＝ Ⓙ** ＋ 上下各半行距，`.bd` 是 flex 項目、外距不會合併），所以段距一收抬頭那一段也收一點點，**但主文行距（＝ Ⓙ ＋ .5 em）掉得更快、比值反而變大**（1.63 → 收一半之後 1.77 倍）。**那正是他同一句話裡講到兩件事的原因**；兩張表因此**都印「抬頭到主文幾 mm」與「那是行距的幾倍」**，只印自己那一欄的話這件事在頁面上看不出來（第九節第 28 條 ②）。⚠⚠ **省下來的高度真的會落到下面**（卡片固定長寬比、帶子靠 `margin-top:auto` 釘底），QR 底下到帶子從 24.4 一路到 26.4 mm —— 那正是要畫人物插圖的那一塊。⚠ Ⓙ 的地板不是黏在一起：行高 1.5 自己就有 .5 em 半行距，最緊那一格（.08 em）還有 3.2 mm。⚠⚠⚠ **那條「不可以比主文行距還窄」的地板從 1.4 倍鬆到 1.0 倍** —— 1.4 是我們自己多加的、比守門自己那行註解寫的理由還嚴的線，而他要的比它緊；**誠實的一半：照現在的幾何沒有一格碰得到 1.0**（抬頭比主文大 1.52 倍，`(Ⓙ+.25×抬頭字級比+.25)÷(Ⓙ+.5)` 恆大於 1，連「完全不留」都還有 1.16 倍），**它真正在守的是「抬頭縮到和主文差不多大」或「行高從 1.5 改掉」**。⚠⚠ **尺重新打開就要重新畫成卡片**（間距並排才比得出來），而**哪幾格畫成卡是算的不是列的**（`值 <= 預設`）。⚠ 那張表的「幾倍」那一格現在帶著 class，守門不可以再用 `<td>…</td>` 去對（第一次跑七列全部誤報）。⚠ 五種負向測過。⚠⚠⚠ **2026-09-16 第八輪（同一天稍晚）：兩把都定案 —— 「保留 J4 H7 其他刪掉」**（推導在 README **第 71-14 節**）。**Ⓙ4（.08 em）＋ Ⓗ7（卡寬 0%），兩把都是最緊的那一格**；值寫回預設、**卡片從頁面上拿掉、兩張表留著**（同 50-22、71-13）。量到 **抬頭到主文 3.9 mm・主文行距 3.2 mm ＝ 1.22 倍・QR 底下到帶子空出 31.9 mm**（改動前 12.8／4.3／2.98／18.6，上午那一版 7.0／4.3／1.63／24.4）。⚠⚠ **「完全不留」不等於兩行黏在一起** —— Ⓗ 收掉的只是 `.bd` 那一截外距，第一行段落自己那一截（Ⓙ）與上下各半行距都還在。**通則：一把尺歸零之前，先算清楚那個量是由幾截疊起來的 —— 歸零的是其中一截，不是那個量。**⚠⚠ **比值反而是全場最小的 1.22 倍，那不是巧合**：兩把一起收的時候分子分母都在掉，而 Ⓗ 只掉分子 —— 上一輪那句「收段距會讓抬頭那一段更顯得遠」在這一組值上被 Ⓗ 蓋過去了。⚠ **空出來的 31.9 mm 就是要畫人物插圖的那一塊**（比改動前多 72%）。⚠⚠⚠ **「・定案」由產生器現算（`值 === 預設`），不要寫進標籤裡** —— 兩邊都寫會印成「完全不留（定案）・定案」**而每一道版面守門都會過**（71-13 已經踩過一次，所以這一輪直接做成守門：逐列比對那一行抬頭 ＋ **標籤自己不准出現「定案」**）。⚠⚠ **守門那道「畫幾張卡」改成 0，但「數帶子」仍然是現場 match 的** —— Ⓗ 那把被打開過一次，**這兩把還會再被打開**。**通則：一個「現在是幾張」的數字，即使當下是 0 也要現場數，不要寫死。**⚠ 六種負向測過（尺又畫回卡片／標籤自己寫著定案／定案記號掛錯格／Ⓙ 那張表整個不見／落選那幾格被從資料裡刪掉／預設值不在表上）。⚠⚠⚠ **2026-09-16 第九輪：底下那條帶子**（使用者一句話五件：「**帶子 logo 和兩邊還有間隔，logo 間隔和兩邊間隔要一致，logo 可以小一點，然後多放一兩個，帶子底色用一般牙科主題色，logo 白色，牙洞就是一般牙科主題色**」，推導在 README **第 71-15 節**）。⚠⚠⚠ **那兩個間隔不一致，是因為它們根本不是同一個東西給的**：改動前中間那個是 SVG 裡 `gap = 最高那一顆 × .4` 算出來的（**2.1 mm**），兩邊那個是「SVG 寬度 ＝ 版心、卡片 ＝ 滿版」剩下的**頁面內距**（(98 − 82.32) ÷ 2 ＝ **7.8 mm**，差 3.7 倍），上下那兩個又是第三份（`.band.logos` 自己的 `padding`）—— **三處三個出處**。→ **整條由一張 SVG 畫完**（底色是裡面一塊 `rect`、四邊的留白烘進 `viewBox`、`.band.logos` 自己 `padding: 0`、SVG 寬度改成 `var(--cw)` 滿版），**那五個間隔因此出自同一個算式、不可能不一致**（守門逐個量，定案都是 **2.36 mm**）。**通則：兩個間隔「看起來不一樣」時，先問它們是不是同一個東西給的 —— 是的話調一個值就好，不是的話調值只會讓某一個螢幕上剛好一樣，真正的修法是讓它們出自同一個算式。**⚠⚠ **「小一點」和「多放一兩個」是兩把尺不是一件事**：那一條一律縮到卡片寬，所以**顆數愈多每一顆愈小**（Ⓚ 9→5.62／10→5.07／**11→4.71 mm 定案**）、**間距愈大每一顆也愈小**（Ⓛ .40→5.00／**.50→4.71 定案**／.60→4.45／.75→4.12）—— 兩個都做得到「小一點」，混成一把就是一把髒的尺。⚠ **改動前是 5.27 mm**，所以「九顆 ＋ 滿版」反而會**變大**（5.62）；定案這一組小 11%。⚠⚠ **「多放一兩個」＝ 有形狀要重複，而重複哪一顆是一個設計項**（只有九個形狀）：挑**站上真的在用的那兩顆**（`r1c2` 頁首那顆、`r3c1` 商家貼文的浮水印），兩顆隔得愈遠愈好、不然會被讀成一個花紋。限制因此從兩條變成三條（**同一顆形狀不可以相鄰**、三顆最長的不可以相鄰、同色不可以相鄰），⚠ **而且三個順序案每一個都要守**（不是定案的那幾格照樣畫在頁面上，排錯了畫面完全正常）。⚠ 同色那一條**定案這一版看不出來**（整條都是白的），留著是因為順序是資料、日後退回「各自的科別色」時它就會回來說話，而且它不會誤報。⚠⚠⚠ **牙洞不要另外填色**：那九個形狀都是**單一路徑 ＋ `fill-rule: evenodd`**，洞是挖穿的 —— **標誌填白、洞就自動透出底下那塊綠** ＝ 他要的「牙洞是一般牙科主題色」；另外畫一塊綠色的洞上去**畫出來一模一樣**，但形狀一改就會對不準、**而且不報錯**（守門掃「整條只准有兩種 `fill`」：底色那一塊 `rect` 與形狀自己的 `currentColor`）。⚠ 底色 `#3f654a` **讀 `wm-sizes.json` 的 `r1c1`**（一般牙科），不在這裡再抄一份色碼；白壓在它上面 **6.54**。⚠⚠ **那三格「顏色」不是被比下去的，是前提換掉了** —— ⓐ 各自的科別色／ⓑ 全部品牌綠／ⓒ 全部柔墨三格都沒挑過，而它們全部是「白紙上的一條標誌」，定案給了這一條一塊底色、同一把尺套不上去；收進 `色案_落選`（**拿掉的是畫面不是理由**，守門要求那段話還在）。⚠ ⓑ 的色碼正好就是定案的底色，**但那是兩件事**（標誌的顏色 vs 底的顏色），不要拿它當「ⓑ 上線了」。⚠ 跟著長出來的一件：整疊從 `0.035 + 帶高 + 0.04` 變成 `帶高` 一項（內距烘進 SVG 了），所以 **QR 底下到帶子從 31.9 mm 變成 35.1 mm**。**九種負向測過**（同形狀相鄰／重複的不在清單裡／`.band.logos` 又補回 padding／牙洞另外填一塊／標誌不是白的／底色寫死一個色碼／面板不印那五個間隔／標籤自己寫著定案／兩邊的間隔和中間不一樣）。⚠⚠⚠ **2026-09-16 第十輪兩件**（使用者：「K3 L2 定案　其他對話做好的 lineQRcode 放進來」，推導在 README **第 71-16 節**）。**① Ⓚ3（十一顆）＋ Ⓛ2（間距 .50）定案** —— 兩個值本來就是預設，所以做的是**把兩把尺的帶子條收掉、兩張表留著**（同 50-22、71-13、71-14）；「・定案」仍然是算出來的，⚠ **三個順序案與四個間距案一格都沒有從資料裡刪掉**（守門仍然逐案驗那三條相鄰的限制 —— 哪天打開，排錯了畫面完全正常）。定案畫出來：帶子高 **9.4 mm**、一顆最高 **4.71 mm**、五個間隔都是 **2.36 mm**。**② 卡上那顆 QR 換成診所自己那一顆** —— 成品是 2026-09-15 在另一條對話定案的 `drafts/channels/qr/fangren-line-qr.svg`（推導在 README **第七十四節**：那一節連同 `qr-brand.mjs`／`check-qr.mjs`／`line-bubble.json`／`qr/` 與 `tools/qr.mjs` 的 `minVer` 一起**取檔案**過來，不是 merge；⚠ 那條分支原本編第七十一節，這邊被立牌與 A4 佔走了，所以整節收成七十四、小節改成 74-N）。⚠⚠⚠ **它和照片上那一顆編碼的是同一個字串**（兩顆都解出 `https://q.1talk.co/HQPRd`）——**換長相不是換流程**，所以待答那兩題（掃完會發生什麼／要不要改用 LINE 自己的加好友碼）**一題都沒有被它回答掉**，那兩題改口寫清楚、沒有順手打勾。**通則：換掉一個東西的長相時，要回頭問它底下那幾題有沒有跟著被回答 —— 沒有的話，把「這一改沒有回答它」寫進那一題。**⚠⚠⚠ **這一頁只搬不畫**：產生器把那個 SVG **原封不動複製**成 `preview/line-stand/qr-line.svg`（`drafts/` 進不了 `_site`），守門**逐位元組比對兩份** ——用 CSS 或 `<path>` 重畫一顆「看起來很像」的碼，畫面完全正常、**只有拿解碼器掃才知道掃不掃得出來**。⚠⚠ **現況那一張刻意維持灰色佔位方塊**（它是照片的逐字對照）。⚠ 尺寸每次出圖都印：框佔卡寬 45% ＝ **44.1 mm**、扣掉四格靜區之後**碼本身 36.3 mm**（下限 1.5 cm）；實際照印刷尺寸算繪再掃過，zxing 讀回同一個字串，⚠ OpenCV 在乾淨原圖上讀不到（＝ 74-10 記過的那件事），不要拿它當唯一依據。⚠⚠ **同一輪把驗收的寬度從八個收到三個**（使用者：「這個只是簡單的設計文件和排版而已　不必要的設定可以拿掉讓速度快一點」）——430／393／390／375／360／834／1440 量出來**逐格完全一樣**（卡片都是 300×446.9，有 `max-width` 上限），只有 320 是另一種；留下 **430／350／320** ＝ 上限那一段、上限的邊界、最窄那一台。**通則：一把掃描的寬度清單，要留的是「會給出不同答案」的那幾格** —— 其餘每一格都只是在重量同一件事，而且每一次都要再開一次瀏覽器。⚠ 卡片寬度的算式一改（`min(300px, 86vw)`）就要把中間那幾格加回來。**四種負向測過。**⚠⚠⚠ **2026-09-16 第十一輪：QR 周圍那圈淡綠色的邊拿掉了**（使用者：「QRcode 周圍有淡綠色的邊可以拿掉嗎」，推導在 README **第 71-17 節**）——⚠⚠⚠ **那圈邊不是外框也不是陰影，是「靜區」被連同碼面一起上了色**：底板原本鋪滿整個 `viewBox`（45 ＝ 碼面 37 ＋ 靜區 4×2），定案改成**只鋪在碼面上**（`PLATE_MODE = 'code'`），**碼本身一個模組都沒有動、靜區也一格都沒有收**（收顏色不影響掃描，收格數會）。⚠⚠ **底板不是裝飾，不可以整塊拿掉了事** —— 這顆碼**淺色那一半的牙是白的**，底板一拿掉它們就和紙一樣白、整片花紋只剩深色那一半（Ⓝ3）。**通則：一個看起來多餘的底色，先問「有沒有東西是靠它才看得見的」。**⚠ 三格（Ⓝ1 靜區也上色／**Ⓝ2 只有碼面上色・定案**／Ⓝ3 整張不上色）都在印刷尺寸（521px ＝ 44.1 mm @300dpi）上**真的拿 zxing 掃過**，三格都讀回同一個字串。⚠⚠⚠ **改的是 `qr-brand.mjs`，所以 `drafts/channels/qr/` 那兩顆成品（印刷與廠商用的那一份）也跟著換了，不只這張卡**；`check-qr.mjs` 重跑兩顆各 30/30。⚠ 那一排**擺的是真的碼**（定案那一格就是卡上那個檔案本人，落選兩格現算成 `qr-plate-full.svg`／`qr-plate-none.svg`）——用 CSS 模擬一顆「很像的碼」畫面完全正常，只有解碼器分得出來；⚠ 那一排的底一定要白（頁面的底是紙色，而落選那兩格的靜區是**透明**的）。**五種負向測過。** ⚠⚠⚠ **2026-09-16 第十二輪：Ⓝ2 定案、Ⓔ 少一行、Ⓕ 拿掉，整頁收成一張卡**（使用者四件：「N2」／「再也不怕忘記看診時間，　這段拿掉」／「保留 E 就好　F 拿掉」／「以上還有其他下面的都不需要了　不要放在提案頁裏」，推導在 README **第 71-18 節**）。**① Ⓝ2 定案** ＝ 收尺：三格的比較從頁面上拿掉、**落選那兩格不再產檔**（`qr-plate-*.svg` 已刪、白名單收回三個檔），三格量出來的東西改由**面板**逐格印，資料裡一格都沒有刪。**② 那一行拿掉**：Ⓔ 從四行變三行（最長仍是 11 個字，釘在 Ⓕ 的 5.49 mm 一個數字都沒動），⚠⚠ 它**同時解掉 Ⓔ 兩條紅線裡的一條**，剩下的「輕鬆接收診所新訊」還亮著，所以 `例外.紅線` 與那句話仍然要印在頁上。⚠⚠⚠ **③ 「拿掉」有兩種，要分清楚是哪一種**：Ⓐ~Ⓓ 是**從資料裡走**（理由搬進 `刪案`），Ⓕ 只是**標 `不印`、不畫出來**（資料與守門原封不動）——**判準是「還有沒有別的東西靠著它」**：Ⓔ 的字級釘在 Ⓕ 身上、它又是紅線那一道唯一乾淨的一案，搬走就會在別處炸開。**④ 收掉九節**（現況與量法／標誌／QR 掃出來是什麼／底板那把尺／拆解六件／Ⓗ Ⓙ Ⓘ 三張表／帶子與 Ⓚ Ⓛ 兩張表／字級表／待答七題），頁面 42.6 → **16.8 KB**。⚠⚠⚠ **真正的工作不在刪，在「每一道守門都有一半是讀頁面的」** —— 那幾節一走，十幾道守門會對著一個不存在的東西驗，**而刪掉它們就等於把守門關掉**；做法照這條線一貫的**翻面、不放水**：現況改驗「不可以再畫在頁上＋資料還在＋字級規則仍然特判它＋`card()` 仍然畫得出那一態」／「再也不怕忘記」從「要印在頁上」翻成「任何一張印出來的卡都不可以再有」／字級「釘在誰・規則會算幾 mm」改到**那一案自己的註**裡找（⚠ 註是一張卡一張卡的，**要切出那一塊再找，掃整頁等於沒掃**）／五把尺逐格的 mm 改驗「產生器真的在逐格算 ＋ 面板真的印」／「對規範（25%）」那一欄改驗「面板在跨線時印得出那個百分比」。⚠⚠ **新增兩道**：⑥之八 逐項擋那六件接回頁面（**接回去畫面完全正常、每一道版面守門都會過**）＋**死 CSS 也要跟著收**；以及「這一頁只印 Ⓔ」（把 Ⓕ 畫回去也是完全正常的一張卡）。⚠⚠⚠ **踩到三個**：**模板字串的 CSS 註解裡不能有反引號**（又一次，錯誤指在幾十行外的 `const HTML =`）／`刪案.節` 少一半時產生器丟 `TypeError`（併進原本那道會說人話的 throw）／⚠⚠ **守門第 ① 道把產生器的 throw 吞成一包位元組陣列** —— 現在接住 stderr 原句印出來。**通則：一道會去跑別的程式的守門，要把那個程式的訊息原句印出來** —— 不然它失敗的樣子比不檢查還難懂。**十三種負向測過。**⚠⚠⚠ **2026-09-16：文字收尾，開始畫人 —— 插圖的提示詞在 `drafts/stand-card-illustration-prompt.md`**（使用者：「參考像廠商原本那張有個人拿著手機……這些人是診所的醫事人員　刷手服顏色是網站上的主題色、有的有穿白色醫師長袍，有的有戴醫師帽，**特別是之前兒童牙科的花紋圖案醫師一定要有**並增加照片這兩種花紋的刷手服醫事人員」）。⚠ **先量那一塊有多大**：QR 底下到帶子 **43.8 mm**，所以是一條 **98 × 43.8 mm（約 2.2:1）的橫幅**，四個人排開**一個人只有 22 mm 寬、軀幹 10 mm**。⚠⚠⚠ **印花不可以照抄照片的密度** —— 兒牙那一輪量過（`og-topic-kids-prompt.md` 第二節）：照片上圖案約軀幹寬的 1/7，畫在 10 mm 的軀幹上印出來是一片灰色雜點；提示詞寫死「1/4~1/5、胸前只看得到三到四個、每種印花只用兩個顏色」。**通則：使用者拿一塊真的布當參考時，要的是梗不是密度。**⚠⚠⚠ **圖裡不畫 QR code** —— 卡片的 QR 就在插圖正上方，再畫一顆等於同一張卡兩顆碼，而且**生成的碼一定掃不出來**（這條線的規矩本來就是 QR 不可以用眼睛驗收）；四個人一律看／指／對準**畫面上緣外面**那個東西，第 2 段是硬規則、`AVOID` 再擋一次。⚠⚠ **底是白卡所以不畫背景**（沒有邊框、房間、地平線，腳下只有一小塊淡影子）；跟著長出一條：**白袍畫在白卡上會不見**，要細線 ＋ 一階暖灰的陰影。⚠ 四個人 ＝ 他列的四個動作（指著喊／雙手展示／舉著手機對準／低頭看手機，最後一個呼應廠商那張），**Ⓐ 那位的布料逐字沿用 `og-topic-kids-prompt.md` 第五節第 4 段、不要重新設計**，而且**不看鏡頭**（朝畫面左外側喊 —— 這張卡擺在櫃檯上，對著讀者喊其實說得通，**要翻是他的決定**）。⚠ **顏色一個都沒新增**：兒牙 `#c28229`＋`#9e6301`／一般牙科 `#3f654a`／牙周 `#317d78`／矯正 `#4478b5`；⚠⚠ Ⓒ Ⓓ 走**淡一階的主題色**當底不是照片上那種飽和的黃與薄荷（兒牙的 `AVOID` 就寫著不要一整塊芥末色的制服 ——**一整塊飽和色在這個尺寸讀成一塊色板不是一件衣服**）。⚠ 參考圖四張、用途分開標，其中兩張由 `node drafts/stand-card-refs-crop.mjs` 從他的手機截圖裁出來（來源不在版控裡，成品已進版控，**裁的時候要把深灰舖棉與牆面一律裁掉**）。⚠ 驗收最要緊的一條：**把圖縮到 98 mm 寬看一次**（螢幕上放大看不算數）。⚠⚠⚠ **提示詞寫完之後使用者才把廠商那張照片傳過來（「照片是這張」）——那是寫的時候手上沒有的東西**，量了三件、**兩件不必改一件會改到提示詞**：⭐ **順手收掉一個開著的問題**（照片裡有一把尺 ＝ 他在回答「要請診所用尺量一次」，量出來 **≈ 97 × 146 mm**，和我們一路在用的 98 × 146 差 1% —— **先前從照片推的那一組是對的**）／**那不是一個人是一隻吉祥物**（白色團塊、閉眼笑、垂耳、腮紅，右下角**一隻** ≈ 31 × 30 mm ＝ 卡寬的 32%，底下被帶子切掉）—— ⚠⚠⚠ **所以「參考廠商那張」參考的是那個動作，不是畫風也不是構圖，那一張不要餵給模型**（餵了會把整張圖帶去吉祥物那一邊，撞到 ILLUSTRATION.md 第四節的 A 級紅線）；⚠ 兩件刻意的差異：他們一隻 31 mm、我們四個人一人只有 22 mm（高度差不多、份量小三成，換到四個動作與三種印花），以及他們手機螢幕上有字而我們寫死空白／⚠⚠⚠ **會改到提示詞的那一件：那隻頭上有三條短斜線**，而我們的 `AVOID` 明文禁止 `sparkles, motion lines` ——⚠ 站上對「用線畫看不見的東西」其實是**評價最高**的（ILLUSTRATION.md 第三節），只是一直畫的是**風與氣流不是情緒**，**沒有自己改掉 AVOID**，收進第六節等他決定；要畫就照那一條（三到四條**同向等距的短直線**、不可以是一條長曲線，而且**只給 Ⓐ 一個人**）。⚠⚠⚠ **2026-09-16：v1 畫好了，而「很難跟立牌結合」量出來是框不合不是圖不合**（使用者：「圖片畫好了　但這樣好像很難跟這個立牌結合欸?」）—— 原檔 **2000×1116 ＝ 16:9**、**上面留白 17.5%**（提示詞自己要的「上下各 11% 完全空白」，模型給得比要求的還多），而卡片上那一塊是 **98 × 43.8 mm ＝ 比例 2.237**：整張直接放，照寬度縮會高 **54.7 mm ＝ 爆框 10.9 mm**、照高度縮則左右空掉 19.5 mm 而且人縮小四分之一。**裁到墨的框就合了**（`node drafts/channels/stand-illus-crop.mjs`，四邊各留 8px）：1784×853 ＝ 比例 **2.091**，比那一塊瘦、所以是**高度在卡**，畫出來 **91.6 × 43.8 mm・左右各餘 3.2 mm・495 dpi**，**版面一個數字都沒有動**（`flex:1 1 auto` ＋ `object-fit: contain`，那一塊剩多少就畫多高；守門量到的「QR 底下到帶子」仍然是 43.8 mm）。⚠⚠⚠ **通則：一張圖放不進某一塊時，先分清楚是「內容太大」還是「框裡有空白」** —— 症狀一模一樣（爆框／縮到能放又太小），但前者要重畫、後者裁一次就好；**判準是量那張圖四邊的空白**（這一張上面空 17.5%，乘上 98 mm 就是 9.6 mm ＝ 它爆掉那 10.9 mm 的大半）。⚠ 一個人在卡片上：Ⓐ 18.7／Ⓑ 27.4／Ⓒ 16.1／Ⓓ 17.2 mm 寬、站著 40~44 mm 高、一顆頭約 **7.3 mm**（比 71-9 估的「一人 22 mm」寬一點 —— 那是拿 98÷4 除的，沒把「有人張開手臂」算進去）。⚠ **§六 那六件一件都沒有動**：這一版沒有畫頭上那三條短線、還是四個人、腳也沒有藏進帶子裡。⚠ 守門新增七道、**七種負向測過**；其中兩道寫完才補對：**「現況那一張也畫了插圖」掃頁面等於沒掃**（現況不畫在頁上了，要守產生器裡那個閘門）、**檔不在時 `readFileSync` 會丟 ENOENT**，要先 `existsSync` 再讀（守門要說人話）。⚠⚠ **通則：手上沒有那張參考物時照描述寫是對的，但拿到之後要逐項量一次再回頭改** ——三件裡兩件量完是「不必改」。⚠⚠⚠ **2026-09-16：v2 出來了，四件**（使用者：「大致可以　但太多綠色刷手服的人　而且最左邊的人長得好怪　再加上他的刷手服和帽子沒有連成一套　中間有一個人畫了一半下半部消失了」，v3 的提示詞在那一份第五之四節，推導在 README **第 71-22 節**）。⚠⚠ **先量再改**：臉那一件已經到了（前排一顆頭 **12 mm**，正好是算出來的 11.9），上緣留白 14%、左右與下緣都出血、沒有字也沒有 QR ——**所以 v3 的第 1 段一個字都不要動**。四件成因各不相同：**綠色**（量到素色深綠佔「人」那一塊的 **24.7%**，而那是**提示詞自己寫的** ——九個人裡六個同一色）→ 拆成**三色各兩人**（深綠／青綠／藍灰，全是站上七科現成的色）；**最左邊那個人**（臉被左緣切掉一半 ＋ 一隻接不到任何人的手）→ ⚠⚠⚠ 成因是提示詞寫 `CUT IN HALF BY THE SIDE EDGE`，**「切一半」被執行在臉上**，改成「邊緣切身體不切臉」；**帽子和衣服不成套**（塗鴉帽配素綠上衣）→ 只寫了 Ⓐ 那位同一塊布、**沒有禁止別人戴印花帽**；**中間半個人**（後排那顆頭落在前排胸口高度、身體消失）→ 寫著「畫小一點、站高一點」，**模型只做了小、沒做高**。⚠⚠⚠ **兩條通則**：**一個「要被切掉」的指令要指名切的是哪一部分**（不指名就會切在最醒目的地方）、**兩件事不要綁在同一句裡**（它會只做比較好畫的那一件，這裡是「縮小」比「重排前後關係」容易）。⚠ 參考圖第 ⑤ 張換成 v2 的成品（`drafts/stand-card-illus-v2-src.jpg`，已進版控）。 ⚠ 還沒決定的因此從四件變**六件**（多了那三條短線、以及腳要不要像廠商那張藏進帶子裡 ——代價是插圖要貼齊帶子上緣、下緣不能再留那 11% 的空白），都在那一份第六節。⚠⚠⚠ **2026-09-16 稍晚：使用者四件**（「1. 人太少 再多一點 8-9個人比較熱鬧／2. 要有人擠進來畫面裡高興熱情推廣介紹的感覺／3. 腳藏進帶子裡，人臉和動作要大一點 現在好小／4. QRcode 上面加一串字 @fafa070（line 商家帳號 ID）」，推導在 README **第 71-21 節**）。**第 4 件是卡片的事、已經做了；第 1~3 件是 v2 的規格**（提示詞那一份第五之三節，含逐字的 v2 提示詞與它自己的驗收）。⚠⚠⚠ **① 和 ③ 直接打架，而 ② 正是讓它們同時成立的那一件** —— 一條 98 mm 寬的帶子，**臉多大由「一個人佔掉這一條多少高度」決定、人數由寬度決定**，所以照字面加人會把「臉大一點」吃掉（4 人一顆頭 7.3 mm、6 人 4.9、8 人 3.6、**9 人只剩 3.2**）。解法是**人擠在一起（前排 5、後排 4 從肩膀之間探出來、兩端被左右緣切掉）＋ 下半身切掉**：切在小腿頭 8.7／大腿 10.5／**髖下 11.9（定案）**／腰 13.4 mm ——**9 個人，一顆頭 7.3 → 約 12 mm（大六成）**。⚠⚠ **通則：一句「A 和 B 同時要」聽起來像打架的時候，先找第三個可以動的量** —— 這裡是「一個人畫到身體哪裡」，它既不是 A 也不是 B，而兩件都靠它解；它也把 §六 那條「只畫三個人」判死（理由沒錯，**錯的是前提** —— 那一條假設一個人多大由人數決定）。⚠⚠⚠ **卡片那一側兩件，版面一個數字都沒有動**：**① 插圖沉進帶子後面**（`.card .illus` 一個負的下外距 ＝ 一個帶子高 ＋ `object-position: bottom center`），那一條 43.8 → **46.3 mm**、看得到 80% —— ⚠⚠⚠ **`.card .band` 非補 `position:relative; z-index:1` 不可**：`<img>` 是行內取代元素、**畫在區塊背景後面**，不給帶子一個堆疊脈絡，**人的腳會蓋在那十一顆標誌上而畫面看起來完全正常**；⚠ 「QR 底下到帶子」量出來一個像素都沒有變。**② `@fafa070` 那一行**——⚠⚠⚠ **它不是免費的，而且吃的正好是他同一句話裡要變大的那一塊**：掛通用的 `.cue`（上外距 .07）要花 **12.4 mm**，所以另外給三個具名常數 **`ID上 .035／ID下 .018／ID字級 .038`**，實際只花 **6.9 mm**（一顆頭因此小約 1.8 mm）。⚠⚠ **上面要比下面寬**（接近律）：那一行是「這顆碼叫什麼」，要讀成 QR 的名字而不是主文的第四行，畫出來上 3.9／下 1.8 mm，守門兩邊都量；⚠ 用**相鄰兄弟** `.cue.id + .qr` 蓋掉 QR 自己的 `.04`。⚠ **要買回那 6.9 mm 還有一條沒走的路：QR 自己還有餘裕**（框 44.1、碼 36.3、下限 15），那是另一個決定、**沒有自己動**，面板把代價印出來。⚠⚠ **那一行和那顆碼去的地方不一樣**（ID 是在 LINE 裡搜尋到帳號、碼編的是廠商的短網址 `q.1talk.co`），已寫進待答。⚠⚠⚠ **裁圖那一支換兩件，而 v1 重跑仍然逐位元組相同**：**只有上緣不准碰到邊**（下緣與兩側出血從 v2 起是規格；上緣是唯一可以補白的一側），以及**裁到墨之後若比那一條還寬，就從頭頂上那塊白裡把高度借回來**，輸出的比例**一定正好是那一條**。⚠⚠⚠ 這一件收掉了 71-20 留下的問題（「下一版留 11% 再裁，還是直接叫它出 2.2:1」）——**兩條都不必選**：模型畫成 2:1、上面留一條白就好。**通則：一個「模型每次都會失手」的要求，先問這一側能不能自己把它補回來**（v1 要 11%、給了 17.5%）。⚠ 原檔的路徑也改成從 `stand-card.json` 的 `插圖.原檔` 讀。**七種負向測過**；⚠⚠ 其中一道**第一次寫太寬** —— 掃整份找 `0.07 + 1.5 * 0.038`，而面板那一行**刻意印著「用通用的間距要幾 mm」**（掃整頁等於沒掃，第十四次），改成只切出 `疊高()` 來掃；⚠ 另一道是量測：那一行**上面那一段是兩截**（主文最後一段自己的下外距 ＋ 它的上外距，`.bd` 是 flex 項目、不會合併），照一截算會差正好一個段距。✅⭐ **2026-09-16 定稿 v3**（使用者：「這版很好~ 用這個」，推導在 README **第 71-23 節**）——原檔 `drafts/stand-card-illus-v3-src.jpg`（2000×989），`node drafts/channels/stand-illus-crop.mjs` 裁成 **2000×945 ＝ 2.116**，正好貼著那一條（2.117）；九個人、三種印花（塗鴉／松樹／恐龍）＋ 三個素色各兩人、四件白袍，每一張臉都完整、兩側切的是身體不是臉、腳藏在帶子後面，臉量出來（印 98 mm 寬）**前排 11.7~14.5 mm** ＝ v2 那一輪要的「臉大一點」到了。⚠⚠⚠ **接上去那一刻查出一個從第一版就在的量測錯誤：那一塊實際只有 86.2 mm，不是 98** ——`.card .illus` 寫的是 `width:100%`，而那是**內距框**（98 − 2×6%），裁圖那一支與面板卻從第一版起都拿整張卡寬 98 在算。三個後果一個都不會讓既有的守門翻臉：**兩側的出血變成兩條白**（而「人被邊緣切掉」是 v2 起的規格，留白時會讀成畫錯）、圖從高度在卡變成**寬度在卡**、**一顆頭小 12%**（正好吃掉他要的「臉大一點」的一部分）。修法照帶子那一條（`width:var(--cw)` ＋ 負的左右外距），改完量到 **98 × 46.3、左右 0.0、上面 0.0**。**通則：一個「照那一塊算出來的比例」的東西，要量一次它真的被畫成多寬** —— 版面、長寬、dpi、溢出每一道守門量的都是圖自己，**它們對「圖被畫在多寬的框裡」一無所知**。⚠⚠⚠ **負向測時還發現一個從第一版就在的洞**：把 `插圖.原檔` 指回舊的那一版、**卻忘了重裁**，先前是 `✓ 全綠` —— 「這張圖是哪一版畫的」從來沒有人在看；補法是在守門裡重跑 `stand-illus-crop.mjs --check` 逐位比對。**通則：一個「A 是從 B 產生的」的關係，守門要真的再產一次，不要只驗 A 自己合不合規格。**⚠ 守門因此多三道（rect 滿版＋上面不空／原始碼的負左右外距／裁圖逐位比對），**五種負向測過**。⚠⚠⚠ **2026-09-16：後面兩行的驚嘆號拿掉**（使用者：「上面兩段文字的驚嘆號拿掉」——主文那三行裡帶著 `！` 的後兩行，那是這張卡上僅有的兩個）。⚠⚠ **它們不是紅線是口氣**（紅線 ＝ 寫了就是說謊、有掃描表在掃；驚嘆號 ＝ 我覺得可以更好），所以先前每一輪面板都沒有標它、也不該標（同第 25-13 節）。⚠⚠⚠ **但這一改收掉的是這一頁自己的矛盾，而它已經放著兩輪**：`拆解` 第五條的最後一行早就寫著「五個驚嘆號也一起拿掉（COPY.md 第十節，行銷體是文體）」，那是拆解**現況**那一張時寫的，而 Ⓔ 是逐字照寫的，所以那一條在 Ⓔ 身上從來沒有生效過。**通則：照寫使用者的字時，我們自己列下來的「口氣」那幾條會靜靜地被繞過去 ——那不是把字改掉的理由（決定權在他），但要記著它在等一個人開口；他開口的那一天，要認得出那正是我們早就寫下來的那一條，不要當成一件新的事。**⚠ **字級一個數字都沒動**（釘在 Ⓕ 5.49 mm，最長那一行仍然是第一行的 11 個字，少掉的兩個字在第二、三行），折行、版面、插圖、帶子、QR 逐格相同；**現況那一張的五個驚嘆號留著**（逐字對照 ＋ 紅線掃描的正向對照）。⚠ 守門新增兩道、**兩種負向測過**：「案」裡不准再出現 `！`（加回去畫面完全正常、折行都不會變，**每一道尺寸守門都會過，只有讀字才看得出來**）／現況那一張要**數到 5 個** `！` ——⚠⚠ 第一版寫成 `includes("！")` **測不出來**（現況有五個，拿掉一個照樣 includes）；**通則：一道「正向對照還在不在」的守門要數個數，不要只問「有沒有」。**⚠⚠⚠ **2026-09-16：插圖 v4 的規格 —— 兩處修正 ＋ 兩個新顏色**（使用者三件：左邊第 2 位男生帽子和刷手服不同色／左邊第 4 位女生指上方那隻手畫成白袍的袖子、而她沒有白袍／**根管的紅與口外的紫各找一個人加到刷手服上，有戴帽的話帽子也要改**）。提示詞在`drafts/stand-card-illustration-prompt.md` **第五之六節**，推導在 README **第 71-25 節**。⚠⚠⚠ **v3 已經定稿並接在卡片上，所以這一版是「修三處」不是「重畫」** —— 提示詞最前面多一塊`WHAT CHANGES IN THIS VERSION`（逐條寫哪三件要改、其餘每一段只是讓它核對），第 ⑤ 張參考圖換成 **v3 的成品本人、標籤寫「整張照抄」** —— **沒有這一塊，已經對的東西會被重新擲一次骰子**（同第二十五節颱風那一輪）。⚠ 先量再改：帽 `#687d77` 對上衣 `#666e84`、那隻袖子量到 `#fefefe`；⚠ 他是**按人頭數的不是按性別數的**。⚠⚠ 順帶量到**v3 實際畫了十個人、提示詞寫的是九個** ——他沒有意見（要的正是熱鬧），所以那個數字一個字都沒改、素色那一段改寫成「其餘每一個穿素色的人」（卡片那張圖的 `alt` 從第一版起就刻意沒寫人數）。⚠⚠⚠ **兩件的成因都是「規則寫了，但只寫了一半」**：① 帽子那一條正文寫了「同色」，而 `AVOID` 只擋「印花帽配素色上衣」、**沒擋「素帽配另一個顏色的素上衣」** ——**通則：寫一條「A 要和 B 一致」的規則時，`AVOID` 要擋的是「不一致」本身，不是某一種特定的不一致**（正文是建議、`AVOID` 才是硬的，而**一條只擋住半邊的禁令，看起來和擋住整件事一模一樣**）；② 那隻手**真的接得回自己的肩膀**、每一條既有的規則都通過，錯的是袖子 ——**通則：「這隻手是誰的」有兩層，接得回肩膀（幾何）與袖子對不對（衣服）**，v4 補上「刷手服是短袖，沒穿白袍的人舉的是裸露的前臂」。⚠⚠ **兩個新顏色讓七科全部到齊**（素色從三色各兩人 → 五色、同一色最多兩人、紅與紫各正好一人，出處 `wm-sizes.json`，**一個新顏色都沒有加**）；⚠⚠⚠ **第 10 段與 `AVOID` 原本寫著 `NO strong saturated red`，不鬆綁的話新指令會被它擋掉** —— 改成「全圖唯一的紅就是那一件磚紅制服，不要消防車紅、猩紅、任何會讀成血的紅」，**「不要有血」那一條原封不動**（**通則又一次：每一輪都要把自己上一版的 `AVOID` 逐條唸過一次**）。⚠ 磚紅要離塗鴉那位遠一點、紅與紫各站一邊。　⚠⚠⚠ **2026-09-16：v4 換上去了**（使用者：「用這張圖重新合上去」）—— 原檔 `drafts/stand-card-illus-v4-src.jpg` 2000×989，裁成 **2000×945 ＝ 比例 2.116、和 v3 逐格相同**，所以**版面一個數字都沒有動**（98.0 × 46.3 mm、左右各餘 0.0、518 dpi、看得到 80%）。⚠ 圖沒有落進 `uploads/`，是照第九節第 26 條從逐字稿撈的。**他點名的三件逐一量過（不是用看的）**：帽與上衣 `#5c6c85`／`#65697c` ＝ 同一支藍灰 ✅（v3 是綠帽配藍灰上衣）／舉起來的前臂 `#fdc0a0` ＝ 裸露的皮膚 ✅（v3 是 `#fefefe` 白袍袖子）／紫 `#905d7e` 帽與上衣同色，**正好一人** ✅。⚠⚠ **兩件沒有到位，都沒有自己改**（改了就要再生成一次，而他要的是「用這張圖」）：**磚紅 `#a24e48` 有兩個人穿**（提示詞寫的是 EXACTLY ONE）、**後排右邊那位的帽 `#447378` 藍綠配上衣 `#5a677e` 藍灰 ＝ 和他這一輪點名的第一件同一種、只是換了一個人**。⚠⚠⚠ **通則：一條「每一個都要如何」的規則，模型會照著改「被點名的那一個」，其餘同類仍然要逐個驗** —— v4 第 6 段已經改寫成「每一頂帽子逐個檢查」並補了兩個反例，仍然漏掉一頂；驗收因此不可以只量他指出來的那一個，要把同一類全部掃一次（近親：v4 那一輪「`AVOID` 要擋的是不一致本身」治的是規則寫了一半，這一條治的是**規則寫對了、執行只做了一半**）。⚠ 守門幫不上忙：那幾件是圖裡面的事，尺寸、比例、dpi、滿版、溢出每一道都會過。⚠⚠⚠ **2026-09-16：分隔線「縮太小了」—— Ⓜ 倍數那把尺又打開了**（使用者：「Logo 分隔線好像縮太小了　放大一點看看」，推導在 README **第 71-30 節**）。⚠⚠ 上一輪的顆數是**他自己講的「三到四倍」**，而三倍畫出來一顆只有 **1.60 mm**、牙洞 **0.128 mm** ＝ 低於平版印刷守得住的 0.2~0.3 mm（71-28 是知情的取捨，這一輪往回走一點）。⚠⚠⚠ **「放大」有三把尺，只開一把**：倍數 3→2→1（1.60→**2.38**→4.71 mm）／基數 11→10→9（1.60→1.72→1.91）／間距 .50→.40（1.60→1.69）—— 後兩把 2026-09-16 都已由他挑定，**而且基數一動就會換掉「哪幾顆重複」**，混進來那把尺就同時在動兩件事。⚠⚠ **只給整數倍，所以 22 和 11 之間沒有停格**（那三條相鄰的限制要在接縫上也成立，71-15）；要一格落在中間只能換基數（9×2 ＝ 18、10×2 ＝ 20），那是他挑過的東西。⚠ 守門現在**逐格重排一次**驗那三條，不是只驗現在那一格。⚠⚠⚠ **那把尺的盡頭不在版面上，在插圖那張圖上** —— 卡片是固定長寬比，分隔線長高多少「QR 底下到卡片下緣」就矮多少（第九節第 28 條 ②），比例一改插圖就要**重裁**：Ⓜ1 三倍（現在）那一條 41.7 mm・裁 8%／**Ⓜ2 二倍 40.1・裁 12% ✓**／Ⓜ3 一倍 35.5・**裁 22% ✗ `stand-illus-crop.mjs` 會擋下來**（超過 20% ＝ 切到手與胸口）。**那一格選了也印不出來，所以頁面上標紅並寫出理由** —— 不標就是送了一個假選項；⚠ 它同時就是搬家前卡片最下面那一條的大小（11 顆），而那一條他說過「太吵雜」。⚠⚠⚠ **裁法抽成一份 `stand-card.mjs` 的 `裁法()`，兩邊都 import** —— 各寫一份的話頁面上會印一組和真的裁出來對不起來的數字**而且不報錯**；墨的框只有裁圖那一支量得到，所以記進 `stand-card.json` 的 `插圖.墨框`，**而那一支每次跑都拿自己量到的值逐個對**。**通則：一份量出來的數字要放到第二個地方用時，記下來可以，但量它的那一支要負責對它 —— 沒有那一道，它就是第二個真相。**⚠⚠ **尺上那三格畫的是真的那一條**（同一支 `bandSvg`、同一個滿版寬度、疊起來左緣對齊），**而且要用新的 class `bnd mrb`、不要叫 `bandrow`** —— 那個名字被 71-16 的守門擋著（Ⓚ／Ⓛ 已定案收掉）。**一把尺重開，要用自己的名字，不要去鬆掉別人的那一道。**⚠ 守門因此把分隔線分成兩種數（卡片上那一條驗 `BAND.顆數`、尺上逐格驗自己的顆數），顏色、牙洞、`fill-rule` 那幾道兩種都驗。⚠⚠⚠ **守門第一版在那一格整個不見時丟 TypeError** —— `ok()` 是收集不是中斷，後面那個 `forEach` 就去 index 一個 undefined。**通則（又一次）：守門要對「那個東西不見了」說人話。**⚠ 「・現在這樣」是算出來的、標籤裡也不准寫死顆數（基數一改它就會說謊）。**八種負向測過。**⚠⚠⚠ **2026-09-16：插圖 v5 的規格 —— 加動作與聲音的線**（使用者：「圖片有點太安靜了……左三黃衣服的看起來是往上叫喊……右邊拿手機打字……其他人也可以稍微加一點　給提示詞」）。提示詞在那一份第五之七節，推導在 README **第 71-27 節**。⚠⚠⚠ **他決定的正是那一份第六節第 5 項，而且範圍比那一條大** —— 那一條只給 Ⓐ 一個人、講的是**情緒**；他要的是**動作與聲音**，所以 `AVOID` 那一行的 `motion lines` 拿掉了（`sparkles`／`speech bubbles`／`icons` 仍然禁）＝ **「每一輪都要把自己上一版的 `AVOID` 逐條唸過一次」的第三次現場**。⚠⚠ 站上那條「用線畫看不見的東西」因此第一次從**風與氣流**擴到**動作與聲音**，形狀照抄颱風那一輪（一組三到四道同向等距的短直線，不可以是一條長曲線）。⚠⚠⚠ **一條只有量過才知道的硬邊界：線不可以往上長** —— 墨的框從 y=104 開始，而裁圖那一支已經從上面那塊白借走 60 列；線畫到那條線以上，那一條就會變成左右留白、人被邊緣切掉讀成畫錯。⚠⚠ **誠實的一半：線加得再多，站著不動的人還是安靜的**（規矩是「一道線沒有在解釋已經發生的動作就不要畫」，而十個人裡只有四個有動作）—— 要更熱鬧只有給後排的人動作，那會動到構圖，**沒有自己走**。⚠⚠⚠ **v5 只加線，人物與衣著維持 v4**（使用者兩次指定「人物和穿著就先這樣」——原本排著順手修那兩件：磚紅兩人／後排右邊那位帽與上衣不同色）。⚠⚠ **難的不是刪那兩句，是刪完會留下孤兒指令** —— v4 的正文與 `AVOID` 裡有一整組為那兩件寫的祈使句（磚紅 `EXACTLY ONE PERSON`、同色不可超過兩人、帽子要和上衣同色），**而 v4 的成品違反其中兩條**，留著就等於一邊說「衣服照抄參考圖 ⑤」一邊叫它改，模型會把衣服重新擲一次骰子。**通則（「上一版的病名會變成下一版的指令」第五次現場）：把一件事從射程裡拿掉時，要連它在正文與 `AVOID` 裡留下的每一條祈使句一起拿掉。**做法：第 6、10 段改寫成「參考圖 ⑤ 已經畫好，逐個照抄、不要重新分配」，數量的硬條件整組拿掉、換成「任何人的刷手服、帽子、印花或顏色都不可以改」；**驗收第 5 條跟著翻面 —— 看到磚紅仍然兩人、⑨ 的帽仍然藍綠才算對**。　⚠⚠⚠ **2026-09-16：v5 換上去了**（使用者：「圖片好了　合上去給我看」）——原檔 `drafts/stand-card-illus-v5-src.jpg` **1456×720**、裁到墨的框 **1456×620 ＝ 2.348**，畫出來 **98.0 × 41.7 mm、左右各餘 0.0 mm**，**版面一個數字都沒有動**（只換 `原檔`／`原尺寸`／`裁成` 三個欄位）。⚠⚠ **衣服是拿整張圖的色票比的，不是挑點量** —— 挑點只答得了「我指的那一個對不對」，而這一輪要答的是「十個人的衣服有沒有任何一件變了」：剔掉白底、線稿、灰與膚色再分箱，磚紅 `#a34e49`→`#9c4b46`（**仍是全圖最大的一塊衣服 ＝ 兩個人**）／藍 `#5374a1`→`#557298`／深綠 `#566d58`→`#566b55`／紫 `#8e5d7d`→`#8a5c77`／青綠 `#447274`→`#486d6a`／**⑨ 的上衣 `#5a6983`→`#5b677a`（帽仍在青綠那一支）**，**色相一支都沒換**、翻面的那兩件都還在 ＝ 第 5 條過。⚠⚠⚠ **線的數量也要量，而偵測器會被它自己的目標騙**：靠「周圍 6px 是不是白的」挑短筆畫，**第一版只數到 4 道** —— 同一組裡的三道線只隔 5~10px，**每一道都被旁邊那一道判成「周圍不乾淨」**，只有落單的活下來；修法是分兩趟（先照形狀挑出候選，再驗周圍時把**其他候選**也算成乾淨）。修好之後 v5 **16~18 道、10~11 組**，同一支偵測器在 v4 上只有 3~8 道（白袍的褶線）＝ 真正加上去約 **10~15 道**，在「不超過 16 道」之內。**通則：一個「數得出來」的驗收，偵測器要先被它自己的目標騙一次才算寫對** ——凡是靠「周圍乾不乾淨」判斷的偵測，同類的東西成群出現時一定要先把同類排除掉。⚠⚠⚠ **兩件要報的**：**解析度第一次往下掉**（前四版 2000 寬、這一版 1456，小 27% → 印出來 **518 dpi 掉到 378**，仍在 300 之上）；墨的框上緣從 y104 往上跑到 **y54**（線畫上去了），下緣裁掉的從 42px 變成 **54px ＝ 墨高的 8.1%**（上限 20%）。⚠⚠⚠ **2026-09-16 稍晚：那條標誌帶搬去當分隔線了**（使用者：「插圖已經很豐富了　下面的 logo 帶反而太吵雜　把 logo 縮小改成淡墨色　移到　芳仁牙醫有 line 囉　當作是分隔線的概念……三到四倍」），推導在 README **第 71-28 節**。三件一起換：**位置**（卡片最下面 → 抬頭底下）、**顏色**（白標誌壓在綠底 → 淡墨 `#5c5f57` 畫在白紙上、底色那塊 `rect` 拿掉）、**顆數**（11 → **33 ＝ 基數 11 × 3**）。⚠⚠⚠ **顆數只給整數倍**：那三條相鄰的限制**在接縫上也要成立**，整數倍等於把同一段接回它自己、接縫那一對驗一次就代表每一個接縫；非整數倍要另外寫一份順序，**不可以就地截一段**（守門改成拿真的排出來的那一列逐對驗）。⚠⚠⚠ **牙洞在這個尺寸印不出來，那不是做壞了**：最小的一顆 1.57 mm 寬、洞 **0.13 mm**，而平版印刷守得住的大約 0.2~0.3 mm —— 那一排讀起來是**一條有節奏的線**，**分隔線本來就不必認得出牙洞**（所以他上一則「牙洞偏小、調整看看」那一件，在這個尺寸沒有意義）。⚠⚠ 它一走，卡片最下面那 9.4 mm 還給插圖：**插圖不再沉進帶子後面**（負的下外距與帶子的 `z-index` 都拿掉），人的下半身改由**卡片自己的下緣**切掉，那一條從 98×46.3（看得到 36.9）變成 **98×41.7、整條都看得到 ＝ 多看到 4.9 mm**。⚠⚠⚠ **裁圖那一支因此兩個方向都要湊**：比那一條寬（矮）→ 從頭頂那塊白借高度；**比那一條瘦（高）→ 從下緣裁掉一截**（這一版 42px，那裡是身體、本來就被下緣切掉）—— ⚠⚠ **上緣永遠不准裁**（那是頭）、**下緣永遠不准補白**（會在卡片最下面留一條白），另加一道「下緣裁掉不可以超過墨的 20%」。⚠⚠⚠ **同一輪治掉他上一則那四件，其中兩件是量出來的**：**「下緣有白縫」版面上沒有縫** —— 帶子的下緣和卡片的下緣**逐位相同**，看到的是**模擬圖自己的**卡片圓角（7px，兩個下角把綠色切掉）＋ 最後**一個裝置列**的抗鋸齒（79 → 最亮 169），**印出來的卡是方的、綠色到邊**；**`@fafa070` 上下 `.035／.018` → `.024／.012`**（3.43／1.76 → 2.35／1.18 mm，2:1 沒動）。⚠⚠⚠ **而「QR 往上、人群也往上」我先算錯了方向**：人物的大小被「滿版 98 mm」鎖死（印出來的頭多大 ＝ 頭的像素 × 98÷2000，只跟橫向密度有關），所以縮 ID 間距省下來的高度**只會變成人頭上多一條白**；真正讓人群往上移的是**帶子搬走**（插圖那一條從「比它寬」翻成「比它瘦」，裁圖改成裁下緣、一列白都不必補）。**通則：一個「變大／往上移」的要求，先問那個東西的大小是被哪一個方向鎖住的 —— 鎖在寬度上的時候，給它更多高度只會多出留白。**⚠⚠⚠ **2026-09-16：中間那兩格 ＋ 顏色那把尺**（使用者：「保留 M1 M2　再做兩版介於兩個之間　顏色再淡一點」，推導在 README **第 71-31 節**）——**兩件各自獨立，所以是兩把尺**，而且**各自畫在對方的預設值上**（顆數那把畫在預設的灰上、顏色那把畫在預設的顆數上）。**① 「介於中間」在上一把尺上畫不出來** —— 上一輪那把只動倍數（基數固定 11），而 **11 的整數倍之間沒有停格**（33→22→11），所以那把尺從「倍數」改成「**顆數**」、每一格自己帶著基數：**Ⓜ1 33（11×3，現在）／Ⓜa 30（10×3）／Ⓜb 27（9×3）／Ⓜ2 22（11×2）**，一顆最高 1.60／1.72／1.91／2.38 mm、插圖那一條 41.7／41.5／41.1／40.1 mm（裁 8／9／9／12% 全部 ✓）。⚠⚠⚠ **代價不是另一把尺，是這一把的副作用**：基數換了「**哪幾顆重複**」就跟著換（11 顆重複 `r1c2`＋`r3c1`、10 顆只有 `r1c2`、**9 顆一顆都沒有**），而那正是他 2026-09-16 挑定的 Ⓚ3 —— **挑中間那兩格等於同時改掉它**，所以每一格都把重複那一串印在頁上、不一樣的還標一行紅字。**通則：一把尺的中間那幾格如果只能靠動另一個已經挑定的變數才做得出來，那不是「多兩格」，是「多兩格 ＋ 一個他要重挑的東西」——要印在同一格上，不要讓它看起來是免費的。**⚠ **Ⓜ3（11 顆）從畫面上拿掉了**（他保留的是 Ⓜ1 與 Ⓜ2，而那一格裁 22% ＝ 選了也印不出來）：**拿掉的是畫面不是那一格**，它留在資料裡標 `不畫`、數字仍然印在面板上。⚠⚠ **順手補上一道 71-30 沒有的**：**兩格算出同一個顆數**（10×3 打成 11×3）—— 尺少一格、兩格同時標「現在這樣」，**畫面完全正常**（負向測第一次就全綠）。**② 顏色那把（Ⓟ）變的是「混多少白」不是換一支色**：`墨` 仍然釘在柔墨 `#5c5f57`（站上 `--ink-soft`），`淡` 是往白色混幾成 ＝ **色相一個度都沒有動**（同夜間模式那條）。四格 **Ⓟ0 不混 6.50／Ⓟ1 三成 `#8d8f89` 3.27／Ⓟ2 五成 `#aeafab` 2.21（預設）／Ⓟ3 七成 `#cecfcd` 1.56**。⚠⚠⚠ **這把尺的盡頭是站上自己那條線**：Ⓟ3 對白 1.56，而站上 `--rule`（`#cdd0d2`，全站每一條分隔線）是 **1.55** —— 一模一樣的份量。**通則：一把「要多淡」的尺，盡頭不要憑感覺挑 —— 去站上找「已經在做同一件事的那個東西」，淡到和它一樣就是盡頭。**⚠⚠ 守門那一道因此**翻面**（同 71-13 的留白）：預設**可以**低於 3:1，但只准在①那一格在尺上②頁面上講得出那條線在哪③**尺上仍然有一格過得了 3:1** 三件同時成立時。⚠⚠⚠ **而那一道第一次寫弱了（這條線第十五次「掃整頁等於沒掃」）**：寫成掃 `3:1`／`--rule`／`1.55`，可是那三段在這一頁上不只出現一次（每一格自己就印著 3:1）——**把那句話整句刪掉照樣全綠**；改成逐段找那句話裡獨一無二的三段。⚠ **踩到的**：**模板字串的 CSS 註解裡不能有反引號**（第七次 —— 為了寫「一定要寫 `>`」而加，整個樣板提早關掉、`HTML` 變成 `false`，症狀是幾十行外的 `writeFileSync` 丟 `ERR_INVALID_ARG_TYPE`），以及 `.mrow b` 要寫成 `.mrow>b`（那一格的說明裡也有 `<b>`，後代選擇器會把它們推成自己一行）。**九種負向測過。**⚠⚠⚠ **2026-09-16 定案：Ⓜb ＝ 27 顆（基數 9 × 3）**（使用者：「留MB」）—— 值寫回預設、**四格的帶子條從頁面上收掉、數字留在面板**（同 50-22、71-13、71-14、71-16）；顏色那把（Ⓟ）**還沒挑**，預設仍然是混白五成。量到 一顆最高 **1.91 mm**・牙洞 0.153・四邊與中間 0.96・帶子高 3.82・抬頭到主文 10.9・插圖那一條 **41.0 mm**（比例 2.390，插圖重裁成 1456×609、下緣裁墨的 10%）。⚠ 牙洞仍然低於平版印刷的 0.2~0.3 mm，**那條取捨沒有變**（分隔線本來就不必認得出牙洞），只是節奏粗了一階。⚠⚠⚠ **它同時改掉了 Ⓚ3**：基數 11 → 9，所以那一條上**一顆形狀都不重複** —— 那不是漏改，是那一格上紅字標著的代價（71-31 把它做成每一格都印的一行），他看過才挑的；`顆數_定案` 因此整段改寫，**不寫的話日後看到 `顆數: 9` 配一份寫著 Ⓚ3 的定案欄，會以為是有人改壞了**。⚠⚠⚠ **「重複」那一欄的意思跟著換了**：它本來是「定案那一份裡真的重複的是哪幾顆」（守門逐字相等），而 9 顆那一份一顆都不重複、10／11 顆那兩份還在資料裡且仍然要靠它驗「有沒有偷放別顆」—— 兩個用途岔開了。定案改成**一張白名單**（＋資料裡多一欄 `重複_說明`），守門換成兩件：定案那一份裡重複的每一顆都要在名單裡、名單裡不可以有三份順序都沒重複過的顆；`重複_說明` 不見了就 throw。**通則：一欄資料同時被兩個地方讀的時候，它們對那一欄的定義可能只是碰巧一致 —— 哪天不一致了，要改的是定義與守門，不是把其中一邊的值湊回去。**⚠⚠ 守門**翻面不放水**：`bnd mrb` 一格都不准畫、`顆案` 一格都不可以刪、**面板要逐格印**，而且不只尺寸還有那兩欄代價（哪幾顆重複、裁不裁得動 —— 一格「選了也印不出來」而看不出來就是送了假選項），三條相鄰的限制仍然逐格重排逐對驗（那把尺還會再打開）。⚠⚠⚠ **第十六次「掃整頁等於沒掃」**：「← 定案」那個記號是算出來的，守門第一版寫成「GEN 裡有沒有 `k.現在 ?`」，而**顏色那把尺的標籤裡也有一個** —— 把面板那一個換成寫死的字照樣全綠；改成**切出面板那一圈**再找。**十種負向測過。** |⚠⚠⚠ **2026-09-16 定案：Ⓟ2（混白五成 `#aeafab` ＝ 本來的預設）** —— 這一輪只有收尺：帶子條從頁面上拿掉、四格的色碼與對白改印在面板、`淡案` 一格都沒刪，守門翻面（`色條.length === 0`／`bnd nrb` 不准再出現／資料 ≥2 格／面板要逐格印），「3:1 那條線在哪」從頁面移到面板。⚠⚠ 同一輪使用者指出**流程太慢**（「只是把已經做出來的選要的保留　不要的移除　為什麼要這麼久」）——**通則：一輪只是收尺的時候，改一次、驗一次、一行紀錄就好，不要再跑整套負向測試與截圖。**
| 招呼圖卡 | `drafts/channels/welcome-card.json`（Flex）＋ `README.md` 第十八節 → 規格頁 **`/preview/line-welcome/`**。✅ **2026-09-03 定案** |
| 招呼圖卡的頭圖 | `drafts/line-hello/`（`generate.mjs`／`bubble.mjs`／`README.md`）→ `preview/line-hello/hero-zenmaru.jpg`，規格頁 `/preview/line-hello/`。✅ 定案 |
| **還沒綁定就按「約診查詢」** | ⚠ **提案中**（2026-09-10 開，`/preview/line-bind-prompt/`；**切換條已拿掉**，網址參數仍然吃得到），推導在 `drafts/channels/README.md` **第三十九節**，守門 `node drafts/channels/check-bind-prompt.mjs`（負向測過四種）。起點是使用者的後台截圖 ＋「製作加入好友但還沒綁定，點約診查詢會跳出綁定通知，**用之前綁定的圖片**製作這個自動回覆」。⚠⚠⚠ **這一則已經在跑了 —— 是改版不是新增**（截圖上 18:57 那張藍色公版卡就是它），而第 14-3 節那張盤點表上**沒有它** ＝ **第四次「我盤的是截圖不是清單」**（前三次是滿意度調查卡、預約成功、約診紀錄查詢）——**那份「會自動送出哪幾則」的清單仍然是跟廠商要的第一件事**。⚠⚠ **它和盤點表的 ⑩「加了好友但沒綁定」不是同一件事**：⑩ 是主動推播（打擾 −8、還卡在「3,205 個好友裡多少人綁了」那個數字），這一則是**他自己按下去那一刻**（打擾 0、不必等那個數字，而且是全帳號**意願最高的一秒**）。**同一件事換一個時機，分數完全不同**；⑩ 那個數字現在決定的只剩「這一張每個月被幾個人看到」。⚠ **只有廠商送得出來**（後台的自動回應只比對關鍵字，不知道這個人綁了沒）。⚠ 現況量到（截圖 1125×2436 ÷3，**那是診所端後台不是病人手機**）：卡片 **312**、頭圖 **312×207（1.5:1）**、按鈕 269×48；**結構和我們要做的一模一樣**（一張頭圖 ＋ 一顆按鈕），但**藥丸、兩行大標題、兩隻吉祥物全部烘在圖裡 ＝ 一個字都改不了**。四件都是內容不是美感：**沒有回答他剛按的那件事**／整張都是廠商的／「啟用完整服務」是系統的話／**沒有第二條路**。⚠⚠⚠ **2026-09-10 稍晚：標題換成使用者指定的逐字**「**30秒快速綁定　啟用完整服務**」（全形空格，**三案共用**，所以那把尺變成「**標題底下寫什麼**」；量到 216.5px／可用 240，**一行放得下、守門要求它就是一行**）。**這推翻了原本那句「僅需花費30秒不抄」——那是他的決定，照做**；但數字的出處沒變（廠商那張公版卡上的，我們沒量過），所以照這條線一貫的做法：**照寫、把證據攤開、給一條收掉的路**（診所自己綁一次計時）。⚠ **面板那一列是第三級「還沒驗過」不是紅線** —— 混成紅色的話他定案的字每次打開都被標紅（＝ 25-13 那條）。⚠ **守門跟著翻面**：「不准出現秒數」→「**標題一定要逐字等於那一句**」（改回舊寫法畫面照樣正常、每一道尺寸守門都會過，只有讀字才看得出來），「48 小時／2 天」那一道沒有動。⚠ 順帶一件**支持他**：「啟用完整服務」和**圖文選單那張的副標「綁定後啟用完整功能」是同一套詞**，而那兩塊前後腳出現在同一個畫面上 —— 一致是對的；原本「那是系統的話」那個批評**移交給底下那一段**（Ⓐ 舉例、Ⓑ 拆開、Ⓒ 不翻譯）。⚠⚠ **按鈕的字沒有開尺** —— 招呼圖卡上已經有一顆一模一樣的（綠底、白色標誌、**「點這裡綁定」**），**同一個動作在同一個帳號裡要長一樣**；守門有一道在對 `welcome-card.json`，那一頭改了這一頁會被擋下來。⚠ **電話那一格是取捨不是美感**：做成第二顆按鈕最顯眼，但**它把人推向電話**，而電話正是這間診所的結構性瓶頸（六個月 1,886 通）——**這張卡的工作本來就是要減少那些電話**；判準同第十一之四節「**看它上一句話**」。⚠⚠ **規格頁第 ② 段的用意：同一張圖會在一分鐘內出現兩次**（這一張 → 公版的「綁定成功」→ 已定稿的綁定完成卡，兩張都吃 `hero-bind.jpg`）—— **沒有自己決定**，兩個解法都不必重畫（這一張不放圖，或綁定完成那一張不放圖，**那一則的字已經定稿、動的只是圖**）。⚠ 那一段擺的是 `shot-bind-done.png`（**它自己那一頁拍出來的圖**），不是用 CSS 再畫一次。⚠⚠ **「✅ …綁定成功！」是以病人的身分送進聊天室的** —— 截圖上它**在左邊、帶著病人的頭像**，和 `[約診紀錄查詢]` 同一側（診所送出的那張卡在右邊、旁邊是「已讀」）。所以廠商說「公版改不了」是合理的，**而且不必改：在它後面再送我們那一張就好**（第三十一節那個結論因此更硬）。⚠ **刻意不寫的三個數字**：30 秒、看診前 48 小時、看診前 2 天 —— 後兩種現在一則一種（綁定完成／招呼圖卡）**還沒統一**，這一則寫「看診前我們也會在這裡提醒你」，**不要生出第三種**（守門在掃）。⚠ **這個資料夾裡只有 `index.html`**：四張圖（`hero-bind.jpg`／`mark-white.png`／`mark-tel.png`／`shot-bind-done.png`）**全部引用別人那一份**，複製一份就是多一個會漂掉的真相。量到：現況 252px／**Ⓐ 374px（一屏 52%）**／Ⓑ＋電話鈕 403／Ⓒ 最短 104，孤字 0・溢出 0・切換條 13.3%・紅線 0。⚠ **Flex 的 JSON 等使用者挑完才寫**（頭圖已經在 `assets/line/` 上，圖不必再做）。⚠⚠⚠ **2026-09-10 再一件：標題和按鈕留著、其他文字全部拿掉**（使用者：「標題和按鈕保存　其他文字拿掉」）——卡上只剩**三樣**：頭圖、標題、那顆「點這裡綁定」。**切換條跟著整條收掉**（三把尺都定了），**網址參數仍然吃得到**（`?c=a|b`／`?h=off`／`?t=note|btn`）。⚠ 量到 **238px ＝ 一屏的 36%，比現況那張 252px 還矮，而且圖與字都是芳仁的**。⚠⚠ **拿掉的三段各帶走一件事，三件不同級**：標題底下那一段是**唯一提到他剛按的「約診查詢」、也是唯一替「完整服務」翻譯**的地方；家人那一句**沒有損失**（綁定完成那一張講過了）；**電話那一行是綁不起來的人唯一的出路**。所以現況那四件現在是「② 治好了、① ③ ④ 維持現況那一版」——那是他的決定，頁面上攤開、**沒有自己留一句回去**。⚠⚠⚠ **電話拿掉之後會走進一個圈子，而那個圈子本來就在**：卡上沒有別的路 → 他直接在這裡打字 → 打到**已經定稿的自動回應**，那一則**確實給得出電話**（不是死路），**但它接著寫著「查自己的約診　點下面選單的『約診查詢』」** ＝ 他剛剛按不動的那一格，**被送回起點**。先前卡上有電話、他不必走到那一步，所以這一改**是讓那個圈子變成可達的，不是造出來的**。兩個解法都不必動這張卡（`?t=note` 把電話放回來，或改自動回應那一句 —— ⚠ **那一則已定稿，要動得他點頭**）。⚠⚠ **通則：拿掉一段字之前，除了問「它是不是某件事實的唯一出處」（第二十三節），還要問「拿掉之後他會改走哪一條路、那條路通不通」**——這一次那條路**是通的、只是繞回原點**，而那只有把兩則擺在一起讀才看得出來。⚠ 守門新增三道並負向測過（**預設那一張只准有標題與按鈕**／**不准再出現 `pv-bar`**／**`var state` 的預設真的是 `c/on/off`** ——三道擋的都是「加回去不會讓任何一道尺寸守門翻臉」）。⚠⚠⚠ **2026-09-10 再一件：標題置中**（使用者：「標題置中」）——卡上只剩三樣，**而按鈕本來就置中**，標題靠左的話只有它不在中軸上；**那正是上一輪拿掉底下那一段才長出來的**（「標題靠左」的前提是它底下還有一段跟著它對齊的內文）＝ 第九節第 28 條 ② 又一次。⚠ 寫 Flex JSON 時這是 text 元件的 **`"align": "center"`** 不是 CSS；版面一個數字都沒動（卡片仍是 238px、標題一行 216.5px）。⚠⚠ 守門再加一道並負向測過：**標題左右留白要一樣（±1px）** —— **量墨不要問 `text-align`**，那個屬性被蓋掉時畫面照樣靠左而每一道尺寸守門都會過。⚠⚠⚠ **2026-09-10 再一件：標題中間那個空格小一點**（使用者：「空格小一點」）——⚠⚠ **答案卡在一件平台事實：Flex 沒有字距** ，站上 HERO 那首詩把全形空格縮窄用的 `letter-spacing: -.57em`（`.g2`）**那條路在 LINE 上不存在**，能改這個空隙的**只有那個空格自己的大小** ＝ 寫 JSON 時標題要拆成 contents 的三段（文字／空格的 span 帶自己的 `size`／文字）。⚠ 拆 span **不影響 `textContent`**，所以「標題逐字」與「只有標題與按鈕」那兩道守門原封不動。四格（其餘每一個字完全一樣）：16px 現在這樣（不必拆 span，整句 217px）／13 ＝ `xs`（214）／**11 ＝ `xxs`（212，建議、預設）**／9 ＝ `"9px"`（210）。⚠⚠ **建議 11 的理由不是好看，是它是 Flex 內建最小的那一階** —— 給廠商的是**一個名字不是一個數字**，少一個「這個 px 值吃不吃」的地方。**通則：同一個效果有好幾種寫法時，挑那個「對方不必多做任何判斷」的寫法。**⚠ 四格並排在第 ② 段，**每一格都畫成卡片真正的 268 與 16px**（這條線第四次記）。⚠⚠⚠ **踩到一個「量測多數出一行」的**：那個空格有了自己的字級，它 rect 的 `top` 和旁邊的字不一樣，而兩邊的量法都是照 `top` 分組 —— 標題被數成 2 行、畫面上一行都沒折。修法是**空白一律跳掉**（`\n`、半形空白、全形空格，JS 的 `\s` 都涵蓋）：**量的是墨，空白沒有墨**。⚠ 守門新增五道並負向測過（span 要在／**畫出來真的是那個大小**／那把尺真的四格／預設含 `g: "11"`／**頁面上不准出現負的字距** —— 用它畫面很像，但那是 LINE 上做不到的路，等於在做不出來的樣子上做決定）。⚠⚠⚠ 最後那一道**第一次跑就被自己的註解擋下來**（頁面上解釋「為什麼不可以用字距」的句子裡就寫著它）——**掃之前要先剝註解**，這條線第五次撞到「每一份檔案都把為什麼寫在自己裡面」。⚠⚠ 另一件是負向測才看到的：守門在「那個 span 根本不見了」時**丟例外**（`getComputedStyle(null)`），擋是擋下來了、印的卻是看不懂的 TypeError ——**守門要對「那個東西不見了」說人話**，那正是它最該說話的時候。⚠ 要問廠商的五題在第三十九節 39-11（最要緊的是**還沒綁定的人按其他格會怎樣、總共有幾種自動回覆**，以及**已綁定但沒有約診**那一則「您尚未有任何預約😃」要不要一起改） |
| **綁定完成** | ✅ **2026-09-04 定稿**（使用者：「最後定稿的是這個」）：**Ⓗ 你整理的・圖卡 ＋ 有頭圖 ＋ 改約那一塊用淡藍 `.10` 的淡底**。Flex 的 JSON 在 **`drafts/channels/bind-done-card.json`**，規格頁 `/preview/line-bind-done/`（**切換條已拿掉**），守門 **`node drafts/channels/check-bind-done.mjs`**，圖片兩張（`node drafts/channels/bind-done-png.mjs`，加 `--chat` 出第二張）：`shot-bind-done.png`（卡片本身，804×1857，3×）與 **`shot-bind-done-chat.png`**（**在 LINE 裡長什麼樣的模擬**，1206×2427，3×）。⚠ 模擬那張要用 **430 寬**拍 —— 429 以下手機框會貼齊頁面兩邊、圓角與左右框線被拿掉。⚠ **換視窗寬度截圖之前要先量「要展示的那個東西有沒有跟著變」**：實測 390 與 430 卡片都是 **268.0px**，只有周邊留白差 12px（第 **19-21** 節）。⚠ **Ⓖ 沒有真的落選** —— 它是「廠商送不出 Flex Message」時的退路（同一份內容一字不差），規格頁最底下仍然畫著它。⚠⚠ **兩塊淡底在 JSON 裡要寫合成後的實色**（警示 `#EEE5E6` ＝ 磚紅 .09、改約 `#E2E8EF` ＝ 淡藍 .10），不要寫半透明。⚠ **沒有拆成 body ＋ footer**（招呼卡那樣拆是因為中間有 separator，這一張兩塊方塊之間刻意沒有分隔線）。推導在第 **19-20** 節。以下是走到定稿的過程 —— **提案中**：`/preview/line-bind-done/`（**八案**，2026-09-03 開；**Ⓖ／Ⓗ 是使用者 2026-09-04 自己整理的那一份**，Ⓖ 純文字、Ⓗ 同一份做成圖卡，帶進三件新事實：提醒**要回覆確認**、**24 小時沒回應系統會自動取消**並遞補、家人綁定**要洽櫃檯**）。推導在 `drafts/channels/README.md` **第十九節**（09-04 那一輪在 **19-12**），定稿文字在 `drafts/line-auto-reply/README.md` 第 24-2 節「貳」。⚠ 使用者挑完才寫 Flex 的 JSON。⚠⚠ **時間的說法 2026-09-04 定案「看診前 48 小時」**，切換條上那一排已經收掉；**但招呼圖卡仍然寫「看診前2天」**（使用者自己逐字寫的、已定案），兩則目前不一致，**要統一得他點頭**。⚠ 這個 48 小時和「看診日 2 天前（不含假日）」是兩個不同的數字（提醒時機 vs 改約期限），**不要順手改成一致**。⚠⚠ 2026-09-04 再兩件（第 **19-13** 節）：「這裡沒有專人即時回覆訊息」**搬到改約那一段前面**（它是「為什麼要打電話」的前提）；24 小時那一段接上一把**強調**的尺（`?e=`），**兩種容器的四格不一樣** —— LINE 的**純文字訊息沒有粗體也沒有顏色**，只剩空行與記號，Flex 圖卡才做得到加粗／換色／淡底方塊。⚠⚠ 2026-09-04 再兩件（第 **19-15** 節）：**圖卡那一側定案「淡底方塊」**（切換條上收掉、寫死；純文字那一側還沒定案、那一排還在）；**改約那一句換成使用者的寫法**「約診更改或取消，最晚請於看診日前 2 天（不含假日）、於診所開診時段來電變更」——「最晚」把期限講死，**「開診時段」取代「週一至週五」**（國定假日休不休診還沒問到，寫「開診時段」就不必替診所答那一題）。⚠ 其餘六案仍是舊寫法，**刻意沒動**，等他挑完哪一案再統一。⚠⚠ 2026-09-04 再一件（第 **19-16** 節）：**改約那一段也做成淡底方塊**，底色四選一（紙灰／品牌綠／深綠松／同上面），**兩塊之間那條分隔線拿掉了**。⚠⚠ **綠與深綠松是 `.16` 不是 `.09`** —— 量過，九分之一濃時四個底色全部落在 **1.1 階**之內（L\* 90.6~91.7），**四格長得一模一樣、那把尺沒有意義**；`.16` 才讀得出是綠的，代價是比上面那塊磚紅重約 4 階。**通則：一把尺給出去之前先算四格彼此差多少，差不出來的尺比沒有尺還糟**。⚠⚠ 2026-09-04 再三件（第 **19-17** 節）：**加了淡藍、深綠松那一格拿掉**（深綠松 `.16` 是 L\* 86.2・彩度 2.5，和品牌綠的 87.6・2.7 分不出來）；淡藍**不能用官方色票那支** `#3C596B`（`.16` 彩度只剩 2.5，讀起來是灰的），取的是**齒顎矯正那支套色** `#4478b5`（`.16` 彩度 6.3，站上唯一淡開之後還讀得出是藍的）——**已知取捨：那是矯正的顏色**，但這一則不在站上也不在任何科別頁裡，不會和標記撞在一起。**通則的第二半：每一格還要撐得起它自己的名字，判準是彩度不是色相。**⚠ **數字前後那幾個半形空格拿掉了**（我照站上內文的習慣加的，他自己寫的那一份沒有），「（不含假日）」的**全形括號換成半形**（全形一組吃掉約 19px）。⚠⚠ **Ⓗ 那兩段之間的空行換成段落間距**：`\n\n` 是一整行 **24.5px**，拆成兩個 `text` ＋ 一個 `margin` 是 **8px**。**通則：Flex 裡兩段的距離有兩把工具，不要只會用空行**（`margin` 有 xs 4 ~ xxl 24 六階）；**Ⓖ 純文字那一側沒有這把工具，只有空行**。⚠⚠ 2026-09-04 再兩件（第 **19-18** 節）：**淡藍收到 `.10`**（使用者：「淡藍色看過去比上面的淡紅色還濃　要調成一樣的」）——「一樣濃」＝ **兩塊合成到卡色之後的 L\* 相同**，基準是磚紅 `.09` 的 L\* 91.6，反解出 a = .101。⚠⚠⚠ **而只有藍做得到，品牌綠與深綠松兩格因此都拿掉了**：壓到同一個 L\* 之後剩下的彩度是**淡藍 4.06**／磚紅 3.29／官方藍 1.50／品牌綠 1.37／深綠松 1.19，而**紙灰自己就是 1.18** ——「等重的綠」畫出來就是紙灰那一格。尺剩三格（紙灰／淡藍 .10／同上面）。**通則的第三半：「看得出顏色」與「和旁邊等重」是兩個要求，只有夠飽和的色相同時滿足得了；判斷方法是把每一支色壓到目標 L\* 再看誰的彩度還在。**⚠ **Ⓗ 那一對全形「」拿掉了，改成把選單名加粗**（使用者：「「」的全形也有很大的空格」）——`「`／`」` 各佔一整個全形字寬但墨只有角落那一撇，一對吃掉 **28.3px**（那一行 183.8 → 155.5）。Flex 就是一個 `span` 加 `weight: bold`，顏色不動。⚠⚠ **Ⓖ 純文字那一對留著，不是漏改** —— 純文字沒有粗體，拿掉引號就標示不出那是選單名。⚠ 實作：`<b>` **不可以寫進資料裡**（`spec()` 要拿同一份字串比對孤字），資料維持純文字、多一個 `strong` 欄位，`render()` 那一層才包。⚠ 2026-09-04 **存成圖片了**（第 **19-19** 節）：`node drafts/channels/bind-done-png.mjs` → `preview/line-bind-done/shot-card-{pa,bl,br}.png`（圖卡三格底色）、`shot-text.png`（純文字泡泡）、`shot-cards.png`（三格並排）。⚠⚠ **出在 `preview/` 不是 `drafts/`** —— drafts 進不了 `_site`，而使用者要的是「也存在網路上」（招呼卡那支 `card-png.mjs` 出在 drafts/，兩支落點不同是刻意的）。⚠⚠ **拍的是提案頁自己那張卡**，所以**那一頁改了就要重跑**，不然圖會開始說謊。⚠⚠⚠ **2026-09-07 廠商回覆：「綁定完成訊息為公版，故無法調整」** —— 那句公版我們本來就沒有要改，而且**不必取代它，在它後面再送一則就好**；真正要更正的是 19-5 那句「改不了 → 只能和自動回應共用」（**那只在「由後台回」的前提下成立**）。詳見第十一之七節第 2 項與 `drafts/channels/README.md` 第三十節，要問的三題已經寫進交付頁 |
| 綁定完成的頭圖 | ✅ **2026-09-03 第四版定案**（使用者：「很不錯，就用這個圖吧」）—— 成品 `preview/line-bind-done/hero-bind.jpg`（**1024×512**，`node drafts/bind-done-hero-crop.mjs`，原檔 `drafts/bind-done-v4-src.jpg`），已接進提案頁 Ⓑ 那一案。定稿提示詞與逐輪推導在 **`drafts/line-bind-done-prompt.md`** —— 〈創造亞當〉的診間版（Gemini 出圖，使用者自己跑，走了四版）。⚠⚠⚠ **不要把原作那個「上帝在上」抹平成兩人等高** ——第一版就是這樣被退回的（寓意沒了，而且兩個成年人同高對指會被讀成醫病關係不單純）：**一側是一團人、飄著、比較高，另一側一個人躺著、比較低**，這個不對等就是那張畫的梗。⚠ 但**不對等只能靠位置給、不可以靠肌肉張力給** —— 第三版把病患的手寫成鬆垮的，整個人看起來像喝醉（＝麻醉或鎮靜的聯想）。⚠ 臉一律照站上那一套（有膚色、眉毛、鼻子、有表情的嘴），**不隨風格參考圖走**。逐輪紀錄與六條驗收在那一份，通則在 ILLUSTRATION.md 第十三節 |
| **提醒卡（看診前 48 小時）** | ✅ **2026-09-04 定稿**（使用者：「完工」）：**Ⓑ 新版 ＋ 頭圖 ＋「點一下下面的按鈕，讓我們知道喔」＋ 只有主鈕的角形 ＋ 圓角方框 ＋ 那句離按鈕 5px**（八把尺全部收成寫死的值、切換條整條拿掉）。規格頁 `/preview/line-remind/`，Flex 的 JSON **`drafts/channels/reminder-card.json`（已重寫，可以拿給廠商了）**，守門 `node drafts/channels/check-remind.mjs`，圖三張 `node drafts/channels/remind-png.mjs`（加 `--chat`／`--old`）。⚠⚠⚠ **寫 JSON 時撞到一件：margin 的數字不會和 CSS 逐字相同，而那是對的** —— 規格頁量的是**墨到墨**，而行高 1.7 在字面下方藏了 **4.8px 的半行距**，同一段在 Flex 的 `text` 元件裡本來就有，所以他挑的「5px」換算成 Flex 是 **`margin: "0px"`**。**通則：把量好的版面翻成另一個排版引擎時，要翻的是「畫出來的距離」，不是屬性值** —— 守門因此不比對 margin 的數字，比對**畫出來的上下比例**（兩組都要 17.0 / 5.0）。⚠ Flex 的 `margin`／`padding` **吃 px 字串**，不必湊 `xs/sm/md` 那張四的倍數表。⚠⚠ **姓名與日期兩處刻意不逐字比對**（JSON 寫 `{{patient}}`／`{{date}}`、規格頁寫看得懂的示範值），守門改成檢查「固定的那一段一樣 ＋ 兩邊都是變數的位置 ＋ 規格頁上不准直接印 `{{…}}`」—— **第一版照字面比，兩處都誤報**。⚠ `那句離按鈕` 是唯一從我建議的那一格換掉的（我 8＝0.47，他挑 **5＝0.29**），**上面那 17.0 一樣沒動**。⚠ 量到：卡片 268.0、高 **579.9**、孤字 0、水平溢出 0、JS 錯誤 0，**仍然比現況 615 矮而且帶著頭圖**。⚠ 只留一個網址參數 `?v=old` ＝ 廠商目前在送的那一則。推導在 `drafts/channels/README.md` 第 **20-16** 節。以下是走到定稿的過程 —— 2026-09-04 開了提案頁（兩案 ＋ 四把尺）。⚠⚠ **文案 2026-09-04 晚間換成使用者自己寫的**（哈囉／病人名字／提醒2天後有約要看牙齒喔／日期／按鈕「ok 沒問題」＋logo／須知只留兩條），**加了頭圖**（2026-09-04 稍晚使用者選定並接上，見本列最後一段）。⚠⚠⚠ **取消鈕那一題的分析結論：建議留，但維持文字鈕** —— 真正重要的理由只有一個：**拿掉之後「來不了」和「沒看到訊息」在診所眼裡會變成同一件事**（24 小時沒回應一律自動取消），而診所對這兩種人的處理本來就不一樣（重要療程會打電話追、定期檢查就當他不想來）—— 那顆按鈕是目前唯一分得出來的東西。次要理由：誤按沒有回頭路。**「兩顆會稀釋主要動作」成立，所以留成文字鈕不是實心第二顆**（現況那一顆本來就是文字鈕，那一點現況是對的）。⚠⚠ **「須知寫來電改約、底下又有按鈕」不是二選一** —— 解法是把兩件事講成兩件事：**按鈕＝告訴我們你不會來、電話＝重新約時間**，寫成一前一後就不打架了。**通則：兩個管道看起來打架時，先問它們是不是同一個動作。**⚠⚠⚠ **「2天後」有事實風險**：真截圖那一則是 7/3（五）送、約診 7/6（一）＝ **3 天**，寫死「2天後」對那位病人是錯的，週一週二的約診都會對不上。三條路（維持但要廠商保證固定／**把天數拿掉**，日期本來就在下一行且最大／讓系統填）已經列給他挑 —— **沒有自己改掉他的字**。**通則：使用者寫的字裡有一個會出錯的事實時，照寫、把證據攤開、給選項，不要自己改也不要不講。**⚠⚠ **他把「戴口罩」拿掉了**（現況第 1 條有），照做了但那是**政策改變不是文案精簡，要醫師點頭**。⚠ **主鈕做成可點的 box 不是 `button`**（Flex 的 button 不支援圖示），logo 用招呼卡已產好的 `mark-white.png`（34px），沒有另產一份。⚠ **有頭圖就把頂端那行診所名拿掉**（同綁定完成），切到不放圖它會回來。⚠⚠ **開場那一句要拆兩行**（「哈囉　〔名字〕」／「提醒2天後…」）—— 他原句把變數夾在中間，名字一長會把「喔」擠成孤字；拆開之後 8 個字的名字都還在寬度內。**用字一個都沒改。**⚠ 量到：現況 615px(73%)／新版全留**含頭圖 789px(94%)**／全砍 378px(45%)，頭圖自己就 134px。⚠⚠ 頭圖畫的時候有一個和綁定完成不一樣的條件：**這一則每次約診都送一次，同一個人一年看很多次 —— 要耐看，不要有太強的梗**。⚠⚠ 這一頁是使用者約好要定**版型與那顆確認的按鈕**的地方（第 19-12 節），定完再回頭看綁定完成那一則要不要多寫一句。推導在 `drafts/channels/README.md` **第二十節**。~~⚠⚠⚠ **`drafts/channels/reminder-card.json` 是 2026-09-02 的舊版，不要拿給廠商**~~ —— **2026-09-04 定稿時已經整份重寫**，現在就是定案的那一張（見本列開頭）。⚠⚠ **24 小時自動取消那件事是「這一張卡」的事**：那 24 小時是從這一則送出的那一刻開始算的，綁定完成那一則只是先告訴他有這回事 —— 所以非寫在提醒卡上不可，不然病人是在不知情的狀態下被計時。⚠⚠ **現況那一張是從使用者 2026-09-04 的真截圖逐像素量出來重做的**（內文 16px、姓名與日期 22px、內距 22px、標題列 43.7、按鈕 60、「⚠ 就診須知 ⚠」**靠右不是置中**）——⚠⚠⚠ **第一版是「照描」的，字級全部猜小了（內文猜成 14、內距猜成 13），把現況量成 438px，結論整個錯**。**通則：照截圖描一份和量一份是兩件事** —— 描出來的很像，數字全錯，而後面每一個判斷都建立在那些數字上。重量之後：現況 615px（73%）、新版全留 696px（82%）、全砍 460px（55%），**新版只多 81px**。⚠⚠⚠ **那張截圖是診所端的後台不是病人手機**（標題是病人名字、有「使用手動聊天」與「已讀」），所以它的卡片 348px **不是病人看到的寬度**；⚠ 而 **268 這個數字我們從來沒有在病人手機上量過**（是招呼卡那一輪的工作值）—— 方向是安全的（真的更寬只是保守），但**要一張病人手機的截圖才收得掉**。⚠⚠⚠ **它不是 48 小時送的**：截圖上 7/3（五）送、約診 7/6（一）＝ 3 天。48 小時前是週六、診所沒開 —— 可能是**系統會避開休診日**（那 19-12 那題有一半有答案了），也可能是**固定 3 天前**。**一個樣本推不出通則，要再看一兩則不同星期的**，或直接看後台排程。⚠ 順帶：協議期限是 7/2（四），7/3 送仍然晚一個開診日 —— 8-1 那個矛盾還在，只是沒那麼嚴重。⚠⚠ **有變數的那一行不能手動斷行**（`{{patient}}` 是系統填的）—— 做法是把「提醒您，」單獨一行、讓變數那一行從行首開始，名字 8 個字以內都不會折。⚠ 日期只要求兩件（拿掉年份、月日改中文），**括號維持廠商現在的半形** —— 要求的改動愈小愈可能真的改到。⚠ 「沒有專人＋改約期限＋電話」那一整塊**逐字沿用綁定完成那一則**（淡藍 `.10`），同一件事兩則用同一組字。⚠ 圖三張 `node drafts/channels/remind-png.mjs`（加 `--old`／`--chat`），**拍的是提案頁自己那張卡，那一頁改了就要重跑**。⚠ 時機**維持 48 小時**（第 15-9 節已結案，不要再提拆兩次或用開診日算）。時間換算 `timing.mjs`。⚠⚠⚠ **2026-09-04 深夜第三輪：兩塊淡底整個拿掉、兩顆新按鈕**（第 **20-12** 節）——使用者六件逐字：請攜帶健保卡／如有發燒…請多休息並來電改約／**粉紅底整個拿掉**／取消鈕**加 logo**（shape_r2c3 嘴巴倒過來）／新增「臨時有事須改約，請致電診所」＋**電話鈕**／**藍灰底整個拿掉**；稍晚兩件：**次要那顆的字改成他寫的「掰掰不去囉」**（原本是我擬的「我不能到」）、**電話鈕上面那一句改成「突然有事想改時間，請致電診所」**（原本是我擬的「臨時有事須改約」）。⚠⚠ 至此**這張卡上每一個字都是使用者自己寫的**，我擬的兩處都被換掉了，方向是同一個：**「須」是規定的口氣、「改約」是診所的行話**，他的版本是病人自己會講的話。**通則：我們寫得出正確的句子，寫不出他那種口氣 —— 文案的最後一哩要留給他。**⚠⚠⚠ **「牙洞怎麼調」的答案是不必調** —— 那顆標誌是單一路徑、洞靠 `fill-rule: evenodd` 挖的，**整張上下翻**（繞自己 bbox 中線）洞就跟著翻到右葉下半、仍然被包著；**只翻外框、洞留原位的話，洞會落在形狀外面、靜靜地消失而且不報錯**。產生器 `remind-marks.mjs` 因此加了守門（逐列數「被墨包住的透明像素」，<30 就 throw，實測 1502 ✓）。⚠ **尺寸是算出來的不是挑的**：主鈕那顆 34px 寬→高 16.8，所以 **nogo 也給 34px**（高 11.0）、**話筒給 16.8px 寬**（＝同高），三顆才像一組。⚠⚠ **取消鈕做成線框鈕不是第二顆綠鈕** —— 「兩顆會稀釋主要動作」成立，所以同框同 logo、但底是空的。⚠⚠⚠ **拿掉那兩塊各帶走一件事實，已經寫在頁面底下給他看**：粉紅底帶走「24 小時沒回應會自動取消並遞補」（**那 24 小時是從這一則送出算的，拿掉之後病人是在不知情的狀態下被計時**；給了兩個補救、**那把尺留著**、沒有自己加回去）；藍灰底帶走「沒有專人」與「看診日前 2 天（不含假日）」——**後者拿掉其實是對的**（這張卡 48 小時才送，期限那時已經過了，它的家是綁定完成那一則）。⚠⚠ **電話做成按鈕不違反 09-04 那條通則** —— 那一條禁的是「說明句裡的號碼穿按鈕的衣服」，而這裡上一句「請致電診所」本身就是叫他現在打。**通則：判斷一個東西該不該做成按鈕，看它上一句話。**⚠ 上一輪那句 `fine`（按了我們會打電話）拿掉了 —— 現在是「請致電診所」在做同一件事，留著會兩句搶同一個位置。⚠ 量測（430／393／390／375／360）：卡片 268.0、高 **563.8**、孤字 0、水平溢出 0、JS 錯誤 0、最寬一行 205.2（可用 240）、八個對比 0 個不過（主鈕 6.62／兩顆線框鈕 8.06／那一句 5.92）、切換條 24.0%／收起 5.1%。⚠⚠ **新版現在比現況矮**（563.8 vs 615）**而且還帶著頭圖** ——上一輪「只多 81px」那句作廢。⚠⚠⚠ **2026-09-04 深夜第四輪：「少了要病患點下去的感覺」**（第 **20-13** 節）——使用者：「原來的版本有說點擊回覆，按鈕上也有手勢提示，你們想一下怎麼弄。」⚠⚠⚠ **拆開來是三件各自獨立的事**（同第九節第 17 條那個淡出）：**①有沒有一句話開口叫他點**（現況有、我們拿掉了，**影響最大** —— 這一則的工作就是「請你回覆」，按鈕再像按鈕也不如一句話直接開口）、**②按鈕上的「往前走」記號**（現況是 👉）、**③按鈕看起來像不像鈕**（現況是藥丸，我們是圓角方框，而**方框在這一站是「框」的語彙**、框不見得等於可以按）。**修一個不會治好另外兩個。**⚠⚠⚠ **還有第四件而且畫面上補不回來**：主鈕是「可點的 box」不是 Flex 的 `button`（button 放不進圖示，而使用者要 logo），**box 沒有按壓效果** —— 「點下去的感覺」有一半是按下那一瞬間的回饋，那一半為了 logo 已經換掉了。⚠ **LINE 對可點的 box 會不會給按壓反白，我們沒有實機驗過**、文件也沒寫 ——**加進廠商那幾題**；「主鈕的形狀」最後一格「真的按鈕（沒 logo）」就是這個取捨，**擺出來讓他比、不是建議**。三條尺 `?c=`／`?g=`／`?s=`，**預設開在建議的那一組**（`c=b`＋`g=main`＋`s=box`）。⚠⚠ **角形用站上自己那一條**（首頁「往下滑」、「回到最上面」、文章卡「繼續讀」同一條，**把 x/y 對調轉成朝右**），**不要打一個「›」** —— 那個字在 Android 與 iOS 上長得不一樣、粗細也對不上。⚠⚠ **筆畫的形狀 `getBBox()` 量不到**（不含 stroke），外框要自己算；產生器的「檔案太小＝空圖」門檻因此從位元組改成**看墨佔多少**（角形只有 21%）。⚠ **角形跟著「字」走不是跟著標誌走**：16px 的字配 11px 高 → 5.3px 寬 ——**三顆圖示的基準各不相同**（nogo 34px 寬跟著標誌、話筒 16.8px 寬＝和主鈕標誌同高），**那不是漏改成一致**。⚠⚠ **現況那句話搬到 268px 會折出孤字「診 ↓」**（三個寬度都是）—— 那句是在**診所端後台 348px 的框**裡看的，**再一次證明病人那一側的寬度要自己量**。⚠ 量到：建議那一組 601.5px（仍比現況 615 矮、而且帶著頭圖）、孤字 0、水平溢出 0、JS 錯誤 0。⚠ **切換條收掉三把已定案的尺**（頭圖／24 小時／就診須知，手機一次只放得下四條），**網址參數仍然吃得到**（`?w=box` 把 24 小時放回來）。⚠⚠⚠ **2026-09-04 深夜第五輪：「那一句和按鈕的間隔很大」**（第 **20-14** 節）——⚠⚠⚠ **量出來問題不是「太大」，是「上下一樣大」**：上面（方塊→字面）17.0、下面（字面→按鈕）**18.8**，**下面反而還大一點**，那句話因此浮在正中間、眼睛沒有理由把它歸給按鈕（接近律）。**所以要改的是上下的比例，不是兩邊一起縮** ——**上面那 17.0 一個字都沒動**（它是「就診須知」與「請你回覆」的分界）。尺 `?p=` 量的是**字面到按鈕**：18.8（1.11）／12（0.70）／**8（0.47，預設）**／5（0.29）。⚠⚠ **看得到的距離不等於 `margin`** —— 行高 1.7 讓字面下方多 **4.8px 的半行距**，`margin-top:14` 畫出來是 18.8；margin 由 `calc(var(--ctagap) - 4.8px)` 扣掉，面板每一格現場重量。**通則：「兩個東西看起來多遠」要量墨到墨，不是盒到盒** ——中文的行高把 3~5px 藏在字的上下，寫 margin 的人看不到。⚠ **電話那一句吃同一條規則、跟著一起動**：這張卡有**兩組「一句話＋一顆按鈕」**，間隔不一致會被讀成兩種不同的關係。⚠⚠⚠ **踩到一個坑：自訂屬性不會傳給兄弟** —— 第一版把 `--ctagap` 設在那一句自己身上，而用它的規則是 `.cta + .btn`（作用在**兄弟**上），於是一路退回預設值：**按了沒反應、不報錯、四格量出來一模一樣**（同第九節第 24、25 條那種病，**只有拿數字量才看得出來**）。**通則：一個變數要影響兄弟，就要設在它們的父層。**⚠ 量到：卡片 268.0、高 **585.9**、孤字 0、水平溢出 0、JS 錯誤 0、兩組都是 17.0/8.0；切換條 **24.7%**（「叫他點的那句」那一列的**標籤**縮短才從 27.5% 收回一列，**卡片上的字一個都沒動**）。⚠ **切換條再收掉「掰掰不去囉」那把尺**（他已說「這樣很好」），`?n=text`／`?n=drop` 仍然吃得到。⚠ 稍晚一件：**「突然有事想改時間，請致電診所」改置中**（使用者指定）——改完**兩組「一句話＋一顆按鈕」的排法完全一致**（兩句都置中、兩顆按鈕本來就置中）；只動 `text-align`，高度、折行、間隔一個數字都沒變。⚠⚠⚠ **2026-09-04：頭圖選定並接上**（使用者：「圖片選好了」「用這張圖片　合併成通知提醒頁面」）—— 候診間裡一群人在揮手（病人、家屬、小孩、長輩與診所的人），站上那套手繪語彙。原檔 `drafts/remind-hero-src.jpg`（1376×768），成品 **`preview/line-remind/hero-remind.jpg`（1024×512）**，產圖 `node drafts/channels/remind-hero-crop.mjs`。⚠⚠ **裁法是上 16 / 下 64，不是對稱的 40/40** —— 16:9 收成 2:1 要拿掉 80 列，而**上面幾乎沒有餘裕**（深墨從第 43 列就開始，對半裁只剩 3 列、頭會貼著上緣），**下面每一列都是墨但那是身體與手不是臉**（最低的下巴在第 660 列上下，裁到第 704 列碰不到任何一張臉），**收緊反而讓臉在 268px 上大一點**。四個候選（0/80、16/64、28/52、40/40）**縮到真實的 268px 並排比過**，只有 40/40 看得出頭髮被切、其餘三個分不出來 —— **判準因此回到「餘裕」與「臉的大小」這兩個量得出來的東西**。⚠⚠ **守門不能照抄 `bind-done-hero-crop.mjs`**：那張圖四邊都是紙底，拿「邊緣中位亮度 vs 紙底」就抓得到烘進去的白框；**這一張四邊全是墨**，同一道會一直誤報。改成四道：①長寬比 ②深墨（<120，每列 ≥8 點）離上緣 ≥12 列（臉沒被切）③邊緣要「**又均勻又亮**」才算白框（p95−p5 < 6 且中位 > 紙底−3）④不超過 LINE 的 1024×1024。⚠ **`width`／`height` 屬性一定要寫** —— 不寫的話圖還沒下載完高度是 0，面板會報出一個比實際矮 134px 的卡片。⚠ 接上圖之後五個寬度逐項**和佔位符那一版相同**（卡片 268.0、高 585.9、頭圖 268×134、孤字 0、水平溢出 0、兩組 17.0/8.0）——佔位符本來就是照 `aspect-ratio: 2/1` 預留的。⚠ 切換條那把尺改名「放頭圖」。⚠⚠ **選圖時看著的是這一則自己的條件：耐看** —— 每一次約診都送一次，同一個人一年看很多次；一群人揮手沒有笑點可以用完（綁定完成那張〈創造亞當〉一輩子只出現一次，可以更用力）。⚠ 圖換了就要重跑 `remind-png.mjs`。推導在 `drafts/channels/README.md` 第 **20-15** 節 |
| **提醒卡印成 A4（櫃檯用）** | ✅ **2026-09-16 做好了**（使用者拍了櫃檯現在在用的那一張護貝 A4：「這是診所櫃檯人員印出來 A4 大小，在約診時提醒患者看診前 48 小時要回覆的提醒通知……**把新的做成這個 A4 版面，我要印出來**」）。產生器 **`node drafts/channels/remind-a4.mjs`** → `drafts/channels/芳仁-約診提醒-A4.pdf`（要印的）＋ `remind-a4.png` ＋ `remind-a4.html`（自給自足，帶去任何一台電腦開瀏覽器就印得出同一張）。⚠⚠⚠ **不重畫卡片** —— 開 `/preview/line-remind/`、把 `.pv-hc.pv-new` 的 markup 與那一頁的樣式表整份抓下來，只做四件事：換示範日期／圖檔內嵌成 data URI／放大／擺進一張 A4（同 og-topic-card 那一輪：**要擺真的產出檔，不要用 CSS 再畫一次**）。**所以那一頁改了就要重跑這一支。**⚠ 另有一道守門在對 `reminder-card.json`（卡上每一段字都要找得到），擋的是「改了規格頁、卻沒回頭改要交給廠商的那一份」。⚠⚠ **照片裡那一張不是誰設計的版面，它就是那則訊息的截圖印在 A4 上**，而且**內容只占紙寬六成、四周本來就很空** —— 所以新的這張左右各留 29mm 不是做壞了：卡片是直的（268×448）、A4 是 210×297，撐滿高度之後寬度就只能到 **151.2mm**（放大 2.132 倍；放頭圖那一版是 118.6mm／1.673 倍）。⚠ 換媒介逐項問過的四件（第九節第 28 條 ⑤）：**三顆按鈕都留著**（這張紙的用途正是「你手機上會看到這個」）／⚠⚠⚠ **日期 2026-09-16 使用者指定照 `0000/00/00` 的寫法印**（他先說「日期顯示是 0000/00/00」，隔一句更正「我是給你日期格式　不是真的 00　只有日期要變而已　其他星期　時間都不用改」）——**那幾個 0 是位數不是字面**：印的是真的示範日期（現算成兩天後、跳過六日），只有**日期那一段**從「9月18日」換成「2026/09/18」，後面的「(五) 09:00」沒有動 ＝ `2026/09/18 (五) 09:00`；⚠⚠ **中間那一版（整串印成字面的 `0000/00/00`）已經作廢，不要拿它訂正現行的寫法**。⚠ 要給廠商的 `reminder-card.json` 仍然寫著「去掉年份、月日改中文」——那是對 LINE 上那張卡的要求，這裡是紙上的示範值，兩件事；`--date="…"` 吃任何字串／⚠⚠ **姓名那一格 2026-09-16 使用者指定連同括號一起拿掉**（開場只剩「哈囉」）——**紙上印一個永遠不會是他的名字，比不印還怪**；拿掉時**前面那個全形空格要一起收掉**，`--name=〔病人姓名〕` 放得回來／電話撥不了但讀得出來／**一句我們自己的話都沒有加**（廠商那一版也沒有，而這條線每一則的字都是使用者自己寫的 —— 要加抬頭等他開口）。⚠ 紙上的卡多了一圈很淡的框線：卡片色 `#F4F4F5` 壓在白紙上看不出邊、螢幕上那圈陰影又印不出來，**那是印刷需要不是版面改動**。⚠⚠ **字型要帶著走**：這台容器只有文泉驛（站上那三支一個都沒有），所以產生器去 Google Fonts 取一份**只含這張紙那 88 個字**的 Noto Sans TC（400／700）內嵌成 data URI（74KB）；取不到會退回系統堆疊並印一行提醒，不會靜靜地換字。⚠ **倍率算完寫死進 CSS、這一頁零 JS** —— 交給瀏覽器現算的話，列印時的重排會讓它和我們量到的不是同一件事（門口那張 A4 踩過）。⚠⚠⚠ **2026-09-16 定案不放頭圖**（使用者：「頭圖拿掉」）：高度一直是撐滿的，所以頭圖換到的不是「排得下」是**字大三成**（1.776 → **2.304 倍**）；**代價是那張紙上一個「芳仁」都沒有了**（廠商那一版有一條寫著診所名的綠帶子，我們這一版的品牌本來就是那張頭圖），他看過這一點之後仍然選它。**挑定的值寫回預設**，`--hero` 是回去的路。⚠⚠⚠ **2026-09-16 稍晚補上診所的頭像**（使用者拿 LINE 的截圖：「要把左邊那個診所 logo 貼上去，這樣才像 line 裡的樣子」）——⚠⚠⚠ **照字面做會做錯：它在卡片上面不是左邊**。逐像素量他那張截圖（1125×2436 ＝ 375 CSS px 的 3×）：頭像圓 30×30、**底緣離卡片上緣 5.7px、左緣比卡片左緣再左 3px**，成因是**圖卡吃滿整條可用寬度**、LINE 放不下並排的頭像就把它擺到上面去。⚠⚠ **規格頁那個聊天室模擬畫的是另一種**（`.pv-msg` ＝ 頭像在旁邊、底部對齊）—— 那一份模擬的是**文字泡泡**，**不要拿它當這張紙的依據，也不要反過來去「訂正」規格頁**。**通則（這條線第五次）：模擬別人的介面之前先確認那一側現在長什麼樣 —— 同一個 app 對不同型別的訊息有不同的排法。**⚠⚠ **形狀讀 `brand/shapes/mark.svg`，不抄第二份也不另存 PNG**（存成圖檔就是把它凍在某一個版本上）：綠圓 ＋ 白色標誌，**牙洞是 `fill-rule: evenodd` 挖穿的，填白之後洞會透出底下的綠** ＝ 截圖上的樣子；⚠⚠ **不可以只抄 `<path d>`** —— 那條路徑有兩段（外框 ＋ 牙洞），少抄一段畫出來是一顆實心的牙**而且每一個數字看起來都還很合理**，守門因此數 `M` 有幾段。標誌佔圓的 **75.6%**（截圖量的），長寬比從那份 SVG 自己的 viewBox 算、不寫死。⚠ **圓的顏色沒有從截圖取**（iPhone 的截圖帶色彩描述檔，量到 rgb(61,95,82) 和站上任何一支都對不起來），用站上一般牙科的套色 `#3f654a` ＝ `assets/icon.svg`／`assets/logo.png` 那一支，**不新增顏色**；⚠⚠ 同一張截圖裡「診所資訊」那張卡的綠帶子量到 (124,138,95)，**那一張是廠商的版本不是我們的**（電話還印著作廢的 `05-5339-369`）——**一張截圖能不能拿來校色，要先問那個畫面是誰做的。**⚠ **`assets/icon-192.png` 不能拿來用**：那一張是白底綠標誌，和 LINE 上那顆正好相反。⚠⚠ **代價：倍率從 2.304 掉到 2.132（字小 7.5%）** —— 頭像那一塊 36px 也吃高度，換到的是「這張紙上又看得到芳仁了」（正好補上「不放頭圖」那一項的代價）與「讀起來真的像 LINE」。⚠ 產出的三個檔**只有 PDF 進版控**（重跑要一台有網路又裝得起 playwright 的機器）。⚠ 守門都在產生器裡（同門口那張 A4，沒有另開 check）：字逐段對 JSON／圖找不到就 throw／**卡片原生高度和定稿那張 451.9（扣掉頭圖）差超過 8% 就 throw**／四邊留白還在／**PDF 只有一頁**／**頭像畫出來真的是 30px、間隙 6px、左移 3px**（±0.5，量的是 rect 不是屬性） | `drafts/channels/README.md` 第七十二節 |
| **評價邀約（看診後）** | ✅ **2026-09-05 定稿**（使用者：「ok 定稿 存起來」）：**Ⓐ ＝ 他自己逐字寫的那一版**。規格頁 `/preview/line-review/`（**切換條已拿掉**，⚠ 網址參數仍然吃得到：`?v=old` 現況／`?a=b` 不分流的替代案），Flex 的 JSON 在 **`drafts/channels/review-card.json`**，守門 **`node drafts/channels/check-review.mjs`**，圖五張 `node drafts/channels/review-png.mjs --all`（加 `--chat`／`--old`）。量到（五個寬度逐格相同）：卡片 **268.0×497.6**、頭圖 268×134、自己斷 10 行＝畫出 10 行、孤字 0、水平溢出 0、JS 0、最寬一行 196px（可用 240）、兩組墨距 **11.5 一致**、主鈕白字 6.62／線框鈕 8.06 都過 AA、emoji 0・拿好處換評價 0。⚠⚠⚠ **定稿不等於可以送出去** —— 那個法遵問題原封不動（卡上兩塊合起來 ＝「願意的人去公開、不開心的人來私下」，Google 禁的正好是那兩件）。**那幾句是他逐字寫的所以照寫**，面板標紅、JSON 的 `_說明` 也寫著；**廠商答出那兩顆按鈕連到哪裡之前（第 17-3 節），不要送**。⚠⚠ **寫 JSON 時 margin 不會和規格頁的 CSS 逐字相同，那是對的**（同提醒卡）：規格頁量的是墨到墨，行高在字面下方藏著約 5px 的半行距 —— 11.5px 換算成 Flex 是 **`margin: 6px`**，**兩組吃同一個數字**。⚠ 開場那一行兩邊結構不同（JSON 是 spans ＋ 另一個 text、頁面是一個 `.hi` 兩行），**守門因此拆開比**。⚠ 兩顆按鈕的 `action.label` 要和按鈕上的字一樣（那會變成病人說出去的話）。⚠⚠⚠ **守門差點自己騙自己**：留了一個「永遠會通過的 `ok()` ＋ 一行 `bad.pop()`」當佔位，結果那一行**把前一條真正的失敗吃掉了**。**通則：不要用「先放一個假檢查再 pop 掉」當佔位** ——陣列不知道哪一個是佔位的，它只會刪掉最後一個，而最後一個往往是真的。⚠ 同一輪還學到**驗守門要驗對地方**：第一次「故意改壞一個字」沒被抓到，不是守門漏了，是我改到 `_說明` 裡那句一模一樣的引文（`replace(…, 1)` 只換第一處）——**要測守門，改的是資料不是註解**。⚠ 「不可以印出 `{{…}}`」那一條**只掃卡片不掃整頁**（規格頁的說明本來就要寫出欄位叫什麼名字）。推導在 `drafts/channels/README.md` 第 **21-16** 節。以下是走到定稿的過程 —— **2026-09-04 開提案頁**（使用者：「製作 line 發送評價邀約訊息頁面　先保留圖卡空間　內容你們討論好　做預覽給我看」）。就是第十七節那張**滿意度調查卡**。提案頁 `/preview/line-review/`（兩案 ＋ **兩把尺**），圖三張 `node drafts/channels/review-png.mjs`（加 `--old`／`--chat`），推導在 `drafts/channels/README.md` **第二十一節**。⚠⚠⚠ **這一則和另外三則不一樣：它不只是「醜」的問題** —— 現在那一張**先問「您願意推薦…嗎？」再把人分成兩條路**（願意 → 分享體驗／不願意 → 提供建議），**如果只有前者通向 Google，那是 review gating**（只向滿意的人索取公開評論），據我所知是 Google 明文禁止的，而**商家檔案是這間診所最大的入口**（六個月 14,973 次瀏覽、1,886 通電話）。⚠⚠ **但那兩顆按鈕連到哪裡仍然沒有答案**（第 17-3 節那三題），所以立場沒變：**這是「要問」不是「你們違規了」** —— 問到之前**不要把新版送上去，也不要急著把舊的關掉**。**新版做了三件**：①**拿掉分流**（同一個邀請給所有人，私下的意見管道也給所有人、不是「不滿意的人才走這條」）②**名字從稱謂變成受詞**（第 17-4 節：那則送到阿嬤的帳號、「親愛的 王丞睿，您…」卻指著孫子；新版「〔病人姓名〕　這次辛苦了／謝謝你們跑這一趟」）③**把「為什麼要寫」講出來** —— 不是為了診所的分數，是**幫到下一個正在找牙醫的人**（＝站上「診所給的是燈」那一條）。⚠⚠ 「**不管寫什麼，都會幫到下一個正在找牙醫的人**」那一句是刻意寫的：它當著病人的面說清楚**我們不是只想要好評**，是這張卡沒有分流最直接的證據。⚠ **沒有標題**（現況那個「滿意度調查」是公家表格的詞，而且標題說滿意度、內文問推薦意願是兩件事）。⚠⚠ 「**這裡沒有專人看訊息**」那一句**非寫不可** —— 才剛請他「說幾句話」，不寫的話他會直接在 LINE 裡回、而沒有人看得到。⚠ **只給兩把尺**（其餘照第 18-1 節自己決定）：**為什麼寫**（三格，真的品味題 —— 差別是**這張卡站在誰那一邊**：下一個病人／寫的人／診所，建議第一格）、**意見那一塊**（三格，**診所的事實題** —— 櫃檯接不接得住，建議「只跟櫃檯說」）。⚠ **預設不放電話鈕的理由是量出來的**：電話本來就結構性忙線（六個月 1,886 通），把「想講幾句話」也導進去等於**拿最稀缺的那條線接最不急的事**。⚠⚠ **頭圖先保留位置**（使用者指定），佔位框和真圖**一樣高**（`aspect-ratio: 2/1`）—— **所以現在量到的高度等圖進來也不會變**；不這樣做的話今天量的每一個數字都會在出圖那天作廢。⚠⚠ **提案頁踩到兩件**（都是「量測壞掉但畫面正常」）：**①量行與字數要只看 `.b` 不能量整張卡** —— 佔位框裡那兩行說明會被算成「被再折出來的行」，「最寬的一行」還會變成那句佔位說明；**通則：佔位用的東西不可以進量測**。**②Ⓐ 現況那張的對比度不要報** —— 它的綠是**我從截圖描的不是量的**（那張截圖沒有可以逐像素反推的行，不像提醒卡那一輪），拿描來的顏色算比值會生出一個**看起來很像發現、其實是自己編的數字**（第一版就報了「按鈕白字 3.49 不過」）。⚠⚠⚠ **一條紅線：不可以拿好處換評價**（折價、抽獎、送洗牙都不行）—— Google 明文禁止，醫療機構在台灣還多一層招徠的疑慮；面板有一列在掃。⚠ **按鈕的網址還沒有**：Google 的「寫評論」深連結需要 **Place ID**，我們只有分享短網址 —— 同綁定那顆按鈕的缺口。⚠ 量到：卡片 268.0、高 **539.8**、自己斷 12 行／畫出 12 行、孤字 0、水平溢出 0、JS 錯誤 0、對比 5.14／6.62／8.06 都過 AA、**emoji 0・「親愛的」0・「斗六芳仁」0**、切換條 15.5%。⚠⚠ **Flex 的 JSON 等使用者挑完才寫**（同前兩則的順序），在那之前不要拿任何東西給廠商。⚠⚠⚠ **2026-09-05 第二輪：開場換成使用者自己寫的，下面那一段給五案**（第 **21-9** 節）——他給的逐字是「哈囉　病人姓名　回去牙齒都還好嗎／有什麼狀況的話記得跟診所聯繫一下」，另加一句「**下面的文字再給我幾個方案讓我選。現在的版本太溫柔太客氣。**」⚠ 開場**用字一個都沒改，只做斷行**（名字那一行只能自己一行）。⚠⚠ **他這一句把整張卡的順序換掉了：先關心、再開口** —— 原本第一句「〔病人姓名〕　這次辛苦了」是對**已經過去**的事道辛苦，他這一版問的是**現在**，而且**先給了一件對病人有用的事**（有狀況要聯繫）才輪到我們要的那一件。**要東西之前先給東西。**⚠⚠⚠ **而它讓底下那一塊變得更必要，不是更不必要**：他叫病人「記得跟診所聯繫一下」，而病人手邊**最近的按鈕就是在這個 LINE 裡回一句** —— 這裡沒有人看。所以那一塊的工作從「另外給一個意見管道」變成「**回答他自己丟出來的問題**：要聯繫，聯繫哪裡」，文字改成「這裡沒有專人看訊息／要找我們就打 05-5339369／或下次來跟櫃檯說一聲」（號碼**沒有做成按鈕**，第十一之四節；代價是 Flex 的文字不會自動可撥，要掛 `tel:` 還沒問過他）。⚠⚠⚠ **「太溫柔太客氣」拆開來是三件各自獨立的事**（同第九節第 17 條）：**①墊詞**（「方便的話」把請求包起來，讀起來像在道歉）、**②不敢說是自己要的**（「會幫到下一個正在找牙醫的人」把受益者推給第三人，很得體，但**不承認這是診所在拜託人**）、**③句子太長**（四行、每行完整，像公關稿；**短句本身就比較不客氣**，這一條靠刪不靠換字）。⚠⚠⚠ **但有一條線不可以跟著跨過去：不可以先問滿不滿意** ——「如果覺得還不錯」是**分流的另一種寫法**，而且**比現況那兩顆按鈕更難被發現**，因為它看起來只是一句客套。**五案裡一句都沒有**，Ⓐ 與 Ⓒ 還反過來明寫「好的不好的都寫」。五案（`?a=`）：**Ⓐ 拜託一件事（建議，動①②）**／Ⓑ 很多人在看（動②，唯一給理由的）／Ⓒ 順手寫幾句（動①②③，最不客氣）／Ⓓ 想知道你怎麼看（動①②，主詞換成診所）／Ⓔ 現在的（對照）。⚠ 量到：卡片 268.0、高 **540／565／516／516／565**、自己斷幾行＝畫出幾行、孤字 0、水平溢出 0、JS 錯誤 0、兩組間隔 17.0/5.0、切換條 16.6~19.8%。⚠⚠ 出圖多兩個開關：`--all`（五案各一張 ＋ 並排）與 `--ask=c`。**並排那一張是「先各拍一張真的卡，再把那五張圖排進一頁重拍」，不是用 CSS 把卡片畫五次**（同 og-topic-card 那一輪）；它是橫的又是 2× 不是 3×，**守門要跳過「高比寬還小」那一道、而且比例尺要一起傳進去**，不然會誤報。⚠⚠⚠ **2026-09-05 第三輪：拿掉「Google」、留一顆「不好的」按鈕、灰底拿掉**（第 **21-10** 節）——①**卡片上不寫平台的名字**（五案逐句拿掉，主鈕換成「留下評論」）；**按鈕連到哪裡沒有變**，這樣寫得通是因為**動作本身講得完整**、平台是按下去之後的事。②**灰底方塊拿掉**，收尾改成兩行純文字並收短成「這裡沒有專人看訊息／牙齒有狀況請打 05-5339369」——原本那句「想說什麼跟櫃檯說」拿掉了，那件事現在由第二顆按鈕做，**兩處講同一件事會互相稀釋**。③⚠⚠⚠ **第二顆按鈕（「不好的」那一條）做了，但它讓這張卡更靠近那條線**：**兩顆按鈕並排本身不是違規** —— Google 禁的是**選擇性地只向滿意的人索取公開評論**、以及**把不滿意的人導離公開評論**。所以要同時守住三件，缺一不可：**①沒有先問滿不滿意**（兩顆一起出現、給所有人）、**②公開那一顆不設條件**（主文明寫「好的不好的都寫」，Ⓐ／Ⓒ 才有 —— **第二顆按鈕最好配 Ⓐ 或 Ⓒ**，配 Ⓑ／Ⓓ／Ⓔ 時面板會標紅）、**③第二顆不可以寫成「改成私下講」**（「與其…不如…」「先跟我們說」「不要留評論」都會讓它從「多一個管道」變成「把負評導開」）。三個候選（`?n=`）一個都沒有「不要公開」的意思：直接告訴我們（建議）／有不好的想說／想私下說幾句。⚠⚠ **它要連到哪裡我們還不知道**（＝第 17-3 節那三題的第二題），**問到之前是佔位的**。⚠ 做成**線框鈕**不是第二顆綠鈕（同提醒卡那顆「掰掰不去囉」），標誌用招呼卡已產好的 `mark-green.png`；標籤上限 **9 個全形字**（可用 186px）。⚠ 面板多兩列：**「公開評論那一條」**（判準是**有沒有先被一個問題篩過**，不是有幾顆按鈕）與**「主文有沒有明說『不好的也寫』／第二顆有沒有『不要公開』的意思」**（現場掃字）。⚠ 量到：卡片 268.0、高 550／575／526／526／575、孤字 0、水平溢出 0、JS 錯誤 0、切換條 16.6~19.8%。⚠⚠⚠ **2026-09-05 第四輪：那一段整組換成使用者自己的材料**（第 **21-11** 節）——他口述三件：**「不要說體驗，要說經驗」**（⚠ **現況那張卡寫的正是「願意並分享體驗」**，所以這是換掉現況那個詞，不是選字偏好）、「很重視他的經驗和意見」、「**願意推薦給身邊的親朋好友嗎**」，以及「**點一下下面的按鈕就可以寫」也不用了**。⚠⚠⚠ **但他要的第三句正好是現況那張卡的形狀** —— 一個是非題放在兩顆按鈕正上方，那兩顆就會被讀成它的答案（願意→留下評論／不願意→直接告訴我們）＝**先問、再分流**，正是第 17-3 節那條要問廠商的疑慮。**沒有拿掉他的句子**：只放進 Ⓒ／Ⓓ 兩案，面板新增一列「**那一段是不是問句**」，掃到「嗎？」就標紅並寫出理由。**通則：使用者給的句子有結構性風險時，照寫、隔離、標紅、給替代案**（同提醒卡那條「2天後」）。⚠ 五案因此重編、**字母整組換人**（上一段那句「Ⓐ／Ⓒ 才有『好的不好的都寫』」已作廢）：Ⓐ 想知道經驗／**Ⓑ 都想聽（建議）**／Ⓒ 經驗＋推薦（⚠ 問句）／Ⓓ 只有推薦（⚠ 問句）／Ⓔ 上一版。⚠ **建議 Ⓑ，理由只有一個**：它用的就是他那句「很想知道你這次的經驗」，只有第二行換成「好的不好的都想聽」——**不是問句**，而且它同時就是守住那條線的第二個條件（公開那一顆不設條件）。⚠ 面板那一列的掃描字串一起放寬成 `好的不好的都(寫|想聽|說)`，不然 Ⓑ 明明寫了卻掃不到 ——**掃描表跟著文案走，改文案要回來改掃描表**。⚠ 那一句 CTA 拿掉之後，那一段到按鈕的距離由 `.ask + .btn { margin-top: 7px }` 給，面板現場量**墨到墨 11.5px**（行高 1.7 在字面下方藏著 4.8px 的半行距，margin 的數字不等於看得到的距離）。⚠ `review-png.mjs` 的 `ASKS` 標籤表要和提案頁的 `ASK` 一致，改一邊要回來改另一邊。⚠ 量到（五個寬度 × 五案 × 三個第二顆標籤）：卡片 268.0、高 **494.6**（Ⓐ／Ⓑ／Ⓓ）／**519.1**（Ⓒ／Ⓔ）、自己斷幾行＝畫出幾行、孤字 0、水平溢出 0、JS 錯誤 0、最寬一行 202px、切換條 14.0~19.8%。⚠⚠⚠ **2026-09-05 第五輪：改成兩塊「一句話 ＋ 一顆按鈕」**（第 **21-12** 節）——使用者逐字給了兩塊（「想瞭解你這次的經驗，／願意推薦給身邊的親朋好友嗎？」＋綠鈕**願意並分享經驗**＋角形；「也很重視你的意見，／不好的直接告訴我們」＋線框鈕**不開心 有話要說**），並指定**收尾那兩行整段拿掉**。**排法整個換掉**：從「一段話 ＋ 兩顆按鈕」變成**兩塊各自成對**（＝提醒卡定稿那一張），兩組墨距都 11.5px、面板現場比對一不一致；角形**只有第一顆有**（他只在那一顆寫了）。⚠⚠⚠ **但這個組合正好就是現況那張卡的形狀**：上面是**是非題**→公開、下面是**負面前提**→私下，合起來就是「願意的人去公開、不開心的人來私下」——Google 禁的兩件各中一件，**而且比現況更明白**。**沒有自己改掉他的字**：Ⓐ 照寫、面板標紅、Ⓑ 是**同排法同口氣**只換兩處的替代案（問句→「好的不好的都想聽」、「不開心」→「想直接跟我們說也可以」）。⚠ **面板那一列改成掃形狀、兩件同時成立才標紅**（①公開那顆上面是不是是非題 ②私下那顆含標籤有沒有負面前提）——**單獨一件都不是分流，判準是那兩件的組合**。⚠ 收尾拿掉帶走一件：開場叫他「記得跟診所聯繫一下」，**卡上現在沒有一個地方寫怎麼聯繫**（面板新增一列在盯），**沒有自己加回去**。⚠ `review-png.mjs` 的 `ASKS` 收成兩案，並排圖的寬度改成**由案數算**（原本寫死 `268*5+18*4`）——**產生器裡凡是跟著資料長度走的數字都不要寫死**。⚠ 量到（五個寬度 × 兩案）：卡片 268.0、高 **497.6**、孤字 0、水平溢出 0、JS 錯誤 0、最寬一行 196px、兩組墨距 11.5 一致、切換條 8.9~10.6%。⚠ **2026-09-05 再一件：第二顆的標籤中間加一個半形空格**（使用者：「不開心 有話要說 中間空格」）——同取消卡那顆「是喔 要取消」，**他當時指定的就是半形**（全形 16px、半形只有 5.6px）；標籤墨寬 112 → **117.6px**（可用 186），一行沒折，卡片 268.0×497.6 五個寬度逐格相同。⚠ 面板掃「私下那顆有沒有負面前提」的正規式是 `不開心`，空格加在「心」後面所以不受影響 —— **日後若把空格加進詞中間，那條掃描要一起改**。⚠⚠ **稍早一件：第二顆的 logo 翻過來**（使用者：「不開心有話要說的 logo 要反過來」）——**不必再產一張**：招呼卡那顆 `mark-green.png` 與提醒卡的 `mark-nogo.png` 是**同一顆形狀 `shape-r2c3`、同一個色 `#2c5238`**，後者只是整張上下翻過來，只改一行 `src` 就好；同寬同比例，**版面一個像素都沒動**。⚠⚠ **翻轉是烘在 PNG 裡不是 CSS 的 `scaleY(-1)`** —— 牙洞靠 `fill-rule` 挖，只翻外框、洞留原位會讓洞落在形狀外面、靜靜地消失而且不報錯。**通則：要一顆翻過來的標誌之前，先查這條線上有沒有人已經翻過**（現有兩顆：綠的 `mark-nogo.png`、白的 `mark-nogo-white.png`，同一支產生器）。⚠⚠⚠ **2026-09-05：頭圖的提示詞寫好了 —— 評審席，鏡頭在評審背後**（推導與兩份提示詞在 **`drafts/line-review-hero-prompt.md`**，通則進 ILLUSTRATION.md 第十四節，紀錄在第 **21-13** 節）。使用者：「電視節目的比賽，評審會舉牌寫圈或叉、或是寫分數……**但如果評審舉牌給分，那個視線不知道要看向哪裡，會不會收到的人以為是有人要對他評分，但其實我們是要邀請他給診所評分。**」⚠⚠⚠ **他點出來的問題比他講的還嚴重一階，而且不是視線的問題**：這一類場面永遠有兩個角色（台上被評的人、桌後的評審），而**觀眾會自動接手鏡頭所在的那一個** —— 評審席畫成正面朝鏡頭 ＝ 鏡頭站在被評的人那一格 ＝ 看的人正在被打分數。**調視線治不好**（就算每個人都別開眼睛，桌子、牌子與所有人的正面仍然朝著我們）。→ **把鏡頭搬到評審背後**，看的人因此和評審坐在同一側，評審只看得到後腦、**根本沒有視線可以出錯**。⚠⚠ **白袍的診所團隊非在畫面裡不可**：台上是空的話，那一排人就預設在評畫面外的某個人 ＝ 又回到看的人身上。⚠ **評審要畫成普通病人不是專家**（阿嬤、帶小孩的爸爸、上班族、家常折疊桌）—— 那一席才會讓人覺得「我也可以坐」。⚠⚠ **近處留一張空椅**（椅子＋桌上一塊空白牌子＋一枝鉛筆）：一次解掉三件 —— 「那一席是給你的」不必寫成字（這張圖零文字）、**會朝著鏡頭的那個位置是空的**、邀請變成具體的東西不是抽象手勢。⚠⚠⚠ **牌子一律空白，這是紅線**：卡片上寫著「好的不好的都想聽」，頭圖若畫著滿滿的高分，**畫面在說「我們期待你給圈」＝ 圖片版的 review gating，而且印在同一張卡上**；一排「✗」是另一個方向的錯。**空白同時是唯一在事實上成立的狀態**（送出的那一刻他還沒寫）——要讓「正在評分」讀得出來就**用動作不要用符號**（一枝懸在空白牌子上方的鉛筆）。⚠ **縮圖判準**：成品 1024×512、卡上只有 268×134，所以收成**兩條帶子**（下暗：三顆後腦＋空椅背／上亮：一排舉起來的白牌子＋白袍那一小團），人數壓到 **3 ＋ 3 ＋ 一張空椅**。⚠ **Ⓑ（他原本描述的正面評審席）也寫了，但有一個 Ⓐ 沒有的風險**：右邊那兩個白袍背影是**唯一**把箭頭轉開的東西，被裁掉或畫小就翻回「有人在評你」。⚠ **風格走站上那一套手繪**（提醒卡頭圖與十一張文章 HERO），不是綁定完成那張的平面線條風 —— **這個帳號目前有兩種畫風，綁定完成那張要不要重畫還沒問過他，不要自己動**。⚠ **還沒做的**：他出圖 → `drafts/review-hero-src.jpg` → 照 `remind-hero-crop.mjs` 改一支裁成 1024×512 → 換掉提案頁那個佔位框（⚠ `<img>` 一定要寫 `width`／`height`，不寫的話面板會量出一張矮 134px 的卡）→ 重跑 `review-png.mjs --all`。⚠⚠⚠ **2026-09-05：頭圖定稿並接上了**（第 **21-14** 節）—— 第四版通過（使用者：「定稿了」），前三版逐輪修正在`drafts/line-review-hero-prompt.md` 第六之三、六之五節，**定稿的提示詞在第六之四節**。成品 `preview/line-review/hero-review.jpg`（1024×512），`node drafts/channels/review-hero-crop.mjs`。⚠⚠ **守門不能照抄 `remind-hero-crop.mjs`** —— 這一張第 0 列就有濃墨（燈與彩旗），那一支的「深墨離上緣 ≥12 列」一直誤報；**兩條走不通的也記著**：膚色偵測（**彩旗是米黃三角形、和膚色同一區**，量到 84188 個假膚色像素）、「牌子是比背板白的白」（牌子也是米白，一列都命中不到）。⚠ 接圖時 **`width`／`height` 屬性 ＋ CSS 的 `height:auto` 三個都要寫**。⚠⚠⚠ **2026-09-05 第五版換上，頭圖這一件結案**（使用者：「換成這張 謝謝」，第 **21-15** 節）—— 第四版有兩處要治，**成因完全不同、所以是兩條通則**（都寫進 ILLUSTRATION.md 第十四節附）：① 左下角空白牌子上寫著 `STILL BLANK`，成因是提示詞第五段的 `STILL COMPLETELY BLANK` —— **大寫的英文片語看起來就像「要寫在那個物件上的內容」，而 `AVOID` 寫「不要有字」擋不住它（正文的權重比 AVOID 高）**；**通則：描述「某個東西上面沒有字」時不要用大寫、不要加引號、不要寫成像標籤的片語**，改成小寫敘述句 `nothing has been written on it yet`。② 桌上兩塊牌子被填了 9 和 8（使用者：「左邊爺爺的板子 和 右邊抱小孩的媽媽 桌上的板子都留白」），成因是**規則綁在數量上** —— 第 6 段寫「桌上那**兩塊**」，**模型畫了四塊**，多的兩塊不在射程裡；**通則：凡是「這一類東西一律如何」的規則都不要綁在數量上**，改成「桌上每一塊躺著的牌子都是空白的，**不管畫了幾塊**」再逐一點名，`AVOID` 補一條同義的否定式。✅ 第五版一次就中（四塊全空白、整張圖零文字，構圖與人物一個都沒動）。⚠⚠⚠ **裁切的地標跟著換圖重定了**：第四版那張有一條**橫貫全寬的舞台線**（第 730 列）當下緣地標，**第五版沒有那條線**，同一支腳本直接 throw ——**通則（第二次記）：守門要跟著這一張圖的形狀走，換圖要重掃、重定地標，而地標要挑「不管畫成什麼樣都一定存在」的東西**；下緣改成**「原檔最後一列有墨的位置」**（鞋子、桌腳、舞台線都成立）。裁法從上 64／下 16 換成 **上 60 / 下 20**：下面最後一列墨在第 746 列（桌腳），再往上就切到鞋子；上面那 60 列逐列的墨約 60 點、下面同寬是 250 點。守門五道：長寬比／**下緣一點墨都不可以裁到**（餘 1 列）／成品前 40 列最長橫線 39px（上限 200）／四邊白框／≤1024。⚠ 量到：五個寬度 × 兩案卡片 **268.0×497.6、頭圖 268×134 逐格相同 ＝ 和第四版、和佔位框那一版都一模一樣**（佔位框照 `aspect-ratio: 2/1` 預留，那一輪的預防生效了），孤字 0、水平溢出 0、JS 錯誤 0 |
| **預約成功通知 ＋ 約診紀錄查詢** | ✅ **2026-09-05 定稿**（使用者：「定稿　存下來　也把 line 模擬存給我。另外　隨機浮水印的圖案和顏色也要記錄在網站上　一起做給廠商」）：**日期新版・中文／每張不同的浮水印／日期 lg 19・姓名 md 16／小字沒有記號／姓名在日期上面／不放地址電話・頭圖・頁首標誌**。規格頁 `/preview/line-booked/`（**切換條與網址參數整組拿掉**，改成六節：新版兩則 → **九顆浮水印的形狀與顏色** → 現況對照 → 要問廠商的 → 檔案清單）。Flex 的 JSON 在 **`drafts/channels/booked-card.json`**（兩則 ＋ 九顆的對照表），守門 **`node drafts/channels/check-booked.mjs`**，圖五張 `node drafts/channels/booked-png.mjs`（`shot-booked` / `shot-query`＝整條輪播 855px / `shot-booked-chat` / `shot-query-chat` / `shot-wm`）。量到：預約成功 **268×149**、輪播每張 **207×114.7**，五個寬度逐格相同、孤字 0、水平溢出 0、JS 0。⚠⚠⚠ **JSON 裡最要緊的一行是 `wrap: true` 不是版型** —— 守門有一條專門擋 `maxLines`，那是這兩則**現在正在出錯**的那件事。⚠⚠ **浮水印的疊法是唯一沒有實機驗過的東西**：Flex 不准 box 的第一個子元件是 `absolute`，所以寫成「內容 box（`relative`）在前、浮水印（`absolute`）在後」，**但文件沒明講後面的元件會不會蓋在絕對定位的圖上面** —— 要一則測試訊息；蓋上去就退回「右邊一顆不重疊」（100% 支援）。⚠ CSS 那一版把標誌往右下溢出 18／10px，Flex 的 offset 不吃負值，所以 JSON 是 0／0，**這是已知差異不是漏改**。⚠⚠ **對照表那九張圖要同一個比例尺縮**，不是各自 fit 到框裡 —— 那九個寬度是墨面積正規化算的，**它們看起來一樣重才是重點**；第一版只寫 `max-width:100%`，長寬比 1.0 的那幾顆整個溢出格子壓到下一列**而且不報錯**。**通則：一組「刻意不一樣大」的東西並排展示時，縮放要用同一把尺。**⚠⚠⚠ **`booked-png.mjs` 有三件不能照抄前兩支**：① 它要**自己起 HTTP 伺服器**（那一頁 `fetch("wm-sizes.json")`，`file://` 拿不到 —— 九顆會退回同一個寬度、對照表整塊畫不出來，**而圖照樣出得來**）；② 守門**不能照抄「高比寬還小就是拍到一截」**（這兩張卡是**橫的**，照抄直接誤報）—— 改成拍之前先 `boundingBox()` 比對「出圖 ＝ 那個元素」，**通則：守門要跟著這一張圖的形狀走，不要跨頁照抄**；③ **拍整條輪播要放開三層**（`.row` 的 `overflow-x:auto` → 手機外框的 `overflow:hidden` → 視窗開到 1000），前兩次的症狀都是「**寬度是對的、右邊一片乾淨的底色**」，**守門抓不到、只有把圖打開看才看得出來**。⚠⚠ **2026-09-05 使用者：「我看到的 logo 有 12 個」——他是對的，但獨立的形狀只有九個**：`mark` ＝ `shape-r1c2` **逐位元組相同**、`tooth-solid` 對 `shape-r2c2` **逐像素差 0.8%**；`mark-outline` **是真的不一樣**（在卡片的裁切下對九顆差 91~95%），**但它進不了這個池子** ——墨只佔 **17.0%**（九顆 59.6~83.2%），按等重換算要 **321px** 寬、**比整張卡 207px 還寬**，用 150px 則露在卡上的墨只有 **4.6%** vs 九顆的 35~38%（**淡七、八倍**）。**要用它就得放掉「九顆一樣重」那條規則 —— 沒有自己決定，池子維持九顆**，規格頁 ③ 節底下把三顆的數字攤開了。**通則：一組素材要並排當同一組，除了「分不分得出來」還要問「份量在不在同一級」。**`booked-mark.mjs` 已加守門（放進重複的就 throw）。推導在 `brand/README.md` 與 README 第 **22-21** 節。⚠ 順序號碼那 12 張（`num-01…12.png`）沒有被選上，**檔案與 `booked-num.mjs` 都留著、沒有自己刪**。推導在 `drafts/channels/README.md` 第 **22-20** 節。以下是走到定稿的過程 —— **2026-09-04 開了提案頁 `/preview/line-booked/`（兩則 ＋ 三把尺）**。使用者給了兩張截圖：「一個是預約好當下跳出的通知，另一個是點開查詢預約跳出的已預約時段，**感覺可以一起製作　你們先改改看**。」⚠⚠ **他是對的**：兩張卡內容 95% 一樣，差的只有查詢那一張多一行「預約狀態」—— 它們是**同一張卡的兩個狀態**，所以提案頁共用一個殼。⚠⚠⚠ **這兩則原本不在第 14-3 節那張盤點表裡** —— 第 16-5b 節「我盤的是截圖不是清單」**第二次被證實**（第一次是滿意度調查卡），**已知的自動訊息從四則變成七則**，而且還不知道有沒有第八則。**廠商那份「會自動送出哪幾則」的清單因此從「做得更好」升級成「非要不可」**。⚠⚠ **截圖又是診所端的後台**（標題是病人名字、有「已讀」與「預約傳送」）——內容是病人看到的，寬度不是。⚠⚠⚠ **但這一次挖到一個等很久的數字**：同一張截圖裡同時有兩種卡，所以是同一個客戶端的同一次量測 —— **單張泡泡 348.7×261.3、輪播裡一張 269.3×316.3、兩張之間 8.7 ＝ 輪播只有單張的 77.2%**；**左右內距兩種都是 22.3（窄了 79px 卻一點都沒收）**，可用寬度因此從 304.1 掉到 **224.7（−26%）**。提案頁的輪播畫在 268 × .772 ＝ **207px**。⚠⚠⚠ **但 207 是推導值**，它建立在一個沒收掉的假設上：LINE 的泡泡寬度是**跟著螢幕縮放**還是**每一種 size 固定 px**。若是後者，**348.7 才是真值、我們用了三輪的 268 反而偏窄 30%**。**兩種讀法分不出來，面板兩種都算；能分出來的只有一張病人手機的截圖**（這個請求從 20-10 開著到現在，這一輪之後重要很多）。⚠⚠⚠ **現況第一嚴重的是日期真的被截斷**：「2026/08/20 15:45 星...」—— 成因是**同一個帳號有兩種日期格式**，而**最長的那一種正好被放進最窄的卡裡**（查詢那張寫 `2026/08/20 15:45 星期四`，預約成功與提醒卡寫 `2026/08/25 (二) 10:15`）。⚠⚠ **只把字改短救不了**（寬度還沒確定）——**真正的修法是讓它折行**（Flex 的 text 預設就會折，是廠商設了 `maxLines: 1` 才會截）：放得下一行、放不下兩行，**哪一種寬度都不會有字消失**；縮短格式是在這之上再多買一層餘裕。⚠⚠⚠ **第二嚴重的是「預約狀態　尚未回覆」**：**誰還沒回覆？**第一個讀法是診所還沒回（會讓他等），第二個讀法是他要回，**但卡上沒有一個地方可以回**。而 24 小時沒回應會自動取消那件事（19-12）如果是同一個欄位，就不只是說法問題 —— **要跟廠商要那張狀態表**（有幾種值、各是什麼意思、和提醒卡那顆確認鈕是不是同一個）。**在那之前那把尺只是換說法，沒有一格敢寫「已為你保留」那一類的話** —— 那是替一個我們不懂的狀態下定義。⚠ 第三：**標誌區塊 87.0px ＝ 整張卡的 33.3%，而那一塊的墨只佔 2.3%**（而且它就出現在診所自己的聊天室裡）。⚠ 第四：**兩張是兩套模板**（單張有分隔線、輪播沒有；輪播的標誌反而大 1.19 倍；明細列 16 vs 14）。⚠ 第五：**標籤那個灰 `#979797` 對白底只有 2.92**。⚠⚠ **新版：一個殼兩個狀態** —— 預約成功有開場那一行、**輪播沒有**（清單裡每張都重寫一次名字＝浪費整行 × N）；**輪播的日期就是第一行**（他要看的是什麼時候）；預約成功結尾多一句「看診前2天會再提醒你一次」把三則串起來（**逐字沿用招呼圖卡，不要改成 48 小時**）。⚠ **這一輪一顆淡底彩色方塊、一顆按鈕都沒有** —— 這兩則是**收據**，沒有要他現在做什麼；電話是說明不是行動（19-14）。⚠ **頭圖預設不放，理由和提醒卡相反**：提醒卡一次送一張、圖是趣味；**輪播一次送 N 張，一張圖就是 N × 104px**，而且他是來查時間的（`?h=y` 看得到）。⚠ **輪播的地址電話建議「不放」**：輪播回答的是什麼時候不是在哪裡。⚠ 量到：預約成功 296 → **254**；查詢每張 **372 → 124**（不放地址）／229（放）；現況的電話在 207 上**折成兩段**、新版沒有。五個寬度（430／393／390／375／360）建議那一組逐格相同、孤字 0、水平溢出 0、JS 0、切換條 24.7%；216 組狀態全掃過。⚠⚠ **字型校正**：「2026/08/25 (二) 10:15」截圖上的墨是 237px、提案頁畫出來 266px ＝ **比 LINE 寬約 10%**，所以面板那個「本來多寬」偏保守，**差 10% 以內的不要當結論**；查詢那張差 **65px（29%）** 且截圖上真的截了 —— 那一格是結論。⚠⚠ **這一則的文字是我們擬的**（「幫你約好了」「等你回覆」）——前三則每一個字都是使用者自己寫的，而且每次他的版本都更好，**等他換**。⚠ **地址逐字照廠商現在送的那一份**（`雲林縣斗六市永樂街70號 1-2樓`）—— 站上寫的沒有「1-2 樓」，兩邊不一樣這件事記著，但沒有自己拿掉。⚠ **Flex 的 JSON 與圖片檔等挑完才做**。推導在 `drafts/channels/README.md` **第二十二節**。⚠⚠⚠ **2026-09-04 稍晚三件**（第 **22-12** 節）：**預約狀態整行拿掉**（使用者指定）——⚠ **但那個問題沒有跟著消失**：廠商那張狀態表還是要，因為如果「尚未回覆」真的會導致自動取消，病人**更**需要知道，只是**知道的地方該是提醒卡那顆按鈕不是一張查詢用的收據**。⚠⚠⚠ **「LINE 裡可以設置對話框浮水印嗎」查證了：做不到，而且不是設定藏起來** ——聊天室背景是**每個使用者在自己手機上設的**（聊天室右上 → 設定 → 背景），而且**只有他自己看得到、對方看不到**；官方帳號後台能改的是**帳號主頁**那張封面照。⚠⚠ **卡片自己裡面可以，但 Flex 有兩條硬限制**（查官方 SDK 原始碼確定的）：**box 沒有背景圖**（`background` 只認 `linearGradient`，沒有 `backgroundImage`）、**image 沒有 opacity**（淡要**烘進 PNG 的 alpha**，同招呼卡那兩顆 logo 烘顏色）。產圖 `node drafts/channels/booked-mark.mjs` → `mark-wm-{08,12,18}.png`（420×207、頁首那一條、`#3f654a`，**沒有新增顏色**）。⚠⚠⚠ **而它要靠 `position: absolute` 疊，這一項沒有實機驗過** —— 文件寫著「**一個 box 的第一個子元件不可以是 absolute**」，但**後面的元件會不會蓋在前面的上面**文件沒明講，**要一支測試訊息**。所以尺上留了一格**「右邊一顆」**（同一行、不重疊、**100% 支援的退路**）。⚠⚠ **「單調」拆成四格，差別在要不要動到廠商**：只有日期 98px／**浮水印 98px（預設，一個字都不用改）**／右邊一顆 104px／**大日子 74px（月小・日 40px・星期時間排旁邊，對單調最有效）**。⚠⚠⚠ **「大日子」不是版面問題是資料問題** —— 現在那一整串日期是**系統填的一個欄位**，要拆成四塊得廠商給（**同提醒卡那條「有變數的那一行不能手動斷行」：版面能不能做，先問那段字是誰填的**）。⚠ 更有效但同樣要問廠商的是**「還有幾天」**。⚠ 浮水印可和其他三格併用（`?wm=1`），切換條放不下第五列。⚠ 提案頁踩到一件：**「大日子」那三塊字級不同 → 逐字分組的 top 不同 → 被面板報成六個孤字**，那是**量測的假警報**，判準同「電話自己一行」：自己排出來的短句不算孤字。⚠ 量到：四格在 430／390／375 逐格相同、卡片 207.0、孤字 0、溢出 0、JS 0、切換條 24.7%；144 組全掃過；浮水印那格「字壓在最濃處」10.86 ✓。⚠⚠⚠ **2026-09-04 再一輪：九顆浮水印**（使用者：「我們有 9 個 logo，可以自動選擇不同的浮水印嗎」，第 **22-13** 節）—— 那九顆 ＝ `brand/shapes/shape-r{1,2,3}c{1,2,3}.svg`，⚠ **`r1c2` 就是站上頁首那顆**，九顆裡本來就含著現在用的那一顆、不是第十顆。產圖 `node drafts/channels/booked-mark.mjs` → `wm-<形狀>-<濃度>.png`（9×3 ＝ 27 張）＋ **`wm-sizes.json`**。⚠⚠⚠ **九顆不能用同一個寬度** —— 四種長寬比（1.00／1.33／2.03／3.08），同寬的話細長那幾顆會矮一大截、看起來輕很多（**同招呼卡 18-10 那條「粗是墨量不是厚度」**）。所以**按墨的面積正規化**，基準是 r1c2 在 150px 時的墨面積，每一顆反解出自己的寬度（**r3c3 墨只佔外框 59.6%，要 215px 才等重**—— 那是「不能同寬」最極端的證據）；九顆的**墨面積離散度 1.14%**，產生器超過 2% 就 throw。⚠⚠ **寬度只有一份出處 `wm-sizes.json`**，提案頁 `fetch` 讀它、日後寫 Flex JSON 也讀它，**不要在第二個地方再寫一份**（讀不到時九顆退回同一個寬度，面板會標紅）。⚠⚠⚠ **「自動選擇」不在我們這一側** —— Flex JSON 是廠商系統組的。三種規則差很多：**按日期算 `(月＋日)%9`（建議，同一筆約診查幾次都是同一顆、取消一筆不影響其他筆）**／按位置輪流（⚠ 取消一筆後面全部換人）／隨機（⚠⚠ 同一筆每次都不一樣，**讀起來像壞掉**）。提案頁現在就是按日期算的。⚠ 廠商不能算的退路兩層：**整包同一顆**、或**兩則各一顆**（靜態、零依賴）。⚠ **這一題和「日期能不能拆成欄位」同一類：版面能不能做，先問那段東西是誰填的。**⚠⚠ 提案頁踩到一件：**按鈕包在 `<span id="sw-…">` 裡，那個 span 不是 flex 容器的話，按鈕會在 span 裡換行、`.r` 的 `overflow-x: auto` 根本用不到**，五格那一列因此變兩倍高（24.7% → 27.5%，而 `scrollWidth === clientWidth` ＝ 沒有在捲）。補一條 `display:flex` 就回到 24.2%。⚠⚠⚠ **九顆做好了，但在這個裁切下只有五顆真的分得出來** —— 逐像素兩兩比過（207×98 的卡、同位置同濃度，差 ≥2 階才算）：36 對中位數 26.6%、最不像的 52.0%，**但有 5 對低於 10%**（r1c1×r3c1 4.8／r1c3×r2c3 4.8／r3c1×r3c2 5.5／r1c1×r3c2 8.9／r1c2×r2c1 9.7）。⚠⚠ **成因是裁切不是形狀**：浮水印只露出右下角一塊，兩顆同長寬比的露出來的那塊幾乎一樣。→ 兩兩都 ≥10% 的最大一組是**五顆**（r1c1／r1c2／r1c3／r2c2／r3c3）。⚠⚠⚠ **但「只用五顆」有代價而且算得出來，所以預設仍然是九顆**：規則是**日期的線性式取餘數**，**兩筆約診差 N 天（或 N 的倍數）就一定撞同一顆** ——**九顆：7／14／21／28／30 天全部不會撞**；五顆：5／10／15／**30** 會撞（**30 天是真的會發生的回診間隔**）。⚠ **實測就撞到了**：示範的 8/20 與 8/25 差 5 天，切成五顆時前兩張是同一顆、**畫面上看起來就是壞掉**。所以**預設九顆**、`?n=5` 切五顆，**面板現場數相鄰有沒有撞**。⚠ **第三條路是把浮水印縮小** —— 形狀露得完整，**九顆都分得出來、間隔行為也好，兩個問題一起解**，代價是品牌感更弱 —— **還沒問使用者**。⚠⚠ **通則：素材數量、可辨識度、撞號機率是三個互相拉扯的量**，挑池子大小之前要三個一起算。⚠⚠⚠ **通則：「有九個素材」不等於「有九種變化」** —— 素材要在**實際會被看到的那個裁切下**兩兩比過才算數（同分享卡那一輪「要看 250px 下讀不讀得懂」：**在成品的尺寸上量，不要在素材的尺寸上量**）。⚠ 量到：180 組 × 3 寬度 ＝ **540 次**、溢出 0、JS 0、切換條 24.2%、`wm-sizes.json` 每組都讀得到、示範那三張相鄰沒有撞；每一顆露在卡上的墨 35.2~37.5%（正規化在卡片上也成立）。⚠⚠⚠ **2026-09-07 廠商回覆：「背景的話，只能全白或其他顏色，無法放圖示。請確認要維持白底背景，或是調整背景色，若要調整請提供色碼」** —— **那一句是對的但答的是另一個問題**：Flex 的 box 真的沒有背景圖，可是**我們要的浮水印不是背景**，是 `image` ＋ `position: absolute`；而**「這張卡裡放得了圖」已經被他們自己的成品證實**（現況那張預約成功卡頂端 87px 裡就有一顆標誌，22-4 ③）。三層退路：① absolute 疊得上去就照 JSON ② 疊不上去改「**右邊一顆不重疊**」③ 連圖都不能放就只剩底色。⚠ **背景色的答案是 `#F4F4F5`，不必改任何東西**（七則的 JSON 本來就寫著它）；**不要換成紙色 `#E2E5E6`** —— 這一站那是「底」的顏色不是「卡片」的。⚠⚠ **②那一份 JSON 等廠商答完再寫**，現在寫等於寫兩份、其中一份會丟掉。詳見第三十節。⚠⚠⚠ **2026-09-13：輪播那張卡的日期定案「指定斷行」**（使用者在手機上看到它折成「2026/08/20 星期」／「四 15:45」—— 一個字被丟到下一行；兩種擺出來之後他選了指定斷行）。⚠⚠⚠ **折點不是排法決定的，是寬度 × 字型決定的** —— 整串在 lg 19 要 215px、輪播那一行只有 179px，而**中日韓文字每兩個字之間都是合法的折點**，所以同一串在他的 iPhone（PingFang）折在「星期／四」中間、在我們這台折在「星期四」後面，**換一台機器折點就會移位**。**通則（第五十九節那條的反面）：把兩樣東西排在一起保證不了它們不被拆開，要它一定不拆只有自己指定斷點、或讓整串放得下一行。**⚠ 落選的「不斷行」要一行就得把日期從 lg 19 降到 **sm 14**（md 16 量到 182px、可用只有 179），**日期會變得比病人姓名還小**，數字留在面板上。⚠⚠ **對廠商的要求**：輪播那一格的 `{{date}}` 值裡帶一個換行（`2026/09/11\n星期五 11:50`），**單張那一則維持一行** —— 兩則字串不一樣是寬度逼出來的，字元與順序完全相同、只差一個換行。⚠⚠⚠ **同一輪他要浮水印「再往左移」，而那正好解掉 42-7 那個矛盾**：往左是 `offsetEnd` 的**正值、Flex 寫得出來**，而現在這一版是往右溢出、`offsetEnd` 不吃負值**寫不出來**（所以 JSON 一直寫著 0px、和我們送過去的模擬圖互相矛盾）——**只要他挑一格「往左」，那一份 JSON 就寫得死了**。四格在第 ②之三 節（現在／貼齊／左 24／左 48），⚠ 往左不是免費的：九顆的寬度 132~215px 各不相同，推過頭換成**左邊**被切。⚠⚠⚠ **「卡片窄一點」那一把我們挑不了** —— bubble 的 `size` 只有 nano／micro／…／giga 那幾階，**輪播已經在 `micro`，下一階 `nano` 我們從來沒有量過**；第 ②之四 節四格是模擬，量出來**地板是藥丸那一列不是日期**（115px → 卡片約 143px）。⚠⚠⚠ 這一輪踩到兩個不報錯的：**`render()` 的共用狀態（stat／wmink／dtm）畫完沒還原**，第二次 render（resize 或 JSON 回來）就把 ①② 一起畫成第 ②之二 節那一版、連出圖都跟著變，**而畫面完全正常**；以及**開頁那段 `fetch(...).catch()` 把 `render()` 的例外一起吞掉**，畫面停在「讀不到 wm-sizes.json」、主控台一個錯都沒有 —— **通則：一個「讀不到就算了」的 catch，不可以把後面那一段真正的工作一起包在裡面。**推導在 README 第六十、六十一節。⚠⚠⚠ **2026-09-13 稍晚：使用者貼了一份外部搜尋的資料進來（「以上是外部搜尋的資料　整合進來」），上面那句「輪播已經在 `micro`、下一階 `nano` 我們從來沒有量過」整條作廢** —— **LINE 官方一個 px 都沒有公布**（文件反過來建議不要用 px 排版），但社群量過：卡米哥（2021 Chatbots Meetup）用 **20×20px 的方塊**數出七階的**內容寬** ＝ **nano 7／micro 9／deca 12／hecto 13／kilo 14／mega 16／giga 26 格**（⚠ 拼法是 `deca` 不是 deci），戴均民再做成一組**「測試寬度名片」**，**送進那支手機就量得到真值**。⚠⚠ **同一條輪播裡每一張 bubble 必須是同一個 `size`**，要換是**整組一起換**。⚠⚠⚠ **而它當場翻出一件我們自己的矛盾**：把那張表加上我們自己的 `paddingAll 14`（左右各一次）對回廠商那張截圖 —— **mega 320＋28 ＝ 348 對量到的 348.7、deca 240＋28 ＝ 268 對量到的 269.3**（差 0.2% 與 0.5%；`micro` 是 208、對 348 只有 0.598，和量到的 0.7723 差很遠）——**所以廠商那條輪播跑的是 `deca`**，而 `booked-card.json` 從第一版就寫著 `"size": "micro"`，**照那一份交出去會比我們給看過的每一張模擬圖窄約 25%，而且沒有人決定過要窄**。**已經改成 `deca`** ＝ 我們模擬圖畫的比例、也＝ 廠商現在跑的那一階，**什麼都不必請他們改**。**通則：一份規格和自己的模擬圖對不起來時，先問「對方現在跑的是哪一個」** ——三者裡有兩者本來就一致，錯的是那份還沒交出去的 JSON（**第 42-7 節那個矛盾的第二次現場**，成因一樣：文字規格與圖分開寫、沒有人把兩份擺在一起唸過）。⚠ **收掉的是「哪一階」不是「幾 px」**：兩條互相獨立的推導對上了（截圖比例 0.7723／表上 0.770），但**絕對值仍然都來自診所端後台那張截圖** —— 26-6 還開著，而**測試寬度名片正好一次收得掉它**。⚠ ②之四 因此整節重做：三格的寬度**從格數算出來**（比例尺 ＝ 179 ÷ 240 ＝ 0.7458），不再是憑感覺的 185／165／145（**那三個不是 LINE 真的有的階，在一個不存在的寬度上判斷「收不收得下」全是假的**）；這台上 nano 132／micro 162／**deca 207（現在）**／hecto 222／kilo 237／mega **267**／giga 416px（⚠ mega 算出 266.7，和我們一路在用的 268 差 0.5% ＝ 第二個來源第一次幫它背書）。**地板是藥丸那一列 115px**：`micro` 可用 134 **收得下**、`nano` 可用 104 **收不下（差 11px）**。⚠⚠⚠ 這一輪踩到兩個：**藥丸那一列是 flex（項目預設 `flex-shrink: 1`），在窄卡上會被壓縮** ——直接量子元素回的是壓過的寬度，`nano` 那一格因此印成「放不下（差 0px）」**而畫面完全正常**；修法照第三十五節：**先切成 `max-content` 再量、量完還原**（**通則第四次記：一個東西在 flex 或 grid 裡就量不到它「本來要多寬」**）。以及**負向測試不要用 `git checkout --` 還原** ——那幾個檔這一輪還沒 commit，第一次還原就把整輪的工作全部擦掉、**而且不報錯**，後面五個負向測試全部在對著舊版跑（**要用 `cp` 到暫存區備份再還原**）。⚠ 掃「deci」又撞到頁面上那句「拼法是 deca 不是 deci」自己（這條線第七次），那一處包在 `<span data-spell>` 裡、掃之前剝掉，**而且要驗那個標記還在、只有一個**。守門新增十道全部負向測過。推導在 README **第六十二節**。⚠⚠⚠ **2026-09-14 使用者定案：輪播改用 `micro`**（「約診記錄查詢用 micro」，這台上 **162px**、可用 134px）—— 上一段那句「沒有人開口要窄，所以沒有動」**隔一天就被他推翻了**，而那句話當時是對的、留著的價值正是它記下了「為什麼當時沒動」。⚠⚠ **這一改和 09-13 那一改方向相反，兩件都對、不要互相訂正**：09-13 把 JSON 從 micro 改成 deca 是因為「和我們自己的模擬圖對不起來、沒有人決定過要窄」，今天他決定要窄，所以同一個值又回到 micro ——**差別是它現在是一個「請你們換過來」的要求，不是一個錯**。⚠⚠⚠ **「量到的」和「我們要的」要分成兩個變數**：`MEAS_W 207`／`MEAS_STEP deca`（**比例尺的錨**）vs `WANT_STEP micro`，`CAR_W` 從後者算 —— 合成一個的話，換一階就等於換掉比例尺、整把尺一起縮水（＝ `post-map.mjs` 那個 `WMREF` 的坑）。**通則：一把相對的尺上，「基準」和「選中的那一格」永遠是兩個變數。**⚠⚠⚠ **卡一窄，09-13 那句話就變成假的**：第五十九節那句「折點會落在時間前面、星期幾仍然跟著它的日期」是在 **deca（可用 179px）** 上量的；micro 只有 **134px**，而「日期 ＋ 星期幾」那一段要 **156px** —— 逐像素量到第 ② 節那張（自由折行）折成 **`2026/08/20星` ／ `期四15:45`**，**星期幾被折斷在中間 ＝ 他 09-13 退回過的樣子**，而且折點還跟著字型跑。**micro 之下唯一治得住的是指定斷行**（`2026/09/11` ＋ 換行 ＋ `星期五 11:50`）。⚠ **沒有自己改第 ② 節那張，也沒有動 `/preview/line-vendor/`**（那一頁仍然畫 207px）——**那是要不要改給廠商的規格，是他的決定**；頁面上現場量、標紅、把唯一的解法擺在旁邊。**通則（第 28 條 ② 的又一次，這一次是「別人的決定」被我的改動弄假的）：一個定案如果附著一句量出來的理由，那句理由就綁著它當時的寬度與版型 —— 那幾個數字一動就要回頭重量一次。**⚠ 面板多一欄「**星期幾有沒有被折斷**」：判準是「星期X」那三個字要整段落在同一行，**不可以只數行數**（折成兩行是對的，折在中間才是壞的，兩者行數一模一樣）。⚠⚠⚠ **同一輪：九顆浮水印各畫一次、每張配約診狀態**（使用者指定）——九格各一張真的輪播卡，標題寫著那一顆的名字與**一個真的會算到它的看診日**，而**形狀是 `shapeFor()` 從那個日期算出來的、不是塞進去的**（守門逐張比對；塞進去的話規則哪天壞了這一頁還是很正常）。⚠ **約診狀態那四個值是輪流掛上去的示範值** ——狀態跟著這一筆走到哪一步、形狀跟著日期，**兩件不相干**，頁面上寫著這一句。⚠⚠ **micro 的代價要印出來**：九顆看得到 **75~88%**，那三顆 3.08:1 的（`r1c3`／`r2c3`／`r3c3`）**整顆比卡片還寬**。⚠⚠⚠ **「九顆怎麼輪」寫上頁面了**（他問的那件事）：規則本來就在 `nwCard()` 的註解裡，**但頁面上從來沒有寫過**，所以他只能從畫面猜、而且猜的方向正好是最糟的那一種。**`shape = 順序[(月 ＋ 日) % 9]`**，月日取**那一筆約診的看診日期**（不是今天、也不是排在第幾張）——因此**同一筆查幾次都是同一顆**、**預約成功那一則和查詢清單裡那一張是同一顆**（兩則共用）、**顏色不必另外定規則**（顏色綁在形狀上）。⚠⚠⚠ **他擔心的那件事是真的、只是不會發生在這條規則上**：面板現場算 2026 整年（只算週一到週五，**261 天**）九顆各 **26~31 天（10.0~11.9%）**；**若真的按星期幾只用得到 5 顆**（加一個月一天的週六是 6 顆），**剩下 3 顆永遠不會出現**。⚠⚠ **那幾個數字是 `rotaStats()` 現場算的不是寫上去的** —— 排班或規則改了它會跟著變，不會變成一段過期的宣稱。**通則：使用者問「這個東西的邏輯是什麼」時，答案要寫在他看得到的那一頁上，而且能現場算的就不要寫死 ——一個只活在程式註解裡的規則，等於沒有人知道。**⚠ 守門新增九道、**七種負向測過**；那道「畫完要還原 `dtm`／`wmink`／`stat`」的正規式**視窗要跟著放寬**（3000 → 6000 字），中間插了新的一節它會靜靜地開始誤報。推導在 README **第六十三節**。⚠⚠⚠ **2026-09-14 稍晚：那三顆細長的浮水印「扁扁的」**（使用者：「1/1 1/13 1/7 logo看起來扁扁的　和其他logo效果比較差　我們針對這三個logo調一下」）—— 他指的三個日期就是 `r1c3`／`r2c3`／`r3c3`，**九顆裡唯一三顆 3.08:1 的形狀**（他是照日期認出形狀的，那順帶再證明一次那條規則真的在跑）。⚠⚠⚠ **「扁扁的」量得出來，而且是換成 micro 才長出來的第二層效應**：卡片 207 → **162px**，而九顆的寬度一個都沒有跟著收（那是按墨的面積正規化算的）—— 浮水印貼右緣、往右溢出 18px，所以左緣 ＝ `180 − 寬度`，**這三顆本來就比整張卡還寬**，於是**橫貫整條卡、左邊還被切掉 8／8／35px**，而另外六顆只佔卡寬 53~83%、讀起來是「角落的一顆標記」。⚠⚠ **deca 那一版九顆的左緣全部收在卡內**（最緊的 r3c3 還剩 10px），所以上一輪看不出來 ——**通則（第九節第 28 條 ② 又一次）：一個「只換一個尺寸」的決定，會把每一個「本來剛剛好」的東西一起推翻，換完要把所有貼著邊界的數字重量一次**（上一輪貼邊界的是日期還剩 2px，這一輪是左緣還剩 10px）。⚠⚠⚠ **但不是「墨變重」也不是「墨變輕」** —— 現場讀那九張 PNG 的 alpha 並換算回 CSS px²（⚠ 九張的原始尺寸 300×300 ~ 602×195 各不相同，直接數原圖的像素比不出份量）：956／990／**979**／998／965／**997**／939／959／**778**，**只差 1.28 倍**，那三顆在現況這一格的份量本來就和另外六顆同一級。**通則：「這一顆看起來比較差」有兩個互相獨立的量 ——佔掉多少地方（形狀）與多少墨（份量）；兩個都量過再決定動哪一個，只量其中一個就會去改另一個而且改不好。**⚠ 尺在第 **②之五** 節（`WIDE3` 三顆**共用同一個倍率** `wideK`，所以彼此仍然等墨、另外六顆一個數字都沒動）：**Ⓐ 1.00 現在這樣（188／188／215px・佔卡寬 100%・墨 101／103／80%）**／Ⓑ 0.75（141／141／161・76／76／88%・**墨剩 56／58／55%**）／Ⓒ 0.66（124／124／142・65／65／77%・42／44／42%）／Ⓓ 0.58（109／109／125・56／56／66%・32／33／32%）。⚠⚠⚠ **這是真的取捨不是「愈小愈好」**：縮下去形狀就對了（Ⓑ 起左邊一格都不切），**但墨 ∝ 倍率²**，面板每一格把兩邊都印出來。⚠ **還有一條不必付墨的路**：把那三顆從池子裡拿掉、只用六顆 ——代價是 `(月＋日) % 6` 在相差 6／12／18／24／**30** 天時會撞同一顆，而 **30 天是真的會發生的回診間隔**（那正是 2026-09-04 選九顆的理由）。**兩條都擺出來，沒有自己決定。**⚠⚠ **名字不可以叫 `WIDE`** —— 底下那支量測函式裡已經有一個同名的區域變數（後台那張卡自己的可用寬度），撞名不報錯、只會在某一支裡讀到另一個東西。⚠ **名單不寫死在兩個地方**：`WIDE3` 就是 `wm-sizes.json` 裡`ratio > 2.5` 的那幾顆，面板**現場驗一次**（對不上就標紅）。⚠⚠ **墨要真的讀像素**（`getImageData` ＋ 逐欄前綴和 ＋ 快取），不可以用倍率² 推一個數字出來 ——推出來的那一欄只是在重複倍率，看不出「左邊被切掉」吃掉了多少；⚠ `file://` 上 canvas 會被判成跨來源而丟例外，接不到就印「—」不要讓面板整個掛掉；⚠ 圖還沒載完量不到墨 → `load` 之後再填一次。⚠ **畫完要還原 `wideK = 1`**（同 `dtm`／`wmink`／`stat`）。⚠⚠ **順手發現：加一節就會讓五張 `shot-*.png` 全部變** —— 元素截圖會先把它捲進畫面，頁面一長捲動位置的小數位就變了，**文字的次像素抗鋸齒跟著變**（逐像素比過：版面、尺寸、字一個都沒動，差的全是字緣）；所以 `booked-png` → `vendor-shots` → `build-vendor` → `build-spec` 一起重跑是正常的。⚠ 同一輪也抓到上一輪 commit 進去的 `shot-wm.png` 是**舊的**（沒有重跑出圖，而那幾張沒有逐位比對的守門）。守門新增七道、**七種負向測過**（甲 `nwCard` 不吃倍率／乙 尺不是四格／丙 還原少了 `wideK`／丁 名單少一顆／戊 面板少印「佔卡寬」或「看得到的墨」（⚠ 要找**印出來的那一段**，掃整份會掃到自己的說明 —— 這條線第八次）／己 面板不驗名單／庚 圖載完不重填）。推導在 README **第六十四節**。⚠⚠⚠ **2026-09-14 稍晚：預約成功通知那一則也把九顆各畫一次**（使用者：「預約成功通知的 9 個 logo 套用版型也做給我看」）—— 同樣九格、同樣拿一個真的會算到那一顆的看診日去畫，但畫的是**單張那一則**（`mega`、268px）。⚠⚠ **兩排不是同一種卡，所以三個共用變數都要換過去再換回來**：輪播是「指定斷行／淡墨浮水印／底套色的藥丸」，單張是「**不斷行／彩色浮水印／沒有約診狀態那一列**」；**換回來那一行掉了的話，底下 ②之三／②之四／②之五 會整批畫錯而且畫面完全正常**（同 63-7 那個還原，只是這一次是中途換）。⚠⚠ **不要順手把這一排改成淡墨** —— 淡墨是 50-19 為了「藥丸和有顏色的 logo 互相吵架」才換的，而這一則沒有藥丸；改成一致不會讓任何一道尺寸守門翻臉，所以頁面上那句理由要留著、守門掃它。⚠⚠⚠ **這一排最有用的一件：「扁扁的」在這一則不會發生** —— 浮水印貼右緣往右溢出 18px，左緣 ＝ **卡片寬 ＋ 18 − 整顆寬**，所以輪播（162）是 `180 − 寬`、單張（268）是 `286 − 寬`；最寬的 `r3c3`（215px）在輪播上左緣 **−35（被切）**、在單張上 **＋71（整顆收得進去）**，**九顆左邊一顆都沒有被切到**，被切掉的只有右邊那 18px（看得到 83~92%）。**所以第 ②之五 節那把尺只作用在輪播，這一則一個數字都不必動。**⚠ 左緣是面板**現場量**的不是寫在說明裡 ——卡片寬或那九個寬度一改，這裡會自己變紅。⚠⚠ **兩排逐格比對 `data-shape`**（九格全部是同一顆才印綠）——「兩則同一顆」是 `(月＋日)%9` 最有價值的性質，要用量的；**只驗「和標題對得上」抓不到**（兩排各自對得上自己的標題，卻可能彼此不同）。⚠⚠ **`MEGA_W` 寫死 268，不要拿 `bubblePx("mega")` 去算**（那條回 267，差 0.5%）—— 這一排要和第 ① 節那一張**逐格一樣寬**，不然它就不是「真的那張卡」了。量到六個寬度逐格相同：卡片 268.0、看得到 89／132／170／134／102／170／86／88／**197**px、左緣 179／136／98／134／166／98／182／180／**71**px、約診狀態 0 列、溢出 0、JS 0。守門新增八道、**八種負向測過**。⚠ 多一節之後 `shot-booked-chat.png`／`shot-wm.png` 又變了 ——同 64-7：**差的全是字緣的次像素抗鋸齒**，版面與尺寸一個都沒動。推導在 README **第六十五節**。⚠⚠⚠ **2026-09-14 稍晚：那一排「1/6 偏小、1/7 太吵」**（使用者原話）—— 他講的兩顆是 **1/6 ＝ `r3c2`**（圓的，106px）與 **1/7 ＝ `r3c3`**（最長的，215px），**兩句話指的是同一件事：九個寬度差了 2.07 倍**。⚠⚠⚠ **成因是那九個寬度按「墨的面積」正規化**（2026-09-04 那條規則）—— **等重換來的代價就是等重不等寬**；而這張 268 的卡把差距放大了（同樣的 px 在 268 上佔掉的比例只有 162 輪播卡的六成，`r3c2` 從 54% 掉到 **33%**、`r3c3` 是 **74%**）。**通則：一把「等重」的尺，換一個容器寬度就會換一個症狀** —— 第六十四節在 162 上治的是「那三顆橫貫整張卡」，這一節在 268 上治的是「圓的那幾顆太小」，**同一個差距的兩端**。⚠⚠⚠ **所以不是一把倍率的尺**：他那兩句話方向相反，乘一個數字兩顆的比值一點都不會變 —— 唯一同時治得到的是**把寬度差距收窄**，往**幾何平均內插**（`w(t) = w^(1-t) × G^t`，G 從 `wm-sizes.json` 現算 ＝ 142.7，**一條規則算九個寬度不是九個各挑一個**）。四格（第 ①之二 節）：**Ⓐ 等重（現在這樣）104~215px・比 2.07・佔卡寬 32~74%**／Ⓑ 收窄三成五 116~186・1.60・37~63%／Ⓒ 收窄七成 130~161・1.24・42~53%／Ⓓ 九顆同寬 143・1.00・47%。⚠⚠⚠ **代價要一起印：墨 ∝ 寬²**，收窄是把墨從細長那幾顆搬到圓的那幾顆，**「九顆一樣重」那條規則會跟著鬆掉**（看得到的墨對 Ⓐ：100／73~127／54~162／**41~198%**）—— **這一格挑的是取捨不是最佳解**（＝第 64-3 節那條「兩個互相獨立的量都要印」第二次現場）。⚠⚠⚠ **還有一個不在這一頁上的代價：那九個寬度是兩則共用的**（`wm-sizes.json` 是唯一出處，`post-*` 那幾支貼文產生器也讀它）—— **挑 Ⓑ 以上輪播也會跟著換**，而第 ②之五 節那把尺正好是這件事的特例，**要重算或直接收掉**；寫在面板上、**沒有自己決定**。⚠ 實作是 `spreadT`（預設 0）＋ `spreadK()`，`nwCard` 裡**兩把尺相乘** `(WIDE3.indexOf(sn) >= 0 ? wideK : 1) * spreadK(sn)` —— 兩把的預設都是「不動」，所以 ①② 與 `#pv-nine` 逐格未動（量過）。⚠⚠ **`spreadT` 也要進還原那一行**（第 65-1 節那條第二次）：`render()` 會被呼叫不只一次，沒還原的話第二次進來 ①② 會整批畫成收窄過的樣子**而畫面完全正常**；①之二 那一段還要排在「換回輪播那一組」**之前**。量到六個寬度溢出 0、JS 0。守門新增九道、**九種負向測過**（⚠ 宣告那一行要**指名接在 `wideK` 後面**那一處 —— 還原那兩處寫的是同一串字，只找 `spreadT = 0;` 的話預設值被改掉照樣會通過）。推導在 README **第六十六節**。⚠⚠⚠ **2026-09-14 稍晚：那把尺挑完了，整頁跟著整理成規格頁**（使用者：「現在這個提案頁因為調整很多次　變得很混亂　整理一下　已經定案的保留　其他舊案拿掉」，並逐顆挑定 **①之二 單張：`r3c3` Ⓑ、`r3c2` Ⓒ、其餘 Ⓐ**、**②之五 輪播：`r1c3` 0.75／`r2c3` 0.75／`r3c3` 0.66**）。⚠⚠⚠ **兩把尺收成兩張表，而兩則的值不一樣**：單張（`mega` 268）`r3c2` 106→**131**、`r3c3` 215→**186**（往九顆的幾何平均 142.7 內插，t ＝ .70／.35），輪播（`micro` 162）`r1c3`／`r2c3` 188→**141**、`r3c3` 215→**142** ——**同一顆 `r3c3` 在兩則上是 186 與 142px，那不是筆誤**：兩則卡片寬差 106px，**症狀不同、治法就不同**（單張是「差 2.07 倍、圓的那顆偏小」，輪播是「整顆比卡片還寬」）。**通則：同一條規則在兩個容器上會長出兩個症狀，收成一組值等於治好一邊、弄壞另一邊。**⚠⚠ **輪播那三顆不再共用同一個倍率**，所以 2026-09-04 那條「彼此等墨」在那一則**不再成立**（墨剩 56／58／42%）—— 那是他逐顆挑的取捨，**不要拿「等墨」去訂正**。⚠ Flex 那一側 `{{watermark_size}}` 因此**拆成 `watermark_size_single`／`watermark_size_carousel` 兩個欄位**。⚠⚠⚠ **收掉四把尺：①之二／②之三（從來沒挑，而且和「往右溢出 18px」那個定案互相矛盾）／②之四（micro 已定案）／②之五** ——**尺可以收，量出來的數字不可以跟著消失**：兩排的「原寬 → 定案寬 ＋ 墨的代價」搬進那兩排自己的面板，「micro 之下第 ② 節那張自由折行會折在『星期／四』中間」搬進新的`#pv-panel-dtwarn`，「整條輪播整組一起換」搬進第 ③ 節。⚠⚠ **第 ③ 節改寫成「給工程師的四件事」**（使用者：「尺寸　再對話框裡的位置寫好　簡潔」「選擇邏輯和顏色　色碼　條列式」）：① 挑哪一顆（`順序[(月＋日)%9]`、月日取**那一筆的看診日**、顏色綁形狀、兩則同一條）② 九顆的色碼／長寬比／**兩則各一欄的寬度**（⚠⚠⚠ 整張表是 `wm-sizes.json` ＋ 那兩張表**現算的**，頁面上沒有再打一份 px）③ 位置（absolute 貼右下、溢出 18／10、⚠ 第一個子元件不可以是 absolute、⚠⚠ offset 不吃負值所以 JSON 只能寫 `0px`，**請照他們 09-10 自己的做法把溢出量統一成 18／10**）④ 卡片（`mega`／`micro`、`paddingAll 14`、日期那一格 `wrap: true` 不要 `maxLines`）。**口氣要跟著讀者換** ——整節沒有一句是「我們怎麼走到這裡」，推導搬到底下的「背景」小節。⚠⚠⚠ **`/preview/line-vendor/` 要跟著改，不然就是第二個真相**：那一頁的定案條寫著「寬度：每一顆自己一個（107~215px）」、九顆上卡畫的也是原寬 ——`vendor-log.json` 改成兩則各一欄並寫出欄位名，`build-vendor.mjs` **從提案頁讀回 `SPREAD1`／`WIDEK2`**（不抄第二份）。**通則（50-22 那條的反面）：一份規格搬到第二個地方之後，那一頁上原本只是示意的東西就變成規格的一部分。**⚠ 守門十四道、**全部負向測過**，其中兩道**自己先寫弱了**：只找 `wmW(n, false)` 會掃到別的面板（要指名那一段）、以及**驗註解不等於驗畫出來的東西**（line-vendor 那一道原本比對 caption 上那句「215 → 186px」，而那句話和實際畫的寬度是兩個各自算的值，只改其中一個照樣過 —— 改成讀每一張 `<figure>` 的 `aria-label`）。**通則：一道守門要量產出物，不要量產出物旁邊那句說明。**量到六個寬度溢出 0、JS 0。推導在 README **第六十七節**。⚠⚠⚠ **2026-09-14：這兩則的對話框底色從卡片色 `#F4F4F5` 換成純白 `#FFFFFF`**（使用者：「約診成功通知　約診查詢　的對話框　目前底色是紙色　改成白色給我看」）。⚠ **他講的「紙色」其實是卡片色** —— 站上兩個很接近的灰白（紙 `#e2e5e6` 是頁面的底、卡 `#f4f4f5` 是卡片），這兩則吃的一直是後者；**他看到的是偏灰的白、不是在指某一個變數名**，照做就好，但下手前要先確認是哪一個（改錯那一個不報錯，只是改了另一塊東西）。⚠⚠ **第十一之七節那句「背景色的答案是 `#F4F4F5`，不必改任何東西」因此作廢** ——那一條擋的是「換成**紙色**」，而且它自己的最後一句就是「真要換是使用者的決定」。**通則：一條寫著「不要改成 X」的紀錄，不等於「不准改」。**⚠⚠⚠ **一個值有六個地方在用，所以先給它一個出處**：規格頁 `:root` 的 **`--wcard`** ——卡片與那九顆的對照格吃 `var(--wcard)`、面板用 `getComputedStyle` 讀它去算對比與 L\*、`booked-mark.mjs`（那道「墨壓在浮水印最濃處」的最壞底色）與 `build-vendor.mjs`（**它把同樣那兩張卡再畫了兩次**：第 3 節九張、第 4 節四張）都從那一頁撈它。**唯一抄一份的是 `booked-card.json`**（要交出去的東西不能寫程式），守門專門比對它。⚠ **那 36 張浮水印 PNG 一張都不必重產**（顏色與淡度烘在 alpha 裡，底色只進守門；換成白之後那道更寬鬆，跑過一次逐位元組相同）。⚠⚠ **跟著變的兩組數字都不是版面**：① 柔墨 12% 疊到卡片色 L\* 89.9 → **93.3**、那九顆 89.6~91.7 → **93.0~95.1**，柔墨仍在帶子裡 ——**2026-09-12 那句「只有顏色變了、份量沒變」沒有被推翻**；⚠ 規格頁原本把這三個數字**寫死在說明裡**，改成「底下那塊面板每次現算」。② ⚠⚠⚠ **落選那一版（約診狀態拿套色當字）的對比整組變了**：4.15／6.62／2.93／5.21 →**4.56／6.62／3.22／5.21** —— 對比是對稱的，純白底下「套色的字壓在白底」和「白字壓在套色上」**變成同一組數字**，原本「藍與橘過不了」現在只剩橘，所以面板改成**現場數誰過不了**。⚠ **落選的理由沒有跟著變**（那一階是給填實的塊用的，不是靠那兩個數字才落選的）。⚠ 守門九種負向測過；⚠⚠ 其中兩件：**掃字第九次撞到自己的說明**（頁面上有一句「先前這兩則寫的是 #F4F4F5」，所以那一道只掃「拿來算的那兩處」不掃整頁）、以及**寫到一半拿掉一道永遠不會失敗的守門**（`check-vendor` 第 ① 道重跑逐位比對，產出的那一頁不可能是舊的）。⚠⚠ **其餘五則沒有動，仍然是 `#F4F4F5`** —— 那是已知的不一致不是漏改，七則要不要一起換是另一個決定。推導在 README **第六十八節**。⚠⚠⚠ **同一天稍晚量過廠商 09-10 送來的那三張截圖：他們本來就是純白，所以這一項不必請他們換**（使用者：「確認廠商回傳的這三張底色是白色還是紙色？」）——零依賴解 PNG，卡片內部 `#ffffff` 佔 75.4~77.2%；⚠⚠ **真正的證明是拿浮水印當試紙**（它的顏色與 12% 的 alpha 烘在 PNG 裡，所以疊出來的實色完全由底色決定）：口外紫量到 rgb(239,236,243)，離「疊在純白」**2.0**、離「疊在卡片色」15.2；牙周青綠量到 rgb(231,239,239)，離純白 **1.0**、離卡片色 16.2 —— 差一個數量級，沒有第二種讀法。**所以 09-14 那一改不是一個新要求，是我們這一份去對齊他們**；規格頁第 ④ 節、`vendor-log.json` 與 `build-spec.mjs` 那三處「這一項也是要請你們換的」都已經改口。⚠⚠⚠ **通則（第六十二節那條的第二次現場）：一份規格和對方的成品對不起來時，先問「對方現在跑的是哪一個」** ——六十二節是 `micro`／`deca`、這一次是 `backgroundColor`，**兩次錯的都是那份還沒交出去的 JSON，不是對方**；而且兩次都不是看一眼看得出來的（`#f4f4f5` 對 `#ffffff` 只差 11 階），**要嘛逐像素量、要嘛拿一個已知成分的東西去反推**。⚠ 順帶再證實一次 22-13「他們真的照那張九顆的表在換」（兩張 ⑥ 的浮水印分別合得上口外紫與牙周青綠）。 |
| **取消／改期那一段對話** | ✅ **2026-09-05 定稿**（使用者：「Ok 定稿 存下來」）：**圖卡 ＋ 確認那一句「要取消這個約診嗎？」＋姓名＋日期 ＋ 綠底「是喔 要取消」配白色的 nogo ＋ 白底線框「按錯惹～我會去～」配主要那顆標誌的深階版 ＋ 按了取消一律回「已取消，若還有其他問題請與診所聯繫喔～」＋ 按了不取消回「OK，約診還在喔～」並列出姓名與約診時間**。規格頁 `/preview/line-cancel/`（**切換條整組拿掉**，改成五節：新版兩則 → 退路 → 現況兩則 → 還沒收掉的 → 檔案清單），Flex 的 JSON 在 **`drafts/channels/cancel-card.json`**（三則：`cancel-confirm`／`cancel-done`／`cancel-keep`），守門 **`node drafts/channels/check-cancel.mjs`**，圖五張 `node drafts/channels/cancel-png.mjs --all`（`shot-cancel` 確認卡／`-go` 按了取消／`-no` 按了不取消／`-chat` 整段對話／`-old` 現況）。量到（430／393／390／375／360 逐格相同）：確認卡 **268×242**、按了取消 268×90、按了不取消 268×134、退路 268×160、現況兩則各 268×108；孤字 0、水平溢出 0、JS 0、對比 0 個不過。⚠⚠ **提案頁變成規格頁時，一頁會從一個狀態變成好幾個狀態並排 —— 凡是用 id 抓的東西都要先改成 class**（原本靠 `#pv-card`／`#pv-res`，一頁只能有一張卡；改成 `.js-card`／`.js-res` ＋ `measure(host, panel, …)`），不然第二段之後全部量到第一段。⚠ 網址參數留著（`?v=a|b|c`／`?l=yn|full|short`／`?o=safe`）。⚠⚠ **`cancel-png.mjs` 的守門是「拍之前先量那個元素，出圖之後和它比」** —— 沒有沿用 remind-png 那條「高一定大於寬」（對橫的圖會誤報），**守門要跟著這一張圖的形狀走，不要跨頁照抄**；拍整段對話用 `:has()` 抓「裝著那一段的那個框」，一頁有五個 `.pv-phone`、寫死順序會錯。⚠⚠ **守門掃 emoji 之前要先把 `_` 開頭的註解鍵剝掉** —— `_說明` 裡寫滿了 **⚠（U+26A0）**，而它落在 emoji 的字碼區間 `☀`–`➿` 裡，第一次跑就誤報「Flex JSON 裡有 emoji」。**通則：這條線每一份 JSON 都把說明寫在資料裡面，掃字的時候一定會撞到。**⚠ **四件沒有跟著定案、面板一直標著**：「按了不會幫你排新的時間」現在整段對話沒有人講（兩條補救都要先問他）、「請與診所聯繫」還是沒有號碼、按「否」之後現況回什麼還沒看過、廠商那四題一題都沒答。推導在 `drafts/channels/README.md` 第 **23-18** 節（那一句最後換成「還在」的推導在**第二十七節**）。以下是走到定稿的過程 —— **2026-09-04 開提案頁**（`/preview/line-cancel/`，三案 ＋ 四把尺，**同日稍晚使用者六件逐字改動、尺收成兩把**）。⚠⚠⚠ **六件（原話逐字照做）**：按了之後**不分來得及／來不及一律顯示同一則**「已取消，若還有其他問題請與診所聯繫喔～」（**2026-09-05 使用者再指定把「（註）這次取消已經超過最晚2天前（不含假日）的時間。」整句拿掉**）／**頁頭那排 logo ＋ 診所名拿掉**／**姓名與約診時間底下的淡底拿掉**（字留著）／**第一顆綠底套色「是喔 要取消」**／**第二顆白底「按錯惹～我會去～」**（2026-09-05 使用者再指定：前者中間改**半形**空格、後者的空格換**全形波浪**並在句尾再加一個）／按鈕的 logo 沿用；**2026-09-05 再兩件**：「按下去只會取消這個時段…要看診請重新來電預約。」**整段拿掉**（Ⓑ 那句短版一起拿）、按了不取消的結果收成**「OK，約診還在喔～」＋ 底下列出姓名與約診時間**（排法逐字照確認卡那一塊：姓名一行、日期 19px 粗體、沒有淡底 —— 他核對的是同一組東西，兩張卡長一樣才對得起來；Ⓑ 純文字那一則同樣補上）。⚠ **那一句 2026-09-05 稍晚換了兩次，定案是「OK，約診還在喔～」**（「Ok，約診保留。」→「OK，約診保留了～」→ **「OK，約診還在喔～」**）：第一次換的是**口氣**（OK 兩個字母都大寫、句號換成全形波浪，和上面那一則「請與診所聯繫喔～」對齊）——**通則：口氣一致在同一段對話裡比在單一則裡重要**，兩秒之內用兩種語氣，讀起來像兩個人寫的；第二次換的是**主詞**：**「保留」是診所在做的事，「還在」是他要確認的事** ——他按這顆的意思是「我沒有要取消」，最想立刻看到的就是那個約診沒有被動到。**通則：回覆一個動作時，講的要是他剛才擔心的那件事的狀態，不是我們為他做了什麼。**⚠ 兩版**字面寬度完全相同**（都是 7 個全形＋2 個半形），所以五個寬度的高度、折行與圖片尺寸逐格沒動。⚠⚠⚠ **那三行帶走的是這一輪最重要的那件事** —— 它是整段對話裡**唯一**告訴病人「按了不會幫你排新的時間」的地方，而 09-04 那張真截圖已經證實那個誤會會發生（病人下一句「可以幫我改晚上嗎」）。**沒有自己加回去**：面板那一列標紅、頁面底下寫清楚它帶走什麼，並給兩條不必放回卡片的路 ——**改那顆按鈕的字**（「預約改期」卻只取消，那才是源頭，本來就在廠商清單上）或**寫進取消成功那一則**。**通則：使用者拿掉一段字時先確認它是不是某件事實的唯一出處；是的話照拿、把代價寫在頁面上、給不必回頭的替代路。**⚠⚠⚠ **那兩個波浪讓 Ⓑ（confirm 範本）從「很緊」變成真的截斷**：「按錯惹～我會去～」量到 **128px／可用 122px**，四個手機寬度都截（「是喔 要取消」反而 96 → **85px**，半形空格省 11px）；**Ⓒ 圖卡不受影響**（按鈕自己一行、可用 240px 且會換行）。**只有廠商說送不出 Flex、退回 Ⓑ 時才要處理**（拿掉句尾那個波浪就回到 112px），沒有自己改他的字。⚠⚠⚠ **那個事實風險 2026-09-05 隨著那一句被拿掉而解掉了**：「（註）…已經超過最晚2天前」**對在期限內取消的人是錯的**（系統確實會成功取消，`?r=ok` 看得到那張真截圖），而這一則是不管來不來得及都送的 —— 拿掉之後剩下的每個字對兩種人都成立，「一律送同一則」才站得住。⚠ **代價**：來不及的人不會在這裡被告知超過期限，但那件事本來就該寫在**按下去之前**（綁定完成那一則已經寫著「最晚請於看診日前 2 天（不含假日）」）。⚠ **通則：使用者拿掉的那一句如果正好是唯一有事實風險的那一句，不要再自己把等價的意思補回別的地方。**⚠ **「請與診所聯繫」沒有給號碼**，建議底下多一行 `05-5339369`，**不要做成按鈕**（第十一之四節）。⚠⚠⚠ **兩顆按鈕的 logo 語意是反過來的**：綠底＝「是喔　要取消」＝**不會來**→ **白色的 nogo**（`mark-nogo-white.png`，由 `drafts/channels/remind-marks.mjs` 一起產，**同一顆翻轉的形狀不要開第二支產生器**）；白底＝「按錯惹　我會去」→ **主要那顆標誌的深階版**（`mark-head-green.png`，2026-09-05 使用者指定「按錯惹的 logo 是現在主要的那個」）。⚠⚠⚠ **不要用 `mark-green.png`** —— 那個檔名只講顏色沒講形狀，它其實是招呼卡「介紹芳仁給朋友」用的 **shape-r2c3**（3.081），不是站上頁首那一條（2.029）；深階版的頁首標誌 2026-09-05 才由 `mark-png.mjs` 補產。**通則：檔名裡的顏色不等於形狀，引用之前先看一眼產生器的 `CASES` 或對長寬比。**提醒卡正好相反 —— **logo 跟著「去不去」走，不是跟著「哪一顆是主鈕」走**（要「位置沿用」改兩行就換得回來）。⚠⚠ **踩到一件：自己指定的斷行會被再折，而面板原本只掃確認卡量不到** ——「（註）這次取消已經超過最晚2天前」量到 250.2px > 可用 240，畫面折成「…2天／前」、孤字卻報 0。修法：斷行改成量過的四行（176／96／176／234），面板掃描範圍加進 `#pv-res` 並新增一行**「自己斷 N 行／畫出 N 行」**。**通則：凡是自己指定斷行的東西，都要有一個「有沒有被再折」的量測。**⚠ **Ⓑ 放他這組字已經超出**（見上，128／122），走 Ⓑ 要再短一點。⚠⚠ **Ⓑ 還有第二處超出，但那一處是佔位符撐的**：confirm 範本第二行（姓名＋日期）量到 **249.1px／可用 240**，「20:00」會被折到第三行；而〔病人姓名〕是 7 個全形字，**真名 3 個字只有 201.1px、放得下**。面板照實標紅，**不要為了讓它變綠去改內容，也不要把示範值改短去騙過自己的量測**（示範值本來就該取偏長的那一種）。⚠ 切換條 29.8% → **9.0%**（`?l=`／`?o=`／`?r=ok` 仍吃得到）。以下是那之前的推導 —— 起點是使用者的後台截圖：病人送「預約改期」→ 系統回一張 confirm 範本「您確定要取消嗎？［是］［否］」→ 按「是」→「很抱歉，因為約診時間太靠近…😭😭」。使用者：「因為可能會有誤按取消狀況，所以有重複確認取消的設定，這個頁面你們討論一下怎麼調整，現在的非常單調。」⚠⚠ **那一層確認沒有要拿掉**（防誤按的理由成立），要治的是它問得夠不夠清楚。⚠⚠⚠ **真正的問題不是單調：那顆按鈕叫「預約改期」，可是它只取消**（第八節：「按下去只是系統上顯示取消」）—— 整段對話沒有一個字告訴病人「按了不會幫你排新的時間」，他按完可能就等著。⚠⚠ **防誤按不是多問一次，是讓他看得到他要毀掉的東西**：現況沒寫**是誰**（一個帳號綁得了好幾位家人）也沒寫**哪一天**，而「是／否」沒有動詞、還有歧義（「是」可以讀成「是，我要去」），**破壞性那顆又站在慣性落點**（左邊）——把「不取消」排前面是**零成本**的一項。⚠⚠ **它單調不是廠商偷懶，是容器**：LINE 官方的 **confirm 範本樣式一個像素都改不了**（放不進圖、顏色、粗體），能改的只有那句話與兩顆按鈕的字 ＝ Ⓑ 案；要有品牌只能換 **Flex 圖卡**（Ⓒ），**和招呼圖卡卡在同一題**（廠商送不送得出 Flex）。⚠⚠⚠ **Ⓑ 有量出來的天花板**：confirm 的按鈕一顆只有半個卡片寬（可用約 **122px**）而且不換行，「不取消，我會到」量到 **112px、只剩 10px** —— 走 Ⓑ 就要挑「短一點」那一格。⚠⚠⚠ **最省事的一件在更前面：期限外那張卡不該出現** —— 系統回「時間太靠近」表示它自己知道來不及，那就直接給電話，**三步變一步**。⚠ 結果那一則少兩件：**沒說約診還在**（他可能以為取消了就不來 ＝ 爽約）、**沒給下一步**（這時唯一有效的路是電話；改約那句逐字沿用綁定完成那一則）。⚠⚠⚠ **2026-09-04 稍晚使用者給了第二張截圖（取消成功那一則），它證實了上面那件事**：那一則送出之後，病人下一句就是「**可以幫我改晚上嗎**」—— 他以為改期還在進行中，而系統只回得出自動回應。**不再是推論。**⚠⚠ **而且那一則印得出病人姓名與完整的約診時間**，所以確認卡沒有印**不是拿不到、是那張卡沒有印** —— 跟廠商談的時候這一條最有用（把姓名與日期加進確認卡**不是新功能**）。⚠ 它自己有四個小地方：「**斗六**芳仁牙醫診所」、**❤️**、「煩請您儘速」是公文體、**日期帶著年份**；但「**預約保留完整療程時間**」是有內容的，改寫時不要弄丟。⚠ **只剩「按否」那一則沒看過**，切換條上那一格仍然是**擬的**。⚠ **兩張截圖上的病人姓名都遮成〔病人姓名〕** —— 那一頁在站上、repo 也是公開的。⚠ **按鈕的 label 會變成病人自己說的那句話**，也是診所日後回看的紀錄 ——「是」等於沒有資訊。量到（改動後）：卡 268.0、Ⓐ 高 108／Ⓑ **160**／**Ⓒ 242**、整段對話 60%、孤字 0、被再折 0、水平溢出 0、JS 錯誤 0、對比 0 個不過、切換條 **9.0%**（四把尺各佔一列時是 29.8%，**標籤收進同一列**才收得住）。⚠ 量孤字**不能拿整張卡去分組** —— 並排的兩顆按鈕在同一個 top 上，會被併成「是否」誤報（踩過）。推導在 `drafts/channels/README.md` **第二十三節**，四題要問廠商的在 23-7 |
| **颱風／臨時休診通知** | ✅ **2026-09-05 定稿**（使用者：「定稿　存起來」）：**圖卡（Flex）・停診 ＋ 頭圖 ＋ 他自己 7/10 打的那一份字（一個字都沒改）**。規格頁 `/preview/line-typhoon/`（**切換條整組拿掉**，改成**五段並排**：定案 圖卡・停診／退路Ａ 純文字／退路Ｂ 文字＋圖／照常看診 草稿／颱風過後 草稿；⚠ 網址參數仍然吃得到：`?w=new` 我們改寫的那一份／`?m=on`／`?t=now`），Flex 的 JSON 在 **`drafts/channels/typhoon-card.json`**，守門 **`node drafts/channels/check-typhoon.mjs`**，圖三張 `node drafts/channels/typhoon-png.mjs --all`（`shot-typhoon` 定案那張卡／`-text` 純文字／`-chat` 在 LINE 裡）。量到（430／393／390／375／360 逐格相同）：卡片 **268×447.8**、頭圖 268×134、聊天室那張照片 232×116、孤字 0、水平溢出 0、JS 0、紅線 0。⚠⚠⚠ **定稿不等於可以送出去**：圖卡的字是**廠商系統裡排好的模板**，不是他在聊天室打的，而他選這條路的前提正是「字是我自己每次打的」—— 所以要跟廠商談成一件事：把 **〔颱風名〕〔日期〕×2〔星期〕四格做成可以填的欄位**（`{{typhoon}}`／`{{date}}` ×2／`{{weekday}}`），他填完、系統發給那天有約的人 —— **那條路同時把「一個一個貼」也解掉**。談不成就退回純文字或「先貼圖再貼字」，**圖不會白畫，三條路共用同一張**（守門有一條在盯）。⚠⚠ 守門有三件是這一則特有的：**同一份字要在三個地方逐字相同**（JSON／「要貼進去的字」那一塊／畫在卡片上的那一份 —— 後者要先把示範值套上去，而示範值那張表是**從規格頁的原始碼讀回來**的）、**`{{date}}` 一定要出現兩次**（最容易漏掉的一格，漏了廠商只會做一個欄位）、**聊天室那張照片和卡片頭圖要是同一個檔**。⚠ **頭圖是 JPEG 不是 PNG**，尺寸要自己掃 SOF 標記，**不能照抄別支的 PNG 讀法**。⚠⚠ **口氣那幾個詞（民眾／切勿／敬請見諒／另行公告）不在守門的紅線表裡** —— 紅線是「寫了就是說謊」（這一份 0 處），口氣是「我覺得可以更好」，而使用者已經決定用自己的字（同第 25-13 節：**面板要標對級別，不然就從量尺變成一直在勸**）。⚠⚠ **提案頁變成規格頁時，一頁從一個狀態變成好幾個狀態並排 —— 凡是用 id 抓的東西都要先改成 class**（`.js-chat`／`.js-panel`／`.js-note`，`measure()` 多收一個 `host`），不改的話第二段之後全部量到第一段（line-cancel 23-18 踩過，這是**第二次**）。⚠ 出圖的守門是「拍之前先量那個元素、出圖之後和它比」，**沒有沿用 remind-png 那條「高一定大於寬」**（對橫的圖會誤報）；元素一律用 `[data-blk]` 抓、不要寫死順序。推導在 `drafts/channels/README.md` 第 **25-24** 節。以下是走到定稿的過程 —— **2026-09-05 開提案頁**（使用者給了 2026-07-10（五）19:37 發出的截圖：「我去系統裡抓颱風休診當天的病患群發的訊息，我想做成圖卡，你們確認 line 上可不可行」）。＝ 第 14-3 節盤點表的 **⑤**（63 分，第 14-6 節：「分數不高，但性價比是全表最高的 —— 要做的只有先寫好放著」）。提案頁 `/preview/line-typhoon/`（四把尺：怎麼送／情況／失約／電話），推導在 `drafts/channels/README.md` **第二十五節**。⚠⚠⚠ **可不可行的答案要分兩層講**：**LINE 平台送得出 Flex**，但**診所現在的送法（一對一聊天室一個一個貼）與後台群發，型別裡都沒有 Flex** —— 只有**廠商從他們系統送**才有（同第 25-11 節那條通則：**「做不到」要講清楚是誰做不到**）。⚠⚠⚠ **而這一則有一件和另外四則都不一樣：內容每次都不一樣**（颱風名、日期、縣府宣布什麼、診所停不停），所以**「圖文訊息」那條路（字烘進圖）在這一則是最貴的** —— 每次颱風都要重做一張圖，而那正是最沒時間的十分鐘。**Flex 沒有這個問題（卡片裡是真文字），純文字也沒有，而且今天就能用。**⚠⚠ **真正的成本不是圖，是那份名單**：現在人工去系統抓、一個一個貼，而**廠商的系統本來就知道那天有哪些人有約** —— 那條路同時解掉「圖卡」和「一個一個貼」，**談的時候要一起談，不要只談圖**。⚠⚠ **現況那一則的三件，前兩件是內容不是美感**：①**第一段和第二段互相矛盾**（「門診將同步暫停、切勿自行前往」vs「若風雨不大而診所恢復開診，將電話連絡」）—— 病人真正的問題是「我明天到底要不要去」，這一則給了兩個答案；正解是**把兩種情況收成同一句**：不管當天有沒有提前開診，**都是我們打電話給你，在那之前不用出門**。②**最重要的那一句埋在第一段的最後**（第一眼看到的是「因應巴威颱風來襲，雲林縣政府已宣布…」＝ 背景不是結論）。③「民眾／切勿自行前往／敬請見諒／另行公告」是**公告的詞**，而這一則是一對一發給有約的病人；「造成不便，敬請見諒」還多一件：**颱風不是診所造成的**，道歉會讓人以為診所本來可以選。⚠ **兩件是對的、不要動**：「並祝颱風天平安」（最有人味的一句，新版留著）、**時機**（縣府宣布之後就發，不要等到當天早上）。⚠⚠⚠ **新版真正的改動是把要改的地方從 4 處變成 1 處**（颱風名拿掉、日期只寫一次，後面用「那天／當天」指回去）—— **那不是省字，每少一個可變欄位就少一個下次要改的地方**，而那是這一則唯一真正的成本；面板現場數 `〔…〕` 出現幾次，**所以文字裡的可變處一律寫成 `〔…〕`、不可以先填好**。⚠⚠ **做了三則不是一則**：停診（＝現況那一則）／**照常看診**（現在沒有，**它擋掉的電話可能比停診那一則還多** —— 颱風天病人最常打來問「你們有沒有開」，而電話是結構性瓶頸；⚠⚠ 但它的重點**不是**「我們有開」是**「你不必勉強來」**，那句話一寫，不來的人會先打電話而不是直接不出現）／**颱風過後**（新的，不是取代那通電話，要不要用是診所的決定）。⚠⚠ **兩把尺是診所的事實題**：**「這次不算你失約」**（政策不是文案，⚠⚠⚠ 而且先卡在一個系統事實 —— **停診之後系統裡那些約診會變成什麼？**48 小時提醒在停診前就送出了，而那張卡寫著「24 小時沒回應會自動取消並遞補」，照樣計時的話這句話就寫不得）、**電話**（**颱風天診所有沒有人接**我們不知道；沒人接的話「有急事可以打」比不寫還糟）。⚠ 量到（430／393／390／375／360 × 4 送法 × 3 情況 × 2 × 3 ＝ **360 組**）：溢出 0・孤字 0・JS 0・自己斷幾行＝畫出幾行、泡泡與卡片五個寬度**都給得到 268**、切換條 **18.3%**；390 上停診那一則 現況 306／純文字新版 462／Flex 圖卡 599px；**紅線現況 2 處、新版三則都是 0**。⚠ **新版比現況高是手動斷行換來的**（每行都短、一眼掃得完），而且兩者在提案頁上**不是同一個比較基準**（現況吃滿寬度、新版一行 14 個全形字）—— **這一則一年只發幾次，高度不是要省的東西**。⚠⚠ **這一輪踩到兩件都不報錯的**：①**空行也有 rect**（`white-space: pre-wrap` 之下段落之間那個 `\n` 自己有 16px 高的框），照 `top` 分組時三個空行就多三行，面板報「自己斷 14 行／畫出 17 行」看起來像折行壞掉、其實一行都沒折 → `lastLine()` 要跳過 `\n`，**量的是墨不是換行符**；⚠ **這段程式是從 `line-auto-reply` 抄來的，那邊也有同一個偏差**（只是沒把行數印出來所以看不見）。②**手動斷行是資料的一部分，不要在算繪那一層抹掉** —— 圖卡標題寫了 `replace(/\s+/g,"　")`，把「颱風過後」本來就兩行的標題壓成一行、折出孤字，**症狀是純文字那一格好好的、只有圖卡壞**。⚠ **頭圖還沒畫**（Ⓒ／Ⓓ 都是佔位框、和真圖一樣高，所以現在量到的數字不會作廢）；⚠⚠ **兩條路的圖不能共用**（聊天室那張 1:1 1040×1040、Flex 頭圖 2:1 1024×512），**走哪一條要先定**。⚠ **Flex 的 JSON 等使用者挑完才寫**。⚠ **建議**：先走純文字（今天就能用、字留在自己手上，同自動回應那一則的定案理由），圖卡當成跟廠商談的一件；**不要走圖文訊息那一條**。⚠⚠⚠ **2026-09-05 使用者定案：文字用現況那一份**（「現況的文字是我自己每次打的，所以還好」）—— **這一句推翻了新版最實際的那個賣點**：我把「每次要改 4 處 → 1 處」當成改進，但那建立在「那一則是模板」這個**沒問過的假設**上，他每次現打，所以少三個欄位對他等於零。**通則：算成本之前先問「這件事現在是誰在做、怎麼做的」**（同 20-10、22-1 那條「截圖是後台不是病人手機」——都是把現況看成我以為的樣子）。⚠⚠⚠ **但它同時關掉了圖卡那條路，那才是這一輪真正的結論**：**「他自己每次打」和「圖卡」在現在的送法下互斥** —— 純文字 ✅／**固定的圖 ＋ 文字 ✅（唯一同時成立的）**／圖文訊息 ❌（字烘在圖裡）／Flex 圖卡 ❌（廠商系統的模板）。**所以這一頁剩下的問題只有一個：要不要為了有圖，每個人多貼一次。**⚠⚠ **第二件：他那一份字的結構也放不進卡片** —— 三大段、沒有手動斷行、第一段一百多個字，而卡片的版型要一個**短標題**才成立。`flexCard()` 因此加了 `titled()`（第一段最多兩行、每行 ≤20 個全形字才做標題），否則**整份當內文排、不假裝它有結構**，面板多一列在報。實測 390：他的字塞進卡片 **268×447.8**＝「一張頭圖 ＋ 一大塊內文」，卡片的版型一項都沒用到（我們改寫的是 268×598.5、有標題有淡底方塊）——**要走圖卡就得連文字一起重寫，那正是他不要的**。⚠ 提案頁跟著收：**四把尺收成兩把**（怎麼送／情況），那把尺**從「哪一版的字」變成純粹的「怎麼送」**（三格用同一份字），已定案的收成網址參數 `?w=new`／`?m=on`／`?t=now`；⚠ **「照常看診」與「颱風過後」他從來沒寫過**，那兩格只有我們擬的草稿，`isMine()` 判斷得出來、面板標著「這一則你沒有寫過」，**不要讓它看起來像已經存在的東西**。⚠⚠ **面板自己也錯了一級**：第一版把「承諾做不到的事」和「公告詞／道歉」混成一列印「紅線 2 處」，**於是他已經定案的那份字每次打開都被標紅** —— 拆成**紅線**（寫了就是說謊，他那份 0 處）與**口氣**（民眾／切勿／敬請見諒，2 處、不上紅色只記著）。**通則：使用者選定的東西還要繼續量，但量出來的東西要標對級別，不然面板就從「量尺」變成「一直在勸」。**⚠ 重新量（90 組）：溢出 0・孤字 0・JS 0・五個寬度都給得到 268・**切換條 18.3% → 10.3%**。⚠ **建議不變但理由換了**：純文字 ＝ 他現在的做法，沒有要改的東西；這一則剩下的價值只有兩件、**都不必動文字** —— **三則先寫好放著**（照常看診那一則現在沒有，而它擋掉的電話可能最多）、以及**跟廠商談那份名單**。推導在第 **25-13** 節。⚠⚠⚠ **2026-09-05 稍晚：頭圖的提示詞寫好了**（`drafts/line-typhoon-hero-prompt.md`，第 **25-14** 節）——使用者：「沿用〔一般牙科著陸頁〕那個背景　但天氣是刮風下大雨　陰陰暗暗的　診所大門鐵門降下來　路上只有幾個行人…傘快要變形很難前進的樣子」。⚠⚠⚠ **那張圖定稿的提示詞明文禁止鐵捲門**（`There is no shutter, no roller door, no garage.` ＋ AVOID 一條）——那是當初為了擋模型把騎樓畫成車庫，不是事實；實景照上**玻璃上緣那條深色箱體就是鐵捲門的收納箱**，門在**玻璃那個平面**降下來、柱子與騎樓仍在門前。**通則：沿用舊提示詞之前，要把它的 `AVOID` 逐條唸過一次** —— 那裡面有一半是「為了擋當時某個特定的失敗」，而 AVOID 通常贏過正文。⚠⚠ **餵參考圖要餵 `drafts/og-topic-general-src.jpg`，不可以餵 `assets/og-topic-general.jpg`** （後者上緣有後製疊上去的綠帶子與中文字，會被學走）。⚠⚠⚠ **一張 2:1 兩條路共用，原本以為要畫兩張** —— 十一之一節那句「兩條路的圖不能共用」**在這一則不成立**：1040×1040 是**圖文訊息**的規格，而這一則是「貼一張照片 ＋ 他自己打字」，照片不必是方的，所以聊天室那張和 Flex 頭圖是同一張 **1024×512**，**圖先畫、路線後定都不會白畫**；提案頁兩個佔位框已一起改成 2:1，整段對話 390 上 **純文字 366.8（43%）／文字＋圖 475.4（56%，原本約 72%）／圖卡 508.7（60%）**。⚠⚠ **這是全線唯一「診所是暗的」那一張**，所以視覺落點從暖燈換成「鐵門那一大塊深色 ＋ 一件亮黃色雨衣」，而且**它是唯一會被讀成「這間店倒了」的一張** ——「陰暗 ＋ 關著的老房子」正是 2026-08-22「像鬼屋欸」那個組合，提示詞因此有一整段在擋剝落／水漬／鏽／封板／蛛網。⚠ 另外三個舊坑一起擋掉：辛苦寫成揉眼睛＝在哭、雨畫成一條長曲線＝靈魂出竅（要一群同向的短線）、零文字要寫成小寫敘述句。⚠ **裁圖的守門不可以照抄 `remind-hero-crop.mjs`**（那一支的「深墨離上緣 ≥12 列」在這張滿是暴風天空的圖上會一直誤報）。⚠⚠ **圖畫好不代表會用到** —— 那一頁還開著的唯一一題仍然是「要不要為了有圖，每個人多貼一次」。⚠⚠⚠ **2026-09-05 第一版出圖：構圖對了、天氣全錯**（使用者「這個效果有 5、6 成了」，四件：白弧線像晴天的微風／**雨的方向和行人抵擋的方向剛好相反**／路樹很平靜／建築太亮）—— **四件全在風、雨、光這三樣全域的東西上，構圖、建築、鐵門、三個人都對了**，所以第二版把第一版那張當**構圖參考圖**餵回去、只重畫天氣。⚠⚠⚠ **雨反了的成因是「相對指代」**：對風寫死了絕對方向、對雨只寫 `all leaning the same way` ——**通則：方向要每一項各自寫死一次，寫成沒有第二種讀法的句子**（「每一道雨的上端靠近畫面左緣、下端靠近右緣」）。⚠⚠ **白弧線與亮牆都是「照抄晴天那張」造成的** —— **通則：沿用風格參考圖時，要逐項問「這一項在新的天氣底下還成立嗎」**；建築的修法不是整張調暗（會掉進灰階紅線），是**排一把明度的梯子**（黃雨衣最亮 → 路面倒影 → 水泥牆 → 天空 → 騎樓與鐵門最暗），驗收就是把成品轉成灰階看那五塊的順序。四條通則已進 ILLUSTRATION.md 第十五節。⚠⚠⚠ **2026-09-05 第二版：強度過了，但雨的方向連兩版都往左下**（使用者：「感覺夠激烈了　但雨的方向還是錯的／診所門口的盆栽…一個被吹歪　一個盆栽被吹倒破掉　裡面的土灑出來」）——**第二版已經把「每一道雨從左上落到右下、一道反的都沒有」寫進正文又寫進 `AVOID`，出圖照樣往左下落**。逐項看那張圖：**雨與路樹已經一致（都在說風從右邊來），不合的只有三個人**。→ 第三版**整個掉頭**：風改成從畫面右邊吹向左邊、**雨與樹一個字不改、把三個人轉過來面向右**。⚠⚠⚠ **通則：同一件事寫死兩次還是失敗，就順著模型的先驗、改另一半**（雨的方向是強先驗，人往哪走完全自由）；檢查只要一句話 —— **人往哪邊傾，雨就一定往另一邊落**。⚠⚠ **第二件：自己上一輪寫的 `AVOID` 會反咬** —— 盆栽站得直挺挺是因為我在第二版寫了`the pots … are still upright`（為了擋災難片），**但被吹破的盆栽不是災難是天氣**；**通則：每一輪都要把自己上一版的 AVOID 逐條唸過一次**。⚠ 破盆栽要寫清楚**時態**（破片乾淨、土深色濕的、植株還綠）——不寫會被畫成堆很久的垃圾，掉進「像鬼屋」那一邊。兩條通則已進 ILLUSTRATION.md 第十五節第 5、6 條。⚠⚠⚠ **2026-09-05 第三版被退回 —— 那個「掉頭」是我自己選錯邊的**（使用者：「不對欸　應該是雨的方向錯了　雨應該要從左上往右下才對　**風是從左往右吹**」）。**通則要修正：「順著模型的先驗、改另一半」只有在兩半都自由時才成立** —— 風往哪邊吹是**使用者的意圖**不是自由度，**先問「這一半真的是自由的嗎」，治不動也要回頭問，不可以自己選邊**（ILLUSTRATION.md 第十五節第 5 條已改掉）。照他的方向重盤第二版那張圖，**錯的只有雨和樹兩樣、而且都是背景**，**三個人完全正確不要動**。⚠⚠⚠ **而方向在提示詞裡寫死兩次都沒用（正文＋AVOID），所以第四版改用參考圖 —— 方向也是形狀**（ILLUSTRATION.md 第十之一節那條「形狀不要用文字描述，用參考圖」以前沒把方向算進去）：`node drafts/channels/typhoon-rain-ref.mjs` → `preview/line-typhoon/rain-direction-ref.png`（1024×512、1029 道線）。⚠ 那張圖**故意長得像圖表不像插畫**（平底色、只有線、**沒有物件、沒有文字、沒有箭頭** —— 箭頭一定會被畫進成品裡），餵的時候要明講「只抄角度與方向，不要抄顏色、不要當背景、不要在畫面上畫一塊灰色的斜紋板」，`AVOID` 也補一條。⚠ 再加一個不靠圖的錨點：**反斜線**（左上→右下 ＝ `\`；右上→左下 ＝ `/`），那兩個字元沒有第二種讀法。⚠ **第四版還是反的話不要再改提示詞**，改成拿第二版那張圖去修「把每一道雨鏡射過來，其他一個像素都不要動」——**一次只改一件**。第四版逐字在 `drafts/line-typhoon-hero-prompt.md` 第五之六節，推導在第 **25-17** 節。⚠⚠⚠ **2026-09-05 第四版：雨連四版都反，而且鐵門的位置錯了**（使用者：「還是錯的　**而且鐵門的位置錯了**」，第 **25-18** 節）。**①鐵門那件比雨嚴重而且是新的：騎樓不見了** —— 捲門被畫在建築最前面那個平面（門落到人行道、柱子壓平貼在門上、轉角柱右邊的側牆也長出捲門）。原始照片上一樓玻璃是**退進去**的，前面還有一道低階、一條騎樓地板、三根柱子。⚠⚠ **第四版其實寫著「hangs BEHIND the three columns」，但整段的主語都是那塊金屬、騎樓本身一個名詞都沒有** ——**通則：要模型畫出「A 在 B 後面」，就要把 A 和 B 之間那個空間當成一個東西寫出來**（地板、階、天花、影子），只寫「在後面」它會理解成「貼著」；並補一條它一定會順手多畫的（側牆沒有捲門）。⚠⚠⚠ **②雨：正文寫死、AVOID 寫死、連方向參考圖都給了，仍然連四版都反** —— 絕對座標模型接不住。**但它四版都沒畫錯「人面向哪一邊」**，所以第五版把方向綁在人身上：「**雨是打在她臉上的，她的背是被遮住的那一側**」，再補兩個形狀錨點（傘被風往背面折回、樹是被推的所以倒向她背過去那一邊）。**通則：絕對方向講不動的時候，把方向掛在模型畫得最穩的那個東西上**（第一層用參考圖給形狀 → 第二層方向也是形狀 → 這是第三層）。⚠ **第五版還是反的話不要再改提示詞**，用**鏡射法**：把圖左右翻了當參考圖讓它畫（它畫自己偏好那個方向的雨從來沒失手），**出圖再翻回來** —— 只有在畫面上一個字都沒有時才成立。第五版逐字在第五之八節。✅ **2026-09-05 第五版：鐵門與雨都對了**（使用者：「這個對了　但**微風徐徐的白色弧線又跑出來了**　要改成颱風猛烈吹送的感覺」，第 **25-19** 節）——其餘全部正確，所以第六版把每一段的口氣從「要改成…」換成「**上一版已經對了，這一段只是讓你核對**」，不然它會把已經對的東西再翻一次。⚠⚠⚠ **白弧線是第二次跑回來，而第五版兩處都寫了禁令（雨那一段的尾巴 ＋ `AVOID`）、兩條都輸了** ——成因是**那幾條白弧線畫在參考圖上**，而正文第 1 段的標題就是「這棟建築照那張圖畫」。**通則：一條「不要照抄那張參考圖的某個元素」的禁令，要寫在「叫它照抄那張圖」的那一段裡面，不能只寫在 `AVOID`** ——`AVOID` 是最後一道網、不是放置條件的地方，**距離會決定哪一條贏**（同第十四節那條「正文權重比 AVOID 高」的空間版）。⚠⚠ 第二件：**風的線條要自己一節、而且要給量得出來的幾何**（一群六到十五道短直線密排、**每道不超過畫面寬的十分之一**、和雨同角度、**貼著東西走不要浮在空中**、**一道線沒有在解釋畫面裡已經發生的動作就不要畫**）——塞在雨那一段尾巴當從屬子句是不夠的。第六版逐字在第五之十節。✅ **2026-09-05 第六版過了**（使用者：「蠻好的　增加一點**窗框　屋簷　積水　滴水**的樣子」，第 **25-20** 節）——**這是第一輪「純粹加東西」的修改**（前六版每一輪都在治一個錯）。⚠⚠⚠ **所以每一段的口氣要再檢查一次**：第六版第 5 段寫著「風的線條是這一輪唯一要改的，把那四五條白弧線全部刪掉」，那句在第七版就變成「把已經畫對的風再重畫一次」的指令 —— 兩處都改成「上一版已經對了，這一段只是讓你核對」。加的四件：窗框（上下緣細亮水線＋往下散開的深色濕痕）／屋簷（**分開的**水滴、**被風吹斜和雨同角度**，不是水簾）／**滴水的落點**（同心小圈＋往上彈的濺點 —— **沒有那些圈，掛在屋簷上的短線只是裝飾**）／積水（低階旁一灘較深較靜的、倒映鐵門與一抹黃雨衣）；順手一件免費的：**騎樓地板留乾、只濕外緣一條**，等於再替「騎樓是退進去的」作證。⚠⚠⚠ **這一輪特有的風險：加濕痕很容易變成「這棟樓很髒」** —— 牆上往下流的深色痕跡正是年久失修最典型的畫法，而這一站有「像鬼屋欸」的前科。**通則：在一張「必須看起來保養得很好」的建築上加天氣的痕跡，一定要先擋髒** ——明寫「這些水是乾淨的雨水，深色只是濕，乾了還是同一個乾淨的灰」，`AVOID` 補鏽色／橘／綠／黑的流痕、青苔、髒污、舊水漬，再補一條縮圖的（濕痕是細而安靜的質感，不可以讓牆變成條紋、不可以搶過那件黃雨衣）。第七版逐字在第五之十二節。⚠⚠⚠ **同一輪差點漏掉第三件：舊版的病名要逐條清掉** —— 第六版第一次組出來時抬頭還留著**第四版**那句「唯一要改的兩件：雨反過來、樹反過來」，而那兩件第五版都已經對了，照送會把對的再翻一次。**通則：提示詞是一版一版疊出來的，裡面有一半的句子是針對某一版的病寫的（「上一版畫反了，鏡射過來」），那些句子在下一輪就變成指令** ——症狀是「上一版對的東西這一版又壞了」，**看起來像模型不穩、其實是提示詞在叫它翻**。做法：每一段的口氣換成「上一版已經對了，這一段只是讓你核對」，定稿前 grep 一次上一版的病名。⚠ 這次差點漏掉的成因是改提示詞的腳本用 `String.replace` **沒有 assert**、對不上就靜靜地沒換（同第九節第 24、25 條）——**每一處替換都要 assert 命中**。⚠⚠⚠ **2026-09-05 第七版：水有了，但屋簷的水滴像冰柱**（使用者：「積水　滴水有了　不過診所屋簷上的積水有點太整齊　不仔細看還有點像冰柱」，第 **25-21** 節）——其餘全部正確，錯的只有那一排水滴的**形狀**：**等距、等長、垂直、每一根都黏在屋簷上、往下收成尖端** ＝ 冰柱的幾何，而水滴**每一件都相反**（下重上細／多數已脫離邊緣／間距與大小全不一樣／被風吹斜）。⚠⚠ **提示詞其實已經寫對三件**（分開、不要連成一片、要被吹斜），只差「哪一端重」，而它用的字是 `short tapered strokes` —— **`tapered` 本身就是「往下收成尖端」，等於在叫它畫冰柱**。**通則：同一個輪廓家族要靠「哪一端重」分辨，不是靠長度、數量或密度**（水滴／冰柱／鐘乳石／火焰／葉子外框都是一頭寬一頭尖，差別只在寬的那一端在上面還是下面）；**第二半：一排等距等長的東西一定會被讀成人造物或結晶** —— 天氣畫出來的東西沒有一項是規律的，判準是**看得出節奏就是錯的**；**第三半：多數水滴要已經脫離邊緣**，全部黏著就一定像冰柱。⚠ 第八版**什麼都不加也不拿掉**，所以第 6 段開頭那句「the building is not shedding any of it」非清掉不可（＝那條「上一版的病名會變成指令」的通則**第三次現場**，一樣是靠 grep 擋下來的），第 6 段改成**先逐項列出「保留、不要重畫」的六樣水**再講那一件要改的。第八版逐字在第五之十四節，通則進 ILLUSTRATION.md 第十五節第 13 條。⚠⚠⚠ **2026-09-05 第八版：冰柱治好了，但水滴又大又滿全圖**（使用者：「我必須說這樣沒有比較好　你們好好檢查一下　怎麼回事　或是讓積水和滴水在診所右側屋簷　和一兩個窗框就好　每個都有很奇怪」，第 **25-22** 節）—— 那四條規則只照做兩條（下重上細 ✅／多數脫離 ✅／間距大小不一 ❌／被吹斜 ❌），**而真正的問題是那四條根本沒寫到的兩件**：⚠⚠⚠ **尺寸**（第八版沒有給任何錨點，只有形容詞 `small`，畫出來每顆和人的眼睛一樣大）、⚠⚠⚠ **數量與位置**（它寫的是「…**以及每一個窗盒的下唇**」＝**一個類別不是一個地點**，於是掛滿兩棟樓、每一層、每一扇窗，連鄰棟都有）。**通則：天氣的細節要指名地點，不要寫成「凡是某一類邊緣都要如何」** —— 那是第十四節「規則不要綁在數量上」的**空間版**（綁在「兩塊」射程太窄、綁在「每一塊」射程太寬，兩種都要點名）；**第二半：形狀對了不代表畫對了** —— 一個細節有**形狀／尺寸／數量與位置**三個獨立的量（同第九節第 17 條那條「淡出有三個獨立的量」），**每加一種細節三個都要給量得出來的錨點**；**第三件：平滑對稱的淚滴輪廓＋裡面一點白色高光 ＝ 水滴的「圖示」不是水**。⚠ 第九版**只做減法**（使用者自己給的解法：只留右側那一段屋簷 ＋ 一兩個窗框），留下來那幾顆縮到**和一道雨差不多細**；路面的積水、倒影、雨圈全部保留。⚠ 抬頭與第 6 段的口氣又要整個換掉（＝「上一版的病名會變成指令」那條通則的**第四次現場**）。第九版逐字在第五之十六節，通則進 ILLUSTRATION.md 第十五節第 14 條。✅⭐ **2026-09-05 第九版定稿並接上**（使用者：「定稿了　做成圖卡」，第 **25-23** 節）——水滴收乾淨了（屋簷只剩右側那一段與幾扇窗有零星幾顆），**構圖、建築、鐵門、光、雨、風、樹、盆栽、三個人一個像素都沒有再動過**。成品 **`preview/line-typhoon/hero-typhoon.jpg`（1024×512）**，裁圖 `node drafts/channels/typhoon-hero-crop.mjs`（**上 40 / 下 40**，兩端都有餘裕所以不偏心），**提案頁兩處吃同一個檔**：聊天室貼的那張（232×116）與 Flex 圖卡的頭圖（268×134）。⚠⚠⚠ **守門第三次不能照抄** —— remind／review 那兩支的地標是**深墨**，而這一張是暴風天、**768 列每一列都有 40 個以上的 <70 像素**，那道量測回的是 `0/767`、完全沒有資訊。**通則（第三次記）：地標要挑「這一張圖才有、而且一定存在」的東西** ——這一張挑**黃雨衣**（提示詞明文寫它是全圖最亮的一塊，也是縮圖上眼睛第一個找到的），原檔第 431~678 列、守門要求成品上下各留 ≥12 列（實測 289/37 ✓）；白框那一道也從「對紙底」改成**絕對亮度**（起伏 <6 且中位 >200）。⚠ 量到五個寬度逐格相同、Flex 圖卡 268×447.8、溢出 0、JS 0，**和佔位框那一版一模一樣**（`aspect-ratio: 2/1` 預留生效）。⚠ **量測的網址參數是 `?v=text|img|card` ＋ `?s=stop|open|after`** —— 第一次寫錯參數名，三種送法量出一模一樣的數字**而且不報錯**，只有「一張圖都沒有」露餡（同第九節第 24、25 條）。⚠⚠ **這一頁唯一還開著的一題沒有變**：**要不要為了有圖，每個人多貼一次**（30 人的名單 ＝ 60 次操作）—— 圖畫好了，所以那現在是純粹的取捨、不再是「還沒有圖」 |

### 十一之二、⚠⚠ LINE Flex Message 的硬條件（不知道會做出對不上的東西）

- **字級是固定 px，不是比例**：`xxs 11／xs 13／sm 14／md 16／lg 19／xl 22／xxl 27`。
  ⚠⚠ 規格頁**一定要用這些 px 畫**。第一版寫成 rem 的比例，每一行都比 LINE 實際的
  小 1~2.7px，使用者一眼看出「灰底裡的字好小」。**規格頁一旦和真實算繪對不上，
  上面做的每一個判斷都是假的** —— `check-welcome.mjs` 就是為這件事寫的。
- **`button` 只有一個 `label`，不支援圖示，也不會換行**（放不下從尾巴截斷）。
  要「圖示＋文字」就做成**可點的 `box`**（box 掛 `action`，裡面 `image` ＋ `text`），
  代價是失去按鈕內建的按壓效果。
- **`image` 的 `size` 給的是「寬度」**，不是高度。兩個長寬比不同的圖不能寫同一個數字。
- ⚠⚠ **`box` 沒有「背景圖」** —— `background` 只認一種型別 `linearGradient`（漸層），
  **沒有 `backgroundImage`**。要浮水印只能疊一個真的 `image` 元件（見第 21-12 節）。
- ⚠⚠ **`image` 沒有 `opacity`** —— 要淡就**把 alpha 烘進 PNG**。
- ⚠ **一個 box 的第一個子元件不可以是 `position: absolute`**。
  ⚠⚠ 而「後面的元件會不會蓋在前面的上面」**文件沒有明講、我們也沒有實機驗過**。
- ⚠⚠ **`image` 的圖檔上限是 1024×1024 px**（HTTPS 的網址、JPEG 或 PNG）。
  所以頭圖一律出 **1024×512（2:1）**，不要出 1040×520 ——
  招呼圖卡那張就是 1040，**要回去重切**（見 `drafts/channels/README.md` 第 19-10 節）。
- **`aspectRatio` 寫錯不報錯也不變形** —— `aspectMode: fit` 只會讓圖**靜靜地縮小、
  四周留白**。`check-welcome.mjs` 會拿它去對 PNG 檔頭的真實尺寸。
- **斷行一律用 `\n` 自己指定**，不要交給自動換行（會把詞劈開、或讓末行只剩一個字）。
  ⚠ `span` 裡的 `\n` **還沒在實機驗過**。
- 量到的尺寸：卡片 **268px** 寬、內距 14px → 按鈕可用 **216px**。

### 十一之三、⚠⚠⚠ 事實查核的紅線

**這個帳號沒有專人即時回覆訊息**（自動回應與「綁定成功」那兩則自己就寫著）。
所以任何訊息上**不可以出現「有問題隨時問」那一類的承諾** —— 那是說謊，
也違反 COPY.md 第三節「診所給的是燈，不是答案」。
`check-welcome.mjs` 有一張掃描表擋著（隨時問／問到／即時回／找得到人…）。

### 十一之四、已經定案的（不要重做）

- **招呼圖卡的文案**：使用者**自己寫的**，逐字定稿，見 `welcome-card.json` 的 `_說明`。
  照站上慣例補的只有兩處（「官方 LINE」中間的空格縮成 9px、「喔～」用全形波浪）。
- **兩顆按鈕的 logo 是兩個不同的形狀**：綁定＝站上頁首那一條（2.029）、
  介紹＝`brand/shapes/shape-r2c3`（3.081）。**兩顆都 34px 寬**。
  ⚠⚠ 介紹那顆的墨只有綁定那顆的 **64%**（等重的是 42px），**那是使用者看過四格
  之後選的取捨，不要拿「兩顆一樣重」去訂正它**（同兒牙填色白字 3.22 那一類）。
- **撥打電話那顆按鈕沒有**（使用者三次整理的清單裡都沒有它）。電話在圖文選單的
  「診所資訊」與帳號主頁上。**不要自己加回去。**
  ⚠⚠ **2026-09-04 這一條擴大到整條線**：使用者看到「綁定完成」那兩張圖卡上的電話按鈕
  說「**診所電話在這裡不要做成按鈕，因為那只是一個說明，做成按鈕的用意是要讓人點下去**」，
  兩顆都拿掉了、號碼回到說明句裡自己一行。**通則：按鈕是「現在請你做這件事」，
  說明句裡的資訊不要穿按鈕的衣服。**
  ⚠ 代價：**純文字訊息 LINE 會自動把號碼變成可撥的，Flex 圖卡裡的文字不會** ——
  要它撥得下去就在那一行掛 `tel:+88655339369` 的 action（外觀完全不變）。
  **要不要掛還沒問過使用者。**
- **頭圖**：對話框、尾巴（.78π／根 62／長 70／指著一樓飾邊）、Zen Maru Gothic 900、
  驚嘆號（頭 17／角度 18°／高 78／點 15／往下 42／離「厚」的墨 14px）。
  推導在 `drafts/line-hello/README.md`。

### 十一之五、規格頁的規矩（和第八節**不一樣**）

`preview/line-*/` 定案之後**不刪頁、不搬進 `history/`** —— 第八節那條規則是給
「站上版型的提案」用的，這幾頁的成品在 LINE 不在站上，而且使用者要拿它們
**整包給廠商看**。定案時做的是：**把切換條拿掉、改寫成規格頁**。
三道 noindex 都還在（頁面 meta、Worker 的 `X-Robots-Tag`、`robots.txt`）。

⚠ 其餘規矩照舊：提案頁只放 fangren.net（不要用 claude.ai 的 artifact）、
**要擺真的產出檔不要用 CSS 再畫一次**、切換條要放在頁尾那支腳本前面。

### 十一之六、改完要跑的守門

```bash
node drafts/channels/check-welcome.mjs   # 招呼圖卡：Flex 的 JSON ↔ 規格頁（文字／字級／按鈕／logo 的檔案・長寬比・寬度／紅線）
node drafts/channels/check-bind-done.mjs # 綁定完成：同上（六段文字逐字＋加粗／字級／顏色／兩塊淡底的合成色・圓角・上外距／頭圖／紅線／切換條已拿掉）
node drafts/channels/check-remind.mjs    # 提醒卡：同上（九段文字＋姓名加粗／字級／顏色／五張圖的檔案・上限・長寬比・寬度／兩組 17.0-5.0 的墨距／紅線／切換條已拿掉）
node drafts/channels/check-bind-prompt.mjs # 還沒綁定就按約診查詢（**提案中，還沒有 JSON**）：
                                          #   ⚠⚠ 資料夾裡只准有 index.html ＋ 它自己拍的那兩張
                                          #     （shot-bind-prompt{,-old}.png；那四張引用的圖仍然一律用別人那一份）——
                                          #     擋的是「複製別人的圖進來」，清單寫死，多一個檔照樣 throw／頭圖 1024×512／
                                          #   **按鈕的字逐字 ＝ welcome-card.json 那一顆**／電話是現行寫法／紅線／
                                          #   ⚠⚠ **預設那一張只准有標題與按鈕**（2026-09-10「其他文字拿掉」）、
                                          #     不准再出現 pv-bar（切換條）、var state 的預設真的是 c/on/off ——
                                          #     三道擋的都是「加回去不會讓任何一道尺寸守門翻臉」／
                                          #   **卡上不准出現秒數，也不准出現 48 小時或 2 天**（那兩種講法還沒統一）／
                                          #   八個寬度 × 三案 × 兩頭圖 × 三電話：溢出 0、**卡片真的畫成 268**／
                                          #   綁定完成那張 PNG 的 width/height 對得上實檔**而且畫出來真的是那個大小**
node drafts/channels/check-cancel.mjs    # 取消／改期（三則）：同上（五段文字逐字／姓名與日期都是變數／保留那一則和確認卡同一組／字級 9 處／顏色 4 處／兩顆 logo 的檔案・長寬比・寬度／兩顆按鈕的順序／紅線／切換條已拿掉）
node drafts/channels/check-auto-reply.mjs # 自動回應：**純文字不是 Flex** —— auto-reply.txt ↔ 規格頁兩處（三邊逐字）／逐行 ≤14 全形字／改約那段＝綁定完成／電話／紅線與 emoji／切換條已拿掉、複製鈕在／泡泡真的給得到 268／孤字 0、溢出 0
node drafts/channels/check-review.mjs    # 評價邀約：同上（六段文字逐字＋兩顆按鈕的 action.label／姓名是變數且加粗／字級 6 處／顏色 6 處／頭圖與三張小圖的檔案・長寬比・寬度・≤1024／紅線與 emoji／**不可以拿好處換評價**／切換條已拿掉）
node drafts/channels/check-typhoon.mjs   # 颱風／臨時休診：同上（三段文字**三邊逐字** —— JSON ↔「要貼進去的字」↔ 畫在卡片上（填示範值後）／四個可填欄位且 {{date}} 兩次／字級 sm 14px／卡底與內文色／段距 8px／頭圖是 **JPEG**（掃 SOF）・2:1・≤1024・**卡片與聊天室同一個檔**／紅線與 emoji／切換條已拿掉）
node drafts/channels/check-booked.mjs    # 約診卡（兩則）：同上（文字逐字／字級 19-16-13／顏色／浮水印九顆的寬度・長寬比・色逐筆對 wm-sizes.json／27 張 PNG 都在且 ≤1024／日期一定要 wrap 不可以有 maxLines／紅線／切換條已拿掉）
node drafts/channels/check-spec.mjs      # **整包給廠商的那一頁**：16 張圖的 width/height ＝ 實檔 ÷ 它自己的倍率
                                          #   （那八則自己拍的是 3×、shot-oa-* 是 1× —— **倍率不是常數**）、每一張都要有 alt、
                                          #   ⚠⚠ **捲軸裡那五張要各自寫 inline width:NNNpx**（＝它的 width 屬性），
                                          #      而且樣式表不准出現 .pv-scroll img{width:auto} —— 那會讓圖退回原始像素尺寸
                                          #   八則的「完整規格」連結與錨點都通、紅線、切換條與 <script> 都不准出現
                                          #   ⚠ 圖檔清單那一道 2026-09-07 拿掉了（表不在頁上了）→ publish-assets.mjs --check 在做同一件事
                                          # ⚠⚠ 2026-09-07 起 check-handover.mjs 併進來了，所以這一支還多四道：
                                          #   ① **重跑 build-spec.mjs、逐位比對**（那一頁整份是產生的，這是最強的一道）
                                          #   ③ 頁上每一個 fangren.net 圖檔 repo 裡都要有
                                          #   ⑥ 抬頭那四行（漏了 charset 就是整頁亂碼，而算繪測驗抓不到）
                                          #   ⑧ 衛教那幾十張都上線了、而且 ≤12 格（順帶跑 publish-handouts.mjs --check）
node drafts/channels/check-post-hours.mjs # **商家貼文的看診時間圖**：24 張圖的尺寸、色碼逐格對 colors.json、
                                          #   「詳情」那段字逐字、沒有孤兒圖、noindex、零 JS、紅線、八個寬度溢出 0
node drafts/channels/check-post-docs.mjs  # **商家貼文的第三格**：4 張圖的尺寸與 alt、
                                          #   ⚠ 四個檔名逐一比對（fangren-docs-1080／slot-410／profile-3up／tri-<直徑>-*）——
                                          #     要貼的是第一張，名字寫錯使用者會貼到模擬圖上去、
                                          #   ⚠⚠ **浮水印的幾何一定要 import wm-triptych.mjs 的 wmFor("docs")**，
                                          #     而且 svg.wm 裡不准出現 right:／bottom:（自己寫一份的話三張就接不成一顆圓，
                                          #     而每一道尺寸守門都會過 —— 只有把三張拼起來看才看得出來）、
                                          #   ⚠⚠ **定稿那兩個值真的寫回常數了**（K0 1.15／MPAD0 13.5）、
                                          #   「詳情」逐字 ＝ detail.txt、案名 ＝ 產生器的 CASES、
                                          #   科別名與醫師姓名逐字 ＝ index.html 而且產生器裡沒有寫死、
                                          #   ⚠⚠ **「每一列印誰」那張表逐格 ＝ 拿 index.html 現算**（本科 或 專長命中）、
                                          #     而且**產生器裡真的寫著那條規則**（只驗表的話，圖可能還是舊的分法）、
                                          #   ⚠⚠ **產生器不准再出現 class="hd"／"rule"／"ft"，detail 以外不准出現 fangren.net**
                                          #     （標題與網址是使用者指定拿掉的 —— 加回去不會讓任何一道尺寸守門翻臉）、
                                          #   ⚠⚠ **小塊不准再套 TONE、醫師不准再出現 .xd**（同上，加回去不會被尺寸守門擋）、
                                          #   沒有孤兒圖、noindex、零 JS、紅線、八個寬度溢出 0
node drafts/channels/check-post-map.mjs   # **商家貼文的位置與停車圖**（一支管兩個版本）：17 張圖的尺寸
                                          #   （成品一律 1080 見方）、**兩塊「詳情」各自逐字 ＝ 它的 .txt**、
                                          #   門口那一版的四點提醒逐字 ＝ drafts/door-notice/body.html、
                                          #   三條網址逐字 ＝ index.html 的 .rl-link、號碼牌順序是 P1 P2 P3
                                          #   而且**刻意不是照距離**、站上那一版的三個停車場逐字 ＝ index.html、
                                          #   ⚠⚠ **QR 那張表逐格 ＝ door-qr.json，而且真的再掃一次去對**
                                          #     （沒有 opencv 就大聲印出來，不會靜靜地放行）、
                                          #   沒有孤兒圖、noindex、零 JS、紅線、八個寬度溢出 0
node drafts/channels/check-richmenu.mjs  # **圖文選單**：頁上那張表逐格 ＝ drafts/line-richmenu-numbers.json、
                                          #   <img> 的 width/height ＝ 實檔檔頭，**而且量 rect**（屬性寫對 ≠ 畫出來是那個大小）、
                                          #   模擬圖一律畫成 375px ＝ 手機上真正的大小、
                                          #   richmenu-*.jpg 逐項對 LINE 的硬條件（尺寸／比例 ≥1.45／≤1MB）、
                                          #   沒有孤兒檔、noindex、零 JS、紅線、八個寬度溢出 0
node drafts/channels/check-post-fb.mjs   # **臉書版那三張貼文圖**：13 張圖的長寬比與 alt、
                                          #   三段文字逐字 ＝ 那一則自己的 .txt（沒有重打）、
                                          #   三張成品 1080 見方且 ≤8MB、沒有孤兒檔、noindex、零 JS、
                                          #   ⚠ 紅線含臉書那條「留言」（說明那一段包在 data-red 裡、掃之前剝掉，
                                          #     而且要驗那個標記還在、只有一段）、
                                          #   ⚠⚠ **門診表與科別醫師那兩顆浮水印的值一個都不可以改**（那是他挑的）、
                                          #     地圖那顆只驗「不可以和另外兩顆一樣」（他要的正是不一樣）、
                                          #     **科別醫師那顆不准再被轉**、FB.docs 一定要宣告 hole: "cut"、
                                          #   ⚠⚠⚠ **最重要的三道**：產生器裡真的寫著「跑完用預設值再跑一次」，
                                          #     以及**真的去解那六張 PNG** ——
                                          #     ⓐ 同一格的 LINE 版與臉書版**不可以逐位元組相同**（相同 ＝ 還原沒有跑，
                                          #       這一道最強、不需要容差）
                                          #     ⓑ 那一層淡墨的**重心**要落在各自的幾何算出來的位置（±100px）
                                          #       ⚠ 不可以比對某一個色階的精確值（4% 與 5% 只差一階，進位不一定一樣）
                                          #     少了還原那一步，repo 裡的 LINE 成品會停在臉書版的浮水印上，
                                          #     三張接不成一顆圓**而且每一張看起來都很正常**
node drafts/channels/check-card-spec.mjs # **只留定案與現況對照的那一頁**：重跑逐位比對／
                                          #   ⚠⚠ ⑪ 這一頁只有「定案」與「現況對照」——十二則的狀態表、綁定完成那兩個
                                          #     方向、還沒有答案的題目、抬頭那塊未定案，一件都不可以印回來
                                          #     （接回去不會讓任何一道版面守門翻臉）／
                                          #   ⚠⚠ ⑩ vendor-log.json 裡一個寫死的「第 N 節」都不准有 ——
                                          #     同一段字印在兩頁上、節號不一樣，而**光看數字抓不到**
                                          #     （新頁自己也有第 3 節、第 4 節）／
                                          #   ⚠ ⑧ 每一張圖都要以 ../ 開頭引用隔壁資料夾那一份，
                                          #     這個資料夾裡只准有 index.html（複製一份 ＝ 第二個副本）／
                                          #   ② 不可以抄訊息的文字（例外：foreignObject 與 .cb 那兩種卡上真的畫出來的字，
                                          #     兩種都要真的存在，不然例外就變成洞）／⑬ 定案齊（浮水印、約診狀態、表九列、
                                          #     兩則的對照與九顆上卡）／noindex／零 JS／紅線／八個寬度溢出 0
node drafts/channels/check-vendor.mjs    # **廠商對接**（2026-09-13 起是給廠商看的那一頁）：重跑逐位比對／
                                          #   ⚠⚠ ⑰ 卡上那幾行**從 booked-card.json 讀出來**、逐行比對（單張 4 行、
                                          #     輪播 2 行 —— 少畫幾行，那張照四行量出來的卡底下就會空一大塊）；
                                          #     ⚠⚠ 2026-09-13 定案「2026/09/11 星期五 11:50」（字一個都不換，只換順序）：
                                          #     不准有換行、不准 white-space:pre、**現場量畫出來幾列與每一列是哪幾個字** ——
                                          #     單張卡（可用 240）只准一列；輪播卡（177）放不下、折成兩列是對的，
                                          #     但折點要落在時間前面（星期幾不可以被拆開，列數一樣是 2）；
                                          #     **`.cb` 的拉丁校正字型（Arial）不准拿掉** —— 這台沒有 Noto Sans TC，
                                          #     拿掉之後折點會跑掉（折在「星期／五」中間）而畫面完全正常，
                                          #     而且**卡高要等於那四行撐出來的高度 ±1.5px**（對不上 ＝ 底下又空一塊）；
                                          #     `日期.廠商` 與 `日期.例` 不可以相同（唯一出處 `日期`，
                                          #     產生器裡不准再寫死一個）、第 3 節那幾張與第 4 節那四張都畫得出來、
                                          #     第 3 節要用 foreignObject 讓瀏覽器排字（改回 <text> 就要自己估字寬，
                                          #     而估寬會跟著字型跑）、第 4 節四張的藥丸顏色與浮水印（淡墨＋溢出）／
                                          #   ⚠⚠ ⑯ 第 1 節「待調整」那一組照抬頭那四件排（每一列的 `未定案` 宣告
                                          #     自己屬於哪一件、已完成那一組不准宣告、卡片上要印得出「未定案 N」）——
                                          #     **讀的是頁面上實際印出來的順序，不是資料**；圈號 ①~⑫ 一個都不換／
                                          #   ⚠⚠ ⑮ 抬頭那一塊「未定案」四件的順序與子項數目（5＋4）、
                                          #     收掉的那一件沒有印回來而且「待答.已決」還留著四題、
                                          #     第 3 節九顆**都**畫在卡片上（不是只有廠商用過的兩顆）、
                                          #     那一排畫的是定案的淡墨 #5c5f57 而不是各科的原色、
                                          #     資料裡的題號還是 {{題:關鍵字}} 記號（產生器現算，
                                          #     寫死的話待答收掉一題就會靜靜地指到別題去）／
                                          #   ⚠⚠ ⑭ 那六塊「我們自己怎麼推出來的」不可以印回頁面上
                                          #     （往返／回覆逐句／別家帳號／綁定．三種現象／④ 的過程／
                                          #      待答的後台・診所・素材）——接回去不會讓任何一道版面守門翻臉；
                                          #     **而且 JSON 裡那六塊不可以少**（不印是一回事，刪掉是另一回事）／
                                          #   ⑧ 每張圖找得到・width/height ＝ 實檔・
                                          #     ⚠ 孤兒圖掃的是**這個資料夾裡的每一個 .jpg**（縮圖與附圖混在一起）／
                                          #   ⑨ 不准出現判語（真不真／有反例／證據力…）、class="tag…"、⚠／
                                          #   ⑫ ⚠⚠ **頁面上不可以出現完整的手機號碼**（09 加八位數）——
                                          #      綁定那一刻的圖裡本來就有一組，那是在出圖時遮掉的；
                                          #      這一道擋的是「有人把它打進 JSON」／
                                          #   ⚠⚠ **頁面上不可以出現任何一則訊息的文字**（從六份 Flex JSON ＋
                                          #      auto-reply.txt 現抽 ≥10 字的長句去掃，不是寫一份禁語表）——
                                          #      ⚠ 唯一的例外是那十三張卡上的字（從 booked-card.json 讀出來的），
                                          #        掃之前把 foreignObject 剝掉，而且**剝掉的那幾塊要真的存在**，
                                          #        不然把整段拿掉就等於把這一道關掉／
                                          #   noindex／零 JS／紅線／內部連結都通／八個寬度溢出 0
node drafts/channels/check-single-card.mjs # **約診卡・單一版型（帶子壓在下緣）**：① 重跑產生器逐位比對（最強的一道）／
                                          #   ② 卡上那幾行逐字 ＝ booked-card.json（姓名與日期填 vendor-log.json）／
                                          #   ③ 27 顆那一條 ＝ band/fangren-band-27.svg（產生器每次跑也比一次）／
                                          #   ④ 資料夾只准有 index.html ＋ 那五張帶子・宣告的尺寸 ＝ 實檔・都 ≤1024／
                                          #   ⑤ 零 JS・noindex・沒有 undefined／⑥ 紅線／
                                          #   ⚠⚠ ⑦ 量**畫出來的東西**：三個寬度（430／390／320）× 12 張卡 ——
                                          #     卡片真的畫成 268／162（content-box，屬性寫對 ≠ 畫出來是那個大小）、
                                          #     帶子真的滿版而且貼著卡片下緣、**輪播那張卡的日期兩列且星期幾沒被拆開**、水平溢出 0
node drafts/channels/check-stand-card.mjs # **櫃檯的小立牌**（文字提案）：重跑逐位比對／
                                          #   ⚠⚠⚠ 2026-09-16 起紅線那一道**翻面**：亮紅的案要在資料裡寫明
                                          #     例外.紅線、那句話要印在頁上，而且**至少要有一案是乾淨的**
                                          #     （Ⓔ 是使用者逐字寫的、照寫不改；沒有那個下限，一張「大家都有
                                          #     例外」的表會把這一道靜靜地關掉）。時間的數字同一套／
                                          #   ⚠⚠⚠ 標誌是**官方授權的商標**：CSS 不准出現 filter／border／
                                          #     background／clip-path／mask／opacity／mix-blend，要有 width:auto，
                                          #     長寬比要從 PNG 檔頭現讀（寫死的話換一個檔，抬頭會靜靜地溢出）／
                                          #   ⚠⚠ 帶子那九顆：順序照 JSON、形狀讀 brand/shapes、寬度讀
                                          #     wm-sizes.json、**垂直置中**（等墨不等高），而且相鄰的兩條限制
                                          #     （最長的不相鄰、同色不相鄰）也要守 —— 排錯了畫面完全正常／
                                          #   ⚠ strip() 要先把 <img> 還原成它的 alt 再剝標籤／
                                          #   每一行逐字印在頁上（漏印一行不會讓版面守門翻臉）／
                                          #   ⚠⚠⚠ **紅線掃描要真的會亮** —— 現況那一張是正向對照，
                                          #     一定要掃得出東西；四案一定是 0。只驗「四案是 0」的話，
                                          #     一張永遠匹配不到的掃描表也會過／
                                          #   四案不准印時間的數字（兩種講法還沒統一，而印刷品改不了）、
                                          #     不准帶廠商的品牌、抬頭一定是診所名／
                                          #   ⚠⚠ 抬頭逐字 ＝ 使用者指定的「芳仁牙醫有 LINE 囉」、四案不准再有副標、
                                          #     現況那一張不可以被套綠泡泡（它是對照）、
                                          #     `--hd-ls`／`--li-pad` 要和算式對得起來、抬頭是 nowrap ＋ 納入「被裁掉」的量測／
                                          #   ⚠⚠ 逐行比對要**先剝標籤**（抬頭裡的 LINE 包著一個 span，
                                          #     整條字串不會再原樣出現在 HTML 裡 —— 不剝等於這一道沒開）／
                                          #   ⚠⚠⚠ 一案可以在資料裡宣告「字級照另一案」（Ⓔ 釘在 Ⓕ 上）——
                                          #     被照的那一案要在、不可以接龍、釘上去之後最長那一行還要收得進版心
                                          #     （放不下的話產生器直接 throw：卡片 overflow:hidden，
                                          #      畫面上只會少掉最後幾個字，而每一道尺寸守門都會過），
                                          #     而且頁上要印出那一整句「釘在誰・規則會算幾 mm」——
                                          #     ⚠ 不可以只找那個數字：別案的字高剛好也是 5.88，掃整頁一定命中／
                                          #   ⚠ Ⓔ 那一行是「看診前2天」不是「約診前」，字級釘在 Ⓕ（兩件都是使用者指定的）／
                                          #   ⚠ 「第 N 題／條」不准寫死（要用 {{題:…}}／{{條:…}} 現算）／
                                          #   noindex、零 JS、這個資料夾裡只准有 index.html／
                                          #   ⚠⚠ 八個寬度：水平溢出 0、卡片比例對、**沒有一行被裁掉**、
                                          #     而且**卡片內距 ＝ 卡寬的 6%**（寫成百分比的話參照會變成父層那一欄，
                                          #     430 與 390 上量到 24.1 vs 21.7，而畫面完全正常）
                                          #   ⚠⚠⚠ 2026-09-16 第五輪起多兩把間距的尺（⑥之六 ＋ ⑧之二）：
                                          #     靜態 —— 切抬頭() 在且 mark() 吃它（切法和字級算式分家的話，
                                          #       抬頭會算出一個放得下、畫出去卻溢出的字級）／舊的留白算式
                                          #       （半形空白 ＋ 字距）不准再出現／留白em 不准再存在／
                                          #     ⚠⚠⚠ :root 的 --bd-gap 要是一個**長度** —— 寫成純數字的話
                                          #       margin-top 整條無效 ＝ 0，而畫面完全正常（踩過）；
                                          #       ⚠ 這一道要**指名 :root 那一塊**，尺上那八張卡的 inline style
                                          #       裡也寫著同一串字，掃整頁等於沒掃（第十一次）／
                                          #     兩把尺都要有現況那一格、預設值要在尺上、留白地板 ≥25%
                                          #       （低於規範的格子不要放進尺裡）、間距地板 ≥ 行距的 1.4 倍／
                                          #     每一格的數字要**整句**印在頁上（幾 mm、行距的幾倍、空出幾 mm）／
                                          #     量出來的 —— 尺上每一格的標誌左右留白 ＝ 宣告的比例（左右還要一樣寬）、
                                          #       抬頭到主文的框距、以及 QR 底下到帶子 ＝ 疊高() 算出來的（±0.4 mm，
                                          #       **那是唯一會告訴我整疊算錯的東西**）；
                                          #     ⚠⚠⚠ **案那幾張也要量一次** —— 尺上每一格都掛著 inline 覆寫，
                                          #       預設值只有它們在吃／抬頭那一行不准再畫出任何空白
                                          #   ⚠⚠⚠ 2026-09-16 第六輪：抬頭到主文定案（Ⓗ4）、尺收成一張表；
                                          #     標誌留白那把拉成五格並**跨過規範那條線**，所以那三道翻面：
                                          #     預設一定要 ≥25%（那條線要由使用者跨，不是我們）／跨線的每一格
                                          #     都要在資料裡宣告例外**而且那句話要印在頁上**／**至少要有一格
                                          #     守得住規範**（不然一張「大家都有例外」的表會把這一道靜靜地關掉）／
                                          #   ⚠⚠ 對照帶：五列都要畫、最鬆÷最緊 ≥2 倍（尺拉不開就等於沒有尺）、
                                          #     那幾行要用 8.5cqw 跟著容器放大、**那條基準虛線要真的落在
                                          #     第一列的標誌左緣（±2px，量出來的）**——
                                          #     算錯時畫面上只是「多一條虛線」，每一道尺寸守門都會過／
                                          #   ⚠ 基準線的 left 要用 cqw 不可以用 % —— % 吃 padding box、
                                          #     cqw 吃 content box，差一個左右內距（實測 12.6px）／
                                          #   ⚠ 「數帶子」要數頁面上真的有幾張 card sc，不可以照尺的格數算
                                          #   ⚠⚠⚠ 2026-09-16 第七輪：Ⓘ4 定案 ＝ 12%，**跨過規範那條線**，所以那三道再翻一次：
                                          #     預設**可以**低於規範，但只准在①那一格宣告了例外②那句話印在頁上
                                          #     ③**頁面上仍然講得出那條線在哪**（釘在「對規範（25%）」那一欄的抬頭上 ——
                                          #     不可以寫成「頁面上有沒有 25%」，別處順便提到也會命中）三件同時成立時／
                                          #     尺上仍然要有一格守得住規範／面板要在跨線時講一句（第 28 條 ④）／
                                          #   ⚠⚠ 對照帶收掉了（class="hdcmp"／"hdrow" 都不准再出現），五格的數字改在表上／
                                          #   ⚠⚠⚠ 拿掉 Ⓐ~Ⓓ 之後**沒有一案是不印數字的**，所以那一道也翻面：
                                          #     一案都不乾淨時，頁面上非得講得出「建議不印」與那兩種講法差在哪／
                                          #   ⚠⚠ 走掉的那幾案要留紀錄（刪案：走了哪幾案／git show …／為什麼），而且都要印在頁上；
                                          #     Ⓕ 不可以跟著走（Ⓔ 的字級釘在它身上、它是唯一乾淨的一案）／
                                          #   ⚠ 頁面上不准再寫死「四案」（案數現算）
                                          #   ⚠⚠⚠ 2026-09-16 第八輪：兩把間距的尺都定案（Ⓗ7 ＋ Ⓙ4），卡片收掉、只留兩張表 ——
                                          #     所以那一道從「算出來的格數」改成 **0 張 card sc**，
                                          #     ⚠⚠ 但「數帶子」仍然是從頁面上現場 match 的（Ⓗ 那把被打開過一次，
                                          #       這兩把還會再打開）；**一個「現在是幾張」的數字，即使當下是 0 也要現場數**／
                                          #   ⚠⚠ 「・定案」是算出來的（值 === 預設）：逐列比對那一行抬頭印得對不對，
                                          #     而且**標籤自己不准出現「定案」**（兩邊都寫會印成「完全不留（定案）・定案」，
                                          #     而每一道版面守門都會過）／兩張表仍然逐格比對每一格的 mm 與倍數
                                          #   ⚠⚠⚠ 2026-09-16 第九輪，底下那條帶子（五件，Ⓚ 顆數 ＋ Ⓛ 間距 ＋ 三個決定）：
                                          #     ⚠⚠⚠ **四邊與顆與顆之間那五個間隔要一模一樣**（逐個量出來對）——
                                          #       改動前三處三個出處（SVG 的 gap／版心對滿版剩的頁面內距／.band.logos 的 padding），
                                          #       現在整條由一張 SVG 畫完，那五個間隔出自同一個算式／
                                          #     ⚠⚠ .band.logos 不准再長出 padding 或底色（底色與四邊的留白都在 SVG 裡）、
                                          #       .bnd 一定要滿版 var(--cw)（不是版心）／
                                          #     ⚠⚠⚠ **牙洞不要另外填色**：整條只准有兩種 fill —— 底色那一塊 rect 與
                                          #       形狀自己的 currentColor；fill-rule evenodd 要在、標誌一律 #ffffff／
                                          #     ⚠ 底色要讀 wm-sizes.json 的 r1c1（一般牙科），不准在產生器裡再抄一份色碼；
                                          #       白壓在它上面 ≥4.5／
                                          #     ⚠⚠ **三個順序案每一個**都要守三條限制（同形狀不相鄰、三顆最長的不相鄰、
                                          #       同色不相鄰），重複的那幾顆要在「重複」那張清單裡 ——
                                          #       不是定案的那幾格照樣畫在頁面上，排錯了畫面完全正常／
                                          #     兩把尺的表逐格比對三個 mm，「・定案」一樣是算出來的／
                                          #     色案_落選 那段話要還在（那三格不是被比下去的，是前提換掉了）
                                          #   ⚠⚠⚠ 2026-09-16 第十輪（Ⓚ3／Ⓛ2 定案 ＋ 真的那顆 QR）：
                                          #     兩把尺的帶子條不准再畫回來（`bandrow` 產生器與頁面都不准出現）、
                                          #     **但定案那一格要留在案裡、落選那幾格不准刪**（表就是靠它們算出來的）／
                                          #     ⚠⚠ 案那幾張要擺 `qr-line.svg`、**現況那一張只准擺灰色佔位方塊**
                                          #       （它是照片的逐字對照，換上新設計的碼就不是對照了）／
                                          #     ⚠⚠⚠ `preview/line-stand/qr-line.svg` 要**逐位元組 ＝ drafts/channels/qr/ 那一份**
                                          #       （只搬不畫；產生器裡自己畫一顆 45×45 的碼也擋）、
                                          #       而且那顆碼印出來 ≥15 mm、面板要印它畫出來多大（第 28 條 ④）／
                                          #     ⚠ 寬度從八個收到三個（430／350／320）—— 其餘五個量出來逐格相同，
                                          #       卡片寬度的算式一改就要把中間那幾格加回來
                                          #   ⚠⚠ 2026-09-16 第十一輪（底板那把尺）：三格都要是**真的碼**、
                                          #     定案那一格就是 QRFILE 本人、落選兩格逐位 ＝ qr-brand.mjs 現算的、
                                          #     標籤自己不准寫「定案」、⚠ 那一排的底一定要白
                                          #     （落選兩格的靜區是透明的，擺在紙色上就不是它印在白卡上的樣子）
                                          #   ⚠⚠⚠ 2026-09-16 第十二輪（Ⓝ2 定案、Ⓔ 少一行、Ⓕ 拿掉、整頁收成一張卡）——
                                          #     上面那一段整組**翻面**：底板三格改成「落選那兩格**不再產檔**（產了就是孤兒）＋
                                          #     面板逐格印＋資料裡三格一格都不准刪」；白名單收回 index.html ＋ 標誌 ＋ QR 三個檔。
                                          #   ⚠⚠ 現況那一張與 Ⓕ **都不畫了**（資料都還在）：數卡片一律用「印的案」，
                                          #     而且逐項擋那六件接回頁面（拆解／待答／表格／現況那一張／底板三格／那條示範帶）——
                                          #     **接回去畫面完全正常、每一道版面守門都會過**；那幾節的**死 CSS 也要跟著收**。
                                          #   ⚠⚠ Ⓔ 的字級「釘在誰・規則會算幾 mm」改到**那一案自己的註**裡找 ——
                                          #     註是一張卡一張卡的，**要切出那一塊再找，掃整頁等於沒掃**（第十三次）。
                                          #   ⚠⚠ 五把尺（Ⓗ Ⓙ Ⓘ Ⓚ Ⓛ）逐格的 mm 改驗「產生器真的在逐格算 ＋ 面板真的印」，
                                          #     「對規範（25%）」那一欄改驗「面板在跨線時印得出那個百分比」—— 地板不可以跟著表消失。
                                          #   ⚠⚠⚠ 守門第 ① 道（重跑產生器逐位比對）要**接住 stderr、把那一句原句印出來** ——
                                          #     不接的話它會把整包輸出當成位元組陣列吐在畫面上，而那正是它最該說人話的時候。
node drafts/line-hello/check.mjs         # 規格頁引用的圖都在、沒有孤兒檔
```

產生器（改了對應的東西才要跑）：

```bash
node drafts/line-hello/generate.mjs      # 頭圖
node drafts/channels/mark-png.mjs        # 按鈕上那兩顆 logo
node drafts/channels/card-png.mjs        # 招呼圖卡的圖片檔（--chat 連聊天室的底）→ **preview/line-welcome/**（2026-09-05 從 drafts/ 搬過來，drafts 進不了 _site）
node drafts/channels/welcome-hero-resize.mjs # 招呼圖卡的頭圖 1040×520 → **1024×512** → preview/line-welcome/hero-welcome.jpg
                                          # ⚠ 守門的地標是「對話框那條深綠」不是亮度（底下是實景照，亮度取外框會回 0~1 ＝ 永遠會通過）
node drafts/channels/oa-shots.mjs        # 把 drafts/line-oa/ 那三組卡的模擬圖搬進 preview/line-spec/（--check 只驗不寫）
                                          # ⚠ 只搬不畫：圖本身由 drafts/line-oa/flex-preview.mjs 從那幾份 Flex JSON 產生
node drafts/channels/publish-assets.mjs  # 把七份 JSON 引用到的 21 個圖檔複製進 assets/line/（--check 只驗不寫）
                                          # ⚠ 清單的唯一出處是那七份 JSON，不要在腳本裡再手寫一份
node drafts/channels/bind-done-png.mjs   # 綁定完成那一則的圖片檔（五張）→ preview/line-bind-done/
node drafts/channels/remind-png.mjs      # 提醒卡的圖片檔（--old 現況／--chat 聊天室）→ preview/line-remind/
node drafts/channels/remind-a4.mjs        # **櫃檯要印的那張 A4**（提醒卡）→ drafts/channels/芳仁-約診提醒-A4.pdf
                                          #   ＋ remind-a4.png ／ remind-a4.html（自給自足、拿去別台電腦也印得出同一張）
                                          # ⚠⚠ 不重畫卡片：整張抓自 /preview/line-remind/，**那一頁改了就要重跑**
                                          # ⚠ 字型內嵌（只含這張紙用得到的那幾個字）—— 這台容器只有文泉驛
                                          # ⚠⚠ 2026-09-16 定案：**不放頭圖、不印姓名那一格**（開場只剩「哈囉」）
                                          #   —— 挑定的值已寫回預設；--hero／--name=〔病人姓名〕 是回去的路
                                          # ⚠⚠⚠ 頭像（綠圓＋白標誌）**在卡片上面不是左邊** —— 形狀讀 brand/shapes/mark.svg，
                                          #   牙洞靠 evenodd 透出底下的綠；規格頁那個模擬畫的是文字泡泡那一種，不要互相訂正
                                          # ⚠⚠⚠ **標誌佔圓寬 0.772、而且比圓心低 0.102 個直徑**（不是垂直置中）——
                                          #   位置與大小（30／6／3）量自**聊天室**截圖、圓裡面長什麼樣量自**帳號主頁**截圖
                                          #   （同一顆圓在那裡有 171px，大六倍才量得準）。**兩張各答各的，不要互相訂正**
                                          # ⚠⚠ 示範日期 2026-09-16 使用者指定 `2026/11/20 08:45`，**星期是照日期算的不要寫死**
node drafts/channels/review-png.mjs      # 評價邀約的圖片檔（同上三張）→ preview/line-review/
node drafts/channels/review-hero-crop.mjs # 評價邀約的頭圖 16:9 → 1024×512 → preview/line-review/hero-review.jpg
node drafts/channels/remind-marks.mjs    # 提醒卡那兩顆新標誌（翻過來的 shape-r2c3 ＋ 頁尾的話筒）→ preview/line-remind/
node drafts/channels/remind-hero-crop.mjs # 提醒卡的頭圖 16:9 → 1024×512 → preview/line-remind/hero-remind.jpg
node drafts/channels/typhoon-hero-crop.mjs # 颱風那一則的頭圖 16:9 → 1024×512 → preview/line-typhoon/hero-typhoon.jpg
                                          # ⚠ 守門的地標是「黃雨衣」不是深墨（整張圖都是暗的）
node drafts/channels/booked-mark.mjs      # 約診卡的浮水印標誌（九顆 × 三個濃度 ＝ 27 張）→ preview/line-booked/wm-<形狀>-{08,12,18}.png ＋ wm-sizes.json
node drafts/channels/booked-png.mjs       # 約診卡的圖片檔（五張：兩張卡、整條輪播、兩張聊天室模擬、九顆對照表）→ preview/line-booked/
                                          # ⚠ 這一支要自己起 HTTP 伺服器（那一頁 fetch wm-sizes.json，file:// 拿不到）
node drafts/channels/typhoon-png.mjs --all # 颱風那一則的圖片檔（三張：定案那張卡／純文字／在 LINE 裡）→ preview/line-typhoon/
node drafts/channels/cancel-png.mjs --all  # 取消那一段的圖片檔（五張：確認卡／按了取消／按了不取消／整段對話／現況）→ preview/line-cancel/
node drafts/channels/booked-num.mjs       # 約診卡輪播的順序號碼頭圖（12 張）→ preview/line-booked/num-01..12.png
node drafts/channels/auto-reply-png.mjs   # 自動回應的圖片檔（--chat 聊天室／--old 現況）→ preview/line-auto-reply/
node drafts/channels/bind-prompt-png.mjs  # 「還沒綁定就按約診查詢」那兩張卡 → preview/line-bind-prompt/
                                          #   shot-bind-prompt.png（我們新做的）／shot-bind-prompt-old.png（現況公版）
                                          # ⚠ 拍的是那一頁自己畫的卡，那一頁改了就要重跑；跑完接著跑 vendor-shots.mjs
node drafts/channels/vendor-shots.mjs      # 那一頁的縮圖（十則各自規格頁的產出檔 → 210px 見方的 JPEG，--check 只驗不寫）
                                          # ⚠ 只搬不畫：那幾頁重跑過出圖腳本之後，這一支也要重跑
node drafts/channels/build-card-spec.mjs  # **只留定案與現況對照的那一頁** → preview/line-card-spec/
                                          # ⚠⚠⚠ 不新寫任何東西 —— 卡片、浮水印、那張表、兩則的對照
                                          #   全部從 build-vendor.mjs import（再抄一份 ＝ 兩個真相，而且兩邊各自都很正常）
                                          # ⚠ 所以 build-vendor.mjs 的寫檔關在「直接執行」底下；改了那一支要兩頁都重跑
                                          # ⚠ 節號用 {{節:關鍵字}}，這一頁自己一份節表；關鍵字在這一頁沒有對應的節就 throw
node drafts/channels/build-vendor.mjs     # **廠商對接的現況與待答** → preview/line-vendor/
                                          # ⚠ 資料的唯一出處是 vendor-log.json，這一支只做排版
                                          # ⚠ 廠商回信、問到一題、發現新的一則自動訊息 —— 都改那份 JSON 再重跑
node drafts/channels/build-spec.mjs       # **給廠商的那一頁** → preview/line-spec/（模擬圖 ＋ 圖檔 ＋ 可複製的文字）
                                          # ⚠ 文字從各則的 JSON 現抽、一個字不重打；模擬圖是那幾頁的產出檔，尺寸現場讀檔頭
                                          # ⚠ 那幾頁重跑過出圖腳本之後，這一支也要重跑
node drafts/channels/publish-handouts.mjs # 衛教懶人包二十張 → assets/handout-*.jpg（--check 只驗不寫）
                                          # ⚠ 頭圖縮到 1024（Flex 上限）並把實際 W×H 寫回 JSON 的 aspectRatio
node drafts/line-oa/handout-crop.mjs [名字…]  # 紙本 → hero（1.91:1，連紙本那圈框一起留著）＋ 每一張的顏色 → handouts/colors.json
                                          # ⚠ 給名字就只重做那幾張、colors.json 合併寫回（不給＝整批，會把沒動的九張也重新編碼一次）
                                          # ⚠ 換過紙本之後接著要跑：health-carousel.mjs d → publish-handouts.mjs → flex-preview（含 cards/）→ oa-shots → build-spec
                                          # ⚠⚠ 加減卡片時 cards/ 那批預覽的編號會整組位移，十四張要全部重產（先 rm preview-*.png）
node drafts/channels/icon-svg.mjs         # 按鈕圖示的向量原檔 → assets/line-src/*.svg（--check 只比對）
node drafts/line-oa/flex-preview.mjs <json> --row  # 輪播改成「一列排開」（格狀版在手機上只看得到最左邊那一欄）
node drafts/line-auto-reply/rich.mjs     # 圖文訊息的圖
node drafts/channels/single-card.mjs      # **約診卡・單一版型** → preview/line-single-card/（--check 只比對）
                                          # ⚠ 卡上的字與兩則的卡片寬 import build-vendor.mjs、帶子的幾何 import stand-card.mjs
                                          # ⚠⚠ 27 顆那一條每次跑都拿 band/fangren-band-27.svg 逐位比對（拿來用，不是再畫一條）
                                          # ⚠⚠⚠ 出圖那一頁的選擇器要寫 svg.bnd —— 寫 svg 會吃到巢狀的那 27 個，最右邊那一顆會不見
node drafts/channels/stand-card.mjs      # **櫃檯的小立牌**（現況 ＋ 四案 → preview/line-stand/，--check 只比對）
                                          # ⚠ 文字的唯一出處是 stand-card.json，這一支只做排版 —— 要改字改那一份再重跑
                                          # ⚠⚠ 字級是一條規則算的不是手挑的：min(卡寬 6%, 版心 84% ÷ 最長那一行的字數)
                                          # ⚠⚠ 抬頭另算一條（要把字距與綠泡泡的左右內距算進去，不然會靜靜地溢出卡片）
                                          # ⚠ 零 JS；卡片裡的長度一律 calc(var(--cw) * k)，不可以用百分比
                                          # ⚠⚠ 底下那條帶子整條由一張 SVG 畫完（底色是裡面一塊 rect、四邊的留白烘進 viewBox）
                                          #   —— 形狀讀 brand/shapes/、寬度與底色讀 preview/line-booked/wm-sizes.json，
                                          #   ⚠⚠⚠ 標誌填白、**牙洞不要另外填**（fill-rule evenodd 挖穿，底下那塊綠自己透出來）
                                          # ⚠⚠⚠ 卡上那顆 QR **只搬不畫**：從 `drafts/channels/qr/fangren-line-qr.svg`
                                          #   原封不動複製成 `preview/line-stand/qr-line.svg`（`drafts/` 進不了 `_site`）；
                                          #   要改那顆碼就改 `qr-brand.mjs` 再重跑，不要改 preview 底下那一份
node drafts/channels/stand-illus-crop.mjs # **立牌上那條人物插圖**：原檔 → preview/line-stand/illus.jpg（--check 只比對）
                                          # ⚠⚠⚠ 裁的是**墨的框**不是整張 16:9 —— v1 的原檔上下留著大片白（那一版的提示詞要的），
                                          #   整張放進卡片那一條會爆框
                                          # ⚠⚠ 「那一條」＝ QR 底下到帶子 ＋ 一個帶子高（插圖沉進帶子後面），
                                          #   兩個數字都從 stand-card.mjs 現算 —— 這一支一個都不寫死
                                          #   （那一行 @fafa070 加上去之後它就變了）
                                          # ⚠⚠⚠ **只有上緣不准碰到邊**：下緣（下半身）與兩側（人擠進畫面）
                                          #   從 v2 起是規格；上緣同時是唯一的調節閥 ——
                                          #   **裁到墨之後若比那一條還寬，它會從頭頂上那塊白裡把高度借回來**，
                                          #   輸出的比例因此一定正好是那一條（模型畫成 2:1、上面留一條白就好）
                                          # ⚠ 原檔的路徑讀 stand-card.json 的 插圖.原檔，換一版改那一份就好
node drafts/channels/qr-brand.mjs        # **兩顆 QR code**（LINE 官方帳號 ＋ 診所網站）→ drafts/channels/qr/，--check 只比對
                                          # ⚠ 成品不在站上（同門口那張 A4），是要給印刷與廠商的向量檔
                                          # ⚠⚠⚠ 驗收**不可以用眼睛** —— `node drafts/channels/check-qr.mjs` 會真的解一次；
                                          #   容器裡沒有解碼器它會大聲說沒驗到、不會安靜放行
                                          #   （臨時裝，不是專案依賴：`pip install zxing-cpp opencv-python-headless`）
                                          # ⚠⚠ 底板鋪多大由 `PLATE_MODE` 決定（2026-09-16 定案 `'code'` ＝ 只鋪碼面、
                                          #   靜區留白 ＝ 周圍那圈淡綠色的邊不見了）；`'full'`／`'none'` 是落選的兩格。
                                          #   ⚠ 它同時決定印刷與廠商那兩顆成品，不只立牌那張卡
node drafts/channels/richmenu-bind.mjs   # **圖文選單**（四格候選 ＋ 在聊天室裡長什麼樣的模擬 ＋ numbers.json）
                                          # ⚠ 要上傳到 LINE 的是 richmenu-*.jpg；mock-*.png 只是模擬
                                          # ⚠ 文案改在那支的 COPY，改完重跑，數字會跟著寫進 numbers.json
node drafts/channels/post-triptych.mjs   # **三格拼起來那顆浮水印「要多大」的尺** —— 一次跑完下面三支
                                          # ⚠⚠⚠ 直徑那把尺**跨三支產生器**，三張要一起重跑才拼得起來，
                                          #     所以用環境變數傳（WM_DIA=380 …）；跑完會再用預設值跑一次，
                                          #     repo 裡留下的三張永遠是定案那一組
                                          # ⚠ 每個直徑出四張：tri-<直徑>-3up.png（排在一起）＋ -hours／-docs／-map.png
                                          #   （各自獨立 —— 獨立看才知道單張上那一塊弧讀起來像什麼）
                                          # ⚠ post-docs.mjs 清檔時跳過 tri-*.png
                                          # ⚠⚠ 直徑 2026-09-09 定案 500，那 12 張已經清掉 ——
                                          #   重跑過這一支就要再清一次，不然 check-post-docs 會擋
node drafts/channels/post-hours.mjs      # **商家貼文的看診時間圖**（24 張候選 ＋ 主頁三格的裁切模擬）
                                          # ⚠ 定稿那一張叫 fangren-hours-1080.png，其餘候選是 post-hours-…
                                          # ⚠ 排班改了要跑它（資料讀 index.html，不重打）
                                          # ⚠⚠ 浮水印吃 wm-triptych.mjs 的 wmFor("hours")，不要在這裡自己算位置
node drafts/channels/post-map-door.mjs   # **商家貼文的位置與停車圖・門口告示版**（八張候選 ＋ 主頁三格 ＋ 410px）
                                          # ⚠ 定案是 door-c（不放 QR ＋ 拿掉「紅線」「開單」那兩句）
                                          # ⚠⚠ dropPts() 不是照順序砍前兩條：砍掉的一定要真的含著那兩個詞
                                          # ⚠⚠ 它會先跑 drafts/door-notice/gen.mjs，再把那張 A4 告示的地圖、
                                          #     四點提醒、三張 QR 卡讀回來排進方畫布 —— 門口那張改了要重跑
                                          # ⚠⚠⚠ 要驗 QR 得裝 opencv：pip install opencv-python-headless
                                          #     （零依賴的專案，這是臨時裝的；沒裝就寫「未驗」）
                                          # ⚠⚠ 浮水印吃 wm-triptych.mjs 的 wmFor("map")，不要在這裡自己算位置 ——
                                          #     舊的 WMSH0／WMX0 那兩把尺留著但**已經不生效**（見 README 36-17）
node drafts/channels/post-docs.mjs       # **商家貼文的第三格（主題與科別・醫師）**（定稿一張 ＋ 主頁三格 ＋ 410px 實際大小）
                                          # ⚠ 定稿那一張叫 fangren-docs-1080.png，其餘兩張是模擬圖
                                          # ⚠ 資料全部從 index.html 的 #doctors／#topics／窄帶讀回來，一個字不重打
                                          # ⚠⚠ **每一列印誰照站上 specHit() 的規則算**（本科 或 專長命中）——
                                          #     兩套分法會岔開，而畫面上完全正常，見 README 36-13
                                          # ⚠ 顏色與形狀逐格比對 post-hours.mjs（兩張並排差一階就會被看出來）
                                          # ⚠ 「折行 N/7 科」是用印的不是用 throw 擋
                                          # ⚠⚠ 標題與頁尾 2026-09-09 拿掉了，**卡住放大的是寬度不是高度** ——
                                          #     「最寬的一列要多寬／可用多寬／還能放大幾倍」每次出圖都印，見 README 36-14
                                          # ⚠⚠ 空出來的是**高度**，只有列距那把尺買得到；字要多大是寬度在卡，見 36-15
                                          # ⚠⚠ 浮水印吃 wm-triptych.mjs 的 wmFor("docs")，不要在這裡自己算位置
                                          # ⚠⚠⚠ DISP（顯示名的縮寫）**要和 post-hours.mjs 逐字相同** ——
                                          #     三格擺在一起，一張縮一張不縮就是同一科有兩個名字；
                                          #     守門是從這一支的原始碼讀那張表，不要在守門裡再寫一份
                                          # ⚠ 「詳情」開頭三案：DOCS_LEAD=a|b|c，預設 b（見 README 36-18）
node drafts/channels/post-fb.mjs         # **同一組貼文圖搬到 Facebook**（三張成品 ＋ 地圖浮水印那把尺 ＋ 動態上的模擬）
                                          # ⚠⚠⚠ **內容一個像素都沒有另外畫** —— 用同樣那三支產生器、
                                          #   只把浮水印切成臉書版（WM_MODE=fb）。另做一套就是第二個真相
                                          # ⚠⚠⚠ **2026-09-09 定案：臉書的相簿貼文也是三格，所以照 LINE 那樣拼一顆**
                                          #   （WM_MODE=fb ＝ 臉書那一組三格 829×413／413／3，量出來的）
                                          #   ⚠ 「一張一顆」那一版沒有刪：WM_MODE=fb-solo 還產得出來（留著當退路）
                                          # ⚠ 牙洞那兩輪的推導（hole: "show" | "cut"、WM_FB_DOCS、WM_FB_MAP）
                                          #   只對 fb-solo 成立，對現在這一版不適用（三格拼一顆，洞在合起來那顆上）
                                          # ⚠ 順序不用重排，但**三張要重新上傳**（貼上去的是一張一顆那一版）
                                          # ⚠⚠ 跑完會用預設值再跑一次，把 LINE 那三張還原成三格版；
                                          #   那一步刪掉的話三張接不成一顆圓，而且看起來很正常（守門會抓）
node drafts/channels/post-map.mjs        # **商家貼文的位置與停車圖・站上地圖版**（八張候選 ＋ 主頁三格 ＋ 410px 實際大小）
                                          # ⚠⚠ 地圖是開 index.html 本人、把站上那張 SVG **1:1 截下來**貼進畫布，
                                          #     所以站上的地圖改了要重跑它
                                          # ⚠ 它會讀 preview/line-post-hours/fangren-hours-1080.png 來畫主頁三格，
                                          #     那張不在就 throw（先跑 post-hours.mjs）
```

### 十一之七、⚠ 還沒有的東西（上線之前一定會卡住）

1. **兩顆按鈕的網址都還不能用**：綁定的是**廠商系統產的**（現在是佔位符
   `【…】`）；「介紹芳仁給朋友」用的推薦官方帳號深連結
   `https://line.me/R/nv/recommendOA/@445rpiiv` **沒有實機測過**。
   ⚠ **不要改填 `https://line.me/R/ti/p/@445rpiiv`** —— 按的人已經是好友，
   點下去只會開到帳號頁，不會叫出轉傳。
2. **廠商那八題**（見 `drafts/channels/README.md` 第十六、十七節）——**2026-09-04 多五題**
   （第 19-12 節）：**那 24 小時的計時會不會跳過休診日**
   （週一的約診，提醒週六發、期限落在週日，電話沒有人接）、被自動取消時會不會通知病人；
   **2026-09-04 再多三題**（第二十二節）：**「預約狀態」總共有幾種值**、各是什麼意思、
   和提醒卡那顆確認鈕是不是同一個欄位；**預約成功／查詢那兩則的版型改得動嗎**
   （尤其**日期那一格能不能換格式、能不能關掉 `maxLines: 1`** —— 那是唯一一件
   **正在出錯**的事）；**輪播最多幾張、超過怎麼辦**（LINE 上限 12 張）。
   ⚠ 「提醒卡的『確認』是什麼動作」**不列在這裡** —— 使用者 2026-09-04 決定那一題交給
   **之後的〈看診前 48 小時提醒〉提案頁**（版型與那顆確認的按鈕在那邊定），定完再回頭
   看綁定完成那一則要不要多寫一句。**在那之前不要自己補「怎麼確認」的說明。**
   八題裡最要緊的是
   **滿意度調查卡那兩顆按鈕連去哪**（可能是 review gating）、以及
   **後台的「招呼訊息」能不能送 Flex Message**（送不出去就要退回圖文分開的版本）。
   ⚠⚠ **2026-09-04 再四題**（取消那一段，見第二十三節 23-7）：
   ① **關鍵字／自動回應送不送得出 Flex Message**（＝ 取消確認要不要做成圖卡，和招呼卡同一題）、
   ② **期限外可不可以直接回電話那一則、不要先問一次**（三步變一步）、
   ③ **「預約改期」那顆按鈕的字改不改得動**（它只取消卻叫改期，**那是誤會的源頭**）、
   ④ **兩個 action 的順序可不可以調**（把「不取消」排前面 ＝ 零成本）。
   ⚠⚠ **2026-09-05 再兩題**（颱風／臨時休診，見第二十五節 25-11）：
   ① ⚠⚠⚠ **颱風休診那天，系統裡那些約診會變成什麼？** 48 小時提醒在停診**之前**
   就送出了，而那張卡寫著「24 小時沒回應會自動取消並遞補」——
   照樣計時的話那些人會被系統當成取消。**這一題決定「這次不算你失約」那句能不能寫。**
   ② **能不能做一個「臨時休診公告」**：診所選一個日期 → 系統自動發給那天有約的人。
   （＝ 同時解掉「圖卡」與「人工一個一個貼名單」）
   ⚠⚠⚠ **2026-09-07 廠商回了 ③ 與 ⑤，兩題答的都是「另一個問題」，而他們答的那一句
   都是對的**（推導在第三十節）：
   ・**③「綁定完成訊息為公版，故無法調整」** —— 我們本來就沒有要改那句話。
     ⚠⚠ 所以**第 19-5 節那句「改不了 → 只能和自動回應共用」要更正**：那個結論
     **只在「由後台回」的前提下成立**。**後台那一側永遠分不開**（關鍵字回應是完全
     一致比對，而那句話含著每個人不同的號碼，抓不到）；**廠商那一側本來就分得開、
     而且成本近乎零**（那句話是**他們的系統送出來的**，所以他們在那一刻就知道綁定
     成功了 —— 不必比對字串），而且**不必取代公版，在它後面再送一則就好**。
     → 換成三題：**那一則現在是誰回的／改由你們系統在綁定當下送這張卡可不可以／
     後台的「自動回應訊息」同時開著會不會兩則一起送**。
     ⚠⚠⚠ 最後那一題會動到**已經定稿的自動回應那一則**（24-4 選純文字的**唯一理由**
     是「字留在診所自己手上」，改由廠商送那條理由就沒了）—— **那是使用者的決定，
     不要自己換。**
     ⚠ 「關鍵字回應送不送得出 Flex」那一題**因此不必問了**（這一則不會由後台回）。
   ・**⑤「背景只能全白或其他顏色，無法放圖示」** —— **那一句是對的**（Flex 的 box
     真的沒有背景圖，`background` 只認 linearGradient），**但我們要的浮水印不是背景**，
     是 `image` ＋ `position: absolute`。
     ⚠⚠⚠ **而「這張卡裡放得了圖」已經被他們自己的成品證實了**：他們現在送的那張
     預約成功卡，**頂端那塊 87px 裡就有一顆標誌**（22-4 第 ③ 條）。→ 要問的只剩
     **「能不能用 absolute 疊在文字後面」**，不是「能不能放圖」；疊不上去就退回
     **「右邊一顆不重疊」**（22-12 那張表的第三格，100% 支援）。
     ⚠⚠ **通則：對方說「做不到」的時候，先去他自己的成品上找反例** —— 找得到的話，
     那句話就從「不可能」縮小成「某一種用法不確定」，而那是問得完的。
     ⚠ **背景色的答案是 `#F4F4F5`，而且不必改任何東西** —— 七則的 JSON 每一份
     本來就寫著它（＝ 網站的卡片色）。**不要為了「看得出來」換成紙色 `#E2E5E6`**：
     這一站 `#F4F4F5` 是**卡片**的顏色、`#E2E5E6` 是**底**的顏色，換過去等於讓卡片
     變成底，而且要**七則一起換** —— 真要換是使用者的決定。
   ・⚠ 順手夾帶的一件：這一輪是「廠商正在動那兩則」的時機，所以把
     **`maxLines: 1` 讓日期真的被截斷**（22-4 ①）寫成一句可以直接照做的要求
     （改成 `wrap: true`、不要限制行數），擺在版型討論**前面**。
   ⚠ 另外兩件要**在後台自己按一次**才算數：**一對一聊天室的「＋」裡有哪幾種型別**
   （有沒有「圖文訊息」）、**群發的「受眾」能不能上傳名單**。
   ⚠ 另外要跟使用者要一張截圖：**按「否」之後**長什麼樣（**取消成功**那一張 2026-09-04 已經拿到了，見第 21-8 節）。
3. ✅ **整合成一頁給廠商看 —— 2026-09-05 做好了**（使用者 2026-09-04 指定的**最後一步**：「最後和其他的 line 頁面統整做成預覽頁給廠商」；09-05 他說「診所官方 line 需要的素材和頁面都處理完了　統整成給廠商的網頁給我看」）。成品 **`/preview/line-spec/`**，守門 `node drafts/channels/check-spec.mjs`，推導在 `drafts/channels/README.md` 第二十六節。⚠⚠ **那一頁不重抄任何一則的文字**（七則的字各有出處且各自有守門，再抄一份就是第八個真相）——只放事實、**那七頁自己拍的成品 PNG**、以及連回各自的規格頁；細節見第十一之一節那一列。⚠ 它連了八處：`/preview/line-welcome/`／`line-auto-reply/`／`line-bind-done/`／`line-remind/`／`line-booked/`／`line-cancel/`／`line-review/`／`line-typhoon/`。⚠ 舊的多情境模擬頁 **`/preview/line-reply/` 內容已落後**（綁定完成與提醒卡都各自定稿過了，那一頁上的是舊版），**整合頁刻意沒有連它** —— 要不要收掉還沒問使用者，**不要自己刪**。⚠⚠⚠ **2026-09-07 整頁改寫成「只留模擬圖」，並補進圖文選單那三組卡**（⑨ 診所資訊／⑩ 主題與科別七科／⑪ 衛教懶人包十張＋行政兩張）—— 見第十一之一節那一列與第二十八節。⚠ **2026-09-07 稍晚兩件**：行政那兩張（預約協議／掛號與文件費用）**使用者指定拿掉**、以及 `.pv-scroll img{width:auto}` 讓五張圖畫成原始像素尺寸（見第十一之一節那一列）。⚠ 十一個項目分兩組（**自動送出的訊息** 八則／**圖文選單點下去回的卡** 三組），**那三組不是推播，混在同一排編號裡會讓廠商以為它們也會自動送**。✅ **衛教懶人包的二十張圖 2026-09-07 上線了**（`node drafts/channels/publish-handouts.mjs`：頭圖縮到 1024＝Flex 上限、三張副檔名對不上的 PNG 轉成 JPEG、6.31MB → 3.08MB）—— ✅ **授權那一題 2026-09-07 解掉了**：診所**重新匯出十張**，署名從「侑津製圖」換成**芳仁牙醫診所**（分兩批傳，見 README 第三十節）。⚠⚠ 第一次匯出只有 **720×1040**（頭圖 720、比用得到的 804 還少）—— **同一天稍晚解掉了**：讀 JFIF 標頭發現是 **96 DPI ＝ Windows PowerPoint 另存成圖片的預設值**，請診所改 `ExportBitmapResolution` 之後十二張重匯成 **1384×2000**，頭圖 **1024×536 ＝ LINE 的上限**（是縮下來不是不夠）。做法與 Mac／PDF 兩條替代路寫在 README 第 30-11 節。**通則：問「為什麼圖這麼小」先讀檔頭的 DPI 與換算出的實體尺寸。**⚠ 順帶清掉三張副檔名對不上的 `.png`（那三張其實是**整支手機的截圖**）。✅ **同一批多出的兩張全新懶人包（牙周疾病報你知／牙齒手術照護須知）2026-09-07 稍晚也進輪播了**，變成 **12 格 ＝ LINE 的上限**。⚠⚠ **代價是收尾那一格拿掉了**（使用者：「選單上在診所資訊已經有診所網站的按鈕可以選了」——他是對的，我原本把那條路寫成「不建議」、理由是「那是唯一通回官網的出口」，**那句話是錯的**；⑨ 診所資訊那張卡的第三顆按鈕就是「到網站看看　fangren.net」。**通則：說「這是唯一的出口」之前，要把其他入口逐個看過一次。**）。⚠ 兩張新的**不是排在最後面**：牙周那兩張接成「早期怎麼發現 → 拖到嚴重怎麼救」（早期那張放**第一格**，受眾最廣）、手術照護緊接在智齒後面。⚠ 收尾那一格的程式碼留著，`health-carousel.mjs` 的 `const CLOSING = false` 改回 `true` 就回來。⚠⚠ **12 格以前只有「印一行字」在守**，現在兩邊各 throw 一次（`health-carousel.mjs` 產完就擋、`check-handover.mjs` 讀 JSON 再擋）。⚠ **插卡片會讓 `cards/` 那批單張預覽的編號整組位移**（`01-perio` → `02-perio`），加減卡片時那十四張要全部重產。推導在 README 第 30-9、30-10 節。⚠ 診所資訊那三顆按鈕的圖示 `assets/line-{pin,phone,logo}.png`（合計 8KB）2026-09-07 一起上了，所以 ⑨ 與 ⑩ 的資產都齊了。⚠⚠⚠ **2026-09-07 稍晚：那一頁和交付包合併了**（使用者：「可以把模擬圖跟交付包結合成一份嗎」）——一則就是一段（模擬圖 → 圖檔 → 可複製的文字），整份由 `node drafts/channels/build-spec.mjs` 產生，守門 `check-spec.mjs`（`check-handover.mjs` 已併入）。**網址留 `/preview/line-spec/`**，line-handover 只剩指路頁。同一輪補上 **⑧ 颱風**那一則。⚠⚠ **交出去之前還缺的四件**（第 26-6 節；2026-09-07 起連同授權那一題一起列在 `/preview/line-handover/` 最後那一節）：Ａ 那三題一題都沒有答案（尤其**評價邀約在答案出來之前不要送**）、**評價卡的按鈕網址**還沒有（Google 的深連結要 Place ID，診所手上只有分享短網址）、**綁定完成那一則到底由誰回**（2026-09-07 廠商答了「那句觸發字串是公版、改不了」，所以換成第三十一節那三題）、以及**「病人手機上的卡片實寬」到現在仍然沒有量過**（全線的判斷都建立在 268px 上，而那是從診所端後台的截圖推導的 —— 一張病人手機的截圖就能收掉）
4. ✅ **圖已經放到 `https://fangren.net/assets/line/` 了**（2026-09-05）——**21 個檔、0.78MB**，由 `node drafts/channels/publish-assets.mjs` 從七份 JSON 自己收出清單再複製過去
   （五張頭圖 `hero-{welcome,bind,remind,review,typhoon}.jpg` 各 1024×512、七顆 logo、九顆浮水印 `wm-<形狀>-12.png`）。
   ⚠ **不必改 `tools/dist.mjs`** —— `assets` 本來就在它的 `ALWAYS` 裡（整個資料夾遞迴複製）。
   ⚠⚠ **招呼圖卡的頭圖本來是 1040×520、超過 LINE 的 1024 上限，而且 `hero-welcome.jpg` 根本沒有人產**，
   同一輪補掉了（`node drafts/channels/welcome-hero-resize.mjs`，見第十一之一節那一列）。
   ⚠ 27 張浮水印只上線**定案用的 12 那一組九張**；08 與 18 留在 `preview/line-booked/`。
5. **2026-09-10 又多五題**（「還沒綁定就按約診查詢」那一則，第三十九節 39-11）：
   這張公版卡**改不改得動**／**還沒綁定的人按其他格會怎樣、總共有幾種自動回覆**
   （⚠ 那份清單第四次被證明要不到就會漏）／**已綁定但沒有約診**那一則
   （「您尚未有任何預約😃」）要不要一起改／**綁定的網址**（和招呼圖卡等同一個）／
   確認「✅ …綁定成功！」是不是**以病人的身分**送進聊天室的。
6. ⚠⚠⚠ **2026-09-10：這幾條線做完的東西「先存起來」，最後再另外一起整理給廠商**
   （使用者原話：「先存起來　最後再另外一起整理好給廠商」）。
   **所以不要一邊做一邊把新的東西塞進 `/preview/line-spec/`。**

   **現在 `/preview/line-spec/` 涵蓋的**：八則自動送出的訊息 ＋ 圖文選單那三組卡（⑨⑩⑪）。

   **做好了但刻意還沒併進去的五頁**（各自定稿、各自有守門，東西都在）：

   | | |
   | --- | --- |
   | `/preview/line-post-hours/` | 商家貼文・看診時間圖（`fangren-hours-1080.png`） |
   | `/preview/line-post-map/` | 商家貼文・位置與周邊停車（`fangren-map-1080.png`） |
   | `/preview/line-post-docs/` | 商家貼文・科別與醫師（`fangren-docs-1080.png`） |
   | `/preview/fb-post/` | 同一組三張搬到 Facebook（相簿三格版） |
   | `/preview/line-richmenu/`、`/preview/line-bind-prompt/` | 圖文選單「手機綁定」與那一則公版卡 |

   ⚠⚠ **那一包和現在這一包不是同一件事，很可能要分兩份**：`line-spec` 交的是
   **廠商要送的訊息**（Flex JSON、圖檔網址、可複製的文字）；貼文與臉書那三張
   **診所自己貼、不經過廠商**（主頁「最新貼文」與粉專相簿都是後台自己上傳）。
   圖文選單則兩邊都有（版型後台設得了，**分眾與綁定網址要廠商**）。
   **整理那一天先問使用者要一份還是兩份，不要自己決定。**

   ⚠ 整理時照 `build-spec.mjs` 已經走通的那一套：**文字與圖檔一個字都不重打**，
   從各自的產出檔現抽、尺寸現場讀檔頭，守門重跑一次逐位比對。
7. ⚠⚠⚠ **2026-09-11 廠商第三封（客戶經理 Sunny）回了兩件，球現在在我們手上** ——
   全文、逐句判斷與「要問什麼」都在 `drafts/channels/README.md` **第四十一節**。三個重點：
   ・**「官方 LINE 已釘死」那一句只有一半是對的**：後台的自動回應**確實**分不開那一則
   （關鍵字是完全一致比對，而那句話含著每個人不同的號碼），但**這一則本來就不必由後台回**
   —— 那句話是**廠商的系統送出來的**，他們在那一刻就知道綁定好了。**卡住的是「誰回」不是
   「LINE 行不行」**（＝第 31-1 節，這一封等於再證實一次；**通則第三次現場：「做不到」
   要先問是誰做不到**）。⚠ 但改由廠商送會動到**已定稿的 ② 自動回應**（第 24-4 節選純文字的
   唯一理由是「字留在自己手上」）——**那是使用者的決定，不要自己換。**
   ・**新資訊：那句綁定成功訊息同時是「櫃檯的工具」**（廠商的理由 b：櫃檯可以馬上用那個號碼
   查病患資料）。所以「不發送」會拿掉櫃檯的一個功能 —— 要問的是**櫃檯能不能從別的地方看到
   那個號碼**。⚠ 理由 a（通知病患綁好了）**我們自己那張定稿的卡就在做**，a 不是留著它的理由。
   ・⚠⚠⚠ **使用者要上傳「其他商家帳號」的兩個狀況給廠商看，而其中一件反駁不了廠商**：
   「有帳號綁完沒有那一句」✅ 直接證明它是**產品決定不是平台限制**；
   「有帳號的自動回應不只一種」❌ **沒有用**（別家多半是固定關鍵字觸發的，和含號碼的那句話
   不是同一件事），拿去講會被一句話擋回來、還會讓前一件的份量一起變輕。
   **截圖要拍到什麼才算數寫在 41-6**（通則：**拿別人家的畫面當證據之前，先問它證明的是哪一句話**）。
   ・順帶兩件收掉了：**22-4 ② 那個「預約狀態　尚未回覆」的歧義有答案了**（那是**診所端**的狀態
   ＝「還沒在樂衍上處理」，所以病人的兩種讀法都是錯的、而且都真的發生過 —— 第 22-11 節
   把整行拿掉是對的，但**那張狀態表仍然要**）；**39-8 那個「以病人的身分送出」的推論被證實**。
   ・約診狀態那一題**兩邊守的是兩件事、可以同時成立**：問法不是「把狀態拿掉」，是
   **「病人自己按過取消之後，他在 LINE 查詢時那一筆能不能就不列出來」**（樂衍後台照舊保留）。
   ・⚠⚠⚠ **2026-09-11 稍晚：使用者拍了四個別家帳號綁定完成那一刻的畫面**（第 41-11 ~ 41-15 節，
   **還有一張等著放進來**）—— 富邦、潔客幫、BREAD ESPRESSO、中國信託。
   **五則的第一句都在講綁定**，中信那一則還是**帶四顆按鈕的 Flex 圖卡**（＝ 和我們那張
   `bind-done-card.json` 同一類）；BREAD 那張最乾淨（12:00 按綁定 → 12:02 商家回話，
   中間**病人那一側一句話都沒有**）。⚠⚠ **但它們證明的不是「別家後台分得出綁定」** ——
   那幾則多半是**那一家的系統送的**，所以**講成「你們後台為什麼不行」會被一句話擋回來**；
   要講的是「那句綁定成功也是**你們的系統**送出來的，所以你們在那一刻就知道，
   **不必比對字串、直接在它後面送一則**」＝ 41-7 的第 1 條，只是現在有五個現成例子。
   ⚠ 圖沒有進 repo、也沒有貼上任何頁面（別人家的畫面，同第三十七節那條）。
   ・⚠⚠⚠ **同一天又三張，而最後一張改變了結論**（第 41-16 ~ 41-21 節）——
   國泰世華（同一分鐘內：打別的字回「我無法理解」、打「綁定」回帶名字的專屬卡）、
   玉山（**商家自己送**「您已成功申請『個人化通知服務』!」，**一個號碼都沒有**）、
   以及 ⭐ **新竹品御牙醫 —— 同一個廠商的另一間診所**：按約診查詢 → **一模一樣那張藍色公版卡**
   → 病人那一側送出「✅ 09xxxxxxxx 綁定成功！」→ **後面什麼都沒有**。
   ⚠⚠ **兩件事因此同時成立**：39-8 那個「以病人身分送出」的推論**被直接證實**（右邊、綠泡泡、已讀）；
   而**同一個系統、同一句話，在那一間診所沒有觸發任何通用回覆** —— 所以問法變成
   **「同一套系統的品御，他們的自動回應是怎麼設的？我們能不能照那個設法」**，
   **不是要求新功能，是問他們自己手上就有的設定。**
   ⚠ 但先知道兩種可能的答案（都有代價）：品御設成**關鍵字模式、沒開「一律回應」**
   （那我們第二十四節那一則就廢了）、或設了**時段**（綁定隨時會發生，避不開）——
   **兩個都要付代價，正好把球推回 41-7 的第 1 條**（改由廠商系統送，那是唯一
   「綁定回專屬的、其他時候照舊回通用的」都成立的路）。
   ⚠ **廠商的理由 a 有反例了**（玉山那則由商家送、而且不含號碼），
   **問題徹底收斂到 b 一句：櫃檯能不能從別的地方看到那個號碼**（要先問診所自己）。
   ⚠⚠ **七張一起丟會被挑最弱的那張回** —— 主力擺品御、玉山補刀，其餘五張是背景不是論據。
   ⚠ **要不要指名品御是使用者的決定**（Sunny 自己開口要帳號，但那等於指出他們對不同客戶設定不同）。
   ⚠ 那個完整的門號沒有抄進任何檔案。
   ・⚠⚠⚠ **2026-09-11 再一輪：使用者自己做了對照實驗**（第 41-22 ~ 41-27 節）——
   對五個帳號各打「早安／午安／晚安」，看**平常打字進去**會回什麼。
   **前面那七張只看得到「綁定那一刻收到什麼」，這一輪看得到另一半** ——兩件擺在一起
   才知道那個帳號有沒有把兩者分開。⭐ **潔客幫是最貼近我們需求的一張**：
   8:10 綁定完成回「謝謝你完成好友綁定＋200 元折價券」、9:34 打「早安」回
   「感謝您來訊…客戶服務時間…」＝ **綁定那一刻回專屬的、其他時候回通用的，而且真的在跑**。
   ⚠⚠ **BREAD 三則都沒有回應 ＝ 它根本沒開自動回應**，而它綁定時仍然收到「麵包學徒」那一則
   → **那一則必定是系統送的不是自動回應**；把它和品御（頁首寫著「正在自動回覆訊息」、
   綁定那句後面卻什麼都沒有）擺在一起，41-17 那三種可能只剩「關鍵字模式」與「時段」。
   ⭐⭐ **因此長出一個使用者五分鐘做得完的實驗（做完再問廠商，話會更硬）：
   對品御那個帳號打一句「早安」** —— 回了通用的 ＝ 它有「一律回應」卻沒被那句綁定成功觸發
   （**那句話根本沒經過後台，問題從「LINE 的限制」變成「他們怎麼設的」**）；沒回 ＝ 關鍵字模式
   （代價是打字進去沒人理，我們第二十四節那一則就廢了）。**兩種結果都用得上。**
   ⚠ 中信「早安／午安／晚安／等好久」四句四則不同，**但全是固定字**，
   **41-6 的判斷第三次確認：仍然反駁不了「含號碼的那一句抓不到」**。
   ⚠ 富邦與國泰世華三句都回同一則 fallback → **回一則通用的是常態、不丟臉**，
   我們那一則的存在是對的，**問題從頭到尾只有「它不該在綁定那一刻跳出來」**。
   ⚠ 潔客幫那則寫「Line@ 會有真人回覆」是**反例不是範本**（這一站沒有專人，第十一之三節）。
   ・⚠⚠⚠ **玉山補做了同一組實驗，而且當場誤命中一次**（第 41-28 節）——
   打「玉山」→ 分行據點、打「早安」→ 問候、打「**午安**」→ ⚠ **「台幣匯款說明」**。
   **兩種可能我們分不出來**（後台把「午安」掛錯一格／他們的智能客服猜錯意圖），
   但**結論一樣：關鍵字分流會出錯，出錯的樣子就是答非所問**，而這是一家大型銀行線上在跑的帳號。
   ⚠⚠ **這反過來支持 41-7 的第 1 條**：要求廠商「用關鍵字去抓那句綁定成功」的話，
   抓錯的代價是**病人在錯的時機收到錯的訊息、而且沒有人會發現**；
   **第 1 條沒有這個風險 —— 他們的系統在那一刻本來就知道，不必比對任何字串。**
   ⚠ 這和站上自己的舊決定是同一件事（第九節那輪頁首搜尋**刻意不做同義詞對照表**，
   理由是「會長出一份和畫面對不起來的第二真相」）——**玉山那一格就是它長出來的樣子。**
   ⚠ 反駁廠商那一句仍然不行（三句全是固定字），**41-6 的判斷第四次確認。**
8. ⚠⚠⚠ **2026-09-11：廠商把 ⑥ 預約成功／⑦ 約診紀錄查詢 兩則照我們的規格改了一版**
   （四張截圖，09-10 14:18~14:27）—— 逐項對在 `drafts/channels/README.md` **第四十二節**。
   ・**六件對上**：姓名（粗）＋「　約好囉」逐字、日期升成整張卡最大的一塊、
     兩行灰色小字**逐字**就是我們 JSON 那兩句、頂端那塊 87px 的標誌區拿掉、
     地址電話拿掉、浮水印放了。⭐ **這是這條線配合度最高的一次，回信要先講這個**；
     也證實了第二十二節開頭那句判斷 —— 這兩則本來就是他們系統在送的 Flex，
     **要問的從來不是「送不送得出」，是「願不願意換這一版」**。
   ・**四件沒對上，照會不會出事排**：
     ① ⚠⚠⚠ **卡上年份寫著 2020**（截圖是 2026-09-10 拍的）——測試資料打錯年（沒事）
        還是系統真的算錯（**每個病人都收到錯的年**）。⚠ **不可以從星期反推**：
        **2020-09-11 和 2026-09-11 都是星期五**，那個「星期五」給不出任何資訊。
     ② ⚠⚠ **日期格式沒換**，而它正是被截斷的那一種；**但真正要修的是 `maxLines: 1` 不是格式**
        （短格式只是多買一層餘裕，折行才是不管多寬都不掉字）。⚠⚠⚠ **2026-09-11 更正：
        「要一張輪播的截圖」是錯的，那張圖本來就在那四張裡面** —— 第四張就是約診查詢的
        輪播，日期 `2026/09/11 11:50 星期五` **完整顯示、沒有截斷**，所以 `maxLines`
        看起來已經處理掉了，只差確認一句；**它同時解掉 ① 的一半**（那一張顯示的是 2026，
        所以 2020 比較可能是測試資料）。**通則：說「還缺一張某某的截圖」之前，
        先把手上每一張都打開看過。**
     ③ ⚠⚠ **「預約狀態」還在**，但**字從「尚未回覆」換成「您已取消，請聯絡診所改約」** ——
        22-4 ② 那個歧義解掉了，而且對著病人講話、給了下一步。**所以我們的要求要跟著縮小**
        成 41-8 那個問法（病人自己取消的那一筆能不能就不要列出來），**不要照原話堅持**；
        做不到就留著這一版的字、不要退回「尚未回覆」。
     ④ ⚠⚠⚠ **浮水印那一項我第一次判錯了，是使用者看出來的**（「預約成功通知有兩張圖案 logo
        不一樣欸」）—— 去逐字稿撈圖逐像素量過：**他們真的照我們那張九顆的表在換，
        形狀與每一顆自己的顏色都對得上**（圓形＋紫 ＝ `r3c1` 口外 `#8e6299`、
        寬形＋青綠 ＝ `r1c2` 牙周 `#317d78`，長寬比量到 2.024 對表上的 2.029）。
        ⚠⚠ **我看錯的成因**：看的是「整個聊天室」那張截圖，兩張卡並排、浮水印只露一小角
        就當成同一顆 —— **通則：「有沒有在變」不可以看並排的縮圖，要把每一張單獨放大比**；
        而且這一頁的守門幫不上忙（廠商送的是截圖不是我們的產出檔）。
        ⚠⚠⚠ **所以這一題升級成「請確認換的規則」**：同一筆約診（2020/09/11）**三分鐘內
        出現兩顆**，若不是在給候選，就是輪替沒綁在那一筆上 ＝ 22-13 明文否決的
        「同一筆每次看都不一樣」。
     ⑤ ⚠⚠⚠ **大小與位置也不一樣 —— 使用者第二次看出來的**（「浮水印　位置　大小
        還是蠻不一樣的」）。**量法：拿形狀中間那個牙洞當尺**（牙洞占圖寬的比例是固定的，
        所以圖被切掉也反推得出它本來多大）。⚠ **顏色比對一定要 ±1 不可以 ±4** ——
        灰色小字的抗鋸齒 `(239,239,239)` 和 12% 的口外紫 `(239,236,243)` 差不到 4，
        ±4 會把整段文字選進去。量出來：**圓形 67px ＝ 卡寬 25.1%（規格 104px ＝ 38.8%）**、
        **寬形 83px ＝ 31.1%（規格 150px ＝ 56.0%）**、**輪播上的寬形 80px ＝ 38.6%
        （規格 72.5%）** ＝ 我們規格的 **53~65%**；而且三張都**往右下溢出 8~10／6~7px、
        右下角被切掉**，我們的 JSON 寫的是 `offsetEnd 0px／offsetBottom 0px` ＝ 收在卡內。
        ⚠⚠ **兩顆之間的相對大小也對不上**（廠商 1:1.24、我們表上 1:1.44）——
        那九個寬度是**按墨的面積正規化**算的，整組等比例縮沒問題、**各縮各的就會破壞
        「九顆一樣重」**。所以這不只是畫小了。
        ⚠⚠⚠ **回信之前要先解決我們自己的矛盾**：`booked-card.json` 寫 0／0 收在卡內，
        而送給廠商看的 `shot-booked.png` **往右下溢出 18／10px**（第二十二節本來就記著
        這個已知差異）—— **廠商很可能是照圖做的**，而他們也證明了溢出做得出來。
        **兩份對齊之後再回信**，不然這一題會來回兩次。**通則：對方沒照規格做的時候，
        先檢查我們送過去的文字規格與圖有沒有互相矛盾 —— 對方會照圖做。**
        ⚠ 三格的對照圖已經做在 `/preview/line-vendor/` 第 ③ 節（我們的 JSON／我們的
        模擬圖／廠商送來的，卡片畫成真的 268×149、溢出的那一塊由 `clipPath` 切掉、
        另疊一圈虛線外框 —— **12% 的淡墨在縮圖上比不出大小，虛線才比得出來**；
        版面一定要 `auto-fit minmax(236px,1fr)` ＋ 每格 `max-width:268px`，
        **寫死三欄的話 390px 上每格只剩 115px，正好毀掉這一節的用途**）。推導在 42-7。
   ・**還看不出來的一件**：浮水印用 `position: absolute` 疊上去**會不會蓋到字**
     （從來沒有實機驗過，那四張的右下角剛好沒有字）。
   ・⚠⚠⚠ **兩條通則**：**① 對方照著改了之後要逐項對，不要只看整體像不像** ——
     沒對上的那四件**全部是「看起來一樣、細節不同」**，整體一看就像照做了；
     判準是把我們那份 JSON 打開**一個欄位一個欄位念過去**。
     **② 對方改好的東西可能比我們要求的好，要看得出來並且改口** ——
     「預約狀態」那一行照原話堅持就是把改進推掉。
9. ⚠⚠⚠ **2026-09-11：綁定完成那一則收成「兩個處理方向」**（使用者指定整理，
   `/preview/line-vendor/` 第 ⑥ 節，推導在 `drafts/channels/README.md` **第四十三節**）。
   **第四十一節是逐句拆對方的話、第 ⑦ 節是一家一家記別家的畫面 —— 那兩節都是證據，
   沒有一節是決定**，這一節才是。
   ・**方向一 從源頭不要送**：要的是廠商系統那一側的一個開關（做得到的話，
     「平常打字進去會收到什麼」整件事就不必再設計）。對方回不能拿掉，兩個理由 ——
     **a 通知病患**（我們自己那張卡就在做，玉山也是商家自己送、不含號碼，**a 撐不住**）、
     **b 櫃檯可以用那個號碼查病患資料**（這一輪才知道的新資訊）。
     ⚠⚠ **問法要跟著換**：對方已經回過「能不能拿掉」，再問一次只會拿到同一句；
     現在問的是**條件句**「若櫃檯用不到那個號碼，關不關得掉」，而那個「若」
     **要先問診所自己**。
   ・**方向二 留著、但讓兩則不一樣**：對方那一句涵蓋的是**後台**的自動回應
     （完全一致比對，而那句話含著每個人不同的號碼）；這一則**不必由後台回**。
   ・⚠⚠⚠ **兩個方向卡住的是同一件事：那句綁定成功歸誰送。** 改由廠商的系統
     在綁定當下直接送我們那張卡，理由 a 與方向二同時成立，而且不必比對任何字串。
   ・⚠⚠ **別家帳號重新分成三種**（原本按「哪一家」排）：**① 綁定那一句之後沒有再跳
     訊息**（品御，同一個廠商同一套系統）／**② 綁定那一則和平常回的不一樣**
     （潔客幫・中信・國泰世華・玉山）／**③ 綁定那一刻什麼都沒送**（BREAD）。
     第三種是這一次才分出來的，它說明的**正好是方向一**，而另外四家說明的是方向二。
     **通則：同一批材料換一個問題就要重新分組** —— 按「哪一家」排的清單，
     換成按「它回答哪一個方向」排，才知道哪一個方向手上是空的。
     ⚠ 每一種都要同時寫**它說明的是什麼**與**它還沒說明的是什麼**（守門 ⑩ 在盯）。
   ・**附圖**：`preview/line-vendor/reply-0910.jpg`（750×1080，原圖裁 `y 480..2100`
     再縮到 750 寬），裁掉狀態列、群組名與輸入列。
     ⚠⚠ **裁切下緣要量過再切**：第一版切在 `y 2050`，**正好把時間戳切成一半** ——
     畫面上只是右下角多一塊糊掉的灰，**每一道守門都會過**；逐列掃亮度才知道泡泡
     下緣在 2093、時間戳在 2045~2073。
     ⚠ **它和「別家帳號的畫面不進 repo」不衝突** —— 那條管的是**別人家的畫面**
     （repo 是公開的），廠商寫給這間診所的回覆是另一類。**兩者要分開講。**
   ・**守門多一道 ⑩**：⚠⚠⚠ **頁面上不可以出現 `undefined`** —— 欄位名打錯時範本會把它
     **原字印在頁面上**，尺寸、溢出、連結、紅線每一道都會過，**只有把頁面打開看
     才看得到**；同一道另外要求兩個方向與三種現象的欄位齊全。兩種都負向測過。
10. ⚠⚠ **2026-09-12：十二則的狀態逐則更新**（使用者交代，`/preview/line-vendor/` 第 ① 節，
   推導在 `drafts/channels/README.md` **第四十四節**）。
   **①② 招呼圖卡與自動回應／⑨ 取消／⑩ 評價邀約／⑫ 圖文選單三組卡 ＝ 已完成**、
   **⑪ 颱風 ＝ 診所自行製作**、**⑧ 提醒卡 ＝ 定稿・等送**（09-12 稍晚改掉，見第 12 項）、
   **⑤ 補上兩張圖**（新做的那一張 ＋ 現況公版藍色卡）。
   ⚠ **③④⑥⑦ 一個字都沒動** —— 他沒有提到那四則，而那四則正是還卡在廠商那一側的。
   ・⚠⚠ **⑩ 改成「已完成」但「兩顆按鈕連到哪裡還沒有答案、在那之前先不送」那一句留著** ——
     **通則：一則的「做完了」和「可以送了」是兩個欄位，不要用一個字蓋掉另一個。**
   ・⚠⚠ **⑧ 那個「圖片 bug」照原話記，沒有自己猜一個原因** —— 這一線每一則的圖都各自
     有推導，而「哪裡壞了」當時只有一句轉述；頁面上因此寫著「是哪一個 bug 還沒寫下來」。
     ⚠⚠⚠ **那一句在寫下來的那一刻就已經過期了，見第 12 項。**
   ・⚠⚠ **⑤ 的兩張要直向疊不要並排**：那一欄只有 64px 寬，並排 148px 會把右邊的字擠到
     剩 80 幾 px（格子最窄 232px）。`圖` 因此改成「字串或一組」都吃。
   ・⚠⚠⚠ **那兩張是 `/preview/line-bind-prompt/` 第一次有自己的產出檔**
     （`node drafts/channels/bind-prompt-png.mjs` → `shot-bind-prompt{,-old}.png`，
     再跑 `vendor-shots.mjs`；**那一頁改了就要重跑這兩支**）。
     **第三十九節那條「資料夾裡只有 index.html」沒有被推翻，只是講清楚了** ——
     它擋的是「**複製別人的圖進來**」，不是「這一頁自己畫出來的東西」；守門第 ② 道
     從「只能有 index.html」改成**一張寫死的允許清單**，多一個檔照樣 throw（負向測過）。
     **通則：一條「這裡不准有東西」的規則，要先問它擋的是哪一種東西 —— 放寬成一張清單，
     比放寬成「大概可以」安全。**
   ・⚠ 現況那一張是**我們照截圖重畫的結構**（吉祥物沒有畫），不是廠商的截圖本人。
11. ⚠⚠⚠ **2026-09-12：綁定完成那一刻，我們自己的帳號現在長什麼樣**
   （使用者給了一張自己帳號、**病人那一側**的截圖：「更新　綁定成功的現況」，
   README 第四十五節）。頁面在 `/preview/line-vendor/` 第 ⑥ 節開頭，
   圖 `preview/line-vendor/bind-live-0912.jpg`。
   ・**看到**：12:06 病人那一側送出「✅ 09xxxxxxxx 綁定成功！…」（右邊、綠泡泡），
     **同一分鐘**帳號回了那一則通用的自動回應，**逐字就是第二十四節定稿的那一份**。
   ・⚠⚠ **它證實兩件先前只是轉述或推論的事**：廠商那句「綁定成功訊息發出時會觸發
     自動回應」**在我們自己的帳號上成立**；「那一句是以病人的身分送出的」（39-8）
     第一次從**病人那一側**看到（先前兩張是診所端後台與別家診所）。
   ・⚠⚠ **但決策點沒有移動**：它說明不了那一則**歸誰送**（第四十三節兩個方向
     共同卡住的地方），也說明不了廠商系統那一側有沒有「不要送」的開關。
     **證據多一張不等於問題往前走一步。**
   ・順帶三件：**自動回應尾巴那張連結預覽卡**＝首頁那組 og 標籤在 LINE 上長出來的
     樣子（2026-08-22 那一輪第一次看到成品）／**選單列寫「點我選服務」＝ 六格那一版
     還在跑**（⚠ 第三十七節那句「選單列是廠商設的手機註冊」講的是**別家**那張截圖，
     我們自己的不是）／⚠ **「看診日前 2 天 (不含假日)」多出約三分之一個字的空隙**
     —— 後台貼的可能是加了半形空格的舊版（19-18 已經拿掉那幾個空格），
     也可能是 LINE 自己補的間距；**打開後台看一眼就知道**，已列進待答的
     「後台按一次就知道」那一組。⚠ 守門幫不上忙：`check-auto-reply.mjs` 比的是
     `auto-reply.txt` ↔ 規格頁，**後台裡真正貼著什麼字只有截圖看得到**。
   ・⚠⚠ **門號在出圖那一步就塗掉再寫上 `09xxxxxxxx`**（repo 是公開的），
     不是靠 CSS 蓋，來源 PNG 沒有進版控；遮字要**撐成和原本那串一樣寬**
     （321 device px），不撐的話後面那幾個字會突然空一塊、看起來像畫面壞了。
   ・守門多兩道並負向測過：**⑩ 多驗「現況」那一塊的欄位**、
     ⚠⚠ **⑫ 頁面上不可以出現完整的手機號碼** —— 圖是出圖時遮的，這一道擋的是
     「日後有人把號碼打進 JSON」，而那**不會讓任何一道版面守門翻臉**。
   ・⚠ 順手量到「375 CSS px 的螢幕上文字泡泡最寬 312.0px」，
     **但它收不掉 26-6 那一題** —— 那是**文字泡泡**不是 Flex 的 mega bubble，
     而且一個螢幕寬度分不出「固定 px」還是「跟著螢幕縮放」。
   ・⚠⚠⚠ **同一天再一件：那十二則分成「已完成 6」與「待調整（或是有落差）6」兩組**
     （使用者：「12 則照順序看有點混亂　先區分成　已完成　待調整（或是有落差）」，
     README 第 44-4 節）。**已完成**＝①②⑨⑩⑪⑫；**待調整**＝③④⑤⑥⑦⑧。
     ⚠⚠⚠ **①~⑫ 那組編號一個都不換也不重編** —— 廠商兩封回信都照那組編號在講，
     那是雙方共用的詞；分組只改「印在哪一塊底下」，圈號、組內順序、文字一個字都沒動。
     ⚠⚠ **待調整那六則不是同一種「待」**（現況公版／定稿等送／提案中／
     改版後四件沒對上），所以每一格的「狀態」那一欄留著，**分組不取代它**。
     ⚠⚠ **組別由資料自己宣告**（每一列多一個 `組`），**不要從「狀態」那串字猜** ——
     那一欄是寫給人看的自由文字，拿它比對的話改一個字分組就會靜靜地跑掉。
     ⚠ 小標用新的 `.mg`（`.grp` 已經被「待答」那一節佔用）。
     ⚠⚠ 守門多一道 **⑪**：每一列都要宣告組別、兩個小標都要在頁上、**而且兩組加起來
     剛好是全部** —— 只驗前兩件的話，組別打成別的值那一列會被 `filter` 靜靜地漏掉，
     **而版面完全正常**。三種都負向測過。
12. ⚠⚠⚠ **2026-09-12：⑧ 那個「圖片 bug」是什麼 —— 而且它兩天前就修好了**
   （使用者：「8 看診前48小時提醒的bug是　插圖　被媽媽抱著的小孩有三隻手　這個在另外的
   對話提過　也做好新的插圖要替換　整合進這個整理」，README **第四十六節**）。
   ・**bug ＝ 提醒卡頭圖裡被媽媽抱著的幼兒多出第三隻手**；修正在
     **`db1825f`（2026-09-10，另一條對話）**，推導在 README 第 20-17 節。
     換的是**原檔**，裁圖、兩張模擬圖、`assets/line/` 那一份、給廠商那一頁全部重跑過，
     構圖與版面一個像素都沒動。**這一輪一張圖都不必重產**，只有那一列的字要改。
   ・⚠⚠⚠ **所以第 10 項那句「圖片待抽換／是哪一個 bug 還沒寫下來」在寫下來的那一刻
     就已經過期了** —— 「不要替只有轉述的 bug 自己猜一個成因」那個判斷是對的，
     **錯的是沒有去找**：`git log --all --grep=提醒` 五秒就指到那個 commit。
     **通則：口述轉來的狀態要先當成「可能已經過期」查一次，不要直接寫進資料** ——
     只要那句話裡出現「還沒／待／要抽換」這一類**指向別人手上的進度**的詞，
     就先 `git log --all --grep` ＋ `ls -la` 那個資料夾一次。
     ⚠ 這是第九節第 28 條 ② 的反向版：② 是「我的改動讓自己先前的一句話變成假的」，
     這一條是「**別人（含另一條對話裡的自己）已經把那句話做成假的了**」。
   ・⚠⚠ **狀態改成「定稿・等送」（＝和 ④ 同一句），但組別沒有動，仍然在「待調整」** ——
     圖修好了，可是這一則由廠商送、廠商還沒換上新版（真截圖那一則是**看診前 3 天**送的，
     48 小時固不固定仍然沒有答案）。**＝ 上面那條「做完了和可以送了是兩個欄位」又一次**：
     搬進「已完成」就等於宣告病人已經收得到新版了。
   ・驗收：`db1825f` 確認在 `HEAD` 上、`preview/line-remind/hero-remind.jpg` 與
     `assets/line/hero-remind.jpg` **md5 相同**、`vendor-shots.mjs --check` 十二張都對得上、
     守門十二道全綠（⑪ 仍是「已完成 6／待調整 6」）。
13. ⚠⚠ **2026-09-12：② 那一節補上廠商送來的三張畫面**（使用者：「整理頁面裡　要放上
   廠商的約診查詢和已約診時段的頁面顯示」，README **第四十七節**）。
   ・那一節原本只擺**我們送過去的規格圖**，而「對上六件／沒對上四件」講的全是**廠商那一版
     長什麼樣** —— 讀的人手上沒有那張圖，每一句都只能相信轉述。
   ・三張：`vendor-booked-a-0910.jpg`（⑥，浮水印是圓形那一顆）／`vendor-booked-b-0910.jpg`
     （⑥ **同一則**，三分鐘後換成寬的那一顆）／`vendor-query-0910.jpg`
     （⑦ 綠泡泡 `[約診紀錄查詢]` ＋ 跳出來的已約診時段）。
     ⚠⚠ **⑥ 那兩張要分開擺**：同一筆約診三分鐘內兩顆不同的浮水印，那正是第 8 項 ④⑤
     兩件的證據，只放一張又變成沒有材料的轉述。
   ・⚠⚠⚠ **⑥ 那兩張上有病人姓名，出圖那一步就塗掉**（同第 11 項那個門號）：塗白之後寫上
     `〔病人姓名〕` 並**橫向壓成和原本那三個字一樣寬**（153px），不撐同寬的話後面那句
     「約好囉」會突然空一塊。**來源 PNG 不進版控**，裁切是逐列掃「不是近黑」找出聊天室
     那一條亮帶。⚠ ⑦ 那一張不必遮（廠商自己的測試資料印的就是 `○ ○ ○`）。
   ・⚠ **這和「別家帳號的畫面不進 repo」不衝突**：那條管的是**別人家的畫面**，
     廠商為這間診所做的成品是另一類。
   ・⚠⚠ 順手改掉一句已經過期的說明：那張輪播規格圖的 caption 還寫著「要一張輪播的截圖
     才看得出 maxLines 改了沒」，而 42-2 當天就更正過、現在那張截圖就在它上面。
     **通則（又一次）：一句「還缺某某」被收掉之後，要回頭把寫著它的每一個地方都掃一次。**
   ・守門 ⑧ 的**孤兒圖**從 `t-*.jpg` 放寬成**這個資料夾裡的每一個 `.jpg`**（縮圖與附圖
     現在混在一起），負向測過；十二道全綠、390px 上水平溢出 0。
14. ⚠⚠ **2026-09-12：⑥⑦ 在第 ① 節也並排對照**（使用者：「第6、7目前只有放我們的
   定稿版本　也把廠商的版本放進來做對照」，README **第四十八節**）。
   ・⚠⚠⚠ **它補的不是圖，是「那一格自己看得懂」** —— 第 ② 節整節在講 09-10 那一版的
     逐項比對，但**第 ① 節那張十二則的表是另一個用途**（一則一格、圖就是它的名字）；
     那兩格擺的是我們的定稿模擬圖、狀態卻寫著「09-10 改版」，**圖與文字指的不是同一個
     版本**，讀的人只看那一格看不出差在哪。**通則：一格裡的圖與文字如果指的不是同一個
     版本，那一格就在說謊。**
   ・**做法沿用 ⑤ 已經走通的那一套，版型一行都沒改**：`圖` 本來就吃「字串或一組」
     （直向疊 —— 那一欄只有 64px，並排會把右邊的字擠到剩 80 幾 px），
     哪一張是哪一張**寫在那一行註解裡**（「上：我們的定稿版本。下：廠商 09-10 送來的
     那一版…」）。⚠ 一度想在每張縮圖底下加小字標「我們／廠商」，**沒有做**：
     ⑤ 已經用註解解決過同一件事，而 64px 底下只塞得下兩個全形字。
   ・⚠⚠ **`vendor-shots.mjs` 第一次吃「不是我們自己拍的」來源**（廠商送來的截圖本人）：
     **來源要分副檔名讀** —— 原本一律走 `png()`（固定位移讀 IHDR），JPEG 進去讀到的是
     垃圾尺寸，而且**不會報錯**（Chromium 拿到 `data:image/png` 開頭的 JPEG 照樣解得開，
     只有裁切用的 `naturalWidth/Height` 會錯 ＝ 靜靜地變形）；補一個按副檔名分流的
     `read()`，mime 跟著走。⚠ 那兩張是**橫的**，所以裁出來是**左邊那一塊正方**
     （前十二張是直的、裁的是上緣）—— 兩張剛好都成立，**是打開看過才留下的**。
   ・⚠ 病人姓名**在做那三張附圖的時候就遮掉了**，縮圖這一層只縮不改 ——
     **不要在兩個地方各遮一次**，其中一個遲早會漏。
   ・守門 ⑧ 自己就涵蓋了（新的兩張縮圖找得到、尺寸對得上、有人引用），
     47-5 放寬過的孤兒圖那一半也涵蓋了來源那兩張；十四張縮圖、21 張圖全綠。
15. ⚠⚠⚠ **2026-09-12：⑦ 那一欄叫什麼**（使用者：「如果廠商那邊預約狀態的欄位無法移除
   我認為內容要修改　保留 您已取消　拿掉請聯絡診所改約……但『預約狀態』這個詞不夠精確
   因為　只要診所或患者其中一方更改　這個預約就不存在了」，README **第四十九節**）。
   ・⚠⚠ **要求排成兩層，順序不可以顛倒**：第一層仍然是 41-8 那一題（**那一筆能不能
     不要列在他的查詢清單裡**）—— 做得到的話底下兩題都不必問，那一列根本不會出現；
     第二層才是收短那一句與換欄位名。**三題平排丟過去，對方會挑最便宜的一題回。**
   ・**拿掉「請聯絡診所改約」照做，而且有既有的理由撐著**（照第二十三節那條規矩查過，
     它不是任何事實的唯一出處）：**那張卡是收據**（第二十二節）、**卡上既沒有電話也沒有
     按鈕**，而改約那條路在他按下取消的當下那一則就給過了。⚠ 代價：他是幾天後才打開
     查詢的，那時可能忘了 —— 但補救的位置是取消當下那一則，不是這張沒有號碼的卡。
   ・⚠⚠⚠ **他講對了，而且可以再精準一階：真正壞掉的是主詞** —— 「尚未回覆」講的是
     **診所**（41-9：還沒在樂衍上處理）、「您已取消」講的是**病人**，**同一欄兩個主詞**，
     所以沒有任何一個以「預約」開頭的名字容得下它。而那張卡的標題是「已約診時段」、
     卡上卻印著「您已取消」＝ **清單的名字和那一列的內容互相矛盾**，那正好是第一層那一題。
   ・**建議「回覆狀態」**：① 那是讀的人已經在用的詞（提醒卡請他按鈕告訴診所會不會到）
     ② 尚未回覆／已確認／已取消三個值都容得下 ③ 主詞收斂成病人一個人。
   ・⚠⚠ **「二次確認」不要用，兩個獨立的理由**：**這個帳號裡那個詞已經有主了**
     （取消那一段防誤按的那一層，第二十三節）—— 同一個帳號裡一個詞指兩件事比不精準更糟；
     而且它是**步驟的名字不是狀態的名字**，放進標籤的位置會讀成「二次確認：您已取消」。
   ・**第三個選項擺出去了但沒有列進建議**：整行不要標籤、只印那一句
     （「您已取消」本身就是完整的句子，標籤佔掉那一行約三分之一的寬度）——
     他的前提是那個欄位移不掉，而標籤很可能和欄位是一體的。
   ・⚠⚠⚠ **命名之前要先拿到值的清單** —— **一個欄位有哪幾種值，決定它叫什麼名字**；
     清單裡若有診所端的值（已報到／已完成／診所取消），「回覆狀態」就會變成新的錯。
     那一題（09-04 問到現在沒有答案）因此升成最先要的一組，問法加了兩句：
     **每一筆都印還是只有出事的那一筆才印**（四張截圖裡只有一筆看得到值）、
     **沒有取消過也還沒回覆的那一筆現在顯示什麼**。
   ・⚠⚠⚠ **最會出事的一件**：廠商說「約診狀態要留下，不然樂衍上的約診會不見」，
     照那句話那一欄的存在理由是「病人按了取消、診所還沒做掉」——
     **如果新的那一句只是舊的那個值換了顯示字，沒有取消過的人也會看到「您已取消」**。
     這比欄位名嚴重得多，而且**從那四張截圖看不出來**。
   ・守門：只改資料不改版型，第 ① 道自己涵蓋；⚠ 新寫的字要避開第 ② 道
     （不可以抄任何一則訊息的文字）—— 所以取消當下那一則只描述、不引用。
   ・⚠⚠⚠ **2026-09-12 稍晚：其他候選的欄位名攤開了**（使用者：「除了回覆狀態
     還有什麼可以參考」，README **第 49-7 節**）——**候選不是按好壞排，是按
     「這個名字假設那一欄裝著什麼」**：甲 只裝病人回了什麼（回覆狀態／你的回覆／
     ⚠ 確認狀態語意不通 —— 取消不是一種確認）／乙 什麼都不假設（目前狀況／狀態，
     任何清單都活得下來，代價是幾乎沒有說任何事）／丙 照廠商給的理由命名
     （處理狀態／處理進度 —— 事實上最誠實，但**主詞留在診所那一側，49-3 那個矛盾
     原封不動**，而且「處理中」會讓病人坐著等）／丁 換一個詞類（備註／提醒 ——
     **只有在「平常不印、有事才印」時才成立**，而那正是還沒問到的那一句）／
     戊 不要標籤。⚠⚠⚠ **選哪一個由值的清單決定**：只有病人端的值 → 甲
     （值是整句用「回覆狀態」、值是短詞用「你的回覆」）／混著診所端的值 → 乙
     （這時「回覆狀態」是錯的，會生出「回覆狀態：已報到」）／平常不印 → 丁。
     ⚠ 建議仍然是**回覆狀態**，頁面上只掛這一個，等他挑。
   ・⚠⚠ **順帶讓這個要求便宜很多的一句已經寫進去了：要改的只是卡片上印出來的那個
     標籤，資料庫欄位叫什麼名字不必動。****通則：提出改名的要求時要先講清楚改的是
     顯示還是結構，不然對方會照最貴的那一種估。**
   ・⚠⚠⚠ **2026-09-12 再一輪：使用者把那一欄的值列出來了，四種**（README **第五十節**）——
     ① 提醒發送前一律「未回覆」／② 發送後點了確定「已確認」／③ 發送後沒點「未回覆」／
     ④ 發送後取消「已取消」。他：「**有兩個狀態都是未回覆，其實意義是不一樣的**。」
     ⚠⚠⚠ **這份清單一到手就把 49-7 的八個候選殺掉四個，而且推翻了 49-4 那個建議**：
     四個值都在講「這一筆走到哪一步」，**第 1 種不是回覆**（那時還沒有人問過他），
     所以 回覆狀態／你的回覆／確認狀態 全部出局；備註 也出局（**每一筆都印**）；
     處理狀態 的主詞在診所那一側。**剩下「目前狀況」。**
     **通則（49-5 的反面）：一個名字只要有一個值套不進去，那個名字就是錯的，
     不是「大致上對」。**
   ・⚠⚠ **真正要改的只有第 1 種**：那兩種不是對等的兩件事（1 ＝ 沒有人問過他、
     3 ＝ 球在他那裡），**把第 1 種換掉，第 3 種原封不動，那個字自己就變成唯一解** ——
     三處收成一處（同第二十節：要求的改動愈小愈可能真的改到）。第 1 種建議印
     **「看診前會再提醒你」**（那是全表最常出現的一格，印一件對他有用的事實）；
     ⚠ **刻意不寫天數也不寫小時** —— 招呼圖卡寫「看診前2天」、綁定完成寫「48 小時」，
     兩種講法還沒統一，寫數字就是生出第三種。
   ・⚠⚠ **第 3 種不要改寫成「等你回覆」** —— **這張卡上沒有按鈕**（它是收據），
     回覆的地方在提醒卡、要往上捲好幾則；寫成催促等於推他去做一個這張卡上沒有的動作
     ＝ 39-10 那個圈子。**描述就好。**
   ・**兩個包裝**：**甲（建議）目前狀況 ＋ 第 1 種印一句話**／乙 回覆狀態 ＋ 第 1 種留空
     （乙 唯一的好處是那個名字活下來，代價是最常出現的那一格是空的）。第 2~4 種兩案相同。
   ・⚠⚠⚠ **要先確認的一件：系統分不分得出第 1 種和第 3 種**（現在印同一個字，
     若儲存值也相同，要分開就得再看一次「提醒發了沒」）—— 兩案都建立在它做得到，
     所以待答第 11 題改寫成先問這一件，第 12 題（照這樣改）排在後面。
   ・⚠ 順帶收掉 49-5 追加的兩句（**每一筆都印**、沒取消也還沒回覆的顯示「未回覆」），
     而 49-5 最擔心的那件**沒有發生**：四個值裡沒有診所端的值，
     所以「沒有取消過的人也看到您已取消」那個風險不成立。
   ・⚠⚠ **第 3 種背後還有一題沒答，所以那一格不可以寫期限** —— 19-12 那個
     「24 小時沒回應會自動取消」到現在沒有答案（待答第 15 題）。
   ・⚠⚠⚠ **2026-09-12 稍晚兩件**（使用者：「系統分得出來　但　看診前會再提醒你　有點長」，
     README **第 50-8、50-9 節**）。**① 前提收掉了**：系統分得出第 1 與第 3 種，
     所以兩案都做得到 —— 待答第 11 題只剩「這四種是全部嗎」（降級），
     第 12 題從條件句變成直接的要求（升成急件）。
     **② 「太長」的成因是形狀不對不是字數**：另外三個**全部是三個字、已／未 ＋ 兩個字**，
     中間插一句七個字的完整句子，那一欄就從一組標籤變成三個標籤配一句話。
     **通則：一欄值裡有一個特別長，先看它是不是和其他值不同詞類（標籤 vs 句子），
     那比「再縮兩個字」有效。**定案 **「待提醒」**（已確認／未回覆／已取消／待提醒，同一族）。
     ⚠⚠⚠ **「待提醒」同一天稍晚被推翻了，見下一條** —— 形狀是對的，答應的事不對。
     ⚠ **不要用「未提醒」** —— 其餘三個的「未」講的是病人，這裡的「未」講的是診所，
     同一欄兩種主詞就是 49-3 那個毛病的縮小版；**「待」不指任何人**。
     ⚠ 代價：50-2 那個「印一件對他有用的事實」縮回去了（「待提醒」沒說什麼時候提醒）——
     要寫時間就得寫天數或小時，而那兩種講法還沒統一。
   ・⚠⚠⚠ **2026-09-12 再一輪：「待提醒」不能用 —— 一個值不可以是一句對未來的承諾**
     （使用者：「我不是很喜歡待提醒　那如果系統哪一天出問題　沒發送提醒　患者就說
     那是你們診所的問題　沒提醒我　我忘了（看似很合理）　診所購置這個系統是友善提醒
     不代表患者自己可以不用記得」，README **第 50-10 節**）。
     ⚠⚠⚠ **這是前面九小節都沒有量過的維度**：49-3 到 50-8 一路在問「**這個值講的是誰**」
     （主詞在病人還是診所那一側），這一題問的是「**這個值答應了什麼**」——
     「待提醒」主詞確實不指任何人（50-8 正是為這一點選它的），
     可是它描述的是**診所接下來會做的一個動作**，而那個動作由一套會出事的系統執行；
     那一行字就印在他自己那一筆約診旁邊，沒送出去的那一天它就從狀態變成承諾。
     **新判準：那一欄的值要是「這一筆現在是什麼狀態」，不可以是「診所接下來會做什麼」。**
     ⚠ 它和第十一之三節那條紅線同族（沒有專人 → 不可以寫隨時問）：**不要印我們保證不了的事**；
     差別是那一條擋服務承諾，這一條擋**被系統執行的動作**。
     **定案第 1 種印「已預約」**（已預約／已確認／未回覆／已取消，同一族），三個理由：
     它是那一筆自己的事實、答應不了任何人任何事；形狀對得上；
     ⚠⚠ **而且和同一欄的「已取消」正好成對，所以不是廢話** ——
     乍看清單標題已經寫著是已約診的時段、再印「已預約」是多餘的，
     **但取消掉的那幾筆也列在同一張清單裡**（那正是 41-8 那一題的由來）。
     ⚠ 落選：待看診（⚠ 假設清單只列未來的約診，那件事沒驗過）、已排定／已登記（行政系統的詞）、
     留空（＝ 50-4 乙案，什麼都不答應但最常出現的那一格是空的）、
     ❌ 未確認（會把第 1 種和第 3 種又併回去）。
     ⚠⚠ **一件更大的、沒有自己動的**：「你們沒提醒我」這個說法**跟這一欄無關也已經成立** ——
     招呼圖卡與綁定完成本來就各自寫著看診前會再提醒（兩則講法還沒統一，19-12），
     所以這一輪不是把那個風險引進來。這一欄加上去的那一階是：那兩則是**加好友、綁定那一刻
     各講一次**的服務說明，這一欄**每一筆約診都印一次、就印在那一筆旁邊** ——
     一句對帳號的概括會因此變成一句對「這一筆」的承諾。
     ⚠⚠ 要不要補一句「提醒是善意的服務，時間仍請自己記得」**是診所的決定，沒有自己加**；
     真要寫，位置在招呼圖卡或綁定完成那一則，不是這張收據上的一個欄位值。
   ・⚠⚠⚠ **2026-09-12 最後一輪：第三種維持「未回覆」，不要寫成「已提醒未回覆」**
     （使用者：「已預約　已確認　已取消　這三個不錯　剩下一個　是未回覆好　還是
     已提醒未回覆　還是　診所已提醒未回覆　或　未回覆提醒　未回覆診所提醒
     （提醒需要回覆嗎？」，README **第 50-11 節**）。
     **先答那個問句：需要** —— 那一則提醒卡（第二十節定稿）底下就有兩顆按鈕、
     上面一句話明白請他按一下告訴診所，**第 2 種「已確認」就是他按了主鈕**。
     ⚠⚠ **但「沒回會怎樣」到現在沒有答案**（19-12，待答第 15 題），
     所以這一格只能描述、不可以寫成催促（50-3）。
     **四個候選全部出局，兩條既有的判準各殺掉一批**：已提醒未回覆 ❌**形狀**
     （50-8：其餘三個都是三個字，多一段就從一組標籤變成一句話）／
     診所已提醒未回覆 ❌ 形狀 ＋ **主詞**（49-3）／未回覆提醒、未回覆診所提醒 ❌ 同上。
     ⚠⚠⚠ **「已提醒」還有一件和長度無關的**：它的主詞是診所，接在「未回覆」前面
     整串讀起來是「**我們提醒過了，是你沒回**」—— **那張卡是收據，同一欄裡不要出現
     一句對讀的人的評語**。⚠ 再一層：**「已提醒」是我們對自己系統的一句宣告**，
     提醒真的沒送到的那一天，那三個字就是印在他眼前的一句錯話 ——
     50-10 擋的是「對未來的承諾」，**這一條是它的過去式版本：一個值也不要替我們自己的
     系統背書**。
     ⚠⚠ **「未回覆」現在不會被誤讀，理由在第 1 種身上**：50-2 那一改之後，
     這一欄會出現「未回覆」的唯一時機就是提醒發出去之後 ——
     **從「已預約」變成「未回覆」這件事本身就是「我們問過你了」**，不必再用字寫一次。
     ⚠ 代價：他幾天後打開查詢時可能忘了被問過什麼，**但補那件事的位置不是這個標籤**
     （這張卡沒有按鈕，寫了只會讓他想回卻回不了 ＝ 39-10 那個圈子），
     真要補是在提醒卡自己。
     **四個值定案：已預約／已確認／未回覆／已取消** —— 都是三個字、都在講這一筆走到
     哪一步、主詞都不是診所、都沒有答應任何事。
   ・⚠⚠⚠ **2026-09-12 再一輪：那一欄叫什麼 —— 「預約狀態」換成「目前狀態」**
     （使用者：「那回到這個項目　廠商現在寫預約狀態　要改成什麼才是精確詞語不會有
     誤會或是糾紛產生」，README **第 50-12 節**）。⚠⚠ **「糾紛」是新加進來的字**
     （49-1 他講的是「不夠精確」），所以這個名字要過的不只是讀不讀得懂，
     還有**它自己有沒有宣稱任何事**。
     **「預約狀態」兩個地方會出事、兩個各自獨立**：配「已預約」是**同一件事講兩次**；
     配「已取消」是**標題和值互相矛盾**（標題說這是某個預約的狀態、值卻說那筆預約
     已經不存在了）—— 後者正是他自己 49-1 講的那一句。**問題不在「狀態」兩個字，
     在「預約」那個名詞把那一列宣告成一筆仍然有效的約診。**
     ⚠⚠ **一個標籤要過五關**（挑名字時逐條跑一次）：① 四種值都裝得下（50-1）
     ② 不可以宣稱那一筆還存在（49-1）③ 不可以答應任何事（50-10，值不行、名字更不行）
     ④ 主詞不可以是診所（49-3）⑤ 不可以是步驟的名字（49-6）。
     **定案「目前狀態」** —— 50-1 已經收到乙組，這一輪在乙組裡再收一步：
     ⚠ **不寫「目前狀況」是因為這是牙科的卡片，「狀況」會有人讀成他嘴巴裡的狀況**，
     「狀態」連這個讀法都沒有；⚠⚠ **「目前」兩個字不要為了短而砍掉** ——
     它做的事和 50-10 同一件，**一個會被截圖下來的畫面上，時間限定就是責任限定**。
     ⚠ **要改的只是卡片上印出來的那個標籤，資料庫欄位叫什麼名字不必動**（49-6）。
     ⚠⚠⚠ **兩層的順序沒有變**：第一層仍然是 41-8（那一筆能不能就不要列出來）——
     做得到的話第 ② 關那個矛盾根本不會發生，所以那一列仍然寫著「那一列若移不掉」。
   ・⚠⚠ **2026-09-12 再一問：「本次預約」評過了，不建議**（使用者：「如果改成
     本次預約　呢？」，README **第 50-13 節**）。**它好的那一半是真的**：
     那是一個**指涉語**不是斷言，所以「本次預約：已取消」通順、**第 ② 關過得了**
     （＝「預約狀態」最嚴重的那一半被它修掉）。**但它卡在第 ① 關而且更明顯**：
     「本次預約：已預約」是同一件事講兩次，而那是**全表最常出現的一格**。
     ⚠⚠⚠ **更根本的：它是「這一列的名字」不是「這個欄位的名字」** ——
     `標籤：值` 裡標籤的工作是說出那個值**是什麼種類的東西**（「目前狀態」說值是一個
     狀態；「本次預約」說值是一筆約診，可是值不是）。它讀得通是**讀的人自己補的**，
     而**卡片上方本來就印著姓名與那一筆的日期** ——「這一列在講哪一筆」已經講過了，
     用標籤再講一次等於**把唯一能解釋那個值的位置拿去重複一件已知的事**。
     **維持「目前狀態」**；它要成立只有一種情況 —— 第 1 種不是「已預約」，
     而那四個值 50-11 才定案。
   ・⚠⚠⚠ **2026-09-12 再一問：「本次預約：已建立」—— 換值救名字，順序反了**
     （使用者：「第1種狀態　就寫　本次預約：已建立　這樣呢」，README **第 50-14 節**）。
     **他解掉的那一關是真的**：50-13 擋住「本次預約」的第 ① 關就是
     「本次預約：已預約」同一件事講兩次，值換成「已建立」那個重複就沒有了。
     ⚠⚠ **但「已建立」在上一輪就被同一族否決過**：50-10 的落選名單裡有
     **已排定／已登記**，理由是「行政系統的詞」——**建立資料、建立帳號、建立紀錄**，
     那是記錄系統在做的事。⚠⚠ 所以它把 **49-3 那個「同一欄兩個主詞」的毛病
     縮小版又放回來**（已預約／已確認／未回覆／已取消四個都在講病人，
     只有「已建立」講系統或櫃檯）；⚠ 從病人那一側看也不對 ——
     **他沒有「建立」什麼，他是約了一個時間**。
     ⚠ **而且標籤的另一半沒有被治到**：50-13 擋它的還有第二件、和值無關 ——
     它是「這一列的名字」不是「這個欄位的名字」，而卡片上方本來就印著姓名與日期。
     ⚠⚠⚠ **真正的通則是 50-1 的反向版**：「一個欄位有哪幾種值，決定它叫什麼名字」，
     所以**不要為了救一個名字去換一個已經定案的值** —— 那四個值 50-11 才逐條走完
     形狀、主詞、不答應任何事三關。**維持：欄位名「目前狀態」、第 1 種印「已預約」。**
     ⚠ 「已建立」配「目前狀態」也不會比較好（讀起來是一筆資料的狀態，
     不是他那次約診的狀態）——那個值不管配哪一個標籤都偏行政系統那一側。
   ・⚠⚠⚠ **2026-09-12 定案：欄位名「約診狀態」、第 1 種印「已排定」**
     （使用者：「約診狀態　1. 已排定　2. 已確認　3. 未回覆　4. 已取消」，
     README **第 50-15 節**）。⚠⚠ **這是一個決定不是兩個** —— 只要標籤裡有
     「預約」或「約診」，第 1 種就不能印「已預約」（同一件事講兩次，而那是全表
     最常出現的一格），所以**標籤與第 1 種的值只能一起定**。
     ⚠⚠⚠ **「已排定」可以而 50-14 的「已建立」不可以，差別在主詞**：
     已排定講的是**那個時段被排好了**（不指任何人）、已建立講的是**誰把這筆資料
     建起來的**（系統或櫃檯）。**所以 50-14 那條通則要收窄一階**：不是
     「不要為了救名字去換值」，是**「換上去的那個值要自己過得了形狀、主詞、
     不答應任何事那三關」**。
     ⚠⚠ **50-10 的落選名單因此要更正**：那一輪的標籤是「目前狀態」、沒有重複
     要拆，所以「已預約」以主詞最貼近病人勝出，**已排定只是輸在那一格、不是有缺陷**。
     **通則：一個值的落選理由要連「當時和誰比、比的是什麼」一起記，換了脈絡
     才知道那個理由還在不在。**（「已登記」仍然出局：登記是櫃檯在做的事。）
     ⚠⚠⚠ **五關裡第 ② 關沒有被治掉，但它不是這個標籤帶進來的** ——
     標題說這是約診的狀態、值卻說那一筆已經取消了，**而那個矛盾在卡片自己的
     標題上本來就有**（那張清單就叫「已約診時段」，取消掉的那一筆也列在裡面）；
     換成「目前狀態」只是讓標籤這一行不參與，**卡片整體的矛盾一個字都沒少**。
     **治得掉它的只有第一層那一題（那一筆不要列出來），兩層的順序仍然沒有變。**
     ⚠ 換到的是「約診」正好是這張卡標題與這條線其餘每一處在用的詞
     （約診紀錄查詢、已約診時段），而 50-1 把「目前狀態」歸在乙組時本來就
     記著它的代價是「幾乎沒有說任何事」。
     ⚠ 要改的只是卡片上印出來的那個標籤與那幾個字，資料庫欄位不必動（49-6）。
   ・⚠⚠⚠ **2026-09-12 再一輪：那四個值要套色、要加粗，而且要兩套色票的版本**
     （使用者：「原始的版本　我發現　**確定會到是綠色字**／所以在新版上　我覺得
     也要套色　粗體也比較明顯／已排定 藍色／已確認 綠色／未回覆 橘色／已取消 紅色／
     但顏色用**這個整理頁的 logo 色**和**門診**兩個版本做給我看」，
     README **第 50-16 節**，頁面第 ④ 節）。
     ⚠⚠ **套色不是新功能，是把現在就有的做法接過去**（那張卡上「會到」本來就是綠字）——
     **通則：提一個新要求之前，先看對方的成品上有沒有同一件事的現成例子，有的話要
     講成「照你們已經在做的那樣」**（30-2 那條「對方說做不到就去他的成品上找反例」的正向版）。
     ⚠⚠ **兩版挑的是同樣那四科**（齒顎矯正藍／一般牙科綠／兒童牙科橘／顯微根管紅），
     **差的只有色票本身** —— 不然一把尺同時動了「挑哪一科」和「用哪一階」兩件事。
     ⚠⚠⚠ **差別是階不是好看**：Ⓐ 那九顆是 PALETTE 的**套色**（整塊填滿用的），
     而這一格是**淺底上的字** —— 藍 4.15、橘 2.93 兩顆掉到 4.5 底下；
     Ⓑ（門診表的 `mix`）本來就是挑給小圖形與字的，只有橘 4.49 差一點點。
     ⚠ **橘是兩版共同最緊的一顆**：站上兒童牙科還有一階更深的 `#9E6301`（4.50 剛好過），
     只換這一顆就好 —— **沒有自己換，第三版會讓那把尺變髒**。
     ⚠ **加粗建議跟著做，但標籤維持不加粗也不套色**（兩樣都動那一列就沒有主從）。
     ⚠⚠ 他看的那張畫面是**診所端的後台**不是病人的手機（同 20-10、22-1）——
     顏色與粗體看得出來，**字的大小與行距不能拿那張圖去量**。
     ⚠⚠⚠ **對比度在產生器裡現算、不寫進資料**（寫死的話換一顆顏色數字不會跟著動），
     而且要**無條件捨去**到小數第二位 —— 四捨五入會印出「4.50（低於 4.5）」這種
     自相矛盾的一行。
     ⚠⚠⚠ **踩到一個只有守門抓得到的**：解釋捨去理由的那段註解寫在**樣板字串裡面**，
     整段被印進 HTML，版面與尺寸完全正常，是守門 ⑨ 掃到註解裡的「說謊」才停下來。
     **通則：產生器裡的註解要寫在樣板字串外面** —— 裡面的斜線星號不是註解，是內容。
   ・⚠⚠⚠ **2026-09-12 定案 Ⓐ，並回答「做成藥丸」那一題**（使用者：「**A 比較好，選 A**　
     我有點好奇　這裡可以做成像網站的**藥丸標籤底套色白字**嗎」，README **第 50-17 節**）。
     ⚠⚠⚠ **他問的不是外觀是階** —— 站上的規矩是「深階給淺底上的字、套色給填實的塊」，
     而 Ⓐ 那九顆就是**套色**，50-16 把它們當字用本來就是用錯階（藍 4.15、橘 2.93）；
     **填成塊配白字才是那一階本來的用法，四顆全部變好、三顆過 4.5**
     （4.56／6.62／3.22／5.21）。**通則：一個「可以這樣做嗎」的問題，先問它問的是不是
     一個階的問題 —— 是的話，答案往往同時修掉上一輪的取捨。**
     ⚠⚠ **兒牙那一顆 3.22 不要訂正**：那正是站上首頁兒童牙科標記按下去的樣子，
     2026-08-08 已定案的取捨；**也不要單獨換成更深的橘**（會讓四顆不再是同一組顏色，
     而「同一組」是 50-16 挑色票時的整個前提）。
     ⚠ **Flex 做得到、不是新元件**：box 給 `backgroundColor`＋`cornerRadius`＋`paddingAll`，
     裡面一個白字 text；要讓它只包住那幾個字，那一列寫成 horizontal box、
     藥丸那一格 **`flex: 0`** ＋ 後面補一個 `filler`。圓角照站上那一族 8.5px。
     ⚠⚠ **兩件代價寫在頁面上**：藥丸會是整張卡唯一一塊填滿的顏色、**比日期還重**
     （那張卡是收據，日期才是主角）；以及對廠商從「換一個色碼」變成「多包一層 box」，
     **要求大一階**（同第二十節：要求的改動愈小愈可能真的改到）。
     ⚠ 標籤維持不加粗也不套色，四個值的字、欄位名與卡片其餘每一處一個字都沒動。
   ・⚠⚠⚠ **2026-09-12：那一列套回我們自己的卡**（使用者：「把這個約診狀態的調整
     **套回我們的設計上**　讓我看看效果」，README **第 50-18 節**）——
     `/preview/line-booked/` 第 **②之二** 節：同一張輪播卡畫兩次（Ⓐ 字套色／Ⓑ 藥丸），
     **四張卡各掛一個值**（一張卡看不出四種的相對份量）。
     ⚠⚠⚠ **那一列不在定稿裡，也沒有放回去** —— 22-11 使用者指定整行拿掉，第 ② 節
     那張卡一個字都沒動；第四十九、五十節整串的前提是**條件句**「廠商那個欄位移不掉的話」。
     ⚠⚠ 顏色從 `wm-sizes.json` **按科**讀回來、對比度現算（不寫死）。
     量到卡片 207px、那一列可用 179、字套色最寬 99、藥丸最寬 115，五個寬度逐格相同。
     ⚠⚠⚠ **踩到那條記過四次的**：輪播卡的 207px 寫在**用 id 列出來**的三條選擇器裡
     （`#pv-slot.car .pv-hc,#pv-slot2.car …`），新 slot 沒加進去就退回 `width:100%`、
     畫成 **218.6px**，畫面完全正常 —— 而在一張 218.6 的卡上判斷「放不放得下」全是假的。
     **通則（第五次）：那一側寫死的尺寸我們也要寫死，而且要再問一次「它是用什麼選到的」。**
     ⚠⚠ 第二個坑在量測：`.st` 是 flex nowrap、**永遠不會折行**，第一版照 rect 的 `top`
     數行數，藥丸有內距、top 本來就不同，四列全被報成兩行（39-9 的近親）。
     **通則：量之前先問「這個排版底下有沒有可能發生我要量的那件事」。**
     ⚠ `#pv-slot3`／`#pv-slot4`（現況那兩張）也不在那三條選擇器裡（310.6／334.5px），
     那兩節比的是內容不是寬度、又是他看過的畫面，**這一輪沒有順手改**。
   ・⚠⚠⚠ **2026-09-12：藥丸垂直置中，再多一版「淡墨色的浮水印」**（使用者：
     「藥丸要與約診狀態垂直置中　另外再做一版有藥丸的版本　但 logo 淡墨色
     因為藥丸和有顏色的 logo 互相吵架」，README **第 50-19 節**）。兩件事互相獨立。
     **① 藥丸低 1.40px，他是對的** —— 逐像素量（標籤那幾個字的墨的**重心**對上塊心，
     四個寬度 × 四個值）。⚠⚠ **改 `align-items` 一點用都沒有**（實測仍然 1.36）：
     藥丸本來就是那一列最高的，塊心等於列心；**成因是塊高是從「行框」長出來的**，
     而行框對的是字型的 ascent／descent、中文方塊字上下用不到那麼多 ——
     **＝ 站上 `/history/spec-tag-fit.html` 那一輪同一件事**（第九節第 9 條第三小節），
     解法照抄：**`line-height: 1`，塊高改由上下兩個內距各自負責**，
     定案 **`padding:.42em .63em .38em`**（上多 .04em，塊心低 **+0.08px**）。
     ⚠ 那 .04em 是掃出來的：上下都 .40em 還差 +0.32，**而再往上挪到 .46／.34
     會讓標籤的下伸部掉到藥丸下緣外面、把整列撐高**（列高 23.77 > 塊高 23.38）。
     ⚠⚠⚠ **量這件事三個坑**：**canvas 的 `measureText` 和版面用的不是同一支字型**
     （這個容器拿不到 Noto Sans TC，算出來差 1.66px，第一版整張掃描表正負號都錯）／
     **門檻式的「墨帶上下緣」會被抗鋸齒騙**，DPR 1 上還會量成整數 px（同一組值量出
     12.00／11.00／10.00 三種）→ 改成**用「比卡色暗多少」當權重算重心** ＋ DPR 8／
     **Blink 把塊內基線進位到整數 px**，所以改內距時標籤會跳一整格，**一次只量一個
     寬度會被騙**。⚠⚠⚠ **面板因此不現算「差多少」**：站上那個「字面中線 ＝ 基線上方
     0.345em」是量 Noto Sans TC 得到的，在這裡偏 0.65px，現算會報出「偏低 0.73px」的
     **假警報** —— 改成現算**幾何**（行高、上下內距、塊高、基線離塊上下緣多遠）
     ＋「不是定案那一組就標紅」，差多少那一格寫逐像素量到的值並寫明方法。
     **通則（又一次）：面板要標對級別，不然它就從量尺變成一直在勸。**
     ⚠⚠ **Flex 那一側複製不了這一招**（`text` 沒有行高可以設，box 裡那個字怎麼坐是
     LINE 決定的）—— 我們寫得出 paddingTop／paddingBottom，但**要一支測試訊息**。
     **② Ⓒ ＝ Ⓑ 只換浮水印的顏色**（形狀、寬度、alpha、位置一個數字都沒動），
     換成站上的柔墨 `#5c5f57`、**沒有新增顏色**。⚠⚠⚠ **濃度要落在那九顆的份量帶裡**：
     九顆在 .12 疊到卡片色是 **L\* 89.63~91.75**，柔墨 .12 是 **89.90** ✅（而且 alpha
     和九顆一模一樣 ＝ 只有顏色變了），墨 .12 是 87.75 ❌ 比九顆每一顆都重，
     墨 .08 是 90.59 ✅ 但要換 alpha。⚠ 柔墨 .12 與墨 .08 只差 0.7 L\*，
     **所以沒有做成一把尺**（差不出來的尺比沒有尺還糟）。成品 `wm-<形狀>-ink12.png`
     九張，同一支 `booked-mark.mjs` 產，守門照舊（墨 10.90／柔墨 5.02，同九顆那一級）。
     ⚠ **`#pv-slot7` 三條 id 選擇器都補上了**（第五次踩過的那一條，守門新增一道在盯）。
     ⚠ 守門新增四道並負向測過：九張 ink12 都在／`#pv-slot7` 在那三條選擇器裡／
     卡片真的在吃 `wmink`／**藥丸的行高與上下內距是定案那一組**
     —— 後兩道擋的都是「改回去畫面照樣正常、每一道尺寸守門都會過」。
   ・⚠⚠⚠ **2026-09-12：約診狀態和藥丸「底部對齊」**（使用者：「約診狀態　和藥丸的
     底部對齊呢　做給我看」，README **第 50-20 節**）—— 做成 **Ⓓ**，配一條三格的
     對照帶（三列都是真的 13px、真的藥丸，底下一條虛線標著藥丸的下緣）；
     ⚠ **三個候選之間只差 1~5px，單看一張卡分不出來**，所以一定要並排 ＋ 一條基準線。
     ⚠⚠⚠ **`align-items: flex-end` 只買到 1px**（墨 5.52 → 4.52）—— 它對齊的是
     **兩個盒子**，而盒子的下緣對齊之後**已經完全一樣了**（面板現場量到 0.00px），
     可是看起來還差 4.5px：**標籤那個盒子的下緣底下還有一整段下伸部的空間**
     （行高 1.6 的半行距 ＋ 字型的 descent），中文方塊字用不到。
     **眼睛讀的是墨，`align-items` 對的是盒子。**
     ⚠⚠ 所以**兩邊都要把行框收掉**：藥丸那一側 50-19 已經做了，這一輪是**標籤也收**
     （`.st.bot .lb{line-height:1}`），收完剩 **0.52px** ＝ 那顆字型的 descent 在 13px
     上的殘餘；再往下挪就要動 `vertical-align`，那會讓「對齊」變成一個寫死的補償值。
     ⚠ **和第九節第 9 條同族但不是同一件事**：那一條治「填色的塊相對於旁邊的字偏低」
     （塊的問題），這一輪治「兩樣東西的下緣要切齊」（列的問題）——**兩個都做完才對**。
     ⚠⚠ **Flex 那一側複製不了**（同 50-19）：`text` 沒有行高可以設，那個字怎麼坐在
     盒子裡是 LINE 決定的；我們寫得出 paddingTop／paddingBottom，**要一支測試訊息**。
     ⚠ **Ⓑ′（只加 flex-end）擺出來是對照不是候選** —— 沒有它，「為什麼還要收標籤的
     行高」講不清楚。⚠ 面板**不現算「墨差多少」**（同 50-19：這個容器沒有 Noto Sans TC，
     現算會報假警報），現算的是**盒子**的下緣差。
     ⚠⚠ **`#pv-slot8` 又踩到那條記過五次的**，而**守門那一道本身也寫錯兩次**：
     **不可以用 `"#pv-slot8" + ".car{"` 去找那一行** —— 逗號串起來的清單裡，
     只有**最後一個** id 後面才接著大括號；改成**用宣告的內容認出那一行**
     （`flex:none;width:207px`）再逐個 id 檢查。
     ⚠ 守門新增三道並負向測過：那兩條 CSS 在、那一列真的在吃 `stbot`
     （拿掉的話 Ⓓ 會畫成和 Ⓑ 一模一樣，而每一道尺寸守門都會過）。
   ・⚠⚠⚠ **2026-09-12：第 ②之二 節收成一件事**（使用者：「保留 logo 墨色　藥丸白字
     　ABC 版本的高度　其他就不用了」，README **第 50-21 節**）—— 頁面上只剩**兩塊**：
     **一張卡**（底套色白字的藥丸 ＋ **淡墨色的浮水印** `wm-<形狀>-ink12.png`）
     ＋ **三種高度並排**（＝ 50-20 那條對照帶，標籤改成他講的 **Ⓐ／Ⓑ／Ⓒ**：
     基線／只加「靠下對齊」／底部對齊）。拿掉的三格：Ⓐ 字套色、Ⓑ 彩色浮水印的藥丸、
     Ⓓ 那張整張的底部對齊卡。
     ⚠⚠ **卡片維持基線對齊（＝ 對照帶的 Ⓐ）** —— 那三種他還沒挑，對照帶是量尺、
     卡片是現況；`stbot` 因此整個拿掉（對照帶自己寫 class，不經過 `nwCard`）。
     ⚠ **「字套色」的程式碼留在 `nwCard` 裡**（`stat === "txt"`）、**頁面上不再畫它**，
     而它落選的那四個數字（藍 4.15／橘 2.93）**面板仍然每次現算** ——
     **落選理由不要跟著畫面一起消失**。
     ⚠⚠⚠ **第六次踩到那條 id 選擇器，而且是反過來的**：輪播的 207px 寫在**用 id 列出來**
     的三條規則裡，這一次是**刪 slot 之後那三條要跟著收** —— 留著不存在的 id 不會壞，
     **但守門那一道還在逐個檢查它們**，等於開始驗頁面上根本沒有的東西。
     **通則：那種「用 id 列出來」的規則，加 slot 要加、減 slot 也要減；而守門的清單是
     同一份事實的第二個副本，兩邊要一起改。**
     ⚠ 守門換掉三道並負向測過：id 清單收成 `#pv-slot7`／`stbot` 那一道換成
     **對照帶三列的 class**（少一個就是三列一模一樣，而每一道尺寸守門都會過）／
     新增「那一張卡一定要是 `stat = "pill"; wmink = true;`」與「不准再出現 `stat = "txt"`」。
   ・⚠⚠⚠ **2026-09-13 定案：對齊選 Ⓐ 基線，定案整合進廠商那一頁**
     （使用者：「對齊選擇 選Ａ定稿　把定稿的內容整合回去先前12項目的整合頁面內」，
     README **第 50-22 節**）。**兩件事各自獨立。**
     **① 尺收掉、數字留下**：`/preview/line-booked/` 第 ②之二 節那條**三列的對照帶
     整組拿掉**（`.st.bot`／`.st.botbox`／`.pv-cmp` 三段 CSS 與建帶子的 JS），
     只剩一張卡 ＋ 一段面板。⚠⚠ **落選那兩種的數字沒有跟著畫面一起消失** ——
     `alignNote()` 改成量**卡片上那一列本人**，後面原樣印出三個逐像素量到的數字
     （**基線 5.52px（定案）／只加「靠下對齊」4.52px／連標籤的行框也收掉 0.52px**）。
     **通則：一把尺收掉之後，它量出來的東西要留在面板上，不要只留在 git 裡。**
     ⚠ 守門換掉三道並負向測過（那一列一定要 `align-items:baseline`／落選那兩種的
     class 都不准出現／面板要印得出那三個數字）；⚠⚠ 第一次負向測時發現
     **「把 `bot` 加回 class」擋不下來**（那時只掃 CSS 選擇器），所以**標記那一側
     也要有一道**（`h += '<p class="st">'`）。
     **② 整合進 `/preview/line-vendor/`**：第 ④ 節從「過程」改成**定案在前、過程在後**
     （資料加一塊 `狀態色.定案` —— 七條 ＋ 前提 ＋ 還要一支測試訊息），
     並把 **① 那張表 ⑦ 那一列**接到這裡（那張表一則一格，不接的話讀的人找不到定案在哪）。
     七條：欄位名 **約診狀態**（改的只是印出來的標籤，資料庫欄位不必動）／
     四個值 **已排定／已確認／未回覆／已取消**／色票 **Ⓐ 整理頁的 logo 色**／
     畫法 **底套色白字加粗的藥丸**（horizontal box ＋ `flex: 0` ＋ `filler`）／
     對齊 **基線**／標籤維持不加粗不套色／浮水印換成**淡墨色**；
     前提那一條照舊：**第一層仍然是「那一筆能不能就不要列出來」**。
     ⚠⚠⚠ **順手抓到一個第二真相**：那一頁的 `.stcard .pill` 還是 50-19 修掉之前的幾何
     （`line-height:1.6` ＋ 上下對稱），**同一顆藥丸在兩頁長得不一樣、而且低 1.40px**。
     **通則：一段規格搬到第二個地方之後，那一頁上原本只是示意的東西就變成規格的一部分 ——
     要逐項回頭問「這裡畫的和那邊定案的是不是同一個數字」。**
     ⚠ 守門 ⑬（三種負向測過）：定案那七條**寫死名字**在資料裡找一次
     （只照 `SET.條` 跑迴圈的話，**資料少一條迴圈也跟著少一圈，那一道等於沒開**）
     ＋ 頁上都印出來了 ＋ 藥丸的行高與上下內距 ＋ 那一列是基線對齊。
   ・⚠⚠⚠ **2026-09-13：③ 那一格從頭到尾是空的**（使用者：「③『綁定成功』那一句……
     這裡的圖片是空白的　但就是下面 ⑦ 綁定完成：兩個方向的第一張圖片　放上去」，
     README **第五十一節**）。`vendor-log.json` 裡 ③ 的 `圖` 寫的是 `""`，而 2026-09-11
     整節改成「一格一則、左邊擺那一則自己的圖」時**前提就是「圖就是那一則的名字」**。
     ⚠⚠ **每一道守門都會過**（沒有 `<img>` 就沒有尺寸可以對、沒有多出來的檔、
     溢出與連結都正常）—— **只有把頁面打開看才看得到**。
     → 守門補一道 ⑧（**① 那張表每一列都要有圖**：空字串、空陣列、陣列裡有空的都 throw），
     負向測過。
     ⚠⚠⚠ **縮圖的裁法要重新問**：來源是第 ⑦ 節那張 `bind-live-0912.jpg`（750×1551），
     而 `vendor-shots.mjs` **一律從左上角裁一塊正方** —— 這一張的**前四百列是上一次對話
     的連結預覽卡**，照預設裁出來是「一張灰色的卡」，**那一格看起來仍然像沒有圖**。
     `SHOTS` 因此多一個選填的 `y`（從第幾列開始裁），這一張給 **350** ＝
     「今天 → 綠泡泡的綁定成功 → 底下那一則自動回應」。
     **通則：一張縮圖的裁法由「它要代表哪一句話」決定，不是由檔案的第一列決定** ——
     前十四張都是卡片（上緣就是頭圖與開場），這一張是**聊天室的截圖**、那一段在中間。
     **來源換了型別（卡片 → 對話），裁法要重新問一次。**
     ⚠ 順手改掉一句過期的：那一列寫著「見第 ⑥ 節的圖」，而那一節現在是**第 ⑦ 節**
     （中間插過新的一節、號碼位移）——**頁內互指用的是會位移的號碼，插一節就要掃一遍。**
