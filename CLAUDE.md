# 芳仁牙醫診所部落格 — 給 Claude Code 的專案守則

雲林縣斗六市的牙醫診所部落格。純靜態網站，**零 npm 依賴**，只用 Node 跑幾支腳本。
正式站 https://fangren.net（Cloudflare Worker + 靜態資產）。

這份檔案是給 AI 助理看的。人類用的說明在 [README.md](README.md)。

> **要動到任何顏色之前，先讀 [PALETTE.md](PALETTE.md)。**
> 那份是配色的唯一依據，每個色值都是從診所自己的照片量出來的（含官方色票、
> 材質實測、對比度實測、版面規則）。**不要另外憑感覺挑色，也不要繞過裡面的
> 對比度限制**（例如 `#A1A398` 量出來撐不住文字，就不要拿它當文字）。
>
> **要寫或改任何對外的品牌文字之前，先讀 [COPY.md](COPY.md)。**
> 那份是文案的唯一依據：HERO 那首詩的寫作規則、品牌定位、**不能寫成什麼**（有紅線），
> 以及正在進行中的收尾候選。使用者對這些句子已經來回修過好幾輪，
> **不要憑感覺重寫，也不要從舊對話裡抄。**
>
> 跑在 **手機／雲端 session（claude.ai/code）** 時：容器看不到使用者電腦上的照片資料夾，
> 也不能執行 `tools/palette-measure.ps1`。這**不影響**配色工作 —— 需要的數字全都已經
> 量好寫在 PALETTE.md 裡了。不要因為讀不到照片就退回目測配色。

---

## 一、不可更動的架構決策

以下是使用者明確要求過的，**不要「順手改善」掉**：

1. **首頁文章列表必須是靜態 HTML。**
   卡片由 `tools/build.mjs` 寫進 `index.html` 的 `<!-- POSTS:START -->` / `<!-- POSTS:END -->`
   區塊。**絕對不要**改成前端 JavaScript 讀 `posts.json` 再動態產生 — 這是刻意的決定，
   為了 SEO 與無 JS 環境。
2. **每篇文章是獨立網址** `/posts/<slug>/`，同一個網域底下，不是 hash route、不是查詢字串。
3. **「最後更新日期」由腳本維護，不要手改。** `tools/build.mjs` 用內容雜湊比對，
   發現文章被改過才把日期換成當天，並依此重新排序（最新在前）。
   `post-meta` 裡的 `updated` 欄位人工改了也會被蓋掉。
4. **部署是 Cloudflare Worker，不是 Pages。** `wrangler.toml` 用 `main = "src/worker.js"`
   ＋ `[assets] directory = "_site"`。不要改成 `pages_build_output_dir`，那是 Pages 專用的，
   會讓建置失敗於 "Missing entry-point"。
5. **文章 HERO 圖寬度不得超過內文欄寬**，且必須等比例縮放。
6. **手機版排版不可破框**（無水平捲動）。改完 CSS 要用 375px 寬檢查。
7. **HERO 圖若使用第三方素材需符合 CC BY**，並在 footer 標示出處。目前照片皆為診所自有。

---

## 二、工作流程（每次動手前後都要做）

### 開工前一定要先同步

這個 repo **有兩個人同時在推 commit**，兩台電腦各自跑 Claude Code。動手前：

**這件事已經自動化了** —— `.claude/settings.json` 的 SessionStart hook 會在每次開啟專案時
自動執行 `node tools/sync.mjs`（等同 `git pull --rebase --autostash`），並把結果顯示給使用者。
所以正常情況下不必再手動 pull。

但如果一個 session 開很久，中途仍要自己補一次：

```bash
node tools/sync.mjs
```

已經發生過 push 被擋、要 rebase 的情況。另外：**未追蹤的本機檔案若和遠端新增的同名檔撞到
會擋住 checkout — 先把本機檔移走再 rebase，不要直接刪掉使用者的檔案。**

### 改完之後

```bash
node tools/build.mjs      # 一定要跑，否則首頁卡片與 sitemap 不會更新
git add -A && git commit -m "..." && git push
```

push 到 `main` 之後 Cloudflare 會自動建置部署，幾分鐘內上線。
**不需要**手動跑 `wrangler deploy`，也不需要 Cloudflare token。

因為 `index.html` 和 `tools/build-manifest.json` 是 build 產物，兩人同時改必然衝突 —
所以「改完 → build → 立刻 push」，不要累積一堆本機 commit。

### 本機預覽

```bash
node tools/serve.mjs      # 預設 http://localhost:8791
```

計數器 API 在本機沒有 Worker 可跑，會**自動隱藏**，網站其他部分完全正常，這是預期行為。

### 使用者的口語指令

使用者不熟悉終端機，會用中文交代。看到這些說法就直接執行對應動作，不要只是把指令貼給他：

| 使用者說 | 你要做的事 |
| --- | --- |
| 「上線」「發布」「推上去」 | `node tools/build.mjs` → `git add -A` → commit（訊息自己擬）→ `git push` |
| **「定案幫我上線」「定案幫我放上去」** | **把目前這條工作分支合併進 `main` 並推上去**，見下面那一段 |
| 「同步」「更新」「拉最新的」 | `node tools/sync.mjs` |
| 「預覽」「我要看看」 | `node tools/serve.mjs`，告訴他開 http://localhost:8791 |
| 「網站好了嗎」 | 確認已 push，提醒 Cloudflare 需要幾分鐘，網址是 https://fangren.net |

上線前若發現遠端有新 commit，先同步再 build、再推。

#### 「定案幫我上線」＝ 合併到 `main`（2026-08-09 起）

雲端 session 的工作都在 `claude/...` 分支上，**Cloudflare 只建置 `main`**，
所以推到分支等於還沒上線。使用者說這句話的時候，他要的是**真的出現在 fangren.net 上**：

```bash
git fetch origin main
git log --oneline HEAD..origin/main      # main 有沒有被另一台推過東西
node tools/build.mjs && git add -A && git commit -m "..."
git push origin <工作分支>                # 先把分支自己推上去
git merge-base --is-ancestor origin/main HEAD && \
  git push origin <工作分支>:main         # 遠端直接快轉，不必 checkout main
```

> ⚠ **不要 `git checkout main && git merge --ff-only`**（2026-08-11 踩到）。
> 雲端 session 的容器裡那個**本機 `main` 常常是別的東西** —— 實際遇到的是
> 689f1f0，對 `origin/main` 「ahead 50, behind 50」，和工作分支**沒有共同祖先**，
> merge 直接回 `refusing to merge unrelated histories`。
> 那條舊線並沒有不見（掛在 `origin/claude/footer-icons-cutoff-9nk1wa` 上），
> 所以**也不要順手把本機 main reset 掉**。
> 上面那條 `<分支>:main` 的推法完全不碰本機 `main`，而且 `--is-ancestor` 先擋一次，
> 快轉不了就會停下來（那時才需要先 rebase 分支）。

推完告訴他 Cloudflare 要幾分鐘，網址是 https://fangren.net。

**動手前先掃一遍要合併的東西，把還沒定案的擋下來。** 分支上常常同時躺著
「已定案並套進版型的」和「還在提案中的」；`PALETTE.md`／`COPY.md` 裡標著
**提案中**、**未定**、**三案待選** 的段落是文件，跟著上去沒關係，
但**不要把還沒定案的顏色或文案套進 `index.html`／`assets/style.css`／`posts/` 再合併**。
不確定就先問他是哪一件定案了。

`preview/` 底下的提案頁**跟著上去是正常的**（本檔第八節：那本來就是給他在手機上看的，
`robots.txt` 與各頁的 `noindex` 已經擋掉搜尋引擎）。

---

## 三、環境需求

- **Node ≥ 20**（`package.json` 的 `engines` 有寫）。不必 `npm install`，沒有任何依賴。
- **git 身分要在 repo 層級設**。全域沒設，不設會 commit 失敗：

  ```bash
  git config user.name "YCLee86" && git config user.email "eugenelee0806@gmail.com"
  ```

- `gh auth login` 即可推送。**這台/那台機器的 gh token 沒有 `workflow` scope**，
  推不了 `.github/workflows/`；`.gitignore` 也直接排除了 `.github/`，所以 repo 內沒有 workflow，
  不要嘗試新增。
- wrangler 只有直接手動部署或操作 D1 時才需要，日常編修用不到。

---

## 四、目錄與檔案

```
index.html              首頁。POSTS 與 SEO 兩個區塊由 build 產生，其餘手寫
PALETTE.md              配色規範。動任何顏色之前先讀這份
COPY.md                 文案規範。動任何品牌文字之前先讀這份
clinic.json             診所這個「實體」的資料（sameAs、座標、郵遞區號…）。
                        **只放頁面上沒有的東西** —— 看診時間、醫師、科別一律回頭讀 index.html
404.html
favicon.ico             根目錄的點陣圖示，**只給 Google 的圖示爬蟲**。
                        由 tools/favicon-ico.mjs 從 assets/favicon.svg 算出來，已進版控。
                        ⚠ 刻意不在任何 <head> 宣告 —— 見 PALETTE.md 第六之七節
site.json               網站正式網址（給 sitemap 用）
wrangler.toml           Worker、靜態資產、自訂網域、D1 綁定
d1-schema.sql           計數器資料表定義，執行一次即可
src/
  worker.js             計數器 API ＋ www 轉址 ＋ /preview/* 密碼閘
  allowed-slugs.js      白名單，由 build 產生，勿手改
assets/
  style.css             全站樣式
  counter.js            前端計數器
posts/<slug>/index.html 一篇文章一個資料夾
preview/<name>/index.html  未上線的改版提案頁，要密碼才看得到（見第八節）
tools/
  build.mjs             產生首頁卡片、更新日期、排序、sitemap、allowed-slugs、結構化資料
  schema.mjs            JSON-LD 產生器（被 build.mjs 匯入，不單獨執行）。
                        看診時間／醫師／科別都是從 index.html 讀回來的，不另外維護一份
  og-images.mjs         把文章 HERO 的 SVG 轉成 assets/hero-*-1600.png（分享圖）。
                        一次性工具，npm run build 不會呼叫它，只有換了插圖或新增文章才跑
  dist.mjs              把上線檔案組進 _site/
  serve.mjs             本機預覽伺服器
  sync.mjs              同步遠端（SessionStart hook 自動呼叫）
  setup.ps1 / setup.sh  新電腦一鍵環境設定
  palette-measure.ps1   從照片實測取色（k-means ＋ 局部裁切），配色改動的依據來源
  favicon-ico.mjs       從 assets/favicon.svg 產生根目錄的 favicon.ico。
                        只有改過 favicon 的顏色或幾何時才要跑，npm run build 不會呼叫它
  build-manifest.json   內容雜湊紀錄，build 自動維護，勿手改
.claude/
  settings.json         SessionStart hook：開啟專案時自動同步（隨 git 走，兩台都生效）
  launch.json           本機預覽伺服器的啟動設定
```

> `tools/setup.ps1` **必須存成 UTF-8 with BOM**。Windows PowerShell 5.1 沒有 BOM 就會
> 用 ANSI 讀檔，裡面的中文全變亂碼、腳本直接解析失敗。編輯這支檔案後要確認 BOM 還在。
>
> `tools/palette-measure.ps1` 走另一條路避開同一個坑：**整支刻意只用 ASCII**，
> 中文一律放在它讀進來的 manifest（用 `-Encoding UTF8` 讀）。改它的時候不要加中文字面值。

`_site/`、`.wrangler/`、`node_modules/` 都在 `.gitignore` 裡，是產物，不要 commit。

### 自動產生、不要手動編輯的檔案

- `index.html` 的 `<!-- POSTS:START -->` ~ `<!-- POSTS:END -->` 之間
- **每一頁 `<head>` 裡 `<!-- SEO:START -->` ~ `<!-- SEO:END -->` 之間**
  （結構化資料、robots、og:image、article:* 等機器讀的 meta，見第十節）
- **文章頁 `<!-- RELATED:START -->` ~ `<!-- RELATED:END -->` 之間**（底部的「延伸閱讀」三張卡）。
  ⚠ 這一段**在 `</main>` 外面**，是刻意的：`<main>` 在內容雜湊涵蓋範圍內，
  卡片放進去的話，每發一篇新文章就會讓所有舊文的「最後更新」跳成今天。
  它同時會避開該篇「上一篇／下一篇」已經指到的兩篇，不然同一個畫面會連兩次同一篇
- `src/allowed-slugs.js`
- `tools/build-manifest.json`
- `sitemap.xml`（首頁那一筆的 `lastmod` 和文章一樣是**比對首頁自己的內容雜湊**得來的，
  不是抄最新文章的日期 —— 只改首頁、沒發新文章時它也要動，否則等於在跟 Google 說「別來了」）
- `robots.txt`（**每次 build 整個重寫**，要加規則請改 `tools/build.mjs` 的產生字串）
- 各文章 `post-meta` 的 `updated` 欄位

---

## 五、新增或修改文章

新增：複製任一個 `posts/<slug>/` 資料夾，改 `<head>`、`post-meta` JSON 區塊、
`<body>` 內文與 `data-views-self="新代碼"`，然後跑 build。
`slug` 必須與資料夾名稱完全相同。

修改：改完內文跑 `node tools/build.mjs` 就好，日期與排序自動處理。
`node tools/build.mjs --check` 可以只看結果不寫檔。

> 改頁首、頁尾這類全站共用區塊**不會**讓所有文章的日期一起跳成當天 —
> 內容雜湊只涵蓋 `post-meta` 與 `<main>` 之內。這是刻意設計，不要改掉。

`post-meta` 多一個**選填**欄位 `about`（2026-08-10 起），把文章綁到具名的醫療實體上：

```json
"about": [{ "type": "MedicalCondition", "name": "牙周病" },
          { "type": "MedicalProcedure", "name": "牙周翻瓣手術" }]
```

實體要從**那一篇自己的 `<h2>` 與內文**挑，不要憑印象填。沒填就整個略過。
⚠ **改到 `post-meta` 就會動到內容雜湊**，那一篇的日期會跳成今天。
只是補後設資料（內文一個字沒改）的話，照第五節那個陷阱的程序把日期改回去。

底部的「延伸閱讀」三張卡不必管，build 會自己產生並排除上下篇。

### 陷阱：跨全部文章的 `<main>` 內修改

雜湊涵蓋 `<main>`，所以如果一次改動所有文章 `<main>` 裡的共用結構（例如 2026-08-02
移除全站作者署名），`build.mjs` 會把每一篇都判定成「內容更新」，五篇舊文的日期一起跳成當天，
排序也跟著亂掉。

處理方式：build 之後**手動把 `tools/build-manifest.json` 裡那些文章的日期改回原本的更新日**，
再跑一次 build 讓頁面與卡片跟著還原。不要放著不管。

---

## 六、診所資料

地址、電話、看診時間都已填妥，`<head>` 的 JSON-LD 也有 `openingHoursSpecification`。
目前**沒有**任何 `【請填入…】` 佔位符。

- 地址：雲林縣斗六市永樂街 70 號
- 電話：**看得見的是 `(05)5339-369`，機器讀的是 `05-533-9369`**（2026-08-10 定案）。
  三份機器讀的值：`<data value="05-533-9369">`、JSON-LD 的 `+886-5-533-9369`、
  `tel:+88655339369`。括號只用在顯示 —— 和 `+886` 併在一起是矛盾的。
  詳見 [PALETTE.md](PALETTE.md) 第六之十七節。

作者署名與「醫療審閱」欄位已於 2026-08-02 全站移除，**不要自作主張加回去**。
但 `post-meta` 的 `author` 欄位仍是 `build.mjs` 的**必填欄位**（只是不再顯示在頁面上），
新增文章時還是要寫。

---

## 七、已知狀況

- **舊站 https://yclee86.github.io/fangren-dental/ 還活著**，GitHub Pages 直接讀 main 分支
  根目錄，會跟著 push 一起更新。過渡期先留著，之後要處理轉址。
- `run_worker_first = true` 是為了 www 轉址而開的（該選項只吃路徑樣式、不吃主機名稱）。
  代價是 Worker 掛掉會影響整站，不再只有計數器。若要拿回這點，改用 Cloudflare 後台
  Rules → Redirect Rules 做轉址，再拿掉那行與 `worker.js` 裡的轉址。
- `tools/hero-new.css` 與 `tools/hero-preview.mjs` 是還沒套用的 HERO 改版實驗，不是死碼。
  2026-08-05 一併進版控，讓另一台電腦與手機 session 也拿得到。
- **Google 搜尋結果會落後好幾天到幾週，那不是網站壞了。** 使用者 2026-08-09 回報
  搜尋結果的標題、描述、小圖「都不對」—— 三樣逐字比對過，全是站上改版之前的舊快照
  （標題與描述是 08-09 才改的，圖示是 08-08 才換的）。**先去 git log 對一次時間再說**，
  不要因為看起來不對就動 `index.html`。要催快一點只有一條路：Google Search Console
  的「網址審查 → 要求建立索引」，那個後台只有使用者能開。

---

## 八、未上線的預覽頁 `preview/`

給客戶／自己在手機上看改版提案用的。`preview/<name>/index.html` 一個提案一個資料夾，
每份都是**自給自足的靜態快照**：樣式整份內嵌在自己的 `<head>` 裡，
刻意不引用 `assets/style.css`，改預覽時才不會不小心動到正式站。
`build.mjs` 完全不會碰這個資料夾（它只讀根目錄的 `index.html` 與 `posts/`），
所以裡面的文章卡片是靜止的，不會跟著新文章更新。

目前有：

- `preview/home-v2/` — 首頁改版提案，2026-08-03 從 Claude Artifact 匯出。
- `preview/canvas/` — **配色主案**（2026-08-04）。暗夜 HERO × 淺色內文，中間以 3px 木色線
  分界；內文底色三選一。頁內有官方色票、花藝比例與對比度的完整數據，數值同 [PALETTE.md](PALETTE.md)。
- `preview/night-calibrated/` — 全暗版（2026-08-04），內文區也是暗的。留著對照用。
- `preview/hero-ppt-*/` — **手機版 HERO 提案（2026-08-05）**，依使用者的 `簡報20260804.pptx` 做的，
  只做 390px。命名分四組：`a`/`b`/`c`/`c1`/`c3` 是版型，`r1`–`r3` 是右緣裁切，
  `t1`–`t3` 是詩的透明度，`s1`/`s1a`/`s1b`/`s2`/`s3` 是文字陰影。
  **`hero-ppt-s1a` 是使用者定案的那一版**，其餘留著互相比較，各頁底部有交叉連結。
  詩的文字內容與尚未定案的收尾在 [COPY.md](COPY.md)。
- `preview/home-mobile/` — **完整手機首頁**（2026-08-06）：定案的 HERO C72 ＋ 1983／9／6 暗夜窄帶
  ＋ 正式站現有資料補完的一整頁。HERO、窄帶、字級、墨色 `#2a2c27` 都已定版，**不要動**。
  `preview/home-mobile-rule/` 是窄帶分隔線三案。
- `preview/home-mobile-g1` `-g2` `-n1` `-n2` `-n3` — **內文底色提案（2026-08-06）**，
  都是 `home-mobile` 的複本，只換 `--paper` / `--card` / `--rule`。
  `g1` 灰紫、`g2` 灰綠（兩案仍有木線）；`n1`/`n2`/`n3` 是中性灰三案且**木線已拿掉**。
  **`n3` 藍灰調 `#e2e5e6` 是使用者選定的那一版。** 詳細數據見 [PALETTE.md](PALETTE.md) 第四節 C。

  > **`preview/home-mobile-n3/` 已經是這個專案目前最完整的一頁**（2026-08-07 更新），
  > 遠遠超過它原本「只換底色」的定位。現在它還包含：捲動時的固定頁首、
  > 醫師介紹與診所資訊的科別色、跨三節共用的科別標記、點主題與科別可篩選文章與醫師、
  > 換過的頁首標誌。**要接手這個專案就從這一頁開始看。**
  > 規則寫在 [PALETTE.md](PALETTE.md) 第六之二節。

- `preview/home-desktop/` — **電腦版首頁提案（2026-08-07）**。把定案的手機版 N3 套到寬螢幕：
  三欄卡片、單列窄帶、橫排頁首。顏色一個都沒有新調，元件規則也不動，只有版面從一欄變多欄。
  **HERO 還在調，尚未定案** —— 見下面幾頁。
- `preview/home-desktop-hero/` `-align/` `-banner/` `-fullscreen/` — HERO 裁切的探索過程，
  每頁都有切換條（網址可帶參數）。依序是：裁切方式四案 → 對齊三案 → 橫幅滿版 → 整屏兩案。
  **都已經被下一頁取代，留著看推論過程。**
- `preview/home-desktop-band/` — **電腦版目前的最新一頁（2026-08-07）**。整屏照片
  （`100svh`、`object-position: 50% 100%` 下緣貼齊）＋ **壓在下緣的窄帶**。
  右下角兩組切換條：底色七案 × 三格間距三案，網址可帶
  `?band=glasstar|glasstar2|blue|tar|ink|glass1|glass2|glass3&gap=now|wide|full`。
  **已定：間距用「拉開」、底色用毛玻璃＋柏油漸層。**
  還沒定：柏油漸層要「標準」還是「深」。細節全部寫在 [PALETTE.md](PALETTE.md) 第六之三節。
- `preview/nav-scrollspy/` — **頁首選單「捲到哪一節哪一項就亮」，定案 Ⓓ2 玻璃並已上線（2026-08-10）**。
  起因是使用者回報「右上三項點過之後三條白線一起留著，邏輯不通」。
  原因是那條白線只有 `:hover`，**手機沒有游標、hover 會黏住**
  —— 和文章卡「手機版不做 hover」是同一個坑，選單當初漏掉了。
  這一頁做兩件事：① hover 關掉；② 加捲動偵測，目前這一節的那一項才亮
  （HERO 那一屏三項都不亮）。
  現況／Ⓐ 白線／Ⓑ 燈亮／Ⓒ 線＋亮／**Ⓓ 系列（Ⓓ1 紙塊・Ⓓ2 玻璃・Ⓓ3 灰塊）**，
  網址可帶 `?nav=now|a|b|c|d1|d2|d3`（預設 Ⓐ；`d` 是被否決的發光原案）。
  是 `index.html` 的**完整複本**，差異清單寫在它 `<head>` 最前面。
  > **Ⓓ 系列**（第二輪，使用者提的）：**同一塊底在三個字之間滑過去**，
  > 不是三個各自亮滅。第一版做成會發光的白燈箱，使用者否決 ——
  > 「太寫實了，跟整個網站的風格不搭」。**成因記著**：這一站沒有任何一處在發光
  > （卡片是投影、頁首是毛玻璃、窄帶是漸層，全是被照亮的東西），
  > 一塊自己會亮的東西等於多了一個光源，怎麼調亮度都不會搭。
  > 第三輪改成三種現有材質：Ⓓ1 紙塊 `--paper` ＋ 墨字（11.15:1）、
  > Ⓓ2 玻璃（只把頁首玻璃擦亮一階）、
  > Ⓓ3 灰塊 `#5f5d5c`（＝「全部」那顆標記的介面灰，.88 白字 5.54:1）。
  > **圓角一律 12px** —— 標記、搜尋框、三種卡片都是這一個，不要另外挑。
  >
  > **第四輪只動 Ⓓ2：框常駐、底才跟著捲動**（使用者：「這三個元素在 HERO 區時
  > 就有玻璃框，比較有暗示它們是按鈕的感覺」）。靜止的框 .26 白（對玻璃 2.23），
  > 目前這一項的框 .45（3.76）＋ 底 .13 白（1.50）。
  > ⚠ **框畫在 `a::before`，不能直接給 `a` 加 border** —— 加了會把選單撐寬，
  > 375 上頁首只剩 37px 餘裕，品牌會被擠成兩行（第六之五節那個坑）。
  > ⚠ **也不要靠「把手機的選單間距開大」來讓框不黏在一起**（試過 5vw，已否決）：
  > **320 上會撞到品牌**，那個寬度的頁首本來就頂到極限（現況量到選單左緣比
  > 品牌右緣還左 1.2px）。改成讓外擴量自己讓步：
  > `padX = min(半個字, (選單間距 − 5) ÷ 2)`，框與框永遠留 5px，**版面一個字都不動**
  > （375 上 3.50px、320 上 2.62px、電腦版仍是半個字 6.88px）。
  >
  > **第五輪（Ⓓ2 的底色與亮度，未定案）**：使用者「玻璃亮的感覺有點橘黃光，
  > 而且和其他沒亮的元素差異不大」。
  > ⚠ **不是錯覺，是把白疊在暖底上的必然結果** —— 頁首玻璃的染色是暖的
  > `#191614`（過了窄帶的實色 `#393736`，R 比 B 高 3），低透明度的白只拉亮度、
  > 色相原封不動搬上來。**解法不必發明新顏色**：改用這一頁自己的紙
  > `--paper #e2e5e6`（R 比 B 低 4），冷正好抵掉底的暖（R−B 從 ＋2.6 收到 ＋0.7）。
  > 亮度三階 .24／.34／.44，底對玻璃 1.91／2.47／3.14 —— **定案 .24**（`?g=` 已移除）。
  > ⚠ **Ⓖ3 的白字只有 3.77:1，過不了 AA**；若選它，那一項的字要翻成墨色。
  > 亮起來那一項的字已從 .88 白提到純白（Ⓖ2 因此 4.14 → 4.80 剛好過 AA）。
  >
  > **第六輪（邊框，已定案）**：使用者**選定 Ⓖ1 淡（.24）**，並問「亮的時候
  > 邊框可不可以細一點」，最後指定 **沒亮 .75px、亮起來 1px**（兩態不同粗；
  > .5px 與「兩態同粗」落選）。Ⓖ／Ⓦ 兩排切換條已刪，數字留在那一頁的 CSS 註解裡。
  > 所以「被點到」是靠**三個量一起講**：底 0 → .24、線的濃度 .26 → .55、
  > 線的粗細 .75 → 1px。兩件事：
  > ① **原本那條線是被畫兩次的** —— 燈箱自己的 border 和 `a::before` 的框是
  >    同一個矩形，兩邊都 .55 會疊成 .80。改成線只由 `a::before` 畫，燈箱只負責面。
  > ② 粗細不再是一個值：**沒亮 .75px、亮起來 1px**（`?w=` 已移除）。
  > ⚠ **那條線是 `box-shadow: inset` 畫的，不是 `border`** ——
  >   Blink 會把 `border-width` 進位到整數 px（實測 `.5px` 的 computed value
  >   回來就是 `1px`，三階完全一樣粗，切換條等於是假的）。inset 陰影吃得到小數，
  >   而且一樣跟著 `border-radius` 走。**日後要做細線一律用這個做法。**
  > ⚠ 小數線畫得出多細由裝置決定：手機 DPR 3 的 .5px ＝ 1.5 個實體像素、銳利；
  >   1x 螢幕上不到一個像素，只能用淡一階的灰近似。**挑的時候以手機為準。**
  > 幾何全部由 JS 現量（選單字級在手機是 clamp，寫死一定會在某個寬度歪掉），
  > 左右各外擴半個字：375 上離隔壁的字 6.0px、320 上 4.7px、1440 上 23.5px。
  > 燈箱由 JS 生出來，**正式站的標記不必動**。
  > **hover 擋了兩道**：`@media (hover: hover) and (pointer: fine)` ＋
  > `html:not([data-nav])` —— 使用者第二輪回報 Ⓐ 仍然「滑到最後三條線都出來」，
  > 所以在有捲動偵測的那幾案裡 hover 完全不畫線，線唯一的來源是 `aria-current`。
  > 切換條右邊有**即時讀數**，畫面和讀數不一致就表示是瀏覽器留下的狀態。
- `preview/nav-frame-height/` — **選單那三個玻璃框的高與圓角，已定案上線（2026-08-11）**。
  三輪：①「要多高」定 **Ⓥ1 2.65**（框高 ÷ 字級）②「圓角要多小」定 **Ⓡ2 8px**
  ③ 使用者指定**推到電腦版與 iPad**，所以那一段是基底規則、不放在任何 @media 裡。
  切換條已刪，這一頁現在是正式站的複本、沒有任何覆寫，**畫面上就是線上的樣子**。
  規則本身寫在 `index.html` 的「框的幾何」那一段，落選案的數字在這一頁 `<head>`。
  > 起因是使用者回報「玻璃框的垂直段好像有點長，但字沒那麼大」。**量出來成立**：
  > 框是圓角矩形，四個角吃掉圓角，中間剩下的那段直線才是眼睛讀到的「垂直段」——
  > 390 上原本是 **12.28px，和一個字（12.48）一樣長**。
  > **成因不是有人把框畫大了，是框根本沒有自己的尺寸**：它從 `a` 的盒子外擴 1px 畫，
  > 而 `a` 的高 ＝ `line-height 1.85` ＋ `padding-block .35rem`。前者是排版用的、
  > 後者是**點擊範圍**用的，兩個都不是為了框而設 —— 等於把「手指按得到的大小」
  > 畫成了看得見的框。上下留白 0.95 字、左右只有 0.30 字，**上下是左右的 3.2 倍**。
  > 左右那 3.74px 動不了（框與框要留 5px，`nav-scrollspy` 那個 320 的坑），只能收上下。
  > **落選**：高度 Ⓥ2 2.36（＝標記的比例）、Ⓥ3 2.15；圓角 Ⓡ1 10px、Ⓡ3 6px。
  > ⚠ **第二輪是第一輪自己引出來的，不是使用者反覆**：圓角是固定 px、框高變小了，
  >   角就吃掉更大的比例 —— 用「弧佔周長」（四個角的弧長 ÷ 整圈周長）量，同一個
  >   12px 從 44.9% 變成 **46.7%，更圓**；320 上是 **54.9%，一半以上的邊都是彎的**。
  >   8px 收到 29.9%。
  > ⚠ **頁首因此離開 12px 那一族，是使用者指定的，不是漏改。** 但 **8px 不是憑空
  >   冒出來的數字** —— 這一站的小標籤本來就是一族小圓角：`.doc dt` 6px、
  >   `.card-tag` 8.5px、`.info-card h3` 9px。選單那三個框正好落在這一族裡。
  > ⚠ **圓角維持 px、不要跟著字級走。** 這一站的圓角是「材質」不是「比例」：
  >   434px 高的文章卡和 31px 高的標記用同一個 12px。高度改 em 是因為它要跟著字。
  > ⚠ **框（`a::before`）和底（`.nav-lamp`）要吃同一個 `--frame-r`**，兩個是同一個
  >   矩形的線與面，只改一邊會露出一圈錯位的邊。高度那一半由 JS 的 `frameBox()`
  >   算，和框共用 `--frame-em`。
  > ⚠ 「框高 < 圓角 ×2」框就會收成膠囊、直段消失（落選的 Ⓥ3 在 320 上就是這樣）。
  >   8px 的門檻是 16px，目前最矮的 320 是 29.14px，離得很遠。
  > ⚠ **`a` 的 `padding-block` 一個字都不能動。** 框是絕對定位的 ::before，改它不影響
  >   版面；改 padding 會改到 `a` 的高 → 頁首那列的列高 → 頁首總高 →
  >   **HERO 那首詩的空隙 8.0px**（Ⓢ1 定案）。十一個視窗實測頁首高與改動前相同。
  > 順手修掉一個原本就歪的漂移：舊的框高 ＝ `1.85em ＋ 11.2px`，一半跟字級走、
  > 一半是固定 px，所以「框高 ÷ 字級」在 320／375／390／430 是 3.05／2.95／2.91／2.86 ——
  > **手機愈窄框相對愈長**，正好是最擠的那一端。改成純 em 之後所有寬度一致。
  > 這和 HERO 那輪「手機的幾何原本不是等比例的」是同一件事（第九節第 6 項第 3 點）。
  > **⚠ 文章頁（`posts/`，走 `assets/style.css`）的選單仍然沒有框**，這一輪沒加 ——
  > 那是第九節第 0 項還沒問過的一題。
  > ⚠ **這一頁踩到一個坑，日後複製 `index.html` 做提案頁一定要記得**：
  >   HERO 那張圖是 `src` ＋ **`srcset`** 兩份路徑，只把 `src=` 換成絕對路徑不夠 ——
  >   瀏覽器仍然選 `srcset`，去要 `/preview/<name>/assets/…` 而 404，
  >   **畫面上不會出現破圖，只會安靜地變成 alt 文字疊在黑底上**，很容易當成「照片比較暗」。
  >   `srcset` 是 08-09 之後才加的，所以更早的幾份提案頁沒有這個問題。
- `preview/doc-cross-spec/` — **⚠ 提案中：一位醫師出現在兩科底下（2026-08-11）**。
  起因是使用者要「李柄輝醫師的專長加上牙周照護」，接著說「還是一般牙科的標籤就好，
  但主題與科別點到牙周治療，可以也把他的醫師介紹卡片顯示出來」。
  **專長那行文字已經上線**（`index.html`），**篩選這一件還沒定案，正式站一個字都沒動。**
  > **卡住的點是結構**：`data-spec` 同時是「篩選的科別」和「配色的來源」，
  > index.html 原本的註解還特別寫這是刻意的（「顏色與篩選永遠不可能對不起來」）。
  > 所以「出現在牙周底下、但維持一般牙科的綠」在現行結構下是矛盾的。
  > 這一頁的做法是**把兩件事拆成兩個屬性**：`data-spec` 照舊只有一個、照舊決定顏色，
  > 新增 `data-spec-also="perio"` 只給篩選那支 JS 讀（空白分隔可多科）。
  > **代價是放棄了「顏色 ＝ 篩選」那個不變量**，日後看到一張卡出現在和它顏色不同的
  > 科別底下就是這個屬性，不是 bug。
  > 切換條：`?doc=a|b|c`（Ⓐ 現況／**Ⓑ 顯示，預設**／Ⓒ 顯示＋在專長欄把命中的字用
  > 1.5px 細線標起來）、`?n=1|2`（**Ⓝ1 只做李柄輝，預設**／Ⓝ2 全部九位）。
  > **Ⓝ2 是這件事真正要先想清楚的一題**：李柄輝進了牙周之後，李侑津（專長寫著
  > 植牙手術、固定假牙、活動假牙）為什麼沒進植牙・假牙重建？謝耀慶（一般牙科治療）
  > 為什麼沒進一般牙科？只做一位是「特例」，全部做是「規則」，兩個都行但要有意識選。
  > 是 `index.html` 的**完整複本**，差異清單寫在它 `<head>` 最前面。
  > **⚠ 這一頁已被下面的 `doc-spec-tags/` 取代**，留著看推論過程就好，不要拿去實作。
- `preview/doc-spec-tags/` — **點到的科別才亮起來，已定案上線（2026-08-11）**。
  主題與科別按下去的時候，把有關聯的醫師與文章都帶出來（醫師的關聯**同時看專科與
  專長**），並讓被點到的那幾顆自己亮起來說「我就是原因」。
  **這一頁現在是正式站 `index.html` 的複本、沒有任何覆寫，切換條也刪了** ——
  畫面上就是線上的樣子。規則本身寫在 `index.html` 的「點到的科別才亮起來」那一段
  CSS 與底下篩選 JS 的註解裡；推導與落選案的數字全部在這一頁的 `<head>`。
  > **做法**：專長那句話**一個字都沒改**，只把每個詞包一層 `<span class="sk"
  > data-spec="…">`、每個頓號包一層 `<span class="sep">`。**篩選改讀那些 span** ——
  > 被某一科篩到的條件是「專科 ＝ 那一科」**或**「專長裡有一個詞屬於那一科」。
  > 上一頁 `doc-cross-spec/` 那個看不見的 `data-spec-also` 因此**不需要了**，
  > 「篩得到什麼」和「畫面上寫什麼」再也不可能對不起來。
  >
  > **四個狀態**（沒點任何一科時 ＝ 和正式站一模一樣）：
  > 命中的專長 → **淡色填滿**；沒命中的專長 → **維持純文字**；
  > 專科藥丸 ＝ 那一科 → 套色填滿，≠ 那一科 → 白底字套色；
  > **文章卡的主題標籤 → 套色填滿**（2026-08-11 使用者第五次要求）。
  > ⚠ **文章那顆用滿色、專長用淡色是刻意的**：文章卡只有一顆標籤，而且它就是那篇
  >   文章的科別本身（和醫師的專科藥丸同一種份量）；專長是「他也會做這件事」，輕一階。
  > ⚠ **只用搜尋框、沒按科別時，文章標籤不套色** —— 搜尋命中的是字不是科別。
  >
  > **定案的四個值**（使用者 2026-08-11）：**Ⓚ3 淡色填滿・Ⓟ1 濃度 12%・
  > Ⓩ2 標記字級 .748rem／11.97px・Ⓧ1 兒童矯正只算兒童牙科**。
  > 落選：Ⓚ1 套色填滿（白字）／Ⓚ2 白底字套色；Ⓟ2 20%／Ⓟ3 30%；
  > Ⓩ1 .72rem（原本）／Ⓩ3 .775／Ⓩ4 .803／Ⓩ5 .83rem（＝內文字級，塊會撐開行框）；
  > Ⓧ2 兒童矯正也算齒顎矯正（會讓齒顎矯正從 2 位變 4 位）。
  > ⚠ **濃度 12% 不是隨便挑的**：淡底會吃掉深階字的對比，實測字對淡底
  >   一般 6.82・牙周 4.74・根管 7.07・**兒童 4.01**・矯正 7.96・贋復 10.42・口外 5.12 ——
  >   **只有 12% 是「除了兒牙以外全過 AA」**（20% 牙周也掉到 4.26，30% 再加口外 4.06）。
  >   兒牙本來就有一條已定案的 AA 例外（填色白字 3.22），4.01 比它還好。
  > ⚠ 「兒童隱適美／兒童早期矯正」歸兒童牙科**是使用者判斷過的**，不要順手併進矯正。
  >
  > **材質沒有一顆是新的**：亮起來的標記和 `.doc-role` 同一組幾何（1.5／.05rem .6rem／
  > 8px），三態的顏色分別是 chip 按下態、線框標記預設態，與兩者之間用 `color-mix`
  > 混出來的淡底（`color-mix` 這一站本來就在用）。**沒有新色票、沒有新圓角。**
  >
  > **⚠ 小字往下坐，兩輪（2026-08-11）**：
  > **第一輪**使用者回報「標籤過的字有點往下跑，專長和專科都是」。成因是中文小字和
  > 大字並排時瀏覽器對的是**基線**、眼睛讀的是**字面中線**（Noto Sans TC 實測
  > 0.345em）。**第二輪**使用者：「專科有對齊名字的垂直中線，但專長上標籤後看起來是
  > 對齊沒被標籤的文字下緣。」**成立，而且第一輪對錯了東西** —— **填滿的標記，眼睛
  > 讀的是那個色塊，不是塊裡面的字**，而塊裡的字本來就比塊的中線高 0.66px。
  > 修完不對稱從 1.33px 降到 **0.02px**（判斷方法：塊上緣比字 ink 上緣高多少、
  > 塊下緣比 ink 下緣低多少，兩個要一樣）。
  > ⚠ **專科藥丸維持「字對字」的 `.158em`** —— 使用者明講那一顆現在是對的。
  >   它比線上高 1.82px，**那是使用者要的，不是提案頁跑掉了**。
  > ⚠⚠ **標記的抬升量不能寫死，要 JS 現量**：Blink 把 inline-block 的塊內基線位置
  >   **進位到整數 px**（五個候選字級量到 13.797／13.797／14.797／15.797／15.797，
  >   跳階），**而且同一字級在不同 DPR 落在不同整數**（.803rem 那階 DPR1 抬 0.274px、
  >   DPR3 抬 1.274px）。做法同 `nav-scrollspy` 的 `--lamp-pad`。
  > ⚠ 量「那一行的基線」的探針要插在標記**後面** —— 插前面的話標記被擠到下一行時
  >   探針會留在上一行（375／320 上量到 −19.44 就是這個坑）。
  >
  > **代價**：放掉「顏色 ＝ 篩選」那個不變量（一張綠卡會出現在牙周底下）。
  > **已寫進 [PALETTE.md](PALETTE.md) 第六之二節**（新增一小節「顏色 ＝ 篩選這個
  > 不變量已經放掉了」，含三種份量的對照表與 12% 的對比度數字）。
  > **維護成本只有一個**：專長 → 科別的對照寫死在 HTML 屬性上，日後新增或修改任何
  > 一位醫師的專長，就要順手給那個詞一個 `data-spec`，否則它不會被任何一科篩到、
  > 也永遠不會亮起來。完整對照表（32 個詞）在那一頁 `<head>`。
  > ⚠ **頓號不要用 `<i>` 包** —— `<i>` 預設是斜體，中文沒有義式斜體、瀏覽器會**合成**
  >   一個傾斜的頓號，畫面上真的看得出來。這一頁第一版踩過，改成 `<span>`。
  > 「沒篩選時和正式站一樣」是量過的：文章區與醫師區的幾何在 1440／375 都完全相同；
  > 逐像素只剩字緣反鋸齒有差，成因是拆成 span 會切斷 shaping run，整行推進量累積差
  > **0.04px**。是 `index.html` 的**完整複本**，差異清單寫在它 `<head>` 最前面。
- `preview/head-blur-ipad/` — **頁首玻璃「招牌之前不要那麼糊」，iPad 這一輪定案 Ⓑ1（2026-08-11）**。
  起因是使用者在 iPad 上回報「那條玻璃太糊了，捲到照片的診所招牌附近就覺得還好」。
  當時的切換條（已刪）：`?blur=now|b1|b2|b3` 與 `&mark=1`（招牌標線）。
  **三輪都定了之後已一起上線（2026-08-11）**，這一頁現在是正式站的複本、沒有任何覆寫。
  > ⚠ **上線的版本和這一輪定的不完全一樣，兩處，都是後兩輪帶來的**：
  > ① 轉場改用**捲動距離**表示（這一輪的 f 0.70 ＝ 118px → 通則「招牌之前 110px」，
  >    回頭套是 f 0.706，差 8px 的捲動）；
  > ② 平段分成兩個值（橫的螢幕 5px、直的 4px）。**iPad 直放仍然是 4px**，
  >    iPad 橫放會拿到 5px（它看得到 95% 的照片寬度，情況和電腦版一樣）。
  > **量出來成立，而且 iPad 比電腦嚴重 —— 成因是裁切，不是模糊本身。**
  > `.hero-photo` 是 `object-fit: cover`，**視窗愈瘦左右被切掉愈多**：
  > iPad 直放只留下原圖中間的 46%（x 430~1170），而那 46% 正好就是
  > **水泥立面加整片窗戶**，全張照片裡結構最規則、對比最強的一塊。
  > 電腦版的條子同時蓋到左右兩側的天空（細節量只有 0.34），糊掉看不出來。
  > **兩邊的模糊都是 6px，差別在底下的東西怕不怕糊。**
  > 逐列實測「條子底下那一段的細節量」（梯度平均絕對值）從天空的 0.4
  > 一路升到立面的 8.9，招牌以下再升到 12~16 —— 但**招牌以下亮度掉到 29~34**
  > （立面是 50~81），暗處的糊看不見，所以使用者說那裡「還好」是對的。
  > **三案形狀相同**：招牌之前平的一個值 → 0.70~0.799 之間升回 6px → 招牌之後維持 6px。
  > 只有平段不同：**Ⓑ1 4px／Ⓑ2 3px／Ⓑ3 2px**（現況 6px）。
  > ⚠ **招牌位置是量出來的**：在 x 850~1060 逐列數亮度 > 120 的點，
  >   招牌是燈箱字（最亮 250 以上）落在 **79.9%~82.9%**；
  >   先前掃錯範圍掃到二三樓窗戶透出來的室內燈（最亮只到 155），標線就畫錯了。
  >   **動那個數字之前先開 `?mark=1` 看標線有沒有壓在招牌上。**
  > ⚠ **「條子底下是照片的哪一列」要把 cover 的裁切一起算**，不能拿 scrollY 當門檻 ——
  >   同一個 scrollY 在不同視窗比例指到的照片內容完全不一樣。
  >   `object-position` 是讀 computed style 的，所以手機版那條 `left center` 也自動算對。
  > ⚠ blur 的值**量化到 0.25px 再寫**，backdrop-filter 每一幀重算是有代價的。
  > ⚠ 值放在 `--head-blur`、CSS 寫 `blur(var(--head-blur, 6px))`，**沒有 JS 就退回現況**。
  > 是 `index.html` 的**完整複本**，差異清單寫在它 `<head>` 最前面。
- `preview/head-blur-mobile/` — **同一件事的手機版，定案 Ⓜ3＋Ⓛ1（2026-08-11）**：
  平段 4px、**f 0.45 起升**回 6px（375 上轉場 97.8px）。落選：Ⓜ1 同 iPad（轉場只有
  27.7px，太快）、Ⓜ2 一路到底、濃度 Ⓛ2 3px／Ⓛ3 2px。
  當時的切換條（已刪）：`?m=now|m1|m2|m3`、`?L=L1|L2|L3` 與 `&mark=1`。
  **已隨三輪一起上線**，這一頁現在是正式站的複本、沒有任何覆寫。
  > **⚠ 手機不是「同一件事的小尺寸」，是另一件事。三個前提全部不成立：**
  > ① **裁切方向相反。** 手機是 `height: 74.72vw` ＋ `object-position: left center`，
  >    盒子變橫的，cover 切**右邊**留左邊 **89%**（連天空、路燈、老屋都在）；
  >    iPad 直放切左右、只留中間 46%（純立面與窗戶）。天空被一起平均進來，
  >    **同一個位置手機的細節量只有 iPad 的一半**（f 52% 時 3.86 vs 7.47）。
  >    所以「手機到底要不要跟著改」本身就是一題（切換條上的「現況」那一顆）。
  > ② **條子蓋掉照片的五分之一，不是二十分之一**（手機 21.1%／iPad 5.1%）——
  >    條子底下永遠是混的（一半天空一半窗戶），不會有「整條壓在立面上」這種乾淨狀態。
  > ③ **整張照片一個滑動就走完**（375 上 221px，iPad 要 1133px）。所以
  >    **iPad 那個轉場搬到手機等於一個瞬間**：118px → **27.7px**。這是形狀三案要解的。
  > **形狀**：Ⓜ1 同 iPad（0.70 起升，27.7px）／Ⓜ2 一路到底（完全不升回去）／
  > Ⓜ3 轉場拉長（0.45 起升，97.8px）。**濃度**：Ⓛ1 4px（＝iPad 定案值）／Ⓛ2 3px／Ⓛ3 2px。
  > ⚠ **招牌在手機的裁切裡面**（落在可見寬度的 60%~75%），所以門檻 0.799 兩邊共用。
  > ⚠ **手機的模糊在實體像素上是 iPad 的 1.5 倍**（DPR 3 vs 2），同樣寫 4px
  >   手機畫出 12 個實體像素、iPad 8 個，再加上手機拿得比較近 ——
  >   **手機不一定要跟 iPad 同一個值**，這就是第二排切換條存在的理由。
  > ⚠ Ⓜ2 連 HERO 之後都不升回去，但實際看不出來：窄帶進條子之後玻璃濃度
  >   一路走到 .84、紙色補滿時整條變不透明，backdrop-filter 的結果就看不見了。
  > ⚠ **上線的版本把這個 0.45 換成了通則「招牌之前 110px」**（回頭套是 f 0.407／
  >   0.423，差不到 12px 的捲動，畫面上等於沒變）。**手機的平段仍然是 4px。**
  > 是 `index.html` 的**完整複本**，差異清單寫在它 `<head>` 最前面。
- `preview/head-blur-desktop/` — **同一件事的電腦版，定案 Ⓓ3 並上線（2026-08-11，收尾這一輪）**：
  **平段 5px、轉場 ＝ 招牌之前 110px。** 落選 Ⓓ1（同樣的轉場但平段 4px）、
  Ⓓ2（照抄手機的 f 0.45）、以及「電腦版不改、維持 6px」。
  當時的切換條（已刪）：`?d=now|d1|d2|d3` 與 `&mark=1`。
  這一頁現在是正式站的複本、沒有任何覆寫。
  > **⚠ 這一頁更正了前一輪講過的一句話。** 先前說「電腦版的細節量只有 0.34、
  > 本來就沒那麼糊」—— **那個 0.34 是捲動 0（條子壓在天空上）那一刻的值，不是整段。**
  > 整段量完，電腦版**接近 iPad，不像手機**：
  >
  > | f（條子下緣） | iPad 直 | 電腦 1440 | 手機 375 |
  > | --- | --- | --- | --- |
  > | 50% | 6.58 | 4.17 | 3.41 |
  > | 70% | 8.79 | 7.52 | 6.55 |
  > | 80%（招牌） | 10.66 | 11.55 | 8.62 |
  >
  > **成因也要寫對**：手機讀起來比較不糊，主因**不是**左右裁切，而是
  > **條子在手機上有 21% 的照片高**（電腦 6.9%、iPad 5.2%）—— 那麼厚的一條
  > 永遠會把大片天空一起平均進來。電腦版的條子薄，量到的就是那一列真正的細節。
  > → **所以「電腦版不需要改」這個前提站不住。**
  > **這一輪真正的題目是：一條規則還是三條。** 把已定案的兩個轉場換算成
  > **捲動距離**（＝手指滑多遠 blur 才走完），兩個分開決定的案子幾乎是同一個數：
  > iPad Ⓑ1（f .70）**118px**、手機 390 Ⓜ3（f .45）**102px**、手機 375 **98px**。
  > ⚠ **這不是巧合** —— 眼睛讀的是「滑一段手指，糊度變了多少」，那本來就是一個
  >   捲動距離，不是照片比例。用比例寫，同一條規則在不同視窗會忽快忽慢
  >   （f .70 起升：iPad 118px、手機 375 只有 28px，差四倍）。
  > **四案**：現況／Ⓓ1 一條規則（4px、招牌之前 **110px** 起升）／
  > Ⓓ2 照抄手機的 f（4px、.45 起升，1440 上是 332px，等於三個尺寸三條規則）／
  > **★ Ⓓ3 收少一階（5px，轉場同 Ⓓ1）**。
  > ⚠ **Ⓓ3 只贏在「平段」那一個數字**（5px 對 Ⓓ1 的 4px）；
  >   **轉場那一半 Ⓓ1 與 Ⓓ3 是同一條**，也就是這一輪推出來的 110px 通則。
  > ⚠ Ⓓ1 本來是唯一完全不需要 @media 的一案；選了 Ⓓ3 之後上線的版本有**一行**：
  >   `:root { --head-blur-flat: 4px }` ＋ `@media (min-aspect-ratio: 1/1) { 5px }`。
  >   **分界用畫面比例不是寬度** —— 決定「裁掉多少天空」的是盒子的長寬比。
  > ⚠ 起升點是**反著算回來的**：先由 f=.799 換出招牌在哪個 scrollY，退 110px，
  >   再換回 f 當門檻，並夾在 f(scroll 0) 以上（視窗很矮時 110px 可能比招牌之前的
  >   全部行程還長）。所以那 110px 在每個視窗都真的是 110px。
  > 是 `index.html` 的**完整複本**。
- `preview/spec-tag-fit/` — **專長標記要怎麼貼合它包住的那幾個字，已定案上線（2026-08-11）**。
  使用者回報兩件：「套色的區塊沒有對到被套色內文的垂直中線，比較偏文字下面一點」、
  「水平長度有點長、超出文字不少，應該可以縮短一點」。
  **兩件是同一個成因**：塊的大小一直是從「行框（`line-height`）＋ 內距」長出來的，
  跟裡面那幾個字的**字面框**沒有關係 —— 行框對的是字型的 ascent／descent，
  那兩個值涵蓋拉丁字母的上伸下伸部，中文用不到那麼多，**而且用不到的部分上下不一樣多**。
  改動前實測（1440，Noto Sans TC）：字面上緣離塊上緣 3.51px、下緣離塊下緣 5.69px，
  **下面比上面多 62%**；側邊留白 ÷ 字級 0.909，而同一張卡的 `.doc dt` 只有 0.553。
  > **定案 Ⓥ3（上下各留 0.28 個標記字級）＋ Ⓧ3（側邊 .34rem）**，切換條已刪，
  > 這一頁現在是正式站的複本、沒有任何覆寫。落選：Ⓥ1 .385（＝維持原本的塊高
  > 21.56）／Ⓥ2 .32（19.98）；Ⓧ1 .5rem（0.776）／Ⓧ2 .42rem（0.669）。
  > 塊高 21.55 → **19.03px**，側邊留白比 0.909 → **0.562（貼齊 `.doc dt` 的 0.553）**。
  > **做法**：`line-height: 1`，塊高改由上下兩個內距各自負責，由 JS 現量成
  > 「字面上下各留一樣多」。上下不對稱從 −2.18px 收到 **−0.01px**。
  > 下限是上下留白 0.159（`padding-bottom` 收到 0），再小就不可能上下對稱。
  > ⚠ **一旦上下對稱了，抬升量就退化成最單純的那條式子** `(A−D)/2 × (父層字級 −
  >   自己的字級)`（1440 上 0.451px，原本 1.618px）—— 塊心此時就是自己那幾個字的
  >   字面中線，把它對到旁邊的字面中線，等於把兩行字的字面中線對在一起。
  > ⚠ **A／D 取全部專長詞的極值，不是每個詞各自量。** 逐詞量的話「齒顎矯正」
  >   （下緣 .172em）和「隱適美」（.094em）會長成不同高度的塊，同一行擺在一起
  >   一眼就看得出來。
  > ⚠ **量字面框要在 400px 上量再等比例縮。** Blink 回來的 `actualBoundingBox`
  >   顆粒很粗（實測 400px 上以 1/64 em 為階），直接拿 12px 去量，半個像素就沒了。
  >   順帶驗到：這樣算出來的字面中線 **0.34375em**，和前一輪手量的 0.345 對得上。
  > **第五輪：專科藥丸（`.doc-role`）也一起擺正。** 使用者：「專科醫師的標籤套色
  > 似乎也不在專科內文的垂直高度中線上。」同一個成因、同一個修法 —— 量出來上面
  > 3.90px、下面 5.44px（差 1.55px），塊心比名字的字面中線低 0.86px，
  > 修完 **0.017 ／ 0.077px**。
  > **只把塊擺正，塊高一個像素都不動**（20.86px）：上下各留 **0.405 個藥丸字級**，
  > 那個數字就是「維持現在這個高度」算出來的。**和專長標記的 0.28 不同是刻意的** ——
  > 藥丸重、專長輕，那一階要留著。
  > ⚠ **`vertical-align: .158em` 沒動、側邊 `.6rem` 沒動。** 藥丸裡的**字**本來就
  >   對得很準（字面中線只差 0.085px），歪的是**塊**。而且**改 padding 不會動到
  >   裡面的字** —— `vertical-align` 釘的是 inline-block 的**基線**，跟 padding、
  >   `line-height` 都無關（實測改動前後都是 0.085px）。
  > ⚠ **同一個偏差這一站每一顆有底色的小標籤都有**（`.doc dt` 0.389／0.542em、
  >   `.card-tag` 0.439／0.546、`.chips button` 0.604／0.821、`.info-card h3`
  >   0.399／0.593）。**那幾顆一顆都沒動** —— 它們都單獨站著，旁邊沒有同一行的字
  >   可以比，看不出來。**不要為了「一致」順手改。**
- `preview/spec-tag-size/` — **套色之後那幾個字要多大，已定案上線（2026-08-11）**。
  使用者：「現在的版本套色後內文字應該是變小了。」**成立，而且一直都是這樣** ——
  專長那一欄的字是 `.83rem`／13.28px，標記裡的字是 `.748rem`／11.97px，
  **被標起來的那幾個字比旁邊的小 1.31px（10%）**。標記亮起來本來是要說
  「這個詞就是你點的那一科」，字卻反而縮了一階。
  **定案 Ⓩ3 `.83rem`（和沒套色的內文一模一樣，完全不縮）**，切換條已刪，
  這一頁現在是正式站的複本。落選 Ⓩ1 `.748rem`（原本）、Ⓩ2 `.79rem`；
  塊高 19.05／20.13／**21.13**。
  > ⚠ **`.83rem` 在上一輪（`preview/doc-spec-tags/` 的 Ⓩ5）是被否決的**，理由是
  >   「塊高 23.52 > dd 行高 23.24，會撐開行框」。**那個理由現在不成立了** ——
  >   塊高當時是從行框長出來的，`spec-tag-fit` 那一輪改成從字面框長，同樣 `.83rem`
  >   只有 21.13px。**這一題是被幾何改動重新打開的，不是有人忘記它被否決過。**
  > ⚠ 不過 Ⓩ3 仍會讓那一列長高 **0.13px**（23.23 → 23.36）。決定行框會不會被撐開的
  >   **不是塊高，是塊的上緣離基線多遠**（Ⓩ3 是 15.13px，dd 的 strut ascent 只有
  >   14.97）。**要再往上加字級，這條才是天花板**，不是塊高。
  > ⚠ **抬升量在 Ⓩ3 正好是 0** —— 式子是 `(A−D)/2 × (父層字級 − 自己的字級)`，
  >   字級一樣就不必抬。
  > 切換條**只動字級**：側邊內距在那一頁寫成 `.4546em`（＝ `.34rem` ÷ 11.968px），
  > 在 Ⓩ1 上和線上完全相同，Ⓩ2／Ⓩ3 才跟著字一起長，所以三案是等比例放大。
- `preview/band-stats-gap/` — **窄帶三格的間距 4rem，定稿紀錄（2026-08-10）**。
  用來看「2026-08-07 定案的 4rem」和「線上誤跑三天的 1.9rem」差在哪；
  選定後已修進 `index.html`，這一頁現在就是正式站的複本、沒有任何覆寫。
- `preview/hero-contact-mobile/` — **手機版的地址與電話，已定案上線（2026-08-10）**。
  位置是使用者指定的：**瀏覽數下面、1983／9／6 上面**，一行置中。
  這一頁是 `index.html` 的**完整複本**（不是自給自足的快照），差異清單寫在它
  `<head>` 最前面；切換條已刪，畫面上就是上線的樣子。
  淘汰：Ⓜ2 一行撐開（「好怪」）、Ⓜ3 兩行（電話在上、地址在下，行距試過四階）。
  > 兩個踩過的坑寫在那一頁與 `index.html` 的註解裡：
  > ① 手機吃到了 ≤1159 那段「兩行版」的溝（`flex: 0 0 1.55rem`），圖示前面多了
  >   7.68px 死空白，**盒子置中但看得見的那一塊偏右 3.61px**。關掉溝之後剩 0.47px。
  >   **不要改用「整組往左推幾 px」去補。**
  > ② 手機那條一行排法要寫 `body .contact-pair`（0,1,1）—— ≤1159 的兩行規則就是
  >   這個權重且 1159 涵蓋 375，只寫 `.contact-pair` 會被整條蓋掉、變成直排兩行。
  這一頁是 `index.html` 的**完整複本**（不是自給自足的快照），只動五處，
  差異清單寫在它 `<head>` 最前面。
  > **裡面有一個要帶回正式站的結構改動**：地址與電話的標記從 `.band` 的直接子元素
  > 移進 `.shell`（手機要它排在兩者之間，就得參與 `.shell` 的流）。電腦版數字完全
  > 不變，但**窄螢幕那段 @media 要補一行 `right: var(--pad)`** —— 包含塊換成
  > `.shell` 之後 `right: 0` 會貼到螢幕邊（985 上實測 0px，正式站是 30px）。
- `preview/hero-contact/` — **窄帶右側的地址與電話，已定案上線（2026-08-10）**。
  六輪收斂：排法（兩端／右側成組／接同一列／兩行／一行拉開／加分隔線）、水平三階、
  垂直五階、話筒大小五階與下移三階，每一個落選案的實測數字都在那一頁的 `<head>` 註解裡。
  切換條已刪，畫面上就是上線的樣子。**要動窄帶右側之前先讀那一頁**，不要重新目測。
- `preview/logo-banner/` — **標誌定稿的前後對照（2026-08-07）**。頁首標誌牙洞 1.3 倍、上移 0.50。
- `preview/logo-favicon/` — **favicon 的顏色，已定案（2026-08-08）**。
  淺色 `#4f6361`、深色 `#4f8065`（那一頁的第 2 案），頁上留著五個候選與推導。
- `preview/logo-favicon-shift/` — 紀錄：favicon 牙洞往上移的四個位置，定案是上移 1.05。
- `preview/logo-favicon-plate/` — **Safari 在圖示後面墊白底的四個亮度候選（2026-08-08）**。
  一個候選一頁（`c1`～`c4`），因為 Safari 是按「圖示網址」分開判斷的。**還沒定案**，
  等使用者在 iPhone 上逐頁看完回報第一顆沒有白底的編號。

  > **`assets/favicon.svg` 已經換成標誌本體了**（2026-08-08），
  > 一個檔同時管淺色與深色分頁列，靠 SVG 裡的 `prefers-color-scheme`。
  > 規則與數據在 [PALETTE.md](PALETTE.md) 第六之七節。
  >
  > **引用它的時候一定要帶 `?v=<版本>`**（現在是 `?v=20260808b`）。
  > Safari 的 favicon 快取是按圖示網址存的，網址沒變就永遠給舊圖 ——
  > 使用者在手機上就是這樣一直看到 2026-08-02 那顆舊牙。改圖之後要把全站的
  > `?v=` 一起換掉，指令與原因寫在 PALETTE.md 第六之七節。
  >
  > **SVG 裡兩個顏色的順序是刻意反過來的**：深色版 `#4f8065` 當預設，
  > 淺色版 `#4f6361` 才放進 `@media (prefers-color-scheme: light)`。
  > **Safari 不看 favicon 裡的深色模式**，只吃沒有條件的那一行；讓它拿到暗的那顆，
  > 對深色分頁列只有 2.67:1，Safari 就會自動墊一塊白底「幫」你。**不要改回來。**

- `preview/hero-portrait/` — **直立（旋轉）螢幕的 HERO，已定案（2026-08-09）**。
  使用者把 1920×1080 轉成直的，詩變成一撮小字。**定案「照片不整屏」`62svh` ＋
  字級改讀 `svh`，已套進 `index.html`**（一段 `@media (min-width: 721px) and
  (max-aspect-ratio: 2/3)`，電腦版與手機版逐項比對過、一個值都沒動）。
  這一頁是定案後的紀錄，切換條已刪。當時比過的五案、落選直排問出來的「左側天空柱」
  地形、以及為什麼 `object-position: 50% 100%` 在直立比例下失效，
  全部在 [PALETTE.md](PALETTE.md) 第六之十三節。
- `preview/spec-*/` — **七個科別色的推導過程（2026-08-09 一整天）**，一輪一頁：
  `spec-surg-violet/`（外科紫，定案）、`spec-prosth-blue/`（贋復 3×3，已被後面兩輪取代）、
  `spec-endo-red/` `-raw/` `-all/`（顯微根管紅，`-all` 是定案頁，十一案×兩條切換條）、
  `logo-green-compare/`（一般牙科綠，十三案，定案）、`spec-ortho-bright/`（矯正提亮，**未定案**）、
  **`spec-ortho-palette/`（矯正換色系：品牌剩的米色＋稻草灰，加資料庫三個，八案）**、
  **`spec-perio-teal/`（⚠ 最新一頁：牙周換青綠、矯正接手牙周的藍。兩科對調已定，
  只剩牙周的青綠要幾階，三案 Ⓣ1／Ⓣ2／Ⓣ3）**、
  `spec-prosth-bluer/`（贋復藍紫，定案 Ⓑ）、
  **`spec-perio-split/`（牙周獨立＋贋復字階，三排切換條，定案 Ⓧ·Ⓡ·Ⓓ2）**。
  每一頁都有完整元件舞台與文章頁模擬。數據全部同步在 [PALETTE.md](PALETTE.md) 第六之九～十四節。

### 沒有鎖（2026-08-06 起）

原本 `src/worker.js` 對 `/preview/*` 要求 HTTP Basic 認證，**已依使用者要求移除**。
不要「順手加回去」。當時的判斷：

- 那道鎖只擋得住 `fangren.net` 這一側，repo 是 **public**，同一份 HTML 在 GitHub 上
  任何人都讀得到，擋了也沒有實質保護。
- 密碼要放在 Cloudflare Secret `PREVIEW_PASSWORD`，而那個 secret 從來沒設過，
  Worker 就一直回 503 —— 結果是使用者自己用手機也打不開提案頁。
  要設它得進 Cloudflare 後台，這件事只有使用者能做。
- 使用者的結論是「沒有網址別人也看不到」，選擇不鎖。

現在 `/preview/*` 只多兩個 header：`X-Robots-Tag: noindex, nofollow, noarchive`
與 `Cache-Control: no-store`（提案頁改得勤，手機不能拿到快取的舊版）。

### 所以 `preview/` 等同對外公開

`/preview/` 底下的東西任何人拿到網址就看得到，只是沒有連結指過去、也不會被搜尋到。
搜尋引擎那一層有三道：頁面自己的 `<meta name="robots" content="noindex, nofollow, noarchive">`
（**新增預覽頁一定要帶**）、Worker 的 `X-Robots-Tag`、以及 `robots.txt` 的
`Disallow: /preview/`（寫在 `build.mjs` 裡）。

**真的不能外流的東西不要放這裡。**

舊的 GitHub Pages 站（`yclee86.github.io/fangren-dental/preview/...`）也送得出這些頁，
但那邊是子路徑，預覽頁裡的 `/assets/...` 絕對路徑會 404 **圖片全破**，
所以要看提案一律用 `https://fangren.net/preview/<name>/`。

### 本機預覽

`node tools/serve.mjs` **不會**要求密碼（跟計數器一樣，本機沒有 Worker），
直接開 http://localhost:8791/preview/home-v2/ 就看得到。這是預期行為。

---

## 九、接續中的工作 —— 換電腦或手機時先讀這一節（2026-08-07）

### 東西不在 `main` 上

目前所有進行中的工作都在分支 **`claude/specialty-copy-scheme-lookup-u1ngx7`**。
新開的 session 預設在 `main`，看不到這些檔案。接續時第一件事：

```bash
git fetch origin claude/specialty-copy-scheme-lookup-u1ngx7
git checkout claude/specialty-copy-scheme-lookup-u1ngx7
git pull
```

**正式站 `fangren.net` 也還沒有這些頁**（Cloudflare 只建置 `main`）。
要在手機上看，用 `node tools/serve.mjs` 開本機，或請使用者提供 artifact 連結。

### 已經定案的（不要再問、不要重做）

| 項目 | 定案 | 依據寫在哪 |
| --- | --- | --- |
| **頁首選單的「目前在哪一節」** | **Ⓓ2 玻璃**（2026-08-10 上線）。三項**常駐一圈玻璃框**（.75px／紙色 .26），捲到哪一節那一項就多一塊底（紙色 .24）＋ 框變濃變粗（.55／1px），字轉純白。**HERO 那一屏三項都只有框**。原本的 hover 底線整條移除 —— 它只有 `:hover`，手機會黏住，三項點過就三條線一起留著 | 本檔第八節、`preview/nav-scrollspy/` |
| **那三個玻璃框的高與圓角** | **框高 `--frame-em: 2.65`（＝字級的倍數，對 `a` 垂直置中）＋ 圓角 `--frame-r: 8px`**（2026-08-11 三輪上線）。**手機／iPad／電腦版同一組值**，所以那一段是基底規則、不在任何 @media 裡。落選：高 2.36／2.15、圓角 10／6px。⚠ 頁首因此離開全站的 12px 圓角族（使用者指定，不是漏改）；8px 落在既有的小標籤那一族（`.doc dt` 6／`.card-tag` 8.5／`.info-card h3` 9）。⚠ **不要改 `a` 的 `padding-block`** —— 會連動頁首高度與 HERO 詩的 8.0px 空隙 | 本檔第八節、`preview/nav-frame-height/` |
| **頁首玻璃「招牌之前不要那麼糊」** | **平段：橫的螢幕（比例 ≥ 1:1）5px、直的螢幕 4px；轉場：招牌之前 **110px** 開始升，招牌（照片高度的 **79.9%**）之後維持 6px**（2026-08-11 三輪上線）。⚠ 成因是 `object-fit: cover` 的裁切，不是哪個裝置的模糊比較重（本來全都是 6px）——iPad 直放只留中間 46%（純立面與窗戶）、電腦看得到整張、手機的條子厚達照片高的 21% 會把天空一起平均進來。⚠ **轉場一定要用捲動距離表示，不是照片比例**：用比例寫，同一條規則在不同視窗會差四倍。⚠ **平段的分界用畫面比例不是 min-width** —— iPad 直放 834 寬會和電腦分在同一邊，而 iPad 橫放看得到 95% 的寬度、情況其實同電腦版。⚠ 「條子底下是照片的哪一列」要把 cover 一起算，`object-position` 讀 computed style 所以三種裁法都自動算對。⚠⚠ **iPad 那 4px 是在舊版型上挑的** —— 決定時 iPad 直放還吃電腦版的 100svh、只留 46% 寬（幾乎全是立面與窗戶），同一天稍晚 70svh 的 iPad 版型補上線（commit 8888946）之後放寬到 66%、天空回來了。**規則本身不受影響**（每一幀從實際盒子算，十二個視窗重驗過），但 iPad 現在比當初更不容易看出糊，4px 是偏保守的那一側 | 本檔第八節、`preview/head-blur-ipad|mobile|desktop/` |
| **點到的科別才亮起來** | **主題與科別按下去 → 帶出有關聯的醫師（專科**或**專長命中）與文章；命中的專長**淡色填滿**（該科的色 12% 混進卡色、字用深階）、沒命中的維持純文字；專科藥丸 ≠ 那一科就退成**白底字套色**；被篩到的文章卡主題標籤**套色填滿**。沒點任何一科時和改動前完全相同（四個寬度逐一元素量過）。⚠ 這一版正式放掉了「顏色 ＝ 篩選」那個不變量 | [PALETTE.md](PALETTE.md) 第六之二節、`preview/doc-spec-tags/` |
| **專長標記的貼合** | **塊要包住的是字面框、不是行框**（2026-08-11 上線）。`line-height: 1`，塊高由上下兩個內距各自負責，JS 現量成「字面上下各留一樣多」＝ **0.28 個標記字級**；側邊 **`.4546em`**（留白比 0.551，≈ `.doc dt` 的 0.553）。上下不對稱 −2.18 → −0.01px。落選 Ⓥ .385／.32、Ⓧ .5／.42rem（那一輪的 Ⓧ3 是 `.34rem`，字級定案後改寫成同比例的 em）。**`.doc-role`（專科藥丸）第五輪一起擺正**：上下各留 **0.405**（＝維持它原本的塊高 20.86），`vertical-align` 與側邊 `.6rem` 都沒動。⚠ **其餘有底色的小標籤（`.doc dt`／`.card-tag`／`.chips button`／`.info-card h3`）一顆都沒動** —— 它們單獨站著，看不出來，不要為了一致順手改 | 本檔第八節、`preview/spec-tag-fit/` |
| **套色之後那幾個字多大** | **`.83rem` ＝ 和沒套色的內文一模一樣，完全不縮**（2026-08-11 上線）。原本 `.748rem` 比內文小 1.31px／10%，標記亮起來卻讓字縮一階，方向是反的。落選 `.748`（原本）、`.79rem`。塊高 21.13px、抬升量因此正好是 **0**。⚠ **`.83rem` 更早被否決過**（會撐開行框），那個理由在塊高改成從字面框長之後不成立了。⚠ **再往上加字級的天花板不是塊高，是塊上緣離基線多遠**（現在 15.13px vs `.doc dd` strut ascent 14.97，那一列已經長高 0.13px） | 本檔第八節、`preview/spec-tag-size/` |
| 首頁區塊改名 | 「治療項目」→ **主題與科別** | [COPY.md](COPY.md) 第六節（含六輪否決清單） |
| 「最新文章」小節標題 | **拿掉**，只留 `aria-label` | 同上 |
| 窄帶瀏覽數 | 加在 1983／9／6 **上面**，單位「次瀏覽」，捲進畫面才往上數 1.4 秒 | 本檔第八節 |
| 那個動畫看不看 `prefers-reduced-motion` | **不看**（2026-08-08）。iOS 的「減少動態效果」很多人開著，看它就等於數字永遠不動；那個設定針對的是位移／視差／縮放，原地長大的數字不屬於那類。頁面其他三處 CSS 仍然照看 | — |
| 瀏覽數的字 | **黑體**（和「次瀏覽」同字同級同色），襯線版落選 | `preview/home-mobile-n3-views-serif/` |
| 文章卡計數器 | 「日期 ・ 數字 次瀏覽」，**不做動畫**。眼睛圖示與日期前的「更新」都已拿掉 | — |
| 電腦版 HERO 裁切 | **整屏 `100svh` ＋ `object-position: 50% 100%`**（下緣貼齊、要切切天空） | [PALETTE.md](PALETTE.md) 第六之三節 |
| 電腦版窄帶位置 | **壓在整屏下緣**，不是接在照片下面 | 同上 |
| **iPad 直放** | **照片 `70svh` ＋ 窄帶脫離照片接在下面**，地址電話一行置中夾在瀏覽數與三格之間。接縫保留原本的模糊＋柏油兩層（原地搬到照片最後 84px，數值一個沒改），窄帶從實測合成色 `#161413` 接下去。關在 `@media (min-width:721px) and (min-aspect-ratio:2/3) and (max-aspect-ratio:9/10)`，四組尺寸逐項比對過電腦版與手機版沒被動到 | [PALETTE.md](PALETTE.md) 第六之十八節 |
| 直立（旋轉）螢幕 | **照片不整屏 `62svh`**，字級改讀 `svh`。關在 `@media (min-width: 721px) and (max-aspect-ratio: 2/3)` 裡，**電腦版與手機版不受影響**（iPad 直放 0.75 這一段吃不到，**改由上面那一列的 `(2/3, 9/10]` 接手，不是還在看電腦版**）。直排落選 | [PALETTE.md](PALETTE.md) 第六之十三節 |
| 窄帶三格間距 | **拉開**（左右各 4rem） | 同上 |
| 兒牙的科別色 | **1a `#c28229`**（十一輪；字與框用 `#9e6301`）。兩個提案頁都已套上、切換條已刪 | [PALETTE.md](PALETTE.md) 第六之二節 |
| chip 的兩態 | 未選＝白底＋深階字與框，選取＝套色填滿。**兒牙不例外** | 同上 |
| 窄帶底色 | **毛玻璃輕 ＋ 柏油漸層「標準」**（「深」落選：底端收成實色，等於把毛玻璃還回去） | [PALETTE.md](PALETTE.md) 第六之三節 |
| 窄帶第一格文案 | 1983年 **中華路開業**（原本是「開業至今」） | — |
| **窄帶的地址與電話（電腦版）** | **一行、中間 1.9rem；貼版心外緣、離照片下緣 15.18px；線條空心圖示（Lucide）；底線常駐；靜止 `.65`／hover `.95` —— 這條帶子上只准兩個白** | [PALETTE.md](PALETTE.md) 第六之十七節、`preview/hero-contact/` |
| **同上（手機版）** | **一行置中，夾在瀏覽數與 1983／9／6 之間**；字 `.7rem`、圖示 `.762`／`.710rem`（照電腦版的比例換算，不是照搬）；≤360 間距收到 1.1rem 防折行。**標記因此必須待在 `.shell` 裡**，電腦版靠絕對定位脫離版面 | [PALETTE.md](PALETTE.md) 第六之十七節第八小節、`preview/hero-contact-mobile/` |
| 手機窄帶的 1983／9／6 | **往下坐 2px**（`.stats` margin-top 2 ＋ shell 下內距 −2）。原本上面 9.7／下面 13.8，現在 11.67／11.78，**窄帶高度沒變** | 同上 |
| 那組圖示的大小 | 釘子 `.85rem`（畫出 12.68px）、話筒 `.7918rem`（11.33px）＋下移 .8px。**要拿「字面」比不是「字級」** —— 數字沒有下伸部，字面只占字級 0.88，用字級算話筒會矮一截、看起來在飄 | 同上（第六之十七節第四小節） |
| 文章卡陰影 | 靜止就有，hover 加重；**位移／模糊／alpha 三個量一起放大**，縮圖同時 `scale(1.05)` | [PALETTE.md](PALETTE.md) 第六之四節 |
| 手機版的卡片 | **只有靜止陰影，沒有 hover**（沒有游標，`:hover` 會黏住） | 同上 |
| 兩頁誰是準 | **電腦版是準**。手機版先做、推廣成電腦版，電腦版定案後再回推手機版 | — |
| 主選單 | **兩頁都三項**（全部文章／醫師介紹／診所資訊）。「主題與科別」不放進去 | [PALETTE.md](PALETTE.md) 第六之五節 |
| 主題與科別 | 標記最前面一顆「全部」，後面接搜尋框（同時篩文章與醫師） | — |
| 「全部」的灰 | **線框 `#4c4948`／填色 `#5f5d5c`**，兩階都錨在頁首玻璃的 `#393736` 往上提亮度 | [PALETTE.md](PALETTE.md) 第六之五節 |
| 頁首「全部文章」 | 跳到 **`#topics`**（主題與科別），不是 `#articles` —— 篩選工具在那裡 | — |
| 文章頁的重點色 | **跟著該篇的科別走**，寫在 `<body data-spec="…">`。字階要用對紙的那一階，不能從首頁搬 | [PALETTE.md](PALETTE.md) 第六之六節 |
| 贋復假牙的藍 | **填色 `#335b8b`、白底的字 `#182f4b`**（Ⓑ·Ⓓ2，2026-08-09 當日第三版）。彩度要跟著亮度按色域比例走，固定住會變灰、和矯正撞在一起；**字階刻意探到 L\* 18.9，是為了讓牙周的深階活下來** | [PALETTE.md](PALETTE.md) 第六之十、十三、十四節 |
| **齒顎矯正**的藍 | **套色 `#4478b5`、白底的字 `#244369`**（2026-08-10 從牙周整組接手）。從使用者給的 `#3496F2` 收到「白底黑底等效」那條線上。**白字只有 4.57:1，是全站最緊的一格，不能再亮**。它同時解掉「矯正不夠亮」：L\* 36.3 → 49.5，**提亮那一輪（第六之十二節）整組作廢** | [PALETTE.md](PALETTE.md) 第六之十四、十六節 |
| **牙周治療**的青綠 | **套色 `#317d78`、白底的字 `#2a6d69`**（Ⓣ1，2026-08-10）。色相來自診療椅／深綠松那一支（h 186~190），**牙周等於回到診療椅**。填色頂在白字剛好過 AA 的高度。⚠ 字階對全站連結色深綠松只有 ΔE 13.6，是全站第二緊的一格 | [PALETTE.md](PALETTE.md) 第六之十六節 |
| 色票藍灰 `#3C596B` | **釋出**（矯正原本用它）。和 `--moss`／`--taupe`／`--tile` 一樣留在變數表裡，不再有科別指向它 | 同上 |
| 顯微根管的紅 | **套色 `#ae4f4d`、白底的字 `#89202d`**（⑪·⑥，2026-08-09）。深階是開幕紅花籃的實測原值。**苔綠＝Logo 色被釋出，科別色裡不再有 Logo 色 —— 這是已知的取捨，不要提議改回去** | [PALETTE.md](PALETTE.md) 第六之十一節 |
| 一般牙科・創辦醫師的綠 | **套色 `#3f654a`、字 `#2c5238`**（⑪·Ⓑ，2026-08-09）。色相＝花藝實測「枝葉亮處」的色相；兩階只差亮度 7.9。`--chair` 釋出但留著。**牙周已於同日拆出去自己一色**，不要再把它併回這一條 | [PALETTE.md](PALETTE.md) 第六之十一節第七小節 |
| 兒牙填色上的白字 | **維持白字**（3.22:1，低於 AA）。2026-08-08 把備案做成實際樣子看過才決定的，**不要再提** | [PALETTE.md](PALETTE.md) 第六之二節 |

### 還沒決定的

0. ~~頁首選單的「目前在哪一節」要用哪一案~~ —— **2026-08-10 定案 Ⓓ2 玻璃並上線**，
   見上面定案表與本檔第八節的 `preview/nav-scrollspy/`。
   > **還沒做的那一半：文章頁的頁首沒有那圈框。** `posts/<slug>/` 走
   > `assets/style.css`，那邊的選單只有純文字（hover 已在同一次改成
   > `@media (hover: hover) and (pointer: fine)`，黏住的問題兩邊都解掉了）。
   > 文章頁沒有可以偵測的區塊，所以**不會有底**，但「三個框」要不要跟著過去
   > 是版面一致性的問題 —— 使用者還沒看過那個樣子，沒有先斬後奏。
   > 要做的話得連 `--lamp-pad` 那段量測一起搬（文章頁的選單間距是
   > `clamp(.7rem, 3.2vw, 1.6rem)`，375 上 12px，框會黏在一起）。
0.5 ~~那三個玻璃框的高與圓角~~ —— **2026-08-11 三輪全部定案並上線**，見上面定案表
   與第八節的 `preview/nav-frame-height/`。框高 `--frame-em: 2.65`、圓角 `--frame-r: 8px`，
   **手機／iPad／電腦版共用同一組值**（使用者第三輪指定推到電腦版與 iPad）。
   > **留下來的一件**：文章頁（`posts/`）的選單仍然沒有框，見上面第 0 項。
1. **要不要把定案套進 `preview/home-desktop/`**，並刪掉中間過程的幾頁
   （`home-desktop-hero/` `-align/` `-banner/` `-fullscreen/`）。
   `home-desktop-band/` 自己的切換條已經全部刪掉了。
2. COPY.md 第六節列的兩件：chips 按下的狀態要不要降一階、故事類內容之後怎麼進「主題與科別」。
3. HERO 收尾詩的三案 A／B／C（[COPY.md](COPY.md) 第四節），從 2026-08-05 就擱著。
4. ~~齒顎矯正的藍要不要提亮~~ —— **2026-08-10 結案上線，見上面定案表。**
   走的不是提亮那條路：使用者從 `preview/spec-ortho-palette/` 的八案挑中青綠、**改給牙周**，
   **牙周原本的藍整組給矯正**，矯正因此從 L\* 36.3 跳到 49.5（比提亮三案最激進的 Ⓒ 還亮）。
   > **`preview/spec-ortho-bright/` 的三案與 [PALETTE.md](PALETTE.md) 第六之十二節整組作廢**，
   > 留著看推論就好，**不要再拿去實作**。第六之十五節（米色／稻草灰那八案）同理。
5. **[PALETTE.md](PALETTE.md) 第六之八節（科別的意象）要重寫**（2026-08-09 起，08-10 更嚴重）。
   顯微根管離開苔綠之後綠色段只剩一般牙科 151；**2026-08-10 牙周落在 h 190（青綠）、
   矯正搬到 h 273**，所以冷色端是牙周 190／矯正 273／贋復 273／外科 320。
   > **兩條意象已經不成立**：矯正原本綁「候診沙發」（那塊布是 `#4e718b`），
   > 牙周原本綁的是拆出來的那顆藍。反過來**牙周現在回到診療椅那一支（h 190）**，
   > 這一段其實可以寫得比原來更順。整節還沒動。
6. **手機版 HERO 的詩要怎麼排**（2026-08-09 起）。起因是使用者的朋友說
   「左上角 logo 的字跟 cover image 的字大小差不多，看起來會比較 noisy，可以增加 spacing」。
   實測成立，**而且手機比電腦嚴重**：375×812 上詩的上緣比頁首那一塊的下緣還高 2.9px，
   兩塊字是疊著的；1440×900 上也只有 63px。
   > **電腦版已結案並上線**（2026-08-09，commit `09b6f9d`）：提案 Ⓒ 鬆 ——
   > 距離 4vh→9vh（空隙 63→108px）、字距 .2em→.32em、行高 1.95→2.45、
   > 首行後間隔 .5em→.9em、max-width 34em→40em。三案的比較留在
   > `preview/head-hero-space/`（`?sp=a|b|c|c2`）。**字級一個都沒動。**
   >
   > **手機版也結案並上線了**（2026-08-10）：提案 **Ⓢ1** ——
   > `padding-top 15vw → 17.9vw`、字距 `.2em → .13em`、全形空格 `1em → .65em`
   > （375 上空隙 −2.9 → 8.0px）。行高 1.75 與首行間隔 .55em **維持原樣**。
   > 一起進去的還有兩個非動不可的等比例修正（左緣 5.13vw、字級下限 9.5px，見下面第 3 點）
   > 與收尾句的 Ⓔ2 陰影（第 10 項）。全部寫在 `index.html` 手機 @media 的長註解裡。
   >
   > 以下是當時的過程，留著避免有人重走：使用者說 Ⓐ／Ⓑ／Ⓒ 三案在手機上「都不好，壓到房子了」，
   > 並給了四點：①第一行再往下降 ②降了會壓到房子，所以收兩段文字之間的間距、
   > 甚至字距 ③第二行比照 ④第三行拆成兩行。
   > 新提案頁 **`preview/hero-mobile-skyline/`**（`?m=m1|m2|m3`，`&sky=1` 疊出天空邊界），
   > 完整推導在那一頁 `<head>` 最後的註解。要接手就從那一頁看，不要重新目測。
   >
   > **那一頁量出來的三件事，接手前先知道：**
   > 1. 照片的可用天空是一個**楔形**（逐列掃描 JPG 得到的實測表就寫在那一頁）：
   >    390 寬時右界從 y52px 的 291px 一路收到 y140px 的 182px，y216px 以下只剩 64px。
   >    詩是「左上角固定、往右往下長」，方向正好相反 —— 這才是一往下推就撞房子的原因。
   > 2. **使用者的第 4 點瞄錯行了**：拆第三行**一點高度都換不到**（拆或不拆，
   >    能降到的最低點完全一樣）。卡住整首詩的是**第一行**（16 格，最長，
   >    而屋簷正好從它右上方壓下來）。拆第一行才換得到。
   > 3. 手機的幾何原本**不是等比例的**：`--pad` 撞在 1.25rem 下限（各寬度都是 20px）、
   >    字級 clamp 的下限 10px 在 320 寬會贏過 3.06vw。兩者都會讓同一組 vw 參數
   >    「390 上剛好閃過、375／320 上就壓到」。那一頁把左緣改成 5.13vw、
   >    字級下限降到 9.5px，整組才等比例。**最壞情況是 375（不是 320，也不是 390）**：
   >    376 以上字級撞到 11.5px 上限、相對變小反而安全。
   >
   > 更大的位移只能靠拆行（Ⓜ1～Ⓜ3，最多能到 39px），使用者看過之後說
   > 「我要的感覺應該在現況和 Ⓜ1 之間」，所以才有 Ⓢ1／Ⓢ2 這兩階，最後選了 Ⓢ1。
   > **提案頁 `preview/hero-mobile-skyline/` 留著**，那六案與整條「位置↔字距」的
   > 階梯表都在裡面，日後要再調位置直接查表，不要重新目測。
7. **⚠ 全站最近的一對是 ΔE 13.3（填色）／10.5（深階）**，都低於原本的最小值 16.1；
   兩顆同色相 h 273，只差亮度。**不是待決事項，是要記住的約束** ——
   日後動到其中任何一顆，都要先把這一對重算過。見 [PALETTE.md](PALETTE.md) 第六之十四節。
   > **原本是「牙周 × 贋復」。牙周換青綠之後那組藍整組給了矯正，所以現在是
   > 「矯正 × 贋復」** —— 數字一模一樣，只是主角換人（第六之十六節）。
8. ~~牙周的青綠要幾階~~ —— **2026-08-10 定案 Ⓣ1 並上線**（兩階 `#317d78`／`#2a6d69`）。
   > **留下來的約束**：牙周的字階對全站連結色深綠松 `#214D48` 只有 ΔE 13.6，
   > 是全站第二緊的一格（最緊的是矯正×贋復 13.3／10.5）。**要動深綠松、牙周或矯正，
   > 得把這幾顆一起算。** 三案的完整數字在 `preview/spec-perio-teal/` 與第六之十六節。
9. ~~三格間距的 4rem「拉開」沒有生效~~ —— **2026-08-10 已修好並上線。**
   `.stats li { padding-inline: 4rem; }`（2026-08-07 定案）之後還有一條同權重、
   排在後面的 `.stats li { position: relative; padding-inline: 1.9rem; … }`，後者贏，
   所以**定案後整整三天線上跑的都是當初被淘汰的 1.9rem**。
   修法是把後面那一份的 `padding-inline` 刪掉、讓原本那條接手（沒有用 !important）。
   > **留下來的規則**：`padding-inline` 在這份樣式表裡**只准出現在那一行**。
   > 要調三格的間距就改那個 4rem，不要在下面的 `.stats li` 裡再寫一次。
   > 對照頁 `preview/band-stats-gap/`，數據見 [PALETTE.md](PALETTE.md) 第六之十七節第七小節。
10. **這個站的綠現在有三支**（2026-08-10 起）：一般牙科 h 151、牙周青綠 h 190、
   深綠松（連結色與診所資訊）h 186。後兩支同色相，只靠亮度分開。
   **診所資訊那顆要不要跟著重看，是還沒問過的一題。**
11. ~~收尾句「巷口」壓在車頭燈上~~ —— **2026-08-10 定案 Ⓔ2 並上線**（只在手機）。
    使用者選完 Ⓢ1 後回報「巷口」看不清楚。逐字量過照片底下的亮度（375 寬）：
    「巷」最亮 219、「口」最亮 236，白字 65% 疊上去的對比度只有 1.24／1.12
    （1.0 ＝ 完全看不見），其他十個字都在 3～7 —— **是局部一個亮點，不是整句都暗**。
    定案的 Ⓔ2 只動 `.hero-sign::before`：`rgba(25,22,20,.72) → .9`、
    範圍 `-1.1em/-1.4em → -1.5em/-2em`，**字的 text-shadow 一個值都沒改**。
    > **兩條不要違反的規則：**
    > ・**blur 維持 18px。** 一收就會看得出那是一個框，而「看不出是框、
    >   只是路面自己暗下去」正是這塊東西的設計重點。
    > ・**只在手機。** 電腦版的裁切是下緣貼齊，收尾句落在純暗的路面上，
    >   十二個字最亮處都 ≤58、對比度 ≥5.9 —— 不需要，也不該加。
    >
    > 落選的 Ⓔ1（加厚字自己的 text-shadow，最內層 `0 0 2px /.9`）留在
    > `preview/hero-mobile-skyline/?e=e1`。它對點光源更有效，
    > 但字會變重，離收尾句那種很輕的落款感遠一些。
12. ~~醫師卡與文章卡：點到的科別才亮起來~~ —— **2026-08-11 五輪定案並上線。**
    見上面的定案表與第八節的 `preview/doc-spec-tags/`。
    > **留下來的一件**：文章頁（`posts/<slug>/`，走 `assets/style.css`）**沒有這一套**
    > —— 那邊沒有篩選工具，所以沒有「亮起來」這件事。這不是漏做，是那一頁沒有這個
    > 需求。但**日後若有人改動 `.doc-role` 或 `.card-tag`，要記得那兩個選擇器現在
    > 多了 `.tag-off` / `.tag-on` 兩態**。
13. ~~套色之後專長那幾個字要多大~~ —— **2026-08-11 定案 `.83rem` 並上線**
    （＝和沒套色的內文一模一樣，完全不縮）。見上面的定案表與第八節的
    `preview/spec-tag-size/`。
    > **留下來的約束**：再往上加字級的天花板**不是塊高，是塊的上緣離基線多遠** ——
    > 現在 15.13px，而 `.doc dd` 那條 strut 的 ascent 只有 14.97px，
    > 那一列已經因此長高 0.13px（看不出來）。再加就會開始把行距推開。
14. ~~頁首玻璃「招牌之前」要多輕~~ —— **2026-08-11 三輪全部定案並上線。**
    見上面的定案表與第八節的 `preview/head-blur-ipad|mobile|desktop/` 三頁。
    > **留下來的一件**：文章頁（`posts/<slug>/`，走 `assets/style.css`）**沒有這一套**
    > —— 那邊沒有 HERO 照片，頁首底下從第一屏開始就是紙色，沒有「照片的哪一列」
    > 可以算。這不是漏做。

（兒牙白字那一件已經結案了，見上面的定案表。口腔外科的紫已定案 `#8e6299`／`#784e84`，
見 PALETTE.md 第六之九節與 `preview/spec-surg-violet/`；贋復假牙的藍已定案
`#335b8b`／`#182f4b`，見第六之十、十三、十四節與 `preview/spec-perio-split/`；
顯微根管的紅已定案 `#ae4f4d`／`#89202d`，見第六之十一節與 `preview/spec-endo-red-all/`；
**`#4478b5`／`#244369` 原本是牙周的，2026-08-10 起改屬齒顎矯正，牙周換成青綠
`#317d78`／`#2a6d69`**，見第六之十四、十六節與 `preview/spec-perio-split/`、
`preview/spec-perio-teal/`。）

### 把提案頁搬進正式站時，要一起帶的東西（2026-08-08 補，踩過）

提案頁是**自給自足的靜態快照**，正式站需要、但快照裡刻意沒有的東西，搬版型的時候
會整個掉在原地。2026-08-07 用電腦版提案頁換掉 `index.html`（commit `2c0f7aa`）時就漏了兩樣：

1. **`<script src="assets/counter.js" defer>` 整支沒帶過去。**
   結果首頁六張卡的瀏覽數永遠是「—」，窄帶那個數字則停在提案頁的**示範值 8642**
   （真值當時是 190）。文章頁一直都有掛，所以只有首頁壞。
2. **`data-views-self="home"` 被寫成 `data-views="home"`。**
   `-self` 才會 POST +1，沒有它首頁的計數就永遠不會長。

所以：**窄帶的 `data-count` 是提案頁專用的示範值，正式站絕對不要留著**，
真值只能來自 D1。`index.html` 的 `.band-views` 現在改成「真實數字回來 ∧ 捲進畫面」
兩個條件都到齊才跑動畫，並靠 `.is-on` 決定現身，在那之前是 `visibility: hidden` ——
不會先閃一個 0。API 掛掉或沒有 JS 就一直隱形，文章卡那邊則連前面的「・」一起收掉。

### 這一輪學到、很容易再踩的坑

1. **`backdrop-filter` 只作用在元素自己的框裡**，框的邊緣本身就是一條可見的線，
   跟顏色無關，調 alpha 消不掉。要用 `mask-image` 讓**模糊本身**淡入／淡出。
2. **CSS 自訂屬性的預設值那一行要排在覆寫它的規則前面。**
   `[data-spec]` 和 `[data-spec="kids"]` 權重相同，
   `[data-spec] { --accent-deep: var(--accent) }` 若排在後面，
   會把兒牙特別指定的深階字蓋回去 —— 顏色看起來「沒吃到」就是這個原因。
3. **在暗底上疊一個同樣暗的顏色，看不出任何變化。** 柏油漸層第一版整條用 `#221e1c`
   （L\* 12）疊在 L\* 6~15 的路面上，使用者說「看不出漸層」就是這個原因。
   要有變化就得換到更深的實測值（照片最底那一列 `#161413`，L\* 6.6）。
4. **iOS 沒有內建任何中文襯線字，`serif` 會整包退回黑體 —— 連數字一起。**
   這一頁是 `<html lang="zh-Hant-TW">`，WebKit 解 generic `serif` 是**照語言**去找的，
   找不到中文襯線就退回系統中文字（PingFang），而 PingFang 有拉丁字，
   所以 1983／9／6 也跟著變黑體。電腦上看不出來（有思源宋體／Times）。
   解法是把一個**確定存在於 iOS 的拉丁襯線字**排在 generic 前面：
   `"Noto Serif TC", "Source Han Serif TC", "Times New Roman", Times, serif`。
   字體比對是逐字做的 —— 數字命中 Times，中文在 Times 裡找不到就繼續往後掉到
   `serif`，跟以前一模一樣，**所以中文一個字都不會變**。
5. **rAF 與 IntersectionObserver 在 `document.hidden` 的分頁裡完全不會被呼叫**，
   連 IO 的第一次回呼都不送。窄帶數字原本「IO 說看得到才開跑」，
   在那種情境下會永遠停在 0。現在改成自己 `getBoundingClientRect()` 判斷，
   並在動畫外面加一條 `setTimeout` 保險絲，時間到了直接寫最終值。
6. **iOS Safari 的 `window.innerHeight` 不是眼睛看得到的高度** —— 它是工具列收起後的
   大視窗高度，比當下可見範圍高出整條網址列（100vh 在 iOS 爆框是同一個原因）。
   拿它判斷「捲進畫面了沒」會**提早**成立，動畫在使用者還沒捲到之前就跑完，
   等他捲到時數字早就停在最終值 —— 症狀是「iPhone／iPad 上完全不會動」。
   要用 `visualViewport.height`（iOS 9.3 以後都有），退回 `innerHeight`。
7. **不要在頁面還在載入的那一刻開跑動畫。** 手機上那一刻主執行緒在解 HERO 那張大圖，
   rAF 拿不到幾個影格，1.4 秒的動畫被壓成一兩下跳動，等於沒有動畫。
   窄帶數字現在多等一個條件：`load` 之後再 300ms（上限 3 秒，圖再慢也要讓數字出現）。
8. **CSS 註解裡不能出現 `</style>` 那個字串**（2026-08-11，`preview/doc-cross-spec/` 踩過）。
   HTML 解析器找結束標籤是**純字串比對**，不管它在不在 CSS 註解裡 —— 寫下去樣式表就在
   那一行被切斷，後面整段全部失效。**症狀最惡劣的地方是它不報錯**：畫面照常、
   舊樣式都在，只有新加的那一段「沒吃到」，很容易往選擇器權重或快取的方向找一小時。
   `<script>` 裡的 `</script>` 同一條。這一站的註解寫得很長，特別容易踩。
   要在註解裡指這件事，就用文字描述（「這份樣式表結尾」），不要貼標籤本身。
9. **中文小字和大字並排，對基線就是低的**（2026-08-11，`preview/doc-spec-tags/` 量出來）。
   瀏覽器對的是**基線**，眼睛讀的是**字面中線**；中文字面中線在基線上方 0.345em
   （Noto Sans TC，400px 下實測 `李柄輝`／`創辦醫師`／`口腔檢查`／`一般牙科治療`
   四組 ink 中線 0.336~0.359em）。所以小字每比大字小 1px，看起來就低 0.345px。

       要往上抬的量 ＝ 0.345 × (父層字級 − 自己的字級) ÷ 自己的字級　（單位 em）

   ⚠ **`em` 指的是元素自己的字級**，所以同樣是 `.72rem` 的兩顆標記，父層不同值就不同：
   醫師卡的專科藥丸（父層 h3 16.8px）是 `.158em`，專長標記（父層 dd 13.28px）是 `.053em`。
   ⚠ **`.doc-role` 正式站本來就沒寫 vertical-align**，所以線上那顆一直低 1.82px。
   ⚠ 抬升會不會撐高行框要自己算：抬完之後元素離基線的高度只要還在 strut 的
   ascent 以內就不會（那一輪是 16.26 < 18.42、15.04 < 16.22，十一個視窗實測沒變）。
   ⚠ **符號正負很容易寫反**：`vertical-align` 正值是**往上**。第一版寫成 `-.12em`
   反而把字往下推了 1.38px，加上原本就低的 0.61 共 1.99px，使用者一眼就看出來。
   ⚠⚠ **有底色的標記要對「塊」，不是對「字」**（第二輪才發現，同一天）。
   把字對齊之後使用者仍然說「看起來對到下緣」—— 因為**填滿的標記，眼睛讀的是那個
   色塊**，而**塊裡的字本來就不在塊的正中央**：塊高 ＝ line-height ＋ 內距 ＋ 框，
   那條線對的是字型的 ascent／descent，中文字面中線比它高 **0.66px**（.72rem 的標記）。
   所以「字對齊了」＝「塊往下掉 0.66px」。要對塊就在字對字的量上再加那 0.66px：
   `.doc-role` .158 → .218em、專長標記 .053 → .111em。
   **判斷方法**：量「塊上緣比旁邊字的 ink 上緣高多少」與「塊下緣比 ink 下緣低多少」，
   兩個數字要一樣。修完是 3.85 / 3.87（原本 3.20 / 4.53）。
   ⚠ **同樣的偏差，字愈小愈明顯**：藥丸旁邊是 16.8px 的名字，1.38px 的不對稱看不太
   出來；標記旁邊只有 13.28px，4.53px 的下墜佔了三分之一行高，一眼就看到。
   ⚠⚠⚠ **抬升量算得出來，但不能寫死** —— Blink 把 inline-block 的**塊內基線位置
   進位到整數 px**，所以「塊中線離自己基線多少」不是字級的線性函數（實測五個字級
   的「基線離塊頂」是 13.797／13.797／14.797／15.797／15.797，跳階），
   **而且同一個字級在不同 DPR 上會落在不同的整數**（同一顆標記 DPR1 要抬 0.274px、
   DPR3 要抬 1.274px）。寫死的 em 會在某些字級／某些裝置上差到 1px。
   解法是**開頁時放一顆零寬探針量一次**，把差額寫進 CSS 變數（`nav-scrollspy` 的
   `--lamp-pad` 是同一個做法）。
   ⚠ **量「那一行的基線」時，探針要插在標記的後面，不是前面。** 三種寫法踩過兩種：
   ①挑一顆純文字當參照 —— 那些詞自己會折行、rect 跨兩行，差幾十 px；
   ②插在前面 —— 標記被擠到下一行時探針留在上一行（375／320 上量到 −19.44 就是這個）；
   ③**插在後面** —— 零寬的東西不會自己折行，一定留在標記那一行。
   ⚠⚠⚠ **第三輪（同一天）：上面兩輪對的都是「塊相對於旁邊那行字」，但塊心本來就
   不在自己那幾個字的中線上，所以對完看起來還是低的。** 有底色的標記，塊的大小
   一直是從「行框 ＋ 內距」長出來的，而**行框對的是字型的 ascent／descent** ——
   那兩個值涵蓋拉丁字母的上伸與下伸部，中文方塊字用不到那麼多，
   **而且用不到的部分上下不一樣多**（`line-height: 1.5` ＋ 上下各 .05rem 的結果是
   上面 3.51px、下面 5.69px，**下面多 62%**）。
   解法是把行框從尺寸裡拿掉：**`line-height: 1`，塊高改由上下兩個內距各自負責**，
   由 JS 現量成「字面上下各留一樣多」。做法與落選案見 `preview/spec-tag-fit/`。
   ⚠ 這個偏差**這一站每一顆有底色的小標籤都有**（`.doc dt` 0.389／0.542em、
     `.doc-role` 0.354／0.519、`.card-tag` 0.439／0.546、`.chips button` 0.604／0.821）。
     只有專長標記被看出來，是因為它夾在一句話裡、左右都有字可以對。
     **其餘那幾顆這一輪一個值都沒動**，不要為了「一致」順手改。
   ⚠ 量字面框用 `canvas` 的 `actualBoundingBoxAscent/Descent`，但**要在 400px 上量
     再等比例縮** —— Blink 回來的值顆粒很粗（實測 400px 上以 1/64 em 為階）。

### 提案頁的切換條

`home-desktop-*` 幾頁都有切換條，網址可帶參數直接開到某一案
（`?band=`、`?gap=`、`?hero=`、`?align=`、`?fill=`）。
**參數的正規式要寫 `[a-z0-9]+`**，寫成 `[a-z]+` 會吃不到 `glass1` 這種帶數字的值，
比對失敗後悄悄退回預設，等於參數沒作用 —— 已經踩過一次。

切換條全部是提案用的，**定案後連同 `data-*` 屬性一起刪掉**，不要留到正式站。

---

## 十、結構化資料與實體（2026-08-10）

整站的 JSON-LD 由 `tools/build.mjs` ＋ `tools/schema.mjs` 產生，寫進每一頁
`<head>` 的 `<!-- SEO:START -->` ~ `<!-- SEO:END -->` 之間。**手改會在下次 build 被蓋掉。**

首頁是一份 `@graph`：`Dentist`（`#dentist`）＋九位醫師（`Person` ＋ `Physician`）
＋ `WebSite` ＋ `WebPage`。六篇文章各一份：`BlogPosting` ＋ `WebPage` ＋
`BreadcrumbList` ＋ 一份**精簡的**診所節點 ＋ `WebSite`。

### 不要違反的規則

1. **資料不重抄。** 看診時間讀 `#clinic` 那張資訊卡、醫師名冊讀 `#doctors`、
   科別讀 `#topics` 的 chips。要改看診時間就去改那張卡，JSON-LD 會自己跟上。
   `clinic.json` 只放**頁面上沒有**的東西（sameAs、座標、郵遞區號、國碼電話）。
2. **文章的 `author` 指向診所，不是某個人。** 2026-08-02 定案的「全站移除作者署名
   與醫療審閱」仍然有效 —— 這是機器讀的欄位，畫面上不會多出任何一行字。
   **不要為了 E-E-A-T 把署名或「醫療審閱」加回頁面上。**
   同理，文章頁刻意用 `WebPage` 而不是 `MedicalWebPage`（後者的重點欄位是
   `lastReviewed`，宣告一個沒有人做的審閱等於造假）。
3. **部定專科 ≠ 訓練經歷**，`hasCredential` 只收「衛生福利部…專科／專科醫師」。
   第一版只比對開頭，把李侑津醫師的「**衛生福利部雙和醫院**」（那是他任職的醫院）
   認成專科認證，等於幫他掛一個沒有的資格。規則同 COPY.md 第八之一節。
4. **分享圖不能用 SVG。** Facebook 與 LINE 的爬蟲不吃 SVG，`og:image` 指到 `.svg`
   等於沒設。點陣複本由 `node tools/og-images.mjs` 產生（`hero-gum.svg` →
   `hero-gum-1600.png`，命名沿用「後綴＝寬度」的慣例），**要一起 commit**。
   新增文章、或換了 HERO 插圖之後要記得跑一次。
5. **首頁 `WebPage.dateModified` 用佔位符**，等 `homeHash` 算完才換成真日期。
   先填日期會變成「換了日期 → 雜湊變了 → 下次又換成今天」的循環，
   首頁的 lastmod 從此天天跳。
6. **`sitemap.xml` 帶圖片擴充**（`xmlns:image`），列的是那一頁上真的有的圖：
   首頁三張診所實景、文章各自的 HERO。只寫 `<image:loc>` ——
   `image:caption`／`title`／`license` 這幾個 Google 2022 年就停用了。
7. `WebSite` **不宣告 `SearchAction`** —— 首頁那個搜尋框是純前端篩選，
   沒有會回結果頁的網址，宣告一個不存在的端點等於說謊。

### 這三種標記不要加（加了沒有回報，或會扣分）

| 類型 | 為什麼 |
| --- | --- |
| `FAQPage` | Google **2026-05-07** 起停止顯示 FAQ 複合式搜尋結果，6 月移除 Search Console 報表、8 月移除 API 資料 |
| `HowTo` | 2023-09 就下架了。〈貝氏刷牙法〉是典型的 how-to，還是不要加 |
| `Review`／`AggregateRating` | 在自己網站上標自己的星等是明文禁止的（self-serving review），會拖累整站 |

### 還沒補上的（都需要使用者提供，不要自己猜）

1. ~~座標~~ —— **2026-08-10 已填**：`23.7101740, 120.5468936`（使用者在 Google 地圖上
   長按診所位置取得）。`clinic.json` 的 `geo`，只出現在首頁的 `Dentist` 節點上。
2. **點陣 logo**（`clinic.json` 的 `logo`）—— Google 的 Organization logo 要 PNG／JPG，
   站上只有 `assets/favicon.svg`。直接放大不行，它的牙洞位置是為了 16px 分頁列調過的
   （PALETTE.md 第六之七節）。要補得先決定用哪個綠、底色與留白 —— 那是配色的決定。
3. **文章 `about` 的 `sameAs`** —— 六篇的 `about` 已於 2026-08-10 填好
   （`post-meta` 的 `about` 欄位，實體全部是從各篇自己的 `<h2>` 與內文挑的），
   但**沒有填 Wikidata 的 `sameAs`**：雲端 session 連不到 wikidata.org，
   而 Q 編號猜錯等於把文章綁到另一個疾病上。
   要補的格式是 `{ "type": …, "name": …, "sameAs": "https://www.wikidata.org/wiki/Q…" }`，
   在有網路的電腦上做。沒填 `about` 的文章會整個略過這個欄位。
4. **`sameAs` 的完整網址** —— Google 地圖與 FB 放的是分享短網址（會轉址，Google 跟得上，
   但完整網址更穩）。LINE 那條 2026-08-10 已由使用者提供並確認：
   `https://line.me/R/ti/p/@445rpiiv`（系統 ID）。
   ⚠ **不要改成 `@fafa070`** —— 那是同一個帳號的基本 ID，我曾據此推出
   `https://page.line.me/fafa070` 放進去，那是推的、沒驗證過，已經換掉。

### ⚠ 已經發現、還沒解決的資料不一致

**LINE 官方帳號上的門診時間和網站不一樣**（2026-08-10 從使用者的手機截圖看到的）：

| | 網站 `#clinic` | LINE 官方帳號「門診時間」 |
| --- | --- | --- |
| 上午 | 08:45–11:30 | 08:45–**12:00** |
| 下午 | 13:45–16:30 | 13:45–**16:40** |
| 晚上 | 17:45–20:00 | 17:45–**20:30** |

LINE 那邊還另外列了一組「營業時間」（早班 08:35–12:05…），是第三組數字。
**哪一組才對只有診所知道**，所以 JSON-LD 目前照網站上的那一組產生，沒有動它。
確認之後改 `index.html` 的看診時間卡就好，結構化資料會自己跟上 ——
但也要**同步改 Google 商家檔案與 LINE**，三邊不一致會直接傷到本地搜尋的實體辨識。

順帶一筆：電話的顯示寫法 2026-08-10 由使用者定案統一成 **`(05)533-9369`**，
全站（首頁三處、六篇文章頁尾、404）都已經改好，JSON-LD 是 `+886-5-533-9369`。
數字一個都沒變，`tel:+88655339369` 也沒動。推導與實測數字在
[PALETTE.md](PALETTE.md) 第六之十七節。
