# 芳仁牙醫診所 — 配色規範

這份是網站配色的**唯一依據**。所有數字都是從診所自己的照片量出來的，不是目測調的。
要改網站配色、或做新的頁面，先讀這份，不要另外憑感覺挑色。

- 最後更新：2026-08-08
- 取色來源：桌面 `02-Final`（開幕茶會約 290 張手機紀錄、專業攝影原檔、`Logo顏色.pptx`）、
  桌面 `看板_20260226`（診所現用的 LINE 圖文選單）
- 量測方法：見本文最後一節
- 對應的預覽頁：**`preview/home-mobile-n3/`（定案的手機首頁：底色 ＋ 固定頁首 ＋ 科別色）**、
  `preview/canvas/`（配色數據頁）、`preview/night-calibrated/`（全暗版）
- 線上都看得到，網址是 `https://fangren.net/preview/<名稱>/`，沒有密碼

> **在手機／雲端 session（claude.ai/code）工作時**：那邊跑在 Linux 容器裡，
> **看不到使用者電腦上的照片，也不能跑 `tools/palette-measure.ps1`**（PowerShell + .NET，
> 只在 Windows 本機能跑）。所以在雲端請**直接把這份文件的表格當成事實來源**，
> 不要說「我讀不到資料夾」然後改用目測配色 —— 需要的數字都已經量好寫在下面了。
> 只有要測「新的照片」時才需要回到 Windows 本機。

---

## 一、最重要的一件事：診所已經有官方色票

`Logo顏色.pptx` 裡就是這八個色。**家具、磁磚、開幕花藝都是照它挑的**，
所以網站不需要再發明配色，直接用這套。

| 名稱 | HEX | 色相 H | 彩度 S | 明度 L |
| --- | --- | --- | --- | --- |
| Deep Taupe | `#7D5A58` | 3 | 17.4% | 41.8% |
| 焦糖褐 | `#9B735E` | 21 | 24.5% | 48.8% |
| 磚紅 | `#AF4C52` | 356 | 39.4% | 49.2% |
| 灰紫 | `#5F5D66` | 253 | 4.6% | 38.2% |
| 藍灰 | `#3C596B` | 203 | 28.1% | 32.7% |
| 深綠松 | `#214D48` | 173 | 40.0% | 21.6% |
| 苔綠（Logo） | `#5D6D55` | 100 | 12.4% | 38.0% |
| 灰綠 | `#A1A398` | 71 | 5.6% | 61.8% |

### 色票 vs 診所實物（實測色相差）

| 診所裡量到的 | 實測色 | 對應色票 | ΔH |
| --- | --- | --- | --- |
| 診療椅 | `#4f6361` | 深綠松 | **1** |
| 候診沙發布料 | `#4e718b` | 藍灰 | 3 |
| 開幕花・深紅 | `#89202d` | 磚紅 | 3 |
| 圓凳與木件 | `#8d5e3a` | 焦糖褐 | 5 |
| 磚紅長椅 | `#7c3721` | 焦糖褐 | 6 |
| 單椅布料 | `#8e7577` | Deep Taupe | 8 |
| 茶室深藍磁磚 | `#1d273d` | 藍灰 | 18 |

七項裡六項落在 8° 以內。這不是巧合，是同一套色票執行出來的。

---

## 二、材質實測（灰階的依據）

### 清水模

| 取樣 | 色 | 彩度 | R−B |
| --- | --- | --- | --- |
| 外牆特寫（白天） | `#d6d4d3` | 3.5% | +3 |
| 外牆特寫（白天） | `#cbcac9` | 1.9% | +2 |
| 外牆特寫（白天） | `#c7c5c5` | 1.8% | +2 |
| 外牆特寫（白天） | `#97999c` | 2.5% | −5 |
| 夜・受光面 | `#83786d` | 9.2% | +22 |
| 夜・暗部 | `#1a110c` | 36.8% | +14 |

**關鍵結論：白天的清水模幾乎是純中性（彩度 1.8~5.1%），夜裡才變暖**
（受光面 9.2%、暗部 36.8%）——因為夜裡照到它的只有診所自己的燈。
所以暗色版的灰階規則是「**暗處最暖、往亮處收回中性**」，不是整條均勻微暖。

### 其他硬裝

| 取樣 | 色 | 彩度 | 備註 |
| --- | --- | --- | --- |
| 診療室白牆 | `#a09a94` | 5.9% | R−B +12 |
| 候診深灰地磚 | `#45413c` / `#36312b` | 7.0% / 11.3% | 微暖 |
| 候診深灰地磚（另一處） | `#4c4f50` | 2.6% | R−B −4 |
| 木地板 | `#a18262` / `#b28450` | 25.1% / 38.9% | H=30 / 32 |
| 木櫃格 | `#956d4a` | 33.6% | H=28 |
| 茶室深藍磁磚 | `#0c111c` / `#1d273d` / `#252d4a` | 33~40% | H=221~227 |

### 診所自己的數位介面（LINE 圖文選單，2026-08-06 加測）

素材：桌面 `看板_20260226`（AlleyPin 做的 LINE RichMenu，含實機截圖 `IMG_1452/1453/1454.PNG`）。
這是診所**現在正在對外使用**的介面，所以它的底色比任何推導都有份量。

| 取樣 | 色 | 彩度 | R−B | 備註 |
| --- | --- | --- | --- | --- |
| 選單底（四邊都量過，一致） | `#e8e8e8` | **0.0%** | **0** | 明度 91.0%，**完全中性** |
| 按鈕白 | `#f8f8f8` | 0.0% | 0 | 明度 97.3% |
| 按鈕陰影／紋理暗部 | `#d3d2d2` | 1.1% | +1 | 明度 82.5% |
| 圖示・深綠松 | `#234b48` | 36.4% | −37 | 官方色票 `#214D48`，ΔH=2 |

**兩個結論：**

1. **診所對外的介面用的是純中性灰，不是暖灰、也不是米。** 這是網站內文底色改用中性灰的直接依據。
2. **底對按鈕白的階差是 1.154:1。** 灰要有東西比才讀得出來——見第四節 B。

版面概念也可以參考：中性灰底＋細線條插圖當紋理＋六格圓形按鈕，
彩色只出現在圖示上，而且每個圖示各一色（深綠松、苔綠、焦糖褐、藍灰、Taupe、磚紅），
正好是第六節第 2、3 條的實作。
使用者不要的是上面那個**錯落配置的細線 logo**，覺得在網站上會顯得凌亂。

### 夜的光

| 取樣 | 色 | 彩度 | 色相 |
| --- | --- | --- | --- |
| 夜空（藍時刻） | `#373b42` | 9.1% | 218 |
| 窗光 | `#ac743d` | 47.6% | 30 |
| 入口燈 | `#ccb393` | 35.8% | 34 |

**木頭與燈光是同一條色相軸**：木地板 H=30／另一間 H=32／窗光 H=30／入口燈 H=34。
夜空 H=218 是整棟建築唯一天然的冷色，而沙發布料的藍灰 H=206 是最接近它的顏色。

---

## 三、軟裝與花藝

### 家具布料（使用者說的「調色出現在布料上」）

| 位置 | 色 | 彩度 | 色相 |
| --- | --- | --- | --- |
| 候診沙發 | `#4e718b` / `#38607b` | 28.1% / 37.4% | 206 / 204 |
| 長椅 | `#2d4455` | 30.8% | 206 |
| 單椅 | `#8e7577` / `#967e7c` | 10.0% / 11.0% | 355 / 5 |
| 磚紅長椅 | `#7c3721` | 58.0% | 15 |
| 診療椅（醫療設備，非家具） | `#4f6361` | 11.2% | 174 |

### 花藝（開幕茶會）

| 照片 | 花色 | 花佔畫面 | 其餘 |
| --- | --- | --- | --- |
| 門口紅花籃 | `#89202d` | 13.0% | 灰、黑、白 |
| 門口紅花籃（另一張） | `#731923` | 11.2% | 灰、木、白 |
| 含羞草 | `#9d8313` | 13.7% | 灰牆、深色 |
| 繡球 | `#6c1226` | 21.5% | 灰、深綠 |
| 茶席櫻花 | `#a0a8b0` | 1.1% | 深藍磁磚 78% |

枝葉本身：`#2f3929`（H=98）、亮處 `#447c56`（H=139）。

**花本身只佔畫面 1~22%。** 這是「花很顯眼但不吵鬧」的量化依據，也是下面版面規則的來源。

### 識別物

名片深綠 `#313734`、名片深藍 `#1d2327`、帆布袋 `#d1c7bc` / `#aba297`。

---

## 四、網站用的兩套色

### A. 暗夜（只用在 HERO 與它下面那條窄帶）

```css
--night:      #191614;  /* 底。彩度 11.1%，R−B +5 —— 深灰地磚／夜間暗部 */
--night-2:    #211e1b;  /* 面 */
--night-line: #38342f;  /* 分隔線 —— 清水模暗部 */
--night-text: #e3e1e0;  /* 主文字。彩度 5.1%，R−B +3 —— 清水模亮部 */
--night-soft: #aaa49e;  /* 次要 —— 診療室白牆 */
--night-faint:#969089;  /* 更淡 */
--wood:       #a18262;  /* 地板實測色。全站只有一條 3px 線用它 */
```

暗底上的文字對比（全部通過 AA，最低 4.70:1）：
主文字 13.82:1／次要 7.30:1／更淡 5.70:1。

### B. 室內（內文區）— **2026-08-06 定案：N3 藍灰調**

```css
--paper:    #e2e5e6;   /* H=203・L=89.4%・R−B −4 */
--card:     #f4f4f5;   /* 往純白推，紙才讀得出是灰 */
--rule:     #cdd0d2;
--ink:      #2a2c27;   /* 主文字 11.15:1 */
--ink-soft: #5c5f57;   /* 次要文字 5.14:1 */
```

色偏取官方色票的**藍灰 `#3C596B`（H=203）**——候診沙發、長椅、夜空同一條軸，
是這棟建築唯一天然的冷色。明度定在 89%，比原本那組低 3 個百分點。

**兩件事比色相更重要，改配色時不要弄丟：**

1. **卡要往純白推，紙才讀得出是灰。** 灰是比出來的，不是調出來的。
   舊的紙 `#ecede7` 對卡 `#f6f7f2` 只差 **1.094:1**，兩塊都偏暖，
   眼睛沒有參照物就一起讀成白——使用者的原話是「單獨看不覺得灰」。
   診所自己的 LINE 圖文選單，底 `#e8e8e8` 對按鈕白 `#f8f8f8` 差 **1.154:1**。
   現在這組是 1.152:1，照著看板走。
2. **不要用暖色偏。** 診所現用的對外介面實測是**純中性**（見第二節「診所自己的數位介面」）。

#### 被換掉的那三個（留著當紀錄，不要再用）

2026-08-04 的「底色三選一」都在明度 92% 那一帶，太淺，灰讀不出來：

| | HEX | R−B | 使用者的判斷 |
| --- | --- | --- | --- |
| ① 清水模灰 | `#edeceb` | +2 | 「最中性的灰很不清楚」 |
| ② 帆布米 | `#f1ece5` | +12 | 沒選 |
| ③ 品牌灰綠（原主案） | `#ecede7` | +5 | 「單獨看不覺得灰」，比較起來偏米 |

同期做過但沒選上的深灰提案：G1 灰紫 `#dedce5`（L88、R−B −7，使用者說「有點紫紫的」）、
G2 灰綠 `#e4e6db`（L88、R−B +9）。**使用者明確表示不要帶紫的感覺。**
中性灰另外兩案：N1 看板灰 `#e8e8e8`、N2 清水模灰 `#dedede`（都是純中性，R−B 0）。
對應的預覽頁 `preview/home-mobile-g1` `-g2` `-n1` `-n2` `-n3`，定案的是 `-n3`。

---

## 五、哪個色能當什麼（實測，不要繞過）

### 淺底上（紙 `#ecede7` / 卡 `#f6f7f2`），官方色票原值直接用

| 色票 | 紙 | 卡 | 能當 |
| --- | --- | --- | --- |
| 深綠松 `#214D48` | 8.03:1 | 8.79:1 | 內文、標籤、標題 |
| 藍灰 `#3C596B` | 6.29:1 | 6.88:1 | 內文、標籤、標題 |
| 灰紫 `#5F5D66` | 5.50:1 | 6.01:1 | 內文、標籤、標題 |
| Deep Taupe `#7D5A58` | 5.13:1 | 5.61:1 | 內文、標籤、標題 |
| 苔綠 `#5D6D55` | 4.71:1 | 5.15:1 | 內文、標籤、標題 |
| 磚紅 `#AF4C52` | 4.48:1 | 4.90:1 | **只能大字**（標語、標題） |
| 焦糖褐 `#9B735E` | 3.56:1 | 3.90:1 | **只能大字** |
| 灰綠 `#A1A398` | 2.17:1 | 2.38:1 | **不能當文字**，只作底或色塊 |

### 暗底上（`#191614`），色相彩度不動、只提明度

原值幾乎都撐不住（深綠松只有 1.90:1、藍灰 2.43:1），必須提亮：

| 色票 | 原值 | 原值對比 | 暗底建議值 | 對比 |
| --- | --- | --- | --- | --- |
| Deep Taupe | `#7D5A58` | 2.98:1 | `#a58280` | 5.23:1 |
| 焦糖褐 | `#9B735E` | 4.29:1 | `#a7826f` | 5.20:1 |
| 磚紅 | `#AF4C52` | 3.41:1 | `#c37479` | 5.23:1 |
| 灰紫 | `#5F5D66` | 2.78:1 | `#8b8893` | 5.18:1 |
| 藍灰 | `#3C596B` | 2.43:1 | `#658fa9` | 5.19:1 |
| 深綠松 | `#214D48` | 1.90:1 | `#439b91` | 5.43:1 |
| 苔綠 | `#5D6D55` | 3.24:1 | `#7c9071` | 5.22:1 |
| 灰綠 | `#A1A398` | **7.04:1** | 不用改 | 7.04:1 |

提亮時**色相與彩度一度都不要動**，只加明度。原值留給亮底、名片、招牌繼續用。

---

## 六、版面規則（從花藝的邏輯來的）

1. **綠是枝條，可以重複出現。** 深綠松與苔綠可以在同一頁出現多次
   （例如「現在看診中」的燈點、分類標籤）。
2. **花一種只出現一次。** 磚紅、Deep Taupe、藍灰各自只用在一個地方，彼此隔開，
   中間全是紙的顏色。實測花藝照片，花只佔畫面 1~22%——版面也照這個比例走。
3. **任何一個手機屏幕上，最多只同時看到兩個彩色。**
4. **暗夜只留在 HERO**，不延伸到內文區。~~兩者的分界是那條 3px 木色線 `#a18262`。~~

   **2026-08-06：木線已依使用者要求拿掉，不要加回去。** 他的理由有兩層：
   在網頁上瀏覽時那條線很突兀，而且**拿地板當診所識別色本身就站不住**——
   識別色應該另外選。現在暗夜窄帶直接接到內文底，中間什麼都沒有。
   `--wood: #a18262` 這個實測值留在本文件第二節當材質紀錄，但**不再有任何元件用它**。

   **識別色尚未決定**，決定之前不要臨時挑一個色來補這個位置。
5. **數字不上色**（1983／9／6 用墨色）。那不是需要被強調的東西。
6. 暗色版裡，彩色只出現在三個地方：文章分類標籤、「現在看診中」的燈點、電話號碼。
   暗色系最容易失敗的地方就是到處都在發光。
7. **不要把 `#A1A398` 拿來當文字**（見上一節）。

---

## 六之二、醫師介紹與診所資訊的科別色（2026-08-07 定案）

對應的預覽頁：`preview/home-mobile-n3/`。這一節推翻了第六節第 3 條，理由寫在最後。

### 藥丸是實心底＋純白字，不是淡底＋色票字

先做過「淡底＋色票原值的字」，走不遠。**藥丸字要維持色票原值，底就壓不下去**：

| 底的濃度（色票混進純白） | 還撐得住 AA 的上限 |
| --- | --- |
| 深綠松 `#214D48`／藍灰 `#3C596B` | 22% |
| Deep Taupe `#7D5A58` | 18% |
| 苔綠 `#5D6D55` | 15% |
| 磚紅 `#AF4C52` | **10%**（12% 就掉到 4.48:1） |

磚紅幾乎動不了，那就是這條路的天花板。**反白反而最寬鬆**，而且六個色的視覺重量終於一致
（淡底那組深綠松 8.31:1 對磚紅 4.75:1，差了快一倍）。

### 六個科別色

藥丸字級 11.5px，AA 要 4.5:1。下表是**純白字疊在該色上**的實測值：

| 科別 | 色 | 來源 | H | 白字對比 |
| --- | --- | --- | --- | --- |
| 一般＋牙周植牙 | `#4f6361` | 診療椅實測（第一節） | 174 | 6.38:1 |
| 顯微根管 | `#5D6D55` | 色票・苔綠（Logo） | 100 | 5.55:1 |
| 齒顎矯正 | `#3C596B` | 色票・藍灰（候診沙發） | 203 | 7.41:1 |
| 口腔外科 | `#7D5A58` | 色票・Deep Taupe（單椅布料） | 3 | 6.04:1 |
| 贋復假牙 | `#252d4a` | 茶室深藍磁磚實測（第二節） | 227 | 13.52:1 |
| 兒童牙科 | `#8d5e3a` | 圓凳與木件實測（第一節） | 28 | 5.53:1 |

診所資訊維持深綠松當家色（它同時是全站連結色），看診時間那張卡是 Deep Taupe。

**三個用實測色而不是色票原值，各有理由，不要「順手改回色票」：**

- `#4f6361` 診療椅 —— 使用者指定給創辦醫師與李侑津醫師。對深綠松 ΔH=1，同一個色的實物版。
- `#8d5e3a` 圓凳與木件 —— 兒牙原本是磚紅，**使用者說紅在品牌裡的暗示性太強，先不要用**。
  換到暖色這一支之後，色票的焦糖褐 `#9B735E` 太淺、白字只有 4.19:1 撐不住，
  所以取同一族的實物實測值（對焦糖褐 ΔH=5）。
- `#252d4a` 茶室深藍磁磚 —— 口外與贋復是不同科，使用者要求顏色不要重複。
  贋復（全口重建）拿整個色盤裡最深的那一個。

**進不來的三個：** 焦糖褐 `#9B735E` 白字 4.19:1；灰綠 `#A1A398` 白字 2.56:1；
灰紫 `#5F5D66` 白字 6.47:1 過得了，但**使用者不要帶紫的感覺**（見第四節 B）。

### 兒牙改黃色系（2026-08-07 提案中，三案未定）

使用者要「從配色資料庫裡找黃色系」給兒牙。先講一個事實：

> **這份文件裡真正量到的黃只有一個 —— 開幕茶會的含羞草 `#9d8313`**
> （第三節花藝，H=49・S=78%・L=35%）。其餘暖色全落在 H=15~34
> （磚紅長椅 15、焦糖褐 21、圓凳 28、窗光 30、入口燈 34），那是褐與琥珀，不是黃。

所以三案**只有色相是變數**，不是各挑一個：

| | HEX | H | S | 白字 | 卡 `#f4f4f5` 上當字 |
| --- | --- | --- | --- | --- | --- |
| 現在（褐） | `#8d5e3a` | 26 | 42% | 5.53:1 | 5.03:1 |
| 1 琥珀黃 | `#966113` | 36 | 78% | 5.23:1 | 4.76:1 |
| 2 中間 | `#8d6811` | 42 | 78% | 5.10:1 | 4.64:1 |
| 3 含羞草 | `#7f6b10` | 49 | 78% | 5.23:1 | 4.76:1 |

規則：**色相**從含羞草的 49 往現有暖色軸靠，取 49 / 42 / 36 三階；
**彩度**固定 78%（含羞草自己的值，一度不動）；
**明度**取「白字仍過 AA 的**最亮**值」—— 黃色越亮越像黃，所以這裡不是壓深，是頂著上限取最亮。

> **黃色在這個系統裡有物理上限，這不是調得不好。**
> 兒牙這一色要同時當**實心藥丸的底（白字）**和**卡上的線性標籤（它自己當字）**，兩邊夾殺。
> 含羞草原色白字只有 **3.69:1**、卡上 **3.36:1**，兩個都不夠；壓到過關的位置它就會讀成
> 芥末／橄欖。**要真正明亮的黃，得先讓掉「實心白字」或「線性標籤過 AA」其中一條。**

#### 第二輪：使用者說「不是黃，是咖啡色、拿鐵色、泰奶色」

他是對的，而且**這不是調偏了 —— 黃色沒有深色版本**。把黃壓暗，它必然變成
橄欖、芥末、拿鐵。當時做過「亮黃 `#e6c637` ＋ **墨字**」（墨字 8.40:1），
但使用者判斷：

> 「為了迎合這個黃，標籤裡的字變成黑色，這樣很突兀，**還是要維持白字**。」

**所以白字是硬條件。** 那一版連同 `--accent-text` / `--on-accent` 兩個衍生變數
一起還原，不要再走「換字色換亮度」這條路。

#### 第三輪：不換字色，改用 Lab 把彩度推到天花板

**前兩輪用 HSL 挑，那是錯的工具 —— HSL 的「彩度」在黃色區會騙人。**
`#7f6b10` 的 HSL S 是 78%，但感知彩度 C\* 只有 48。
改用 **Lab／LCh 全域掃描**：取同時滿足「白字 ≥4.5:1」與
「拿它當卡 `#f4f4f5` 上的字 ≥4.5:1」、且色相落在黃帶裡**彩度最高**的顏色。

第三輪取 Lab h 80~90°，使用者判斷：

> 「有點混到髒髒的綠，變成有點嘔吐物腹瀉感……方向不對，往橙一點點的方向走。」

成因是**黃帶靠綠的那一側在低亮度下一定會泛綠** —— 而白字把亮度鎖在 L\* 47，
那一側躲不掉。三案（`#926800` / `#8a6c00` / `#866e00`）作廢。

#### 第四輪：往橙走（作廢）

第三輪泛綠的成因是**黃帶靠綠的那一側在低亮度下一定會泛綠** —— 白字把亮度鎖在
L\* 47，那一側躲不掉。往橙走同時解決泛綠、又能提高彩度天花板：

```
h 82° C* 54.9    h 70° C* 59.3    h 58° C* 68.4
h 78° C* 56.1    h 66° C* 61.9    h 50° C* 75.3
```

做了 `#986600`（h76.5・C\*56）／`#a36000`（h68.7・C\*59）／`#aa5c00`（h64.3・C\*62）三案。
使用者：「比之前好很多，但還是有點牛奶糖的感覺，比較帶開咖啡感。」

> **這一輪最有價值的發現：** `#aa5c00` 的色相 64.3° 和原本的 `#8d5e3a`（61.7°）
> 幾乎相同，但彩度是 61.9 對 32.1。**所以「拿鐵感」的成因一直是彩度不足，不是色相** ——
> 前四輪一直在挪色相，方向本身就錯了。

#### 第五輪：明黃（做過，方法保留）

**明黃 ＋ 白字在物理上不存在**（L\* 75 的黃配白字只有 1.94:1）。
保留白字，天花板就是 L\* 50 —— **第一到第四輪全部卡在同一個地方**。
使用者第三輪反對的是「字變成黑色很突兀」，那和「非白字」不是同一件事，
所以第五輪起字改用**同一個黃的深階**（色相只差 6~10°），是同族的深淺配。

#### 第六輪：錨在使用者挑的四個傳統色（目前的三案）

使用者從《天然染色經》的黃色卡裡挑出四個貼近他心中的黃。
（以下 hex 是從他的截圖**目視估**的，色相方向可信、絕對值不精確 ——
拿到原始色票的 hex 要回來重算。）

| | HEX | L\* | C\* | Lab h | 白字 |
| --- | --- | --- | --- | --- | --- |
| 神黃 | `#e9a72c` | 73.0 | 69.2 | 78.1° | 2.09:1 |
| 雄黃 | `#e28a1c` | 65.3 | 70.9 | 68.2° | 2.66:1 |
| 萬壽菊黃（他自註偏橘） | `#ef8b21` | 67.2 | 73.6 | 64.8° | 2.50:1 |
| 鼬黃 | `#d9a73f` | 71.4 | 59.0 | 82.0° | 2.20:1 |

> **四個全落在 L\* 65~73。那正是它們看起來像「他要的黃」的原因，
> 也正是白字全部失守的原因 —— 兩件事是同一件事。**

> 而且：把它們壓到白字能用的高度，得到的就是第四輪那組。
> 雄黃色相 68.2° 壓下來是 `#a75e00`，和第四輪的 `#a36000`（h68.7・C\*59.3）
> **幾乎是同一個色**，也就是他說「牛奶糖」的那一個。

所以三案改成三種取捨，各自錨在他挑的一個黃上：

| | 底 | L\* | C\* | 字 | 字疊在底上 |
| --- | --- | --- | --- | --- | --- |
| **A 守白字**（錨雄黃） | `#a75e00` | 47.3 | 60.7 | **白字** | 4.95:1 |
| **B 神黃** | `#f6a600` | 74.1 | 80.3 | `#6c3b00` | 4.58:1 |
| **C 鼬黃** | `#e8a600` | 72.5 | 77.3 | `#643b00` | 4.56:1 |

A 一條規則都沒破，但就是牛奶糖那一帶。
B、C 是真的明黃，代價是**字讓掉白色**，而且兒牙的 chip 與文章標籤**必須改成填色**
（亮黃當字讀不出來：`#f6a600` 疊在卡上只有 1.4:1）。

> 實作加了 `--accent-deep`（當字用的階）與 `--on-fill`（填色上的字）兩個衍生變數，
> **預設完全等於原本的行為**，所以其他五科一個像素都不會變。

#### 第七輪：A 與 B 之間補三階

使用者：「我覺得我想要的在 A B 之間。」在 Lab 空間把 A→B 等距切三份
（L\* 47.3→74.1、C\* 60.7→80.3、h 66.3→75.9），各取該亮度‧色相下的最高彩度。

**白字的臨界點量出來了：`L* 48` 還過（4.72:1），`L* 50` 就破（4.43:1）。**
A 本身站在懸崖邊（L\* 47.3），所以**中間三階全部都要深色字**，沒有例外。

| | 底 | L\* | C\* | 白字 | 深階字 | 字疊在底上 |
| --- | --- | --- | --- | --- | --- | --- |
| **A** | `#a75e00` | 47.3 | 60.7 | **4.95:1 ✓** | — | — |
| 1 | `#c06f00` | 54.6 | 67.2 | 3.81 ✗ | `#2a1700` | 4.52:1 |
| 2 | `#d38000` | 61.0 | 71.7 | 3.06 ✗ | `#482300` | 4.52:1 |
| 3 | `#e89300` | 68.0 | 76.9 | 2.44 ✗ | `#5e2f00` | 4.57:1 |
| **B** | `#f6a600` | 74.1 | 80.3 | 2.02 ✗ | `#6c3b00` | 4.58:1 |

> **一個反直覺的地方：越靠近 A，深階字反而越黑。**
> 第 1 階的字 `#2a1700` 亮度只有 L\* 10，幾乎就是黑色 —— 正是使用者先前嫌棄的那種。
> 越往 B 走，底越亮，字反而可以是暖棕（`#5e2f00` → `#6c3b00`），讀起來才像同一族的深淺配。
> **所以中間三階是「兩頭不到岸」：不夠亮到像明黃，字又被迫接近黑。**
> 真正的兩個穩定點是 **A（守白字）** 與 **B（明黃＋暖棕字）**。

除 A 之外，卡上對比都是 1.4~3.5，全部不夠，所以兒牙的 chip 與文章標籤一律改填色。

#### 第八輪：五案全部換白字（只為了看樣子）

使用者：「這 5 個全部換成白字。」照做，但要記得**這不是可上線的狀態**——
除 A（4.95:1）之外四案的白字全部掉出 AA 小字的 4.5:1。
這一輪的價值是確認了：**填色上的字用白色時，這一族的亮度天花板就是 A。**

#### 第九輪：範圍收到 A~2，中間補兩階（目前的五案）

使用者：「感覺在 A 1 2 之間，保留 A 1 2 再加 2 個進來，共 5 個。」
第七輪的 3（`#e89300`）與 B（`#f6a600`）整組淘汰——**明黃那一頭出局了**。
新的兩階取 A→1、1→2 在 Lab 空間的中點（L\* 與色相都內插），
再取該亮度‧色相下的最高彩度，和保留的三個是同一條線上長出來的。

| | 底 | L\* | C\* | h | 白字疊在底上 |
| --- | --- | --- | --- | --- | --- |
| **A** | `#a75e00` | 47.3 | 60.7 | 66.3° | **4.95:1 ✓** |
| A+ | `#b46600` | 51.0 | 64.0 | 66.8° | 4.34 ✗（差 0.16） |
| 1 | `#c06f00` | 54.6 | 67.2 | 67.3° | 3.81 ✗ |
| 1+ | `#ca7700` | 57.8 | 69.5 | 68.5° | 3.41 ✗ |
| 2 | `#d38000` | 61.0 | 71.7 | 69.7° | 3.06 ✗（壓在大字門檻上） |

> **第七輪的深階字（`#2a1700` 那組）全部作廢。** 那組是「和底同亮度差」推出來的，
> 越靠近 A 就越黑，正是使用者嫌棄的方向。第九輪改成**整族共用一個線框色**
> `#a4600f`（對卡 `#f4f4f5` 4.52:1）——因為這一族拿來當**字**的可讀天花板
> 本來就是 L\* 47，也就是 A 自己的亮度，再亮就掉出 4.5:1。
>
> 結論很乾淨：**五階的差別只在「按下去」的填色，沒按下去的線框態五案長得一模一樣。**

#### chip 的兩態回到標準合約（2026-08-07）

第八輪為了讓亮黃看得見，把兒牙的 chip 強制成「永遠填色」。使用者要求做回去：
**沒點的時候白底，點下去才套色**——也就是回到和其他四科完全相同的元件合約：

| 狀態 | 底 | 字 | 框 |
| --- | --- | --- | --- |
| 未選 | `var(--card)` | `var(--accent-deep)` | `var(--accent-deep)` |
| 選取 | `var(--accent)` | `var(--on-fill)` | `var(--accent)` |

**科別色不該有自己的互動規則。** 兒牙一旦例外，「選取」這個狀態在這一排 chip 裡
就不再是同一件事——使用者一眼就會發現，這也正是他要求做回去的原因。

#### 第十輪：留下 1，另加 HBR 那張圖 04 的沙金（2026-08-07）

使用者從第九輪的五階裡留下 **1（`#c06f00`）**，另外指了一張《哈佛商業評論》IG 圖，
要那張圖裡 04 那一格的金色。

> **色值是目視估的，不是量的。** 那張截圖只在對話裡，沒有落到容器的檔案系統，
> 沒辦法取樣。`#c8a464` 是照沙金／古銅金那一族估出來的。要精準就得拿到檔案或 hex。

| | 底 | L\* | C\* | h | 白字 | 深階字 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `#c06f00` | 54.6 | 67.2 | 67.3° | 3.81 ✗ | `#a4600f` |
| 04 沙金 | `#c8a464` | 69.3 | 38.4 | 82.3° | 2.35 ✗ | `#8f6a15`（對卡 4.51:1） |

**這個金和前九輪的黃不是同一種東西**，兩個數字就分得出來：
彩度只有 38（那條梯子是 61~72），亮度卻高到 69。**很淡但很亮** ——
這正是它讀起來像金屬金、而不像果汁黃的原因。

> 一個容易誤判的地方：第三輪被否決的「拿铁色／泰奶色」是 C\* 32，和這個 38 很接近。
> 差別只在亮度（那組 L\* 50 上下，這個 69）。
> **低彩度在暗處變成奶茶，在亮處才變成金 —— 是同一條路的兩端。**

白字 2.35:1 是照片裡的做法，但照片裡那是超大級數的裝飾數字；
搬到 13px 的 chip 上會糊掉。真要用，填色上的字得換成 `#8f6a15`。

#### 第十一輪：1 與沙金之間補兩階

在 Lab 空間取 1→04 沙金的三等分點。

| | 底 | L\* | C\* | h | 白字 | 深階字 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `#c06f00` | 54.6 | 67.2 | 67.3° | 3.81 ✗ | `#a4600f` |
| 1a | `#c28229` | 59.5 | 57.6 | 72.3° | 3.22 ✗ | `#9e6301` |
| 1b | `#c59347` | 64.4 | 48.0 | 77.3° | 2.75 ✗ | `#93681b` |
| 04 沙金 | `#c8a464` | 69.3 | 38.4 | 82.3° | 2.35 ✗ | `#8f6a15` |

> **這一段和第九輪那條梯子在比不一樣的東西。**
> 第九輪只在動亮度，彩度是各亮度的上限、被動跟著跑；
> 這一段是**彩度自己在掉**（67 → 58 → 48 → 38），色相同時往橙金那頭走 67°→82°。
> 四個底的亮度只差 15，彩度卻差了將近一倍 ——
> **這一排真正在比的是「要多飽和」，不是「要多亮」。**

深階字仍各自收在 L\* 47（這一族當字的天花板），彩度跟著各自的底走，
所以四案的線框態這次**不完全相同**：1 的框最艷、沙金的框最收。差別很小，但不是零。

### 定案：1a `#c28229`（2026-08-07）

十一輪之後定下來。**兩個提案頁都已經套上去了**（`preview/home-desktop-band/`
與 `preview/home-mobile-n3/`），切換條連同 `data-kids` 一起刪掉。

| 用途 | 值 | 對比 |
| --- | --- | --- |
| 底（實心藥丸、按下去的 chip） | `#c28229` | 白字 **3.22:1** ⚠ |
| 字與框（線性標籤、未選的 chip） | `#9e6301` | 對卡 `#f4f4f5` 4.51:1 ✓ |

為什麼是它：最後一輪比的不是亮度而是**彩度**（1 的 67.2 → 沙金的 38.4），
1a 落在 57.6 —— 還是黃，但已經開始有金屬感，不會讀成果汁。

> ### 白字 3.22:1 —— **已結案，維持白字（2026-08-08）**
>
> 填色上的白字是 3.22:1，不到 AA 小字的 4.5:1。
> 2026-08-08 把兩條路各做成實際的樣子給使用者看過（並排的藥丸、
> 陽光下的模擬、縮小／模糊／灰階三種情境、七科的對比長條），
> 他的回覆是「**維持白字 不用動**」。
>
> 所以這是一個**看過證據之後做的取捨**，不是待辦事項。
> **不要再提，也不要「順手改善」掉。** 兩條備案留在這裡只是紀錄：
>
> | | 做法 | 代價 |
> | --- | --- | --- |
> | 甲 | 填色上的字換 `#3c2400`（同色相、對底 4.51:1） | 顏色不動，只有字變深 |
> | 乙 | 兒牙的實心藥丸改成線框 | 兒牙就和其他五科長得不一樣 |
>
> 線框那一側（`#9e6301`）本來就是合規的，不用動。

### 兒牙為什麼需要三個變數，其他五科不用

其餘五科的 `--accent` 一個值就能同時當「字」和「底」，因為它們夠深。
兒牙的黃不行：**當底要亮才像黃，當字要深才讀得到**，同一個值做不到兩件事。
所以拆成 `--accent`／`--accent-deep`／`--on-fill`，預設全部等於原本的行為。

> **⚠ 這三個變數的預設值那一行必須排在科別色前面。**
> `[data-spec]` 和 `[data-spec="kids"]` 權重相同，排在後面會把兒牙的
> `--accent-deep` 蓋回 `var(--accent)`，黃字就糊在卡上 —— 這個坑已經踩過一次。


### 這一節為什麼推翻第六節第 3 條

第 3 條說「一個手機屏幕上最多只同時看到兩個彩色」。一科一色做不到——七個科別、
九張卡，捲動時一定會同時看到三四個。使用者在 2026-08-07 明確要求「同一種科別顏色一樣」、
「不同科顏色不要重複」，所以**第 3 條在醫師介紹這一節不適用**。

守住的是另一條、也是更根本的那條：**面積**。彩色只出現在藥丸這種小塊上，
卡底 `#f4f4f5` 一動不動（卡一被染色就會往紙靠，第四節 B 第 1 條就毀了），
加總起來仍在花藝實測的 1~22% 之內。

### 文章內頁的科別藥丸是實心的（2026-08-08）

上面那張表說「文章標籤」走線性，指的是**首頁文章卡**上的那顆 —— 一次看到六張卡，
六顆實心會太花。**文章內頁的那顆是實心底＋純白字**，跟醫師藥丸同一種份量：

- 它在內頁的角色不是「站上有哪些科」，而是「你現在讀的是這一科」—— 跟醫師藥丸的
  「這位醫師是這一科」同一層，不是分類清單。
- 藥丸自己帶 `data-spec`，所以底與字直接吃 `--accent` / `--on-fill` 那組變數，
  顏色不寫死在 `.post-tag` 裡。

> **2026-08-08 晚上這一顆的顏色跟著改了兩件事**（新版型合併進 main 的時候）：
>
> 1. 兒牙從圓凳褐 `#8d5e3a` 換成定案的黃 `#c28229`（十一輪，見上面）。
>    白字在它上面是 3.22:1，那是**已結案、看過對照後決定維持**的取捨。
> 2. `.post-head` 的底從純白 `#fff` 變成 N3 的卡色 `#f4f4f5`。
>    其他五科的白字仍是 5.55~13.52:1，一個都沒掉出 AA。
>
> 這一顆之外，**整頁的重點色也跟著科別走了**（連結、h3、重點框、按鈕），
> 規則寫在第六之六節 —— 那一節的字階和這一顆的填色是兩回事，不要互相搬。

圓角同樣照 0.384 的比例算：框高 23.04px（字 12px、行高 1.6、上下內距各 1.92）→ **9px**。
`rem` 認的是 `html` 的 16px 不是 `body` 的 17px，算框高的時候不要用錯基準。

同一次把內頁標題上方那一行併成一條：**首頁 › 科別藥丸 · 發布日 · 瀏覽數**。
原本是麵包屑在標題上、「發布・最後更新・瀏覽數」在標題下。**「最後更新」在內頁拿掉了**
（首頁卡片上還在），麵包屑的「文章 › 科別」兩層也收成藥丸一層。

### 順帶定案的兩件事

- **卡的左邊條收掉了。** 曾經有一條 3px 的 accent 色邊條，藥丸改實心之後它只是描邊，
  已經回到普通的 1px `--rule`。
- **四個小節的副標全部拿掉**（「依最後更新時間排序…」「看診團隊與各自的專科」
  「雲林縣斗六市・在地經營超過四十年」）。第一句在解釋機器怎麼排，
  COPY.md 第三節早就用同一個理由否決過「一篇一篇寫在這裡」；第二句是標題的同義重述；
  第三句同一頁已經講了三次。**頁尾那句留著。**

### 科別標記的形狀與填色（2026-08-07 定案）

科別色現在跨三處共用同一個 `data-spec`：**主題與科別的 chips、文章卡的分類標籤、醫師藥丸**。
所以「兒童牙科」在三個地方一定是同一個色。篩選功能讀的也是同一個屬性，
顏色與篩選不可能對不起來。

**填色分兩層，對應的是瀏覽者心裡的層級，不是隨機的：**

| | 填色 | 為什麼 |
| --- | --- | --- |
| 醫師藥丸 | **實心底＋純白字** | 這是在**認人**（這位醫師是哪一科），份量最重 |
| 主題與科別・文章標籤・診所資訊 | **線條與文字套色，底不套色** | 這是**分類**（有哪些科、這篇屬於哪一科） |

線性版的底維持 `--card #f4f4f5` 不是透明 —— 主題與科別的 chips 躺在紙 `#e2e5e6` 上，
底若透明，兒牙 `#8d5e3a` 對紙只有 **4.37:1** 掉出 AA；墊那層卡色才是 5.03。
**「底不套色」的意思是底不吃科別色，不是底完全不上色。**

**圓角要看比例，不是絕對值。** 四個標記一開始全寫 8px，主題與科別那顆明顯偏方 ——
因為它的框大得多。基準是醫師藥丸的 **半徑 ÷ 框高 = 0.384**，其餘各自照這個比例算：

| | 框高 | 圓角 | 比例 |
| --- | --- | --- | --- |
| 醫師藥丸（基準） | 20.86px | 8px | 0.384 |
| 主題與科別 chip | 31.00px | 12px | 0.387 |
| 文章標籤 | 22.33px | 8.5px | 0.381 |
| 診所資訊標籤 | 23.66px | 9px | 0.380 |
| 專長／資歷／學歷 標籤 | 22.13px | 6px | 0.271 ← 刻意比標記方一階 |

專長標籤是紙色底 `#e2e5e6` ＋ 次要墨色 `#5c5f57`（5.14:1）。
它是 grid 項目，**`justify-self: start` 與 `align-self: start` 兩個都要**：
少了前者底會被欄寬撐滿，少了後者底會被整列的高度拉長（grid 預設是 stretch）。

### 標誌本身（2026-08-07）

標誌路徑取自 `20241205 芳仁牙醫LOGO_中英標準字.ai`，是一個 path、兩條子路徑
（嘴 ＋ 用 `fill-rule="evenodd"` 挖出來的那顆牙）。**嘴一個節點都沒有改過。**

牙那條子路徑依自己的中心放大，因為原比例的洞在小尺寸會被抗鋸齒吃掉。
放大會連帶把洞下方的肉吃薄，所以要往上移補回來 ——
**目標是讓「洞下方的肉」回到原標誌的 3.43（viewBox 單位，逐像素掃描量出來的）**：

| | 放大 | 放大後洞下方的肉 | 上移 | 結果 |
| --- | --- | --- | --- | --- |
| banner（顯示 31.88 × 15.70px） | 1.3× | 2.93 | 0.50 | **3.43** |
| favicon（要撐到 16px） | 1.6× | 2.38 | 1.05 | **3.43** |

favicon 沒有底框，寬度撐滿方框（路徑 bbox 剛好等於 viewBox，沒有多餘的邊可以修；
它自己是 2.03:1，所以高度最多只到方框的 46%，那是比例決定的）。

~~**favicon 的顏色還沒定案** —— 淺色已定 `#4f6361`（診療椅原值），深色還在挑。~~
**2026-08-08 已定案：淺色 `#4f6361`、深色 `#4f8065`，並且兩者的先後順序是刻意反過來寫的
（Safari 不看 favicon 的深色模式）—— 完整說明在第六之七節。**
過程與結論寫在 `preview/logo-favicon/`。兩件量出來的事先記在這裡：

- **PALETTE 第五節「提亮時彩度不要動」那條，對診療椅不成立。** 那條是為深綠松（彩度 40%）寫的；
  診療椅彩度只有 11.2%，把一個快沒有顏色的色往亮處推只會愈來愈灰 —— 使用者的形容是「像漂白過」。
- **深色版不需要提那麼亮。** 淺色 `#4f6361` 亮度 Y=0.11，對深色分頁列只有 2.41 不合格；
  非文字圖形的下限 3.0 只需要 Y=0.15。一度做到 Y=0.28（對比 4.8），多花了兩倍，
  結果就是深淺兩版看起來像兩個主題色。

---

## 六之三、電腦版：壓在照片下緣的窄帶（2026-08-07 定案）

電腦版把窄帶從「接在照片下面的另一個區塊」改成「**壓在整屏照片的下緣**」。
這一改推翻了兩件原本成立的事，兩件都要重新算。

### 1. 藍黑 H=220 的前提消失了

窄帶原本的藍黑（`#17191d` → `#282b31` → `#2d3037`，H=220・S=10%）是
「夜空 `#1b2635` × 柏油 `#221e1c` 各半、彩度 ×0.72」算出來的 —— **因為它當時
夾在夜空與紙色中間，兩邊都要接。**

疊到照片上之後它壓住的位置**只有柏油**，夜空離得很遠，取中間值的理由不存在了。
使用者也直接指出藍黑在照片上看起來突兀。

比較過的四個實色方向（三個實色案的做法是同一套：**亮度一個都不動，只轉色相**，
L\* 維持 10.2 / 17.5 / 19.6 三個停點，所以差別純粹是色調）：

| | 色相 | 三個色停 | 來源 |
| --- | --- | --- | --- |
| 藍黑（舊） | H=220 | `#17191d` `#282b31` `#2d3037` | 夜空×柏油中間值 |
| 柏油 | H=20 | `#1d1917` `#312b28` `#37302d` | 柏油實測 `#221e1c` |
| 墨色 | H=88 | `#1a1d17` `#2d3128` `#32372d` | 內文墨色 `#2a2c27` |

### 2. 定案方向：毛玻璃 ＋ 柏油漸層

模糊 `blur(4px) saturate(.92)`，整條一致。疊在上面的色**從完全沒有顏色漸層到柏油**，
而且是**兩個實測值之間**的漸層：

```
上  rgba(34,30,28, 0)      #221e1c 柏油（一般實測值，L* 12）
    rgba(34,30,28, .22)    28%
    rgba(22,20,19, .58)    62%   ← 換成 #161413
下  rgba(22,20,19, .88)    100%  #161413 照片最底那一列，L* 6.6
```

**定案就是上面這組（「標準」）。** 做過的「深一階」是同樣的色、
alpha 改成 `0 → .32 → .74 → 1`，最下緣完全不透 —— 落選的理由是
**它在最後 10% 把毛玻璃的好處還回去了**：底端收成一條實色的線，
等於又變回實色案，只是位置換到下緣。

三格間距同時定案為**「拉開」**（`.stats li { padding-inline: 4rem }`，靠內容寬度、
整組置中）。淘汰 1.9rem（三格擠在版心中央，兩側各空一大塊，像沒排完）
與「各佔版心三分之一」（那是手機 390px 排不下才用的辦法，
搬到 1160px 會拉得太開，三格之間的關係就散了）。

**`preview/home-desktop-band/` 的三組切換條連同 `data-band`／`data-gap`／`data-kids`
已經全部刪掉**，落選的 CSS 也一併移除，只有落選的理由留在註解裡。

> **為什麼一定要換到 `#161413`：** 第一版整條都用 `#221e1c`，使用者說「看不出漸層」。
> 原因很簡單 —— 它壓住的路面本來就是 L\* 6~15，**拿一個和底下差不多深的顏色去疊，
> 當然看不出變化**。換成照片最底那一列的 `#161413`（L\* 6.6）之後往下走既加深又換色，
> 路面的標線與反光會逐漸被壓掉，「越下面越深」才看得出來。

`saturate(.92)` 是必要的：門口的暖光被模糊抹開之後整條帶子會發黃，
第一版用 `saturate(1.06)` 更把它推過去。

### 3. `backdrop-filter` 的框邊 —— 這一條很容易再踩

**`backdrop-filter` 只作用在元素自己的框裡。** 框內的東西被模糊、框外的沒有，
兩者在框的邊緣直接相接 —— **那條邊界本身就是一條可見的線，和顏色無關**，
就算遮罩的 alpha 從 0 開始也消不掉。

捲到照片下半部時，頁首、照片、窄帶會因此變成三塊上下堆疊的板。

解法：把模糊放到一個 `::before` 上，再用 `mask-image` 讓**模糊本身**淡入／淡出。
遮罩住的是濾鏡的結果，所以模糊是漸漸出現的，沒有框邊。

- 窄帶：上緣 `0% → 38%` 淡入
- 頁首：下緣 `76% → 100%` 淡出

### 4. 頁首玻璃：一層固定的薄色，深淺交給底下的東西決定

> **這一條推翻了先前所有關於「頁首玻璃終點顏色」的做法，不要改回去。**

走過的路：終點從藍灰 `#32353c` → 暖色 `#2a2523` → 在 `#2a2523`~`#3a3532` 之間做五階
→ 試過「捲進內文整條翻成卡色淺條、文字轉墨色」（太淺，淘汰）。
**全部都是在調「終點的顏色」，而那個框架本身就錯了。**

原本的規則是「顏色從幾乎透明**內插到一個不透明的終點**」，理由是怕條子在窄帶與紙色
之間切換時跟著變亮。但那個目標本身站不住 —— **玻璃本來就會跟著底下變**，
那是它該有的行為，不是要消除的缺陷。實際造成的毛病是：條子壓在照片最暗的下半部
與窄帶上時，反而因為往不透明的灰內插而**變淺**，看起來像一塊浮在夜色上的灰板。

現在的做法是：**顏色鎖死成一個，只讓濃度隨捲動變。**

```
顏色  rgb(25,22,20)   ＝ 暗夜 #191614，從頭到尾不換
濃度  alpha .06 → .76 ＝ 走的區間是「條子開始壓到窄帶」到「窄帶整條過完」
濾鏡  blur(6px) saturate(1.08)
```

**顏色固定**是關鍵：合成色因此一定跟著底下走 —— 壓在夜色上就深、壓在紙色上就淺。
**濃度不能也固定**：試過把 alpha 也鎖成 .76，結果 HERO 一捲動條子就變成一塊黑板，
玻璃感整個不見了 —— 那個通透感本來就是靠低 alpha 讓底下透過來撐出來的。

三段各自的長相（截圖實測）：

| 條子壓在哪 | alpha | 合成色 | 導覽白字 `.88` |
| --- | --- | --- | --- |
| HERO 區（天空、建築） | ≈ .06 | 幾乎不染色，只有模糊 | — |
| 窄帶通過中 | .06 → .76 | 底下本來就是夜色，越走越沉 | — |
| 窄帶／照片下半部 | .76 | `#1b1815` | 15.7:1 |
| 紙色 `#e2e5e6` | .76 | `#494846` | 8.2:1 |

**2026-08-07 定案：濃度取 `.84`，而且捲進內文之後轉成實心。**

使用者要的是「到了淺色區就不要透了，顏色維持 `.84` 的樣子；但深淺交界那一段
捲動仍然要有透的感覺，往回捲也一樣」。這兩件事不衝突 ——
**`.84` 的玻璃疊在紙色上算出來就是 `#393736`**，和實心版要用的顏色一模一樣。
所以把「玻璃 → 實心」的切換點放在**紙色已經完全補到條子底下**的那一刻，
兩邊顏色相等，切換看不見；而且它是捲動位置的函數不是時間動畫，所以來回捲都一樣。

為什麼要有實心這一段：紙色區本身是平的，透不透看起來一樣；
但再往下捲，**文章卡、縮圖、彩色 chips 會從條子底下經過** —— 玻璃會把它們糊上來，實心不會。

兩個進度值，剛好接續（`u` 開始的那一刻正是 `t` 到 1 的那一刻）：

| | 範圍 | 條子底下是什麼 | 條子的長相 |
| --- | --- | --- | --- |
| `t` | 窄帶進入 → 填滿條子 | 夜色 | 玻璃，alpha `.06 → .84` |
| `u` | 窄帶離開 → 紙色補滿 | 夜色漸換成紙色 | 玻璃漸轉實心，止於 `#393736` |

**實心色不是手寫的**，是用同一個 `A_MAX` 疊在 `--paper` 上算出來的，
所以日後改濃度，實心色會自己跟著走，兩邊永遠相等、不會脫鉤。

截圖實測整段捲動的條子中段顏色，單調遞增、沒有跳動：

```
#141110 → #16110e → #1c1917 → #1f1c1b → #242221 → #2a2827 → #373434 → #393736
   HERO 區（玻璃）          窄帶填滿      交界（玻璃轉實心）        紙色（實心）
```

先前試過的濃度：`.76` 使用者說「透感有點多」、`.92` 已回到嫌太黑的那一帶。
下限是對比度：`.70` 以下紙色區的導覽白字會逼近 4.5:1。

> **頁首的淡出只能在深色區。** 條子捲到紙色上時，那段「由深漸淡到透明」
> 在淺色底上會被讀成**一條陰影** —— 這一頁本來一個陰影都沒有（只有 HERO 那兩層文字陰影）。
> 所以淡出的起點做成 CSS 變數 `--head-fade`，由捲動的 JS 逐幀寫入：
> `76 + 24 × t³`（`t` 是窄帶填滿條子的進度）。深色區是 76%（軟邊），
> 紙色上來時收到 100%（無淡出、無陰影）。用三次方是因為
> **綁 `t` 不綁 `u`，這一點很容易搞反，理由是幾何的：**
> 淡出區和紙色**都是從條子的下緣往上長的，必然重疊** ——
> 所以只要紙色進來一點點，那段「由深漸淡到透明」就會壓在紙上變成一條陰影。
> 沒有「兩者錯開」這種解，唯一的解是**在紙色出現之前就把淡出收完**。
> `t` 到 1 的那一刻正是 `u` 開始的那一刻，也就是紙色剛要出現的臨界點，收在這裡剛剛好。
> 收完之後條子下緣是硬邊，但那一刻它正好和「窄帶結束、紙色開始」那條原本就存在的邊重合，
> 所以看不出多了一條線。（2026-08-07 曾經改綁 `u`，立刻長出使用者說的那條陰影，已改回。）

---

## 六之四、文章卡的陰影與 hover（2026-08-07）

原本這兩頁從頭到尾沒有陰影 —— 只有 HERO 那兩層，而且是為了讓字在照片上讀得出來。
使用者要求文章卡加上陰影，條件講得很精確：

> 「除了圖卡會像現在有整個往上加陰影效果外，圖片還會微微放大。另外每個圖卡在游標
> 還沒點到時就自帶一點陰影……但又不能做太多到游標移到圖卡才加強的陰影效果，
> 這樣就沒差別了。」

**兩件事要同時成立**：靜止就有陰影，而且 hover 還要看得出變化。
關鍵不是絕對值，是**兩者之間的級距**：

| | 偏移 y | 模糊 | alpha |
| --- | --- | --- | --- |
| 靜止 | 4px | 10px | .09 |
| hover | 16px | 32px | .16 |
| 倍率 | 4× | 3.2× | 1.8× |

```css
--card-shadow:       0 1px 2px  rgba(42, 44, 39, .07), 0 4px  10px rgba(42, 44, 39, .09);
--card-shadow-hover: 0 5px 10px rgba(42, 44, 39, .09), 0 16px 32px rgba(42, 44, 39, .16);
```

**三個量要一起放大**（位移、模糊、alpha），「浮起來」才會是一個明確的事件，
而不是慢慢變濃。只加深 alpha 而不動模糊與位移，就是使用者說的「沒差別」。
卡本身同時抬 4px（原本 2px）—— 陰影與位移是同一個動作的兩半。

> **陰影色用墨色 `#2a2c27` 不是純黑。** 紙是 `#e2e5e6`（L\* 89），
> 純黑陰影在這個亮度的底上會讀成灰髒的一圈；帶著內文墨色的傾向才像同一頁的東西。

### 縮圖的放大

```css
.card-thumb { transition: transform .5s cubic-bezier(.22, .61, .36, 1); }
.card:hover .card-thumb { transform: scale(1.05); }
```

- **1.05 是刻意的小。** 16:9 的縮圖放大 5% 只會讓構圖往外推幾個 px，
  看得出來在動，但不會變成「換了一張圖」。
- **時間比卡片的位移長（.5s 對 .22s）。** 卡是立刻回應游標、圖是慢慢推進去，
  兩個速度不同才讀得出是兩層，而不是整張卡在縮放。

### 三個實作上的坑

1. **`.card` 要加 `isolation: isolate`。** 卡有圓角 ＋ `overflow: hidden`，
   裡面又有東西在 `transform` —— 沒有獨立的堆疊脈絡時，
   Safari 會讓縮圖的四角溢出圓角。
2. **hover 那一組要包在 `@media (hover: hover)` 裡**（手機版尤其）。
   觸控裝置沒有游標，`:hover` 會在點下去之後「黏住」不放，看起來像卡片壞掉。
3. **`prefers-reduced-motion` 只拿掉會動的部分，陰影留著。**
   陰影是靜態的層次資訊，不是動效。

---

## 六之五、電腦版與手機版的同步（2026-08-07）

**方向已經反過來了，接手的人要知道：**

> 手機版 `preview/home-mobile-n3/` 是先做的，電腦版 `preview/home-desktop-band/`
> 是從它推廣出去的。但**電腦版定案得比較晚、也比較完整**，
> 所以從 2026-08-07 起 **以電腦版為準**，改動先進電腦版，再回推手機版。

這一輪回推的東西：

| | 手機版原本 | 改成 |
| --- | --- | --- |
| 頁首玻璃 | 兩組 rgba 內插、終點不透明 | **顏色固定、只有濃度在變**，過了窄帶再換成實色（見下） |
| 頁首下緣 | 沒有 mask，框邊是一條硬線 | `mask-image` ＋ `--head-fade`，深色區 76%、紙色上收到 100% |
| 頁尾 | 一段文字，含「在地經營超過四十年」 | 三段：聯絡／站內導覽／關於這些文章 |
| 按下的 chip | 只換 `background` 與 `color` | 加上 `border-color` —— 少了它兒牙會變成深棕框包黃底 |
| chip 換色 | 沒有過渡 | `transition: background-color .15s` |
| 卡片 | 沒有陰影、沒有焦點框 | 靜止陰影 ＋ `:focus-visible` |
| 卡片註記 | 「更新 2026/08/02 ・ 👁 1,284」 | 「2026/08/02 ・ 1,284 次瀏覽」 |

**沒有回推的一件事：hover。** 使用者明講手機版不要「上移 ＋ 縮圖放大」。
手機沒有游標，`:hover` 會在點下去之後黏住不放，看起來像卡片壞掉。
所以手機版的卡片只有靜止陰影，連 `transition` 都不需要 —— 沒有東西會變。

### 頁首玻璃的正確模型（兩個進度，前後相接）

```
t  窄帶進入條子 → 條子被窄帶填滿。這一段只調濃度 .06 → .84
u  窄帶開始離開 → 完全離開。這一段把玻璃換成實色 #393736
   （＝ .84 濃度疊在紙色上的合成色，所以交接那一瞬間顏色數值完全相同）
```

- **顏色固定 `#191614`，不要再去調「終點的顏色」。** 往不透明終點內插的結果是
  條子壓在照片最暗的下半部與窄帶上時反而**變淺**，像一塊浮在夜色上的灰板。玻璃不會這樣。
- **過了窄帶必須變成實色。** 只要還留一點透明度，底下換成紙色時條子一定會跟著變亮。
- **`--head-fade` 只能綁 `t`，不能綁 `u`。** 淡出的那一段和「紙色從下面填進來」
  是從同一條下緣往上長的，綁 `u` 兩者必定重疊，淺底上那段「由深漸淡」會被讀成一條陰影。
  綁 `t³` 是讓淡出在紙色出現**之前**就收乾淨。

### 主選單兩頁都是三項，「主題與科別」不放進去

390px 上四項的算式（量真機截圖得到的）：

```
左右內距 31.2 ＋ 標誌 34.6 ＋ 標誌間距 10.1 ＋ 標題六字 95
        ＋ 頁首間距 11.7 ＋ 選單四項 209.5  ＝  392.1px   （螢幕只有 390）
```

`.site-nav a` 有 `white-space: nowrap` 不會縮，所以**被擠的一定是品牌**，
「芳仁牙醫診所」就斷成「芳仁牙醫診／所」兩行。退回三項之後的剩餘空間：
375px → 61.6、390px → 66.7、414px → 71.0。

**電腦版明明放得下，也一起拿掉** —— 兩頁的主選單要是同一組，
不然改一頁就得記得改另一頁，這種事一定會漏。這一節的入口在頁尾的站內導覽。

> 順手加的兩道保險，之後就算又被擠到也不會斷行：
> `.brand { flex: 0 0 auto }` ＋ `.brand-text b, .brand-text small { white-space: nowrap }`。

### ⚠ 不要讓 `img` 比它的框寬，就算外面有 `overflow: hidden`

手機版 HERO 原本靠 `width: 113%` ＋ 外框 `overflow: hidden` 做右緣 13% 的裁切。
**Blink 收得住，iOS Safari 收不住** —— 使用者在真機上連續回報兩次
「頁首的文字跑到範圍外面」，成因就是它：

```
img 比框寬 → iOS 把版面視窗（ICB）撐到內容寬度
          → .phone（width:100%）跟著變寬 → img 又跟著變寬
          → 390 的螢幕最後排成大約 590
```

版面一變寬，`.sticky-head`（`left: 0; right: 0`）就跟著文件的寬度走，
**主選單被帶到文件的最右邊**，而內容還在左邊 —— 那正是截圖裡的樣子。
`body { overflow-x: hidden }` 擋不住，因為問題發生在「版面視窗被撐大」這一層，
不是「內容被畫出去」那一層。

改用 `object-fit` 之後畫面一模一樣，但 img 本身就是 100% 寬：

```css
.hero-photo img {
  position: absolute; inset: 0;
  width: 100%; height: 100%; max-width: 100%;
  object-fit: cover; object-position: left center;
}
```

框是 `W × 0.7472W`（比例 1.338），照片是 1600×1058（比例 1.512）——
`cover` 會以高度為準放大到 1.13W 寬再左右裁，`object-position: left`
讓裁掉的正好是右緣那 13%。**和原本的 `width: 113%` 是同一個結果。**

改完之後 375／390／414 三個寬度下，**沒有任何一個元素的右緣超出視窗**
（連把 `overflow` 強制設成 `visible` 去量都一樣）。這是這一頁第一次做到。

> 電腦版的 HERO 本來就用 `object-fit: cover`，沒有這個問題。

### ⚠ 使用者的手機是 **375 CSS px**，不是 390

2026-08-07 用診斷頁在他手機上量到的：

```
screen.width      375
innerWidth        375
scrollWidth       375      （＝ 可左右拉 0px）
visualViewport    375 × 667（縮放 1.00）
devicePixelRatio  3
UA                iPhone OS 18_7 / AppleWebKit 605.1.15
```

375 × 667 配 dPR 3 表示他開了**顯示放大（Display Zoom）**，
也就是螢幕硬體比 375 大、但版面照 375 排、字和元件都等比放大。
**這一頁所有的寬度驗算都要以 375 為準**，先前算的 390 太寬鬆 ——
主選單四項那次超出的其實比我算的 2px 更多。

> 順帶這張診斷也證實了一件事：**claude.ai 的檢視器沒有把頁面塞進比螢幕寬的框**，
> `innerWidth` 就等於 `screen.width`。所以先前的水平溢出是頁面自己的問題，
> 不是檢視器的。診斷頁：`https://claude.ai/code/artifact/49b85912-755a-4dc6-81a9-76ca8d541b75`

改完之後在 375 與 390 兩個寬度量到的結果一致：
`html.scrollWidth` 等於視窗寬、**可左右拉 0px**、品牌名單行。

### ⚠ 虛擬元素的 `position: absolute` 是找**祖先**，不是找自己那個元素

首頁把窄帶從「壓在整屏下緣」改成手機的「接在照片下面」時，我寫了：

```css
.hero > .band { position: static; }     /* ← 錯 */
```

`.band::before`（毛玻璃 `blur(4px)`）與 `.band::after`（柏油漸層）都是
`position: absolute; inset: 0`。`.band` 一旦是 `static` 就**不是包含塊**，
兩層改用 `.hero`（`position: relative`）—— 於是整張 HERO 照片被糊掉、再蓋一層柏油色。
使用者的原話是「Hero 區的圖片整個變暗糊掉了」。

```css
.hero > .band { position: relative; }   /* 留在流內，但仍是自己虛擬元素的包含塊 */
```

手機版本來就不用毛玻璃（帶子接在照片下面，底下沒有東西可以透），
所以兩層直接 `display: none`，換回定案的夜色漸層。

### 頁首品牌兩行要右緣切齊，靠的是 `text-align-last: justify`

```css
.brand-text small { text-align: justify; text-align-last: justify; }
```

區塊寬度由較寬的那一行（標題六字）決定，副標八字用字距拉開填滿同一個寬度，
兩行的右緣才會切齊。**捲動收合時也成立**，因為兩行的字級是整組 ×0.9、比例不變。
量到的結果：375／390／414 三個寬度、正常與收合兩種狀態，右緣差都是 `0.00`。

### ⚠⚠ 內嵌 `<svg>` 一定要寫 `width` / `height` 屬性

**這是頁首搞了七輪的真正源頭。**

`<svg>` 沒有 `width`／`height` 屬性時，**它的內在尺寸在 WebKit 是預設的 300×150**。
`.brand-mark` 是 flex 項目、`min-width` 預設 `auto`（＝ min-content），
於是它縮不到 300px 以下 —— `.brand` 的 max-content 就變成

```
300（標誌被頂住） ＋ 間距 ＋ 品牌文字  ≈  395.5
```

而真機量到的就是 `.brand w = 395.5`。**Blink 把 `width: 100%` 的 SVG 當成
min-content 0，所以在這裡怎麼量都正常。**

```html
<svg width="44.2873" height="21.8244" viewBox="0 0 44.2873 21.8244">
```
```css
.brand-mark { min-width: 0; }
.brand-mark svg { width: 100%; max-width: 100%; height: auto; }
```

屬性只是給「內在尺寸」用的，CSS 的 `width: 100%` 仍然覆蓋它，畫面一個像素都不變。
兩頁的標誌都補了。

#### 頁首的版型：grid，不是 flex

```css
.site-header { display: grid; grid-template-columns: max-content minmax(0, 1fr); }
.site-nav    { flex-wrap: nowrap; min-width: 0; justify-content: flex-end; }
```

- `max-content` 軌道：左欄就是品牌的內容寬度，不接受引擎推論。
- `minmax(0, 1fr)`：右欄下限 0，**不會反過來把格線撐寬**。
- 選單 `flex-wrap: nowrap`：使用者要求一定同一行。

量到的結果：300／320／375／390／414 五個寬度下 `可左右拉` 全部 0、
頁首維持一行、選單末項都在畫面內。

#### 這一段真正的教訓

七輪裡我換過六種寫法（品牌可縮／鎖死／`width: max-content`／
裁 `html`／裁 `.phone`／裁 `.hero-stage`／改 grid），**沒有一次打中**，
因為我一直以為在比字型寬度（差幾 px）。實際上是一個 `<svg>` 的預設內在尺寸（300px）。

> **把可疑元素的實際尺寸印在頁面上請使用者截圖，比再改五版 CSS 快得多。**
> `.brand w = 395.5` 這個數字出現之後，兩輪就收斂了。

### ⚠ 頁首那一列：讓它「不可能溢出」，不要算「剛好放得下」

2026-08-07 為了這一列來回四次，三種壞結果其實是同一個病：

| 做法 | 結果 |
| --- | --- |
| 品牌可縮 | 選單的 `nowrap` 贏 → 「芳仁牙醫診所」斷成「芳仁牙醫診／所」 |
| 品牌鎖死 `flex: 0 0 auto` | 換成整列比視窗寬 → 選單被推出畫面、**頁面可以左右拉、捲動時會晃** |
| 在 `.phone` 上裁掉 | 晃是不晃了，但右對齊的選單**整組被裁掉、看不見** |

**病因是「這一列在真機上放不下」，三個做法只是在決定誰去承受它。**
而我在容器裡量到 375px 下只用了 313（還剩 61）—— 容器沒有 PingFang TC，
**那個數字不能拿來保證真機**，這一輪之前已經因此判斷錯兩次。

所以不再賭算得剛好，改成把「溢出」這個可能性拿掉：

```css
.site-header { flex-wrap: wrap; }              /* 放不下就讓選單掉下一行 */
.site-nav    { flex-wrap: wrap; min-width: 0; justify-content: flex-end; }
```

`min-width: 0` 是關鍵：沒有它，flex 項目不會縮到 min-content 以下，
就只能往外撐。加了之後 240／280／320／375／390 五個寬度量到的
`scrollWidth` 全部等於視窗寬、**可左右拉 0px**，而且選單一律看得到 ——
320 以下會掉到第二行（會和詩擠一點），但那是三種壞結果裡唯一可接受的。

> **通則：版面規則要能吸收字型的差異，不是去預測它。**
> 「算出來還剩 61px」在換一套中文字型之後就不成立了；
> 「放不下就換行」在任何字型下都成立。

### 水平方向收在 `.hero-stage` 上，用 `clip` —— **不要收在 `.phone` 或 `html` 上**

```css
.hero-stage { overflow-x: clip; }     /* HERO 是唯一量得到水平溢出的地方 */
```

三個位置都試過，只有這一個對：

| 放哪裡 | 結果 |
| --- | --- |
| `html` | `.sticky-head` 黏不住了。量到捲到 700px 時 `header.top = -700`（整條跟著捲走）。 |
| `.phone` | 頁首被裁掉。`.phone` 包著頁首，一旦它比視窗寬，**右對齊的主選單整組看不見** —— 使用者回報「全部文章／醫師介紹／診所資訊 沒有顯示」。 |
| **`.hero-stage`** | ✓ 它是溢出真正的來源（照片、`.hero-sign::before` 那顆用負 inset 往外撐的藥丸），**而且不是 `.sticky-head` 的祖先**（兩者是 `.phone` 底下的兄弟），所以裁它動不到頁首。 |

用 `clip` 不用 `hidden`：`hidden` 會建立捲動容器，`clip` 不會。

> **通則：裁切要放在「溢出的來源」上，不要放在「頁面的邊界」上。**
> 放在邊界看起來比較保險，實際上是把所有右對齊的東西一起賭進去。

### ⚠ 不要在 `html` 上加 `overflow-x: clip`

2026-08-07 加過一次，**當天就退掉**。當時是想擋 iOS 的水平捲動，
但那只是猜測、沒有證據，代價卻是**手機版的固定頁首整條壞掉**：

```
加了之後量到：scrollY=300 → header.top = -300
              scrollY=700 → header.top = -700   （跟著捲走，完全沒黏住）
退掉之後：     三個位置 header.top 都是 0
```

`overflow-x: clip` 讓 `html` 變成裁切容器，`.sticky-head` 的 `position: sticky`
就失去它原本相對視窗黏著的行為。**水平方向就靠 `body { overflow-x: hidden }`，
那一直都在，也一直都夠。**

> 教訓不在 CSS，在流程：**為了一個沒有重現步驟的猜測，去動一個正在正常運作的東西。**
> 使用者回報「頁首的字跑到很奇怪的地方」時，那張截圖裡還有 artifact 檢視器自己的
> 浮動按鈕，成因並不明確；正確的作法是先問清楚或先量到，而不是先下藥。

### 量手機版的版面時，不要用 `--window-size` 直接截圖

這個容器裡的無頭 Chromium **把版面視窗寬度夾在 500px 以上**：
下 `--window-size=390,760` 得到的畫布是 390 寬，但頁面其實是照 `innerWidth = 500` 排的，
截出來就是「右邊被切掉」的假象 —— 看起來像破框，其實沒有。

要量真正的 390px，把頁面放進一個 `width: 390px` 的 `<iframe>` 再截外層那一頁。
實測 375／390／414／430 四個寬度，`scrollWidth` 都等於視窗寬，沒有水平溢出。

---

## 六之六、文章頁的科別色（2026-08-08）

文章頁不再全篇深綠松，**重點色跟著這一篇的科別走** ——
和首頁「主題與科別」那顆標記同一個顏色。使用者要的：
「套色一起改 跟著首頁的科別標籤顏色動」。

科別寫在 `<body data-spec="…">`（**在 `<main>` 之外**，所以不會動到內容雜湊、
六篇的更新日期一格都不會跳）。標籤 → 代碼的對照表就是 `tools/build.mjs` 的 `SPEC`，
和首頁卡片共用同一份，不會有兩套。

哪些東西吃這個色：

| 元件 | 用哪一階 | 為什麼 |
| --- | --- | --- |
| 小節標題前的 3px 短棒 | `--accent` | 是「底」不是字，只要 3:1 |
| 重點框 `.callout` 左邊那條 | `--accent` | 同上 |
| 實心按鈕 `.btn` | 底 `--accent`＋字 `--on-fill` | 白字，兒牙就是那個 3.22 的問題所在 |
| 內文連結、`h3`、線框按鈕 | `--accent-deep` | 是字，要 4.5:1 |
| 頁尾電話 | **維持 `--teal`** | 頁尾是全站共用的落款，不屬於某一篇 |

### ⚠ 字階不能直接從首頁搬過來，底不一樣

首頁的標記坐在**卡片** `#f4f4f5` 上，文章的內文坐在**紙** `#e2e5e6` 上，
紙比卡深一階（1.152:1），字就得跟著再深一點才守得住 4.5:1。

| 科別 | 原色 | 疊在紙上 | 文章頁的字階 | 疊在紙上 |
| --- | --- | --- | --- | --- |
| 一般・牙周 `--chair` | `#4f6361` | 5.04 ✓ | 原色 | 5.04 |
| 齒顎矯正 `--blue` | `#3C596B` | 5.85 ✓ | 原色 | 5.85 |
| 口腔外科 `--taupe` | `#7D5A58` | 4.77 ✓ | 原色 | 4.77 |
| 贋復假牙 `--tile` | `#252d4a` | 10.68 ✓ | 原色 | 10.68 |
| 顯微根管 `--moss` | `#5D6D55` | **4.38 ✗** | `#596951` | 4.65 |
| 兒童牙科 | `#c28229` | **2.54 ✗** | `#915800` | 4.61 |

所以只有兩科需要另外一階。**兒牙在首頁是 `#9e6301`（對卡 4.51），
搬到紙上只剩 3.91 —— 不能直接用。** 兩個值不是筆誤，是兩個底。

### hover 用 `color-mix` 壓深，不要為七個科別各寫一條

```css
.btn:hover { background: color-mix(in srgb, var(--accent) 84%, #1a1a17); }
```

舊瀏覽器不支援就整條失效，按鈕維持原色，不會壞掉。

### ⚠ `.views` 的「次瀏覽」文章頁**標記裡本來就有**

首頁的卡片是 `build.mjs` 產生的，「次瀏覽」是它寫進去的；
文章頁的 `<main>` 裡本來就打著 `<span class="views-n">—</span> 次瀏覽`。
第一版照抄首頁的做法補了一個 `::after { content: "次瀏覽" }`，
結果變成「1,284 次瀏覽 次瀏覽」。要拿掉的只有眼睛圖示。

另外 `.views` 是 `inline-flex`，標記裡數字和「次瀏覽」中間那個半形空白
會被 flex 吃掉（變成「1,284次瀏覽」），要用 `gap: .3em` 補回來。

---

## 六之七、favicon（2026-08-08 上線）

`assets/favicon.svg` 從 2026-08-02 那顆手繪牙換成**標誌本體**。
被三處引用：`index.html`、六篇文章、`404.html`（`preview/` 各頁也都有引用）。

### 改完一定要換 `?v=` —— 否則 Safari 永遠給舊圖

所有 `<link rel="icon">` 都帶版本參數：

```html
<link rel="icon" href="assets/favicon.svg?v=20260808b" type="image/svg+xml">
```

**這不是裝飾，是這顆圖示唯一能換掉的辦法。**
2026-08-08 換成標誌本體以後，使用者在手機 Safari 的分頁列表裡看到的還是
2026-08-02 那顆「深綠圓角方塊 ＋ 白牙」—— 檔案在伺服器上早就換了。

原因：**Safari 的 favicon 是存在一個獨立的圖示資料庫裡，鍵值是「圖示的網址」。**
只要網址一個字都沒變，它就認定手上那張還有效，
**重新整理沒用、清網頁快取也沒用**（那是另一份快取），開著的分頁更不會重抓。
Chrome／Firefox 沒這麼固執，所以在電腦上看是對的 —— 這件事只會在 Safari 上出現。

所以規則是：**動到 `assets/favicon.svg` 的內容（換色、改幾何），
就把全站 `?v=` 後面那個日期一起換成當天。** 一行指令掃過所有頁：

```bash
grep -rl 'favicon\.svg?v=' --include='*.html' . \
  | xargs sed -i 's|favicon\.svg?v=[0-9a-z]*|favicon.svg?v=<新版本>|g'
```

同一天內改第二次就在後面接字母（`20260808` → `20260808b`），
所以正規式要吃得下字母 —— 寫成 `[0-9]*` 會比對失敗，等於沒換。

換過之後，使用者那邊仍要**把分頁重新整理一次**才會換圖（新網址要被載到才會去抓）；
已經開著沒動的舊分頁會維持舊圖，關掉重開即可。

`<svg>` 上的 `width="64" height="64"` 也是為 Safari 留的：
`viewBox` 已經夠用，但少數算圖器對沒有內建尺寸的 SVG 會直接放棄，
放棄的時候 Safari 就退回它快取裡的上一張 —— 症狀和上面一模一樣，很難分辨。
兩個屬性成本是零，留著。

### 一個檔管兩種分頁列 —— 但 Safari 不算在內

SVG favicon 吃得到自己 `<style>` 裡的 `prefers-color-scheme`，
所以不需要兩個檔、也不需要在 `<head>` 寫兩個 `<link media="…">`。
**唯一的例外是 Safari：它不看 favicon 裡的深色模式，只會用沒有條件的那一行。**
（`<link>` 上的 `media` 屬性它同樣不看，所以拆成兩個檔也救不了。）

所以順序是**刻意反過來寫**的 —— 深色版當預設，淺色版放進 media query：

```svg
<style>
  path { fill: #4f8065; }                                    /* Safari 只看得到這一行 */
  @media (prefers-color-scheme: light) { path { fill: #4f6361; } }
</style>
<path fill="#4f8065" … />
```

Chrome／Firefox 兩種分頁列的結果和以前完全一樣，只有 Safari 從
「永遠拿到暗的那顆」變成「永遠拿到亮的那顆」。為什麼非這樣不可，見下一小節。

`fill` 屬性**要留著**：不吃 `<style>` 的算圖器（少數 RSS 閱讀器、舊工具）
會退回屬性上的顏色，而不是變成黑色方塊。CSS 的權重高於呈現屬性，
所以在正常瀏覽器裡不會打架。

### Safari 會自己在圖示後面墊一塊白底

**症狀**：iPhone 上按網址列（或開分頁列表），標誌被裝在一個**白色圓角方塊**裡，
旁邊別人的網站（用 emoji 當圖示的）卻是乾乾淨淨貼在黑底上。

**原因**：Safari 在深色模式下會自動幫「對比不夠」的圖示墊一塊白底或黑底，
好讓暗色圖示在黑底上還看得見。**淺色模式不會做這件事**（它容忍低對比圖示）。
門檻沒有公開，Apple 沒有文件，只能試。

把上一節那件事接起來就是完整的故事：Safari 不看深色模式 → 它一直拿到淺色版
`#4f6361` → 那個顏色**對深色列只有 2.67:1**，連非文字圖形的 3.0 下限都沒過 →
Safari 判定看不清楚 → 墊白底。順序換過來之後 Safari 拿到 `#4f8065`（3.73:1，過線）。

| 顏色 | 對白底 | 對純黑 | 對深色列 `#1c1c1e` |
| --- | --- | --- | --- |
| `#4f6361` 淺色 | 6.38:1 | 3.29:1 | **2.67:1**（不合格，會被墊白底） |
| `#4f8065` 深色 | 4.56:1 | 4.61:1 | 3.73:1 |

**如果 3.73 還是不夠**，只能再往上一階。代價是**淺色分頁列會跟著變淡** ——
Safari 只用同一個顏色畫兩種分頁列，在深底提亮就等於在白底減對比。
四個候選與各自的代價做成了可以在手機上逐頁看的頁面：

| 候選 | 色值 | 對深色列 | 對白底 |
| --- | --- | --- | --- |
| c1 | `#4f8065` | 3.73:1 | 4.56:1 |
| c2 | `#5d9877` | 5.04:1 | 3.37:1 |
| c3 | `#71a889` | 6.21:1 | 2.74:1（低於 3.0） |
| c4 | `#87b59c` | 7.39:1 | 2.30:1（低於 3.0） |

`preview/logo-favicon-plate/` —— 一個候選一頁、一頁一個圖示網址，
Safari 才會分開判斷。頁上的圖示刻意**不帶 media query**，測的就是 Safari 實際拿到的那顆。

> **不要用「加個底色方塊」來解這件事。** 那正是 2026-08-02 那顆手繪牙被換掉的原因，
> 見本節最後的幾何說明。要解就解亮度。

### 兩個顏色

| | 色值 | 亮度 Y | 對分頁列 | 說明 |
| --- | --- | --- | --- | --- |
| 淺色 | `#4f6361` | 0.11 | 5.81:1（白底） | 診療椅實測色，和科別色的 `--chair` 同一個值 |
| 深色 | `#4f8065` | 0.16 | 3.37:1（`#22252a`） | 色相 147°、彩度 24% 都和淺色同組，**只提亮度** |

深色版**不是另挑一個色**，是同一個色提亮到剛好站得住：

- `#4f6361` 直接放深底只有 2.41:1，**不合格**（非文字圖形的下限是 3.0）。
- 只要把亮度 Y 從 0.11 提到 0.15 就過線。早期版本提到 0.28（對比 4.8），
  多花了兩倍，結果**看起來像另一個主題色** —— 使用者就是被這個落差刺到的。
- 定案的 3.37 離下限留了餘裕（各家瀏覽器的深色分頁列深淺不一，
  貼著 3.0 會有風險），同時和淺色的色差 ΔE 壓到 20.7，並排要盯著看才分得出來。

五個候選（對比 3.0／3.4／3.8／4.3／4.8）與完整推導留在 `preview/logo-favicon/`。
要換成別案只改 SVG 裡那一行。

### 幾何

牙洞 **1.6 倍、上移 1.05** —— 比頁首那顆（1.3 倍、上移 0.50）誇張，
因為 favicon 要在 **16px** 下還看得出那個洞。同一條路徑，只有洞的比例不同。
`preview/logo-favicon-shift/` 是上移位置的四案紀錄。

> **標誌是橫的**（約 2:1），放進 64×64 的 viewBox 就只佔中間一條。
> 這是刻意的：**不要為了填滿方框而加圓角底色或把標誌拉高** ——
> 舊的那顆手繪牙就是「深綠圓角方塊 ＋ 白牙」，換掉它的理由正是那不是診所的標誌。

### 根目錄的 `favicon.ico` —— 只給 Google，不給瀏覽器（2026-08-09）

**這一顆和上面那顆 SVG 分工不同，不要混在一起看。**

| | `assets/favicon.svg` | `/favicon.ico` |
| --- | --- | --- |
| 誰在用 | 瀏覽器分頁列（Chrome／Firefox／Safari） | Google 搜尋結果那顆小圖 |
| 在 `<head>` 裡宣告 | 有，帶 `?v=` | **沒有，刻意不宣告** |
| 深淺色 | 兩色，靠 SVG 裡的 media query | 一色，`#4f8065` |
| 底 | 透明 | 透明 |

由 `tools/favicon-ico.mjs` 從 SVG 算出來，內含 16／32／48／96 四種尺寸的 PNG
（ICO 從 Vista 起就允許內容是 PNG）。48 與 96 是 Google 要的 —— 它要求邊長是 48 的倍數。

**為什麼要多一顆點陣圖：** 搜尋結果的圖示是 Google 另一支爬蟲另外抓的，
排程和網頁索引各走各的，而 `/favicon.ico` 是它在讀不到或不採用 `<link>` 宣告時
會主動去試的老位置。多放一顆等於多給它一條確定吃得下的路。

**⚠ 絕對不要把它加進任何 `<head>`。**
瀏覽器只有在頁面**完全沒有** `<link rel="icon">` 時才會去要 `/favicon.ico`；
我們每一頁都宣告了 SVG，所以瀏覽器一律走 SVG，行為和現在完全一樣。
一旦寫了 `<link rel="icon" href="/favicon.ico">`，Safari 很可能改用這顆點陣圖 ——
上面那整節「順序反過來寫、讓 Safari 拿到 `#4f8065` 才不會被墊白底」就等於白做。

**顏色只能是 `#4f8065`（深色版那一顆）。** ICO 是一張圖走天下，沒有深淺色可分，
而 Google 的搜尋結果淺色深色兩種底都會出現。`#4f8065` 正是為了「算圖器不看深色模式」
這種情境挑的，兩邊都站得住：對白底 4.56:1、對純黑 4.61:1，都過非文字圖形的 3.0。
淺色版 `#4f6361` 對純黑只有 3.29、對深色列 2.67，單張用會在深底吃虧。
所以腳本會**主動把 SVG 的 `<style>` 拿掉**，改把沒有條件的那個 fill 寫死到 path 上 ——
算出來的顏色不能看 Chromium 當下是淺色還是深色模式。

改過 `assets/favicon.svg` 的顏色或幾何之後要重跑：

```bash
node tools/favicon-ico.mjs           # 重新產生
node tools/favicon-ico.mjs --check   # 只比對，不一致就回傳非零
```

它需要一個 headless Chromium 來算圖，**平常的 `npm run build` 不會呼叫它**
（這個 repo 零 npm 依賴，Windows 本機通常也沒有那個執行檔）。
`favicon.ico` 已經進版控，跑不動它不影響建置或部署。

> **踩過的兩個坑，改這支腳本之前先看：**
>
> 1. **剪 `<style>` 之前要先剪掉 `<!-- -->` 註解。** `favicon.svg` 的註解裡本來就在
>    討論 `<style>` 這個標籤，字面上就有 `<style>`；直接比對會咬到註解裡那一個，
>    一路刪到真正的 `</style>`，連註解自己的 `-->` 都被刪掉 ——
>    `<path>` 整條被吞進沒有結尾的註解裡，算出來是一張全透明的空圖。
>    **ICO 的結構、每張的尺寸全都正常，光看檔案看不出來。**
> 2. **新版 headless 的 `chrome` 在容器裡畫不出 SVG 圖形。** 背景色照樣上得去，
>    `<rect>`／`<polygon>`／`<path>` 一個都沒有。Playwright 的 `headless_shell`
>    （舊 headless）三種都正常，所以腳本把它排在候選的最前面。
>
> 這兩件都會產出「看起來很正常的空圖示」，所以腳本裡有一道保險絲：
> 同一張頁面**不放 SVG** 再算一次當對照組，兩張如果一個位元組都不差就直接失敗。

---

## 六之八、科別的意象（2026-08-08）

**這一節是六之二節的另一半。** 六之二寫的是「哪一科拿哪個色、白字幾比幾」，
來源是診所的實物；這一節寫的是「**為什麼那個色是對的**」。使用者給的樣板是兒牙：

> 「兒童牙科的對象是小孩，小孩是活潑的、對未來有無限的可能，所以我堅持用黃色。」

推導鏈是四段：**對象是誰 → 那個人處在什麼狀態 → 這一科真正在做的事 → 一個意象**，
顏色接在最後。這一節把其餘六科補上同一條鏈。

> **這一節不改任何色值，也不推翻六之二。**
> 六個色是先從診所實物量出來的（**只有兒牙的黃例外 —— 診所裡沒有黃**），
> 意象是事後整理的。但整理完之後對得上，而且對得比預期整齊 —— 見第三小節。

### 一、七科各自的意象

排序照色相，不照選單順序 —— 這樣三組的分界看得出來（理由在第三小節）。

| 科別 | 對象 | 動作 | 病人付出的 | 意象 | 色 | H |
| --- | --- | --- | --- | --- | --- | --- |
| 兒童牙科 | 還在長的人 | **長** | 情緒（怕） | 還沒定型的未來 | `#c28229` | 35 |
| 口腔外科 | 帶著一件明確的事來的人 | **移除** | 情緒（痛） | 被灰包住的紅 | `#7D5A58` | 3 |
| 顯微根管 | 一顆還救得回來的牙 | **保留** | 幾次回診 | 花謝了枝條還在 | `#5D6D55` | 100 |
| 一般牙科・牙周 | 沒有事件的人 | **維持** | 習慣 | 每個人都會坐上去的那張椅子 | `#4f6361` | 174 |
| 齒顎矯正 | 願意用一到三年換一個結果的人 | **排列** | 時間 | 坐著等的沙發、夜空 | `#3C596B` | 203 |
| 贋復假牙 | 要重新規劃一整口的人 | **重建** | 時間與錢 | 茶室裡坐下來談 | `#252d4a` | 227 |

#### 兒童牙科 `#c28229` —— 還沒定型的未來

使用者自己給的那一段，其餘六科都是照它反推的。對象不是牙，是**人，而且是還在長的人**：
牙會換、臉會變、習慣還沒定，什麼都還沒定案。黃是七個色裡唯一「往上、往外」的那一個。

**它也是唯一不從診所實物量出來的色** —— 診所的家具、磁磚、花藝裡沒有黃
（第三節真正量到的黃只有開幕茶會的含羞草 `#9d8313`，是花不是硬裝）。
這件事不是缺陷，是這一科的定義：**兒牙的主體本來就不在診所裡，在孩子身上。**

還有一層：兒牙也是七科裡唯一「這次看得好不好，決定往後四十年」的科。
第一次進診所的經驗會跟著人走，所以這一色要是**進門就看得到、而且不嚇人**的那種。

#### 口腔外科 `#7D5A58` —— 被灰包住的紅

對象是帶著一件明確的事來的人（拔智齒、囊腫、顎面）。這一科的關係是**事件**，不是長線：
有開始、有結束，通常一天。

Deep Taupe 的色相 3 是整套色票裡**最靠近正紅的一個**（比磚紅 `#AF4C52` 的 356 還正），
但彩度只有 17.4%。**它是唯一帶血色相、卻被壓到不喊叫的色。**
外科是七科裡唯一該和紅有關的一科，而診所刻意沒有給它磚紅 ——
六之二節記著使用者的原話：紅在品牌裡的暗示性太強。

Taupe 正好是那句話的解：**份量都在，但不出聲。**
實物是單椅布料，一個人坐的那一張。

> **⚠ 這一段已經被使用者推翻了（2026-08-09）**，他說血色的意象怪怪的，而他是對的：
> 上面這段是**從顏色反推意象**，順序反了。照他給的完整定位重推，
> 這一科的病人付出的不是痛，是**風險**；動作不是「移除」，是「交付」。
> 靛／紫的五案與完整推導見**第六之九節**。這一段留著當紀錄。
> **已定案：填色 `#8e6299`、白底的字 `#784e84`。**

#### 顯微根管 `#5D6D55` —— 花謝了枝條還在

對象不是人，是**一顆已經在痛、但還救得回來的牙**。這一科在做的事只有一個字：**保留**。
不是換掉，是把裡面清乾淨，留住外面那顆自己的牙。牙髓本來就是牙齒裡活著的那一部分。

它拿到的是**苔綠，也就是 Logo 本身的色**，而且和花藝裡枝葉的實測
（`#2f3929` H=98、亮處 `#447c56` H=139）同一族。
第六節第 1 條寫「綠是枝條，可以重複出現」—— 花會謝，枝條留著。

**這是七科裡最強的一個對應：診所的核心價值（保住自己的牙優於置換）拿到了診所自己的標誌色。**
延伸的話，家色深綠松 `#214D48`（H=173）和苔綠同族，也在同一個意思上 —— 保留與延續是一件事。

#### 一般牙科・牙周 `#4f6361` —— 每個人都會坐上去的那張椅子

對象是**沒有事件的人**：沒特別哪裡痛，或狀況還沒變成事件。
這一科在做的事是**維持**，半年一次回來洗牙、檢查、牙周維護。不是一次解決，是一直來。

意象就是那張診療椅 —— 診所裡唯一每個人都會坐上去的東西，使用頻率最高的物件。
實測色 `#4f6361` 對家色深綠松 ΔH=1，**它是家色的實物版**：日常＝家色，這一層是相合的。
使用者指定給創辦醫師與李侑津醫師的也正是這一色。

> **兩科共用一色，在意象層是說得通的 —— 但有一個要盯的地方。**
> 一般牙科顧的是牙冠上面看得到的，牙周顧的是牙齒底下的地基，
> **病人的行為完全一樣（定期回來）、時間軸也一樣（無限期）**，是同一件事的兩個深度。
> 使用者「不同科顏色不要重複」那條規則，管的是**事件型**的科；這兩科不是事件，是同一條長線。
>
> 要盯的是 **`build.mjs` 的 `SPEC` 把「植牙」也放在 `perio` 這一格** ——
> 而植牙是徹頭徹尾的事件型（要規劃、要分期、要幾個月）。
> 意象層給的答案很清楚：**植牙屬於 `prosth`**，
> 首頁 chip 寫的「植牙・假牙重建」本來就在 `prosth` 那顆上。
> 目前沒有任何文章用「植牙」這個標籤，所以還沒出事；**第一篇植牙文出現之前要先決定**。

#### 齒顎矯正 `#3C596B` —— 坐著等的沙發

對象是**願意用一到三年換一個結果的人**。這一科在做的事是**排列**，
而它真正要病人付出的是時間 —— 牙齒一個月只走幾分之一毫米，急不得。

藍灰是候診沙發的色（H=206，實測 `#4e718b`），也和夜空 `#373b42`（H=218）同一條軸 ——
第二節寫著，那是**整棟建築唯一天然的冷色**。
**整套色票裡唯一的冷色，給了唯一以時間為主要成本的科。**

還有一層和 COPY.md 第三節的紅線直接相關：矯正是七科裡唯一**病人自己每天要做功課**的科
（戴、清、按時回診）。「診所給的是燈，不是答案」在這一科最具體。

#### 贋復假牙 `#252d4a` —— 茶室裡坐下來談

對象是牙已經缺了、要重新規劃一整口的人。這一科在做的事是**重建**：
不是修一顆，是把整個結構重新排一次，要談、要分期、要幾個月。

它拿到的是茶室深藍磁磚，**整個色盤最深的一個**（L\* 19.1，白字 13.52:1）——
份量最重的科拿最沉的色。而且磁磚是貼在牆上不會動的東西，這一層也對得上「重建」。

意象取「茶室」而不是「藍」是有意的：**這一科最缺的不是技術，是坐下來把話講完的時間。**

#### 全部 `#5f5d5c` —— 刻意沒有意象

它不是一科，是「不篩選」。`index.html` 那段註解已經寫了：六個彩色各代表一科，
這一顆若也上色就等於多了一科不存在的科別。
它錨在頁首玻璃 `#393736` 往上提亮度，**所以它屬於介面，不屬於科別。**

### 二、兒牙為什麼調了十一輪 —— 意象層的解釋

六之二節記了十一輪的過程，但沒說為什麼**只有這一科**這麼難。把六個色的 Lab 攤開就看得出來：

| | L\* | C\* |
| --- | --- | --- |
| 贋復 `#252d4a` | 19.1 | 19.9 |
| 矯正 `#3C596B` | 36.3 | 14.8 |
| 一般牙周 `#4f6361` | 40.3 | 8.1 |
| 外科 `#7D5A58` | 41.8 | 15.7 |
| 根管 `#5D6D55` | 44.1 | 15.8 |
| **兒牙 `#c28229`** | **59.5** | **57.6** |

**兒牙在兩個維度上都是離群值**：最亮，而且彩度是其餘五科的三到七倍。

這不是調過頭，這正是「活潑、無限可能」翻成顏色之後的樣子 ——
**它是這套色盤裡唯一被要求往外跑的色，而整套色票的體質是安靜的**
（其餘五科全部擠在 C\* 8~20）。十一輪撞的天花板一直是同一個：
要它夠亮才像黃、又要白字站得住，而白字把亮度鎖死在 L\* 47~48。
最後定在 59.5 是**放掉白字的 AA**（3.22:1，已結案）換來的 —— 那筆帳付的就是這一科的意象。

### 三、量出來的三組（這一節最有用的部分）

六個色相排開：

```
外科 3    兒牙 35   │   根管 100   一般牙周 174   │   矯正 203   贋復 227
      情緒               維持與保留                    時間與規劃
```

**三組在色相環上是連續的，沒有交錯。** 而分組的依據不是顏色，是第一小節那張表的
「病人付出的」那一欄：

| 色溫 | 科 | 病人付出的 | 所以 |
| --- | --- | --- | --- |
| 暖（H 3~35） | 外科、兒牙 | **情緒**（痛、怕） | 需要被安撫 → 暖 |
| 綠（H 100~174） | 根管、一般牙周 | **習慣**（回來、刷） | 需要持續 → 活的、枝條的綠 |
| 冷（H 203~227） | 矯正、贋復 | **時間與錢** | 需要冷靜規劃 → 冷 |

> **⚠ 這張表已經被取代（2026-08-09）。** 外科改成紫之後移到冷色端 289，
> 暖色只剩兒牙，冷色段多出「越冷門檻越高」這一層。**取代它的版本在六之九第七小節第 1 條**，
> 那一版比這一張好。這一張留著看推論。

> **這是量出來的，不是設計出來的。** 六個色是 2026-08-07 各自從診所實物挑的，
> 挑的時候沒有這張表；功能分組是 2026-08-08 才做的。兩者對得上，
> 表示診所實物本身就帶著這個秩序 —— 也表示**這張表可以拿來預測下一個色該落在哪**。

### 四、這一節能拿來做什麼

1. **新增科別時，色相的位置已經被決定了。**
   先問「病人在這一科付出的是情緒、習慣、還是時間與錢」，答案直接指定色相段。
   例如口腔黏膜／顏面疼痛 → 情緒 → 暖色段；美容牙科／貼片 → 規劃 → 冷色段。
2. **色盤其實已經滿了，不要無限加科別色。**
   六個色之間只剩兩段空隙，兩段都不能用：
   **35→100 的黃綠帶**是兒牙第三輪「泛綠、嘔吐物感」被否決的那一帶；
   **227→3 的紫紅帶**，紅的部分是磚紅、而紅在品牌裡的暗示性太強（六之二）。
   ~~紫的部分使用者明確不要（第四節 B）。~~
   **2026-08-09 更正：紫的部分開放了**，使用者主動要求口腔外科往靛／紫走（第六之九節）。
   第四節 B 那句「不要帶紫」講的是**內文底色**（一整片 L\* 88 的淡紫），
   不是一顆 L\* 40 的深紫藥丸 —— 底色那條仍然有效。
   所以再加科別**只能擠既有的三段**，那會壓縮現有六色之間的辨識度。
3. **故事類內容的位置**（COPY.md 第六節未決的第 2 題）——
   這一節給的建議是：**故事不要再拿一個新顏色**。
   「主題與科別」裝的故事若屬於某一科（療程小故事），就吃那一科的色；
   屬於診所本身的（開業史、四十多年）走中性灰那一支，和「全部」同族 ——
   理由和「全部」不上色是同一個：那不是第七科。**這是建議，不是定案。**
4. **`口腔外科` 與 `顯微根管` 目前沒有 chip**（沒有對應文章）。
   有了文章要補的時候，顏色與意象在這裡都已經備好了。

### 五、這一節**不是**對外文案

上面那些詞（「被灰包住的紅」「花謝了枝條還在」）是**內部推導用的**，
拿去當頁面上的字會立刻踩到 COPY.md 第二、三節：不出現「牙齒」、主詞要是人、
不要行銷／品牌腔 —— 第六節已經為了同一件事否決過六輪。
要把這些變成使用者看得到的字，得重新走 COPY.md 的規則，**不要直接搬。**

---

## 六之九、口腔外科改靛／紫（2026-08-09，五案提案中）

使用者給了口腔外科完整的定位，然後說：

> 「我感覺這個意象和目前顏色有點衝突（血色的意象似乎怪怪的），
> 心裡覺得應該會是紫色或靛色（先有高技術門檻的專業感），
> 再加入降低彩度或色相或其他方式達到支持、堅定、不喧賓的感覺。」

> **定案（2026-08-09）：填色 `#8e6299`、白底的字 `#784e84`。**
> 已套進 `index.html` 與 `assets/style.css`。`--taupe` 本身留著 ——
> 診所資訊的「看診時間」卡還在用它。
> **`main` 上仍然是原本的 Deep Taupe**，這個分支不會自動部署，要上線得先合併。
> 推導從下面第一小節開始，定案的驗算在第八小節最後。

### 一、新的定位（使用者給的，這是推導的起點）

| | |
| --- | --- |
| 目標族群 | 高中後期～研究所、剛進社會的青年；以及**怕拔智齒所以和智齒相伴多年的中年人** |
| 中年人為什麼終於來 | 智齒部分露出不好清潔、或和第二大臼齒有間隙，沒睡好、較累的時候就脹痛（怕，所以蠻能忍） |
| 他們想找的 | 技術好、拔牙不痛、快 |
| 實際上是什麼 | 切開、翻瓣、修骨，甚至把牙齒切半取出 —— **一定是手術等級** |
| 醫師要顧的風險 | 下顎神經管、上顎鼻竇穿孔 |
| 診所要做的事 | 說明清楚它不簡單 → 病患有信心交付 → **病患自己也接受並承擔風險**（真出事醫師有對應方法） |

### 二、為什麼「血色」這個意象站不住 —— 使用者是對的

六之八節把外科寫成「被灰包住的紅」，理由是 Taupe 的色相 3 是色票裡最靠近正紅的一個。
**那是從顏色反推意象，不是從意象推顏色 —— 順序反了。**

照新的定位重推，這一科的病人真正付出的不是痛，是**風險**；
診所給的也不是安撫，是**說明**。整條鏈變成：

| | |
| --- | --- |
| 對象 | 忍了很多年、終於決定處理的人 |
| 動作 | **交付** |
| 病人付出的 | **風險**（聽懂之後自己承擔） |
| 意象 | **鋪在四周的那塊布** |

> 意象改成手術巾是有依據的，但要說得準確：
> **外科真正拿來對抗血色的顏色是藍綠與藍**（手術衣、手術巾用血的補色，
> 是為了讓術者不留殘像、看得清楚）。那個方向本身就證實了使用者的直覺 ——
> **外科的顏色不該是血，應該是外科自己用來抵銷血的那一端。**
>
> 但**那兩段色相在這套系統裡已經被佔走了**：一般牙周 174、矯正 203。
> 所以往同一個方向再走一步，落在冷色端還空著的地方 —— 也就是靛與紫。
> **不要把「紫＝手術巾的顏色」寫成事實，手術巾不是紫的。**
> 站得住的是那塊布的**功能**：它把範圍框出來（堅定）、
> 讓注意力不在血上（不喧賓）、它不是主角但少了它做不成（支持）。

### 三、這一輪和兒牙那十一輪的差別：完全沒有被對比度綁住

先講一個讓這一輪好做很多的事實。L\* 40 這一帶各色相的彩度上限：

```
h 280°  50.6      h 300° 106.8      h 320°  98.4
h 290°  69.1      h 307° 119.0      h 340°  73.9
```

**上限是 50~124，而五案只用到 22。** 兒牙那十一輪是「要亮才像黃、白字又把亮度鎖死」，
兩頭夾殺；靛紫在這個亮度帶連邊都碰不到。所以：

> **五案的亮度全部固定在 L\* 40**（＝官方色票灰紫自己的亮度），
> 白字 6.44~6.50、對卡 5.85~5.91、對紙 5.08~5.13 —— **五案幾乎一模一樣**。
> 這一排比的**純粹是色相與彩度**，沒有任何一案是被對比度逼出來的。
> 這也表示口腔外科可以維持**一個變數就夠**（不像兒牙要三個），
> 文章內頁的字階也直接用原值，不必另外開一階。
>
> **⚠ 這一句在第二輪被收回了（見第八小節）** —— 使用者要求填色與白底的字分開，
> 而那個要求正好解開了「一個值被紙上的 L\* 43.4 綁住」的天花板。**收回是好事。**

L\* 40 是這樣定的：白字要 4.5:1 的上限約 L\* 49.8、當字疊在卡上 47.4、
**疊在紙上 43.3（最緊的一關，六之六節那一條）**。取 40 三關都有餘裕，
而且和其餘五科同一帶（36~44）。

### 四、五案

固定：**L\* = 40、白字**。變數只有兩個：**色相（靛↔紫）× 彩度（收斂↔存在感）**。
① 是錨點 —— 官方色票裡唯一的紫，地位等同兒牙那一輪的含羞草。

| | 底 | L\* | C\* | Lab h | HSL H | 白字 | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **① 官方灰紫**（色票原值） | `#5F5D66` | 40.0 | 5.6 | 299° | 253 | 6.47 | 5.89 | 5.11 |
| **② 靛・收斂** | `#5e5c70` | 39.9 | 12.3 | 296° | 246 | 6.48 | 5.90 | 5.12 |
| **③ 靛・存在感** | `#5c5b7f` | 40.1 | 22.0 | 294° | 242 | 6.45 | 5.86 | 5.09 |
| **④ 紫・收斂** | `#685a6b` | 40.1 | 11.9 | 320° | 289 | 6.44 | 5.85 | 5.08 |
| **⑤ 紫・存在感** | `#6f5575` | 39.9 | 22.0 | 320° | 289 | 6.50 | 5.91 | 5.13 |

彩度的兩階是照家族自己的範圍定的：其餘五科（不含兒牙）落在 C\* 8.1~19.9，
**22 是貼著家族上限、12 是家族中位** —— 「不喧賓」的標準不是低到看不見，
是不要超過贋復太多。兒牙是 57.6，五案沒有一個接近它的一半。

### 五、辨識度：這一輪真正的篩選條件

對比度五案全過，所以決定勝負的是**它和已經在用的色分不分得開**（CIE76 ΔE）：

| | 對贋復 `#252d4a` | 對矯正 `#3C596B` | 對全部 `#5f5d5c` | 對根管 `#5D6D55` |
| --- | --- | --- | --- | --- |
| ① 官方灰紫 | 25.5 | 12.6 | **6.1 ✗** | 21.7 |
| ② 靛・收斂 | 22.4 | 11.7 | 12.8 | 28.1 |
| ③ 靛・存在感 | 21.3 | 16.3 | 22.5 | 37.5 |
| ④ 紫・收斂 | 24.2 | 16.3 | 12.0 | 28.0 |
| ⑤ 紫・存在感 | 24.2 | **22.8** | 22.2 | 38.1 |

**① 出局，而且是唯一硬性出局的。** 官方灰紫 L\* 40.0、C\* 5.6，
「全部」那顆灰是 L\* 39.6、C\* 1.0 —— **亮度只差 0.4，ΔE 只有 6.1**。
在藥丸那個尺寸上兩顆會讀成同一個色，而「全部」不是一科（六之八第一節），
科別色和它撞在一起是最不能撞的一種。

> 這正好是第四節 B「灰是比出來的，不是調出來的」的另一面：
> **一個彩度 5.6 的紫，旁邊站著一個彩度 1.0 的灰，它就不是紫了。**

**靛（②③）還有一個結構性的問題**：HSL 色相 242~246，
夾在矯正 203 與贋復 227 中間再往外一點點 —— 冷色端三顆會擠在 203/227/242 的 39° 內。
紫（④⑤）的 289 離贋復 62°，是唯一真正走出去的。

### 六、目前的判斷

**⑤ 紫・存在感 `#6f5575`** 是數字上最乾淨的一案：對四個現役色的 ΔE 全部 ≥22，
色相 289 在整個色盤上沒有鄰居，彩度 22 貼著家族上限但沒超過兒牙的四成。
要更收斂就往 ④，要讀起來像靛就往 ③（代價是和贋復擠在一起）。

**這是建議，不是定案。** 對應的提案頁見本節最後。

### 七、如果改了，有四件事要跟著改

1. **六之八的三組會重排，而且會變好。**
   外科離開「情緒」組，冷色端變成 **矯正 203 → 贋復 227 → 外科 289**，仍然連續。
   分組的意思要跟著改寫：

   | 色溫 | 科 | 病人付出的 | 為什麼 |
   | --- | --- | --- | --- |
   | 暖 | 兒牙 35 | 情緒 | **唯一不需要被說服、只需要不害怕的科** |
   | 綠 | 根管 100、一般牙周 174 | 習慣 | 靠一直回來 |
   | 冷 | 矯正 203、贋復 227、外科 289 | 時間／錢／**風險** | **要先聽懂，才決定交付** |

   > 冷色段還多出一個順序：**越冷，門檻越高**（時間 → 錢與規劃 → 風險與技術）。
   > 六之八寫的版本沒有這一層，這一輪的定位把它逼出來了。

2. **Taupe `#7D5A58` 被釋出，但沒有浪費** —— 它還在診所資訊的「看診時間」卡
   （`.info-card[data-hue="taupe"]`）。而且釋出之後，暖紅那一整段就沒有任何科別在用，
   和「紅在品牌裡的暗示性太強」那條原則反而更一致。

3. **第四節 B「不要用暖色偏／不要帶紫的感覺」要標清楚適用範圍，不是推翻。**
   那句話的出處是**內文底色** —— G1 灰紫 `#dedce5`（L\* 88、整片鋪滿）使用者說「有點紫紫的」。
   **一整片 L\* 88 的淡紫，和一顆 L\* 40 的深紫藥丸不是同一件事**，
   大面積的色偏會決定整頁的調性，藥丸只有幾百個像素。兩件事不衝突，
   但**底色那條仍然有效，不要拿這一節去改底色。**

4. **六之八第四小節第 2 條要改寫。** 原文說「227→3 的紫紅帶不能用」，
   理由是紫不要、紅太強。**紫的部分開放了，紅的部分不變。**

### 八、第二輪：⑤ 選定，拆成填色與白底兩個值（2026-08-09）

使用者選了 ⑤，同時提出：

> 「標籤在白底時候的跟套色的可以是兩個不一樣的。套色的明度不太夠，
> 可以再稍微拉一點點彩度跟明度，讓它套色下去後就是有點**燈亮**的感覺，可以再清楚一點。
> 白底的時候現在看起來有點**紫黑紫黑**的。」

#### 這個要求本身解開了一個天花板

第三小節說「口腔外科可以維持一個變數就夠」—— **那句話要收回，而且收回是好事。**
一個值同時要當底又要當字的時候，它被最嚴的那一關綁著：

| 這個值要做什麼 | 天花板 |
| --- | --- |
| 當填色的底，上面壓白字 | L\* 49.9 |
| 當字疊在卡 `#f4f4f5` 上 | L\* 47.3 |
| **當字疊在紙 `#e2e5e6` 上（文章內頁）** | **L\* 43.4 ← 綁住一切的那一關** |

所以 ⑤ 只能停在 39.9。**拆成兩個值之後，填色那一側只剩白字這一關，
天花板變成 49.9 —— 一口氣多了 6.5 階亮度。**
使用者要的「燈亮」在單一變數下做不到，是拆開才做得到的。

機制不用發明：`--accent`（填色）／`--accent-deep`（白底的字）／`--on-fill`
兒牙已經在用，**外科會是第二個用兩階的科**。

> ⚠ 那三個變數的預設值那一行**必須排在科別色前面**。
> `[data-spec]` 和 `[data-spec="surg"]` 權重相同，排在後面會把深階字蓋回去。
> 兒牙踩過一次，見六之二節最後。

#### 填色：往「燈亮」走（色相鎖 320°，明度與彩度一起往上）

| | 底 | L\* | C\* | 白字 |
| --- | --- | --- | --- | --- |
| ⑤ 基準 | `#6f5575` | 39.9 | 22.0 | 6.50 |
| **F1 亮一階** | `#7a5a82` | 42.9 | 27.0 | 5.81 |
| **F2 亮兩階** | `#865f90` | 46.0 | 32.4 | 5.18 |
| **F3 頂著上限** | `#8e6299` | 48.0 | 36.0 | 4.82 |

F3 是白字撐得住的最後一階（L\* 50 就掉到 4.4 附近）。
F2、F3 拿去當字不夠（對卡 4.71／4.38）—— **但那正是拆開的意義，它們不必當字。**

#### 白底的字：三案是診斷式的，各只動一個變數

「紫黑」有兩個可能的成因：亮度不夠，或彩度不夠。**所以三案不是三個口味，
是三個假設** —— 使用者選哪一個，就直接回答了成因是哪一個。

| | 值 | L\* | C\* | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- |
| ⑤ 基準 | `#6f5575` | 39.9 | 22.0 | 5.91 | 5.13 |
| **T1 只拉彩度** | `#784e84` | 39.8 | 36.2 | 5.93 | 5.15 |
| **T2 只拉明度** | `#775d7d` | 43.1 | 21.8 | 5.24 | 4.55 |
| **T3 兩個一起** | `#7f578a` | 43.0 | 33.9 | 5.26 | 4.57 |

> **T1 是免費的**：亮度一度沒動，所以對比和 ⑤ 幾乎一樣，純粹多拿了彩度。
>
> 會先問「是不是彩度」是**兒牙那一輪學到的**：當時「拿鐵感」的成因其實是彩度不足，
> 卻挪了四輪色相都沒打中（六之二第四輪）。這次先把兩個成因分開來問，不要一起調。

**T2、T3 貼著紙的天花板 43.4**（對紙 4.55／4.57，過了但餘裕不多）。
再亮就得像顯微根管與兒牙那樣替文章頁另開一階（六之六節），**四案目前都不必**。

#### 定案：T1 配 F3（使用者選的）

```css
[data-spec="surg"] { --accent: #8e6299; --accent-deep: #784e84; }
```

**這個選擇同時回答了診斷題：「紫黑」的成因是彩度不足，不是亮度。**
T1 的亮度和 ⑤ 差 0.1，一度沒動 —— 解決問題的是彩度從 22.0 拉到 36.2。
第三輪的兩個假設之中，亮度那一支（T2）落選了。

而且兩階剛好長成整個色盤裡最乾淨的一組：

| | 填色 F3 | 深階 T1 | 差 |
| --- | --- | --- | --- |
| L\* | 48.0 | 39.8 | **8.2** |
| C\* | 36.0 | 36.2 | −0.1 |
| Lab h | 320.4° | 319.9° | 0.5° |

**色相與彩度完全相同，只差亮度。它不是「兩個色」，是同一個色的兩個高度。**
兒牙那一組（`#c28229` / `#9e6301`）色相差 6°、彩度差 20 ——
那是被物理逼出來的；這一組是選出來的。

#### 每一個實際用到的地方都驗過

| 用在哪 | 用哪一階 | 對比 | 門檻 |
| --- | --- | --- | --- |
| 醫師藥丸（實心＋白字 11.5px） | `#8e6299` | 4.82 | 4.5 ✓ |
| chip 選取（實心＋白字 13px） | `#8e6299` | 4.82 | 4.5 ✓ |
| 文章內頁 實心藥丸／按鈕（白字） | `#8e6299` | 4.82 | 4.5 ✓ |
| chip 未選（字與框，底是卡） | `#784e84` | 5.93 | 4.5 ✓ |
| 文章卡標籤（字與框，底是卡） | `#784e84` | 5.93 | 4.5 ✓ |
| 文章內頁 連結／`h3`（底是紙） | `#784e84` | 5.15 | 4.5 ✓ |
| 小節短棒／`.callout` 左條（底是紙） | `#8e6299` | 3.81 | 3.0 ✓ |

> **這是七科裡第一組完全沒有例外的科別色。**
> 兒牙的填色白字是 3.22（已結案的取捨）；顯微根管與兒牙都還要替文章頁另開一階字色
> （六之六節）。這一組兩者皆免 —— 兩個值就把七個位置全部蓋掉。

ΔE 也是分得最開的一組：對六個現役色最小 30.4（深階對贋復），其餘落在 34~81。

#### 要盯的一件事：填色比字亮 8.2 階

chip 的未選與選取會有明顯的深淺差。兒牙也是這樣（填色 59.5、字 47），
所以不是新的行為 —— 但兒牙是被逼的，外科是選的。上線前在真機上看一眼。

### 九、提案頁

`preview/spec-surg-violet/` —— 第一輪五案各自套在三個真實元件上（實心藥丸、
未選的 chip、文章卡標籤）並和其餘六科並排；第二輪的兩條梯子可以任意組合，
同時看首頁藥丸、chip 兩態、文章卡標籤、以及文章內頁（底是紙不是卡）。
網址可帶 `?f=f2&t=t1`。

---

## 六之十、贋復假牙的藍提亮（2026-08-09，3×3 提案中）

使用者說：

> 「植牙假牙重建的標籤目前是很深的藍色。白底的藍色不要那麼深，稍微淺一點點；
> 套色下去後的藍色標籤也是很深，可以比照像紫色那樣再亮一點。」

> **定案（2026-08-09）：填色 `#4a5789`（F2）、白底的字 `#364067`（T1）。**
> 已套進 `index.html` 與 `assets/style.css`。`--tile` 本身留著 ——
> 它是第二節的材質實測值，只是 `prosth` 不再直接指向它。
> 推導從下面第一小節開始，定案的驗算在第六小節。

提案頁 `preview/spec-prosth-blue/`，網址可帶 `?f=f1|f2|f3&t=t1|t2|t3`。

### 一、「很深」是量得出來的，不是感覺

| 科別 | 色 | L\* | C\* | 白字 |
| --- | --- | --- | --- | --- |
| 兒童牙科（填色） | `#c28229` | 59.5 | 57.6 | 3.22 |
| 口腔外科（填色） | `#8e6299` | 48.0 | 36.0 | 4.82 |
| 顯微根管 | `#5D6D55` | 44.1 | 15.9 | 5.55 |
| 一般・牙周 | `#4f6361` | 40.3 | 8.1 | 6.38 |
| 齒顎矯正 | `#3C596B` | 36.3 | 14.8 | 7.41 |
| **贋復假牙（現行）** | **`#252d4a`** | **19.1** | 19.9 | **13.52** |

其餘六科擠在 L\* 36~48 的十二階裡，**贋復一個人低了十七階**，
白字 13.52 是 AA 門檻的三倍。

**當初這樣定的理由已經消失了。** 六之二節寫的是「口外與贋復是不同科、顏色不要重複，
贋復拿整個色盤裡最深的那一個」—— 但口腔外科在六之九節換成 H 288 的紫之後，
離贋復的 227 有六十幾度，**不必再靠深度去分**。這一輪不是推翻決定，
是那個決定的前提被上一輪拆掉了。

### 二、⚠ 彩度不能不動 —— 這一輪唯一真正的技術判斷

直覺做法是「色相不動、彩度不動，只提明度」。**那樣會壞，而且壞在看不見的地方：**

| L\* | 固定 C\*=19.9 的結果 | HSL 彩度 | ΔE 對矯正 |
| --- | --- | --- | --- |
| 19.1（現行） | `#252d4a` | 33% | 21.3 |
| 30 | `#3e4564` | 23% | 14.0 |
| 34 | `#484e6e` | 21% | 13.0 |
| **38** | `#525878` | **19%** | **12.5** |
| 42 | `#5b6182` | 18% | 13.7 |
| 46 | `#656b8d` | 17% | 16.0 |

深藍的彩度天花板隨亮度長得很快（h 287 的 maxC：L\* 19 是 37.5，L\* 48 是 70.4），
**C\* 固定不動就等於一路變灰** —— HSL 彩度掉一半，ΔE 對齒顎矯正掉到 12.5。
兩顆冷色藥丸常常並排在同一張卡片旁邊，這個距離不夠。
（對照六之九：① 官方灰紫對「全部」只有 6.1，是硬性出局；12.5 沒那麼糟，但也不該接受。）

> **所以六個值的彩度都跟著亮度按色域比例放大** —— 固定在現行值自己的位置
> （該亮度下彩度上限的 **51%**），色相鎖死在 **Lab 286.7°**。
> HSL 彩度就一路守在 29~32%（現行 33%），ΔE 對矯正維持 18~29。
> **顏色的個性一度沒變，變的只有高度** —— 等於同一塊茶室磁磚被燈打亮，
> 和六之八節給贋復的意象（茶室裡坐下來談）是同一件事。

**這一輪離開了實測值**：茶室深藍磁磚三個取樣是 `#0c111c` / `#1d273d` / `#252d4a`，
現行值已經是其中最亮的一個，再往上就沒有照片可以錨。六個值全部是從 `#252d4a`
自己延伸出來的，不是另外挑的藍 —— 這是刻意的，**不要改成從別處取色**。

### 三、兩條梯子

拆成兩個值的機制和外科、兒牙一樣：`--accent`（填色）／`--accent-deep`（白底的字）。
天花板只跟明度有關、跟色相無關，所以下面三個數字對每一科都一樣：
白字 4.5:1 → L\* 49.8；當字疊在卡 `#f4f4f5` → 47.4；**當字疊在紙 `#e2e5e6` → 43.4（最緊）**。

**填色（套色）—— 只受白字那一關限制：**

| | 值 | L\* | C\* | HSL | 白字 | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 現行 | `#252d4a` | 19.1 | 19.9 | 227/33% | 13.52 | 12.30 | 10.68 |
| **F1 亮一階** | `#3a456d` | 30.1 | 25.7 | 227/31% | 9.33 | 8.48 | 7.36 |
| **F2 亮兩階** | `#4a5789` | 38.0 | 30.8 | 228/30% | 6.96 | 6.34 | 5.50 |
| **F3 頂著上限** | `#5e6fac` | 47.9 | 36.1 | 227/32% | 4.83 | 4.40 | 3.82 |

F3 和外科的填色站在同一個高度（47.9 對 48.0、白字 4.83 對 4.82），
**兩科的視覺重量會完全一致**。F3 對紙 3.82 也過得了小節短棒／`.callout` 左條的 3:1。

**白底的字 —— 被紙的 L\* 43.4 綁著：**

| | 值 | L\* | C\* | HSL | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- | --- |
| 現行 | `#252d4a` | 19.1 | 19.9 | 227/33% | 12.30 | 10.68 |
| **T1 淺一點點** | `#364067` | 27.9 | 25.3 | 228/31% | 9.16 | 7.95 |
| **T2 中間** | `#44507e` | 34.9 | 28.8 | 228/30% | 7.09 | 6.15 |
| **T3 進家族帶** | `#526197` | 42.1 | 32.7 | 227/30% | 5.43 | 4.72 |

T3 的 42.1 和外科深階 39.8、一般牙周 40.3 同一帶，對紙留 4.72（外科那一輪接受過 4.55）。
**三案都不必替文章內頁另開第三階** —— 顯微根管與兒牙都要，這一科免。

### 四、九個組合裡有三個是反向的

「填色 L\* − 字 L\*」，正數才和系統一致（外科 +8.2、兒牙 +12.5）：

| | T1 | T2 | T3 |
| --- | --- | --- | --- |
| **F1** | +2.1 | **−4.9 反向** | **−12.1 反向** |
| **F2** | +10.1 | +3.0 | **−4.1 反向** |
| **F3** | +20.0 | +13.0 | +5.8 |

反向的意思是 **chip 按下去反而變深**，和全站行為相反。提案頁那三格畫虛線框。

### 五、ΔE：六個值對現役色

| 值 | 對矯正 | 對一般牙周 | 對根管 | 對全部 | 對外科填色 | 對外科深階 |
| --- | --- | --- | --- | --- | --- | --- |
| 現行 `#252d4a` | 21.3 | 30.9 | 42.9 | 29.1 | 36.6 | 30.4 |
| F1 `#3a456d` | **18.0** | 29.7 | 42.8 | 28.0 | 27.2 | 22.5 |
| F2 `#4a5789` | 21.6 | 33.0 | 46.0 | 31.5 | 22.1 | 19.6 |
| F3 `#5e6fac` | 28.7 | 38.7 | 50.9 | 37.7 | **21.0** | 22.2 |
| T1 `#364067` | 18.7 | 30.2 | 43.3 | 28.5 | 28.5 | 23.3 |
| T2 `#44507e` | 19.7 | 31.3 | 44.5 | 29.8 | 23.7 | 20.2 |
| T3 `#526197` | 23.8 | 34.7 | 47.4 | 33.5 | 21.1 | 20.1 |

**越往上走，離齒顎矯正越遠、離口腔外科越近** —— 兩端都有牆，但兩端都還有空間。
最小的是 F1 對矯正的 18.0，六個值全部安全（出局線是 6.1）。

### 六、定案：F2 配 T1（使用者選的）

```css
[data-spec="prosth"] { --accent: #4a5789; --accent-deep: #364067; }
```

我建議的是 F3·T1（填色頂著上限、和外科同高度）。**使用者選了 F2 —— 而且他是對的。**
F3 的填色比字亮 20.0 階，chip 按下去會跳一大截；F2 收在 **+10.1**，
和外科的 +8.2 同一個量級，整個色盤的深淺行為就一致了。
「比照紫色」真正該比照的是那個**落差**，不是「也頂著上限」。

| | 填色 F2 | 深階 T1 | 差 |
| --- | --- | --- | --- |
| L\* | 38.0 | 27.9 | **10.1** |
| C\* | 30.8 | 25.3 | 5.5 |
| Lab h | 287.2° | 287.3° | 0.1° |

**每一個實際用到的地方都驗過：**

| 用在哪 | 用哪一階 | 對比 | 門檻 |
| --- | --- | --- | --- |
| 醫師藥丸（實心＋白字 11.5px） | `#4a5789` | 6.96 | 4.5 ✓ |
| chip 選取（實心＋白字 13px） | `#4a5789` | 6.96 | 4.5 ✓ |
| 文章內頁 實心藥丸／按鈕（白字） | `#4a5789` | 6.96 | 4.5 ✓ |
| chip 未選（字與框，底是卡） | `#364067` | 9.16 | 4.5 ✓ |
| 文章卡標籤（字與框，底是卡） | `#364067` | 9.16 | 4.5 ✓ |
| 文章內頁 連結／`h3`（底是紙） | `#364067` | 7.95 | 4.5 ✓ |
| 小節短棒／`.callout` 左條（底是紙） | `#4a5789` | 5.50 | 3.0 ✓ |

和外科一樣**沒有任何例外**，也不必替文章內頁另開第三階（顯微根管與兒牙都要）。
餘裕還很大 —— 深階對紙 7.95，離 4.5 有得是空間，之後想再提亮不必重來。

### 七、改了哪兩行

```css
/* assets/style.css：原本 --accent 與 --accent-deep 都是 var(--tile) */
/* index.html：      原本只有 --accent: var(--tile)，深階由預設那行帶成同一個值 */
[data-spec="prosth"] { --accent: #4a5789; --accent-deep: #364067; }
```

⚠ `[data-spec]` 的預設值那一行**必須排在 `[data-spec="prosth"]` 前面**，
否則深階字會被蓋回 `var(--accent)`（六之二節最後那一條，兒牙踩過）。
兩個檔案都已經是這個順序。

---

## 六之十一、顯微根管改紅色系（2026-08-09，五案提案中）

使用者：「顯微根管我要用紅色系，從配色資料庫抓紅色，做五個版本。」

> **定案（2026-08-09）：套色 `#ae4f4d`、白底的字 `#89202d`。**
> 已套進 `index.html` 與 `assets/style.css`。**深階是實物原值** ——
> 開幕茶會門口那籃花的實測色（第三節花藝）。
> `--moss` 本身留著（它是第一節色票裡的 Logo 色），只是不再有科別指向它，
> 文章頁原本那階 `#596951` 也一併拿掉了。
> 走到這裡花了六輪，推導從下面第一小節開始，定案的驗算在最後一節。

最新的提案頁是 **`preview/spec-endo-red-all/`**（十一案 × 兩條切換條，`?l=6&f=11`）。
`preview/spec-endo-red/`（正規化的五案）與 `preview/spec-endo-red-raw/`（三個原值）
留著看推論過程。

### 一、要放掉的兩件事（先攤開，不是反對）

1. **顯微根管現在拿的是 Logo 自己的色。** 苔綠 `#5D6D55` ——
   六之八節寫的「七科裡最強的一個對應」。改成紅之後，**科別色裡不再有 Logo 色**。
2. **「紅在品牌裡的暗示性太強」是使用者自己定過的原則**（六之二節，兒牙離開磚紅的理由）。

反過來說有兩件支持它的：暖紅那一整段從外科改紫之後就空著；
而且**紅在這一科比在外科站得住** —— 六之九節否決外科的血色是因為「病人付出的是風險不是痛」，
但根管真正在保留的就是牙齒裡活著、有血流的那一部分，紅在這裡不是傷口，是還活著的證據。

### 二、資料庫裡的紅，統一亮度之後只剩三個

| 來源 | 色 | L\* | C\* | Lab h |
| --- | --- | --- | --- | --- |
| 官方色票・磚紅 | `#AF4C52` | 45.5 | 44.2 | 22 |
| 門口紅花籃 | `#89202d` | 30.7 | 48.4 | 23 |
| 紅花籃（另一張） | `#731923` | 25.1 | 43.0 | 24 |
| 繡球 | `#6c1226` | 22.9 | 41.5 | 18 |
| 磚紅長椅 | `#7c3721` | 32.1 | 39.7 | 44 |
| 色票・Deep Taupe | `#7D5A58` | 41.8 | 15.7 | 26 |
| 單椅布料 | `#8e7577` | 51.6 | 10.4 | 14 |

> **⚠ 發現一：磚紅、紅花籃、繡球其實是同一個色的三個亮度。**
> 照片裡差很多，但那個差幾乎全部是亮度（45.5 / 30.7 / 22.9）——
> 色相 18~23、彩度 41~48 幾乎一樣。統一拉到 L\* 46 之後彼此只差
> **ΔE 4.0~8.0**，藥丸尺寸下是同一顆。
>
> **⚠ 發現二：低彩度那一端被「看診時間」卡佔住。**
> Deep Taupe 與單椅布料是同一族（彼此 ΔE 6.1），而 **Taupe 還在
> `.info-card[data-hue="taupe"]` 上**。拿它那個彩度做科別色 ΔE 只有 **4.4**，
> 比讓官方灰紫硬性出局的 6.1 還近。

**所以可用範圍是被這兩件事夾出來的。**

### 三、五案（填色 L\* 46、深階 L\* 38，只動色相與彩度）

亮度差 7.9，和外科的 +8.2、贋復的 +10.1 同一量級。深階彩度照色域比例縮
（贋復那一輪定下的規則）。**五案對比度幾乎一模一樣**：白字 5.18~5.21、
深階對卡 6.31~6.36、對紙 5.48~5.52、填色對紙 4.09~4.11 —— 沒有一案是被對比度逼出來的。

| | 填色 | 深階 | C\* | Lab h | 依據 |
| --- | --- | --- | --- | --- | --- |
| **① 磚紅・收斂** | `#9b5b5d` | `#804b4c` | 28 | 21 | 彩度收到剛好脫離 Taupe |
| **② 磚紅・官方**（錨點） | `#b04d53` | `#923f44` | 44.2 | 22 | **色票磚紅的色相彩度原值**，只搬亮度 |
| **③ 酒紅** | `#b14b62` | `#923d50` | 44 | 10 | 往繡球（h 18）那一端 |
| **④ 燒磚** | `#a85541` | `#8b4635` | 42 | 40 | 往磚紅長椅（h 44）那一端 |
| **⑤ 磚紅・滿彩** | `#bf3f4d` | `#9f333e` | 56 | 22 | 貼著兒牙 57.6，**超出資料庫實測** |

### 四、辨識度：只有兩個鄰居要盯

| 案 | 填色對 Taupe | 深階對 Taupe | 填色對兒牙深階 |
| --- | --- | --- | --- |
| ① | 13.5 | **9.5** | 45.4 |
| ② | 28.8 | 23.0 | 44.7 |
| ③ | 29.9 | 24.0 | 53.9 |
| ④ | 27.6 | 21.8 | **31.3** |
| ⑤ | 40.6 | 33.6 | 48.4 |

冷色三科距離都在 31 以上，不是問題。**① 靠 Taupe、④ 靠兒牙**，其餘三案兩邊都寬。
① 的深階對「全部」`#5f5d5c` 也只有 23.5 —— 低彩度那一端兩面受擠。
五案彼此最接近的一對是 ②③（9.3）。

### 五、目前的建議

**② 磚紅・官方。** 它不是調出來的 —— 是色票裡唯一的紅，而且花籃與繡球在同一亮度下
都落在它 4.4 / 4.0 以內，**三個實測紅一起指向同一個答案**。
這個形狀出現過兩次（兒牙的含羞草、外科的官方灰紫），兩次都證明那個錨是對的起點。
**④ 燒磚建議避開** —— 暖色段只剩兒牙，再放一個 h 40 的紅會互相干擾。

### 五之二、第二輪：使用者說「這五個都是磚紅」（2026-08-09）

**他是對的，而且那是第三小節那一步造成的。**

> 五案把亮度全部鎖在 L\* 46。**但磚紅、紅花籃、繡球本來的差別幾乎全部就在亮度上**
> （45.5 / 30.7 / 22.9）—— 色相 18~23、彩度 41~48 三個本來就很接近。
> 亮度一拉平，剩下的差只有 ΔE 4~8，當然看起來是同一顆磚紅。
>
> **這是第二小節「發現一」的另一面，我當時只用它來刪選項，沒發現它同時也預告了
> 五案會長得一樣。** 原值彼此是 ΔE 11.4~23.0，是分得開的。

原值版提案頁 `preview/spec-endo-red-raw/`：三個紅一個數字都沒動，
各做**白底套字**（字與整圈框都是該色）與**整圈套色、字反白**兩態，
卡 `#f4f4f5` 與紙 `#e2e5e6` 兩種底都看。

| 紅 | L\* | C\* | h | 套字・卡 | 套字・紙 | 反白 |
| --- | --- | --- | --- | --- | --- | --- |
| 官方磚紅 `#AF4C52` | 45.5 | 44.2 | 22 | 4.80 ✓ | **4.17 ✗** | 5.28 ✓ |
| 門口紅花籃 `#89202d` | 30.7 | 48.4 | 23 | 8.28 ✓ | 7.18 ✓ | 9.10 ✓ |
| 繡球 `#6c1226` | 22.9 | 41.5 | 18 | 10.90 ✓ | 9.47 ✓ | 11.99 ✓ |
| （現行苔綠 `#5D6D55`） | 44.1 | 15.9 | 134 | 5.05 ✓ | **4.38 ✗** | 5.55 ✓ |

**原值這一輪完全沒有撞色問題**：三個紅對現役六科最近的也有 ΔE 41.9（磚紅對外科），
對「看診時間」卡的 Taupe 是 28.9 / 34.6 / 32.2。
上一輪那些逼近的數字，全部是被壓到 L\* 46 才產生的。

**兩件要知道的：**

1. **磚紅在紙上當字 4.17 不夠**（style.css 本來就寫著「只能待在卡上」）。
   同六之六節那一招可解：白底的字改 **`#a7484e`**（L\* 43.4、對紙 4.51、對卡 5.19，
   離原值 ΔE 2.6 肉眼看不出），整圈套色仍用原值。**花籃與繡球不需要這一步。**
2. **花籃 30.7、繡球 22.9 是「很深」的那一區。** 贋復同一天才從 19.1 提亮到 38.0，
   理由就是太深、掉出家族帶（36~48）。繡球比當時的贋復只亮 3.8 階。

**目前建議：門口紅花籃 `#89202d`。** 兩種底、兩個狀態全部直接過，不必另開字階；
彩度 48.4 三個裡最高，整圈套色最有存在感；亮度 30.7 落在另外兩個中間。而且它是實物。

### 五之三、第三輪：七案併在同一排（2026-08-09）

使用者要「兩案併起來、做成兩態、跟其他科別標籤放在一起、一鍵切換」。
提案頁 **`preview/spec-endo-red-all/`** —— ①~⑤ 是正規化的五案（兩階），
⑥⑦ 是原值的紅花籃與繡球（單一值），七案都排進真正的七科標籤列裡，
白底套字與整圈套色兩態、卡與紙兩種底，外加一整段文章內頁模擬。

> **切換條是兩條、各自獨立**（2026-08-09 第四輪追加）：
> 上排管**白底字套色**那一階，下排管**整圈套色**那一階，網址可帶 `?l=2&f=6`。
> 兩排同號＝那一案原本設計的樣子；**兩排不同號＝當場自訂一組兩階**
> （例如白底字 ② `#923f44` 配整圈套色 ⑥ `#89202d`，資料庫裡沒有但完全成立）。
> 讀數第三行是「套色 L\* − 套字 L\*」，49 個組合的值全部由 CSS 產生；
> **負數會標「⚠ 反向」**（套色比套字暗，chip 按下去反而變深）——
> 套色選 ⑥⑦、白底字選 ①~⑤ 的組合全部是反向的，⑦ 配 ①~⑤ 差到 −15。

> **官方磚紅的原值不必另外佔一格 —— ② 就是它。**
> ② `#b04d53` 對色票原值 `#AF4C52` 只差 **ΔE 0.38**（色相彩度一樣，亮度差 0.4）。
> **唯一的差別是原值在紙上當字只有 4.17，② 壓下去的那 0.4 階正好把它救回來。**

| 案 | 填色 | 套字 | 反白 | 套字・卡 | 套字・紙 | 對 Taupe |
| --- | --- | --- | --- | --- | --- | --- |
| ① 磚紅・收斂 | `#9b5b5d` | `#804b4c` | 5.19 | 6.31 | 5.48 | **9.5** |
| ② 磚紅・官方 | `#b04d53` | `#923f44` | 5.21 | 6.33 | 5.50 | 23.0 |
| ③ 酒紅 | `#b14b62` | `#923d50` | 5.18 | 6.36 | 5.52 | 24.0 |
| ④ 燒磚 | `#a85541` | `#8b4635` | 5.20 | 6.32 | 5.48 | 21.8 |
| ⑤ 磚紅・滿彩 | `#bf3f4d` | `#9f333e` | 5.21 | 6.33 | 5.50 | 33.6 |
| **⑥ 紅花籃・原值** | `#89202d` | `#89202d` | 9.10 | 8.28 | 7.18 | 34.6 |
| **⑦ 繡球・原值** | `#6c1226` | `#6c1226` | 11.99 | 10.90 | 9.47 | 32.2 |

**⑥⑦ 在兩態的行為和 ①~⑤ 不一樣，切換時要分開看：**
整圈套色時 ①~⑤ 都是 L\* 46，⑥ 是 30.7、⑦ 是 22.9 —— **⑥⑦ 按下去明顯比其他六科重**
（其餘六科填色落在 L\* 36~60）；白底套字則反過來，①~⑤ 的套字階 L\* 38 和其他科同一帶，
**⑦ 的線框在一排裡幾乎讀成黑色**。兩態都偏重是 ⑥⑦ 的代價，換來的是不必拆兩階、而且是實物原值。

**真正的分岔是 ② 對 ⑥**：要一顆和其他六科等重的紅，還是一顆比較沉、辨識度更強的紅。
其餘五案都是繞著這兩個的變化。

#### 第五輪：使用者挑 `l6 + f4`，於是多了 ⑧（2026-08-09）

他選**白底字 ⑥ `#89202d`、套色 ④ `#a85541`**。分析下來：

| | 值 | L\* | Lab h | 家族裡的位置 |
| --- | --- | --- | --- | --- |
| 白底字 ⑥ | `#89202d` | 30.7 | 23.4 | 在「全部」31.4 與贋復 27.9 之間 —— **不是離群值** |
| 套色 ④ | `#a85541` | 45.9 | 40.1 | 在外科 48.0 與一般 40.3 之間 —— **也不是** |

**兩階各自都站得住，對比也全過**（白字 5.20、短棒對紙 4.10、字對卡 8.28／對紙 7.18）。
**問題只在兩階之間的關係：色相差 16.7°。**

| 科 | ΔL\* | Δ色相 |
| --- | --- | --- |
| 兒童牙科 | 12.3 | 0.3 |
| 口腔外科 | 8.2 | 0.5 |
| 贋復假牙 | 10.1 | −0.2 |
| **`l6 + f4`** | **15.2** | **16.7** |

現役三組都在 1° 以內 —— 六之九節那句「**它不是兩個色，是同一個色的兩個高度**」在這一組
不成立：白底字是 h 23 的胭脂紅，套色是 h 40 的陶土橘紅，兩階彼此 ΔE 21.0。

> **而且這兩階會同框，不是輪流出現。** 文章內頁的科別藥丸／短棒／重點框左條／實心按鈕
> 吃套色階，同一頁的 `h3`／連結／線框按鈕吃白底字階；首頁按下顯微根管 chip（套色）之後，
> 篩出來的文章卡標籤是白底字。**16.7° 沒有地方可以躲。**

**⑧ 燒磚・深** 就是為了這一組加的。使用者選 ⑥ 是要**深**、選 ④ 是要**暖**，
兩者同時成立需要一個「暖而且深」的紅 —— **資料庫裡剛好有：磚紅長椅 `#7c3721`**
（L\* 32.1・C\* 39.7・h 44.3，第三節家具布料實測）。

| ⑧ | 值 | L\* | C\* | h | 對比 |
| --- | --- | --- | --- | --- | --- |
| 白底字 | `#7c3721`（實測原值） | 32.1 | 39.7 | 44.3 | 對卡 7.88／對紙 6.84 |
| 套色 | `#b05032`（同色相拉到 L\* 45.9） | 45.9 | 51.7 | 44.1 | 白字 5.20／對紙 4.11 |

**⑧ 兩階色相差 0.2°**，和外科、贋復同一個等級。
`l8 + f4` 是使用者要的那組，色相差降到 4.2°；`l8 + f8` 是 0.2°。
提案頁預設已改成 `l8 + f4`。

⚠ **⑧ 的套色是七案裡最靠近兒牙的**（對兒牙深階 27.1、填色 30.6，④ 是 31.3／34.4）——
h 44 比 ④ 的 h 40 又往兒牙的 h 72 走了一點。暖色段只剩兒牙，這是 ⑧ 要付的代價。

#### 第六輪：⑨⑩⑪ —— ④ 與 ⑥ 之間補三階（2026-08-09）

使用者看過 ⑧ 之後仍然選 `l6 + f4`，要「在 ④、⑥ 之間再做三個」。
所以這三案**不動亮度、只走色相**：

- **亮度鎖在他選的那組結構**：套色 L\* 45.9（④ 自己的）、白底字 L\* 30.7（⑥ 自己的），
  ΔL\* 一律 **+15.2**，和他現在看到的完全一樣。
- **色相把 16.7° 均分成四段**：40.1 → 35.9 → 31.8 → 27.6 → 23.4。
- **彩度兩端都錨在實測值**：套色 42.2（④ 燒磚）→ 44.2（官方磚紅）；
  白底字 39.7（磚紅長椅）→ 48.4（紅花籃）。
- **每一案自己的兩階同色相**（差 0°），這正是 `l6+f4` 那 16.7° 想解掉的東西。

| | 套色 | 白底字 | 白字 | 對卡 | 對紙 | 白底字對 Taupe |
| --- | --- | --- | --- | --- | --- | --- |
| **⑨ 近④**（h 35.9） | `#aa5345` | `#7e2e24` | 5.21 | 8.29 | 7.20 | 28.8 |
| **⑩ 中點**（h 31.8） | `#ad5149` | `#822a26` | 5.19 | 8.28 | 7.18 | 30.8 |
| **⑪ 近⑥**（h 27.6） | `#ae4f4d` | `#852529` | 5.22 | 8.33 | 7.23 | 32.6 |

> **⚠ 套色那一階變化很小，這是物理不是失誤。**
> 相鄰兩案的套色只差 **ΔE 3.2**，④ 到 ⑥ 整段也只有 12.7 ——
> 套色的彩度全程守在 42~44，17° 的色相在那個彩度上就是走不遠。
> **白底字那一階差得比較開**（相鄰 3.9、整段 15.5），它的彩度從 39.7 走到 48.4。
> **看差別要看白底字那一排。**

⑧ 和 ⑨⑩⑪ 是同一個問題的兩條路：**⑧ 是「往 ④ 那端走到底並收乾淨」**
（拿實測的磚紅長椅當白底字，兩階差 0.2°，但整組更暖、更靠兒牙）；
**⑨⑩⑪ 是「在中間找一點」**。

> **⚠ 切著比對的頁面，讀數不能讓版面跳。**
> 使用者回報「點 ⑧~⑪ 和 ④ 之間切換，頁面會跳，不好比較」。
> 原因是讀數那三行的長度隨案子變（有的帶「★ 你挑的那一組」這種尾巴、
> 案名從「④ 燒磚」到「⑨ ④⑥之間・近④」差很多），窄螢幕下在一行與兩行之間
> 來回，整頁跟著位移最多 43px。**在比對用的頁面上，這比顏色本身還致命 ——
> 眼睛要重新找位置，就比不出兩個顏色的差。**
>
> 解法是把可變長度的那幾行**先把兩行的高度佔住**：
> `.readout span{display:block}` ＋ `@media (max-width:35rem){min-height:calc(2*1.85em)}`，
> 帶案名的兩個標籤同理（`@media (max-width:44rem)`，2×1.5em）。
> 量過 560px 以上讀數不會換行、704px 以上案名不會換行，所以斷點取在那裡。
>
> **附帶的坑：`span` 改成 `display:block` 之後，原本分隔用的 `<br>` 會各自
> 再佔一整行**，讀數從 77px 變成 120px。改 block 就要把 `<br>` 拿掉。
>
> 360~1280px 全部量過，同一頁 121 個組合的文件高度一致（360px 有 1px 的
> 次像素差，看不出來）。

> **這一輪順手踩到的版型坑：** 切換條是 `left:50%` ＋ `translateX(-50%)` 的
> **收縮包覆 flex 容器**，裡面放會換行的列時，寬度會塌成很窄的一條
> （十一顆擠成 188px，四行高 216px）。**要明寫寬度**
> （`width:min(calc(100vw - 1.4rem), 30rem)`），換行的那幾顆才用得到整條空間。
> 順便把每一顆按鈕改成「色塊本身＋白字編號」，高度從 216px 收到 112px ——
> 十一案的白字對比最低 5.17，編號讀得到。

**同一頁還帶一段完整的文章內頁模擬**（2026-08-09 追加），吃同一個切換條。
規格逐條照 `assets/style.css` 抄的，**哪個元件吃哪一階一格都沒改**（六之六節那張表）：
科別藥丸、小節標題前的 3px 短棒、重點框左條、實心按鈕 → 填色階；
內文連結、`h3`、線框按鈕 → 套字階；`h2` 本身與頁尾電話不變。

> **文章內頁是七案差最多的地方** —— 它同時用到兩階，而且底是紙（最嚴的一關）。
> ⑥⑦ 的 `h3` 與連結仍然明顯是紅的（有底線幫忙），但比內文墨色 `#2a2c27`
> 只淺一點，**整篇的重量會往下壓一階**。⑦ 對紙 9.47，那是「太安全」的另一面。
> ② 那一階（對紙 5.50）和其餘六科的字階同一帶，讀起來是「另一科的文章」
> 而不是「另一種頁面」。

### 六、定案：白底字 ⑥ 配套色 ⑪（使用者選的）

```css
[data-spec="endo"] { --accent: #ae4f4d; --accent-deep: #89202d; }
```

| | 值 | L\* | C\* | Lab h | 來源 |
| --- | --- | --- | --- | --- | --- |
| 套色 | `#ae4f4d` | 45.8 | 43.6 | 27.5 | ⑪，④⑥ 之間的第三階 |
| 白底的字 | `#89202d` | 30.7 | 48.4 | 23.4 | **開幕紅花籃實測原值**（第三節） |

**兩階色相差 4.1°** —— 他先挑的 `l6 + f4` 是 16.7°，補了三階之後收掉了。
ΔC\* −4.8、**ΔL\* 15.1**。

**七個實際用到的地方都驗過：**

| 用在哪 | 用哪一階 | 對比 | 門檻 |
| --- | --- | --- | --- |
| 醫師藥丸（實心＋白字 11.5px） | `#ae4f4d` | 5.22 | 4.5 ✓ |
| chip 選取（實心＋白字 13px） | `#ae4f4d` | 5.22 | 4.5 ✓ |
| 文章內頁 實心藥丸／按鈕（白字） | `#ae4f4d` | 5.22 | 4.5 ✓ |
| chip 未選（字與框，底是卡） | `#89202d` | 8.28 | 4.5 ✓ |
| 文章卡標籤（字與框，底是卡） | `#89202d` | 8.28 | 4.5 ✓ |
| 文章內頁 連結／`h3`（底是紙） | `#89202d` | 7.18 | 4.5 ✓ |
| 小節短棒／`.callout` 左條（底是紙） | `#ae4f4d` | 4.12 | 3.0 ✓ |

ΔE 也安全：套色最近的是「看診時間」卡 Taupe 28.2、白底字是 34.6。

> ⚠ **這一組的 ΔL\* 15.1 是色盤裡最大的**（兒牙 12.3、贋復 10.1、外科 8.2）。
> 方向是對的（套色比字亮，chip 按下去會亮），但落差比其他科明顯。上線後看一眼。

### 七、跟著改的四件事（已完成三件）

1. ✅ **`--moss` 被釋出且沒有別人接手**。變數留著（它是第一節色票裡的 Logo 色），
   但**科別色裡不再有 Logo 色** —— 這是這次改動最實際的損失，不是疏漏。

   > **對照頁：`preview/logo-green-compare/`（2026-08-09，`?g=a|b|c`）。**
   > 把釋出的苔綠放回「一般牙科・定期檢查／牙周治療／創辦醫師」現在用的
   > 診療椅綠旁邊（那三個是同一個值 `--chair #4f6361`），加上家色深綠松當第三個參考點。
   >
   > | | 值 | L\* | C\* | Lab h | 白字 | 對卡 | 對紙 |
   > | --- | --- | --- | --- | --- | --- | --- | --- |
   > | 診療椅（現行） | `#4f6361` | 40.3 | 8.1 | 190 | 6.38 | 5.81 | 5.04 |
   > | 苔綠（Logo） | `#5D6D55` | 44.1 | 15.9 | 134 | 5.55 | 5.05 | **4.38 ✗** |
   > | └ 壓深的字階 | `#596951` | 42.5 | 16.0 | 134 | 5.89 | 5.36 | 4.65 |
   > | 深綠松（家色） | `#214D48` | 29.7 | 16.7 | 186 | 9.46 | 8.61 | 7.47 |
   >
   > **苔綠對診療椅只有 ΔE 13.7**，但那不是「幾乎一樣」——**兩者色相差 56°**
   > （134 偏黃綠、190 偏藍綠），只是彩度都很低（15.9 與 8.1）所以距離拉不開。
   > **低彩度的差別要靠並排才看得出來**，這就是那一頁存在的理由。
   >
   > 三件判斷依據：①苔綠比診療椅亮 3.8 階、彩度多一倍，換過去那一科會稍微浮一點；
   > ②苔綠要兩階（紙上 4.38 不夠），診療椅一個值管到底 —— 但 `#596951` 剛從
   > 顯微根管拿掉，是現成的；③**診療椅對家色深綠松 ΔH 只有 4°，它是家色的實物版**
   > （第一節），苔綠離家色 52°，是另一支。換過去等於讓「一般牙科」離家色遠一點、
   > 離 Logo 近一點。**兩邊都站得住，還沒定。**
2. ✅ **文章頁那階 `#596951` 已拿掉**（六之六節）。苔綠原值對紙只有 4.38 才要另開一階；
   新的深階對紙 7.18，**七科裡需要第三階的就只剩兒牙**。
3. ⚠ 兩階的預設值那一行必須排在 `[data-spec="endo"]` 前面（六之二節最後）。兩個檔案都已經是。
4. ⬜ **六之八的三組要重排，這次是變差不是變好。** 綠色段只剩一般牙周 174，
   「保留與延續」那一組的兩個成員拆散了；紅要接進暖色段（兒牙 35 旁邊）還是自己成一段，
   還沒重寫。**這一件還沒做。**

---

## 七、量測方法（要加新照片時照這個做）

**這一節只適用於 Windows 本機。** 手機／雲端 session 跑在 Linux 容器裡，
沒有 PowerShell、沒有 .NET Drawing，也沒有使用者的照片資料夾 —— 在那邊請用上面的表格，
不要嘗試執行這支腳本。

`tools/palette-measure.ps1` 用 .NET 解 JPEG、跑 k-means 抽主色，
可以整張測，也可以只測指定區域。

```powershell
powershell -ExecutionPolicy Bypass -File tools\palette-measure.ps1 -Manifest jobs.txt
```

`jobs.txt` **必須存成 UTF-8**，一行一個工作：

```
標籤|C:\path\to\photo.jpg|6
標籤|C:\path\to\photo.jpg|3|0.20,0.60,0.45,0.90
```

第三欄是要抽幾個主色（k），第四欄是裁切範圍 `x0,y0,x1,y1`（0~1 的比例，可省略）。
輸出每個色的 HEX、佔畫面比例、彩度 S、明度 L、色相 H、R−B。

要定裁切範圍時，先產生帶座標格線的縮圖比較好抓：腳本加 `-Grid` 參數即可。

對比度不要用眼睛判斷，算出來。WCAG AA：內文 4.5:1，大字（≥18.66px 粗體或 ≥24px）3:1。

---

## 八、還沒做的

- 桌機版排法
- 把 `preview/home-mobile-n3/` 的固定頁首與科別色併進正式站的 `index.html`
- 亮／暗自動切換（跟系統設定走）
- HERO 疊字在淺色版下的第二種寫法
