# 芳仁牙醫診所 — 配色規範

這份是網站配色的**唯一依據**。所有數字都是從診所自己的照片量出來的，不是目測調的。
要改網站配色、或做新的頁面，先讀這份，不要另外憑感覺挑色。

- 最後更新：2026-08-09
- 取色來源：桌面 `02-Final`（開幕茶會約 290 張手機紀錄、專業攝影原檔、`Logo顏色.pptx`）、
  桌面 `104_logo.pptx`（**九顆的完整版，見第一節**）、
  桌面 `看板_20260226`（診所現用的 LINE 圖文選單）
- 量測方法：見本文最後一節
- 對應的預覽頁：**`/history/home-mobile-n3.html`（定案的手機首頁：底色 ＋ 固定頁首 ＋ 科別色）**、
  `/history/canvas.html`（配色數據頁）、`/history/night-calibrated.html`（全暗版）
- 提案頁定案後就刪了（2026-08-12 起），推導文字留在 `https://fangren.net/history/<名稱>.html`，沒有密碼

> **在手機／雲端 session（claude.ai/code）工作時**：那邊跑在 Linux 容器裡，
> **看不到使用者電腦上的照片，也不能跑 `tools/palette-measure.ps1`**（PowerShell + .NET，
> 只在 Windows 本機能跑）。所以在雲端請**直接把這份文件的表格當成事實來源**，
> 不要說「我讀不到資料夾」然後改用目測配色 —— 需要的數字都已經量好寫在下面了。
> 只有要測「新的照片」時才需要回到 Windows 本機。

---

## 一、最重要的一件事：診所已經有官方色票

`Logo顏色.pptx` 裡就是這八個色。**家具、磁磚、開幕花藝都是照它挑的**，
所以網站不需要再發明配色，直接用這套。

| 名稱 | HEX | 色相 H | 彩度 S | 明度 L |
| --- | --- | --- | --- | --- |
| Deep Taupe | `#7D5A58` | 3 | 17.4% | 41.8% |
| 焦糖褐 | `#9B735E` | 21 | 24.5% | 48.8% |
| 磚紅 | `#AF4C52` | 356 | 39.4% | 49.2% |
| 灰紫 | `#5F5D66` | 253 | 4.6% | 38.2% |
| 藍灰 | `#3C596B` | 203 | 28.1% | 32.7% |
| 深綠松 | `#214D48` | 173 | 40.0% | 21.6% |
| 苔綠（Logo） | `#5D6D55` | 100 | 12.4% | 38.0% |
| 灰綠 | `#A1A398` | 71 | 5.6% | 61.8% |

### ⚠ 其實是九顆 —— `104_logo.pptx` 才是完整的那份（2026-08-09 更正）

上面那八顆出自 `02-Final\Logo顏色.pptx`。**桌面根目錄還有一份 `104_logo.pptx`，
裡面是九顆**（使用者截圖給的那張九宮格就是它），而且色值和上表略有出入：

| 名稱 | `104_logo.pptx` | L\* | C\* | Lab h | 上表的色票 | ΔE |
| --- | --- | --- | --- | --- | --- | --- |
| Deep Taupe | `#805751` | 41.2 | 19.2 | 32.6 | `#7D5A58` | 4.1 |
| 焦糖褐 | `#9E7253` | 51.8 | 27.5 | 60.7 | `#9B735E` | 6.3 |
| 磚紅 | `#944449` | 39.4 | 36.6 | 21.2 | `#AF4C52` | 9.7 |
| 灰紫 | `#5E5A61` | 38.8 | 4.6 | 311.8 | `#5F5D66` | 1.8 |
| 藍灰 | `#315568` | 34.3 | 16.5 | 244.7 | `#3C596B` | 2.8 |
| 深綠松 | `#0B4B46` | 28.2 | 20.5 | 187.2 | `#214D48` | 4.1 |
| 苔綠 | `#5A6E4F` | 44.1 | 20.3 | 132.8 | `#5D6D55` | 4.5 |
| 稻草灰（＝灰綠） | `#A8A998` | 68.7 | 9.3 | 111.6 | `#A1A398` | 3.9 |
| **米色（第九顆）** | **`#D3CBC5`** | **82.2** | **4.3** | **66.6** | **上表沒有** | — |

**九顆裡的米色不是新色** —— 它等於第三節「識別物」的**帆布袋實測 `#d1c7bc`，ΔE 只有 3.1**。

站上目前用的仍是上面那八顆的值（`#3C596B` 等），**不要為了對齊 `104_logo.pptx` 去改站上的色**：
差最大的磚紅 9.7 也還在同一顆的範圍內，而每一顆站上的值都各自經過對比度調校。
這張表的用處是「品牌到底有哪幾顆」，不是新的取值來源。

### 色票 vs 診所實物（實測色相差）

| 診所裡量到的 | 實測色 | 對應色票 | ΔH |
| --- | --- | --- | --- |
| 診療椅 | `#4f6361` | 深綠松 | **1** |
| 候診沙發布料 | `#4e718b` | 藍灰 | 3 |
| 開幕花・深紅 | `#89202d` | 磚紅 | 3 |
| 圓凳與木件 | `#8d5e3a` | 焦糖褐 | 5 |
| 磚紅長椅 | `#7c3721` | 焦糖褐 | 6 |
| 單椅布料 | `#8e7577` | Deep Taupe | 8 |
| 茶室深藍磁磚 | `#1d273d` | 藍灰 | 18 |

七項裡六項落在 8° 以內。這不是巧合，是同一套色票執行出來的。

---

## 二、材質實測（灰階的依據）

### 清水模

| 取樣 | 色 | 彩度 | R−B |
| --- | --- | --- | --- |
| 外牆特寫（白天） | `#d6d4d3` | 3.5% | +3 |
| 外牆特寫（白天） | `#cbcac9` | 1.9% | +2 |
| 外牆特寫（白天） | `#c7c5c5` | 1.8% | +2 |
| 外牆特寫（白天） | `#97999c` | 2.5% | −5 |
| 夜・受光面 | `#83786d` | 9.2% | +22 |
| 夜・暗部 | `#1a110c` | 36.8% | +14 |

**關鍵結論：白天的清水模幾乎是純中性（彩度 1.8~5.1%），夜裡才變暖**
（受光面 9.2%、暗部 36.8%）——因為夜裡照到它的只有診所自己的燈。
所以暗色版的灰階規則是「**暗處最暖、往亮處收回中性**」，不是整條均勻微暖。

### 其他硬裝

| 取樣 | 色 | 彩度 | 備註 |
| --- | --- | --- | --- |
| 診療室白牆 | `#a09a94` | 5.9% | R−B +12 |
| 候診深灰地磚 | `#45413c` / `#36312b` | 7.0% / 11.3% | 微暖 |
| 候診深灰地磚（另一處） | `#4c4f50` | 2.6% | R−B −4 |
| 木地板 | `#a18262` / `#b28450` | 25.1% / 38.9% | H=30 / 32 |
| 木櫃格 | `#956d4a` | 33.6% | H=28 |
| 茶室深藍磁磚 | `#0c111c` / `#1d273d` / `#252d4a` | 33~40% | H=221~227 |

### 診所自己的數位介面（LINE 圖文選單，2026-08-06 加測）

素材：桌面 `看板_20260226`（AlleyPin 做的 LINE RichMenu，含實機截圖 `IMG_1452/1453/1454.PNG`）。
這是診所**現在正在對外使用**的介面，所以它的底色比任何推導都有份量。

| 取樣 | 色 | 彩度 | R−B | 備註 |
| --- | --- | --- | --- | --- |
| 選單底（四邊都量過，一致） | `#e8e8e8` | **0.0%** | **0** | 明度 91.0%，**完全中性** |
| 按鈕白 | `#f8f8f8` | 0.0% | 0 | 明度 97.3% |
| 按鈕陰影／紋理暗部 | `#d3d2d2` | 1.1% | +1 | 明度 82.5% |
| 圖示・深綠松 | `#234b48` | 36.4% | −37 | 官方色票 `#214D48`，ΔH=2 |

**兩個結論：**

1. **診所對外的介面用的是純中性灰，不是暖灰、也不是米。** 這是網站內文底色改用中性灰的直接依據。
2. **底對按鈕白的階差是 1.154:1。** 灰要有東西比才讀得出來——見第四節 B。

版面概念也可以參考：中性灰底＋細線條插圖當紋理＋六格圓形按鈕，
彩色只出現在圖示上，而且每個圖示各一色（深綠松、苔綠、焦糖褐、藍灰、Taupe、磚紅），
正好是第六節第 2、3 條的實作。
使用者不要的是上面那個**錯落配置的細線 logo**，覺得在網站上會顯得凌亂。

### 夜的光

| 取樣 | 色 | 彩度 | 色相 |
| --- | --- | --- | --- |
| 夜空（藍時刻） | `#373b42` | 9.1% | 218 |
| 窗光 | `#ac743d` | 47.6% | 30 |
| 入口燈 | `#ccb393` | 35.8% | 34 |

**木頭與燈光是同一條色相軸**：木地板 H=30／另一間 H=32／窗光 H=30／入口燈 H=34。
夜空 H=218 是整棟建築唯一天然的冷色，而沙發布料的藍灰 H=206 是最接近它的顏色。

---

## 三、軟裝與花藝

### 家具布料（使用者說的「調色出現在布料上」）

| 位置 | 色 | 彩度 | 色相 |
| --- | --- | --- | --- |
| 候診沙發 | `#4e718b` / `#38607b` | 28.1% / 37.4% | 206 / 204 |
| 長椅 | `#2d4455` | 30.8% | 206 |
| 單椅 | `#8e7577` / `#967e7c` | 10.0% / 11.0% | 355 / 5 |
| 磚紅長椅 | `#7c3721` | 58.0% | 15 |
| 診療椅（醫療設備，非家具） | `#4f6361` | 11.2% | 174 |

### 花藝（開幕茶會）

| 照片 | 花色 | 花佔畫面 | 其餘 |
| --- | --- | --- | --- |
| 門口紅花籃 | `#89202d` | 13.0% | 灰、黑、白 |
| 門口紅花籃（另一張） | `#731923` | 11.2% | 灰、木、白 |
| 含羞草 | `#9d8313` | 13.7% | 灰牆、深色 |
| 繡球 | `#6c1226` | 21.5% | 灰、深綠 |
| 茶席櫻花 | `#a0a8b0` | 1.1% | 深藍磁磚 78% |

枝葉本身：`#2f3929`（H=98）、亮處 `#447c56`（H=139）。

**花本身只佔畫面 1~22%。** 這是「花很顯眼但不吵鬧」的量化依據，也是下面版面規則的來源。

### 識別物

名片深綠 `#313734`、名片深藍 `#1d2327`、帆布袋 `#d1c7bc` / `#aba297`。

---

## 四、網站用的兩套色

### A. 暗夜（只用在 HERO 與它下面那條窄帶）

```css
--night:      #191614;  /* 底。彩度 11.1%，R−B +5 —— 深灰地磚／夜間暗部 */
--night-2:    #211e1b;  /* 面 */
--night-line: #38342f;  /* 分隔線 —— 清水模暗部 */
--night-text: #e3e1e0;  /* 主文字。彩度 5.1%，R−B +3 —— 清水模亮部 */
--night-soft: #aaa49e;  /* 次要 —— 診療室白牆 */
--night-faint:#969089;  /* 更淡 */
--wood:       #a18262;  /* 地板實測色。全站只有一條 3px 線用它 */
```

暗底上的文字對比（全部通過 AA，最低 4.70:1）：
主文字 13.82:1／次要 7.30:1／更淡 5.70:1。

### B. 室內（內文區）— **2026-08-06 定案：N3 藍灰調**

```css
--paper:    #e2e5e6;   /* H=203・L=89.4%・R−B −4 */
--card:     #f4f4f5;   /* 往純白推，紙才讀得出是灰 */
--rule:     #cdd0d2;
--ink:      #2a2c27;   /* 主文字 11.15:1 */
--ink-soft: #5c5f57;   /* 次要文字 5.14:1 */
```

色偏取官方色票的**藍灰 `#3C596B`（H=203）**——候診沙發、長椅、夜空同一條軸，
是這棟建築唯一天然的冷色。明度定在 89%，比原本那組低 3 個百分點。

**兩件事比色相更重要，改配色時不要弄丟：**

1. **卡要往純白推，紙才讀得出是灰。** 灰是比出來的，不是調出來的。
   舊的紙 `#ecede7` 對卡 `#f6f7f2` 只差 **1.094:1**，兩塊都偏暖，
   眼睛沒有參照物就一起讀成白——使用者的原話是「單獨看不覺得灰」。
   診所自己的 LINE 圖文選單，底 `#e8e8e8` 對按鈕白 `#f8f8f8` 差 **1.154:1**。
   現在這組是 1.152:1，照著看板走。
2. **不要用暖色偏。** 診所現用的對外介面實測是**純中性**（見第二節「診所自己的數位介面」）。

#### 被換掉的那三個（留著當紀錄，不要再用）

2026-08-04 的「底色三選一」都在明度 92% 那一帶，太淺，灰讀不出來：

| | HEX | R−B | 使用者的判斷 |
| --- | --- | --- | --- |
| ① 清水模灰 | `#edeceb` | +2 | 「最中性的灰很不清楚」 |
| ② 帆布米 | `#f1ece5` | +12 | 沒選 |
| ③ 品牌灰綠（原主案） | `#ecede7` | +5 | 「單獨看不覺得灰」，比較起來偏米 |

同期做過但沒選上的深灰提案：G1 灰紫 `#dedce5`（L88、R−B −7，使用者說「有點紫紫的」）、
G2 灰綠 `#e4e6db`（L88、R−B +9）。**使用者明確表示不要帶紫的感覺。**
中性灰另外兩案：N1 看板灰 `#e8e8e8`、N2 清水模灰 `#dedede`（都是純中性，R−B 0）。
對應的預覽頁 `/history/home-mobile-g1.html` `-g2` `-n1` `-n2` `-n3`，定案的是 `-n3`。

---

## 五、哪個色能當什麼（實測，不要繞過）

### 淺底上（紙 `#ecede7` / 卡 `#f6f7f2`），官方色票原值直接用

| 色票 | 紙 | 卡 | 能當 |
| --- | --- | --- | --- |
| 深綠松 `#214D48` | 8.03:1 | 8.79:1 | 內文、標籤、標題 |
| 藍灰 `#3C596B` | 6.29:1 | 6.88:1 | 內文、標籤、標題 |
| 灰紫 `#5F5D66` | 5.50:1 | 6.01:1 | 內文、標籤、標題 |
| Deep Taupe `#7D5A58` | 5.13:1 | 5.61:1 | 內文、標籤、標題 |
| 苔綠 `#5D6D55` | 4.71:1 | 5.15:1 | 內文、標籤、標題 |
| 磚紅 `#AF4C52` | 4.48:1 | 4.90:1 | **只能大字**（標語、標題） |
| 焦糖褐 `#9B735E` | 3.56:1 | 3.90:1 | **只能大字** |
| 灰綠 `#A1A398` | 2.17:1 | 2.38:1 | **不能當文字**，只作底或色塊 |

### 暗底上（`#191614`），色相彩度不動、只提明度

原值幾乎都撐不住（深綠松只有 1.90:1、藍灰 2.43:1），必須提亮：

| 色票 | 原值 | 原值對比 | 暗底建議值 | 對比 |
| --- | --- | --- | --- | --- |
| Deep Taupe | `#7D5A58` | 2.98:1 | `#a58280` | 5.23:1 |
| 焦糖褐 | `#9B735E` | 4.29:1 | `#a7826f` | 5.20:1 |
| 磚紅 | `#AF4C52` | 3.41:1 | `#c37479` | 5.23:1 |
| 灰紫 | `#5F5D66` | 2.78:1 | `#8b8893` | 5.18:1 |
| 藍灰 | `#3C596B` | 2.43:1 | `#658fa9` | 5.19:1 |
| 深綠松 | `#214D48` | 1.90:1 | `#439b91` | 5.43:1 |
| 苔綠 | `#5D6D55` | 3.24:1 | `#7c9071` | 5.22:1 |
| 灰綠 | `#A1A398` | **7.04:1** | 不用改 | 7.04:1 |

提亮時**色相與彩度一度都不要動**，只加明度。原值留給亮底、名片、招牌繼續用。

---

## 六、版面規則（從花藝的邏輯來的）

1. **綠是枝條，可以重複出現。** 深綠松與苔綠可以在同一頁出現多次
   （例如「現在看診中」的燈點、分類標籤）。
2. **花一種只出現一次。** 磚紅、Deep Taupe、藍灰各自只用在一個地方，彼此隔開，
   中間全是紙的顏色。實測花藝照片，花只佔畫面 1~22%——版面也照這個比例走。
3. **任何一個手機屏幕上，最多只同時看到兩個彩色。**
4. **暗夜只留在 HERO**，不延伸到內文區。~~兩者的分界是那條 3px 木色線 `#a18262`。~~

   **2026-08-06：木線已依使用者要求拿掉，不要加回去。** 他的理由有兩層：
   在網頁上瀏覽時那條線很突兀，而且**拿地板當診所識別色本身就站不住**——
   識別色應該另外選。現在暗夜窄帶直接接到內文底，中間什麼都沒有。
   `--wood: #a18262` 這個實測值留在本文件第二節當材質紀錄，但**不再有任何元件用它**。

   **識別色尚未決定**，決定之前不要臨時挑一個色來補這個位置。
5. **數字不上色**（1983／9／6 用墨色）。那不是需要被強調的東西。
6. 暗色版裡，彩色只出現在三個地方：文章分類標籤、「現在看診中」的燈點、電話號碼。
   暗色系最容易失敗的地方就是到處都在發光。
7. **不要把 `#A1A398` 拿來當文字**（見上一節）。

---

## 六之二、醫師介紹與診所資訊的科別色（2026-08-07 定案）

對應的預覽頁：`/history/home-mobile-n3.html`。這一節推翻了第六節第 3 條，理由寫在最後。

### 藥丸是實心底＋純白字，不是淡底＋色票字

先做過「淡底＋色票原值的字」，走不遠。**藥丸字要維持色票原值，底就壓不下去**：

| 底的濃度（色票混進純白） | 還撐得住 AA 的上限 |
| --- | --- |
| 深綠松 `#214D48`／藍灰 `#3C596B` | 22% |
| Deep Taupe `#7D5A58` | 18% |
| 苔綠 `#5D6D55` | 15% |
| 磚紅 `#AF4C52` | **10%**（12% 就掉到 4.48:1） |

磚紅幾乎動不了，那就是這條路的天花板。**反白反而最寬鬆**，而且六個色的視覺重量終於一致
（淡底那組深綠松 8.31:1 對磚紅 4.75:1，差了快一倍）。

### 六個科別色

藥丸字級 11.5px，AA 要 4.5:1。下表是**純白字疊在該色上**的實測值：

| 科別 | 色 | 來源 | H | 白字對比 |
| --- | --- | --- | --- | --- |
| 一般＋牙周植牙 | `#4f6361` | 診療椅實測（第一節） | 174 | 6.38:1 |
| 顯微根管 | `#5D6D55` | 色票・苔綠（Logo） | 100 | 5.55:1 |
| 齒顎矯正 | `#3C596B` | 色票・藍灰（候診沙發） | 203 | 7.41:1 |
| 口腔外科 | `#7D5A58` | 色票・Deep Taupe（單椅布料） | 3 | 6.04:1 |
| 贋復假牙 | `#252d4a` | 茶室深藍磁磚實測（第二節） | 227 | 13.52:1 |
| 兒童牙科 | `#8d5e3a` | 圓凳與木件實測（第一節） | 28 | 5.53:1 |

診所資訊維持深綠松當家色（它同時是全站連結色），看診時間那張卡是 Deep Taupe。

**三個用實測色而不是色票原值，各有理由，不要「順手改回色票」：**

- `#4f6361` 診療椅 —— 使用者指定給創辦醫師與李侑津醫師。對深綠松 ΔH=1，同一個色的實物版。
- `#8d5e3a` 圓凳與木件 —— 兒牙原本是磚紅，**使用者說紅在品牌裡的暗示性太強，先不要用**。
  換到暖色這一支之後，色票的焦糖褐 `#9B735E` 太淺、白字只有 4.19:1 撐不住，
  所以取同一族的實物實測值（對焦糖褐 ΔH=5）。
- `#252d4a` 茶室深藍磁磚 —— 口外與贋復是不同科，使用者要求顏色不要重複。
  贋復（全口重建）拿整個色盤裡最深的那一個。

**進不來的三個：** 焦糖褐 `#9B735E` 白字 4.19:1；灰綠 `#A1A398` 白字 2.56:1；
灰紫 `#5F5D66` 白字 6.47:1 過得了，但**使用者不要帶紫的感覺**（見第四節 B）。

### 兒牙改黃色系（2026-08-07 提案中，三案未定）

使用者要「從配色資料庫裡找黃色系」給兒牙。先講一個事實：

> **這份文件裡真正量到的黃只有一個 —— 開幕茶會的含羞草 `#9d8313`**
> （第三節花藝，H=49・S=78%・L=35%）。其餘暖色全落在 H=15~34
> （磚紅長椅 15、焦糖褐 21、圓凳 28、窗光 30、入口燈 34），那是褐與琥珀，不是黃。

所以三案**只有色相是變數**，不是各挑一個：

| | HEX | H | S | 白字 | 卡 `#f4f4f5` 上當字 |
| --- | --- | --- | --- | --- | --- |
| 現在（褐） | `#8d5e3a` | 26 | 42% | 5.53:1 | 5.03:1 |
| 1 琥珀黃 | `#966113` | 36 | 78% | 5.23:1 | 4.76:1 |
| 2 中間 | `#8d6811` | 42 | 78% | 5.10:1 | 4.64:1 |
| 3 含羞草 | `#7f6b10` | 49 | 78% | 5.23:1 | 4.76:1 |

規則：**色相**從含羞草的 49 往現有暖色軸靠，取 49 / 42 / 36 三階；
**彩度**固定 78%（含羞草自己的值，一度不動）；
**明度**取「白字仍過 AA 的**最亮**值」—— 黃色越亮越像黃，所以這裡不是壓深，是頂著上限取最亮。

> **黃色在這個系統裡有物理上限，這不是調得不好。**
> 兒牙這一色要同時當**實心藥丸的底（白字）**和**卡上的線性標籤（它自己當字）**，兩邊夾殺。
> 含羞草原色白字只有 **3.69:1**、卡上 **3.36:1**，兩個都不夠；壓到過關的位置它就會讀成
> 芥末／橄欖。**要真正明亮的黃，得先讓掉「實心白字」或「線性標籤過 AA」其中一條。**

#### 第二輪：使用者說「不是黃，是咖啡色、拿鐵色、泰奶色」

他是對的，而且**這不是調偏了 —— 黃色沒有深色版本**。把黃壓暗，它必然變成
橄欖、芥末、拿鐵。當時做過「亮黃 `#e6c637` ＋ **墨字**」（墨字 8.40:1），
但使用者判斷：

> 「為了迎合這個黃，標籤裡的字變成黑色，這樣很突兀，**還是要維持白字**。」

**所以白字是硬條件。** 那一版連同 `--accent-text` / `--on-accent` 兩個衍生變數
一起還原，不要再走「換字色換亮度」這條路。

#### 第三輪：不換字色，改用 Lab 把彩度推到天花板

**前兩輪用 HSL 挑，那是錯的工具 —— HSL 的「彩度」在黃色區會騙人。**
`#7f6b10` 的 HSL S 是 78%，但感知彩度 C\* 只有 48。
改用 **Lab／LCh 全域掃描**：取同時滿足「白字 ≥4.5:1」與
「拿它當卡 `#f4f4f5` 上的字 ≥4.5:1」、且色相落在黃帶裡**彩度最高**的顏色。

第三輪取 Lab h 80~90°，使用者判斷：

> 「有點混到髒髒的綠，變成有點嘔吐物腹瀉感……方向不對，往橙一點點的方向走。」

成因是**黃帶靠綠的那一側在低亮度下一定會泛綠** —— 而白字把亮度鎖在 L\* 47，
那一側躲不掉。三案（`#926800` / `#8a6c00` / `#866e00`）作廢。

#### 第四輪：往橙走（作廢）

第三輪泛綠的成因是**黃帶靠綠的那一側在低亮度下一定會泛綠** —— 白字把亮度鎖在
L\* 47，那一側躲不掉。往橙走同時解決泛綠、又能提高彩度天花板：

```
h 82° C* 54.9    h 70° C* 59.3    h 58° C* 68.4
h 78° C* 56.1    h 66° C* 61.9    h 50° C* 75.3
```

做了 `#986600`（h76.5・C\*56）／`#a36000`（h68.7・C\*59）／`#aa5c00`（h64.3・C\*62）三案。
使用者：「比之前好很多，但還是有點牛奶糖的感覺，比較帶開咖啡感。」

> **這一輪最有價值的發現：** `#aa5c00` 的色相 64.3° 和原本的 `#8d5e3a`（61.7°）
> 幾乎相同，但彩度是 61.9 對 32.1。**所以「拿鐵感」的成因一直是彩度不足，不是色相** ——
> 前四輪一直在挪色相，方向本身就錯了。

#### 第五輪：明黃（做過，方法保留）

**明黃 ＋ 白字在物理上不存在**（L\* 75 的黃配白字只有 1.94:1）。
保留白字，天花板就是 L\* 50 —— **第一到第四輪全部卡在同一個地方**。
使用者第三輪反對的是「字變成黑色很突兀」，那和「非白字」不是同一件事，
所以第五輪起字改用**同一個黃的深階**（色相只差 6~10°），是同族的深淺配。

#### 第六輪：錨在使用者挑的四個傳統色（目前的三案）

使用者從《天然染色經》的黃色卡裡挑出四個貼近他心中的黃。
（以下 hex 是從他的截圖**目視估**的，色相方向可信、絕對值不精確 ——
拿到原始色票的 hex 要回來重算。）

| | HEX | L\* | C\* | Lab h | 白字 |
| --- | --- | --- | --- | --- | --- |
| 神黃 | `#e9a72c` | 73.0 | 69.2 | 78.1° | 2.09:1 |
| 雄黃 | `#e28a1c` | 65.3 | 70.9 | 68.2° | 2.66:1 |
| 萬壽菊黃（他自註偏橘） | `#ef8b21` | 67.2 | 73.6 | 64.8° | 2.50:1 |
| 鼬黃 | `#d9a73f` | 71.4 | 59.0 | 82.0° | 2.20:1 |

> **四個全落在 L\* 65~73。那正是它們看起來像「他要的黃」的原因，
> 也正是白字全部失守的原因 —— 兩件事是同一件事。**

> 而且：把它們壓到白字能用的高度，得到的就是第四輪那組。
> 雄黃色相 68.2° 壓下來是 `#a75e00`，和第四輪的 `#a36000`（h68.7・C\*59.3）
> **幾乎是同一個色**，也就是他說「牛奶糖」的那一個。

所以三案改成三種取捨，各自錨在他挑的一個黃上：

| | 底 | L\* | C\* | 字 | 字疊在底上 |
| --- | --- | --- | --- | --- | --- |
| **A 守白字**（錨雄黃） | `#a75e00` | 47.3 | 60.7 | **白字** | 4.95:1 |
| **B 神黃** | `#f6a600` | 74.1 | 80.3 | `#6c3b00` | 4.58:1 |
| **C 鼬黃** | `#e8a600` | 72.5 | 77.3 | `#643b00` | 4.56:1 |

A 一條規則都沒破，但就是牛奶糖那一帶。
B、C 是真的明黃，代價是**字讓掉白色**，而且兒牙的 chip 與文章標籤**必須改成填色**
（亮黃當字讀不出來：`#f6a600` 疊在卡上只有 1.4:1）。

> 實作加了 `--accent-deep`（當字用的階）與 `--on-fill`（填色上的字）兩個衍生變數，
> **預設完全等於原本的行為**，所以其他五科一個像素都不會變。

#### 第七輪：A 與 B 之間補三階

使用者：「我覺得我想要的在 A B 之間。」在 Lab 空間把 A→B 等距切三份
（L\* 47.3→74.1、C\* 60.7→80.3、h 66.3→75.9），各取該亮度‧色相下的最高彩度。

**白字的臨界點量出來了：`L* 48` 還過（4.72:1），`L* 50` 就破（4.43:1）。**
A 本身站在懸崖邊（L\* 47.3），所以**中間三階全部都要深色字**，沒有例外。

| | 底 | L\* | C\* | 白字 | 深階字 | 字疊在底上 |
| --- | --- | --- | --- | --- | --- | --- |
| **A** | `#a75e00` | 47.3 | 60.7 | **4.95:1 ✓** | — | — |
| 1 | `#c06f00` | 54.6 | 67.2 | 3.81 ✗ | `#2a1700` | 4.52:1 |
| 2 | `#d38000` | 61.0 | 71.7 | 3.06 ✗ | `#482300` | 4.52:1 |
| 3 | `#e89300` | 68.0 | 76.9 | 2.44 ✗ | `#5e2f00` | 4.57:1 |
| **B** | `#f6a600` | 74.1 | 80.3 | 2.02 ✗ | `#6c3b00` | 4.58:1 |

> **一個反直覺的地方：越靠近 A，深階字反而越黑。**
> 第 1 階的字 `#2a1700` 亮度只有 L\* 10，幾乎就是黑色 —— 正是使用者先前嫌棄的那種。
> 越往 B 走，底越亮，字反而可以是暖棕（`#5e2f00` → `#6c3b00`），讀起來才像同一族的深淺配。
> **所以中間三階是「兩頭不到岸」：不夠亮到像明黃，字又被迫接近黑。**
> 真正的兩個穩定點是 **A（守白字）** 與 **B（明黃＋暖棕字）**。

除 A 之外，卡上對比都是 1.4~3.5，全部不夠，所以兒牙的 chip 與文章標籤一律改填色。

#### 第八輪：五案全部換白字（只為了看樣子）

使用者：「這 5 個全部換成白字。」照做，但要記得**這不是可上線的狀態**——
除 A（4.95:1）之外四案的白字全部掉出 AA 小字的 4.5:1。
這一輪的價值是確認了：**填色上的字用白色時，這一族的亮度天花板就是 A。**

#### 第九輪：範圍收到 A~2，中間補兩階（目前的五案）

使用者：「感覺在 A 1 2 之間，保留 A 1 2 再加 2 個進來，共 5 個。」
第七輪的 3（`#e89300`）與 B（`#f6a600`）整組淘汰——**明黃那一頭出局了**。
新的兩階取 A→1、1→2 在 Lab 空間的中點（L\* 與色相都內插），
再取該亮度‧色相下的最高彩度，和保留的三個是同一條線上長出來的。

| | 底 | L\* | C\* | h | 白字疊在底上 |
| --- | --- | --- | --- | --- | --- |
| **A** | `#a75e00` | 47.3 | 60.7 | 66.3° | **4.95:1 ✓** |
| A+ | `#b46600` | 51.0 | 64.0 | 66.8° | 4.34 ✗（差 0.16） |
| 1 | `#c06f00` | 54.6 | 67.2 | 67.3° | 3.81 ✗ |
| 1+ | `#ca7700` | 57.8 | 69.5 | 68.5° | 3.41 ✗ |
| 2 | `#d38000` | 61.0 | 71.7 | 69.7° | 3.06 ✗（壓在大字門檻上） |

> **第七輪的深階字（`#2a1700` 那組）全部作廢。** 那組是「和底同亮度差」推出來的，
> 越靠近 A 就越黑，正是使用者嫌棄的方向。第九輪改成**整族共用一個線框色**
> `#a4600f`（對卡 `#f4f4f5` 4.52:1）——因為這一族拿來當**字**的可讀天花板
> 本來就是 L\* 47，也就是 A 自己的亮度，再亮就掉出 4.5:1。
>
> 結論很乾淨：**五階的差別只在「按下去」的填色，沒按下去的線框態五案長得一模一樣。**

#### chip 的兩態回到標準合約（2026-08-07）

第八輪為了讓亮黃看得見，把兒牙的 chip 強制成「永遠填色」。使用者要求做回去：
**沒點的時候白底，點下去才套色**——也就是回到和其他四科完全相同的元件合約：

| 狀態 | 底 | 字 | 框 |
| --- | --- | --- | --- |
| 未選 | `var(--card)` | `var(--accent-deep)` | `var(--accent-deep)` |
| 選取 | `var(--accent)` | `var(--on-fill)` | `var(--accent)` |

**科別色不該有自己的互動規則。** 兒牙一旦例外，「選取」這個狀態在這一排 chip 裡
就不再是同一件事——使用者一眼就會發現，這也正是他要求做回去的原因。

#### 第十輪：留下 1，另加 HBR 那張圖 04 的沙金（2026-08-07）

使用者從第九輪的五階裡留下 **1（`#c06f00`）**，另外指了一張《哈佛商業評論》IG 圖，
要那張圖裡 04 那一格的金色。

> **色值是目視估的，不是量的。** 那張截圖只在對話裡，沒有落到容器的檔案系統，
> 沒辦法取樣。`#c8a464` 是照沙金／古銅金那一族估出來的。要精準就得拿到檔案或 hex。

| | 底 | L\* | C\* | h | 白字 | 深階字 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `#c06f00` | 54.6 | 67.2 | 67.3° | 3.81 ✗ | `#a4600f` |
| 04 沙金 | `#c8a464` | 69.3 | 38.4 | 82.3° | 2.35 ✗ | `#8f6a15`（對卡 4.51:1） |

**這個金和前九輪的黃不是同一種東西**，兩個數字就分得出來：
彩度只有 38（那條梯子是 61~72），亮度卻高到 69。**很淡但很亮** ——
這正是它讀起來像金屬金、而不像果汁黃的原因。

> 一個容易誤判的地方：第三輪被否決的「拿铁色／泰奶色」是 C\* 32，和這個 38 很接近。
> 差別只在亮度（那組 L\* 50 上下，這個 69）。
> **低彩度在暗處變成奶茶，在亮處才變成金 —— 是同一條路的兩端。**

白字 2.35:1 是照片裡的做法，但照片裡那是超大級數的裝飾數字；
搬到 13px 的 chip 上會糊掉。真要用，填色上的字得換成 `#8f6a15`。

#### 第十一輪：1 與沙金之間補兩階

在 Lab 空間取 1→04 沙金的三等分點。

| | 底 | L\* | C\* | h | 白字 | 深階字 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `#c06f00` | 54.6 | 67.2 | 67.3° | 3.81 ✗ | `#a4600f` |
| 1a | `#c28229` | 59.5 | 57.6 | 72.3° | 3.22 ✗ | `#9e6301` |
| 1b | `#c59347` | 64.4 | 48.0 | 77.3° | 2.75 ✗ | `#93681b` |
| 04 沙金 | `#c8a464` | 69.3 | 38.4 | 82.3° | 2.35 ✗ | `#8f6a15` |

> **這一段和第九輪那條梯子在比不一樣的東西。**
> 第九輪只在動亮度，彩度是各亮度的上限、被動跟著跑；
> 這一段是**彩度自己在掉**（67 → 58 → 48 → 38），色相同時往橙金那頭走 67°→82°。
> 四個底的亮度只差 15，彩度卻差了將近一倍 ——
> **這一排真正在比的是「要多飽和」，不是「要多亮」。**

深階字仍各自收在 L\* 47（這一族當字的天花板），彩度跟著各自的底走，
所以四案的線框態這次**不完全相同**：1 的框最艷、沙金的框最收。差別很小，但不是零。

### 定案：1a `#c28229`（2026-08-07）

十一輪之後定下來。**兩個提案頁都已經套上去了**（`/history/home-desktop-band.html`
與 `/history/home-mobile-n3.html`），切換條連同 `data-kids` 一起刪掉。

| 用途 | 值 | 對比 |
| --- | --- | --- |
| 底（實心藥丸、按下去的 chip） | `#c28229` | 白字 **3.22:1** ⚠ |
| 字與框（線性標籤、未選的 chip） | `#9e6301` | 對卡 `#f4f4f5` 4.51:1 ✓ |

為什麼是它：最後一輪比的不是亮度而是**彩度**（1 的 67.2 → 沙金的 38.4），
1a 落在 57.6 —— 還是黃，但已經開始有金屬感，不會讀成果汁。

> ### 白字 3.22:1 —— **已結案，維持白字（2026-08-08）**
>
> 填色上的白字是 3.22:1，不到 AA 小字的 4.5:1。
> 2026-08-08 把兩條路各做成實際的樣子給使用者看過（並排的藥丸、
> 陽光下的模擬、縮小／模糊／灰階三種情境、七科的對比長條），
> 他的回覆是「**維持白字 不用動**」。
>
> 所以這是一個**看過證據之後做的取捨**，不是待辦事項。
> **不要再提，也不要「順手改善」掉。** 兩條備案留在這裡只是紀錄：
>
> | | 做法 | 代價 |
> | --- | --- | --- |
> | 甲 | 填色上的字換 `#3c2400`（同色相、對底 4.51:1） | 顏色不動，只有字變深 |
> | 乙 | 兒牙的實心藥丸改成線框 | 兒牙就和其他五科長得不一樣 |
>
> 線框那一側（`#9e6301`）本來就是合規的，不用動。

### 兒牙為什麼需要三個變數，其他五科不用

其餘五科的 `--accent` 一個值就能同時當「字」和「底」，因為它們夠深。
兒牙的黃不行：**當底要亮才像黃，當字要深才讀得到**，同一個值做不到兩件事。
所以拆成 `--accent`／`--accent-deep`／`--on-fill`，預設全部等於原本的行為。

> **⚠ 這三個變數的預設值那一行必須排在科別色前面。**
> `[data-spec]` 和 `[data-spec="kids"]` 權重相同，排在後面會把兒牙的
> `--accent-deep` 蓋回 `var(--accent)`，黃字就糊在卡上 —— 這個坑已經踩過一次。


### 這一節為什麼推翻第六節第 3 條

第 3 條說「一個手機屏幕上最多只同時看到兩個彩色」。一科一色做不到——七個科別、
九張卡，捲動時一定會同時看到三四個。使用者在 2026-08-07 明確要求「同一種科別顏色一樣」、
「不同科顏色不要重複」，所以**第 3 條在醫師介紹這一節不適用**。

守住的是另一條、也是更根本的那條：**面積**。彩色只出現在藥丸這種小塊上，
卡底 `#f4f4f5` 一動不動（卡一被染色就會往紙靠，第四節 B 第 1 條就毀了），
加總起來仍在花藝實測的 1~22% 之內。

### 文章內頁的科別藥丸是實心的（2026-08-08）

上面那張表說「文章標籤」走線性，指的是**首頁文章卡**上的那顆 —— 一次看到六張卡，
六顆實心會太花。**文章內頁的那顆是實心底＋純白字**，跟醫師藥丸同一種份量：

- 它在內頁的角色不是「站上有哪些科」，而是「你現在讀的是這一科」—— 跟醫師藥丸的
  「這位醫師是這一科」同一層，不是分類清單。
- 藥丸自己帶 `data-spec`，所以底與字直接吃 `--accent` / `--on-fill` 那組變數，
  顏色不寫死在 `.post-tag` 裡。

> **2026-08-08 晚上這一顆的顏色跟著改了兩件事**（新版型合併進 main 的時候）：
>
> 1. 兒牙從圓凳褐 `#8d5e3a` 換成定案的黃 `#c28229`（十一輪，見上面）。
>    白字在它上面是 3.22:1，那是**已結案、看過對照後決定維持**的取捨。
> 2. `.post-head` 的底從純白 `#fff` 變成 N3 的卡色 `#f4f4f5`。
>    其他五科的白字仍是 5.55~13.52:1，一個都沒掉出 AA。
>
> 這一顆之外，**整頁的重點色也跟著科別走了**（連結、h3、重點框、按鈕），
> 規則寫在第六之六節 —— 那一節的字階和這一顆的填色是兩回事，不要互相搬。

圓角同樣照 0.384 的比例算：框高 23.04px（字 12px、行高 1.6、上下內距各 1.92）→ **9px**。
`rem` 認的是 `html` 的 16px 不是 `body` 的 17px，算框高的時候不要用錯基準。

同一次把內頁標題上方那一行併成一條：**首頁 › 科別藥丸 · 發布日 · 瀏覽數**。
原本是麵包屑在標題上、「發布・最後更新・瀏覽數」在標題下。**「最後更新」在內頁拿掉了**
（首頁卡片上還在），麵包屑的「文章 › 科別」兩層也收成藥丸一層。

### 順帶定案的兩件事

- **卡的左邊條收掉了。** 曾經有一條 3px 的 accent 色邊條，藥丸改實心之後它只是描邊，
  已經回到普通的 1px `--rule`。
- **四個小節的副標全部拿掉**（「依最後更新時間排序…」「看診團隊與各自的專科」
  「雲林縣斗六市・在地經營超過四十年」）。第一句在解釋機器怎麼排，
  COPY.md 第三節早就用同一個理由否決過「一篇一篇寫在這裡」；第二句是標題的同義重述；
  第三句同一頁已經講了三次。**頁尾那句留著。**

### 科別標記的形狀與填色（2026-08-07 定案）

科別色現在跨三處共用同一個 `data-spec`：**主題與科別的 chips、文章卡的分類標籤、醫師藥丸**。
所以「兒童牙科」在三個地方一定是同一個色。篩選功能讀的也是同一個屬性，
顏色與篩選不可能對不起來。

**填色分兩層，對應的是瀏覽者心裡的層級，不是隨機的：**

| | 填色 | 為什麼 |
| --- | --- | --- |
| 醫師藥丸 | **實心底＋純白字** | 這是在**認人**（這位醫師是哪一科），份量最重 |
| 主題與科別・文章標籤・診所資訊 | **線條與文字套色，底不套色** | 這是**分類**（有哪些科、這篇屬於哪一科） |

線性版的底維持 `--card #f4f4f5` 不是透明 —— 主題與科別的 chips 躺在紙 `#e2e5e6` 上，
底若透明，兒牙 `#8d5e3a` 對紙只有 **4.37:1** 掉出 AA；墊那層卡色才是 5.03。
**「底不套色」的意思是底不吃科別色，不是底完全不上色。**

**圓角要看比例，不是絕對值。** 四個標記一開始全寫 8px，主題與科別那顆明顯偏方 ——
因為它的框大得多。基準是醫師藥丸的 **半徑 ÷ 框高 = 0.384**，其餘各自照這個比例算：

| | 框高 | 圓角 | 比例 |
| --- | --- | --- | --- |
| 醫師藥丸（基準） | 20.86px | 8px | 0.384 |
| 主題與科別 chip | 31.00px | 12px | 0.387 |
| 文章標籤 | 22.33px | 8.5px | 0.381 |
| 診所資訊標籤 | 23.66px | 9px | 0.380 |
| 專長／資歷／學歷 標籤 | 22.13px | 6px | 0.271 ← 刻意比標記方一階 |

專長標籤是紙色底 `#e2e5e6` ＋ 次要墨色 `#5c5f57`（5.14:1）。
它是 grid 項目，**`justify-self: start` 與 `align-self: start` 兩個都要**：
少了前者底會被欄寬撐滿，少了後者底會被整列的高度拉長（grid 預設是 stretch）。

### 標誌本身（2026-08-07）

標誌路徑取自 `20241205 芳仁牙醫LOGO_中英標準字.ai`，是一個 path、兩條子路徑
（嘴 ＋ 用 `fill-rule="evenodd"` 挖出來的那顆牙）。**嘴一個節點都沒有改過。**

牙那條子路徑依自己的中心放大，因為原比例的洞在小尺寸會被抗鋸齒吃掉。
放大會連帶把洞下方的肉吃薄，所以要往上移補回來 ——
**目標是讓「洞下方的肉」回到原標誌的 3.43（viewBox 單位，逐像素掃描量出來的）**：

| | 放大 | 放大後洞下方的肉 | 上移 | 結果 |
| --- | --- | --- | --- | --- |
| banner（顯示 31.88 × 15.70px） | 1.3× | 2.93 | 0.50 | **3.43** |
| favicon（要撐到 16px） | 1.6× | 2.38 | 1.05 | **3.43** |
| 頁尾（顯示 25.3 × 12.5px） | 再 1.15× | — | 再 1.4 | **不是 3.43，見下** |

#### ⚠ 頁尾那一顆沒有遵守 3.43（2026-08-09 定案，B 案）

頁尾的診所名前面也放了標誌，只有 **12.5px 高，比 favicon 還小**，洞橫向只剩約 2px。
表格最後一行的兩個倍率是**疊在 banner 那顆上面的**（`index.html` 的頁尾直接拿 banner 的
路徑再加工），換算成原標誌就是 1.495× 與上移 1.9。

**它的肉不是 3.43，是比原本更多。** 用同一支逐像素掃描把兩顆放在同一個尺規上量
（每單位 40px、取洞底下最薄的那一欄）：

| | 最薄的肉 |
| --- | --- |
| banner（頁首那顆） | 3.13 單位 |
| **頁尾這顆** | **4.13 單位** |

多了一個單位、32%，也就是**牙比那條規則規定的位置高**。這是使用者在實際大小逐案看過
之後挑的（三個位移 × 四種放大做法），**不是從 3.43 推出來的**。規則的用意是「放大時
不要讓外觀走樣」，這一顆是刻意讓它走樣一點點。要照規則辦事的話，正確做法是
「只放大、上移到肉回到 3.13」，那大約是上移 0.4 —— 使用者看過那個級距（第一輪的 0.8）
之後選了更高的。

> 掃描腳本的量法和上面那張表不同尺規：同一顆 banner，那張表寫 3.43、這支腳本量 3.13。
> **要比較就用同一支腳本，不要把兩邊的數字混著看。**

還有一筆更正，避免後人被誤導：**牙的底部本來就有一道分叉，那是牙自己的輪廓**，
不是嘴的邊緣被切開。原始比例下洞的下方一直有 3.13 個單位的實心肉，沒有破過。

#### 頁尾標誌怎麼和底下兩行對齊

標誌進來之後，底下的釘子與話筒要和它對齊，**三行的字必須同一個起點**。做法是在左邊留一條
固定寬度的「溝」：`--gutter = 標誌寬 + 標誌和字的距離`。第一行標誌待在溝裡、「芳」從溝的
右緣起跑；二三行圖示**置中在同一條溝裡**，「雲」與「0」同樣從溝的右緣起跑。連結的 `gap`
因此收成 0，間距整個併進 `--gutter`。

好處是**標誌一改大小，溝跟著變，三行永遠一起動**。量過 375／1280 兩個寬度、首頁與文章頁，
三行起點的 x 完全相同。標誌大小是使用者在 .78／.88／.98rem 三案裡選的**最小那個**。

#### 頁尾的地址與電話改用一般牙科的套色（2026-08-09 定案）

釘子・地址・話筒・號碼四樣統一吃 **`#3f654a`**（一般牙科的**套色**，不是白底字階）。
（挑這個顏色的當天 `perio` 還和 `general` 共用這個綠；同一天稍晚牙周獨立成藍 `#4478b5`，
**頁尾不受影響** —— 它吃的是寫死的值，不是那條規則。）
**診所名與標誌不動**，維持墨色 `#2a2c27` —— 只有那兩行連結上色。

| | 色 | L\* | 對頁尾卡 `#f4f4f5` |
| --- | --- | --- | --- |
| 原本 | `--teal` `#214D48` | 29.7 | 8.61:1 |
| **定案** | **套色 `#3f654a`** | **39.3** | **6.02:1** |
| 落選 | 白底字階 `#2c5238` | 31.5 | 8.06:1 |

三階都過 AA 的 4.5:1，所以這題沒有對比度限制擋著，純粹是使用者在
`preview` 的三案並排上挑的。**注意這一顆違反第六之六節「淺底上的字用白底那一階」** ——
套色本來是給標記填滿、上面配白字用的。使用者看過三案的實際樣子之後選了比較亮的套色，
6.02:1 站得住，**不要拿那條規則來「修正」它**。

> ⚠⚠ **這個值是刻意寫死的，不要改成 `var(--accent)`。**
> 文章頁的 `<body>` 帶著該篇的科別（`data-spec="prosth"` 等），用 `--accent` 會讓頁尾
> 跟著文章變色：顯微根管的文章頁尾變紅、贋復變藍、兒牙變黃。
> 頁尾是全站共用元件，要的是**一個固定的顏色**。
> 已量過首頁與三種不同科別的文章頁，四頁的頁尾都是 `rgb(63,101,74)`。

favicon 沒有底框，寬度撐滿方框（路徑 bbox 剛好等於 viewBox，沒有多餘的邊可以修；
它自己是 2.03:1，所以高度最多只到方框的 46%，那是比例決定的）。

~~**favicon 的顏色還沒定案** —— 淺色已定 `#4f6361`（診療椅原值），深色還在挑。~~
**2026-08-08 已定案：淺色 `#4f6361`、深色 `#4f8065`，並且兩者的先後順序是刻意反過來寫的
（Safari 不看 favicon 的深色模式）—— 完整說明在第六之七節。**
過程與結論寫在 `/history/logo-favicon.html`。兩件量出來的事先記在這裡：

- **PALETTE 第五節「提亮時彩度不要動」那條，對診療椅不成立。** 那條是為深綠松（彩度 40%）寫的；
  診療椅彩度只有 11.2%，把一個快沒有顏色的色往亮處推只會愈來愈灰 —— 使用者的形容是「像漂白過」。
- **深色版不需要提那麼亮。** 淺色 `#4f6361` 亮度 Y=0.11，對深色分頁列只有 2.41 不合格；
  非文字圖形的下限 3.0 只需要 Y=0.15。一度做到 Y=0.28（對比 4.8），多花了兩倍，
  結果就是深淺兩版看起來像兩個主題色。

### ⚠ 「顏色 ＝ 篩選」這個不變量已經放掉了（2026-08-11 上線）

`index.html` 原本的註解寫著：主題與科別的對應「讀的是 HTML 上的 `data-spec`，
跟配色共用同一個屬性，**所以顏色與篩選永遠不可能對不起來**」。

**這條 2026-08-11 起不再成立**，是使用者要求的結果，不是 bug：

> 「主題與科別點下去，出現有關聯的醫師，包括專科、專長。」

醫師的專長那句話現在每個詞都包一層 `<span class="sk" data-spec="…">`，**篩選改讀那些
詞**，所以一位醫師可以出現在兩科底下。具體的例子：李柄輝醫師的卡片是**一般牙科的綠**
（`.doc[data-spec="general"]`，藥丸寫「創辦醫師」），但他的專長寫著「牙周照護」，
所以按**牙周治療**的時候他也會出現 —— 一張綠卡出現在青綠那一科底下。

**看到這種情況不要當成配色錯誤。** 換來的是關聯講得更清楚：亮起來的那顆標記就是理由。

三種份量，全部是這一節既有的兩態加上它們之間的一階，**沒有新色票**：

| 元件 | 沒篩選 | 按了某一科 |
| --- | --- | --- |
| 文章卡的主題標籤 `.card-tag` | 白底字套色 | **套色填滿**（滿色，那顆就是該篇的科別本身） |
| 醫師的專科藥丸 `.doc-role` | 套色填滿 | ＝那一科：維持填滿／≠：**退成白底字套色** |
| 醫師的專長 `.sk` | **純文字**（沒有框也沒有底） | 命中：**淡色填滿**／沒命中：維持純文字 |

淡色填滿 ＝ `color-mix(in srgb, var(--accent) 12%, var(--card))` ＋ `--accent-deep` 的字。
**12% 是量出來的**：淡底會吃掉深階字的對比，字對淡底實測

    一般 6.82・牙周 4.74・根管 7.07・兒童 4.01・矯正 7.96・贋復 10.42・口外 5.12

**只有 12% 是「除了兒牙以外全部過 AA」**（20% 牙周掉到 4.26，30% 再加口外 4.06）。
兒牙的 4.01 沿用它既有的那條 AA 例外（填色白字 3.22，2026-08-08 定案維持）。

⚠ **維護成本**：專長 → 科別的對照寫死在 HTML 屬性上（32 個詞）。日後新增或修改任何
一位醫師的專長，就要順手給那個詞一個 `data-spec`，否則它不會被任何一科篩到、也永遠
不會亮起來。完整對照表與全部推導在 `/history/doc-spec-tags.html` 的 `<head>`。

⚠ **`.doc-role` 同一天多了一行 `vertical-align: .158em`。** 在那之前它沒有
`vertical-align`，藥丸的字一直比醫師名字低 1.82px（中文小字和大字並排時瀏覽器對的是
基線、眼睛讀的是字面中線）。專長標記走的是另一條 —— **填滿的標記眼睛讀的是色塊**，
所以對的是「塊對字」而不是「字對字」，而且抬升量由 JS 現量（Blink 把 inline-block 的
塊內基線進位到整數 px，寫死會在某些 DPR 差到 1px）。細節見 CLAUDE.md 第九節的坑那一段。

---

## 六之三、電腦版：壓在照片下緣的窄帶（2026-08-07 定案）

電腦版把窄帶從「接在照片下面的另一個區塊」改成「**壓在整屏照片的下緣**」。
這一改推翻了兩件原本成立的事，兩件都要重新算。

### 1. 藍黑 H=220 的前提消失了

窄帶原本的藍黑（`#17191d` → `#282b31` → `#2d3037`，H=220・S=10%）是
「夜空 `#1b2635` × 柏油 `#221e1c` 各半、彩度 ×0.72」算出來的 —— **因為它當時
夾在夜空與紙色中間，兩邊都要接。**

疊到照片上之後它壓住的位置**只有柏油**，夜空離得很遠，取中間值的理由不存在了。
使用者也直接指出藍黑在照片上看起來突兀。

比較過的四個實色方向（三個實色案的做法是同一套：**亮度一個都不動，只轉色相**，
L\* 維持 10.2 / 17.5 / 19.6 三個停點，所以差別純粹是色調）：

| | 色相 | 三個色停 | 來源 |
| --- | --- | --- | --- |
| 藍黑（舊） | H=220 | `#17191d` `#282b31` `#2d3037` | 夜空×柏油中間值 |
| 柏油 | H=20 | `#1d1917` `#312b28` `#37302d` | 柏油實測 `#221e1c` |
| 墨色 | H=88 | `#1a1d17` `#2d3128` `#32372d` | 內文墨色 `#2a2c27` |

### 2. 定案方向：毛玻璃 ＋ 柏油漸層

模糊 `blur(4px) saturate(.92)`，整條一致。疊在上面的色**從完全沒有顏色漸層到柏油**，
而且是**兩個實測值之間**的漸層：

```
上  rgba(34,30,28, 0)      #221e1c 柏油（一般實測值，L* 12）
    rgba(34,30,28, .22)    28%
    rgba(22,20,19, .58)    62%   ← 換成 #161413
下  rgba(22,20,19, .88)    100%  #161413 照片最底那一列，L* 6.6
```

**定案就是上面這組（「標準」）。** 做過的「深一階」是同樣的色、
alpha 改成 `0 → .32 → .74 → 1`，最下緣完全不透 —— 落選的理由是
**它在最後 10% 把毛玻璃的好處還回去了**：底端收成一條實色的線，
等於又變回實色案，只是位置換到下緣。

三格間距同時定案為**「拉開」**（`.stats li { padding-inline: 4rem }`，靠內容寬度、
整組置中）。淘汰 1.9rem（三格擠在版心中央，兩側各空一大塊，像沒排完）
與「各佔版心三分之一」（那是手機 390px 排不下才用的辦法，
搬到 1160px 會拉得太開，三格之間的關係就散了）。

**`/history/home-desktop-band.html` 的三組切換條連同 `data-band`／`data-gap`／`data-kids`
已經全部刪掉**，落選的 CSS 也一併移除，只有落選的理由留在註解裡。

> **為什麼一定要換到 `#161413`：** 第一版整條都用 `#221e1c`，使用者說「看不出漸層」。
> 原因很簡單 —— 它壓住的路面本來就是 L\* 6~15，**拿一個和底下差不多深的顏色去疊，
> 當然看不出變化**。換成照片最底那一列的 `#161413`（L\* 6.6）之後往下走既加深又換色，
> 路面的標線與反光會逐漸被壓掉，「越下面越深」才看得出來。

`saturate(.92)` 是必要的：門口的暖光被模糊抹開之後整條帶子會發黃，
第一版用 `saturate(1.06)` 更把它推過去。

### 3. `backdrop-filter` 的框邊 —— 這一條很容易再踩

**`backdrop-filter` 只作用在元素自己的框裡。** 框內的東西被模糊、框外的沒有，
兩者在框的邊緣直接相接 —— **那條邊界本身就是一條可見的線，和顏色無關**，
就算遮罩的 alpha 從 0 開始也消不掉。

捲到照片下半部時，頁首、照片、窄帶會因此變成三塊上下堆疊的板。

解法：把模糊放到一個 `::before` 上，再用 `mask-image` 讓**模糊本身**淡入／淡出。
遮罩住的是濾鏡的結果，所以模糊是漸漸出現的，沒有框邊。

- 窄帶：上緣 `0% → 38%` 淡入
- 頁首：下緣 `76% → 100%` 淡出

### 4. 頁首玻璃：一層固定的薄色，深淺交給底下的東西決定

> **這一條推翻了先前所有關於「頁首玻璃終點顏色」的做法，不要改回去。**

走過的路：終點從藍灰 `#32353c` → 暖色 `#2a2523` → 在 `#2a2523`~`#3a3532` 之間做五階
→ 試過「捲進內文整條翻成卡色淺條、文字轉墨色」（太淺，淘汰）。
**全部都是在調「終點的顏色」，而那個框架本身就錯了。**

原本的規則是「顏色從幾乎透明**內插到一個不透明的終點**」，理由是怕條子在窄帶與紙色
之間切換時跟著變亮。但那個目標本身站不住 —— **玻璃本來就會跟著底下變**，
那是它該有的行為，不是要消除的缺陷。實際造成的毛病是：條子壓在照片最暗的下半部
與窄帶上時，反而因為往不透明的灰內插而**變淺**，看起來像一塊浮在夜色上的灰板。

現在的做法是：**顏色鎖死成一個，只讓濃度隨捲動變。**

```
顏色  rgb(25,22,20)   ＝ 暗夜 #191614，從頭到尾不換
濃度  alpha .06 → .76 ＝ 走的區間是「條子開始壓到窄帶」到「窄帶整條過完」
濾鏡  blur(6px) saturate(1.08)
```

**顏色固定**是關鍵：合成色因此一定跟著底下走 —— 壓在夜色上就深、壓在紙色上就淺。
**濃度不能也固定**：試過把 alpha 也鎖成 .76，結果 HERO 一捲動條子就變成一塊黑板，
玻璃感整個不見了 —— 那個通透感本來就是靠低 alpha 讓底下透過來撐出來的。

三段各自的長相（截圖實測）：

| 條子壓在哪 | alpha | 合成色 | 導覽白字 `.88` |
| --- | --- | --- | --- |
| HERO 區（天空、建築） | ≈ .06 | 幾乎不染色，只有模糊 | — |
| 窄帶通過中 | .06 → .76 | 底下本來就是夜色，越走越沉 | — |
| 窄帶／照片下半部 | .76 | `#1b1815` | 15.7:1 |
| 紙色 `#e2e5e6` | .76 | `#494846` | 8.2:1 |

**2026-08-07 定案：濃度取 `.84`，而且捲進內文之後轉成實心。**

使用者要的是「到了淺色區就不要透了，顏色維持 `.84` 的樣子；但深淺交界那一段
捲動仍然要有透的感覺，往回捲也一樣」。這兩件事不衝突 ——
**`.84` 的玻璃疊在紙色上算出來就是 `#393736`**，和實心版要用的顏色一模一樣。
所以把「玻璃 → 實心」的切換點放在**紙色已經完全補到條子底下**的那一刻，
兩邊顏色相等，切換看不見；而且它是捲動位置的函數不是時間動畫，所以來回捲都一樣。

為什麼要有實心這一段：紙色區本身是平的，透不透看起來一樣；
但再往下捲，**文章卡、縮圖、彩色 chips 會從條子底下經過** —— 玻璃會把它們糊上來，實心不會。

兩個進度值，剛好接續（`u` 開始的那一刻正是 `t` 到 1 的那一刻）：

| | 範圍 | 條子底下是什麼 | 條子的長相 |
| --- | --- | --- | --- |
| `t` | 窄帶進入 → 填滿條子 | 夜色 | 玻璃，alpha `.06 → .84` |
| `u` | 窄帶離開 → 紙色補滿 | 夜色漸換成紙色 | 玻璃漸轉實心，止於 `#393736` |

**實心色不是手寫的**，是用同一個 `A_MAX` 疊在 `--paper` 上算出來的，
所以日後改濃度，實心色會自己跟著走，兩邊永遠相等、不會脫鉤。

截圖實測整段捲動的條子中段顏色，單調遞增、沒有跳動：

```
#141110 → #16110e → #1c1917 → #1f1c1b → #242221 → #2a2827 → #373434 → #393736
   HERO 區（玻璃）          窄帶填滿      交界（玻璃轉實心）        紙色（實心）
```

先前試過的濃度：`.76` 使用者說「透感有點多」、`.92` 已回到嫌太黑的那一帶。
下限是對比度：`.70` 以下紙色區的導覽白字會逼近 4.5:1。

> **頁首的淡出只能在深色區。** 條子捲到紙色上時，那段「由深漸淡到透明」
> 在淺色底上會被讀成**一條陰影** —— 這一頁本來一個陰影都沒有（只有 HERO 那兩層文字陰影）。
> 所以淡出的起點做成 CSS 變數 `--head-fade`，由捲動的 JS 逐幀寫入：
> `76 + 24 × t³`（`t` 是窄帶填滿條子的進度）。深色區是 76%（軟邊），
> 紙色上來時收到 100%（無淡出、無陰影）。用三次方是因為
> **綁 `t` 不綁 `u`，這一點很容易搞反，理由是幾何的：**
> 淡出區和紙色**都是從條子的下緣往上長的，必然重疊** ——
> 所以只要紙色進來一點點，那段「由深漸淡到透明」就會壓在紙上變成一條陰影。
> 沒有「兩者錯開」這種解，唯一的解是**在紙色出現之前就把淡出收完**。
> `t` 到 1 的那一刻正是 `u` 開始的那一刻，也就是紙色剛要出現的臨界點，收在這裡剛剛好。
> 收完之後條子下緣是硬邊，但那一刻它正好和「窄帶結束、紙色開始」那條原本就存在的邊重合，
> 所以看不出多了一條線。（2026-08-07 曾經改綁 `u`，立刻長出使用者說的那條陰影，已改回。）

---

## 六之四、文章卡的陰影與 hover（2026-08-07）

原本這兩頁從頭到尾沒有陰影 —— 只有 HERO 那兩層，而且是為了讓字在照片上讀得出來。
使用者要求文章卡加上陰影，條件講得很精確：

> 「除了圖卡會像現在有整個往上加陰影效果外，圖片還會微微放大。另外每個圖卡在游標
> 還沒點到時就自帶一點陰影……但又不能做太多到游標移到圖卡才加強的陰影效果，
> 這樣就沒差別了。」

**兩件事要同時成立**：靜止就有陰影，而且 hover 還要看得出變化。
關鍵不是絕對值，是**兩者之間的級距**：

| | 偏移 y | 模糊 | alpha |
| --- | --- | --- | --- |
| 靜止 | 4px | 10px | .09 |
| hover | 16px | 32px | .16 |
| 倍率 | 4× | 3.2× | 1.8× |

```css
--card-shadow:       0 1px 2px  rgba(42, 44, 39, .07), 0 4px  10px rgba(42, 44, 39, .09);
--card-shadow-hover: 0 5px 10px rgba(42, 44, 39, .09), 0 16px 32px rgba(42, 44, 39, .16);
```

**三個量要一起放大**（位移、模糊、alpha），「浮起來」才會是一個明確的事件，
而不是慢慢變濃。只加深 alpha 而不動模糊與位移，就是使用者說的「沒差別」。
卡本身同時抬 4px（原本 2px）—— 陰影與位移是同一個動作的兩半。

> **陰影色用墨色 `#2a2c27` 不是純黑。** 紙是 `#e2e5e6`（L\* 89），
> 純黑陰影在這個亮度的底上會讀成灰髒的一圈；帶著內文墨色的傾向才像同一頁的東西。

### 縮圖的放大

```css
.card-thumb { transition: transform .5s cubic-bezier(.22, .61, .36, 1); }
.card:hover .card-thumb { transform: scale(1.05); }
```

- **1.05 是刻意的小。** 16:9 的縮圖放大 5% 只會讓構圖往外推幾個 px，
  看得出來在動，但不會變成「換了一張圖」。
- **時間比卡片的位移長（.5s 對 .22s）。** 卡是立刻回應游標、圖是慢慢推進去，
  兩個速度不同才讀得出是兩層，而不是整張卡在縮放。

### 三個實作上的坑

1. **`.card` 要加 `isolation: isolate`。** 卡有圓角 ＋ `overflow: hidden`，
   裡面又有東西在 `transform` —— 沒有獨立的堆疊脈絡時，
   Safari 會讓縮圖的四角溢出圓角。
2. **hover 那一組要包在 `@media (hover: hover)` 裡**（手機版尤其）。
   觸控裝置沒有游標，`:hover` 會在點下去之後「黏住」不放，看起來像卡片壞掉。
3. **`prefers-reduced-motion` 只拿掉會動的部分，陰影留著。**
   陰影是靜態的層次資訊，不是動效。

---

## 六之五、電腦版與手機版的同步（2026-08-07）

**方向已經反過來了，接手的人要知道：**

> 手機版 `/history/home-mobile-n3.html` 是先做的，電腦版 `/history/home-desktop-band.html`
> 是從它推廣出去的。但**電腦版定案得比較晚、也比較完整**，
> 所以從 2026-08-07 起 **以電腦版為準**，改動先進電腦版，再回推手機版。

這一輪回推的東西：

| | 手機版原本 | 改成 |
| --- | --- | --- |
| 頁首玻璃 | 兩組 rgba 內插、終點不透明 | **顏色固定、只有濃度在變**，過了窄帶再換成實色（見下） |
| 頁首下緣 | 沒有 mask，框邊是一條硬線 | `mask-image` ＋ `--head-fade`，深色區 76%、紙色上收到 100% |
| 頁尾 | 一段文字，含「在地經營超過四十年」 | 三段：聯絡／站內導覽／關於這些文章 |
| 按下的 chip | 只換 `background` 與 `color` | 加上 `border-color` —— 少了它兒牙會變成深棕框包黃底 |
| chip 換色 | 沒有過渡 | `transition: background-color .15s` |
| 卡片 | 沒有陰影、沒有焦點框 | 靜止陰影 ＋ `:focus-visible` |
| 卡片註記 | 「更新 2026/08/02 ・ 👁 1,284」 | 「2026/08/02 ・ 1,284 次瀏覽」 |

**沒有回推的一件事：hover。** 使用者明講手機版不要「上移 ＋ 縮圖放大」。
手機沒有游標，`:hover` 會在點下去之後黏住不放，看起來像卡片壞掉。
所以手機版的卡片只有靜止陰影，連 `transition` 都不需要 —— 沒有東西會變。

### 頁首玻璃的正確模型（兩個進度，前後相接）

```
t  窄帶進入條子 → 條子被窄帶填滿。這一段只調濃度 .06 → .84
u  窄帶開始離開 → 完全離開。這一段把玻璃換成實色 #393736
   （＝ .84 濃度疊在紙色上的合成色，所以交接那一瞬間顏色數值完全相同）
```

- **顏色固定 `#191614`，不要再去調「終點的顏色」。** 往不透明終點內插的結果是
  條子壓在照片最暗的下半部與窄帶上時反而**變淺**，像一塊浮在夜色上的灰板。玻璃不會這樣。
- **過了窄帶必須變成實色。** 只要還留一點透明度，底下換成紙色時條子一定會跟著變亮。
- **`--head-fade` 只能綁 `t`，不能綁 `u`。** 淡出的那一段和「紙色從下面填進來」
  是從同一條下緣往上長的，綁 `u` 兩者必定重疊，淺底上那段「由深漸淡」會被讀成一條陰影。
  綁 `t³` 是讓淡出在紙色出現**之前**就收乾淨。

### 主選單兩頁都是三項，「主題與科別」不放進去

390px 上四項的算式（量真機截圖得到的）：

```
左右內距 31.2 ＋ 標誌 34.6 ＋ 標誌間距 10.1 ＋ 標題六字 95
        ＋ 頁首間距 11.7 ＋ 選單四項 209.5  ＝  392.1px   （螢幕只有 390）
```

`.site-nav a` 有 `white-space: nowrap` 不會縮，所以**被擠的一定是品牌**，
「芳仁牙醫診所」就斷成「芳仁牙醫診／所」兩行。退回三項之後的剩餘空間：
375px → 61.6、390px → 66.7、414px → 71.0。

**電腦版明明放得下，也一起拿掉** —— 兩頁的主選單要是同一組，
不然改一頁就得記得改另一頁，這種事一定會漏。這一節的入口在頁尾的站內導覽。

> 順手加的兩道保險，之後就算又被擠到也不會斷行：
> `.brand { flex: 0 0 auto }` ＋ `.brand-text b, .brand-text small { white-space: nowrap }`。

### ⚠ 不要讓 `img` 比它的框寬，就算外面有 `overflow: hidden`

手機版 HERO 原本靠 `width: 113%` ＋ 外框 `overflow: hidden` 做右緣 13% 的裁切。
**Blink 收得住，iOS Safari 收不住** —— 使用者在真機上連續回報兩次
「頁首的文字跑到範圍外面」，成因就是它：

```
img 比框寬 → iOS 把版面視窗（ICB）撐到內容寬度
          → .phone（width:100%）跟著變寬 → img 又跟著變寬
          → 390 的螢幕最後排成大約 590
```

版面一變寬，`.sticky-head`（`left: 0; right: 0`）就跟著文件的寬度走，
**主選單被帶到文件的最右邊**，而內容還在左邊 —— 那正是截圖裡的樣子。
`body { overflow-x: hidden }` 擋不住，因為問題發生在「版面視窗被撐大」這一層，
不是「內容被畫出去」那一層。

改用 `object-fit` 之後畫面一模一樣，但 img 本身就是 100% 寬：

```css
.hero-photo img {
  position: absolute; inset: 0;
  width: 100%; height: 100%; max-width: 100%;
  object-fit: cover; object-position: left center;
}
```

框是 `W × 0.7472W`（比例 1.338），照片是 1600×1058（比例 1.512）——
`cover` 會以高度為準放大到 1.13W 寬再左右裁，`object-position: left`
讓裁掉的正好是右緣那 13%。**和原本的 `width: 113%` 是同一個結果。**

改完之後 375／390／414 三個寬度下，**沒有任何一個元素的右緣超出視窗**
（連把 `overflow` 強制設成 `visible` 去量都一樣）。這是這一頁第一次做到。

> 電腦版的 HERO 本來就用 `object-fit: cover`，沒有這個問題。

### ⚠ 使用者的手機是 **375 CSS px**，不是 390

2026-08-07 用診斷頁在他手機上量到的：

```
screen.width      375
innerWidth        375
scrollWidth       375      （＝ 可左右拉 0px）
visualViewport    375 × 667（縮放 1.00）
devicePixelRatio  3
UA                iPhone OS 18_7 / AppleWebKit 605.1.15
```

375 × 667 配 dPR 3 表示他開了**顯示放大（Display Zoom）**，
也就是螢幕硬體比 375 大、但版面照 375 排、字和元件都等比放大。
**這一頁所有的寬度驗算都要以 375 為準**，先前算的 390 太寬鬆 ——
主選單四項那次超出的其實比我算的 2px 更多。

> 順帶這張診斷也證實了一件事：**claude.ai 的檢視器沒有把頁面塞進比螢幕寬的框**，
> `innerWidth` 就等於 `screen.width`。所以先前的水平溢出是頁面自己的問題，
> 不是檢視器的。診斷頁：`https://claude.ai/code/artifact/49b85912-755a-4dc6-81a9-76ca8d541b75`

改完之後在 375 與 390 兩個寬度量到的結果一致：
`html.scrollWidth` 等於視窗寬、**可左右拉 0px**、品牌名單行。

### ⚠ 虛擬元素的 `position: absolute` 是找**祖先**，不是找自己那個元素

首頁把窄帶從「壓在整屏下緣」改成手機的「接在照片下面」時，我寫了：

```css
.hero > .band { position: static; }     /* ← 錯 */
```

`.band::before`（毛玻璃 `blur(4px)`）與 `.band::after`（柏油漸層）都是
`position: absolute; inset: 0`。`.band` 一旦是 `static` 就**不是包含塊**，
兩層改用 `.hero`（`position: relative`）—— 於是整張 HERO 照片被糊掉、再蓋一層柏油色。
使用者的原話是「Hero 區的圖片整個變暗糊掉了」。

```css
.hero > .band { position: relative; }   /* 留在流內，但仍是自己虛擬元素的包含塊 */
```

手機版本來就不用毛玻璃（帶子接在照片下面，底下沒有東西可以透），
所以兩層直接 `display: none`，換回定案的夜色漸層。

### 頁首品牌兩行要右緣切齊，靠的是 `text-align-last: justify`

```css
.brand-text small { text-align: justify; text-align-last: justify; }
```

區塊寬度由較寬的那一行（標題六字）決定，副標八字用字距拉開填滿同一個寬度，
兩行的右緣才會切齊。**捲動收合時也成立**，因為兩行的字級是整組 ×0.9、比例不變。
量到的結果：375／390／414 三個寬度、正常與收合兩種狀態，右緣差都是 `0.00`。

### ⚠⚠ 內嵌 `<svg>` 一定要寫 `width` / `height` 屬性

**這是頁首搞了七輪的真正源頭。**

`<svg>` 沒有 `width`／`height` 屬性時，**它的內在尺寸在 WebKit 是預設的 300×150**。
`.brand-mark` 是 flex 項目、`min-width` 預設 `auto`（＝ min-content），
於是它縮不到 300px 以下 —— `.brand` 的 max-content 就變成

```
300（標誌被頂住） ＋ 間距 ＋ 品牌文字  ≈  395.5
```

而真機量到的就是 `.brand w = 395.5`。**Blink 把 `width: 100%` 的 SVG 當成
min-content 0，所以在這裡怎麼量都正常。**

```html
<svg width="44.2873" height="21.8244" viewBox="0 0 44.2873 21.8244">
```
```css
.brand-mark { min-width: 0; }
.brand-mark svg { width: 100%; max-width: 100%; height: auto; }
```

屬性只是給「內在尺寸」用的，CSS 的 `width: 100%` 仍然覆蓋它，畫面一個像素都不變。
兩頁的標誌都補了。

#### 頁首的版型：grid，不是 flex

```css
.site-header { display: grid; grid-template-columns: max-content minmax(0, 1fr); }
.site-nav    { flex-wrap: nowrap; min-width: 0; justify-content: flex-end; }
```

- `max-content` 軌道：左欄就是品牌的內容寬度，不接受引擎推論。
- `minmax(0, 1fr)`：右欄下限 0，**不會反過來把格線撐寬**。
- 選單 `flex-wrap: nowrap`：使用者要求一定同一行。

> ⚠⚠ **2026-08-16 兩欄對調了：`minmax(0, 1fr) max-content`。**
> 使用者在 iPad 上回報「左上角的雲林斗六・永樂街斷行了」（斷在「永樂／街」中間）。
> 上面那個寫法解決的是「會不會溢出」，但沒有回答**放不下的時候誰讓步** ——
> 軌道演算法會先把 `max-content` 那一欄長到滿，剩下的才給 `fr`，
> 所以擠的時候讓步的是**選單**（Chromium 實測直接疊到品牌上，最多重疊 209px），
> 而品牌裡唯一沒有 `nowrap` 的 `<small>` 就在那裡斷成兩行。
> 現在改成**選單 `max-content`（絕不讓步，它是三個固定寬的框）、品牌 `1fr`（可縮）**，
> 品牌再靠三條讓步得好看：`.brand { width: max-content; max-width: 100% }`
> （`width: max-content` 不能省，不然 `<a>` 撐滿整欄、右邊一大片空白變成回首頁的熱區）、
> `.brand-text small { white-space: nowrap }`、`.brand-text { flex-wrap: wrap }`。
> 擠不下時副標**整串**掉到第二行（＝手機那一段本來就在用的兩行式），不是斷字。
> 頁首的 `gap` 同時從 2rem 收到 1rem —— 品牌靠左、選單靠右，有空間時那個值
> 一個像素都看不出來，它只在擠的時候生效，收掉等於多還 19px 給品牌。
> ⚠ 成因不只是版面：這一格本來就是全站最緊的（iPad +19% 之後 744 上
> 「品牌 → 選單」只剩 94.6px），真機再套上瀏覽器的「最小字級／文字縮放」就不夠 ——
> 實測最小字級設 20px 時只剩 9.3px。**不要用「我這台放得下」當驗收。**
> 驗收：18 個寬度 × 兩頁 × 捲動前後共 72 組逐項比對頁首幾何，
> 只有 320 那一欄有差（品牌原本溢出、疊到選單 7.9px，現在讓開 9.6px），其餘 0 項。
>
> ⚠⚠ **同一天再改一次：直式平板（≥721px 且比例 ≤ 9/10）的品牌一律兩行。**
> 上面那個「擠不下時副標整串掉到第二行」使用者看了說
> **「這樣斷行很沒水準，那你還不如做成手機的樣式就好，做這樣很奇怪耶。」**
> —— 那個兩行是**半套的**：字不斷了，但「診所名｜地名」中間那條直立分隔線還在，
> 掉到第二行就變成地名前面孤零零一豎。手機那一段本來就有完整的兩行式
> （分隔線收掉、兩行都 `text-align-last: justify` 對齊左右緣），原樣搬到直式平板。
> ⚠ **是「一律兩行」不是「擠不下才兩行」**：擠不擠得下**看使用者自己的字級設定**
> （他那台 iPad 的網頁文字約放大 24% —— 6 個字的診所名畫出 150px、標稱只有 122px，
> 而標誌那 45px 是寫死的 px、一點都沒放大 → 是 Safari 的「網頁文字大小」，
> 不是頁面縮放）。做成條件式，同一台 iPad 會因為那個設定長得不一樣。
> ⚠ **文章頁一起改**（2026-08-15「首頁切到文章頁，頁首不要變」還在）。
> ⚠ iPad **橫放不吃這一段**（比例 > 9/10，落在電腦版），橫放仍然是一行。
> ⚠ 兩行都要寫 justify：這一段的比值 14.82 ÷ 20.14 ＝ 0.736 看起來 < 6/8，
> 但副標字距 .04em、診所名只有 .01em，實際畫出來是 123.3 vs 122.0，**副標比較寬**。
> 驗收：同樣 72 組，只有七個直式平板寬度（721／744／768／800／820／834／1024）
> 的品牌三個元素有差，兩行左右緣完全切齊、分隔線 `display: none`、頁首高度沒動；
> 手機、iPad 橫放、電腦版 0 項有差。

量到的結果：300／320／375／390／414 五個寬度下 `可左右拉` 全部 0、
頁首維持一行、選單末項都在畫面內。

#### 這一段真正的教訓

七輪裡我換過六種寫法（品牌可縮／鎖死／`width: max-content`／
裁 `html`／裁 `.phone`／裁 `.hero-stage`／改 grid），**沒有一次打中**，
因為我一直以為在比字型寬度（差幾 px）。實際上是一個 `<svg>` 的預設內在尺寸（300px）。

> **把可疑元素的實際尺寸印在頁面上請使用者截圖，比再改五版 CSS 快得多。**
> `.brand w = 395.5` 這個數字出現之後，兩輪就收斂了。

### ⚠ 頁首那一列：讓它「不可能溢出」，不要算「剛好放得下」

2026-08-07 為了這一列來回四次，三種壞結果其實是同一個病：

| 做法 | 結果 |
| --- | --- |
| 品牌可縮 | 選單的 `nowrap` 贏 → 「芳仁牙醫診所」斷成「芳仁牙醫診／所」 |
| 品牌鎖死 `flex: 0 0 auto` | 換成整列比視窗寬 → 選單被推出畫面、**頁面可以左右拉、捲動時會晃** |
| 在 `.phone` 上裁掉 | 晃是不晃了，但右對齊的選單**整組被裁掉、看不見** |

**病因是「這一列在真機上放不下」，三個做法只是在決定誰去承受它。**
而我在容器裡量到 375px 下只用了 313（還剩 61）—— 容器沒有 PingFang TC，
**那個數字不能拿來保證真機**，這一輪之前已經因此判斷錯兩次。

所以不再賭算得剛好，改成把「溢出」這個可能性拿掉：

```css
.site-header { flex-wrap: wrap; }              /* 放不下就讓選單掉下一行 */
.site-nav    { flex-wrap: wrap; min-width: 0; justify-content: flex-end; }
```

`min-width: 0` 是關鍵：沒有它，flex 項目不會縮到 min-content 以下，
就只能往外撐。加了之後 240／280／320／375／390 五個寬度量到的
`scrollWidth` 全部等於視窗寬、**可左右拉 0px**，而且選單一律看得到 ——
320 以下會掉到第二行（會和詩擠一點），但那是三種壞結果裡唯一可接受的。

> **通則：版面規則要能吸收字型的差異，不是去預測它。**
> 「算出來還剩 61px」在換一套中文字型之後就不成立了；
> 「放不下就換行」在任何字型下都成立。

### 水平方向收在 `.hero-stage` 上，用 `clip` —— **不要收在 `.phone` 或 `html` 上**

```css
.hero-stage { overflow-x: clip; }     /* HERO 是唯一量得到水平溢出的地方 */
```

三個位置都試過，只有這一個對：

| 放哪裡 | 結果 |
| --- | --- |
| `html` | `.sticky-head` 黏不住了。量到捲到 700px 時 `header.top = -700`（整條跟著捲走）。 |
| `.phone` | 頁首被裁掉。`.phone` 包著頁首，一旦它比視窗寬，**右對齊的主選單整組看不見** —— 使用者回報「全部文章／醫師介紹／診所資訊 沒有顯示」。 |
| **`.hero-stage`** | ✓ 它是溢出真正的來源（照片、`.hero-sign::before` 那顆用負 inset 往外撐的藥丸），**而且不是 `.sticky-head` 的祖先**（兩者是 `.phone` 底下的兄弟），所以裁它動不到頁首。 |

用 `clip` 不用 `hidden`：`hidden` 會建立捲動容器，`clip` 不會。

> **通則：裁切要放在「溢出的來源」上，不要放在「頁面的邊界」上。**
> 放在邊界看起來比較保險，實際上是把所有右對齊的東西一起賭進去。

### ⚠ 不要在 `html` 上加 `overflow-x: clip`

2026-08-07 加過一次，**當天就退掉**。當時是想擋 iOS 的水平捲動，
但那只是猜測、沒有證據，代價卻是**手機版的固定頁首整條壞掉**：

```
加了之後量到：scrollY=300 → header.top = -300
              scrollY=700 → header.top = -700   （跟著捲走，完全沒黏住）
退掉之後：     三個位置 header.top 都是 0
```

`overflow-x: clip` 讓 `html` 變成裁切容器，`.sticky-head` 的 `position: sticky`
就失去它原本相對視窗黏著的行為。**水平方向就靠 `body { overflow-x: hidden }`，
那一直都在，也一直都夠。**

> 教訓不在 CSS，在流程：**為了一個沒有重現步驟的猜測，去動一個正在正常運作的東西。**
> 使用者回報「頁首的字跑到很奇怪的地方」時，那張截圖裡還有 artifact 檢視器自己的
> 浮動按鈕，成因並不明確；正確的作法是先問清楚或先量到，而不是先下藥。

### 量手機版的版面時，不要用 `--window-size` 直接截圖

這個容器裡的無頭 Chromium **把版面視窗寬度夾在 500px 以上**：
下 `--window-size=390,760` 得到的畫布是 390 寬，但頁面其實是照 `innerWidth = 500` 排的，
截出來就是「右邊被切掉」的假象 —— 看起來像破框，其實沒有。

要量真正的 390px，把頁面放進一個 `width: 390px` 的 `<iframe>` 再截外層那一頁。
實測 375／390／414／430 四個寬度，`scrollWidth` 都等於視窗寬，沒有水平溢出。

---

## 六之六、文章頁的科別色（2026-08-08）

文章頁不再全篇深綠松，**重點色跟著這一篇的科別走** ——
和首頁「主題與科別」那顆標記同一個顏色。使用者要的：
「套色一起改 跟著首頁的科別標籤顏色動」。

科別寫在 `<body data-spec="…">`（**在 `<main>` 之外**，所以不會動到內容雜湊、
六篇的更新日期一格都不會跳）。標籤 → 代碼的對照表就是 `tools/build.mjs` 的 `SPEC`，
和首頁卡片共用同一份，不會有兩套。

哪些東西吃這個色：

| 元件 | 用哪一階 | 為什麼 |
| --- | --- | --- |
| 小節標題前的 3px 短棒 | `--accent` | 是「底」不是字，只要 3:1 |
| 重點框 `.callout` 左邊那條 | `--accent` | 同上 |
| 實心按鈕 `.btn` | 底 `--accent`＋字 `--on-fill` | 白字，兒牙就是那個 3.22 的問題所在 |
| 內文連結、`h3`、線框按鈕 | `--accent-deep` | 是字，要 4.5:1 |
| 頁尾電話 | **維持 `--teal`** | 頁尾是全站共用的落款，不屬於某一篇 |

### ⚠ 字階不能直接從首頁搬過來，底不一樣

首頁的標記坐在**卡片** `#f4f4f5` 上，文章的內文坐在**紙** `#e2e5e6` 上，
紙比卡深一階（1.152:1），字就得跟著再深一點才守得住 4.5:1。

| 科別 | 原色 | 疊在紙上 | 文章頁的字階 | 疊在紙上 |
| --- | --- | --- | --- | --- |
| 一般・牙周 `--chair` | `#4f6361` | 5.04 ✓ | 原色 | 5.04 |
| 齒顎矯正 `--blue` | `#3C596B` | 5.85 ✓ | 原色 | 5.85 |
| 口腔外科 `--taupe` | `#7D5A58` | 4.77 ✓ | 原色 | 4.77 |
| 贋復假牙 `--tile` | `#252d4a` | 10.68 ✓ | 原色 | 10.68 |
| 顯微根管 `--moss` | `#5D6D55` | **4.38 ✗** | `#596951` | 4.65 |
| 兒童牙科 | `#c28229` | **2.54 ✗** | `#915800` | 4.61 |

所以只有兩科需要另外一階。**兒牙在首頁是 `#9e6301`（對卡 4.51），
搬到紙上只剩 3.91 —— 不能直接用。** 兩個值不是筆誤，是兩個底。

### hover 用 `color-mix` 壓深，不要為七個科別各寫一條

```css
.btn:hover { background: color-mix(in srgb, var(--accent) 84%, #1a1a17); }
```

舊瀏覽器不支援就整條失效，按鈕維持原色，不會壞掉。

### ⚠ `.views` 的「次瀏覽」文章頁**標記裡本來就有**

首頁的卡片是 `build.mjs` 產生的，「次瀏覽」是它寫進去的；
文章頁的 `<main>` 裡本來就打著 `<span class="views-n">—</span> 次瀏覽`。
第一版照抄首頁的做法補了一個 `::after { content: "次瀏覽" }`，
結果變成「1,284 次瀏覽 次瀏覽」。要拿掉的只有眼睛圖示。

另外 `.views` 是 `inline-flex`，標記裡數字和「次瀏覽」中間那個半形空白
會被 flex 吃掉（變成「1,284次瀏覽」），要用 `gap: .3em` 補回來。

---

## 六之七、favicon（2026-08-08 上線）

`assets/favicon.svg` 從 2026-08-02 那顆手繪牙換成**標誌本體**。
被三處引用：`index.html`、六篇文章、`404.html`。

### 改完一定要換 `?v=` —— 否則 Safari 永遠給舊圖

所有 `<link rel="icon">` 都帶版本參數：

```html
<link rel="icon" href="assets/favicon.svg?v=20260808b" type="image/svg+xml">
```

**這不是裝飾，是這顆圖示唯一能換掉的辦法。**
2026-08-08 換成標誌本體以後，使用者在手機 Safari 的分頁列表裡看到的還是
2026-08-02 那顆「深綠圓角方塊 ＋ 白牙」—— 檔案在伺服器上早就換了。

原因：**Safari 的 favicon 是存在一個獨立的圖示資料庫裡，鍵值是「圖示的網址」。**
只要網址一個字都沒變，它就認定手上那張還有效，
**重新整理沒用、清網頁快取也沒用**（那是另一份快取），開著的分頁更不會重抓。
Chrome／Firefox 沒這麼固執，所以在電腦上看是對的 —— 這件事只會在 Safari 上出現。

所以規則是：**動到 `assets/favicon.svg` 的內容（換色、改幾何），
就把全站 `?v=` 後面那個日期一起換成當天。** 一行指令掃過所有頁：

```bash
grep -rl 'favicon\.svg?v=' --include='*.html' . \
  | xargs sed -i 's|favicon\.svg?v=[0-9a-z]*|favicon.svg?v=<新版本>|g'
```

同一天內改第二次就在後面接字母（`20260808` → `20260808b`），
所以正規式要吃得下字母 —— 寫成 `[0-9]*` 會比對失敗，等於沒換。

換過之後，使用者那邊仍要**把分頁重新整理一次**才會換圖（新網址要被載到才會去抓）；
已經開著沒動的舊分頁會維持舊圖，關掉重開即可。

`<svg>` 上的 `width="64" height="64"` 也是為 Safari 留的：
`viewBox` 已經夠用，但少數算圖器對沒有內建尺寸的 SVG 會直接放棄，
放棄的時候 Safari 就退回它快取裡的上一張 —— 症狀和上面一模一樣，很難分辨。
兩個屬性成本是零，留著。

### 一個檔管兩種分頁列 —— 但 Safari 不算在內

SVG favicon 吃得到自己 `<style>` 裡的 `prefers-color-scheme`，
所以不需要兩個檔、也不需要在 `<head>` 寫兩個 `<link media="…">`。
**唯一的例外是 Safari：它不看 favicon 裡的深色模式，只會用沒有條件的那一行。**
（`<link>` 上的 `media` 屬性它同樣不看，所以拆成兩個檔也救不了。）

所以順序是**刻意反過來寫**的 —— 深色版當預設，淺色版放進 media query：

```svg
<style>
  path { fill: #4f8065; }                                    /* Safari 只看得到這一行 */
  @media (prefers-color-scheme: light) { path { fill: #4f6361; } }
</style>
<path fill="#4f8065" … />
```

Chrome／Firefox 兩種分頁列的結果和以前完全一樣，只有 Safari 從
「永遠拿到暗的那顆」變成「永遠拿到亮的那顆」。為什麼非這樣不可，見下一小節。

`fill` 屬性**要留著**：不吃 `<style>` 的算圖器（少數 RSS 閱讀器、舊工具）
會退回屬性上的顏色，而不是變成黑色方塊。CSS 的權重高於呈現屬性，
所以在正常瀏覽器裡不會打架。

### Safari 會自己在圖示後面墊一塊白底

**症狀**：iPhone 上按網址列（或開分頁列表），標誌被裝在一個**白色圓角方塊**裡，
旁邊別人的網站（用 emoji 當圖示的）卻是乾乾淨淨貼在黑底上。

**原因**：Safari 在深色模式下會自動幫「對比不夠」的圖示墊一塊白底或黑底，
好讓暗色圖示在黑底上還看得見。**淺色模式不會做這件事**（它容忍低對比圖示）。
門檻沒有公開，Apple 沒有文件，只能試。

把上一節那件事接起來就是完整的故事：Safari 不看深色模式 → 它一直拿到淺色版
`#4f6361` → 那個顏色**對深色列只有 2.67:1**，連非文字圖形的 3.0 下限都沒過 →
Safari 判定看不清楚 → 墊白底。順序換過來之後 Safari 拿到 `#4f8065`（3.73:1，過線）。

| 顏色 | 對白底 | 對純黑 | 對深色列 `#1c1c1e` |
| --- | --- | --- | --- |
| `#4f6361` 淺色 | 6.38:1 | 3.29:1 | **2.67:1**（不合格，會被墊白底） |
| `#4f8065` 深色 | 4.56:1 | 4.61:1 | 3.73:1 |

**如果 3.73 還是不夠**，只能再往上一階。代價是**淺色分頁列會跟著變淡** ——
Safari 只用同一個顏色畫兩種分頁列，在深底提亮就等於在白底減對比。
四個候選與各自的代價做成了可以在手機上逐頁看的頁面：

| 候選 | 色值 | 對深色列 | 對白底 |
| --- | --- | --- | --- |
| c1 | `#4f8065` | 3.73:1 | 4.56:1 |
| c2 | `#5d9877` | 5.04:1 | 3.37:1 |
| c3 | `#71a889` | 6.21:1 | 2.74:1（低於 3.0） |
| c4 | `#87b59c` | 7.39:1 | 2.30:1（低於 3.0） |

`/history/logo-favicon-plate.html` —— 一個候選一頁、一頁一個圖示網址，
Safari 才會分開判斷。頁上的圖示刻意**不帶 media query**，測的就是 Safari 實際拿到的那顆。

> **不要用「加個底色方塊」來解這件事。** 那正是 2026-08-02 那顆手繪牙被換掉的原因，
> 見本節最後的幾何說明。要解就解亮度。

### 兩個顏色

| | 色值 | 亮度 Y | 對分頁列 | 說明 |
| --- | --- | --- | --- | --- |
| 淺色 | `#4f6361` | 0.11 | 5.81:1（白底） | 診療椅實測色，和科別色的 `--chair` 同一個值 |
| 深色 | `#4f8065` | 0.16 | 3.37:1（`#22252a`） | 色相 147°、彩度 24% 都和淺色同組，**只提亮度** |

深色版**不是另挑一個色**，是同一個色提亮到剛好站得住：

- `#4f6361` 直接放深底只有 2.41:1，**不合格**（非文字圖形的下限是 3.0）。
- 只要把亮度 Y 從 0.11 提到 0.15 就過線。早期版本提到 0.28（對比 4.8），
  多花了兩倍，結果**看起來像另一個主題色** —— 使用者就是被這個落差刺到的。
- 定案的 3.37 離下限留了餘裕（各家瀏覽器的深色分頁列深淺不一，
  貼著 3.0 會有風險），同時和淺色的色差 ΔE 壓到 20.7，並排要盯著看才分得出來。

五個候選（對比 3.0／3.4／3.8／4.3／4.8）與完整推導留在 `/history/logo-favicon.html`。
要換成別案只改 SVG 裡那一行。

### 幾何

牙洞 **1.6 倍、上移 1.05** —— 比頁首那顆（1.3 倍、上移 0.50）誇張，
因為 favicon 要在 **16px** 下還看得出那個洞。同一條路徑，只有洞的比例不同。
`/history/logo-favicon-shift.html` 是上移位置的四案紀錄。

> **標誌是橫的**（約 2:1），放進 64×64 的 viewBox 就只佔中間一條。
> 這是刻意的：**不要為了填滿方框而加圓角底色或把標誌拉高** ——
> 舊的那顆手繪牙就是「深綠圓角方塊 ＋ 白牙」，換掉它的理由正是那不是診所的標誌。

### 根目錄的 `favicon.ico` —— 只給 Google，不給瀏覽器（2026-08-09）

**這一顆和上面那顆 SVG 分工不同，不要混在一起看。**

| | `assets/favicon.svg` | `/favicon.ico` |
| --- | --- | --- |
| 誰在用 | 瀏覽器分頁列（Chrome／Firefox／Safari） | Google 搜尋結果那顆小圖 |
| 在 `<head>` 裡宣告 | 有，帶 `?v=` | **沒有，刻意不宣告** |
| 深淺色 | 兩色，靠 SVG 裡的 media query | 一色，`#4f8065` |
| 底 | 透明 | 透明 |

由 `tools/favicon-ico.mjs` 從 SVG 算出來，內含 16／32／48／96 四種尺寸的 PNG
（ICO 從 Vista 起就允許內容是 PNG）。48 與 96 是 Google 要的 —— 它要求邊長是 48 的倍數。

**為什麼要多一顆點陣圖：** 搜尋結果的圖示是 Google 另一支爬蟲另外抓的，
排程和網頁索引各走各的，而 `/favicon.ico` 是它在讀不到或不採用 `<link>` 宣告時
會主動去試的老位置。多放一顆等於多給它一條確定吃得下的路。

**⚠ 絕對不要把它加進任何 `<head>`。**
瀏覽器只有在頁面**完全沒有** `<link rel="icon">` 時才會去要 `/favicon.ico`；
我們每一頁都宣告了 SVG，所以瀏覽器一律走 SVG，行為和現在完全一樣。
一旦寫了 `<link rel="icon" href="/favicon.ico">`，Safari 很可能改用這顆點陣圖 ——
上面那整節「順序反過來寫、讓 Safari 拿到 `#4f8065` 才不會被墊白底」就等於白做。

**顏色只能是 `#4f8065`（深色版那一顆）。** ICO 是一張圖走天下，沒有深淺色可分，
而 Google 的搜尋結果淺色深色兩種底都會出現。`#4f8065` 正是為了「算圖器不看深色模式」
這種情境挑的，兩邊都站得住：對白底 4.56:1、對純黑 4.61:1，都過非文字圖形的 3.0。
淺色版 `#4f6361` 對純黑只有 3.29、對深色列 2.67，單張用會在深底吃虧。
所以腳本會**主動把 SVG 的 `<style>` 拿掉**，改把沒有條件的那個 fill 寫死到 path 上 ——
算出來的顏色不能看 Chromium 當下是淺色還是深色模式。

改過 `assets/favicon.svg` 的顏色或幾何之後要重跑：

```bash
node tools/favicon-ico.mjs           # 重新產生
node tools/favicon-ico.mjs --check   # 只比對，不一致就回傳非零
```

它需要一個 headless Chromium 來算圖，**平常的 `npm run build` 不會呼叫它**
（這個 repo 零 npm 依賴，Windows 本機通常也沒有那個執行檔）。
`favicon.ico` 已經進版控，跑不動它不影響建置或部署。

> **踩過的兩個坑，改這支腳本之前先看：**
>
> 1. **剪 `<style>` 之前要先剪掉 `<!-- -->` 註解。** `favicon.svg` 的註解裡本來就在
>    討論 `<style>` 這個標籤，字面上就有 `<style>`；直接比對會咬到註解裡那一個，
>    一路刪到真正的 `</style>`，連註解自己的 `-->` 都被刪掉 ——
>    `<path>` 整條被吞進沒有結尾的註解裡，算出來是一張全透明的空圖。
>    **ICO 的結構、每張的尺寸全都正常，光看檔案看不出來。**
> 2. **新版 headless 的 `chrome` 在容器裡畫不出 SVG 圖形。** 背景色照樣上得去，
>    `<rect>`／`<polygon>`／`<path>` 一個都沒有。Playwright 的 `headless_shell`
>    （舊 headless）三種都正常，所以腳本把它排在候選的最前面。
>
> 這兩件都會產出「看起來很正常的空圖示」，所以腳本裡有一道保險絲：
> 同一張頁面**不放 SVG** 再算一次當對照組，兩張如果一個位元組都不差就直接失敗。

---

## 六之八、科別的意象（2026-08-08）

**這一節是六之二節的另一半。** 六之二寫的是「哪一科拿哪個色、白字幾比幾」，
來源是診所的實物；這一節寫的是「**為什麼那個色是對的**」。使用者給的樣板是兒牙：

> 「兒童牙科的對象是小孩，小孩是活潑的、對未來有無限的可能，所以我堅持用黃色。」

推導鏈是四段：**對象是誰 → 那個人處在什麼狀態 → 這一科真正在做的事 → 一個意象**，
顏色接在最後。這一節把其餘六科補上同一條鏈。

> **這一節不改任何色值，也不推翻六之二。**
> 六個色是先從診所實物量出來的（**只有兒牙的黃例外 —— 診所裡沒有黃**），
> 意象是事後整理的。但整理完之後對得上，而且對得比預期整齊 —— 見第三小節。

### 一、七科各自的意象

排序照色相，不照選單順序 —— 這樣三組的分界看得出來（理由在第三小節）。

| 科別 | 對象 | 動作 | 病人付出的 | 意象 | 色 | H |
| --- | --- | --- | --- | --- | --- | --- |
| 兒童牙科 | 還在長的人 | **長** | 情緒（怕） | 還沒定型的未來 | `#c28229` | 35 |
| 口腔外科 | 帶著一件明確的事來的人 | **移除** | 情緒（痛） | 被灰包住的紅 | `#7D5A58` | 3 |
| 顯微根管 | 一顆還救得回來的牙 | **保留** | 幾次回診 | 花謝了枝條還在 | `#5D6D55` | 100 |
| 一般牙科・牙周 | 沒有事件的人 | **維持** | 習慣 | 每個人都會坐上去的那張椅子 | `#4f6361` | 174 |
| 齒顎矯正 | 願意用一到三年換一個結果的人 | **排列** | 時間 | 坐著等的沙發、夜空 | `#3C596B` | 203 |
| 贋復假牙 | 要重新規劃一整口的人 | **重建** | 時間與錢 | 茶室裡坐下來談 | `#252d4a` | 227 |

#### 兒童牙科 `#c28229` —— 還沒定型的未來

使用者自己給的那一段，其餘六科都是照它反推的。對象不是牙，是**人，而且是還在長的人**：
牙會換、臉會變、習慣還沒定，什麼都還沒定案。黃是七個色裡唯一「往上、往外」的那一個。

**它也是唯一不從診所實物量出來的色** —— 診所的家具、磁磚、花藝裡沒有黃
（第三節真正量到的黃只有開幕茶會的含羞草 `#9d8313`，是花不是硬裝）。
這件事不是缺陷，是這一科的定義：**兒牙的主體本來就不在診所裡，在孩子身上。**

還有一層：兒牙也是七科裡唯一「這次看得好不好，決定往後四十年」的科。
第一次進診所的經驗會跟著人走，所以這一色要是**進門就看得到、而且不嚇人**的那種。

#### 口腔外科 `#7D5A58` —— 被灰包住的紅

對象是帶著一件明確的事來的人（拔智齒、囊腫、顎面）。這一科的關係是**事件**，不是長線：
有開始、有結束，通常一天。

Deep Taupe 的色相 3 是整套色票裡**最靠近正紅的一個**（比磚紅 `#AF4C52` 的 356 還正），
但彩度只有 17.4%。**它是唯一帶血色相、卻被壓到不喊叫的色。**
外科是七科裡唯一該和紅有關的一科，而診所刻意沒有給它磚紅 ——
六之二節記著使用者的原話：紅在品牌裡的暗示性太強。

Taupe 正好是那句話的解：**份量都在，但不出聲。**
實物是單椅布料，一個人坐的那一張。

> **⚠ 這一段已經被使用者推翻了（2026-08-09）**，他說血色的意象怪怪的，而他是對的：
> 上面這段是**從顏色反推意象**，順序反了。照他給的完整定位重推，
> 這一科的病人付出的不是痛，是**風險**；動作不是「移除」，是「交付」。
> 靛／紫的五案與完整推導見**第六之九節**。這一段留著當紀錄。
> **已定案：填色 `#8e6299`、白底的字 `#784e84`。**

#### 顯微根管 `#5D6D55` —— 花謝了枝條還在

對象不是人，是**一顆已經在痛、但還救得回來的牙**。這一科在做的事只有一個字：**保留**。
不是換掉，是把裡面清乾淨，留住外面那顆自己的牙。牙髓本來就是牙齒裡活著的那一部分。

它拿到的是**苔綠，也就是 Logo 本身的色**，而且和花藝裡枝葉的實測
（`#2f3929` H=98、亮處 `#447c56` H=139）同一族。
第六節第 1 條寫「綠是枝條，可以重複出現」—— 花會謝，枝條留著。

**這是七科裡最強的一個對應：診所的核心價值（保住自己的牙優於置換）拿到了診所自己的標誌色。**
延伸的話，家色深綠松 `#214D48`（H=173）和苔綠同族，也在同一個意思上 —— 保留與延續是一件事。

#### 一般牙科・牙周 `#4f6361` —— 每個人都會坐上去的那張椅子

對象是**沒有事件的人**：沒特別哪裡痛，或狀況還沒變成事件。
這一科在做的事是**維持**，半年一次回來洗牙、檢查、牙周維護。不是一次解決，是一直來。

意象就是那張診療椅 —— 診所裡唯一每個人都會坐上去的東西，使用頻率最高的物件。
實測色 `#4f6361` 對家色深綠松 ΔH=1，**它是家色的實物版**：日常＝家色，這一層是相合的。
使用者指定給創辦醫師與李侑津醫師的也正是這一色。

> **兩科共用一色，在意象層是說得通的 —— 但有一個要盯的地方。**
> 一般牙科顧的是牙冠上面看得到的，牙周顧的是牙齒底下的地基，
> **病人的行為完全一樣（定期回來）、時間軸也一樣（無限期）**，是同一件事的兩個深度。
> 使用者「不同科顏色不要重複」那條規則，管的是**事件型**的科；這兩科不是事件，是同一條長線。
>
> 要盯的是 **`build.mjs` 的 `SPEC` 把「植牙」也放在 `perio` 這一格** ——
> 而植牙是徹頭徹尾的事件型（要規劃、要分期、要幾個月）。
> 意象層給的答案很清楚：**植牙屬於 `prosth`**，
> 首頁 chip 寫的「植牙・假牙重建」本來就在 `prosth` 那顆上。
> 目前沒有任何文章用「植牙」這個標籤，所以還沒出事；**第一篇植牙文出現之前要先決定**。

#### 齒顎矯正 `#3C596B` —— 坐著等的沙發

對象是**願意用一到三年換一個結果的人**。這一科在做的事是**排列**，
而它真正要病人付出的是時間 —— 牙齒一個月只走幾分之一毫米，急不得。

藍灰是候診沙發的色（H=206，實測 `#4e718b`），也和夜空 `#373b42`（H=218）同一條軸 ——
第二節寫著，那是**整棟建築唯一天然的冷色**。
**整套色票裡唯一的冷色，給了唯一以時間為主要成本的科。**

還有一層和 COPY.md 第三節的紅線直接相關：矯正是七科裡唯一**病人自己每天要做功課**的科
（戴、清、按時回診）。「診所給的是燈，不是答案」在這一科最具體。

#### 贋復假牙 `#252d4a` —— 茶室裡坐下來談

對象是牙已經缺了、要重新規劃一整口的人。這一科在做的事是**重建**：
不是修一顆，是把整個結構重新排一次，要談、要分期、要幾個月。

它拿到的是茶室深藍磁磚，**整個色盤最深的一個**（L\* 19.1，白字 13.52:1）——
份量最重的科拿最沉的色。而且磁磚是貼在牆上不會動的東西，這一層也對得上「重建」。

意象取「茶室」而不是「藍」是有意的：**這一科最缺的不是技術，是坐下來把話講完的時間。**

#### 全部 `#5f5d5c` —— 刻意沒有意象

它不是一科，是「不篩選」。`index.html` 那段註解已經寫了：六個彩色各代表一科，
這一顆若也上色就等於多了一科不存在的科別。
它錨在頁首玻璃 `#393736` 往上提亮度，**所以它屬於介面，不屬於科別。**

### 二、兒牙為什麼調了十一輪 —— 意象層的解釋

六之二節記了十一輪的過程，但沒說為什麼**只有這一科**這麼難。把六個色的 Lab 攤開就看得出來：

| | L\* | C\* |
| --- | --- | --- |
| 贋復 `#252d4a` | 19.1 | 19.9 |
| 矯正 `#3C596B` | 36.3 | 14.8 |
| 一般牙周 `#4f6361` | 40.3 | 8.1 |
| 外科 `#7D5A58` | 41.8 | 15.7 |
| 根管 `#5D6D55` | 44.1 | 15.8 |
| **兒牙 `#c28229`** | **59.5** | **57.6** |

**兒牙在兩個維度上都是離群值**：最亮，而且彩度是其餘五科的三到七倍。

這不是調過頭，這正是「活潑、無限可能」翻成顏色之後的樣子 ——
**它是這套色盤裡唯一被要求往外跑的色，而整套色票的體質是安靜的**
（其餘五科全部擠在 C\* 8~20）。十一輪撞的天花板一直是同一個：
要它夠亮才像黃、又要白字站得住，而白字把亮度鎖死在 L\* 47~48。
最後定在 59.5 是**放掉白字的 AA**（3.22:1，已結案）換來的 —— 那筆帳付的就是這一科的意象。

### 三、量出來的三組（這一節最有用的部分）

六個色相排開：

```
外科 3    兒牙 35   │   根管 100   一般牙周 174   │   矯正 203   贋復 227
      情緒               維持與保留                    時間與規劃
```

**三組在色相環上是連續的，沒有交錯。** 而分組的依據不是顏色，是第一小節那張表的
「病人付出的」那一欄：

| 色溫 | 科 | 病人付出的 | 所以 |
| --- | --- | --- | --- |
| 暖（H 3~35） | 外科、兒牙 | **情緒**（痛、怕） | 需要被安撫 → 暖 |
| 綠（H 100~174） | 根管、一般牙周 | **習慣**（回來、刷） | 需要持續 → 活的、枝條的綠 |
| 冷（H 203~227） | 矯正、贋復 | **時間與錢** | 需要冷靜規劃 → 冷 |

> **⚠ 這張表已經被取代（2026-08-09）。** 外科改成紫之後移到冷色端 289，
> 暖色只剩兒牙，冷色段多出「越冷門檻越高」這一層。**取代它的版本在六之九第七小節第 1 條**，
> 那一版比這一張好。這一張留著看推論。

> **這是量出來的，不是設計出來的。** 六個色是 2026-08-07 各自從診所實物挑的，
> 挑的時候沒有這張表；功能分組是 2026-08-08 才做的。兩者對得上，
> 表示診所實物本身就帶著這個秩序 —— 也表示**這張表可以拿來預測下一個色該落在哪**。

### 四、這一節能拿來做什麼

1. **新增科別時，色相的位置已經被決定了。**
   先問「病人在這一科付出的是情緒、習慣、還是時間與錢」，答案直接指定色相段。
   例如口腔黏膜／顏面疼痛 → 情緒 → 暖色段；美容牙科／貼片 → 規劃 → 冷色段。
2. **色盤其實已經滿了，不要無限加科別色。**
   六個色之間只剩兩段空隙，兩段都不能用：
   **35→100 的黃綠帶**是兒牙第三輪「泛綠、嘔吐物感」被否決的那一帶；
   **227→3 的紫紅帶**，紅的部分是磚紅、而紅在品牌裡的暗示性太強（六之二）。
   ~~紫的部分使用者明確不要（第四節 B）。~~
   **2026-08-09 更正：紫的部分開放了**，使用者主動要求口腔外科往靛／紫走（第六之九節）。
   第四節 B 那句「不要帶紫」講的是**內文底色**（一整片 L\* 88 的淡紫），
   不是一顆 L\* 40 的深紫藥丸 —— 底色那條仍然有效。
   所以再加科別**只能擠既有的三段**，那會壓縮現有六色之間的辨識度。
3. **故事類內容的位置**（COPY.md 第六節未決的第 2 題）——
   這一節給的建議是：**故事不要再拿一個新顏色**。
   「主題與科別」裝的故事若屬於某一科（療程小故事），就吃那一科的色；
   屬於診所本身的（開業史、四十多年）走中性灰那一支，和「全部」同族 ——
   理由和「全部」不上色是同一個：那不是第七科。**這是建議，不是定案。**
4. **`口腔外科` 與 `顯微根管` 目前沒有 chip**（沒有對應文章）。
   有了文章要補的時候，顏色與意象在這裡都已經備好了。

### 五、這一節**不是**對外文案

上面那些詞（「被灰包住的紅」「花謝了枝條還在」）是**內部推導用的**，
拿去當頁面上的字會立刻踩到 COPY.md 第二、三節：不出現「牙齒」、主詞要是人、
不要行銷／品牌腔 —— 第六節已經為了同一件事否決過六輪。
要把這些變成使用者看得到的字，得重新走 COPY.md 的規則，**不要直接搬。**

---

## 六之九、口腔外科改靛／紫（2026-08-09，五案提案中）

使用者給了口腔外科完整的定位，然後說：

> 「我感覺這個意象和目前顏色有點衝突（血色的意象似乎怪怪的），
> 心裡覺得應該會是紫色或靛色（先有高技術門檻的專業感），
> 再加入降低彩度或色相或其他方式達到支持、堅定、不喧賓的感覺。」

> **定案（2026-08-09）：填色 `#8e6299`、白底的字 `#784e84`。**
> 已套進 `index.html` 與 `assets/style.css`。`--taupe` 本身留著 ——
> 診所資訊的「看診時間」卡還在用它。
> **`main` 上仍然是原本的 Deep Taupe**，這個分支不會自動部署，要上線得先合併。
> 推導從下面第一小節開始，定案的驗算在第八小節最後。

### 一、新的定位（使用者給的，這是推導的起點）

| | |
| --- | --- |
| 目標族群 | 高中後期～研究所、剛進社會的青年；以及**怕拔智齒所以和智齒相伴多年的中年人** |
| 中年人為什麼終於來 | 智齒部分露出不好清潔、或和第二大臼齒有間隙，沒睡好、較累的時候就脹痛（怕，所以蠻能忍） |
| 他們想找的 | 技術好、拔牙不痛、快 |
| 實際上是什麼 | 切開、翻瓣、修骨，甚至把牙齒切半取出 —— **一定是手術等級** |
| 醫師要顧的風險 | 下顎神經管、上顎鼻竇穿孔 |
| 診所要做的事 | 說明清楚它不簡單 → 病患有信心交付 → **病患自己也接受並承擔風險**（真出事醫師有對應方法） |

### 二、為什麼「血色」這個意象站不住 —— 使用者是對的

六之八節把外科寫成「被灰包住的紅」，理由是 Taupe 的色相 3 是色票裡最靠近正紅的一個。
**那是從顏色反推意象，不是從意象推顏色 —— 順序反了。**

照新的定位重推，這一科的病人真正付出的不是痛，是**風險**；
診所給的也不是安撫，是**說明**。整條鏈變成：

| | |
| --- | --- |
| 對象 | 忍了很多年、終於決定處理的人 |
| 動作 | **交付** |
| 病人付出的 | **風險**（聽懂之後自己承擔） |
| 意象 | **鋪在四周的那塊布** |

> 意象改成手術巾是有依據的，但要說得準確：
> **外科真正拿來對抗血色的顏色是藍綠與藍**（手術衣、手術巾用血的補色，
> 是為了讓術者不留殘像、看得清楚）。那個方向本身就證實了使用者的直覺 ——
> **外科的顏色不該是血，應該是外科自己用來抵銷血的那一端。**
>
> 但**那兩段色相在這套系統裡已經被佔走了**：一般牙周 174、矯正 203。
> 所以往同一個方向再走一步，落在冷色端還空著的地方 —— 也就是靛與紫。
> **不要把「紫＝手術巾的顏色」寫成事實，手術巾不是紫的。**
> 站得住的是那塊布的**功能**：它把範圍框出來（堅定）、
> 讓注意力不在血上（不喧賓）、它不是主角但少了它做不成（支持）。

### 三、這一輪和兒牙那十一輪的差別：完全沒有被對比度綁住

先講一個讓這一輪好做很多的事實。L\* 40 這一帶各色相的彩度上限：

```
h 280°  50.6      h 300° 106.8      h 320°  98.4
h 290°  69.1      h 307° 119.0      h 340°  73.9
```

**上限是 50~124，而五案只用到 22。** 兒牙那十一輪是「要亮才像黃、白字又把亮度鎖死」，
兩頭夾殺；靛紫在這個亮度帶連邊都碰不到。所以：

> **五案的亮度全部固定在 L\* 40**（＝官方色票灰紫自己的亮度），
> 白字 6.44~6.50、對卡 5.85~5.91、對紙 5.08~5.13 —— **五案幾乎一模一樣**。
> 這一排比的**純粹是色相與彩度**，沒有任何一案是被對比度逼出來的。
> 這也表示口腔外科可以維持**一個變數就夠**（不像兒牙要三個），
> 文章內頁的字階也直接用原值，不必另外開一階。
>
> **⚠ 這一句在第二輪被收回了（見第八小節）** —— 使用者要求填色與白底的字分開，
> 而那個要求正好解開了「一個值被紙上的 L\* 43.4 綁住」的天花板。**收回是好事。**

L\* 40 是這樣定的：白字要 4.5:1 的上限約 L\* 49.8、當字疊在卡上 47.4、
**疊在紙上 43.3（最緊的一關，六之六節那一條）**。取 40 三關都有餘裕，
而且和其餘五科同一帶（36~44）。

### 四、五案

固定：**L\* = 40、白字**。變數只有兩個：**色相（靛↔紫）× 彩度（收斂↔存在感）**。
① 是錨點 —— 官方色票裡唯一的紫，地位等同兒牙那一輪的含羞草。

| | 底 | L\* | C\* | Lab h | HSL H | 白字 | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **① 官方灰紫**（色票原值） | `#5F5D66` | 40.0 | 5.6 | 299° | 253 | 6.47 | 5.89 | 5.11 |
| **② 靛・收斂** | `#5e5c70` | 39.9 | 12.3 | 296° | 246 | 6.48 | 5.90 | 5.12 |
| **③ 靛・存在感** | `#5c5b7f` | 40.1 | 22.0 | 294° | 242 | 6.45 | 5.86 | 5.09 |
| **④ 紫・收斂** | `#685a6b` | 40.1 | 11.9 | 320° | 289 | 6.44 | 5.85 | 5.08 |
| **⑤ 紫・存在感** | `#6f5575` | 39.9 | 22.0 | 320° | 289 | 6.50 | 5.91 | 5.13 |

彩度的兩階是照家族自己的範圍定的：其餘五科（不含兒牙）落在 C\* 8.1~19.9，
**22 是貼著家族上限、12 是家族中位** —— 「不喧賓」的標準不是低到看不見，
是不要超過贋復太多。兒牙是 57.6，五案沒有一個接近它的一半。

### 五、辨識度：這一輪真正的篩選條件

對比度五案全過，所以決定勝負的是**它和已經在用的色分不分得開**（CIE76 ΔE）：

| | 對贋復 `#252d4a` | 對矯正 `#3C596B` | 對全部 `#5f5d5c` | 對根管 `#5D6D55` |
| --- | --- | --- | --- | --- |
| ① 官方灰紫 | 25.5 | 12.6 | **6.1 ✗** | 21.7 |
| ② 靛・收斂 | 22.4 | 11.7 | 12.8 | 28.1 |
| ③ 靛・存在感 | 21.3 | 16.3 | 22.5 | 37.5 |
| ④ 紫・收斂 | 24.2 | 16.3 | 12.0 | 28.0 |
| ⑤ 紫・存在感 | 24.2 | **22.8** | 22.2 | 38.1 |

**① 出局，而且是唯一硬性出局的。** 官方灰紫 L\* 40.0、C\* 5.6，
「全部」那顆灰是 L\* 39.6、C\* 1.0 —— **亮度只差 0.4，ΔE 只有 6.1**。
在藥丸那個尺寸上兩顆會讀成同一個色，而「全部」不是一科（六之八第一節），
科別色和它撞在一起是最不能撞的一種。

> 這正好是第四節 B「灰是比出來的，不是調出來的」的另一面：
> **一個彩度 5.6 的紫，旁邊站著一個彩度 1.0 的灰，它就不是紫了。**

**靛（②③）還有一個結構性的問題**：HSL 色相 242~246，
夾在矯正 203 與贋復 227 中間再往外一點點 —— 冷色端三顆會擠在 203/227/242 的 39° 內。
紫（④⑤）的 289 離贋復 62°，是唯一真正走出去的。

### 六、目前的判斷

**⑤ 紫・存在感 `#6f5575`** 是數字上最乾淨的一案：對四個現役色的 ΔE 全部 ≥22，
色相 289 在整個色盤上沒有鄰居，彩度 22 貼著家族上限但沒超過兒牙的四成。
要更收斂就往 ④，要讀起來像靛就往 ③（代價是和贋復擠在一起）。

**這是建議，不是定案。** 對應的提案頁見本節最後。

### 七、如果改了，有四件事要跟著改

1. **六之八的三組會重排，而且會變好。**
   外科離開「情緒」組，冷色端變成 **矯正 203 → 贋復 227 → 外科 289**，仍然連續。
   分組的意思要跟著改寫：

   | 色溫 | 科 | 病人付出的 | 為什麼 |
   | --- | --- | --- | --- |
   | 暖 | 兒牙 35 | 情緒 | **唯一不需要被說服、只需要不害怕的科** |
   | 綠 | 根管 100、一般牙周 174 | 習慣 | 靠一直回來 |
   | 冷 | 矯正 203、贋復 227、外科 289 | 時間／錢／**風險** | **要先聽懂，才決定交付** |

   > 冷色段還多出一個順序：**越冷，門檻越高**（時間 → 錢與規劃 → 風險與技術）。
   > 六之八寫的版本沒有這一層，這一輪的定位把它逼出來了。

2. **Taupe `#7D5A58` 被釋出，但沒有浪費** —— 它還在診所資訊的「看診時間」卡
   （`.info-card[data-hue="taupe"]`）。而且釋出之後，暖紅那一整段就沒有任何科別在用，
   和「紅在品牌裡的暗示性太強」那條原則反而更一致。

3. **第四節 B「不要用暖色偏／不要帶紫的感覺」要標清楚適用範圍，不是推翻。**
   那句話的出處是**內文底色** —— G1 灰紫 `#dedce5`（L\* 88、整片鋪滿）使用者說「有點紫紫的」。
   **一整片 L\* 88 的淡紫，和一顆 L\* 40 的深紫藥丸不是同一件事**，
   大面積的色偏會決定整頁的調性，藥丸只有幾百個像素。兩件事不衝突，
   但**底色那條仍然有效，不要拿這一節去改底色。**

4. **六之八第四小節第 2 條要改寫。** 原文說「227→3 的紫紅帶不能用」，
   理由是紫不要、紅太強。**紫的部分開放了，紅的部分不變。**

### 八、第二輪：⑤ 選定，拆成填色與白底兩個值（2026-08-09）

使用者選了 ⑤，同時提出：

> 「標籤在白底時候的跟套色的可以是兩個不一樣的。套色的明度不太夠，
> 可以再稍微拉一點點彩度跟明度，讓它套色下去後就是有點**燈亮**的感覺，可以再清楚一點。
> 白底的時候現在看起來有點**紫黑紫黑**的。」

#### 這個要求本身解開了一個天花板

第三小節說「口腔外科可以維持一個變數就夠」—— **那句話要收回，而且收回是好事。**
一個值同時要當底又要當字的時候，它被最嚴的那一關綁著：

| 這個值要做什麼 | 天花板 |
| --- | --- |
| 當填色的底，上面壓白字 | L\* 49.9 |
| 當字疊在卡 `#f4f4f5` 上 | L\* 47.3 |
| **當字疊在紙 `#e2e5e6` 上（文章內頁）** | **L\* 43.4 ← 綁住一切的那一關** |

所以 ⑤ 只能停在 39.9。**拆成兩個值之後，填色那一側只剩白字這一關，
天花板變成 49.9 —— 一口氣多了 6.5 階亮度。**
使用者要的「燈亮」在單一變數下做不到，是拆開才做得到的。

機制不用發明：`--accent`（填色）／`--accent-deep`（白底的字）／`--on-fill`
兒牙已經在用，**外科會是第二個用兩階的科**。

> ⚠ 那三個變數的預設值那一行**必須排在科別色前面**。
> `[data-spec]` 和 `[data-spec="surg"]` 權重相同，排在後面會把深階字蓋回去。
> 兒牙踩過一次，見六之二節最後。

#### 填色：往「燈亮」走（色相鎖 320°，明度與彩度一起往上）

| | 底 | L\* | C\* | 白字 |
| --- | --- | --- | --- | --- |
| ⑤ 基準 | `#6f5575` | 39.9 | 22.0 | 6.50 |
| **F1 亮一階** | `#7a5a82` | 42.9 | 27.0 | 5.81 |
| **F2 亮兩階** | `#865f90` | 46.0 | 32.4 | 5.18 |
| **F3 頂著上限** | `#8e6299` | 48.0 | 36.0 | 4.82 |

F3 是白字撐得住的最後一階（L\* 50 就掉到 4.4 附近）。
F2、F3 拿去當字不夠（對卡 4.71／4.38）—— **但那正是拆開的意義，它們不必當字。**

#### 白底的字：三案是診斷式的，各只動一個變數

「紫黑」有兩個可能的成因：亮度不夠，或彩度不夠。**所以三案不是三個口味，
是三個假設** —— 使用者選哪一個，就直接回答了成因是哪一個。

| | 值 | L\* | C\* | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- |
| ⑤ 基準 | `#6f5575` | 39.9 | 22.0 | 5.91 | 5.13 |
| **T1 只拉彩度** | `#784e84` | 39.8 | 36.2 | 5.93 | 5.15 |
| **T2 只拉明度** | `#775d7d` | 43.1 | 21.8 | 5.24 | 4.55 |
| **T3 兩個一起** | `#7f578a` | 43.0 | 33.9 | 5.26 | 4.57 |

> **T1 是免費的**：亮度一度沒動，所以對比和 ⑤ 幾乎一樣，純粹多拿了彩度。
>
> 會先問「是不是彩度」是**兒牙那一輪學到的**：當時「拿鐵感」的成因其實是彩度不足，
> 卻挪了四輪色相都沒打中（六之二第四輪）。這次先把兩個成因分開來問，不要一起調。

**T2、T3 貼著紙的天花板 43.4**（對紙 4.55／4.57，過了但餘裕不多）。
再亮就得像顯微根管與兒牙那樣替文章頁另開一階（六之六節），**四案目前都不必**。

#### 定案：T1 配 F3（使用者選的）

```css
[data-spec="surg"] { --accent: #8e6299; --accent-deep: #784e84; }
```

**這個選擇同時回答了診斷題：「紫黑」的成因是彩度不足，不是亮度。**
T1 的亮度和 ⑤ 差 0.1，一度沒動 —— 解決問題的是彩度從 22.0 拉到 36.2。
第三輪的兩個假設之中，亮度那一支（T2）落選了。

而且兩階剛好長成整個色盤裡最乾淨的一組：

| | 填色 F3 | 深階 T1 | 差 |
| --- | --- | --- | --- |
| L\* | 48.0 | 39.8 | **8.2** |
| C\* | 36.0 | 36.2 | −0.1 |
| Lab h | 320.4° | 319.9° | 0.5° |

**色相與彩度完全相同，只差亮度。它不是「兩個色」，是同一個色的兩個高度。**
兒牙那一組（`#c28229` / `#9e6301`）色相差 6°、彩度差 20 ——
那是被物理逼出來的；這一組是選出來的。

#### 每一個實際用到的地方都驗過

| 用在哪 | 用哪一階 | 對比 | 門檻 |
| --- | --- | --- | --- |
| 醫師藥丸（實心＋白字 11.5px） | `#8e6299` | 4.82 | 4.5 ✓ |
| chip 選取（實心＋白字 13px） | `#8e6299` | 4.82 | 4.5 ✓ |
| 文章內頁 實心藥丸／按鈕（白字） | `#8e6299` | 4.82 | 4.5 ✓ |
| chip 未選（字與框，底是卡） | `#784e84` | 5.93 | 4.5 ✓ |
| 文章卡標籤（字與框，底是卡） | `#784e84` | 5.93 | 4.5 ✓ |
| 文章內頁 連結／`h3`（底是紙） | `#784e84` | 5.15 | 4.5 ✓ |
| 小節短棒／`.callout` 左條（底是紙） | `#8e6299` | 3.81 | 3.0 ✓ |

> **這是七科裡第一組完全沒有例外的科別色。**
> 兒牙的填色白字是 3.22（已結案的取捨）；顯微根管與兒牙都還要替文章頁另開一階字色
> （六之六節）。這一組兩者皆免 —— 兩個值就把七個位置全部蓋掉。

ΔE 也是分得最開的一組：對六個現役色最小 30.4（深階對贋復），其餘落在 34~81。

#### 要盯的一件事：填色比字亮 8.2 階

chip 的未選與選取會有明顯的深淺差。兒牙也是這樣（填色 59.5、字 47），
所以不是新的行為 —— 但兒牙是被逼的，外科是選的。上線前在真機上看一眼。

### 九、提案頁

`/history/spec-surg-violet.html` —— 第一輪五案各自套在三個真實元件上（實心藥丸、
未選的 chip、文章卡標籤）並和其餘六科並排；第二輪的兩條梯子可以任意組合，
同時看首頁藥丸、chip 兩態、文章卡標籤、以及文章內頁（底是紙不是卡）。
網址可帶 `?f=f2&t=t1`。

---

## 六之十、贋復假牙的藍提亮（2026-08-09，3×3 提案中）

使用者說：

> 「植牙假牙重建的標籤目前是很深的藍色。白底的藍色不要那麼深，稍微淺一點點；
> 套色下去後的藍色標籤也是很深，可以比照像紫色那樣再亮一點。」

> **定案（2026-08-09）：填色 `#4a5789`（F2）、白底的字 `#364067`（T1）。**
> 已套進 `index.html` 與 `assets/style.css`。`--tile` 本身留著 ——
> 它是第二節的材質實測值，只是 `prosth` 不再直接指向它。
> 推導從下面第一小節開始，定案的驗算在第六小節。

提案頁 `/history/spec-prosth-blue.html`，網址可帶 `?f=f1|f2|f3&t=t1|t2|t3`。

### 一、「很深」是量得出來的，不是感覺

| 科別 | 色 | L\* | C\* | 白字 |
| --- | --- | --- | --- | --- |
| 兒童牙科（填色） | `#c28229` | 59.5 | 57.6 | 3.22 |
| 口腔外科（填色） | `#8e6299` | 48.0 | 36.0 | 4.82 |
| 顯微根管 | `#5D6D55` | 44.1 | 15.9 | 5.55 |
| 一般・牙周 | `#4f6361` | 40.3 | 8.1 | 6.38 |
| 齒顎矯正 | `#3C596B` | 36.3 | 14.8 | 7.41 |
| **贋復假牙（現行）** | **`#252d4a`** | **19.1** | 19.9 | **13.52** |

其餘六科擠在 L\* 36~48 的十二階裡，**贋復一個人低了十七階**，
白字 13.52 是 AA 門檻的三倍。

**當初這樣定的理由已經消失了。** 六之二節寫的是「口外與贋復是不同科、顏色不要重複，
贋復拿整個色盤裡最深的那一個」—— 但口腔外科在六之九節換成 H 288 的紫之後，
離贋復的 227 有六十幾度，**不必再靠深度去分**。這一輪不是推翻決定，
是那個決定的前提被上一輪拆掉了。

### 二、⚠ 彩度不能不動 —— 這一輪唯一真正的技術判斷

直覺做法是「色相不動、彩度不動，只提明度」。**那樣會壞，而且壞在看不見的地方：**

| L\* | 固定 C\*=19.9 的結果 | HSL 彩度 | ΔE 對矯正 |
| --- | --- | --- | --- |
| 19.1（現行） | `#252d4a` | 33% | 21.3 |
| 30 | `#3e4564` | 23% | 14.0 |
| 34 | `#484e6e` | 21% | 13.0 |
| **38** | `#525878` | **19%** | **12.5** |
| 42 | `#5b6182` | 18% | 13.7 |
| 46 | `#656b8d` | 17% | 16.0 |

深藍的彩度天花板隨亮度長得很快（h 287 的 maxC：L\* 19 是 37.5，L\* 48 是 70.4），
**C\* 固定不動就等於一路變灰** —— HSL 彩度掉一半，ΔE 對齒顎矯正掉到 12.5。
兩顆冷色藥丸常常並排在同一張卡片旁邊，這個距離不夠。
（對照六之九：① 官方灰紫對「全部」只有 6.1，是硬性出局；12.5 沒那麼糟，但也不該接受。）

> **所以六個值的彩度都跟著亮度按色域比例放大** —— 固定在現行值自己的位置
> （該亮度下彩度上限的 **51%**），色相鎖死在 **Lab 286.7°**。
> HSL 彩度就一路守在 29~32%（現行 33%），ΔE 對矯正維持 18~29。
> **顏色的個性一度沒變，變的只有高度** —— 等於同一塊茶室磁磚被燈打亮，
> 和六之八節給贋復的意象（茶室裡坐下來談）是同一件事。

**這一輪離開了實測值**：茶室深藍磁磚三個取樣是 `#0c111c` / `#1d273d` / `#252d4a`，
現行值已經是其中最亮的一個，再往上就沒有照片可以錨。六個值全部是從 `#252d4a`
自己延伸出來的，不是另外挑的藍 —— 這是刻意的，**不要改成從別處取色**。

### 三、兩條梯子

拆成兩個值的機制和外科、兒牙一樣：`--accent`（填色）／`--accent-deep`（白底的字）。
天花板只跟明度有關、跟色相無關，所以下面三個數字對每一科都一樣：
白字 4.5:1 → L\* 49.8；當字疊在卡 `#f4f4f5` → 47.4；**當字疊在紙 `#e2e5e6` → 43.4（最緊）**。

**填色（套色）—— 只受白字那一關限制：**

| | 值 | L\* | C\* | HSL | 白字 | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 現行 | `#252d4a` | 19.1 | 19.9 | 227/33% | 13.52 | 12.30 | 10.68 |
| **F1 亮一階** | `#3a456d` | 30.1 | 25.7 | 227/31% | 9.33 | 8.48 | 7.36 |
| **F2 亮兩階** | `#4a5789` | 38.0 | 30.8 | 228/30% | 6.96 | 6.34 | 5.50 |
| **F3 頂著上限** | `#5e6fac` | 47.9 | 36.1 | 227/32% | 4.83 | 4.40 | 3.82 |

F3 和外科的填色站在同一個高度（47.9 對 48.0、白字 4.83 對 4.82），
**兩科的視覺重量會完全一致**。F3 對紙 3.82 也過得了小節短棒／`.callout` 左條的 3:1。

**白底的字 —— 被紙的 L\* 43.4 綁著：**

| | 值 | L\* | C\* | HSL | 對卡 | 對紙 |
| --- | --- | --- | --- | --- | --- | --- |
| 現行 | `#252d4a` | 19.1 | 19.9 | 227/33% | 12.30 | 10.68 |
| **T1 淺一點點** | `#364067` | 27.9 | 25.3 | 228/31% | 9.16 | 7.95 |
| **T2 中間** | `#44507e` | 34.9 | 28.8 | 228/30% | 7.09 | 6.15 |
| **T3 進家族帶** | `#526197` | 42.1 | 32.7 | 227/30% | 5.43 | 4.72 |

T3 的 42.1 和外科深階 39.8、一般牙周 40.3 同一帶，對紙留 4.72（外科那一輪接受過 4.55）。
**三案都不必替文章內頁另開第三階** —— 顯微根管與兒牙都要，這一科免。

### 四、九個組合裡有三個是反向的

「填色 L\* − 字 L\*」，正數才和系統一致（外科 +8.2、兒牙 +12.5）：

| | T1 | T2 | T3 |
| --- | --- | --- | --- |
| **F1** | +2.1 | **−4.9 反向** | **−12.1 反向** |
| **F2** | +10.1 | +3.0 | **−4.1 反向** |
| **F3** | +20.0 | +13.0 | +5.8 |

反向的意思是 **chip 按下去反而變深**，和全站行為相反。提案頁那三格畫虛線框。

### 五、ΔE：六個值對現役色

| 值 | 對矯正 | 對一般牙周 | 對根管 | 對全部 | 對外科填色 | 對外科深階 |
| --- | --- | --- | --- | --- | --- | --- |
| 現行 `#252d4a` | 21.3 | 30.9 | 42.9 | 29.1 | 36.6 | 30.4 |
| F1 `#3a456d` | **18.0** | 29.7 | 42.8 | 28.0 | 27.2 | 22.5 |
| F2 `#4a5789` | 21.6 | 33.0 | 46.0 | 31.5 | 22.1 | 19.6 |
| F3 `#5e6fac` | 28.7 | 38.7 | 50.9 | 37.7 | **21.0** | 22.2 |
| T1 `#364067` | 18.7 | 30.2 | 43.3 | 28.5 | 28.5 | 23.3 |
| T2 `#44507e` | 19.7 | 31.3 | 44.5 | 29.8 | 23.7 | 20.2 |
| T3 `#526197` | 23.8 | 34.7 | 47.4 | 33.5 | 21.1 | 20.1 |

**越往上走，離齒顎矯正越遠、離口腔外科越近** —— 兩端都有牆，但兩端都還有空間。
最小的是 F1 對矯正的 18.0，六個值全部安全（出局線是 6.1）。

### 六、定案：F2 配 T1（使用者選的）

```css
[data-spec="prosth"] { --accent: #4a5789; --accent-deep: #364067; }
```

我建議的是 F3·T1（填色頂著上限、和外科同高度）。**使用者選了 F2 —— 而且他是對的。**
F3 的填色比字亮 20.0 階，chip 按下去會跳一大截；F2 收在 **+10.1**，
和外科的 +8.2 同一個量級，整個色盤的深淺行為就一致了。
「比照紫色」真正該比照的是那個**落差**，不是「也頂著上限」。

| | 填色 F2 | 深階 T1 | 差 |
| --- | --- | --- | --- |
| L\* | 38.0 | 27.9 | **10.1** |
| C\* | 30.8 | 25.3 | 5.5 |
| Lab h | 287.2° | 287.3° | 0.1° |

**每一個實際用到的地方都驗過：**

| 用在哪 | 用哪一階 | 對比 | 門檻 |
| --- | --- | --- | --- |
| 醫師藥丸（實心＋白字 11.5px） | `#4a5789` | 6.96 | 4.5 ✓ |
| chip 選取（實心＋白字 13px） | `#4a5789` | 6.96 | 4.5 ✓ |
| 文章內頁 實心藥丸／按鈕（白字） | `#4a5789` | 6.96 | 4.5 ✓ |
| chip 未選（字與框，底是卡） | `#364067` | 9.16 | 4.5 ✓ |
| 文章卡標籤（字與框，底是卡） | `#364067` | 9.16 | 4.5 ✓ |
| 文章內頁 連結／`h3`（底是紙） | `#364067` | 7.95 | 4.5 ✓ |
| 小節短棒／`.callout` 左條（底是紙） | `#4a5789` | 5.50 | 3.0 ✓ |

和外科一樣**沒有任何例外**，也不必替文章內頁另開第三階（顯微根管與兒牙都要）。
餘裕還很大 —— 深階對紙 7.95，離 4.5 有得是空間，之後想再提亮不必重來。

### 七、改了哪兩行

```css
/* assets/style.css：原本 --accent 與 --accent-deep 都是 var(--tile) */
/* index.html：      原本只有 --accent: var(--tile)，深階由預設那行帶成同一個值 */
[data-spec="prosth"] { --accent: #4a5789; --accent-deep: #364067; }
```

⚠ `[data-spec]` 的預設值那一行**必須排在 `[data-spec="prosth"]` 前面**，
否則深階字會被蓋回 `var(--accent)`（六之二節最後那一條，兒牙踩過）。
兩個檔案都已經是這個順序。

---

## 六之十一、顯微根管改紅色系（2026-08-09，五案提案中）

使用者：「顯微根管我要用紅色系，從配色資料庫抓紅色，做五個版本。」

> **定案（2026-08-09）：套色 `#ae4f4d`、白底的字 `#89202d`。**
> 已套進 `index.html` 與 `assets/style.css`。**深階是實物原值** ——
> 開幕茶會門口那籃花的實測色（第三節花藝）。
> `--moss` 本身留著（它是第一節色票裡的 Logo 色），只是不再有科別指向它，
> 文章頁原本那階 `#596951` 也一併拿掉了。
> 走到這裡花了六輪，推導從下面第一小節開始，定案的驗算在最後一節。

最新的提案頁是 **`/history/spec-endo-red-all.html`**（十一案 × 兩條切換條，`?l=6&f=11`）。
`/history/spec-endo-red.html`（正規化的五案）與 `/history/spec-endo-red-raw.html`（三個原值）
留著看推論過程。

### 一、要放掉的兩件事（先攤開，不是反對）

1. **顯微根管現在拿的是 Logo 自己的色。** 苔綠 `#5D6D55` ——
   六之八節寫的「七科裡最強的一個對應」。改成紅之後，**科別色裡不再有 Logo 色**。
2. **「紅在品牌裡的暗示性太強」是使用者自己定過的原則**（六之二節，兒牙離開磚紅的理由）。

反過來說有兩件支持它的：暖紅那一整段從外科改紫之後就空著；
而且**紅在這一科比在外科站得住** —— 六之九節否決外科的血色是因為「病人付出的是風險不是痛」，
但根管真正在保留的就是牙齒裡活著、有血流的那一部分，紅在這裡不是傷口，是還活著的證據。

### 二、資料庫裡的紅，統一亮度之後只剩三個

| 來源 | 色 | L\* | C\* | Lab h |
| --- | --- | --- | --- | --- |
| 官方色票・磚紅 | `#AF4C52` | 45.5 | 44.2 | 22 |
| 門口紅花籃 | `#89202d` | 30.7 | 48.4 | 23 |
| 紅花籃（另一張） | `#731923` | 25.1 | 43.0 | 24 |
| 繡球 | `#6c1226` | 22.9 | 41.5 | 18 |
| 磚紅長椅 | `#7c3721` | 32.1 | 39.7 | 44 |
| 色票・Deep Taupe | `#7D5A58` | 41.8 | 15.7 | 26 |
| 單椅布料 | `#8e7577` | 51.6 | 10.4 | 14 |

> **⚠ 發現一：磚紅、紅花籃、繡球其實是同一個色的三個亮度。**
> 照片裡差很多，但那個差幾乎全部是亮度（45.5 / 30.7 / 22.9）——
> 色相 18~23、彩度 41~48 幾乎一樣。統一拉到 L\* 46 之後彼此只差
> **ΔE 4.0~8.0**，藥丸尺寸下是同一顆。
>
> **⚠ 發現二：低彩度那一端被「看診時間」卡佔住。**
> Deep Taupe 與單椅布料是同一族（彼此 ΔE 6.1），而 **Taupe 還在
> `.info-card[data-hue="taupe"]` 上**。拿它那個彩度做科別色 ΔE 只有 **4.4**，
> 比讓官方灰紫硬性出局的 6.1 還近。

**所以可用範圍是被這兩件事夾出來的。**

### 三、五案（填色 L\* 46、深階 L\* 38，只動色相與彩度）

亮度差 7.9，和外科的 +8.2、贋復的 +10.1 同一量級。深階彩度照色域比例縮
（贋復那一輪定下的規則）。**五案對比度幾乎一模一樣**：白字 5.18~5.21、
深階對卡 6.31~6.36、對紙 5.48~5.52、填色對紙 4.09~4.11 —— 沒有一案是被對比度逼出來的。

| | 填色 | 深階 | C\* | Lab h | 依據 |
| --- | --- | --- | --- | --- | --- |
| **① 磚紅・收斂** | `#9b5b5d` | `#804b4c` | 28 | 21 | 彩度收到剛好脫離 Taupe |
| **② 磚紅・官方**（錨點） | `#b04d53` | `#923f44` | 44.2 | 22 | **色票磚紅的色相彩度原值**，只搬亮度 |
| **③ 酒紅** | `#b14b62` | `#923d50` | 44 | 10 | 往繡球（h 18）那一端 |
| **④ 燒磚** | `#a85541` | `#8b4635` | 42 | 40 | 往磚紅長椅（h 44）那一端 |
| **⑤ 磚紅・滿彩** | `#bf3f4d` | `#9f333e` | 56 | 22 | 貼著兒牙 57.6，**超出資料庫實測** |

### 四、辨識度：只有兩個鄰居要盯

| 案 | 填色對 Taupe | 深階對 Taupe | 填色對兒牙深階 |
| --- | --- | --- | --- |
| ① | 13.5 | **9.5** | 45.4 |
| ② | 28.8 | 23.0 | 44.7 |
| ③ | 29.9 | 24.0 | 53.9 |
| ④ | 27.6 | 21.8 | **31.3** |
| ⑤ | 40.6 | 33.6 | 48.4 |

冷色三科距離都在 31 以上，不是問題。**① 靠 Taupe、④ 靠兒牙**，其餘三案兩邊都寬。
① 的深階對「全部」`#5f5d5c` 也只有 23.5 —— 低彩度那一端兩面受擠。
五案彼此最接近的一對是 ②③（9.3）。

### 五、目前的建議

**② 磚紅・官方。** 它不是調出來的 —— 是色票裡唯一的紅，而且花籃與繡球在同一亮度下
都落在它 4.4 / 4.0 以內，**三個實測紅一起指向同一個答案**。
這個形狀出現過兩次（兒牙的含羞草、外科的官方灰紫），兩次都證明那個錨是對的起點。
**④ 燒磚建議避開** —— 暖色段只剩兒牙，再放一個 h 40 的紅會互相干擾。

### 五之二、第二輪：使用者說「這五個都是磚紅」（2026-08-09）

**他是對的，而且那是第三小節那一步造成的。**

> 五案把亮度全部鎖在 L\* 46。**但磚紅、紅花籃、繡球本來的差別幾乎全部就在亮度上**
> （45.5 / 30.7 / 22.9）—— 色相 18~23、彩度 41~48 三個本來就很接近。
> 亮度一拉平，剩下的差只有 ΔE 4~8，當然看起來是同一顆磚紅。
>
> **這是第二小節「發現一」的另一面，我當時只用它來刪選項，沒發現它同時也預告了
> 五案會長得一樣。** 原值彼此是 ΔE 11.4~23.0，是分得開的。

原值版提案頁 `/history/spec-endo-red-raw.html`：三個紅一個數字都沒動，
各做**白底套字**（字與整圈框都是該色）與**整圈套色、字反白**兩態，
卡 `#f4f4f5` 與紙 `#e2e5e6` 兩種底都看。

| 紅 | L\* | C\* | h | 套字・卡 | 套字・紙 | 反白 |
| --- | --- | --- | --- | --- | --- | --- |
| 官方磚紅 `#AF4C52` | 45.5 | 44.2 | 22 | 4.80 ✓ | **4.17 ✗** | 5.28 ✓ |
| 門口紅花籃 `#89202d` | 30.7 | 48.4 | 23 | 8.28 ✓ | 7.18 ✓ | 9.10 ✓ |
| 繡球 `#6c1226` | 22.9 | 41.5 | 18 | 10.90 ✓ | 9.47 ✓ | 11.99 ✓ |
| （現行苔綠 `#5D6D55`） | 44.1 | 15.9 | 134 | 5.05 ✓ | **4.38 ✗** | 5.55 ✓ |

**原值這一輪完全沒有撞色問題**：三個紅對現役六科最近的也有 ΔE 41.9（磚紅對外科），
對「看診時間」卡的 Taupe 是 28.9 / 34.6 / 32.2。
上一輪那些逼近的數字，全部是被壓到 L\* 46 才產生的。

**兩件要知道的：**

1. **磚紅在紙上當字 4.17 不夠**（style.css 本來就寫著「只能待在卡上」）。
   同六之六節那一招可解：白底的字改 **`#a7484e`**（L\* 43.4、對紙 4.51、對卡 5.19，
   離原值 ΔE 2.6 肉眼看不出），整圈套色仍用原值。**花籃與繡球不需要這一步。**
2. **花籃 30.7、繡球 22.9 是「很深」的那一區。** 贋復同一天才從 19.1 提亮到 38.0，
   理由就是太深、掉出家族帶（36~48）。繡球比當時的贋復只亮 3.8 階。

**目前建議：門口紅花籃 `#89202d`。** 兩種底、兩個狀態全部直接過，不必另開字階；
彩度 48.4 三個裡最高，整圈套色最有存在感；亮度 30.7 落在另外兩個中間。而且它是實物。

### 五之三、第三輪：七案併在同一排（2026-08-09）

使用者要「兩案併起來、做成兩態、跟其他科別標籤放在一起、一鍵切換」。
提案頁 **`/history/spec-endo-red-all.html`** —— ①~⑤ 是正規化的五案（兩階），
⑥⑦ 是原值的紅花籃與繡球（單一值），七案都排進真正的七科標籤列裡，
白底套字與整圈套色兩態、卡與紙兩種底，外加一整段文章內頁模擬。

> **切換條是兩條、各自獨立**（2026-08-09 第四輪追加）：
> 上排管**白底字套色**那一階，下排管**整圈套色**那一階，網址可帶 `?l=2&f=6`。
> 兩排同號＝那一案原本設計的樣子；**兩排不同號＝當場自訂一組兩階**
> （例如白底字 ② `#923f44` 配整圈套色 ⑥ `#89202d`，資料庫裡沒有但完全成立）。
> 讀數第三行是「套色 L\* − 套字 L\*」，49 個組合的值全部由 CSS 產生；
> **負數會標「⚠ 反向」**（套色比套字暗，chip 按下去反而變深）——
> 套色選 ⑥⑦、白底字選 ①~⑤ 的組合全部是反向的，⑦ 配 ①~⑤ 差到 −15。

> **官方磚紅的原值不必另外佔一格 —— ② 就是它。**
> ② `#b04d53` 對色票原值 `#AF4C52` 只差 **ΔE 0.38**（色相彩度一樣，亮度差 0.4）。
> **唯一的差別是原值在紙上當字只有 4.17，② 壓下去的那 0.4 階正好把它救回來。**

| 案 | 填色 | 套字 | 反白 | 套字・卡 | 套字・紙 | 對 Taupe |
| --- | --- | --- | --- | --- | --- | --- |
| ① 磚紅・收斂 | `#9b5b5d` | `#804b4c` | 5.19 | 6.31 | 5.48 | **9.5** |
| ② 磚紅・官方 | `#b04d53` | `#923f44` | 5.21 | 6.33 | 5.50 | 23.0 |
| ③ 酒紅 | `#b14b62` | `#923d50` | 5.18 | 6.36 | 5.52 | 24.0 |
| ④ 燒磚 | `#a85541` | `#8b4635` | 5.20 | 6.32 | 5.48 | 21.8 |
| ⑤ 磚紅・滿彩 | `#bf3f4d` | `#9f333e` | 5.21 | 6.33 | 5.50 | 33.6 |
| **⑥ 紅花籃・原值** | `#89202d` | `#89202d` | 9.10 | 8.28 | 7.18 | 34.6 |
| **⑦ 繡球・原值** | `#6c1226` | `#6c1226` | 11.99 | 10.90 | 9.47 | 32.2 |

**⑥⑦ 在兩態的行為和 ①~⑤ 不一樣，切換時要分開看：**
整圈套色時 ①~⑤ 都是 L\* 46，⑥ 是 30.7、⑦ 是 22.9 —— **⑥⑦ 按下去明顯比其他六科重**
（其餘六科填色落在 L\* 36~60）；白底套字則反過來，①~⑤ 的套字階 L\* 38 和其他科同一帶，
**⑦ 的線框在一排裡幾乎讀成黑色**。兩態都偏重是 ⑥⑦ 的代價，換來的是不必拆兩階、而且是實物原值。

**真正的分岔是 ② 對 ⑥**：要一顆和其他六科等重的紅，還是一顆比較沉、辨識度更強的紅。
其餘五案都是繞著這兩個的變化。

#### 第五輪：使用者挑 `l6 + f4`，於是多了 ⑧（2026-08-09）

他選**白底字 ⑥ `#89202d`、套色 ④ `#a85541`**。分析下來：

| | 值 | L\* | Lab h | 家族裡的位置 |
| --- | --- | --- | --- | --- |
| 白底字 ⑥ | `#89202d` | 30.7 | 23.4 | 在「全部」31.4 與贋復 27.9 之間 —— **不是離群值** |
| 套色 ④ | `#a85541` | 45.9 | 40.1 | 在外科 48.0 與一般 40.3 之間 —— **也不是** |

**兩階各自都站得住，對比也全過**（白字 5.20、短棒對紙 4.10、字對卡 8.28／對紙 7.18）。
**問題只在兩階之間的關係：色相差 16.7°。**

| 科 | ΔL\* | Δ色相 |
| --- | --- | --- |
| 兒童牙科 | 12.3 | 0.3 |
| 口腔外科 | 8.2 | 0.5 |
| 贋復假牙 | 10.1 | −0.2 |
| **`l6 + f4`** | **15.2** | **16.7** |

現役三組都在 1° 以內 —— 六之九節那句「**它不是兩個色，是同一個色的兩個高度**」在這一組
不成立：白底字是 h 23 的胭脂紅，套色是 h 40 的陶土橘紅，兩階彼此 ΔE 21.0。

> **而且這兩階會同框，不是輪流出現。** 文章內頁的科別藥丸／短棒／重點框左條／實心按鈕
> 吃套色階，同一頁的 `h3`／連結／線框按鈕吃白底字階；首頁按下顯微根管 chip（套色）之後，
> 篩出來的文章卡標籤是白底字。**16.7° 沒有地方可以躲。**

**⑧ 燒磚・深** 就是為了這一組加的。使用者選 ⑥ 是要**深**、選 ④ 是要**暖**，
兩者同時成立需要一個「暖而且深」的紅 —— **資料庫裡剛好有：磚紅長椅 `#7c3721`**
（L\* 32.1・C\* 39.7・h 44.3，第三節家具布料實測）。

| ⑧ | 值 | L\* | C\* | h | 對比 |
| --- | --- | --- | --- | --- | --- |
| 白底字 | `#7c3721`（實測原值） | 32.1 | 39.7 | 44.3 | 對卡 7.88／對紙 6.84 |
| 套色 | `#b05032`（同色相拉到 L\* 45.9） | 45.9 | 51.7 | 44.1 | 白字 5.20／對紙 4.11 |

**⑧ 兩階色相差 0.2°**，和外科、贋復同一個等級。
`l8 + f4` 是使用者要的那組，色相差降到 4.2°；`l8 + f8` 是 0.2°。
提案頁預設已改成 `l8 + f4`。

⚠ **⑧ 的套色是七案裡最靠近兒牙的**（對兒牙深階 27.1、填色 30.6，④ 是 31.3／34.4）——
h 44 比 ④ 的 h 40 又往兒牙的 h 72 走了一點。暖色段只剩兒牙，這是 ⑧ 要付的代價。

#### 第六輪：⑨⑩⑪ —— ④ 與 ⑥ 之間補三階（2026-08-09）

使用者看過 ⑧ 之後仍然選 `l6 + f4`，要「在 ④、⑥ 之間再做三個」。
所以這三案**不動亮度、只走色相**：

- **亮度鎖在他選的那組結構**：套色 L\* 45.9（④ 自己的）、白底字 L\* 30.7（⑥ 自己的），
  ΔL\* 一律 **+15.2**，和他現在看到的完全一樣。
- **色相把 16.7° 均分成四段**：40.1 → 35.9 → 31.8 → 27.6 → 23.4。
- **彩度兩端都錨在實測值**：套色 42.2（④ 燒磚）→ 44.2（官方磚紅）；
  白底字 39.7（磚紅長椅）→ 48.4（紅花籃）。
- **每一案自己的兩階同色相**（差 0°），這正是 `l6+f4` 那 16.7° 想解掉的東西。

| | 套色 | 白底字 | 白字 | 對卡 | 對紙 | 白底字對 Taupe |
| --- | --- | --- | --- | --- | --- | --- |
| **⑨ 近④**（h 35.9） | `#aa5345` | `#7e2e24` | 5.21 | 8.29 | 7.20 | 28.8 |
| **⑩ 中點**（h 31.8） | `#ad5149` | `#822a26` | 5.19 | 8.28 | 7.18 | 30.8 |
| **⑪ 近⑥**（h 27.6） | `#ae4f4d` | `#852529` | 5.22 | 8.33 | 7.23 | 32.6 |

> **⚠ 套色那一階變化很小，這是物理不是失誤。**
> 相鄰兩案的套色只差 **ΔE 3.2**，④ 到 ⑥ 整段也只有 12.7 ——
> 套色的彩度全程守在 42~44，17° 的色相在那個彩度上就是走不遠。
> **白底字那一階差得比較開**（相鄰 3.9、整段 15.5），它的彩度從 39.7 走到 48.4。
> **看差別要看白底字那一排。**

⑧ 和 ⑨⑩⑪ 是同一個問題的兩條路：**⑧ 是「往 ④ 那端走到底並收乾淨」**
（拿實測的磚紅長椅當白底字，兩階差 0.2°，但整組更暖、更靠兒牙）；
**⑨⑩⑪ 是「在中間找一點」**。

> **⚠ 切著比對的頁面，讀數不能讓版面跳。**
> 使用者回報「點 ⑧~⑪ 和 ④ 之間切換，頁面會跳，不好比較」。
> 原因是讀數那三行的長度隨案子變（有的帶「★ 你挑的那一組」這種尾巴、
> 案名從「④ 燒磚」到「⑨ ④⑥之間・近④」差很多），窄螢幕下在一行與兩行之間
> 來回，整頁跟著位移最多 43px。**在比對用的頁面上，這比顏色本身還致命 ——
> 眼睛要重新找位置，就比不出兩個顏色的差。**
>
> 解法是把可變長度的那幾行**先把兩行的高度佔住**：
> `.readout span{display:block}` ＋ `@media (max-width:35rem){min-height:calc(2*1.85em)}`，
> 帶案名的兩個標籤同理（`@media (max-width:44rem)`，2×1.5em）。
> 量過 560px 以上讀數不會換行、704px 以上案名不會換行，所以斷點取在那裡。
>
> **附帶的坑：`span` 改成 `display:block` 之後，原本分隔用的 `<br>` 會各自
> 再佔一整行**，讀數從 77px 變成 120px。改 block 就要把 `<br>` 拿掉。
>
> 360~1280px 全部量過，同一頁 121 個組合的文件高度一致（360px 有 1px 的
> 次像素差，看不出來）。

> **這一輪順手踩到的版型坑：** 切換條是 `left:50%` ＋ `translateX(-50%)` 的
> **收縮包覆 flex 容器**，裡面放會換行的列時，寬度會塌成很窄的一條
> （十一顆擠成 188px，四行高 216px）。**要明寫寬度**
> （`width:min(calc(100vw - 1.4rem), 30rem)`），換行的那幾顆才用得到整條空間。
> 順便把每一顆按鈕改成「色塊本身＋白字編號」，高度從 216px 收到 112px ——
> 十一案的白字對比最低 5.17，編號讀得到。

**同一頁還帶一段完整的文章內頁模擬**（2026-08-09 追加），吃同一個切換條。
規格逐條照 `assets/style.css` 抄的，**哪個元件吃哪一階一格都沒改**（六之六節那張表）：
科別藥丸、小節標題前的 3px 短棒、重點框左條、實心按鈕 → 填色階；
內文連結、`h3`、線框按鈕 → 套字階；`h2` 本身與頁尾電話不變。

> **文章內頁是七案差最多的地方** —— 它同時用到兩階，而且底是紙（最嚴的一關）。
> ⑥⑦ 的 `h3` 與連結仍然明顯是紅的（有底線幫忙），但比內文墨色 `#2a2c27`
> 只淺一點，**整篇的重量會往下壓一階**。⑦ 對紙 9.47，那是「太安全」的另一面。
> ② 那一階（對紙 5.50）和其餘六科的字階同一帶，讀起來是「另一科的文章」
> 而不是「另一種頁面」。

### 六、定案：白底字 ⑥ 配套色 ⑪（使用者選的）

```css
[data-spec="endo"] { --accent: #ae4f4d; --accent-deep: #89202d; }
```

| | 值 | L\* | C\* | Lab h | 來源 |
| --- | --- | --- | --- | --- | --- |
| 套色 | `#ae4f4d` | 45.8 | 43.6 | 27.5 | ⑪，④⑥ 之間的第三階 |
| 白底的字 | `#89202d` | 30.7 | 48.4 | 23.4 | **開幕紅花籃實測原值**（第三節） |

**兩階色相差 4.1°** —— 他先挑的 `l6 + f4` 是 16.7°，補了三階之後收掉了。
ΔC\* −4.8、**ΔL\* 15.1**。

**七個實際用到的地方都驗過：**

| 用在哪 | 用哪一階 | 對比 | 門檻 |
| --- | --- | --- | --- |
| 醫師藥丸（實心＋白字 11.5px） | `#ae4f4d` | 5.22 | 4.5 ✓ |
| chip 選取（實心＋白字 13px） | `#ae4f4d` | 5.22 | 4.5 ✓ |
| 文章內頁 實心藥丸／按鈕（白字） | `#ae4f4d` | 5.22 | 4.5 ✓ |
| chip 未選（字與框，底是卡） | `#89202d` | 8.28 | 4.5 ✓ |
| 文章卡標籤（字與框，底是卡） | `#89202d` | 8.28 | 4.5 ✓ |
| 文章內頁 連結／`h3`（底是紙） | `#89202d` | 7.18 | 4.5 ✓ |
| 小節短棒／`.callout` 左條（底是紙） | `#ae4f4d` | 4.12 | 3.0 ✓ |

ΔE 也安全：套色最近的是「看診時間」卡 Taupe 28.2、白底字是 34.6。

> ⚠ **這一組的 ΔL\* 15.1 是色盤裡最大的**（兒牙 12.3、贋復 10.1、外科 8.2）。
> 方向是對的（套色比字亮，chip 按下去會亮），但落差比其他科明顯。上線後看一眼。

### 七、跟著改的四件事（已完成三件）

1. ✅ **`--moss` 被釋出且沒有別人接手**。變數留著（它是第一節色票裡的 Logo 色），
   但**科別色裡不再有 Logo 色** —— 這是這次改動最實際的損失，不是疏漏。

   > **對照頁：`/history/logo-green-compare.html`（2026-08-09，`?g=a|b|c`）。**
   > 把釋出的苔綠放回「一般牙科・定期檢查／牙周治療／創辦醫師」現在用的
   > 診療椅綠旁邊（那三個是同一個值 `--chair #4f6361`），加上家色深綠松當第三個參考點。
   >
   > | | 值 | L\* | C\* | Lab h | 白字 | 對卡 | 對紙 |
   > | --- | --- | --- | --- | --- | --- | --- | --- |
   > | 診療椅（現行） | `#4f6361` | 40.3 | 8.1 | 190 | 6.38 | 5.81 | 5.04 |
   > | 苔綠（Logo） | `#5D6D55` | 44.1 | 15.9 | 134 | 5.55 | 5.05 | **4.38 ✗** |
   > | └ 壓深的字階 | `#596951` | 42.5 | 16.0 | 134 | 5.89 | 5.36 | 4.65 |
   > | 深綠松（家色） | `#214D48` | 29.7 | 16.7 | 186 | 9.46 | 8.61 | 7.47 |
   >
   > **苔綠對診療椅只有 ΔE 13.7**，但那不是「幾乎一樣」——**兩者色相差 56°**
   > （134 偏黃綠、190 偏藍綠），只是彩度都很低（15.9 與 8.1）所以距離拉不開。
   > **低彩度的差別要靠並排才看得出來**，這就是那一頁存在的理由。
   >
   > 三件判斷依據：①苔綠比診療椅亮 3.8 階、彩度多一倍，換過去那一科會稍微浮一點；
   > ②苔綠要兩階（紙上 4.38 不夠），診療椅一個值管到底 —— 但 `#596951` 剛從
   > 顯微根管拿掉，是現成的；③**診療椅對家色深綠松 ΔH 只有 4°，它是家色的實物版**
   > （第一節），苔綠離家色 52°，是另一支。換過去等於讓「一般牙科」離家色遠一點、
   > 離 Logo 近一點。**兩邊都站得住，還沒定。**
   >
   > **2026-08-09 追加：三個錨之間每條邊補兩階，繞成一圈（椅→苔→松→椅，共九顆）。**
   > Lab 線性內插。相鄰兩階 ΔE 4.3~7.1，很細，切著看比看數字準。
   > **只有 ④ 苔綠原值在紙上不夠（4.38），其餘八顆兩種底、兩個狀態都直接過。**
   >
   > | | 值 | L\* | C\* | Lab h | 白字 | 對紙 |
   > | --- | --- | --- | --- | --- | --- | --- |
   > | ① 診療椅（錨・現行） | `#4f6361` | 40.3 | 8.1 | 190 | 6.38 | 5.04 |
   > | ② 椅→苔 ⅓ | `#54665d` | 41.5 | 9.3 | 162 | 6.11 | 4.83 |
   > | ③ 椅→苔 ⅔ | `#596a59` | 43.0 | 12.5 | 144 | 5.78 | 4.57 |
   > | ④ 苔綠（錨・Logo） | `#5D6D55` | 44.1 | 15.9 | 134 | 5.55 | **4.38 ✗** |
   > | ⑤ 苔→松 ⅓ | `#4b6251` | 39.3 | 14.3 | 151 | 6.63 | 5.24 |
   > | ⑥ 苔→松 ⅔ | `#37584c` | 34.6 | 15.5 | 168 | 7.89 | 6.23 |
   > | ⑦ 深綠松（錨・家色） | `#214D48` | 29.7 | 16.7 | 186 | 9.46 | 7.47 |
   > | ⑧ 松→椅 ⅓ | `#315450` | 33.1 | 13.8 | 187 | 8.35 | 6.60 |
   > | ⑨ 松→椅 ⅔ | `#415c59` | 36.9 | 10.9 | 188 | 7.24 | 5.72 |
   >
   > **三條邊的性格完全不同，這是這一圈最有用的地方：**
   > **①→④** 色相 190→134，藍綠轉黃綠，亮度只動 3.8 階 —— 在問「要不要往 Logo 那個綠靠」。
   > **④→⑦** 亮度掉 14.4 階、彩度只多 0.8 —— 幾乎純粹是變深。
   > **⑦→①** 色相幾乎不動（186→190），亮度升 10.6、彩度掉 8.6 ——
   > **同一個藍綠的三個高度**，和現役那些兩階色是同一種關係。
   > 換句話說 ⑧⑨ 是現成的「深綠松家族兩階」候選。
   >
   > **⑩~⑬：⑤ 再翠一點（2026-08-09）。** 使用者說 ⑤ `#4b6251` 的亮度套色下去
   > 不錯，但想要「新鮮一點的綠、再翠一點，可是不能真的太翠，太翠綠會喧賓」。
   >
   > > **剛好的事：⑤ 的色相就是枝葉的色相。** 第三節量過枝葉亮處 `#447c56`，
   > > Lab 色相 **151.2**；⑤ 是 **151.1**。使用者挑中的那一階，
   > > 剛好落在診所自己那叢綠葉的色相上。**所以「再翠」不必換色相，只要動彩度。**
   >
   > 固定 L\* 39.3 與 Lab h 151.1，彩度 14.3 → 31.5（＝枝葉亮處自己的彩度）：
   >
   > | | 值 | C\* | HSL | 白字 | 對卡 | 對紙 |
   > | --- | --- | --- | --- | --- | --- | --- |
   > | ⑤ 基準 | `#4b6251` | 14.3 | 136/13% | 6.63 | 6.04 | 5.24 |
   > | ⑩ 翠一階 | `#45644d` | 18.6 | 135/18% | 6.60 | 6.00 | 5.21 |
   > | ⑪ 翠兩階 | `#3f654a` | 22.9 | 137/23% | 6.62 | 6.02 | 5.23 |
   > | ⑫ 翠三階 | `#386646` | 27.2 | 138/29% | 6.65 | 6.05 | 5.25 |
   > | ⑬ 枝葉亮處的彩度 | `#306843` | 31.5 | 140/37% | 6.59 | 6.00 | 5.20 |
   >
   > **對比一模一樣**（亮度沒動），所以這一排比的純粹是翠度；**四階都是單一值**，
   > 對紙 5.2 遠高於 4.5，不必另開字階。相鄰 ΔE 約 4.3，整段 17.4。
   >
   > **「太翠會喧賓」有一條量得出來的線。** 家族彩度分兩段：低段一般 8.1、矯正 14.8、
   > 苔綠 15.9、深綠松 16.7；高段外科 36.0、根管與贋復 43.6、兒牙 57.6。
   > **⑬ 的 31.5 仍在外科的 36 以下** —— 就算推到枝葉亮處的彩度，在整排標籤裡
   > 也不會是最吵的。真正喧賓要到 36 以上，而那超出資料庫裡任何一個綠。
   >
   > ⚠ **⑬ 不是枝葉亮處本身。** 原值 `#447c56` 是 L\* 47.4（比 ⑬ 亮 8.1 階），
   > 白字只剩 4.93，而且會跳出使用者選定的亮度。**⑬ 是「把枝葉的彩度搬到那個亮度上」。**
   >
   > 另外：⑤ 離釋出的苔綠只有 ΔE 6.8，**越往翠走離苔綠越遠**（7.6 → 17.8）。
   >
   > **⑪ `#3f654a` 選定為套色（2026-08-09）。** 接著決定白底字／文章字那一階，
   > 以 ⑪ 為準只降亮度，**色相 150.9 與彩度 22.9 都鎖住**：
   >
   > | | 值 | L\* | 比套色深 | 對卡 | 對紙 | ΔE 對深綠松 |
   > | --- | --- | --- | --- | --- | --- | --- |
   > | 同 | `#3f654a` | 39.3 | 0 | 6.02 | 5.23 | 16.4 |
   > | **Ⓐ 深一階** | `#33593e` | 34.4 | 5.0 | 7.24 | 6.28 | 14.5 |
   > | **Ⓑ 深兩階** | `#2c5238` | 31.5 | 8.0 | 8.06 | 6.99 | 13.4 |
   > | **Ⓒ 深三階** | `#254a31` | 28.1 | 11.0 | 9.09 | 7.89 | 13.1 |
   >
   > **⚠ 彩度刻意不跟著縮。** 往深處走時色域天花板會降，照比例縮彩度的話
   > 「翠」會在文字階被稀釋掉，而且會更靠近深綠松（ΔE 掉到 10.4）。
   > 鎖住 22.9 之後色域佔比反而從 50% 升到 63% —— **翠度保住，離連結色也還有 13~15。**
   > 色域在三個深度都夠（maxC 41.4 / 38.9 / 36.5），不必 clamp。
   >
   > **要盯的是深綠松 `#214D48`**（全站連結色＋診所資訊家色）：它會和這一階
   > **同時出現在同一篇文章裡**（內文連結吃科別色、頁尾電話固定深綠松）。
   > 越深越近，但色相差 36°（151 偏綠、186 偏藍綠），並排分得開。
   >
   > 落差的家族刻度：外科 +8.2、贋復 +10.1、兒牙 +12.3、顯微根管 +15.1 ——
   > **Ⓐ +5.0 比現役任何一組都小、Ⓑ +8.0 幾乎等於外科、Ⓒ +11.0 在贋復與兒牙之間。**
   >
   > ### ✅ 定案（2026-08-09）：套色 `#3f654a`、字 `#2c5238`（⑪ 配 Ⓑ）
   >
   > ```css
   > [data-spec="general"],
   > [data-spec="perio"] { --accent: #3f654a; --accent-deep: #2c5238; }
   > ```
   >
   > 吃這一組的有三個地方：主題與科別的「一般牙科・定期檢查」與「牙周治療」、
   > 醫師介紹的**創辦醫師**。已套進 `index.html` 與 `assets/style.css`。
   >
   > | | 值 | L\* | C\* | Lab h |
   > | --- | --- | --- | --- | --- |
   > | 套色 | `#3f654a` | 39.3 | 22.9 | 150.9 |
   > | 字 | `#2c5238` | 31.5 | 23.1 | 151.1 |
   >
   > **色相差 −0.2°、彩度差 −0.2，只差亮度 7.9** —— 和口腔外科那一組
   > （0.5°／−0.1／8.2）同一個等級，是色盤裡最乾淨的兩組之一。
   >
   > 七個實際位置全過：白字 6.62、字對卡 8.06、字對紙 6.99、短棒對紙 5.23（門檻 3.0）。
   > `--chair #4f6361` 留著（第二節材質實測值），只是不再有科別指向它 ——
   > 和 `--moss`、`--tile` 一樣。
   >
   > **這一色的來歷值得記著**：它不是挑出來的，是**推出來的** ——
   > 顯微根管改紅 → 苔綠被釋出 → 和診療椅並排比 → 繞一圈九階 → 使用者挑中的
   > 那一階**剛好落在花藝實測「枝葉亮處」的色相上** → 再往枝葉的彩度推兩階 → 收在 22.9。
   > 整條鏈沒有一步是目測的。
2. ✅ **文章頁那階 `#596951` 已拿掉**（六之六節）。苔綠原值對紙只有 4.38 才要另開一階；
   新的深階對紙 7.18，**七科裡需要第三階的就只剩兒牙**。
3. ⚠ 兩階的預設值那一行必須排在 `[data-spec="endo"]` 前面（六之二節最後）。兩個檔案都已經是。
4. ⬜ **六之八的三組要重排，這次是變差不是變好。** 綠色段只剩一般牙周 174，
   「保留與延續」那一組的兩個成員拆散了；紅要接進暖色段（兒牙 35 旁邊）還是自己成一段，
   還沒重寫。**這一件還沒做。**

---

## 六之十二、齒顎矯正的藍提亮（2026-08-09，三案提案中）

使用者：「矯正的標籤套色之後不夠亮。」**量下來他是對的，而且是兩件事疊在一起。**

> **⚠ 這一輪已被第六之十六節取代（2026-08-09）。** 使用者決定牙周換青綠、
> **矯正直接接手牙周那顆藍 `#4478b5`（L\* 49.5）** —— 比這裡最激進的 Ⓒ（48.1）還亮。
> 底下三案留著看推論，**不要再拿去實作**。

**還沒定案。** 提案頁 `/history/spec-ortho-bright.html`（`?c=0|1|2|3`）。
正式站與 `index.html` 一個字都還沒動。

### 一、矯正同時是最暗與最灰的一顆

| 填色亮度排序 | L\* | | 彩度排序 | C\* |
| --- | --- | --- | --- | --- |
| 兒童牙科 | 59.5 | | 兒童牙科 | 57.6 |
| 口腔外科 | 48.0 | | 口腔外科 | 36.0 |
| 顯微根管 | 45.8 | | 顯微根管 | 43.6 |
| 全部 | 39.6 | | 贋復假牙 | 30.8 |
| 一般・牙周 | 39.3 | | 一般・牙周 | 22.9 |
| 贋復假牙 | 38.0 | | **齒顎矯正** | **14.8** |
| **齒顎矯正** | **36.3** | | （全部 1.0，那顆本來就是中性灰） |

**七個彩色科別裡，矯正的亮度與彩度都是最後一名。**

### 二、⚠ 這個色相的彩度天花板很低 —— 這一輪最重要的限制

Lab h 248 在 sRGB 裡的彩度上限：L\* 36 是 27.2、L\* 44 是 31.4、L\* 48 是 33.5。
**對照贋復的 h 287 在 L\* 46 可以到 68** —— 差一倍以上。

> **所以「亮」在這裡主要只能靠明度，彩度幫不上太多忙。**
> 好處是它不會失控：推到佔色域 77% 也才 C\* 25.9，仍在贋復 30.8 與外科 36.0 之下。

### 三、三案（色相鎖 248.1，**深階維持官方色票不動**）

深階固定 `#3C596B`（對卡 6.74・對紙 5.85），所以「比深階亮多少」就是 chip 按下去的落差。

| | 套色 | L\* | C\* | 佔色域 | 白字 | 對紙 | 比深階亮 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 現行 | `#3C596B` | 36.3 | 14.8 | 54% | 7.41 | 5.85 | 0.0 |
| **Ⓐ 亮一階** | `#426276` | 39.9 | 16.0 | 55% | 6.48 | 5.12 | 3.6 |
| **Ⓑ 亮兩階** | `#3f6d88` | 43.9 | 21.1 | 67% | 5.59 | 4.42 | 7.6 |
| **Ⓒ 亮三階** | `#39799a` | 48.1 | 25.9 | 77% | 4.80 | 3.79 | 11.8 |

**深階不動是這一輪的關鍵設計**：矯正原本是單一值，提亮之後套色會超過紙上當字的
天花板（L\* 43.4），本來就得拆兩階 —— 而**色票原值剛好可以直接當那個字階**，
不必另外調一個色。

### 四、資料庫給了一個天然的上界

**候診沙發亮處實測 `#4e718b`**（L\* 46.0・C\* 18.9・h 254，第三節家具布料）——
現行的色票藍灰就是從這塊布來的。它落在 Ⓑ 與 Ⓒ 之間。

> **Ⓒ 的 48.1 已經比那塊布的實測亮處還亮。** 要守住「顏色都來自診所實物」這條原則，
> **Ⓑ 到沙發原值那一段是最安全的範圍。**

### 五、辨識度

| | 對「全部」填色 | 對贋復填色 | 對一般・牙周填色 |
| --- | --- | --- | --- |
| 現行 | 16.1 | 21.6 | 28.9 |
| Ⓐ | 17.1 | 21.0 | 29.6 |
| Ⓑ | 22.5 | 20.3 | 33.5 |
| Ⓒ | 28.2 | 22.3 | 37.4 |

**越亮離「全部」那顆灰越遠、離贋復越近**（兩顆都是冷色）。最小值是 Ⓑ 對贋復的 20.3，
遠高於出局線 6.1 —— **三案都沒有撞色問題**。

### 六、目前的建議

**Ⓑ `#3f6d88`。** 亮度 43.9 把矯正從吊車尾拉到中段（贋復 38.0 與顯微根管 45.8 之間），
彩度 21.1 和一般・牙周的 22.9 同一帶 —— 不再是又暗又灰的那一顆，也沒變成搶眼的那一顆。
比深階亮 7.6，幾乎等於口腔外科的 8.2。

---

## 六之十三、贋復的藍偏藍紫（2026-08-09，三案提案中）

使用者：「植牙假牙重建的套色藍怪怪的，有點藍紫。」**他是對的，而且是同一天提亮它時造成的。**

**還沒定案。** 提案頁 `/history/spec-prosth-bluer.html`（`?c=0|1|2|3`）。

### 一、成因：提亮的時候把色相鎖死了

第六之十節把 `#252d4a`（Lab h 286.7）提亮到 `#4a5789`，**色相一格沒動**。
問題是：

> **在 L\* 19 那個深度，h 287 讀起來就是深海軍藍；提亮到 L\* 38 之後，
> 同一個色相開始讀成藍紫。色相沒變，但它在不同亮度上的個性變了。**

對照：其他藍的色相都在 **248~254**（矯正 248.1、候診沙發 254.0、長椅 254.0）。
贋復的 287 離它們 33~39°，那一段正是藍紫。
**「鎖住色相」在第六之十節是對的規則（避免變灰），但它保證的是彩度不掉，
不保證色相在新亮度上讀起來一樣。這是那條規則的漏洞。**

### 二、三案（亮度完全不動，只把色相往藍拉）

兩階同步移動，色相差維持 1° 以內。彩度上限壓在**色域 75%** ——
越往藍走天花板降得越快（h287 是 60.5，h266 只剩 36.0），不設限 Ⓒ 會變成螢光的網頁藍。

| | 套色 | 深階 | Lab h | HSL | 佔色域 | 白字 | 深階對紙 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 現行 | `#4a5789` | `#364067` | 287.2 | 228/30% | 51% | 6.96 | 7.95 |
| **Ⓐ** | `#3f598a` | `#2d4268` | 280 | 219/37% | 63% | 6.98 | 7.93 |
| **Ⓑ** | `#335b8b` | `#244368` | 273 | 213/46% | 75% | 6.98 | 7.98 |
| **Ⓒ** | `#315d85` | `#234463` | 266 | 209/46% | 75% | 6.92 | 7.98 |

**亮度沒動 → 四案對比幾乎一模一樣**，這一排比的純粹是色相。

### 三、⚠ 唯一的取捨：越不藍紫，越靠近齒顎矯正

ΔE 對矯正（用它提亮後的 Ⓑ `#3f6d88` 算，最壞情況）：
現行 20.3 → Ⓐ 17.7 → Ⓑ 15.7 → **Ⓒ 10.7**。
現役七科填色最近的一對是 **16.1**，所以 **Ⓒ 出局**（HSL 色相 209 也只離矯正的 203 六度）。

**建議 Ⓐ**：離開藍紫（HSL 228 → 219），對矯正還留 17.7、對外科 25.6，兩邊都寬。

### 四、三案都離開了茶室磁磚的實測色相

這是**知情的取捨**：忠於實測（現行），還是讓提亮之後的顏色讀起來對（Ⓐ／Ⓑ）。
定案之後第六之十節那一段要跟著改寫，**不要留著「色相鎖 286.7」那句話**。

---

## 六之十四、⚠ 牙周想用矯正提亮案的 Ⓒ —— 那會撞色

使用者說想把矯正提亮三案的 **Ⓒ `#39799a` 留給牙周治療**。**這件事有數字上的障礙，
先記下來免得下次又走一遍：**

> **Ⓐ Ⓑ Ⓒ 是同一個色相的三個高度**（Lab h 247~249），不是三個顏色。
> 拿其中兩個給兩個不同科別，它們只會差亮度與彩度：
>
> | 矯正取 | 對牙周 Ⓒ 的 ΔE | |
> | --- | --- | --- |
> | 現行 `#3C596B` | 16.3 | 勉強（等於現役最近的一對 16.1） |
> | Ⓐ `#426276` | 12.8 | 偏近 |
> | **Ⓑ `#3f6d88`** | **6.4** | **✗ 幾乎等於出局線 6.1** |

**而 Ⓑ 正是矯正那一輪的建議案。** 也就是說：
**矯正提亮 ＋ 牙周拿 Ⓒ，兩件事不能同時成立。**

> **2026-08-09 追加：使用者決定「贋復用 Ⓑ、牙周用矯正的 Ⓒ」。**
> 已做成 `/history/spec-perio-split.html`。**做出來之後多冒出一個問題：**
> 牙周套色 `#39799a` 是 L\* 48.1，**超過「當字疊在紙上」的天花板 43.4，
> 所以它非開深階不可** —— 而那個深階和矯正同色相（差 0.9°），會直接壓在矯正頭上。
>
> | 牙周深階 | 值 | L\* | 兩階落差 | 對紙 | ΔE 對矯正 `#3C596B` |
> | --- | --- | --- | --- | --- | --- |
> | Ⓐ | `#235067` | 32.0 | 16.1 | 6.87 | **6.4 ✗** |
> | Ⓑ | `#1e465b` | 28.0 | 20.1 | 7.96 | 9.0 |
> | Ⓒ | `#193d4f` | 24.0 | 24.1 | 9.10 | 12.4 |
>
> **要嘛接受和矯正很近，要嘛接受很大的兩階落差**（現役最大是顯微根管的 15.1，
> Ⓑ 的 20.1 已經遠超）。建議 Ⓑ。
>
> 冷色端現在三顆，套色彼此：牙周對矯正 16.3、牙周對贋復 17.0、贋復對矯正 17.8
> —— **全部貼著現役最近的一對（16.1），過得了但沒有餘裕。**
>
> ⚠ **而且矯正等於不能提亮了**：它一動到 Ⓑ `#3f6d88` 就和牙周撞（ΔE 6.4）。
> 第六之十二節那一輪的結論被這一個決定否決掉了。
>
> **2026-08-09 續：牙周套色改用 Ⓧ `#4478b5`（使用者給的 `#3496F2` 收到白黑等效線）。**
> 那顆的色相 273 就是贋復那一支，於是牙周的深階整條被贋復佔滿 ——
> 同色相 L\* 37~43 對贋復套色 ΔE 1.0~5.8、L\* 28~31 對贋復深階 0.7~3.8。
>
> | 牙周深階 | 值 | h | 兩階色相差 | 最小 ΔE |
> | --- | --- | --- | --- | --- |
> | Ⓡ 同色相 | `#244369` | 273 | 0° | **0.7**（對贋復深階） |
> | **Ⓟ** | `#1e4156` | 252 | 21° | 10.7（對矯正） |
> | Ⓠ | `#1e465b` | 248 | 25° | 9.0（對矯正） |
>
> **使用者傾向 Ⓟ。** 接著他要求把贋復的白底字往下探兩階，看能不能讓開：
>
> | 贋復白底字 | 值 | L\* | 落差 | 對紙 | 對牙周 Ⓡ | Ⓟ | Ⓠ |
> | --- | --- | --- | --- | --- | --- | --- | --- |
> | 現行 | `#244368` | 27.8 | 10.1 | 7.98 | **0.7 ✗** | 11.1 | 11.7 |
> | Ⓓ1 | `#1d3858` | 23.0 | 15.0 | 9.43 | **5.9 ✗** | 9.3 | 10.9 |
> | **Ⓓ2** | `#182f4b` | 18.9 | 19.0 | 10.72 | **10.5 ✓** | 10.3 | 12.4 |
>
> > **關鍵：選 Ⓓ2 之後牙周的 Ⓡ 就活了** —— 牙周可以回到「兩階同色相」，
> > 和其餘六科一樣（Ⓟ Ⓠ 都要歪 21°~25°，牙周會是唯一的例外）。
> > **這是唯一能把牙周救回標準結構的路。**
> > 而且 Ⓓ2 的 L\* 18.9 幾乎剛好回到茶室磁磚原值 `#252d4a` 的 19.1 ——
> > 贋復的字階繞一圈回到自己的出處。代價是對卡 12.35，字會偏深藍黑。
>
> 提案頁 `/history/spec-perio-split.html` 現在是三排切換條：
> 牙周套色六案 ／ 贋復白底字三案 ／ 牙周深階三案。

### 定案（2026-08-09 上線）

| 科別 | 套色 `--accent` | 白底字／文章字 `--accent-deep` |
| --- | --- | --- |
| **牙周治療** | **`#4478b5`** | **`#244369`**（Ⓡ，同色相） |
| **贋復假牙**（改） | `#335b8b` | **`#182f4b`**（Ⓓ2，從 `#244368` 再往下探 8.9 個 L\*） |

兩顆的兩階都在**同一個色相**上，和其餘五科同結構（Ⓟ Ⓠ 要歪 21°~25°，會讓牙周
變成全站唯一的例外）。**這一組能成立完全靠贋復的字階讓到 L\* 18.9** ——
沒有那一步，牙周深階對贋復深階只有 ΔE 0.7。

| | 套色 | L\* | C\* | h | 深階 | L\* | C\* | h | Δh |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 牙周 | `#4478b5` | 49.5 | 37.5 | 272.8 | `#244369` | 27.9 | 25.7 | 273.9 | +1.1 |
| 贋復 | `#335b8b` | 37.9 | 30.9 | 273.2 | `#182f4b` | 18.9 | 20.1 | 273.5 | +0.3 |

八個位置全部過關：

| | 白字在塊上 | 塊對紙（3.0） | 字對卡 `#f4f4f5` | 字對紙 `#e2e5e6` |
| --- | --- | --- | --- | --- |
| 牙周 | **4.57** ✓ | 3.61 ✓ | 9.18 ✓ | 7.97 ✓ |
| 贋復 | 6.98 ✓ | 5.51 ✓ | 12.35 ✓ | 10.72 ✓ |

> 牙周的白字 **4.57 是全站最緊的一格**（門檻 4.5，只剩 0.07）。
> **`#4478b5` 這顆不能再亮一點點**，動了就掉出 AA。

### ⚠ 牙周與贋復現在是全站最近的一對

| | 填色 ΔE | 深階 ΔE |
| --- | --- | --- |
| 牙周 × 贋復 | **13.3** | **10.5** |
| 牙周 × 矯正 | 22.6 | 16.3 |
| 矯正 × 贋復 | 18.7 | 19.7 |

**13.3 與 10.5 都低於這一輪之前的全站最小值 16.1。** 這是知情的取捨：
兩科同色相、只差亮度，讀起來是「深藍／淺藍」而不是兩個顏色。
**日後任何一顆要再調，都必須先把這一對重算過。**

> **2026-08-09 追加：這一對還在，但換了名字。** 第六之十六節把牙周換成青綠、
> **`#4478b5` / `#244369` 整組交給齒顎矯正** —— 所以 13.3／10.5 現在是
> **「矯正 × 贋復」**。約束一模一樣，這一節其餘的推導也全部有效，只是主角換人。

### 順帶解掉的兩件

- **矯正提亮重新可行。** 牙周搬到 h 273 之後，矯正 h 248 旁邊空了出來 ——
  第六之十二節的三案不再有撞牙周的問題（`/history/spec-ortho-bright.html` 還在，未定案）。
- 第六之十三節那一輪（贋復藍紫）的 **Ⓑ 已經套上線**，`#335b8b` 就是它。

---

## 六之十五、齒顎矯正：品牌剩的兩顆 ＋ 資料庫三個（2026-08-09，八案提案中）

使用者：「品牌 logo 選色只剩下左下的米色和右上的稻草灰還沒用，把這兩個拿進來做看看，
另外根據配色資料庫再找不與現有衝突的三個顏色。」

**還沒定案。** 提案頁 `/history/spec-ortho-palette.html`（`?c=0`～`7`）。
`index.html` 一個字都還沒動。這一輪和第六之十二節（藍提亮三案）是**兩條並行的路**，
那一頁還在，Ⓑ 也放進這一頁的切換條裡一起比。

### 一、⚠ 這兩顆的原值不能當科別色 —— 四個位置全部不過

| 顆 | HEX | L\* | C\* | h | 白字（4.5） | 塊對紙（3.0） | 當字對卡（4.5） | 墨字疊上去 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 稻草灰 | `#A8A998` | 68.7 | 9.3 | 111.6 | **2.39 ✗** | **1.89 ✗** | **2.17 ✗** | 5.91 |
| 米色 | `#D3CBC5` | 82.2 | 4.3 | 66.6 | **1.60 ✗** | **1.26 ✗** | **1.46 ✗** | 8.82 |

第五節早就寫過灰綠「不能當文字，只作底或色塊」，這一輪把另外三格也補齊了：
**米色對紙只有 1.26 —— 它和紙幾乎是同一塊**，連當色塊都浮不起來。

> 所以照兒牙那一輪的做法：**拿色相，亮度與彩度重新調**。
> 兒牙的出處是圓凳木件 `#8d5e3a`，定案的 `#c28229` 兩個量都動過。
> **顏色的出處是色相，不是那個 HEX。**

### 二、八案（五個新案的亮度都頂在白字的天花板）

深階＝白底字。除了現行與 Ⓑ，每一案的兩階都在同一個色相上（和其餘六科同結構）。

| | 套色 | L\* | C\* | h | 白字 | 塊對紙 | 白底字 | 對卡 | 對紙 | 兩階落差 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 現行 | `#3C596B` | 36.3 | 14.8 | 248 | 7.41 | 5.85 | `#3C596B` | 6.74 | 5.85 | 0.0 |
| Ⓑ 藍提亮 | `#3f6d88` | 43.9 | 21.1 | 249 | 5.59 | 4.42 | `#3C596B` | 6.74 | 5.85 | 7.6 |
| **Ⓔ 稻草灰・橄欖** | `#6d7730` | 47.8 | 39.8 | 112 | 4.85 | 3.83 | `#565e24` | 6.32 | 5.49 | 9.8 |
| **Ⓔ2 稻草灰・收斂** | `#707644` | 48.0 | 28.8 | 112 | 4.81 | 3.80 | `#585d35` | 6.30 | 5.47 | 9.9 |
| **Ⓕ 米色・摩卡** | `#7f624a` | 43.9 | 19.9 | 66 | 5.60 | 4.42 | `#5d4127` | 8.49 | 7.37 | 13.8 |
| Ⓖ 青綠（診療椅） | `#317d78` | 47.8 | 24.8 | 190 | 4.84 | 3.83 | `#2a6d69` | 5.47 | 4.75 | 5.9 |
| Ⓗ 芥末（含羞草） | `#82713a` | 48.1 | 32.7 | 92 | 4.80 | 3.79 | `#67582c` | 6.35 | 5.51 | 10.2 |
| Ⓘ 磚橙（磚紅長椅） | `#b65536` | 47.9 | 51.9 | 44 | 4.84 | 3.82 | `#914229` | 6.34 | 5.50 | 9.9 |

**五個新案的亮度全部釘在 L\* 48 那一線**（Ⓕ 是 43.9，再亮就撞兒牙）——
那是「白字剛好還過 AA」的天花板，所以**這五案都已經是各自色相能給的最亮值**，
比現行的 36.3 高了快 12 階。八案的八個位置全部過關。

### 三、辨識度（ΔE 對其他七顆的套色）

| | 一般 | 牙周 | 顯微根管 | 兒牙 | 贋復 | 口外 | 全部 | 資訊 | **最小** |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 現行 | 28.9 | 28.1 | 56.4 | 75.9 | 18.7 | 36.5 | 16.1 | 17.6 | 16.1 |
| Ⓑ | 33.5 | 20.9 | 61.1 | 80.2 | 15.7 | 35.8 | 22.5 | 24.7 | 15.7 |
| **Ⓔ** | 27.8 | 76.3 | 56.0 | 38.4 | 70.5 | 73.5 | 40.1 | 42.8 | **27.8** |
| **Ⓔ2** | 20.2 | 65.4 | 49.8 | 41.3 | 59.8 | 62.8 | 29.5 | 34.4 | **20.2** |
| **Ⓕ** | 29.4 | 56.3 | 30.5 | 41.0 | 49.8 | 45.7 | 19.4 | 34.8 | **19.4** |
| Ⓖ | 18.1 | 42.3 | 67.7 | 73.4 | 38.6 | 55.5 | 26.8 | 20.0 | 18.1 |
| Ⓗ | 30.1 | 70.2 | 41.5 | 30.9 | 64.4 | 62.6 | 32.9 | 42.0 | **30.1** |
| Ⓘ | 63.0 | 81.7 | **16.4** | 29.6 | 76.5 | 60.0 | 51.5 | 68.2 | 16.4 |

比較基準是**現在的全站最小值 13.3**（牙周對贋復，第六之十四節）。
白底字階那一排另外算過，只有一格要盯：**Ⓖ 的 `#2a6d69` 對診所資訊的深綠松只有 13.6**
（連結色和文章字會很像）。其餘：Ⓔ 22.8、Ⓔ2 17.2、Ⓕ 21.1、Ⓗ 26.7、Ⓘ 18.7。

### 四、為什麼稻草灰能成立、米色不能

- **稻草灰 h 112 落在色盤的空檔裡。** 顯微根管離開苔綠之後，綠色段只剩一般牙科的
  h 151，中間空了 39° —— 所以它一調上來就是全場最寬的兩案（Ⓔ 27.8、Ⓔ2 20.2）。
- **米色 h 66 和兒牙的 h 72 只差 6°。** 數字全部合格（最小 19.4），
  **但它往下壓 38 個亮度階之後叫摩卡褐，不是米色了**。第六之二節那條「顏色的出處是色相」
  在這裡反過來咬人：色相留住了，你認得的那顆卻沒留住。

Ⓔ 與 Ⓔ2 的差別只有彩度（39.8 對 28.8）：**Ⓔ 是一個明確的橄欖綠，Ⓔ2 才留得住
稻草灰原本那種被曬過的灰調**（原值彩度只有 9.3）。這一組選哪個是品牌感的問題，
不是對比度的問題。**目前傾向 Ⓔ2 `#707644`。**

### 五、三個資料庫案的取捨

- **Ⓗ 芥末 `#82713a`（含羞草 h 91）數字最好（30.1）。** 但含羞草那個色相在第六之二節
  是兒牙那一輪的素材，兩顆都會讀成暖黃系。
- **Ⓖ 青綠 `#317d78`（診療椅／深綠松 h 190）** 是唯一還空著的冷色非藍段，chip 上很乾淨；
  問題全在白底字對深綠松的 13.6。
- **Ⓘ 磚橙 `#b65536`（磚紅長椅 h 44）最危險**：對顯微根管只有 16.4，而且同屬紅褐一支，
  並排會讀成「深一點的紅」跟「淺一點的紅」。

### 六、⚠ 換掉藍不只是換顏色

矯正的藍在第六之八節綁的意象是**候診沙發**（「坐著等的沙發」），
布料實測 `#4e718b` 也是它的來源。換走等於這一條意象斷掉，那一節要跟著重寫。

> **反過來有一個好處**：冷色端會空出來。現在牙周與贋復是全站最近的一對（13.3、10.5），
> 矯正一走，那兩顆日後要拉開就有空間 —— 第六之十四節最後那個約束會鬆掉。

**如果使用者要的只是「亮一點」而不是換色系，第六之十二節的 Ⓑ `#3f6d88` 仍然是最省事的一案。**

### 七、這一輪的 ΔE 是 CIE76，和全篇一致

順帶記一件事，免得下次算出來對不上：**這份文件從頭到尾的 ΔE 都是 CIE76**
（Lab 空間的直線距離），不是 ΔE2000。同一對顏色兩種算法差很多 ——
例如牙周對贋復的填色，CIE76 是 13.3，ΔE2000 只有 11.1；矯正對贋復 CIE76 18.7、
ΔE2000 只有 8.9。**要沿用文件裡的門檻（出局線 6.1、最小值 13.3），就必須用 CIE76 算。**

---

## 六之十六、牙周換青綠、矯正接手牙周的藍（2026-08-10 定案上線）

使用者看完上一節那八案：「這個青綠很不錯，而且我覺得更適合牙周。選青綠色給牙周，
現在牙周的色都換給矯正。」**兩科對調這件事是決定，剩下的只有牙周的青綠要幾階。**

**定案 Ⓣ1，2026-08-10 上線**（`index.html` 與 `assets/style.css` 兩份都改了）。
提案頁 `/history/spec-perio-teal.html`（`?t=1|2|3`）。

| | 原 | 新（已上線） |
| --- | --- | --- |
| 牙周治療 | `#4478b5` / `#244369` | **`#317d78` / `#2a6d69`**（Ⓣ1，兩階） |
| 齒顎矯正 | `#3C596B`（單一值） | **`#4478b5` / `#244369`**（整組從牙周搬過來） |

**色票的藍灰 `#3C596B`（`--blue`）就此釋出**，和 `--moss`／`--taupe`／`--tile` 一樣
留在變數表裡，只是不再有科別指向它。

### 一、這一手順便解掉了兩件懸案

1. **矯正「不夠亮」結案。** L\* 從 36.3（七科最暗）跳到 **49.5（七科最亮）**，
   彩度 14.8 → 37.5 —— 比第六之十二節最激進的 Ⓒ（48.1）還亮。**那一輪的三案可以收起來了。**
2. **第六之十五節的五個新案不必再選** —— 矯正拿到的是現成的、已經驗過的一組值。

**代價：全站最緊的那一格跟著搬家。** `#4478b5` 的白字 4.57（門檻 4.5，只剩 0.07），
原本掛在牙周身上，現在掛在矯正身上。**這顆一樣不能再亮一點點。**

### 二、⚠ 青綠的字階會壓在深綠松身上 —— 這一輪唯一的技術問題

診所資訊與**全站連結色**是深綠松 `#214D48`（Lab h 185.8、L\* 29.7），
青綠是 h 190。**同一支色相，所以青綠的字階往下壓就會碰到它**，而且和色相無關 ——
把青綠推到 h 205 再算，L\* 38~40 的字階對深綠松仍然只有 9.4~12.7。**是亮度靠太近，不是色相。**

| | 套色 | 白字 | 塊對紙 | 白底字 | 對卡 | 對紙 | 兩階落差 | 字階對深綠松 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **Ⓣ1 兩階** | `#317d78` | 4.84 | 3.83 | `#2a6d69` | 5.47 | 4.75 | 5.9 | **13.6** |
| **Ⓣ2 單一值** | `#2f7873` | 5.18 | 4.09 | 同一個值 | 4.71 | 4.09 | 0.0 | **18.0** |
| **Ⓣ3 兩階・字階更彩** | `#317d78` | 4.84 | 3.83 | `#116f6a` | 5.45 | 4.73 | 5.8 | 16.1 |

- **Ⓣ1** 填色頂到白字剛好過 AA 的高度（最亮的青綠），字階只能停在 L\* 41.9。
- **Ⓣ2** 整顆退 1.8 個 L\* 就同時滿足「白字 ≥4.5」與「自己當字對卡 ≥4.5」，
  於是**變成單一值** —— 和深綠松、和現行的矯正同一種做法，七科裡本來就有這種結構。
- **Ⓣ3 不建議**：兩階自己只差 ΔE 6.1，做了兩階卻看不出兩階。

**使用者定案 Ⓣ1 —— 要最亮的那顆青綠，並且保留兩階結構。**
（當時建議的是 Ⓣ2 單一值，理由是它把 13.6 那一格換成 18.0；使用者選了亮度與兩階。）

> **這個 13.6 為什麼可以接受**：站上**本來**的一般牙科深階 `#2c5238` 對深綠松就只有 **13.4**。
> 所以 Ⓣ1 沒有比現況更糟，只是同一種緊格從一處變兩處。
> **但它是全站第二緊的一格，動深綠松之前一定要先想到它。**

### 三、對調後最近的幾對

| 對 | 填色 ΔE | 深階 ΔE | |
| --- | --- | --- | --- |
| 齒顎矯正 × 贋復假牙 | **13.3** | **10.5** | 同色相 h 273 —— **就是原本那一對，只是換了名字** |
| 一般牙科 × 診所資訊 | 16.4 | 13.4 | 本來就在站上 |
| 牙周 Ⓣ1 × 診所資訊 | 20.0 | 13.6 | Ⓣ2 是 18.0／18.0 |
| 一般牙科 × 牙周 | 18.1 | 18.6 | 綠對青綠，色相差 39° |

**第六之十四節那條約束原封不動地跟著搬過去**：全站最近的一對仍是 13.3／10.5，
只是主角從「牙周×贋復」變成「**矯正×贋復**」。日後動到矯正或贋復任何一顆，
一樣要先把這一對重算過。

### 四、還沒處理的一件

**這個站的綠變成三支**：一般牙科 h 151、牙周青綠 h 190、深綠松（連結色）h 186。
後兩支同色相。青綠只出現在 chip 與科別標記上，深綠松出現在連結與診所資訊卡上，
兩者不太會並排 —— 但**「診所資訊那顆要不要跟著重看」是下一題**。

### 五、意象層要跟著改寫

第六之八節裡矯正綁的是**候診沙發**（`#4e718b` 那塊布）、牙周綁的是診療椅那一族。
對調之後兩條都不成立了：矯正拿的是牙周那顆從 `#3496F2` 收下來的藍，
牙周拿的是診療椅色相（h 190）——**牙周反而回到了診療椅那一支**，這一段可以重寫得更順。

---

## 六之十七、窄帶右側的地址與電話（2026-08-10 定案上線，只有電腦版）

把頁尾那組「圖示＋地址／電話」也放進 HERO 的窄帶。**六輪**收斂，
完整推導與每一個落選案的實測留在 `/history/hero-contact.html`（那一頁現在是定稿紀錄，
切換條已刪）。這裡只記與配色規範有關、以及日後動到會踩到的幾條。

### 一、這條帶子上只准有兩個白

窄帶裡本來就只有兩階白：

| 階 | 值 | 誰在用 |
| --- | --- | --- |
| 亮 | `rgba(255,255,255,.95)` | 1983／9／6 那三個數字 |
| 常 | `rgba(255,255,255,.65)` | 「中華路開業／醫師駐診／部定專科」三個標籤、瀏覽數與「次瀏覽」 |

第三輪時我自作主張給聯絡一個 **`.82`**，理由是「它可以按，該亮一階」。
使用者當場指出「**這樣有三個白**」—— 他是對的：多一階等於在這條帶子上多開一個層級，
而聯絡並不是新的層級，它就是標籤那一層的東西。

**定案：靜止 `.65`（＝標籤那階），hover／focus `.95`（＝1983 那階）。**
亮起來這件事由**狀態**表達，不由靜止的顏色表達。圖示吃 `currentColor` 一起亮。
⚠ 不要再開第三階，也不要把靜止值挪到 `.65` 與 `.95` 中間。

### 二、為什麼不能沿用頁尾那個綠

頁尾那組吃一般牙科的套色 `#3f654a`，成立是因為它站在頁尾卡片 `#f4f4f5` 上（6.02:1）。
同一顆綠丟到窄帶（毛玻璃＋柏油漸層，L\* 8~20）上只有 **1.3:1**，等於看不見。
所以 HERO 這一組一定是白的 —— 這不是風格選擇，是對比度的結論。

### 三、實心圖示換成線條空心

頁尾兩顆是 Font Awesome Free 6 的 solid 版。實心圖示疊在夜景照片上會變成兩個小黑塊，
而窄帶裡其他東西（數字、標籤、兩條分隔線）全是細的。
改用 **Lucide 的 `map-pin` / `phone`（ISC，https://lucide.dev）**，
`fill: none` ＋ `stroke: currentColor`，署名寫在 SVG 上面的 HTML 註解裡，不要刪。

線寬 **1.3px**，靠 `vector-effect: non-scaling-stroke` 讓它不隨框縮放 ——
兩顆框不一樣大，沒有它同一個 `stroke-width` 會畫成 1.16px 與 0.97px 兩種粗細。
⚠ 這個屬性**不會繼承**，要下在 `path`／`circle` 上，不是下在 `<svg>` 上。

### 四、圖示大小要拿「字面」比，不是「字級」

這是這一輪最值得記的一條。使用者回報「電話 icon 相對於後面的號碼數字感覺有點飄起來」，
逐像素量出來成立，而且原因是我第三輪算錯了：

| | 畫出來 | 對**字面**的倍率 | 下緣相對文字底 |
| --- | --- | --- | --- |
| 釘子 ／ 中文 | 12.68px | 1.057 | **往下探出 0.77px** |
| 話筒 ／ 數字（錯的那版） | 10.30px | 0.936 | **縮在裡面 0.64px** |
| 話筒 ／ 數字（定案） | 11.33px | 1.030 | 往下探出 0.69px |

一顆踩在字底下面、一顆浮在字底上面，**兩行的關係是反的** —— 並排看就是「飄」。

錯在拿頁尾的「圖示高 ÷ **字級**」去回推。那個比例在頁尾成立，但
**字級不等於字面**：中文字面占字級 0.96，數字只有 0.88（數字沒有下伸部）。
同一個比例套到數字上，畫出來就矮一截。

定案 `.7918rem`（＝原本 10.30px 的 ＋10%）＋ `translateY(.8px)`。
五階 ＋9/10/11/12/13% 全部做成頁面讓使用者挑；比例最貼近釘子的其實是 ＋13%、
下緣距離最貼近的是 ＋11~12%，**使用者挑的是比兩者都小一階的 ＋10%**。
⚠ 那兩個是尺，不是答案。不要事後「照數字修正」把它推去 ＋11% 或 ＋13%。

### 五、位置：兩個數字，一個有依據一個沒有

- **水平**：貼**版心外緣**（1440 上離螢幕右緣 132.5px）。
  比過版心內緣（172.5，全站骨架那條線，使用者嫌太左）與貼照片邊（40，右得夠，
  但不站在骨架上、而且螢幕越寬離版心越遠 —— 2560 上會離版心外緣 660px）。
- **垂直**：離照片下緣 **15.18px**。
  比過五階：8.72（基線切齊「部定專科」，使用者說「快要超出圖片」，量出來確實只剩
  8.7px）、11.95、**15.18**、18.40、21.63（對三格中線，也等於和 1983 同高）。
  ⚠ **15.18 在幾何上沒有依據** —— 它是頭尾兩階之間四等分的第二格，就是使用者挑的
  那一格。不要事後幫它編一個對齊理由，也不要順手挪到最近的錨點上。

### 六、三個踩過的坑

1. **不要讓它參與版面。** 第一輪把它放進 `1fr auto 1fr` 的格線，邊欄裝不下時
   格線會去偷中間那格 —— 實測**三格的中軸被往左推了 42.4px**，而窄帶和照片
   共用同一條中軸是第六之三節的定案。現在它是 `position: absolute`、
   掛在 `.band` 底下（**不在 `.shell` 裡**），完全不參與版面。
   放在 `.band` 底下還有第二個好處：參考框變成整張照片，「版心外緣」可以用
   `(100% - --shell)/2` 精確算回來，不必碰 `100vw`（後者會把捲軸算進去，
   1440 的視窗實際只有 1425，差 7.5px）。
   代價：`.band > .shell` 的 `z-index: 1` 蓋不到它，要自己寫 `z-index`。
2. **連結要用 `display: flex`，不是 `inline-flex`。**
   行內 flex 盒的基線是**它第一個項目的基線**，而第一個項目是圖示、圖示沒有文字，
   只好拿自己的底邊當基線。兩顆圖示不一樣高 → 地址與電話兩行的基線差了 1.13px。
   改成 `flex` 之後 `<p>` 的高度就是連結的高度，兩行幾何一致，基線自然疊合（實測差 0）。
3. **`@media` 不會增加權重。** 窄螢幕退回兩行那條規則原本寫 `.contact-pair`（0,1,0），
   被 `body[data-y="…"] .contact-pair`（0,2,0）整條蓋掉，退路等於沒生效 ——
   1100 上量到整組還停在原位、比三格中線低 3.75px。要抬權重。

### 七、順帶量到的一件：三格的 4rem 沉了三天（2026-08-10 已修好上線）

**`index.html` 的「三格間距 4rem（2026-08-07 的拉開）」定案後從來沒生效過。**
`.stats li { padding-inline: 4rem; }` 之後還有一條同權重（都是 0,0,2）、
排在後面的 `.stats li { position: relative; padding-inline: 1.9rem; … }`，後者贏。
實測 1440 上算出來是 30.4px、三格只有 330.1px 寬 ——
**線上跑的是當初被淘汰的 1.9rem，整整三天沒人發現。**

使用者在 `/history/band-stats-gap.html` 看過兩案之後選了 4rem，已修好上線。
修法是**把後面那一份的 `padding-inline` 刪掉**，讓原本那條接手，
正式站沒有留下任何 `!important`。

| 1440 實測 | 三格那一組 | 文字之間 | 離右邊的地址電話 |
| --- | --- | --- | --- |
| 舊（1.9rem） | 330.1px | 60.8px | 152.9px |
| **新（4rem）** | **531.7px** | **128px** | **85.6px** |

窄帶本身 1425×84 兩案完全相同 —— **變寬的是裡面那一組，不是窄帶**（它本來就滿版）。
三格的中軸 712.5 ＝ 版心中軸，兩案都沒動。手機完全不受影響（三格各佔三分之一、
內距 .5rem 是另一套規則，實測 375 上兩案都是 335px／8px）。

聯絡那一組不必跟著改：它是絕對定位的，只是離「部定專科」的空隙從 152.9 收成
**85.6px** —— 仍然遠大於群內的 30.4px，還是讀得出來是一組。
順帶一提，「窄過 1160 退成兩行」那個斷點在修好之後才名副其實：
舊幾何下其實能撐到約 909px，那時取 1160 是保守；現在剛好。

> **留下來的規則**：`padding-inline` 在這份樣式表的 `.stats li` 上**只准寫一次**。
> 要調間距就改那個 4rem，不要在下面的 `.stats li` 裡再寫一次 —— 這個坑就是這樣來的。

---

### 八、手機版（2026-08-10 當天稍晚定案上線）

位置是使用者指定的：**瀏覽數下面、1983／9／6 上面**，一行置中。
兩輪，過程留在 `/history/hero-contact-mobile.html`。
淘汰：一行撐開（「好怪」）、兩行（電話在上、地址在下，行距試過 4.8/7.2/8.8/11.2px）。

**字級與圖示是換算來的，不是照搬。** 電腦版是「聯絡 .78rem ÷ 標籤 .7rem ＝ 1.114」；
手機標籤 .625rem × 1.114 ＝ .696 → **.7rem**。圖示跟著 11.2 ÷ 12.48 ＝ 0.897 縮：
釘子 .762rem、話筒 .710rem。**兩顆的比例關係沒變**，所以第四節那件「話筒不會飄」
一起帶過來了；下移仍是 .8px（補話筒比釘子矮，和字級無關）。

**又一次「看起來沒置中」，又一次不是置中算錯。** 使用者：「從圖釘 icon 到電話號碼
最後一碼 9 這整個區塊看起來就沒有置中」。量出來：

| | |
| --- | --- |
| `.c-addr` 方塊左緣 → 釘子第一筆墨水 | **7.68px 死空白** |
| 「9」的墨水 → `.c-tel` 方塊右緣 | 0.44px |
| 盒中心／墨水中心 | 187.49／**191.11**（偏右 3.61px） |

那 7.68px 來自 `@media (max-width: 1159px)` 裡「兩行版」的溝 —— 那條溝是電腦版
退成兩行、要讓「雲」和「0」切齊左緣才需要的，手機是一行置中，沒有左緣要對。
關掉之後偏移剩 0.47px。**修法是把多餘空白拿掉，不是整組往左推 3.6px** ——
補償數字會在字級或圖示一改時走鐘。

**1983／9／6 往下坐 2px。** 使用者：「離窄帶的下緣還有點距離，反而離地址和電話很近」。
量出來上面 9.7px、下面 13.8px，差 4.1px。做法是**兩邊各動 2px**（`.stats` 加
margin-top 2、shell 下內距減 2）→ 11.67／11.78，而且**窄帶高度一點都沒變**。
只加 margin 會長高 2px；只收內距是「帶子變矮」不是「那一列往下坐」，上面的擁擠沒解到。

**兩個要記住的實作約束：**

1. 標記**必須待在 `.band > .shell` 裡**。手機要它排在瀏覽數與 1983 之間，就得參與
   `.shell` 的直向流；電腦版則靠 `position: absolute` 脫離版面（1983／9／6 才推不動）。
   包含塊因此是 `.shell`：`right: 0` 就是版心外緣，式子比原本掛在 `.band` 時更短。
   ⚠ 但**窄螢幕那段 @media 要補 `right: var(--pad)`** —— 視窗窄過 1160 時 `.shell`
   是滿版的，`right: 0` 會貼死在螢幕邊（985 上實測 0px，搬家前是 30px）。
2. 手機那條一行排法要寫 **`body .contact-pair`**（0,1,1）。≤1159 的「退成兩行」
   就是這個權重、而 1159 涵蓋 375 —— 只寫 `.contact-pair`（0,1,0）會被整條蓋掉，
   手機變成直排兩行。**這個坑同一天踩了兩次**（提案頁與 `index.html` 各一次）。

**320 級窄機**：一行整組 267.1px、版心內寬 280px，餘裕只有 12.9px（4.6%），
還在頁尾地址折行那個坑的射程內。≤360 把間距收到 1.1rem → 254.3px、餘裕 25.7px（9.2%）。

---

## 七、量測方法（要加新照片時照這個做）

**這一節只適用於 Windows 本機。** 手機／雲端 session 跑在 Linux 容器裡，
沒有 PowerShell、沒有 .NET Drawing，也沒有使用者的照片資料夾 —— 在那邊請用上面的表格，
不要嘗試執行這支腳本。

`tools/palette-measure.ps1` 用 .NET 解 JPEG、跑 k-means 抽主色，
可以整張測，也可以只測指定區域。

```powershell
powershell -ExecutionPolicy Bypass -File tools\palette-measure.ps1 -Manifest jobs.txt
```

`jobs.txt` **必須存成 UTF-8**，一行一個工作：

```
標籤|C:\path\to\photo.jpg|6
標籤|C:\path\to\photo.jpg|3|0.20,0.60,0.45,0.90
```

第三欄是要抽幾個主色（k），第四欄是裁切範圍 `x0,y0,x1,y1`（0~1 的比例，可省略）。
輸出每個色的 HEX、佔畫面比例、彩度 S、明度 L、色相 H、R−B。

要定裁切範圍時，先產生帶座標格線的縮圖比較好抓：腳本加 `-Grid` 參數即可。

對比度不要用眼睛判斷，算出來。WCAG AA：內文 4.5:1，大字（≥18.66px 粗體或 ≥24px）3:1。

---

## 八、還沒做的

- 桌機版排法
- 把 `/history/home-mobile-n3.html` 的固定頁首與科別色併進正式站的 `index.html`
- 亮／暗自動切換（跟系統設定走）
- HERO 疊字在淺色版下的第二種寫法

---

## 六之十二、頁尾電話的話筒圖示（2026-08-09 上線）

**全站第一個、目前唯一的圖示。** 頁首標誌不算 —— 那是標誌本體，不是介面圖示。

在這之前這個站一顆圖示都沒有，而且 2026-08-07 才刻意把文章卡上瀏覽數的眼睛圖示
**拿掉**過（理由：站上只該有一種說「幾次瀏覽」的方式）。所以加這一顆是使用者主動
要求的，不是順手加的 —— 他拿 modernanimal.com 的頁尾當參考圖。

### 定案

| 項目 | 值 |
| --- | --- |
| 圖形 | 實心話筒（Font Awesome Free 6 `phone`，CC BY 4.0） |
| 大小 | **`.82em`** |
| 號碼粗細 | **`500`** |
| 底線 | **`1px`／`text-underline-offset: .18em`**，只畫在 `.num` 上 |
| 間距 | `gap: .45em`（`inline-flex`、`align-items: center`） |

### 三個數字都是比出來的，不要憑感覺調

**一、圖示 `.82em`，不是 `1em`。**
第一版做成 `1em`，使用者一眼就說「比字大」。他是對的：`1em` 給的是**整個 em 框**
（頁尾那一行 17.3px），但數字實際只有約 **12.4px** 高 —— 剩下的空間是留給注音、
括號那些上下伸出去的東西的。所以 `1em` 的圖示比數字高了將近**四成**。

`.72em` 才是數學上完全切齊，但**實心圖形切齊反而顯小**（老排版規則），
所以往上抓到 `.82em`：略高於數字，看起來一樣重。

> 尺寸一律用 `em` 不用 `px`。文章頁的 `.foot-tel` 是 `1.05rem`、首頁是 `1.08rem`，
> 同一份規則兩邊都對得上，日後字級再改也不必回來調。

**二、號碼 `500`，不是 `400`。**
圖示是實心的，`400` 的數字撐不住它 —— 眼睛會先看到圖示，但頁尾唯一該搶眼的是電話。
`500` 和站上小節標題、醫師姓名同一階，**不是新的粗細**。

> `600`／`700` 也做過。使用者選 `500`。順帶一提：**iPhone 上 `700` 可能和 `600`
> 長得一樣** —— 系統的中文字（PingFang）最粗只到 Semibold，沒有真正的粗體。

**三、底線畫在 `.num` 上，不是畫在 `<a>` 上。**
連結是 `inline-flex`，底線若下在連結上會**連圖示下面一起拉一條過去**。
參考的那一站也只畫在號碼上。所以號碼要包在自己的 `<span class="num">` 裡。

`1px` 是刻意的：站上所有的線都是 `1px`（卡框、分隔線、窄帶分隔），這一條和它們同一個重量。
`2px` 那一版最接近參考圖，但那會讓頁尾多一條比其他線都粗的東西，落選。

### 第二版：地址也加了釘子，兩行拉成一對（2026-08-09 同日）

第一版上線後使用者要求把地址那行也做起來，前後又改了三輪。**舊規則作廢**：

> ~~地址刻意不上色、不加底線，只有滑過去才表態，電話那行才是頁尾唯一該搶眼的東西。~~

作廢的理由：**手機上根本沒有 hover。** 佔多數的訪客永遠不會知道地址點得下去 ——
那是一個從來不宣告自己是連結的連結。現在兩行同規格，改靠**字級**分層。

| 項目 | 值 |
| --- | --- |
| 診所名 `h4.foot-brand` | **1.25rem**（文章頁 1.22rem）。其餘兩欄的 h4 維持 .95rem |
| 地址・電話 | 都是 **1.08rem**（文章頁 1.05rem）、深綠、**500**、1px 底線 |
| 釘子 | **高 1.1rem × 寬 .825rem** |
| 話筒 | **.89rem × .89rem** |
| 圖示欄寬 | **1.15rem**，圖示各自置中 |
| 圖示與文字的間距 | **.5rem** |

使用者選的是 B 案（診所名大一階）。A 案是三行等大，落選。

#### 四個踩過的坑

**一、欄寬與間距一定要用 `rem`，不能用 `em`。**
`em` 跟著各自那一行的字級走。地址和電話字級只要不同，同一個 `.45em` 就算出兩個
不同的間距，**文字起點就對不齊** —— 使用者一眼看出來。改成固定 `rem` 之後，
兩行的文字一定從同一個 x 開始，日後字級再怎麼調都不會歪。

**二、釘子的寬高要分開寫。**
話筒的 viewBox 是 `512×512`（方的），釘子是 `384×512`（直立水滴）。
第一版把釘子的寬高都設成同一個值，結果它**只填滿 75% 的寬** ——
既比話筒小一圈，左右還各多出一條空白把間距撐開。寬度必須按 `.75` 的比例另外算。

**三、釘子要比話筒高，數字不同是刻意的。**
釘子只有話筒 0.75 倍寬，同高會看起來小一號。`1.1rem` 對 `.89rem`，
兩顆才一樣重。**兩顆靠置中對齊中軸**，不是靠對齊左邊。

**四、`.ico` 的高度要等於一行的行高（`1.8em`），並且 `align-items: flex-start`。**
否則地址在很窄的畫面折成兩行時，圖示會滑到兩行的正中間。

#### 號碼不折行

`05-533-9369` 有兩個連字號，那是**天然的斷行點**；拆成「05-」「533-9369」兩行
等於不能用。實測正式站在 320px～1280px 都折不到（電話盒 137px、最窄的欄 280px），
所以 `white-space: nowrap` 是保險，不是在修現有的 bug。

> 順帶記一筆：使用者回報**提案頁**上號碼折成兩行。量過之後是**面板寬度**的事 ——
> 電話要窄於 240px 才折、地址要窄於 320px。提案頁自己的邊距（頁面 20×2 ＋
> 卡片 21×2 = 82px）在窄面板裡吃掉太多。正式站不受影響，**先量再改**。

### 第三版：全部收成兩級字（2026-08-09 同日，現行）

第二版上線後使用者在 iPhone 上看到實況，回報三件事，**這一版才是現行的**：

| | 第二版（作廢） | **第三版（現行）** |
| --- | --- | --- |
| 三顆欄位標題 | 診所名 1.25rem，其餘 .95rem | **三顆都 .95rem** |
| 地址・電話 | 1.08rem | **.95rem** |
| 釘子 | 1.1rem 高 | **.97rem 高 × .73rem 寬** |
| 話筒 | .89rem | **.78rem** |
| 圖示欄寬／間距 | 1.15rem／.5rem | **1rem／.45rem** |
| 號碼寫法 | 05-533-9369 | **05-5339-369** |

> **整個頁尾現在只有兩級字：`.95rem` 與 `.82rem`。**
> `.95rem`＝三顆標題 ＋ 地址 ＋ 電話；`.82rem`＝站內導覽的連結與免責內文。
> 使用者原話：「字級大小差太多了，芳仁牙醫診所還有站內導覽跟這些文章應該要一樣大小。」
> **`.foot-brand` 那條例外已經拿掉，不要加回去。**

電話不再靠字級搶眼 —— 它有套色、500、底線、圖示四樣，夠了。
釘子與話筒是按字級 1.08→.95 **等比**縮下來的，兩顆的比例是使用者看過並定案的，
**不要單獨動其中一個**。

#### 號碼改成 05-5339-369

**數字完全相同**（都是 055339369），只有分組不同，所以 `tel:+88655339369` 一個字都沒動。
改的是三處**顯示**：頁尾七份、首頁「診所資訊」卡、`<head>` 的 JSON-LD `telephone`。
同一頁不能有兩種寫法，所以三處一起改。

#### ⚠ 再改一次：看得見的和機器讀的分開（2026-08-10，**這一版才是現行的**）

使用者定案。前面兩版（`05-533-9369`、`05-5339-369`、以及當天中途的 `(05)533-9369`）
全部作廢，留著看推論就好。

**這一版的重點是：畫面上的分組和機器讀的分組不必是同一種。**

| | 值 | 出現在哪 |
| --- | --- | --- |
| **看得見的** | `(05)5339-369` | 窄帶、診所資訊卡、頁尾（八個頁面） |
| **機器讀的** | `05-533-9369` | `<data value="…">` 屬性 |
| **國際格式** | `+886-5-533-9369` | JSON-LD 的 `telephone`（來自 `clinic.json`） |
| **撥號用** | `tel:+88655339369` | 連結的 href，**從頭到尾一個字都沒動** |

做法是 HTML5 的 `<data>` 元素 —— 它存在的目的就是「同一個東西的機器可讀版本」：

```html
<data class="num" value="05-533-9369">(05)5339-369</data>
```

`value` 是機器讀的那一份，元素內容是人看的那一份。原本是 `<span>`，
**CSS 全部是 class 選擇器，換標籤名不影響任何樣式**（已逐條確認）。

**括號不進機器讀的那幾份。** `(05)` 是國內撥號的寫法，和 `+886` 併在一起是矛盾的
（國際格式要把區碼的前導 0 拿掉）。

> **實話一句，寫在這裡免得後人高估它：**
> `<data value>` **目前沒有任何搜尋引擎會讀**。真正在做實體辨識的是
> JSON-LD 的 `telephone` 與 `tel:` 連結，那兩份**在這次改動之前就已經是
> Google 的分組了**。加 `<data>` 是把「顯示 ≠ 機器值」這件事在原始碼裡講清楚，
> 語意正確、零成本、零風險，但不要期待它帶來排名。
>
> 另外：Google 比對電話是**去掉標點之後比數字**的（三種寫法都會正規化成
> `055339369`），所以顯示要用哪一種分組**純粹是品牌決定，不影響 SEO**。

改到的地方：`index.html` 三處（窄帶、診所資訊卡、頁尾）、六篇文章頁尾、`404.html`，
以及三個在 CLAUDE.md 裡被寫成「正式站複本」的提案頁
（`hero-contact/`、`hero-contact-mobile/`、`band-stats-gap/`）。
**其餘的舊提案頁沒有動** —— 那些是各輪的定稿紀錄，不是現況（2026-08-12 起只剩 `history/` 的文字存檔）。

##### 量過的：號碼變寬 5.5px，但沒有折行也沒有撞到

一個連字號換成兩個括號，字面變寬。逐寬度實測（Chromium，`.hero-contact` 是 `nowrap`）：

| | 舊 | 現行 | 差 |
| --- | --- | --- | --- |
| 窄帶電話（手機 375） | 93.2px | 98.1px | +4.9 |
| 窄帶電話（電腦 ≥1160） | 103.0px | 108.5px | +5.5 |
| 頁尾號碼 | 101.3px | 108.0px | +6.7 |

· **手機 320／360／375／390／430 全部一行、都沒有破框。** 最窄的 320 上
  「地址＋電話」共 253.8px、容器 265px，還有 11.2px 餘裕（舊的是 16.1px）。
· **電腦 ≥1160**：`li` 的**外框**確實從 +0.6px 變成 −4.9px（重疊），
  **但那不是看得見的東西** —— 第三格左右各有 `padding-inline: 4rem`，
  真正的字（「5 個／部定專科」）離地址還有 **59.1px**（1160）～**66.6px**（1280 以上）。
  舊版是 64.6／72.1px。**量外框會得到錯誤的結論，要量 `li` 的子元素。**

> **⚠ 順帶量到一個既有的 bug，和這次的改動無關：**
> **視窗寬約 730～880px 時，窄帶第三格「5 個 部定專科」和右側的地址／電話會疊在一起。**
> 那一段走的是 ≤1159 的「兩行版」，地址與電話堆成兩行往左長，就壓到第三格上。
> **改號碼之前就是這樣**（768 上：舊 −19.7px、新 −25.1px，兩張截圖比對過），
> 這次只是讓它多疊了 5.4px。**還沒修**，因為怎麼修屬於這一節鎖住的版面規則
> （一行／1.9rem／貼版心外緣），要先問使用者。

#### 地址斷行：重現不了，所以上了三道保險

使用者在 iPhone 上看到「…永樂街 70」＋「號」，一個字孤零零掉到第二行。
**在 Chromium 上量不出來**：地址需要 206px，最窄的欄也有 272px，餘裕 66～471px，
320～1280px 七個寬度全部一行。所以沒有單一「修好了」的改動，改成三件獨立的事：

1. **字級 1.08 → .95rem。** 地址從 234px 收到 206px。
2. **「70 號」用 `&nbsp;` 綁住。** 中文每個字之間都是斷行點，只要有任何誤差就會斷，
   而斷在最後一個字是最難看的位置。綁住之後真要斷也會斷在「永樂街」前面，
   看起來像正常的地址換行。
   ⚠ **不要改用 `white-space: nowrap`** —— 那會在窄機上撐破版面，
   違反 CLAUDE.md 第一節第 6 條。
3. **`display: flex` ＋ `width: fit-content`，不要用 `inline-flex`。**
   WebKit 對 inline-flex 在 grid 格子裡的收縮寬度算法和 Blink 有落差，會拿接近
   min-content 的值去排 —— 這是唯一能解釋「Chromium 不斷、Safari 斷」的原因。
   block flex ＋ fit-content 不走那條路徑，外觀完全一樣（寬度貼齊內容）。
   `max-width: 100%` 是安全帶：真的放不下時收縮換行，不要撐破版面。
   實測 280px 無水平捲動。

> 教訓：**回報「手機上壞掉」的時候，先在真的瀏覽器量，不要照著感覺調。**
> 量完發現重現不了，就上多道獨立的保險，並把「重現不了」這件事寫下來 ——
> 下次有人看到這段程式碼覺得繞，才知道為什麼不能簡化回 inline-flex。

### 沒有做的一件（提過，使用者沒要）

- **hover 時把底線加重。** 手機沒有滑鼠，不受影響；電腦上目前就是永遠一條 `1px`。

（地址的地圖釘原本列在這裡「沒要」，2026-08-09 當天使用者改變主意做了 —— 見下一節。）

### 素材授權

Font Awesome Free 的圖示是 **CC BY 4.0**，要署名。話筒是 `phone`、釘子是 `location-dot`，
同一套、同一個授權。署名放在**七個檔案裡那兩顆 SVG 上面的 HTML 註解**（出處、授權、
連結都在），沒有做成頁尾看得見的一行字。
**改圖或搬檔的時候不要把那段註解刪掉。**

> 使用者若日後想要看得見的署名，加在 `.foot-bottom` 那一列即可。

### 要改的話有七個檔

markup 在 `index.html` 與 `posts/*/index.html`（六篇各一份，這個站沒有樣板）；
CSS 在**兩個地方**：`index.html` 的內嵌 `<style>`（首頁）與 `assets/style.css`（文章頁）。
**兩份 CSS 必須一模一樣，改一邊就要改另一邊。**

> 頁尾在 `<main>` 外面，所以改它**不會**讓六篇文章的更新日期一起跳成當天
> （見 CLAUDE.md 第五節）。這次驗證過，六篇日期都沒動。

## 六之十三、直立（旋轉）螢幕的 HERO（2026-08-09 定案）

使用者把 1920×1080 的螢幕**轉成直的**，回報 HERO 左邊那首詩「看起來非常小」，
問「為什麼字的大小、比例、位置不會自動調整」。

### 成因：這一頁只有照片跟著高度長

| | 1440×900 | 1080×1920 |
| --- | --- | --- |
| `.hero-photo` `height: 100svh` | 900px | **1920px（2.13 倍）** |
| `.hero-poem` `clamp(12.5px, 1.05vw, 15px)` | 15px | 12.5px（**撞下限**，1.05vw 只有 11.3px） |
| `.hero-sign` `clamp(11.5px, .95vw, 14px)` | 14px | 11.5px（撞下限） |
| `.hero-copy` `padding-top …clamp(1.2rem, 4vh, 2.6rem)` | 41.6px | 41.6px（**撞上限**，4vh = 76.8px 被擋住） |

字級只讀 `vw`、還掉到 clamp 的下限；內距讀了 `vh` 卻撞在上限。**高度多出來的 1000px
沒有任何一條式子接得住**，全部變成空天空 —— 三行詩實際 79px 高，在 900px 的畫面裡
佔 10.6%，在 1920px 裡只佔 4.1%。是比例落差，不是字級設錯。

### 照片的裁切在這個比例下換了方向 —— 六之三節的定案失效

1600×1058（1.512）的橫幅塞進 1080×1920 的框，`cover` 的縮放由**高度**決定
（1920 ÷ 1058 = 1.815 倍），照片被放到 2904px 寬 —— **左右各裁掉 31.5%，只露出中間
37%**（實測 cover 裁切量 0.633），垂直方向反而一點都沒切。

所以第六之三節定案的 `object-position: 50% 100%`（下緣貼齊、要切切天空）
**在直立比例下完全沒有作用**。要讓垂直方向開始被裁，框得矮到 37svh
（1080 ÷ 1.512 = 714px）以下。

### 定案：照片不整屏

五案做成 `/history/hero-portrait.html` 給使用者在他自己的旋轉螢幕上比過：

| 案 | 做什麼 | 實測（1080×1920，字級「中」） |
| --- | --- | --- |
| 現狀 | 不改，對照組 | 詩 12.5px、第一行 240px 寬 |
| 橫排放大 | 只把字級接上高度，位置不動 | 22.1px、第一行 424px |
| 直排・上 | `writing-mode: vertical-rl`，貼左上 | 155×424 的一柱 |
| 直排・中 | 同上，`margin-block: auto` 垂直置中 | 落在 y 734–1158 |
| **照片不整屏** | **`height: 62svh`** | **照片 1190px，左右裁切 63% → 41%** |

```css
@media (min-width: 721px) and (max-aspect-ratio: 2 / 3) {
  .hero-photo { height: 62svh; }
  .hero-poem { font-size: clamp(15px, 1.15svh, 24px); }   /* → 22px */
  .hero-sign { font-size: clamp(13px, .95svh, 18px); }    /* → 18px */
}
```

`62svh`：1920 高時照片 1190px，縮放降到 1.12 倍，左右只裁掉 41%，
街角、門面、隔壁的矮房子都回得來；詩與收尾句因此靠近，不再隔著 1000px 空天空對望。
窄帶是 `position: absolute; bottom: 0` 掛在 `.hero` 上，會自己跟上來，不必處理。

> **三行詩剛好閃過屋頂，這件事是驗過的、也是脆的。** 62svh 時屋頂左端落在畫面
> (387, 214)；三行的實測是 —— 第一行伸到 x **456** 但只佔 y 123–155（在屋頂上方），
> 伸到 y 220–252 的第三行只到 x **350**（在屋頂左邊）。詩整塊還躺在天空上，
> 靠的是**三行由長到短**這個既有的形狀。日後若把字級加大、或改動詩的斷句長度，
> 要回來重量一次這三個數字。

**字級仍要換成 `svh`** —— 只把照片變矮不夠，字級讀 `vw` 這件事沒解決。
`1.15svh` 的錨點在電腦版身上（1440×900 的 15px ＝ 1.667svh），保守取 1.15 是因為
**旋轉螢幕的觀看距離和一般螢幕一樣，字級要照桌機的尺度看，不是照海報**。
下限 15px 留給比較矮的直立畫面（例如 800×1200）。

### 界線：為什麼是 `min-width: 721px` 和 `max-aspect-ratio: 2/3`

- `min-width: 721px` —— 721 是手機版型（`max-width: 720px`）的補集，手機一個像素都碰不到。
- `max-aspect-ratio: 2/3` —— `aspect-ratio` 是**寬 ÷ 高**，所以這是「比 0.667 更瘦的畫面」。
  1080×1920 = 0.5625 ✓、1200×1920（16:10 旋轉）= 0.625 ✓、
  **iPad 直放 1024×1366 = 0.75 ✗ 這一段吃不到它。**
  > ⚠ **iPad 已經不是「看電腦版」了**（2026-08-10 起，見**第六之十八節**）：
  > 它由另一段 `(2/3, 9/10]` 接手，照片 70svh ＋ 窄帶接在照片下面。
  > 兩段刻意不重疊 —— 那一段的下界寫 `min-aspect-ratio: 2/3` 就是為了不蓋掉這一段。
  > **這一行原本寫著「它現在看到的是電腦版，那個樣子不動」，那句話已經被推翻了。**

驗過：把改動前後放進同尺寸 iframe 逐項比對，**1440×900 與 390×844 兩組的字級、行高、
書寫方向、`.hero-copy` 內距、詩的座標、`scrollWidth` 每一個值都相同**。

> 已知邊界：旋轉螢幕若開 **150% 縮放**，視窗只有 720 CSS px 寬，會落回手機版型而不是
> 這一段（720 是 `max-width: 720px` 的邊界，含）。那個結果也還可以看，不另外處理。

### 落選的直排問出了這個比例的地形，留著

這張照片的建築偏右，屋頂上緣在 y≈17%，但**左側天空一路延伸到 y≈78%** ——
左邊有一條約 **340px 寬、1500px 高的天空柱**。橫排最長那行（17 字）22px 時實測 424px、
大字級 535px，都比柱子寬，往下移就會壓到屋頂，**所以橫排只有頂端那一塊能整條躺在
天空上**；直排整首詩 155×424，整條進得去還有餘裕。要重提直排的話地形在這裡。

轉 90° 之後有兩件事要跟著改（做過，寫下來免得重踩）：
`height: max-content` 讓框等於最長那行、不會多切一行；**`width: max-content` 不能省** ——
`<p>` 是區塊元素、寬度會撐滿版心，而 `vertical-rl` 的區塊起點在**右邊**，
少了它整首詩會跑到畫面最右側去。`.lead` 原本的 `margin-bottom: .5em` 是「第一行與第二行
之間」，轉過來要改成 `margin-left`。

> 詩的字、斷句、順序在任何一案裡都沒有動（COPY.md 第一節的定稿），改的只有排版。


---

## 六之十八、iPad 直放：照片 70svh ＋ 窄帶接在照片下面（2026-08-10 定案）

推導：`/history/hero-ipad.html`（照片高度四階）→ `/history/hero-ipad-band.html`（接縫與地址位置，
定案後已換成正式站的複本）。

### 為什麼 2026-08-09 那條旋轉螢幕的規則涵蓋不到 iPad

那一條是 `max-aspect-ratio: 2/3`（0.667），而 iPad 直放是 0.75 —— 當初就寫明刻意排除。
**而且實際的視窗比例比 0.75 更寬**：media query 算的是視窗，Safari 的分頁列與工具列
會從高度扣掉約 137 CSS px。

| 機型 | 螢幕 | 扣掉瀏覽器之後 | 比例 |
| --- | --- | --- | --- |
| iPad mini 6 | 744×1133 | 744×996 | 0.747 |
| iPad 10.9" | 820×1180 | 820×1043 | 0.786 |
| iPad Pro 11" | 834×1194 | 834×1057 | 0.789 |
| iPad Pro 12.9" | 1024×1366 | 1024×1229 | 0.833 |

所以新的一段取 **(2/3, 9/10]**：

```css
@media (min-width: 721px) and (min-aspect-ratio: 2 / 3) and (max-aspect-ratio: 9 / 10)
```

下界寫 `min-aspect-ratio: 2/3` 是**為了不蓋到上面那條** —— 旋轉螢幕（0.5625）
2026-08-09 已經定案 62svh ＋ 窄帶疊在照片上，那個決定沒有被推翻，兩段刻意不重疊。
> **要不要讓旋轉螢幕也改成這一版，是還沒問過的一題。**

### 定案的內容

| | 值 |
| --- | --- |
| 照片高度 | **70svh**（四階裡選的：100svh 太裁／62svh／70svh／不裁太扁） |
| 窄帶 | **脫離照片、接在下面**（`.hero > .band` 改 `relative`） |
| 窄帶高度 | `min-height` 84 → **104**（使用者要「往下移一些」），實際撐到 126.4 |
| 地址電話 | **一行置中，夾在瀏覽數與三格之間**（＝手機版的位置） |
| 接縫 | **保留模糊＋柏油** |

### 接縫：整節的重點

使用者的條件是「新接的窄帶必須和照片裡的柏油路加模糊的下半部接在一起」。

把 `assets/hero-clinic-night.jpg` 畫進 canvas 逐列取整列平均：

| 位置 | 色 | L* |
| --- | --- | --- |
| 底起 1px | **#131110** | 5.2 |
| 底起 4px | #141211 | 5.6 |
| 底起 8px | #161412 | 6.5 |
| 底起 32px | #1b1817 | 8.6 |
| 底起 64px | #211c1a | 10.8 |
| 底起 200px | #2f2c29 | 18.2 |

**但畫面上看到的不是 #131110。** 窄帶的柏油層底端是 `rgba(22,20,19,.88)`，
和照片合成之後：

```
.88×(22,20,19) + .12×(19,17,16) = (21.6, 19.6, 18.6) → #161413
```

正好就是 `.band::after` 那段註解寫的值。**所以接縫要接的是 #161413。**

做法**不重畫**，把現在那兩層原地搬家：

```css
/* 原本 inset: 0（疊在窄帶自己身上） */
.band::before, .band::after { top: auto; bottom: 100%; height: 84px; }
```

兩層就原封不動移到照片的最後 84px 上 —— 模糊強度、mask、柏油漸層的每一個值
都沒有動，所以照片下半部看起來和改動前完全一樣。窄帶再從 #161413 起跑：

```css
background-image: linear-gradient(180deg, #161413 0%, var(--band-bg) 62%, var(--band-bot) 100%);
```

方向是**往下變亮**，和手機版同一個方向 —— 下緣要有明度才接得住紙色。

落選的「直接接」（兩層關掉、照片直接結束）會在照片上切出一條硬邊，
車子與路面的細節突然中斷，一眼就看得到。

### 兩個不要違反的

1. **`.hero > .band` 要用 `relative`，不能用 `static`。**
   `.band::before`／`::after` 是 `absolute`，`.band` 若是 `static` 就不是它們的
   包含塊，兩層會改用 `.hero` —— 於是**整張照片被 blur 糊掉再蓋一層柏油色**。
   手機版那段註解記過同一個坑。
2. **`.hero-copy` 的 `z-index` 要抬到 4。** 模糊層現在蓋在照片最後 84px 上，
   而收尾句「到巷口的芳仁」正好落在那一段，不抬的話它會跟著被糊掉。

### 還沒解決的

**這一段只涵蓋直立比例。** 視窗寬 721–870 但**橫向**的情況（例如 768×500 的
分割畫面）仍然是舊的兩行絕對定位，第三格與地址電話還是會撞（實測 −25.1px）。
那是 `/history/band-tablet-gap.html` 的 Ⓐ／Ⓑ／Ⓒ 在處理的事，**還沒定案**。

### 逐項比對

改動前後放進同尺寸 iframe 比對照片高、窄帶 position／高度／上緣、聯絡的
position／排法／寬度、詩與收尾句的字級、`.hero-copy` 下內距、文件寬：

**1440×900、1280×800、375×812、1080×1920 四組完全相同**，只有 iPad 那一組有變。

---

---

## 六之十九、照片與窄帶的接縫（2026-08-12 四輪定案上線）

使用者在 iPhone 上回報：「hero 圖片到窄帶隱隱約約還是看得到分界線，
**電腦版翻轉螢幕、iPad 這個分界更明顯**。」

### ⚠ 第一版做錯了方向 —— 這一節整個重寫過

第一版是**在照片下緣疊柏油漸層**（手機 `22vw`、收到不透明；iPad 84 → 132px；
電腦／旋轉螢幕的柏油層往上多長 64px）。接縫確實不見了，但使用者一看就說：

> 「你把照片的下半部做成漸層黑然後接上窄帶這個做法錯的，應該是要把窄帶做成
> 比較深的漸層接在照片下半部，因為這樣照片下半部整個都黑掉的很奇怪，
> 照片都被弄掉了。」

他是對的。**手機的照片只有 291px 高（390 寬），`22vw` 是它的 29%** ——
等於為了一條線把整條街收掉。

**所以規則是一句話：接縫在窄帶那一側解決，照片不准動。**
日後再遇到「兩塊東西接不起來」，先問「能不能只改我自己這一塊」。

### 量法

Playwright 逐視窗擷圖 → 用 `zlib` 自己解 PNG（容器裡沒有 PIL、沒有 ImageMagick）
→ **每一列取中位數 L\***，再看相鄰兩列的差。

⚠ **一定要取中位數，不能取平均。** 照片下半部有路燈、車頭燈、門口的燈，
幾顆亮點就能把整列的平均拉高 4 個 L\*（iPad 接縫上方實測：中位數 6.45、平均 10.4）。
拿平均去比對接縫會得到完全錯的結論。

### 三種版型三個成因（不是同一件事）

| 版型 | 成因 | 改動前 |
| --- | --- | --- |
| 手機（≤720） | **顏色與色溫都對不上。** 窄帶第一列是 `--band-top` `#17191d`（L\* 8.71、冷，B−R +6），照片最後一列是 `#151311`（L\* 6.01、暖，R−B +4） | **ΔL\* +2.69 ~ +3.65**，還換色溫 |
| iPad 直放 | 顏色很準（柏油層收在 `.88`，和照片合成是 `#161413`），但**照片還剩 12% 活著** —— 門口的燈、車頭燈那些亮點一路撐到接縫，過了接縫變成 0。看到的不是亮度跳階，是**殘影突然斷掉** | ΔL\* +0.31 ~ +0.37 |
| 電腦／旋轉螢幕 | 沒有跳階。但兩層的 alpha 都是**線性**從 0 起跑，而**一條線性漸層在它的起點是一條線** —— 值連續、一階導數在那一列跳階（馬赫帶）。整條窄帶因此讀起來像一塊貼上去的長方形 | 中位數看不出來，擷圖上看得到 |

### 定案

**1. 手機：窄帶從 `#151311` 起跑，整條 smoothstep 走到底。照片一個像素都不碰。**

`#151311` 是照片最後一列的實測整列平均，這個值很穩 ——
320／360／375／390／414／430 六個寬度量下來都是它（720 才掉到 `#141211`）。
手機的裁法是「右緣裁 13%、下緣完整落地」，各寬度看到的是同一批像素等比例縮放。

- **顏色接上** → 跳階歸零。
- **斜率也要接上**：照片往下變暗、窄帶往下變亮，在同一列折返會長出一條馬赫帶。
  整條用 smoothstep，而 **smoothstep 在起點的斜率本來就是 0** ——
  接縫這一側自動是平的，不必另外寫平段。
- 整條因此**比原本深**（上半段 L\* 8.5 → 6.0）—— 這正是使用者要的
  「窄帶做成比較深的漸層」。

```css
background-image: linear-gradient(180deg,
  #151311 0%,
  #161412 10%, #181615 20%, #1a1919 30%, #1d1d1e 40%,
  #212224 50%, #25262a 60%, #282a2f 70%, #2b2d33 80%, #2c2f36 90%,
  var(--band-bot) 100%);
```

> ⚠ `--band-bot` 沒動，接紙色那一頭完全照舊。
> ⚠ **不是換一組配色。** 藍黑三停（六之三節第 1 點）當初是「夾在夜空與紙色中間、
> 兩邊都要接」取的中間值，而窄帶的**上緣接的是柏油、不是夜空** ——
> 電腦版 2026-08-07 就為同一個理由把上緣換掉了，手機這裡是補上同一件事。
> `--band-top` 仍然留在變數表裡（`.hero` 的底色用它）。

#### 1a. 漸層要攤在**整條**窄帶上（使用者第二輪回報）

> 「感覺這個漸層從黑邊到深藍有點短，看起來很突兀，可以再拉長一點。」

成立。第一次做出來的是「前 12% 平段 ＋ 12%~62% 走完 ＋ 後 38% 幾乎不動」——
**13.8 個 L\* 全部擠在中間那 53px 裡**，107px 高的窄帶只用了一半。
改成 0% 一路走到 100%（平段拿掉，靠 smoothstep 自己的零斜率起點），
長度 53px → **107px**。

#### 1b. 黑要再往下壓（使用者第三輪回報）

> 「看起來黑漸層可以再往下一點。」

成立。攤開之後那條曲線是**對稱的** smoothstep，走到窄帶一半就已經是中間色。
把參數先取 **1.6 次方**再丟進 smoothstep（`S(t^1.6)`）：

| | 對稱 smoothstep | `S(t^1.6)`（現在） |
| --- | --- | --- |
| 「還算黑」（L\* 低於 10）撐到窄帶的 | 36% | **51%**（手機 55px／iPad 63px） |
| 50% 的位置 | L\* 13.2 | **L\* 9.8** |
| 最陡的一列 | 0.194 | 0.217 每像素（＋12%） |

⚠ **頭尾兩端的斜率仍然是 0** —— 起點展開是 `3t^3.2`，終點 `S′(1)=0` 乘任何
東西都是 0。所以接縫那一側維持平的，「拉長」那一輪的好處一個都沒有還回去。
最陡的一列雖然回升 12%，也還只有沒拉長之前（0.326）的三分之二。

```css
/* 手機。iPad 同一條曲線，只有起點是 #161413 */
background-image: linear-gradient(180deg,
  #151311 0%,
  #151311 10%, #151312 20%, #161513 30%, #181716 40%,
  #1c1b1b 50%, #1f1f21 60%, #232428 70%, #282a2f 80%, #2b2e34 90%,
  var(--band-bot) 100%);
```

⚠ **`--band-bg`（L\* 17.5）因此不再是這條漸層的色停。** 它原本是「中段」那一停，
而中段現在由曲線自己算出來。**變數沒有拿掉** ——
`background-color` 的後備、`.stats li` 那條分隔線的 `color-mix` 都還在用它。

⚠ 8-bit 的暗部漸層拉長會擔心色帶，實測**沒有**：Chromium 會自己抖動，
量出來 107px 裡有 **65 個色階、平均 1.7px 一階**（最長的一階是最上面那段
刻意留黑的 13px，那一段本來就該是同一個顏色）。

⚠⚠ **這一輪踩到一個 CSS 註解的坑，很容易再犯。** 有一版把單位寫成
`L星號 斜線 px` —— **那兩個字元連在一起就是 CSS 註解的結束記號**，
整段註解在那裡被提早關掉，底下 `.band` 的整條規則跟著失效
（`background-image` 變成 `none`、接縫又跳回 +2.7），而且**完全不報錯**。
和「CSS 註解裡不能出現樣式表的結束標籤」是同一類（CLAUDE.md 第九節第 8 條）。

**2. 兩層的 alpha 一律改成 smoothstep（3t²−2t³）。範圍一個像素都沒有多吃。**

頭尾兩端的斜率都是 0，起點與終點都找不到。**顏色一個都沒動** ——
`#221e1c` → `#161413` 兩個實測值、窄帶內部 `0 / .22 / .58 / .88` 四個濃度停點
全部維持六之三節的原值，**改的只有 alpha 的形狀**。
模糊層的 mask 從 `0% → 38%` 的直線改成 smoothstep 收在 44%
（終點往後挪，否則 smoothstep 前段走得慢、中段會比原本濃）。

**3. iPad：柏油層只把最後 24% 收到全不透明，高度維持 84px。**

76% 之前和定案完全相同；76%~100% 那 20px 從 `.88` 收到 `1`。
那 20px 的照片本來就已經被壓掉 88%，再收掉剩下的 12% 看不出來，
但**殘影變成漸漸消失的**。接縫的顏色仍然是 `#161413` ——
`.88×柏油 + .12×照片` 那條算式的極限就是它，數字沒有變，
順帶讓那條算式從「約等於」變成「就是」。

> ⚠ 這**不是**六之三節否決掉的「深一階」。那一案不透明的位置在窄帶的**下緣**
> （＝螢幕最底），會變回一條實色的線；這裡在**照片與窄帶的交界**，
> 窄帶從同一個顏色接下去，沒有任何一條邊。兩件事只是 alpha 都走到 1，方向相反。

iPad 窄帶的漸層做法同手機：從 `#161413` 起跑、整條走到 `--band-bot`（126px 全用上）。

### 量出來的結果

| 視窗 | 改動前 ΔL\* | 改完的 ΔL\* |
| --- | --- | --- |
| 320／360／390 | +2.69 | **0.00** |
| 375 | +2.79 | +0.10 |
| 414／430 | +2.99／+3.02 | +0.30／+0.33 |
| 600／720（平板直放，少見） | +3.31／+3.65 | +0.61／+0.92 |
| 744／820／834／1024／768×1024（iPad 直放） | +0.31 ~ +0.37 | **0.00** |
| 1080×1920／1200×1920／1440×2560（旋轉） | 窄帶疊在照片上，修的是它的**上緣** | 同左 |

二十二個視窗全掃過，**沒有任何一個出現橫向捲動**。

### 照片被動到多少（這一版的重點）

| | |
| --- | --- |
| 手機 | 照片那 291 列**逐像素比對，0 個像素有差**（最大差 0）。動到的只有窄帶那 107 列 |
| 電腦（1440×900） | 柏油層的範圍沒有變；窄帶那 84px 逐列量，差在 **±0.75 L\* 以內**，而且**多半是變亮** —— smoothstep 的前段比原本的線性更透，照片反而露得更多。窄帶上緣以上完全相同（0.00） |

### 落選：第一版（2026-08-12 當天稍早）

| 做法 | 為什麼落選 |
| --- | --- |
| 手機在照片下緣疊 `22vw` 的柏油層、收到 alpha 1（alpha 用 smoothstep 的平方 S²） | 接縫是不見了，但**吃掉照片 29% 的高度**，路面與亮著燈的門面整片變黑。使用者：「照片都被弄掉了」 |
| iPad 柏油層 84 → 132px | 同上，多吃照片 48px。改成「維持 84px、只收最後 24%」 |
| 電腦／旋轉螢幕柏油層往上多長 64px（`inset: -64px 0 0 0`） | 濃度只到 `.04`、其實看不出來，但同一個方向。**smoothstep 本身就夠了** —— 拔掉之後那條線一樣不見，而且照片露得比原本多。連帶 `.hero-copy` 的 `z-index` 也不必從 2 抬到 4 |

---

## 六之二十、加到手機主畫面的圖示（2026-08-12）

使用者提供了診所的兩個 Illustrator 原始檔（`20241205_芳仁牙醫_LOGO_外框.ai`、
`20241125_芳仁牙醫_Pattern.ai`），要求「抓出網站上用的圖案，做成可以放到主畫面的圖示」。

### 先量到的事：站上那顆標誌**本來就是原廠向量**

`.ai` 是 PDF 相容的，內容流讀得出來。`LOGO_外框.ai` 第 3 頁中間那個圓圈裡的標誌本體，
和 `index.html` 頁首那條路徑逐點比對：

| | |
| --- | --- |
| 比例 | 等比例縮 **0.6454888747** 倍（只有縮放與位移，沒有非等比） |
| 節點 | 25 個（8 段貝茲 ＋ 起點） |
| 最大殘差 | **0.0022**（在 44.2873 寬的 viewBox 上，＝ 0.005%） |
| 原始外框 | 68.6097 × 33.8115 pt，長寬比 **2.02918** ＝ 站上 viewBox 的比例 |

殘差就是 PDF 座標只有六位小數的進位誤差。**也就是說站上那顆不是誰描出來的，
本來就是原廠檔**，這次沒有「換一個圖形」，只是把同一條路徑重新正規化成 1000 單位寬
（省掉 `index.html` 那組 `translate(-232.8729 -333.8286)` 偏移）。

> ⚠ **AI 檔裡的標誌是空心的**（2.293pt 線框），站上用的是**把中線填實**那一版。
> 這次跟著站上走 —— 使用者要的是「網站上用的圖案」。

### 順帶驗證到：`Pattern.ai` 裡的九個形狀就是九顆官方色票

第一節那張「其實是九顆」的表（來自 `104_logo.pptx`）這次得到獨立佐證 ——
`Pattern.ai` 的九個牙形各上一色，取出來和第一節的九顆逐一對得上，
每個通道差 1~2（PDF 的 CMYK→RGB 進位）：

| 色票 | 本節從 AI 檔取出 | 第一節（`104_logo.pptx`） |
| --- | --- | --- |
| Deep Taupe | `#815751` | `#805751` |
| 焦糖褐 | `#9E7253` | `#9E7253` |
| 磚紅 | `#944449` | `#944449` |
| 灰紫 | `#5E5A62` | `#5E5A61` |
| 藍灰 | `#305569` | `#315568` |
| 深綠松 | `#104C46` | `#0B4B46` |
| 苔綠 | `#5A6E50` | `#5A6E4F` |
| 稻草灰 | `#A8A998` | `#A8A998` |
| 米色 | `#D3CBC6` | `#D3CBC5` |

**這不是新的取值來源**（同第一節那句話），站上的色一個都不要為了對齊它去改。
用處是：九顆色票這件事現在有兩份互相獨立的證據。

### 定案的配色：紙色底 ＋ 一般牙科的綠（使用者第二輪指定）

| | 色值 | 來自哪 |
| --- | --- | --- |
| 底 | `#e2e5e6` | `--paper`，全站內文底色 |
| 牙 | `#3f654a` | **一般牙科的套色**（`[data-spec="general"]` 的 `--accent`），第六之十一節第七小節 |

對比 **5.23:1**，遠高於非文字圖形的 3.0 下限。**兩顆都不是新色。**

第一版用的是 `#4f6361`（favicon 的診療椅綠，對紙 5.04），使用者看過之後說
「**紙色底，但用一般牙科 定期檢查的綠**」——〈定期檢查〉那一篇的 `<body data-spec>`
就是 `general`，所以指的是同一顆。**份量幾乎沒變（5.04 → 5.23），換的是色相。**

> ⚠ **一般牙科有兩階，這裡取的是「套色」那一階，不是深階 `#2c5238`（對紙 6.99）。**
> 深階是「白底上的**字**」用的，套色才是「填實的**塊**」用的 ——
> 首頁的 chip 按下去就是 `#3f654a` 填滿站在紙色上，和這顆圖示是同一種用法。
> 標誌是一塊填實的形狀，不是字。三階都算成實際的圖比對過才選的。

> ⚠ **主畫面圖示和分頁列圖示現在不同色，這是刻意的、不要「順手」改成一致。**
> 兩者的底完全不同（一個是紙色磚、一個是使用者的分頁列），
> 對比也是各自算的 —— 見下面「為什麼這顆和 `assets/favicon.svg` 是兩個檔」。

一起做成圖看過、沒有選上的三案（`preview/` 沒開頁，是直接算成 PNG 給使用者看的）：

| | 底 | 牙 | 為什麼沒選 |
| --- | --- | --- | --- |
| Ⓑ | `#D3CBC5` 米色 | `#5D6D55` 苔綠 | 兩顆都是官方色票、最貼近品牌紙感，但和站上現在的綠不同調 |
| Ⓒ | `#214D48` 深綠松 | `#D3CBC5` 米色 | 深色磚，桌布上最跳，**但方向接近 2026-08-02 被換掉的「深綠圓角方塊 ＋ 白牙」** |
| Ⓓ | 白 | `#9E7253` 焦糖褐 | ＝ AI 檔第三個圓圈實際用的那一版，最忠於原始 logo，但和站上的綠色系不同調 |

> 四案都是「紙色／米色／白 ＋ 一顆綠或褐」，使用者第一輪沒有從中挑，
> 而是直接指定了一顆站上既有的科別色（見上面）。**Ⓐ 的底 `#e2e5e6` 留著，
> 只有牙的顏色換掉。**
>
> **要再換只改 `assets/icon.svg` 裡那兩個色值，再跑 `node tools/app-icons.mjs`**，
> 其餘不必動 —— 但**記得把各頁 `icon-180.png?v=` 換成當天**，
> Safari 的圖示快取是按網址存的（和 favicon 同一個坑，第六之七節）。

### 為什麼這顆和 `assets/favicon.svg` 是兩個檔

第六之七節那句「**不要用加個底色方塊來解**」講的是**分頁列**那顆 ——
它的底是透明的，而且真的可以透明。主畫面這顆**沒有這個選項**：

| | `assets/favicon.svg` | `assets/icon.svg` |
| --- | --- | --- |
| 用在哪 | 分頁列 | 主畫面（iOS 加入主畫面／Android 加到主螢幕） |
| 底 | **透明** | **不透明**（滿版 `<rect>`） |
| 顏色 | 兩色，SVG 裡的 media query 分深淺分頁列 | 一色 |
| 牙洞 | 放大 **1.6 倍**、上移 1.05 | **原始比例，不放大** |
| 最小尺寸 | 16px | 180px |

三個差異都是被平台逼出來的，不是風格選擇：

1. **底非有不可。** iOS 收到帶 alpha 的 `apple-touch-icon` 會直接壓在**純黑**上。
   標誌是暗色的，壓完就是一塊看不出東西的黑磚。
   所以「主畫面圖示是一塊磚、四周留白是磚的一部分」——
   那和「為了填滿方框而加圓角底色」是兩件事。
2. **牙洞不放大。** 1.6 倍是為了 16px 下那個洞不被抗鋸齒吃掉；
   這裡最小 180px，洞有 14px 寬，放大反而失真。
3. **只要一色。** 主畫面沒有深淺兩種底可分（桌布是使用者自己的）。

### 大小：76%／68% 兩個版本

標誌是橫的 2.02918:1，兩個平台的安全範圍不一樣，**取小的那個還要留餘裕**：

| | 給誰 | 標誌寬 | 算式 |
| --- | --- | --- | --- |
| 一般 | iOS `apple-touch-icon`、Android `purpose: any` | 778px／**76.0%** | iOS 圓角約邊長 22.4%，標誌垂直落在 y 320~704，離角落曲線區（229px）很遠，左右吃的是直邊 |
| maskable | Android `purpose: maskable` | 696px／**68.0%** | 安全區是**直徑 80% 的圓**。2.02918:1 的矩形塞進直徑 819.2 的圓，寬度上限 **734.8px（71.8%）**，76% 會超出 |

maskable 那張由 `tools/app-icons.mjs` 把同一份 `icon.svg` 的倍率換掉再算一次 ——
**兩張的路徑與顏色永遠同一個來源**，不會有一邊改了另一邊忘了改。

### 產物與怎麼重算

```bash
node tools/app-icons.mjs           # 重新產生
node tools/app-icons.mjs --check   # 只比對，不一致就回傳非零
```

| 檔 | 尺寸 | 誰在讀 |
| --- | --- | --- |
| `assets/icon-1024.png` | 1024 | iOS，`<link rel="apple-touch-icon">`（**刻意不是 180**，見下一段） |
| `assets/icon-192.png` | 192 | Android，`site.webmanifest` 的 `purpose: any` |
| `assets/icon-512.png` | 512 | 同上，安裝／啟動畫面用大的那張 |
| `assets/icon-maskable-512.png` | 512 | Android，`purpose: maskable` |

和 `tools/favicon-ico.mjs` 一樣需要 headless Chromium，**`npm run build` 不會呼叫它**，
產物已進版控，跑不動不影響建置與部署。

### `site.webmanifest` 的 `display` 是 `"browser"`，不是 `"standalone"`

刻意的：這是一個**會連出去**的內容網站（Google 地圖、LINE、電話），
`standalone` 會把網址列與上一頁一起收掉，等於把人關在裡面。
同理**沒有加** `<meta name="apple-mobile-web-app-capable">`。
加到主畫面照樣有圖示、有名字，點下去在瀏覽器裡開 —— 這就是使用者要的。

`<meta name="apple-mobile-web-app-title" content="芳仁牙醫">` 則是必要的：
不寫的話 iOS 會拿 `<title>` 去截，首頁那句是
「芳仁牙醫診所｜雲林斗六・永樂街」，截完會變成半句。

### 踩到的坑：`String.replace` 又換到註解裡那一個

maskable 版原本是 `source.replace("scale(.778)", "scale(.696)")`。
`icon.svg` 的**註解裡就在討論那個倍率**，字面上共三處，真正生效的 `<g>` 排在最後 ——
`replace` 換到的是**註解裡第一個**，`<g>` 紋風不動。

**症狀最惡劣的地方是它完全不報錯**：兩張 512 都產出來了、尺寸對、顏色數一樣、
**逐位元組完全相同**（7900 B），檔案列表上看不出任何異狀，`cmp` 過才發現。

和 CLAUDE.md 第八節那個 `String.replace('</body>', …)` 換到註解裡那一個是**同一種坑**。
這一站的註解寫得又長又常在討論程式碼本身，特別容易中。

現在的做法：比對**整個 `transform` 屬性**（含前面那段位移到畫布中心的 translate），
而不是單獨的倍率；另外加一道**「只准命中一次」**的檢查 ——
0 次代表 `<g>` 的寫法被改過（maskable 會悄悄退化成一般版），
≥2 次代表比對太寬鬆咬到註解，兩種都當場停下來。
> 這道檢查隨即就派上用場：改完之後我把新的註解寫成引用那個屬性字串本身，
> 命中變成 2 次、腳本立刻擋下來。**要在註解裡指這件事就用文字描述，不要貼字串本身**
> —— 和第八節那條「不要在 CSS 註解裡貼結束標籤」是同一條規則。

### 保險絲：把像素讀出來看

`tools/favicon-ico.mjs` 的註解記過兩種「看起來很正常的空圖」
（Chromium 畫不出 `<path>` 只剩底色、SVG 被改壞整張透明），兩種都不會讓 Chromium 報錯。
所以這支腳本產完每一張都會用 `zlib` 自己解 PNG 檢查：

- **顏色數 < 8 就停** —— 只有底色代表標誌沒畫出來（抗鋸齒正常會有兩百多色）。
- **有半透明像素就停** —— iOS 會把它壓在純黑上。

> ⚠ **色彩型態要現讀，不能寫死 RGBA。** `icon.svg` 的底是不透明的，
> Chromium 因此把 alpha 通道整個省掉、寫成**色彩型態 2（RGB，每像素 3 位元組）**；
> favicon 那邊是透明底，出來的才是型態 6。第一版寫死 6，結果在**產物完全正確**的
> 情況下報錯。順帶一提，**沒有 alpha 通道正是我們要的結果**。


### 為什麼 apple-touch-icon 給 1024 而不是「尺寸剛好」的 180（2026-08-12 當天第三輪）

第一版照 Apple 文件給 180×180（iPhone 主畫面 60pt × @3x ＝ 180px，尺寸完全吻合）。
使用者上線後加到主畫面，回報「**看起來模模糊糊的**」。

從他的截圖（1125×2436，iPhone @3x）逐像素量，四件事一起指向同一個結論：

| 量的東西 | 結果 |
| --- | --- |
| 標誌邊緣 10%~90% 過渡（螢幕上） | **5px** |
| 同一張截圖裡 Tailscale 白點的同一條邊 | **0px**（硬邊）→ 截圖與螢幕本身是銳利的 |
| 我們自己的 `icon-180.png` 量同一條邊 | **1px** → **檔案本身是銳利的** |
| 螢幕上的牙洞 | 乾淨的 10px，佔標誌寬 **0.0794**（設計值 0.0786） |
| 標誌佔圖示（把 5px 模糊各攤一半扣掉後） | **0.760** ＝ 76%，正是 any 版（maskable 是 68%） |
| 圖示的顏色 | **綠的**（不是第一版的青灰）→ 手機拿到的是上線後的當前檔，不是舊快取 |

**牙洞那一條是關鍵，它排除了「拿小圖放大」。** 5px 的過渡若來自解析度不足，
等效來源只有 36px 上下，那麼牙洞在來源裡只有 2.8px、放大後會糊成一團，
比例也不可能還對到小數第三位。**乾淨的 10px 洞 ＋ 正確的比例 ＝ 圖是對的、只是被模糊了。**

所以：**iOS 拿到的是正確且銳利的圖，是它自己又縮放了一次。**
1:1 不會糊，所以它渲染的尺寸不是 180。**放大會糊，縮小不會** ——
於是改成餵它 1024，不管 iOS 想畫多大（180、512、或 iOS 26 那套玻璃效果用的更大畫布），
都變成往下縮。

> ⚠ **不要為了「符合文件」再補一個 180 的 `<link>`。**
> 兩個都宣告的話 iOS 會挑尺寸剛好的那一個（180），等於繞回原地。
> 這一站刻意**只留一個** `apple-touch-icon`。
>
> ⚠ **圖示換了之後，主畫面上那顆不會自己更新** —— iOS 是在「加入主畫面」那一刻
> 把圖抓下來存進那個捷徑裡的。`?v=` 換掉只保證**下一次**抓的是新的，
> 已經放在主畫面上的那顆要**刪掉重加**才會換。這和 favicon 的快取是兩回事，
> 兩個都要處理。
>
> ⚠ 這一輪**沒有動 `icon.svg` 一個字元**（顏色、幾何、大小全部不變），
> 只換了餵給 iOS 的解析度。1024 那張 17.2 KB，只有加到主畫面時抓一次。


### 第四輪：iOS 26 的玻璃效果會把顏色抬亮，用實測反推補回去（2026-08-12）

使用者把那張 PNG 直接用 Safari 打開 —— **是銳利的**。所以檔案、Cloudflare 都沒問題，
問題全部在 iOS 產生主畫面捷徑那一步。

從截圖量出 iOS 疊的那層玻璃**不是均勻的白色覆蓋**，它幾乎只抬暗色：

| | 檔案裡 | 螢幕上 | 位移 |
| --- | --- | --- | --- |
| 綠 | `#3f654a` rgb(63,101,74) | rgb(89,115,94) | **+26 / +14 / +20** |
| 紙 | `#e2e5e6` rgb(226,229,230) | rgb(227,228,230) | +1 / −1 / 0（等於沒動） |

兩個已知點解得出每個通道的線性映射 `out = m×in + b`：

    R: 0.8466×in + 35.7      G: 0.8828×in + 25.8      B: 0.8718×in + 29.5

三個斜率都在 0.85~0.88，是一層**往亮端壓縮**的疊加。既然量得到就補得回去 ——
反推 `in = (target − b) / m`，得到 **`#205533`**，疊加之後正好落回 `#3f654a`。

| 檔案裡放 | 疊加後在螢幕上 | 對目標 ΔE |
| --- | --- | --- |
| `#3f654a`（原本） | rgb(89,115,94) | **9.2** |
| `#2c5238`（一般牙科深階） | rgb(73,98,78) | 6.7 |
| **`#205533`（精確補償）** | **rgb(63,101,74)** | **0.0** |

> ⚠⚠ **`icon.svg` 裡放的永遠是品牌真值 `#3f654a`，補償只發生在 `tools/app-icons.mjs`。**
> 來源檔要看得出「這顆圖示是什麼顏色」，補償是輸出階段的裝置修正 ——
> 就像印刷的打樣補償不會回頭改設計稿。要關掉把 `COMPENSATE` 設成 `false`。
>
> ⚠ **`#205533` 不是新的品牌色，不要拿去用在別的地方。** 它是一個裝置補償值，
> 存在的唯一理由是「疊完之後等於 `#3f654a`」。站上任何其他地方要用綠，
> 一律回頭讀第六之十一節。
>
> ⚠ **這組數字是從那一支手機、那一組設定量到的。** 使用者若打開「減少透明度」，
> 或 Apple 之後改了效果，補償就會過頭（圖示偏暗）。
>
> ⚠ **模糊沒有解，也沒有在這一輪處理。** 邊緣過渡從檔案的 1px 變成螢幕上的 5px，
> 是 iOS 產生捷徑圖示時自己重新取樣的，網站這側沒有介面可以影響。
> 使用者自己的判斷也是「模糊的問題應該無解」，這一節只修顏色。

**排除掉的（都有量測，不要再重查）**：不是 Cloudflare（PNG 直接開是銳利的）、
不是我們的檔案（1px 過渡）、不是淺色底（同一張主畫面上 McDonald's／台灣虎航／簡報
都是淺色底且銳利）、不是 favicon、不是 maskable、不是解析度不足。


### 第五輪：圖示底色改純白（2026-08-12，使用者指定）

使用者把圖示和 Google Maps 放進同一個資料夾，說「**我要底色像 Google Maps 那樣的亮度**」。
從那張截圖量：

| | 檔案裡 | 螢幕上 |
| --- | --- | --- |
| Google Maps | 純白 | rgb(255,255,255) |
| 芳仁（紙色底） | `#e2e5e6` rgb(226,229,230) | rgb(227,228,230) |

**兩顆都原封不動地通過了玻璃效果** —— 這證實了第四輪的觀察：
**那層效果只抬暗色，亮端完全不碰**。所以螢幕上就是實打實差 **27 階亮度**，
並排一看就很明顯，跟任何效果都無關，是底色本來就不一樣。

所以底色從 `--paper` `#e2e5e6` 改成**純白 `#ffffff`** —— 白進去就是白出來，
和 Maps 同亮度。順帶把標誌對底的對比從 5.23:1 拉到 **6.62:1**。

> ⚠ **這是圖示專用的決定。站上的底色仍然是 `--paper`，不要跟著改。**
> 圖示是獨立站在別人的桌布上、和別家 App 並排比的，那個情境和網頁內文無關。
>
> ⚠⚠ **第四輪那條線性模型只在量測點附近有效，不要拿去外推。**
> 用兩點（綠、紙）解出的線性式預測「純白 → 螢幕 251.3」，
> 但 Maps 實測是 **255**。真實的曲線是**亮端等於原值、暗端才抬**，不是一條直線。
> 綠色的補償值 `#205533` 是往**比量測點更暗**的方向外推出來的，
> 同樣可能有偏差 —— 要校準就再拿一張主畫面截圖量一次螢幕上的綠，
> 和目標 `#3f654a` 比對後修正 `GLASS` 那組數字。


### 第六輪：照別站的成功案例重做 —— 只給一張 192（2026-08-12）

使用者去把 **blog.ichentsai.tw**（同一支手機、同一個流程，圖示完全銳利）的原始碼挖出來，
關鍵只有一行：

```html
<link rel="apple-touch-icon" sizes="192x192" href="/icon-192.png?v=4">
```

**192 只比 iPhone 主畫面要的 180 大 6%。** 放大會糊，大幅縮小也會糊
（快速降採樣），只有「幾乎不用縮」才銳利 —— 而我們前五輪一路走反方向。

| 輪次 | 我們給 iOS 的 | 縮放比 |
| --- | --- | --- |
| 一 | apple-touch-icon 180，manifest 有 512 | 1:1，但 manifest 有更大的可挑（÷2.8） |
| 二~五 | apple-touch-icon 1024，manifest 1024 排第一 | **÷5.7** |
| **六（現在）** | **只有 192，全站唯一的 any 版** | **÷1.07** |

第二個關鍵是**不要給一整組**。第五輪結束時我們同時掛著
根目錄 `apple-touch-icon.png` 與 `-precomposed.png`（各 180）
＋ manifest 的 192／512／1024／maskable —— 五種候選。
**候選一多，iOS 會挑錯那張再縮放**，這解釋了為什麼「根目錄放 180、1:1」那一輪仍然糊：
iOS 根本沒用那張。

現在整站能被 iOS 找到的 any 版**只有 `assets/icon-192.png` 一張**：

- 根目錄的 `apple-touch-icon.png`／`-precomposed.png` 刪掉了
- `icon-1024.png`、`icon-512.png` 刪掉了
- manifest 只剩 192（any）＋ maskable 512（Android 專用，purpose 不同）

> ⚠ **不要「順手」補回任何一個 any 版尺寸**（57／76／120／152／512／1024），
> 也不要再放根目錄的 `apple-touch-icon.png`。那就是把選擇權還給 iOS，等於繞回原地。
>
> ⚠ **`sizes` 屬性只是宣告，iOS 不會驗證。** 檔案實際像素必須真的是 192×192 ——
> `tools/app-icons.mjs` 產生時會檢查尺寸對不上就丟例外。
>
> ⚠ **`?v=` 要保留並在換圖時加一。** 第三輪我曾經把它拿掉（理由是「捷徑是加入當下
> 存的，查詢字串沒用」）—— 那個理由只對**已經放在主畫面上的那顆**成立，
> 對**下一次抓取**是有用的，別站也是這樣做（他們已經到 `?v=4`）。
> **換圖要同時做兩件事：`?v=` 加一，而且主畫面那顆刪掉重加。**

底色（純白）與顏色補償（`#3f654a` → `#205533`）**這一輪都沒有動** ——
那兩件已經量測確認有效，見第四、五輪。


### 第七輪：192 有效但不夠，補上剩下兩個 meta（2026-08-12）

第六輪上線後，使用者把**三個版本放進同一個資料夾**同框比較（同一秒、同一支手機，
最乾淨的對照）：

| 版本 | 邊緣過渡（換算成同尺寸） |
| --- | --- |
| ① 紙色底（第四輪） | 5.1px |
| ② 白底・1024（第五輪） | 5.1px |
| **③ 白底・192（第六輪）** | **4.0px** |
| 檔案本身 | 1px |

**192 確實有效（改善約 22%），但沒有解決。** 使用者自己看出來「還是有一點重疊的感覺」，
量測支持這個判斷。

⚠ 順帶一筆：**Google Maps 用這個指標量出來是 17px**，但那不代表它糊 ——
它的圖釘本來就是漸層色塊，沒有硬邊界可量。**這個指標只能拿來比我們自己的版本**，
不要拿去跨圖形比較。

逐項比對 blog.ichentsai.tw 之後，圖示的宣告已經完全一樣（192 ＋ `sizes` ＋ `?v=`），
只剩兩個 meta 它有、我們沒有：

```html
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="mobile-web-app-capable" content="yes">
```

這一輪補上，並把 `site.webmanifest` 的 `display` 從 `"browser"` 改成 `"standalone"`
（兩者本來就該一致，`browser` 配上使用者手動打開的「打開為網頁 App」是矛盾的狀態）。

> ⚠⚠ **副作用：從主畫面開啟會變成「網頁 App」模式，沒有網址列。**
> 原本 `display: "browser"` 的理由是「這站會連出去（地圖、LINE、電話），
> standalone 等於把人關在裡面」—— 那個顧慮**實際上不成立**：那幾個都是外部 App，
> 照樣會跳出去；站內導覽靠頁首選單。而且使用者每次加入時本來就手動把
> 「打開為網頁 App」打開了，這只是把它變成預設。
>
> ⚠ **要退回**：把那兩個 meta 刪掉，`display` 改回 `"browser"`。
>
> ⚠ **如果這一輪仍然糊，下一個要拆的是 manifest 裡的 maskable 512** ——
> 那是全站僅存的大檔。理論上 `purpose: maskable` 不會被 iOS 當成 apple-touch-icon，
> 但「候選一多就挑錯」這件事已經證實過一次了。


### 第八輪：做一個「拆掉所有宣告」的對照頁（2026-08-12）

第七輪（補 web-app meta ＋ display: standalone）上線後，使用者回報**還是有**。

到這裡已經試過的變數：尺寸（180／192／1024）、候選數量（五個 → 一個）、
底色（紙色／純白）、顏色補償、根目錄慣例路徑、查詢字串、web-app meta、display。
再盲改下去只是浪費時間，所以改成**做一個能一次分辨清楚的對照**。

**`/preview/icon-test/`** —— 和首頁用**完全同一張圖檔**，但那一頁刻意什麼都沒有：

- 沒有 `site.webmanifest`（連 `<link rel="manifest">` 都沒有）
- 沒有 `apple-mobile-web-app-capable` / `mobile-web-app-capable`
- 沒有 `apple-mobile-web-app-title`
- 沒有任何其他尺寸的圖示宣告

只留一行 `<link rel="apple-touch-icon" sizes="192x192">`。**圖檔相同、顏色相同，
唯一的變數是「宣告的環境」。**

| 結果 | 結論 |
| --- | --- |
| 對照頁那顆**銳利** | 是我們多宣告的東西害的 → 回首頁拆掉對應的部分 |
| 兩顆**一樣糊** | 和宣告無關，是 iOS 對這張圖本身的處理 → **停手**，站方沒有能做的事 |

同一輪也把 `site.webmanifest` 的 icons 清到**只剩 192**（拆掉 maskable 512，
全站最後一個大檔）。`assets/icon-maskable-512.png` 仍然照產、照進版控，
確認不是它之後把那一筆加回去就好。

> ⚠ 對照頁定案後要**刪掉**（CLAUDE.md 第八節的規則），推導文字搬進 `history/`。


### 第九輪：透明底測試頁（2026-08-12）

使用者問：「改成沒有背景、跟著手機主題變，會不會就沒有這問題？」

**先講查證結果：iOS 不支援網頁捷徑的深淺色自適應圖示。**
Apple 開發者論壇 [thread/801448](https://developer.apple.com/forums/thread/801448)
（2025-09 提問，至今**零回覆**）問的正是這件事：`apple-touch-icon` 或 manifest
能不能像原生 App 那樣提供 dark/tinted 變體。原生 App 有 Icon Composer 與
asset catalog，**網頁捷徑沒有對應機制**。
透明的部分 iOS 是**直接填黑**，不會跟著主題變。

> ⚠ 這一條和第六之七節那句「favicon 可以透明」不衝突 —— 分頁列那顆真的可以透明，
> 主畫面這顆不行。兩者的差別本節從第一輪就寫在 `icon.svg` 的註解裡。

**但這個方向仍然值得測，理由不一樣**：到目前為止所有「銳利」的參考案例
底色都是**深**的（blog.ichentsai.tw 那顆是黑底、Tailscale 也是），
而我們糊掉的版本**全部是淺底**。深底是唯一還沒試過的變數。

所以做了 `/preview/icon-test-b/`：宣告環境和測試頁 A 完全一樣（乾淨、
只有一行 `apple-touch-icon`），**唯一的變數是圖檔沒有背景**，
標誌改用官方色票的**米色 `#D3CBC5`**（深綠 `#3f654a` 疊在黑底上只有 3.17:1，
等於看不見；米色是 13.12:1）。

| 三顆比下來 | 結論 |
| --- | --- |
| A（乾淨宣告・白底）銳利 | 是我們多宣告的東西害的 |
| B（乾淨宣告・深底）銳利 | 是**淺底**害的 → 配色要取捨 |
| 三顆一樣糊 | iOS 就是這樣處理這張圖，**到此為止** |

> ⚠ `assets/icon-192-alpha.png` **只給測試頁用，正式站沒有引用**。
> 它是唯一帶 alpha 的主畫面圖示，`tools/app-icons.mjs` 的
> 「不准有半透明像素」保險絲對它是跳過的（`alpha: true`）。
> ⚠ 它**不套玻璃補償** —— 補償是為那顆暗綠算的，淺色標誌幾乎不會被抬。
> ⚠ 測試結束後這兩頁與這張圖都要刪掉。


### 第九輪的結果：**站方無解，到此為止**（2026-08-12 結案）

兩個對照頁都加到實機上量了（同一張截圖、同一秒）：

| | 邊緣過渡 |
| --- | --- |
| A・拆光所有宣告 ＋ 白底 | **4px** ← 和正式站一模一樣 |
| B・拆光所有宣告 ＋ 深底（透明被填黑）＋ 米色標誌 | **3px** |
| 檔案本身 | **1px** |

**兩個假設都被否定了：**

1. **不是我們多宣告的東西。** 對照頁 A 把 manifest、web-app meta、其他尺寸全部拿掉，
   只留一行 `apple-touch-icon`，量出來和正式站一樣是 4px。
2. **不是淺色底。** 對照頁 B 換成深底只從 4px 進步到 3px，離 1px 還很遠，
   而且放大看**一樣有** iOS 疊上去的立體處理（上緣亮邊 ＋ 底下陰影）。

結論：**iOS 在產生捷徑圖示時會自己重新取樣並疊上 Liquid Glass 的立體效果，
網站這一側沒有任何介面可以影響它。** 九輪到此結案。

**因此還原了兩件為了診斷而做、但證實無關的改動：**

- `site.webmanifest` 的 `display` 從 `standalone` 改回 **`"browser"`**，
  並把 `apple-mobile-web-app-capable` / `mobile-web-app-capable` 兩個 meta 移除
  （第七輪為了比照別站加的，沒有效果；原本選 `browser` 的理由仍然成立）。
  ⚠ 使用者若想要「從主畫面開啟就是 App 模式」，把那兩個 meta 加回去、
  `display` 改成 `standalone` 即可 —— 那是**偏好**，不是修 bug。
- manifest 的 **maskable 512 加回來了**（第八輪為了排除嫌疑拆掉的），
  Android 的自適應圖示因此恢復。

**留在正式站的三項成果（都有量測依據，不要退回去）：**

| | 效果 |
| --- | --- |
| `apple-touch-icon` 只宣告一張 **192**（`sizes` ＋ `?v=`），全站沒有第二個 any 版候選 | 九輪裡**唯一量得出改善**的一步：5.1 → 4.0px |
| 底色**純白 `#ffffff`** | 和 Google Maps 同亮度（實測兩者都原封不動通過玻璃效果） |
| 顏色補償 `#3f654a` → **`#205533`** | 疊加後正好還原成品牌色，ΔE 9.2 → 0.0 |

> ⚠ **上面第七~九輪引用的那兩組百分比（75.8/85.0 與 90.9/95.2/95.9）是錯的**，
> 我的裁切框吃到桌布與 iOS 自己畫的遮罩邊。修正後的數字見下面第十、十一輪。
>
> ⚠ **不要再為了「圖示有點糊」重開這個案子。** 完整推導與排除清單在
> `/history/icon-homescreen-blur.html`。真的要再試，先讀那一頁，不要重走。


### 第十輪：整片套色版落選，理由是**補償空間**（2026-08-12）

使用者拿 innovarad.tw（新思惟國際，整片紅底 ＋ 白色字標）當例子，
要求做一版整片套色的來測。做了 `/preview/icon-test-c/`（底＝品牌綠、標誌＝白）。

先修正量測方法：前幾輪的裁切框吃到桌布與 iOS 自己畫的遮罩邊（那條邊永遠是硬的），
數字在不同截圖之間亂跳。改成**只取圖示正中央 40%**之後才穩定：

| | 一個像素跨掉整體反差的百分之幾 |
| --- | --- |
| A・白底＋深綠 | 30.5% |
| B・黑底＋米色 | 48.3% |
| **C・整片綠＋白標誌** | **47.5%** |
| 新思惟・整片紅＋白字 | 76.5% |

⚠ **這個指標有一半在量「圖案有沒有又長又直的硬邊」，不是單純在量模糊。**
同一張截圖的原生 App：ChatGPT 93.9%、Gemini 61.2%、Safari 54.6%、
**Gmail 47.7%** —— Gmail 是原生 App，和我們的 C（47.5%）一模一樣。
所以 C 已經在原生 App 的區間裡，剩下的差距是**我們標誌的形狀**
（一團平滑曲線，沒有直邊硬角可以「看起來銳利」），不是缺陷。

**但 C 還是落選了，理由和銳利度無關。** 使用者看出「新思惟的白很徹底，
我們的不是」，量出來完全正確：

| | 標誌內部 | 「幾乎純白 ≥250」佔比 |
| --- | --- | --- |
| 新思惟 | rgb(255,255,255) | **94.7%** |
| C（我們） | rgb(220,227,222) | **2.8%** |
| C 的**檔案本身** | rgb(255,255,255) | **97.9%** |

水平剖面顯示不是漸層、不是邊緣暈開，是**整片被均勻拉掉**。反推混合比例：

    255 − r×(255−32) = 220 → r = 0.157
    255 − r×(255−85) = 227 → r = 0.165
    255 − r×(255−51) = 222 → r = 0.162

三個通道一致：**iOS 把背景色以約 16% 均勻混進標誌。**（新思惟那顆是 0%，
原因不明，沒有查到解釋，也不要編一個。）

**決定性的不對稱：**

| | iOS 的扭曲 | 能不能補償 |
| --- | --- | --- |
| **白底 ＋ 深綠標誌** | 標誌被**拉亮** | **可以** —— 反推後把檔案調深，實測 ΔE 9.2 → 0.0 |
| 套色底 ＋ 白標誌 | 標誌被**拉暗 16%** | **不行** —— 白已經是 255，沒有往上補的空間 |

**所以最後選白底，不是因為它比較銳利（它不是，30.5% vs 47.5%），
而是因為它的扭曲可以量、可以補，而且已經補準了。**

### 第十一輪：牙洞放大 ×1.18 ＋ 上移 12（2026-08-12 定案上線）

使用者：「牙洞感覺可以調一下，放大＋上移。」四案實測（牙洞在手機上的實際寬度，
標誌在 180px 圖示上是 136.8px 寬）：

| | 放大 | 上移 | 手機上的洞寬 |
| --- | --- | --- | --- |
| 現況 | ×1.00 | — | 11.1px |
| **Ⓐ（定案）** | **×1.18** | **12** | **13.1px** |
| Ⓑ | ×1.34 | 24 | 14.8px |
| Ⓒ | ×1.50 | 36 | 16.6px |
| Ⓓ | ×1.66 | 48 | 18.4px |

> ⚠⚠ **這一輪推翻了 `icon.svg` 原本那句「主畫面最小 180px，牙洞不必放大」。**
> 那句話沒有把 iOS 的柔化算進去：柔化約 4~5px，而原始比例的洞只有 11.1px 寬，
> 兩側各被吃掉一截就糊成一個淺色斑點。
>
> ⚠ **和 favicon 放大 1.6 倍的理由不同**：favicon 對抗的是 16px 下的抗鋸齒，
> 這裡對抗的是 iOS 的柔化。**兩邊的倍率不必一致，不要為了「統一」去改。**
> （Ⓓ 的 ×1.66 剛好等於 favicon 的比例，但那是巧合，不是選它的理由，而且它落選了。）
>
> ⚠ 上移量跟著放大等比走 —— 洞變大本來就會往下長，不上移會顯得更低。

### 收尾（2026-08-12）

三個對照頁（`icon-test`／`-b`／`-c`）與兩張實驗圖示（`icon-192-alpha.png`、
`icon-192-solid.png`）都已刪除，推導收進 `/history/icon-homescreen-blur.html`。
`tools/app-icons.mjs` 的 `alpha` / `swap` 支援程式碼留著沒刪，日後要再做實驗
加一筆 TARGET 就會動。

**正式站最終狀態：**

| | |
| --- | --- |
| `<link rel="apple-touch-icon" sizes="192x192" href="/assets/icon-192.png?v=…">` | 全站唯一的 any 版候選 |
| 底色 | 純白 `#ffffff` |
| 標誌 | 品牌綠 `#3f654a`（檔案裡是補償值 `#205533`） |
| 牙洞 | ×1.18、上移 12 |
| manifest | `display: "browser"`、192 any ＋ 512 maskable |

---

## 六之二十一、位置與周邊停車的地圖（2026-08-14 定案上線）

首頁「診所資訊」的第一張卡。四十二輪的完整推導在
[/history/clinic-simple-map.html](https://fangren.net/history/clinic-simple-map.html)，
這裡只留**顏色的結論與不能違反的約束**。

### 這張圖只用了一顆新色

| 用途 | 色值 | 來源 |
| --- | --- | --- |
| 路面 | `--rule` `#cdd0d2` | 既有 |
| 街廓 | `--card` `#f4f4f5` | 既有 |
| 街名 | `--ink-soft` `#5c5f57`，700 | 既有 |
| 診所色塊、店名、圖釘 | `#3f654a` | 一般牙科的套色（第六之十一節第七小節） |
| 停車場・平常 | `#7a7d77`＝柔墨混 80% 卡色 | 既有色推出來的 |
| **停車場・點到** | **`#365685`（路牌藍）** | **這一輪唯一的新色** |

**路牌藍 `#365685`，L\* 36.2** —— 亮度最接近台灣路牌的 `#0058a8`（L\* 37.5），
彩度收過一階，才站得住紙色底。使用者從五階裡挑的，落選：
地磚深藍 `#252d4a`（太深）、深藍二 `#2d4065`、中藍 `#314b75`、
矯正藍 `#4478b5`（太淺，而且點到看不出來）。

### 三條不要違反的規則

1. **藍是「點到」的意思，不是「停車場」的意思。**
   三塊平常都上藍的話，藍就只剩深淺兩階可以表達點沒點到 ——
   實測那條路是死的（越淺兩態落差越小，2.31 → 1.49）。
   現在點下去是**換色相**（灰 → 藍），完全不靠亮度，
   所以兩態的亮度幾乎一樣（39.8 vs 36.2，對比只有 1.14），整塊不會忽明忽暗。
2. **停車場平常那一階不能再更淡。** 白色的 P 站在上面是 4.18:1，
   下限（大字粗體）是 3:1；混到 70% 只剩 3.41、65% 只剩 3.10、60% 就破線。
   要更淡就得把 P 改成深色，那和「像路上的停車標誌那樣反白」衝突。
3. **診所是主角，停車場是第二層。** 足額柔墨 `#5c5f57` 對路面 4.20:1、
   診所那塊綠 4.27:1 —— 一樣重，而灰有三塊、綠只有一塊，灰反而贏。
   退到 80% 之後對路面 2.70:1 才分得出主次。
   ⚠ 這件事和「診所掛一塊實心綠名牌」是**一組的**：
   只調灰不掛名牌，圖上會變成三塊淡灰加一塊綠、誰都不突出。

### 牌子（點到之後那塊對話框）

淺底藍字：底 ＝ 路牌藍 10% 混紙卡色、外框與**空心的三角**都是足額路牌藍、
字與圖釘與底線也是路牌藍（對那個淺底 5.84:1）。
**診所那一側反過來是實心綠底白字** —— 一深一淺才分得出
「這是目的地」與「這是可以選的停車場」。

> ### ⚠ 2026-08-21：診所那一塊換成「綠塊寫字 ＋ 站在上面的圖釘」
>
> 原本是「綠塊裡放白色標誌 ＋ 底下一塊綠底白字的對話框（`.clab`）」，
> 使用者拿 cizoo.com 的截圖指定改成圖釘：**標誌搬進圖釘頭上的白圈、
> 綠塊直接寫「芳仁牙醫」（白字 ＋ 底線，仍然是連到 Google 地圖的連結）**，
> 對話框整塊刪掉。十輪的推導在
> [/history/clinic-map-pin.html](https://fangren.net/history/clinic-map-pin.html)。
>
> **顏色一顆都沒有新增**，只是換了位置：
>
> | | |
> | --- | --- |
> | 釘身、綠塊、圈裡的標誌 | `#3f654a`（一般牙科的套色） |
> | 圈、綠塊上的字與底線 | `--card` 紙卡色 —— **同一塊綠上仍然只有一個白** |
> | 影子 | `--ink` `#2a2c27`，十六層每層 α .026（＝站上卡片陰影那一支墨） |
>
> ⚠ **白字對綠 6.02:1、標誌對釘身 6.02:1**，都遠高於門檻。
> ⚠ 上面那一句「一深一淺才分得出目的地與停車場」**仍然成立**：
>   診所現在是實心綠塊 ＋ 一顆立起來的綠圖釘，停車場的牌子還是淺底藍字。
> ⚠ 影子那一層是**這張圖上第一次出現的墨色**（在此之前整張圖只有路面、街廓、
>   街名、綠、藍五種）。它很淡（最深讓底下暗 71 階、覆蓋約 142 CSS px²），
>   而且只出現在圖釘右上那一小片 —— 要調整先回頭讀那一頁的量測。

⚠ 停車場牌子上那顆小圖釘是**頁尾地址前面那一顆**（Font Awesome 的 map-marker，
384×512，實心），不是 HERO 窄帶那顆（Lucide，24 格見方，描邊不填色）。這一站兩顆都在用。
⚠⚠ **診所那顆大圖釘是第三顆，而且是算出來的不是引用的**（2026-08-21）——
頭的半徑、腰身、尖端的圓角三個參數現算成一條路徑（見上面那一段與
[/history/clinic-map-pin.html](https://fangren.net/history/clinic-map-pin.html)）。
不要拿 Font Awesome 那顆去代替：那顆的頭與釘子是相切的水滴，
使用者這一輪要的正好是「圓和釘子看得出分界」。

⚠ 停車場的 **P 自己指定 Arial／Helvetica ＋ 700**，不吃全站的中文字型堆疊 ——
同一個 `font-weight: 800` 在 PingFang TC 上落在 Semibold、在微軟正黑上會**合成假粗體**，
兩邊粗細差很多。加粗靠的是 `paint-order: stroke` 的同色描邊，不是字重。
