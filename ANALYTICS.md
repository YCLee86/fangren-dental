# 流量分析 — Google Search Console 接線說明

這份是「怎麼讓腳本自己去 Google Search Console（以下簡稱 GSC）抓資料」的設定步驟。
人做的部分（在 Google 的兩個後台點一點）寫在這裡，程式的部分之後由 `tools/gsc-fetch.mjs` 負責。

**2026-08-16 建立。目前狀態：步驟已寫好，還沒有人去點，金鑰也還沒建。**

> 這份文件**不會**進到 fangren.net —— `tools/dist.mjs` 用白名單複製檔案，
> 根目錄的 `.md` 一律不打包（`README.md`／`PALETTE.md`／`COPY.md` 也一樣）。

---

## 為什麼要接 GSC

站上已經有 D1 計數器（窄帶那個數字、文章卡上的「N 次瀏覽」），但那兩者量的東西不一樣：

| | 量的是什麼 | 資料在哪 |
| --- | --- | --- |
| **D1 計數器**（已經有了） | 人**進來之後**：哪一篇被看幾次 | Cloudflare D1 |
| **GSC**（這份要接的） | 人**進來之前**：搜什麼字、曝光幾次、點了沒、排第幾名 | Google |

「有人搜『斗六 牙周病』看到我們但沒點」這種事，只有 GSC 知道。
兩份合起來才看得出「哪些查詢帶得進人、帶進來之後有沒有人讀完」。

---

## 零、先確認一件事：你登錄的是哪一種資源

這件事**會決定程式裡那個網址怎麼寫**，弄錯會一直回 403。

打開 GSC，看左上角資源選單裡 `fangren.net` 那一筆長什麼樣：

| 你看到的 | 叫做 | 程式裡要寫 |
| --- | --- | --- |
| 只有 `fangren.net`（沒有 `https://`） | **網域資源** | `sc-domain:fangren.net` |
| `https://fangren.net/`（有協定、結尾有斜線） | **網址前置字元資源** | `https://fangren.net/` |

⚠ 兩種都能用，不必改。**但要告訴我是哪一種**，或者做完第四節那個驗證，
腳本會自己把你有權限的資源印出來，就不必猜了。

⚠ 如果你當初是用 **DNS TXT 記錄**（在 Cloudflare 加一筆）驗證的，那幾乎一定是**網域資源**。
站上目前**沒有** `google-site-verification` 的 meta 標籤，所以不會是「HTML 標記」那種驗證方式。

---

## 一、Google Cloud 那一半

網址：<https://console.cloud.google.com/>
用**你自己那個能開 GSC 的 Google 帳號**登入（eugenelee0806@gmail.com）。

### 1. 建一個專案

左上角專案選單 → 「新增專案」。名字隨便取，`fangren-analytics` 之類的就好。
建好之後**確認左上角切到這個新專案**（很容易忘，後面的步驟都會做到錯的專案去）。

### 2. 啟用 Search Console API

搜尋列打 `Search Console API` → 點進去 → 按「**啟用**」。

⚠ 要找的是「**Search Console API**」。清單裡還有一個「Google Search Console API」的舊條目、
以及完全無關的「Custom Search API」（那是站內搜尋，不是這個）。認 `searchconsole.googleapis.com` 這個名字最準。

### 3. 建服務帳號

左邊選單 → 「IAM 與管理」→「**服務帳號**」→「建立服務帳號」。

- **名稱**：`gsc-reader`（隨便取）
- **授予角色那一步：直接跳過，不要給任何角色。**
  ⚠ 這裡的角色是「這個帳號能碰 Google Cloud 的什麼」，和 GSC 完全無關。
  GSC 的權限是在第二節另外給的。給了也沒用，只是多開一個洞。

建好之後，清單上會出現一個 email，長這樣：

```
gsc-reader@fangren-analytics-123456.iam.gserviceaccount.com
```

**把這串複製起來，第二節要用。**

### 4. 產生金鑰

點進剛建好的那個服務帳號 → 上面的「**金鑰**」分頁 → 「新增金鑰」→「建立新的金鑰」
→ 選 **JSON** → 建立。

瀏覽器會直接下載一個 `.json` 檔。**這個檔案就是密碼本身**，下一節講怎麼保管。

⚠ 這個檔**只會給你一次**，Google 不留副本。掉了就回來這一頁再建一把新的（舊的可以刪掉）。

---

## 二、Search Console 那一半（最容易漏掉的一步）

**做完第一節，程式還是進不去。** 服務帳號現在只是一個「存在的身分」，
它對你的網站沒有任何權限 —— 要在 GSC 這邊把它請進來。

網址：<https://search.google.com/search-console>

1. 左上角切到 `fangren.net` 那個資源
2. 左邊選單最底下 →「**設定**」
3. 「**使用者和權限**」
4. 右上角「**新增使用者**」
5. **email 貼上第一節第 3 步那串 `...iam.gserviceaccount.com`**
6. 權限選「**受限**」

⚠ **「受限」就夠了。** 讀成效報表（我們要的那些數字）只需要這一階。
「完整」還能改設定、加使用者、送出移除網址的要求 —— 一支只負責讀數字的腳本不需要那些。

⚠ 貼 email 的時候 Google **不會**寄邀請信、也不會有人按「接受」——
服務帳號沒有信箱。貼下去按確定，清單上直接出現就是成功了。

---

## 三、金鑰放哪（這一節是紅線）

> ### ⚠⚠ 這個 repo 是 **public** 的。
> 金鑰 JSON **絕對不能 commit**。那個檔案裡有一段 `"private_key": "-----BEGIN PRIVATE KEY-----..."`，
> 任何人 clone 下來就能用你的身分讀你的 Search Console。
> GitHub 的自動掃描通常會在幾分鐘內偵測到並通知 Google 撤銷，但**那幾分鐘就已經外洩了**。

`.gitignore` 已經加上這幾條擋著（`gsc-key*.json`、`*-service-account*.json`、`secrets/`），
但那只是保險，不是許可 —— 不要去試探它擋不擋得住。

### 那到底要放哪？

看你在哪台機器上跑：

**在你自己的電腦上（建議）**
把 JSON 放在 repo **外面**，例如 `文件\fangren-金鑰\gsc-key.json`，
然後跟我說路徑。放在 repo 外面就完全沒有誤 commit 的可能。

**在手機／雲端 session（claude.ai/code）**
⚠ 這種 session 的容器是**用完就丟**的，關掉就沒了，沒辦法把金鑰存著給下次用。
所以流程是：**每次要抓資料的時候，把 JSON 的內容貼進對話**，
我寫進暫存資料夾（不是 repo）、抓完資料、session 結束時自動消失。
抓回來的**數字**會 commit（那不是秘密），**金鑰不會**。

如果覺得每次貼很麻煩，那就固定在你自己的電腦上跑這件事。

---

## 四、怎麼確認接通了

設定完之後，把 JSON 給我（照上一節的方式），我會先跑一個最小的呼叫：

```
GET https://searchconsole.googleapis.com/webmasters/v3/sites
```

它會回傳「這個身分看得到哪些資源」。三種結果：

| 回什麼 | 代表 | 怎麼修 |
| --- | --- | --- |
| 列出 `sc-domain:fangren.net`（或 `https://fangren.net/`） | ✅ 成功，第零節那題也順便答完了 | — |
| `{"siteEntry": []}` 空的 | 第一節做完了，**第二節漏了** | 回第二節把服務帳號加進使用者名單 |
| 403 `Search Console API has not been used...` | **第一節第 2 步的 API 沒啟用**，或啟用在別的專案 | 回第一節第 2 步，確認左上角專案是對的 |
| 401 `Invalid Credentials` | JSON 貼壞了、或金鑰已被撤銷 | 回第一節第 4 步重建一把 |

---

## 五、接通之後我會做什麼

寫 `tools/gsc-fetch.mjs`，**維持這個專案零 npm 依賴的原則** ——
服務帳號的認證要自己簽一個 JWT 再換 access token，Node 內建的 `crypto` 就做得到 RS256，
不需要 `googleapis` 那包（那包會裝進來幾十個依賴）。

抓回來的資料存成 JSON／CSV 進版控，之後要看趨勢就有歷史可比。

能抓的維度（可以組合）：

- `query` — 人家搜了什麼字
- `page` — 落在哪一頁（可以對到六篇文章）
- `date` — 每天
- `device` — 手機／電腦／平板
- `country` — 國家
- `searchAppearance` — 出現在哪種搜尋結果裡

每筆都有四個數字：**曝光、點擊、點閱率、平均排名**。

---

## 六、已知限制（先知道，免得之後覺得資料怪怪的）

1. **資料會延遲 2～3 天。** 今天抓不到今天，最新的通常是前天。這是 Google 那邊的處理時間，不是壞掉。
2. **只有 16 個月。** 更早的 Google 自己也不留了 —— 所以愈早開始定期抓存下來愈好，
   存進 repo 之後就變成我們自己的歷史資料，不受這條限制。
3. **查詢字會被抽掉一部分。** 搜尋量太少的字詞 Google 為了隱私不給（避免反推出個人）。
   所以「查詢字那一份的點擊總和」會**小於**「總點擊數」，這是正常的，不是漏抓。
4. **單次呼叫最多 25000 列**，超過要分頁（`startRow`）。腳本會自己處理。
   ⚠ 這比網頁後台「匯出」的 1000 列上限寬鬆很多，這也是走 API 的主要好處之一。
5. **免費。** 配額對這個規模的網站等於用不完，不會產生 Google Cloud 費用。
   ⚠ 但建專案的時候 Google 可能會問要不要綁信用卡 —— **這個 API 不需要**，可以跳過。

---

## 七、順帶一提：舊站還活著

`https://yclee86.github.io/fangren-dental/` 目前還在（見 CLAUDE.md 第七節）。
如果那個網址也被登錄成 GSC 資源，它會是**另一份獨立的資料**，
而且兩邊內容一樣、會互相稀釋搜尋排名。

**這件事還沒查過**，等第四節那個驗證跑起來、看到完整的資源清單就知道了。
真的有兩份的話要處理轉址，那是另一件事。
